/**
 * Shared constants for the in-CRM storage system (v2, desktop).
 */

export const CRM_STORAGE_BUCKET = "crm-files"
export const MAX_UPLOAD_BYTES = 100 * 1024 * 1024 // matches the existing Drive upload ceiling
