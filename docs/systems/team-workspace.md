# Team Workspace (internal Slack-replacement chat)

_Last verified against code: 2026-09-05 — Claude (**A `team_chat_send` DM dictated on someone's behalf was permanently invisible to the very person who dictated it.** Antonio asked Claude to relay a message to Luca via the MCP tool's `dm_user_id` path; the tool reported success and the message was genuinely in the database, correctly addressed — but Antonio could not find it anywhere in his own Team Workspace, even after refreshing. Root cause (bug-hunter, full repo pass): `postTeamMessage`'s DM branch (`lib/team/post-message.ts`) always resolved the thread via `findOrCreateDm(CLAUDE_SENDER_UUID, target)` — keyed to Claude's own sentinel identity, never to the real acting human — regardless of who actually dictated the send (that person was only ever stamped onto the MESSAGE row's `on_behalf_of_user_id`, never folded into the THREAD's own `dm_key`). Every visibility surface (the `get_team_threads`/`search_team_messages` RPCs, the floating widget's own `myDmThreads()`, the main Team Chat page's Direct Messages list) gates on "does `dm_key` contain MY id" — so the dictating human's own id being absent made the conversation unreachable to them via any listing or search, from its very first message, for as long as the on-behalf-of feature has existed (`20260729-1900-team-chat-on-behalf-of.sql` only ever touched unread/mention COUNT queries, never this underlying thread-key model). The recipient could see it fine the whole time — this was invisible only to the one person who asked for it to be sent.
**Fix:** `resolveTargetThread` now takes the already-resolved `actingUserId` (moved to compute BEFORE thread resolution, not after) and keys a `dm_user_id` send to that real person instead of the sentinel whenever one is known, falling back to the sentinel only for a genuinely autonomous Claude-initiated DM with nobody dictating it. **Second bug the same review caught before it could ship:** the push-notification code that finds "the other DM participant" filtered out only the Claude sentinel — correct when `dm_key` was always `Claude:target`, but once a dictated DM can be keyed `actingUser:target` instead, neither half is the sentinel, so the old filter would have handed the push to the ACTOR (who dictated the message) instead of the real recipient. Fixed by extracting `otherDmParty` (`lib/team/workspace.ts`, pure, unit-tested) to exclude every known non-recipient identity at once — the sentinel AND the acting user — correct for the old shape, the new shape, and the self-DM case alike. **Not touched, flagged only:** existing sentinel-keyed threads created before this fix (like the one from this exact incident) stay exactly as they are — a live conversation the dictating human still cannot find through normal navigation, recoverable only by resending with an explicit `thread_id`, as was done to recover Antonio's own message. A bulk repair of historical on-behalf-of threads is a separate, deliberately out-of-scope decision — this fix is forward-looking only.
**Adversarial pass before shipping (bug-hunter, full repo sweep of every `dm_key`/sender-identity consumer in the team-chat system): 0 blockers, 0 majors.** Confirmed no other read/list/push/label surface makes the same broken assumption; confirmed the reorder is a no-op for channel/thread_id/bug-reply sends; confirmed a self-dictated DM, an already-existing old thread receiving a future message from a different actor, and `on_behalf_of` impersonation all behave correctly (the acting-user id is always server-derived from the caller's own authenticated identity — the MCP tool and the worker executor never accept it as a raw model-supplied parameter). Two minor items, one fixed here: the fix's core line (which id gets keyed) had no direct test — added `tests/unit/team-post-message-dm-keying.test.ts`, mocked at the `findOrCreateDm`/directory boundary rather than the raw Supabase client, pinning the real-actor case, the email-resolution case, the autonomous-Claude fallback, the unresolvable-actor fallback, and the self-DM case. Left as accepted, not fixed: resolving the acting user now runs even on a request that's about to fail with "target not found" (one extra directory-listing API call on an already-erroring path — cosmetic, not a correctness issue). Also flagged, confirmed NOT live: `lib/ai-agent/slack-claude.ts`'s Slack-event handler sets up `team_chat_send` without ever building an acting-user context, which would reproduce this exact bug if it ever ran — but it has no caller anywhere in the app (confirmed dead code, matching an existing audit doc) and is deliberately left alone rather than "fixed" inside an unrelated change; worth remembering if that surface is ever revived.)_
_Prior: 2026-09-05 — Claude (**"Mark done" — closes the ONE gap the three-round `ever_mentioned` fix below named on the same day: a per-user way to clear a handled mention from the floating chat's client list, without resolving/archiving the whole shared conversation for everyone else.** Antonio, immediately after confirming round three fixed the noise: "in the floating chat, after reading a message I want the option to mark it done and disappear from the list" — confirmed directly it should REAPPEAR on a fresh mention, not hide forever, and that the action belongs inside the opened conversation (not a row-level icon on the collapsed list).
Built as its OWN sparse table, `internal_thread_mention_dismissals` (thread_id, user_id, dismissed_at) — deliberately NOT a reuse of `internal_thread_reads` (its epoch-seeding is the exact bug that broke round one, above) or `internal_thread_later` (presence-only, explicitly documented as orthogonal to read/mention state; here the TIMESTAMP is load-bearing, since a mention only counts if it is newer than the caller's own dismissal). `POST /api/team/threads/[id]/dismiss-mention` upserts the caller's own stamp forward; `app/api/team/threads/route.ts`'s `ever_mentioned` computation now compares each mention's `created_at` against it via a new pure, unit-tested function (`everMentionedThreadIds` in `lib/team/chat-window-threads.ts`) — extracted specifically so this exact class of timestamp comparison, which has now caused real bugs twice in one file in one day, is tested directly rather than trusted inline. The button (`components/team-chat/floating-chat.tsx`, both `DesktopWindow` and `MobileSheet` headers) renders only when `thread_type === 'discussion' && ever_mentioned === true`, mirroring the existing header icon pattern.
**Caught before shipping, not by a reviewer but by the build itself:** the first version iterated the returned `Set` directly in a `for...of` inside the route, which this project's TypeScript target rejects without `downlevelIteration` — fixed by wrapping in `Array.from()`. A reminder that `npm run build`, not just unit tests, is part of "done" for any new Set/Map-returning helper.
**Verified live in sandbox, both directions, not just the happy path:** seeded a mention (thread appears) → clicked Mark done (network call 200s, thread disappears, confirmed via a direct DOM/network check after a `find`-vs-`computer`-coordinate-frame mismatch made a naive click land on one of 134 unrelated pre-existing "Mark done" buttons elsewhere on the same dashboard — a per-card action on the unrelated To-Do board that happens to share the exact same label) → seeded a SECOND mention dated after the dismissal → thread reappears. Test data cleaned up from sandbox afterward.
**A bug-hunter pass BEFORE shipping (requested by precedent, not asked again — Antonio's explicit "run the bug hunter" from round three was treated as standing guidance for this same-day follow-up too) found and closed 2 real issues:** (1) [major] the two lookups in `app/api/team/threads/route.ts` (mentions, dismissals) ran as one `Promise.all` but were graded asymmetrically on failure — a transient error on the MENTIONS query correctly failed closed (nothing shows), but the identical failure shape on the DISMISSALS query silently read as "nobody has ever dismissed anything," which would have flooded every already-cleared conversation straight back onto the list — reproducing the exact noise complaint that took three rounds to fix, via ordinary Supabase flakiness rather than a bad filter. Fixed: the comparison only runs when BOTH queries actually succeeded; either one failing leaves the set empty for that request, matching the same fail-safe already applied to a total lookup failure. (2) [major] the dismissal was stamped with the SERVER's clock when the request happened to be processed, not the moment the button was actually clicked — ordinary request latency was enough room for a mention sent moments before the click to still lose the timestamp race and be silently swallowed. Fixed: the client now sends its own clock reading from the instant of the click (`resolveDismissedAt`, `lib/team/chat-window-threads.ts`, unit-tested), used only if it parses and isn't in the future; a genuinely simultaneous human collision on the exact same thread is still possible and self-heals on the next mention — accepted, not engineered around further. Also fixed a minor gap the same pass found: a failed "Mark done" request previously failed silently (no error shown, matching the passive `markRead` ping it was modelled on) — now surfaces the same way a failed send does (R099), since this is a deliberate button click, not a background ping. **Confirmed pre-existing, not a regression, not fixed here:** the new table's RLS policy is a byte-for-byte copy of `internal_thread_later`'s own (role-gated, not row-owner-scoped) — today's actual safety is the route hardcoding the session's own user id, never the DB policy, since both call sites use the service-role client; flagged for awareness, matches existing convention, not this change's to fix alone. Production ship pending the same surgical process as the fix below.)_
_Prior: 2026-09-05 — Claude (**THE FLOATING CHAT'S CLIENT LIST TOOK THREE ATTEMPTS TO FIX IN ONE DAY. Read all three rounds before touching `openConversations`'s filter in `lib/team/chat-window-threads.ts` again — the WRONG two both looked correct, matched real precedent, and passed every test written for them.**
Antonio, live in production (his own screenshot): "plus I have this floating message where I receive a lot messages that are not related to me and I can't use it" → "I don't want to have all that conversations in the floating. it's noise."
**ROUND ONE (shipped, wrong):** `openConversations` (the floating chat's own client quick-list, `components/team-chat/floating-chat.tsx`) filtered on `is_participant` — a field the RPC (`get_team_threads`) already returns, and already the correct, established check three OTHER consumers of the same rows use (`lib/team/workspace.ts`'s two notification builders, `realtime-notifications.tsx`'s toast filter). Looked right, matched precedent, passed unit tests, verified live in sandbox. **Antonio checked his real screen minutes later — unchanged.** Root cause, found by querying PRODUCTION directly (sandbox's fixture data was too small to expose this): `is_participant` is TRUE the instant a row exists in `internal_thread_reads`, and TWO existing, intentional code paths (`lib/team/find-conversation.ts`'s `findOrCreateConversation`, on every brand-new client conversation; `app/api/team/share/route.ts`'s admin-notify fallback) deliberately seed EVERY other staff member with such a row at the EPOCH (`1970-01-01`) so a notification dot fires once. `is_participant` was true on 120 of Antonio's 122 live discussion threads — barely a filter.
**ROUND TWO (shipped, also wrong):** a stricter, genuine (non-epoch) `last_read_at` check — real engagement, not just a row's existence. Correctly narrowed 122 → 47. Verified against production data before shipping this time. STILL didn't match his screen. Asked him directly why: "I don't read them at all unless i have been mentioned. but they are messy because most of them are luca or claude conversation about the clients." Checked a sample of the 47 the second fix let through: **zero messages actually SENT by him in any of them.** A genuine `last_read_at` only proves he once opened a thread to check on it (a real habit — "I have to know everything") — not that it means anything to him day to day.
**ROUND THREE (this fix):** `app/api/team/threads/route.ts` now computes `ever_mentioned` — has an `internal_messages` row in this thread ever carried him in `mentioned_user_ids`, regardless of read state — and `openConversations` filters on that instead. Verified against production before shipping: 122 discussion threads → 1. A 5-question adversarial pass (bug-hunter, requested explicitly by Antonio after two misses) confirmed the query mechanism is sound (`mentioned_user_ids` is written consistently by `resolveMentions()` across all 3 send paths; the `.contains()` pattern is already proven elsewhere in `staff-alerts/route.ts`) and found one real, accepted, DOCUMENTED-NOT-HIDDEN gap: **there is no per-user way to dismiss a single stale mention from this list** — `mentioned_user_ids` is never touched by a message edit (`app/api/team/messages/[id]/route.ts` doesn't call `resolveMentions` on PATCH), and the only exits from the list are the THREAD-level `resolved_at`/`archived_at` fields, which remove it for every other viewer too. A wrong or long-since-handled mention can only be practically retired by resolving/archiving the whole shared conversation. At today's real count (1) this doesn't bite; it is the one way this exact complaint could quietly re-accumulate over months, named here on purpose rather than discovered a fourth time.
**The lesson, stated so the next session doesn't re-learn it three times:** "does a row exist," "did this person do something once," and "does this actually mean anything to them today" are three different, all-plausible-sounding definitions of "mine," and the schema will happily say yes to the first two while the person means the third. A definition that matches an established precedent and passes a green test suite proves the CODE executes as designed — it does not prove the DATA in production has the shape assumed, and it does not substitute for asking the person what they actually meant. The fix that finally mattered was the one verified by re-deriving its exact number from a live production query and confirmed by directly asking Antonio which definition matched his own words — not the one that merely compiled and passed.)_

_Prior: 2026-09-04 — Claude (**STAFF ALERTS GAINED A CHAT HALF — this subsystem is now READ by a second surface, new `lib/team/chat-alerts.ts` (dev job `b1fb9705`), which the Staff Alerts bell (see `docs/systems/staff-notes.md`) merges alongside its own note alerts (DMs, @mentions, work-channel posts alongside note replies/shares). Documented HERE, not just there, because getting this subsystem's own semantics wrong would have shipped an alert feed that silently misses ordinary channel chatter — which the first draft of this code did, twice, both caught by a 5-reviewer council pass (senior-engineer, ai-architect, bug-hunter, project-director, system-counselor) before this ever reached sandbox QA.**
**`get_team_threads`'s `unread_count` means something DIFFERENT depending on `thread_type` — verified against the LIVE function body on BOTH sandbox (a one-off node+pg script; `psql` unavailable in this environment) AND production (system-counselor's own live read), not assumed from the migration history.** For `dm` it is a plain count of messages after `last_read_at`, including a manual "Mark unread" via the RPC's own `GREATEST` — trustworthy, and already what `lib/team/chat-window-threads.ts`'s own floating-chat badge leans on (reused as-is here too, no second copy of that number). For `channel` it is NOT that: it counts root messages promoted into the Threads panel (a title, an assignee, a non-todo status, or a reply — see the RPC body under `get_team_threads`), which an ordinary un-promoted top-level post never satisfies. Confirmed via the actual send path (`app/api/team/threads/[id]/messages/route.ts`): a brand-new channel post writes ONLY the `internal_messages` row, never an `internal_thread_state` row. Trusting `unread_count` for channel alerts would have silently missed plain channel chatter — probably the MOST common channel activity there is, and exactly the failure shape (a real, common case the alert never fires for) that already burned the notes half of this same feature once (see `docs/systems/staff-notes.md` 2026-09-04c). Fixed by computing channel activity from raw `internal_messages` instead, mirroring `realtime-notifications.tsx`'s own toast skip rules exactly (own messages, `on_behalf_of_user_id === me`, `deleted_at` set, pending placeholder bodies `['…','⋯']`) — the one thing this file's own doctrine already insists on (`channelNotifiesStaff`'s doc comment): the phone/toast and any other surface reading "does this notify" must use the SAME predicate, never a second copy that can silently disagree. A manual "Mark unread" (`app/api/team/threads/[id]/mark-unread/route.ts`, real and reachable from the sidebar) carries no message of its own, so it's honored separately: no fresh message found but the read pointer's own `manual_unread` flag is set → alert using the RPC's thread-level preview, matching what Team Chat's own sidebar would show for that thread right now.
**`general` is EXCLUDED from plain-unread alerting entirely — mention-only, matching an existing, deliberate product decision.** `app/api/team/notifications/route.ts`'s own comment states it plainly: "general is deliberately mention-only" — the sidebar dot, its dropdown, and the dashboard badge all already treat plain general chatter as not notification-worthy. An earlier draft of this code silently reversed that precedent by raw-recomputing general the same as channel; caught in council review (ai-architect) before it ever reached sandbox. General still alerts on a genuine `@mention`, same as every other thread type.
**Mentions are MESSAGE-grain, not thread-grain, and computed from raw messages too — NOT from `mention_count`.** The RPC's own `last_message`/`last_sender_name`/`last_message_at` are the thread's overall latest row regardless of whether THAT message is the one that mentions you (its LATERAL join carries no mention filter) — trusting them for a mention alert can show the WRONG content: a mention followed by an unrelated reply before the alert is next checked would read "X mentioned you" with body text that never mentioned you, and a stale timestamp (caught in council review, senior-engineer). Fixed: a targeted query for messages where `mentioned_user_ids` contains the viewer, filtered through the SAME `latestQualifyingActivity` skip rules, feeding the alert its own actual sender/body/timestamp. Mentions win priority over a plain DM/channel alert on the SAME thread — never two alerts for one thread, mirroring the toast's own mention > DM > channel ordering.
**One alert per thread is a real, accepted loss of granularity, stated not hidden (ai-architect):** dismissing — or even just opening — a thread's alert advances its read pointer for the WHOLE thread, same as Team Chat's own "mark read" already does, so several unread messages collapse to one alert and acting on it clears all of them. A dedicated per-message dismissal table would close this fully but is disproportionate for a 2-person tool; instead, when more than one qualifying item sits behind the shown preview, the body says so ("+N more"). One named residual gap this does NOT close (bug-hunter): "+N more" only counts items of the SAME kind, so a thread with an unread mention AND a separate, older, unrelated plain post shows only the mention with no count reflecting the plain post — dismissing the mention means that plain post can never surface as its own alert afterward. Nothing is actually lost (the message is still fully visible by opening the thread), only its unread flag; full detail and reasoning in `lib/team/chat-alerts.ts`'s own header comment.
**A bare row-count `LIMIT` on the channel-messages query is not enough at this system's REAL volume, verified against production (system-counselor): one channel alone (`td-taxreturn`) carries hundreds of messages all-time.** A combined, unbounded-below `LIMIT` lets old, already-read history from a busy channel crowd out a genuinely unread message in a quiet one — permanently, not just for one refresh (bug-hunter). Fixed: `earliestPossibleUnreadMs` (`lib/team/chat-alerts.ts`) computes the OLDEST read pointer among the relevant channel threads and the query never looks further back than that — nothing older than the earliest possible unread point can matter. Falls back to an unbounded query (LIMIT alone, raised to 500 for headroom) only when at least one relevant thread has no read-pointer row at all (never opened by this person), which by construction means that thread's own history is small.
**Dismissing a chat alert IS marking the thread read — literally the same write `POST /api/team/threads/[id]/read` makes**, now via one shared helper (`lib/team/mark-thread-read.ts::markThreadRead`) both routes call instead of two independently hand-rolled copies of the same upsert (ai-architect: this diff already applied that discipline to one shared helper — `parsedMs` — inconsistently skipping it here; fixed to match).
**Opening a chat alert can leave the bell showing stale state — a real bug found in live sandbox QA, not by the council:** the floating chat's own `markRead` and the full team-chat page's default read-on-open both advance the SAME read pointer this feature computes alerts from, but neither knows the Staff Alerts query exists to invalidate it — clicking Open marked the thread read server-side while the bell kept showing the alert and the red badge. Fixed two ways: `openAlert` now best-effort invalidates the Staff Alerts query shortly after a chat open (timed after the mark-read write has had a moment to land), and the query itself now polls every 60s as the actual guarantee regardless of timing, matching the interval `sticky-notes-layer.tsx` and `floating-chat.tsx` already use for the same reason.
**A second, separate live bug found in the same QA pass:** clicking a chat alert's Open button while already sitting on the `/team-chat` page updated the URL (`?thread=<id>`) but did not actually select that thread — the page's own deep-link effect didn't pick up a same-page client-side navigation, reading as a dead click. Verified live that a full page load of the identical URL selects correctly. Fixed in `staff-alerts-bell.tsx`'s `openAlert`: the navigation fallback does a hard `window.location.href` specifically when the target page is the one already open, leaving the in-place-open and cross-page-navigation cases (both proven working live) untouched.
**Read-only consumer — nothing in this subsystem's own write paths, RPC, or schema changed** beyond the one shared read-helper extraction above. The muted channel list (`SILENT_CHANNEL_SLUGS`) is called by name (`channelNotifiesStaff`), never re-listed. 35 unit tests in `tests/unit/team-chat-alerts.test.ts`.)_

_Prior: 2026-09-03 — Claude (**`team_chat_send` gained real file attachments — was text-link only.** The MCP tool (Antonio, live use: "you have to attach the file, not send a link") and its shared choke-point `postTeamMessage()` (`lib/team/post-message.ts`) could only post plain text, so a file had to be pasted into the body as a raw link — no attachment chip, unlike the client-facing portal chat. Root cause: the human send route (`POST /api/team/threads/[id]/messages`) already supported attachments end to end; `postTeamMessage` is a SEPARATE choke-point (mirrors that route but is not the same code path) and never threaded `attachments` through to the `internal_messages` insert at all. Fixed: `PostTeamMessageInput` gained `attachments` (same `ChatAttachment` shape portal chat uses: url/name/mime_type?/size?), inserted into `internal_messages.attachments`; new pure `validateTeamPostAttachments` (`lib/team/post-message-validate.ts`) mirrors the human route's own guard — every attachment url must already be hosted on our own Storage, never an arbitrary off-site link. This also became available to the AI worker's own identical `team_chat_send` tool (`lib/ai-agent/worker-tools.ts`), since both call the same choke-point. **Not built here — a real gap, not a decision:** no team-chat equivalent of `portal_chat_attach_file` exists to land a file on Storage in the first place; callers reuse that tool today (its name says "portal" but the copy-into-Storage mechanism is generic) — a team-chat-native version (writing under the `assets` bucket's `team-chat/<threadId>/` path, matching the browser upload's own convention) is a separate, larger follow-up if it's wanted. **Flagged, not fixed here:** the `assets` bucket both portal and team chat attachments land in is already known to be publicly reachable by anyone with the link — see open dev_task `3e0578d9`. This change extends that same exposure to team chat rather than introducing a new one.)_
_Prior: 2026-09-02 — Claude (**ROOT CAUSE OF THE "Failed to fetch" upload-put FOUND — it was never our code or Supabase's infrastructure. It was Chrome's QUIC protocol misbehaving on one specific machine (Luca's Mac Mini).** The 2026-09-01b entry below correctly fixed two real app-level regressions (file duplication, cross-thread attachment leak — both still stand, still shipped) but could not explain why the underlying network failure kept recurring for Luca specifically. This pass isolated it by elimination, all live-tested, not assumed: same login (support@tonydurante.us) succeeds from Antonio's machine and from Safari on Luca's own Mac, fails only in Chrome on that Mac — survived a brand-new zero-extension Chrome profile (rules out extensions), no VPN, no antivirus, Chrome itself fully up to date (152.0.7977.76, confirmed current for Mac as of 2026-09-01), and the Google Workspace admin console for support@tonydurante.us's org unit has zero custom policy configured (Security/Reporting/Data-controls categories all at Google default, Enterprise Connectors not even set up) — so it was not a company-managed browser policy either. DevTools Network tab showed the actual CORS preflight (OPTIONS request) to the storage upload URL failing with a bare `net::ERR_FAILED`, before any of our code or Supabase's own response could matter. Disabling the QUIC protocol flag in that one Chrome installation (`chrome://flags/#enable-quic` → Disabled) fixed it, confirmed working by Antonio. Lesson for the next session hitting an unexplained "Failed to fetch" on this upload path: check the browser/machine axis (Safari vs Chrome, clean profile, QUIC flag) before re-suspecting the app or Supabase again — this exact symptom has now been seen to have three completely different causes across the two fix passes (two real app bugs, one client-side networking bug), and none of them will show up in server-side logs._

_Prior: 2026-09-01b — Claude (**The 2026-09-01 re-fix (below) also shipped a same-day regression — declaring a fix "done" after lint/tests/build but before real adversarial review or live testing is exactly the gap that produced two bad ships in one day. A live production test (Antonio + Luca, watching in real time) immediately showed the exact same file duplicating itself in the composer after a failed retry — the earlier fix's `pendingFiles` fix hadn't actually landed correctly in combination with the new `cachedAttachments` mechanism it introduced.** Root cause investigation this pass (system-counselor): the underlying "Failed to fetch" network failure could NOT be pinned to our own config — our storage CORS is wide open (`access-control-allow-origin: *`, verified via a live OPTIONS request), the bucket config and traffic are trivial (8 objects written across the whole 2.5-hour failure window, no contention), and the error table's dedup-by-fingerprint mechanism was silently HIDING the true frequency (`occurrence_count` was 12, not the "1" the raw row's `created_at` implied — a real gap in how this doc's own investigation methodology read that table). The closest lead is an unresolved, publicly-acknowledged incident on Supabase's own infrastructure since 2026-08-14 (their API Gateway, "degraded_performance", still open as of this entry) — plausible but not a confirmed match for this exact symptom. Treat the underlying network failure as something the app must survive gracefully, not something our code can eliminate.

A second bug-hunter pass (Antonio's explicit instruction: "find the real cause first, run it with the bug hunter to catch and prevent bugs in the entire system") on the LIVE, shipped code found the actual regression: `handleSend` stopped clearing `pendingFiles` at the moment Send is clicked (a genuine diff from the pre-regression code, not an intentional design choice), so the failed-upload re-stage step merged the failed file back into a `pendingFiles` that had never actually been emptied — duplicating it. Worse, the SAME pass found the `cachedAttachments` mechanism (added specifically to avoid re-uploading a file that already succeeded when only the final send POST fails) was never scoped to the conversation it belonged to and was never cleared on switching channels — a file successfully uploaded in one client's channel could silently attach itself to a message sent in a DIFFERENT client's channel if the user switched away before the original send resolved. This is the more serious of the two: a real cross-client data-misdirection risk, not just a cosmetic duplicate.

**Fix, this pass:** `pendingFiles` is cleared synchronously the instant Send is clicked (restores the pre-regression behavior, closes the duplication at its source). A new `useEffect` clears both `pendingFiles` and `cachedAttachments` whenever the selected conversation changes (covers files staged but never sent). Every state write inside `handleSend` that touches `cachedAttachments`, `pendingFiles`, the typed draft, the reply target, or the optimistic message-list insert is now gated on `selectedIdRef.current === sendThreadId` (the thread this particular send was actually for, captured once at the start) — the underlying upload and the message POST still always target the correct thread regardless of what the user is looking at when the async work resolves; only the LOCAL composer UI state is guarded. The file strip now renders `cachedAttachments` too (was previously invisible — a client-scoped chip you can't see is a chip you can't stop), with its own remove control gated the same way as the original one. The per-message attachment cap (`CHAT_ATTACHMENT_MAX_COUNT`, 5) now counts `cachedAttachments` too — a follow-up bug-hunter finding on this exact new code caught that the cap was bypassable after a partial failure — plus a hard `.slice()` immediately before the POST as defense in depth regardless of how the array was assembled.

**Verification, done properly this time:** a SECOND, independent bug-hunter pass specifically re-attacked the corrected code (not just re-read the plan) and confirmed all three original findings closed, then found and this session fixed two more issues the correction itself introduced (the new remove-button lacked the same disabled guard as the original; the cap bypass above). Live testing against the real sandbox environment: a monkey-patched `fetch` forced the storage PUT to fail on every attempt — confirmed via screenshot that exactly one file chip remained after the failure, not two, and via `window.__putCallCount` that all 4 configured retry attempts genuinely ran. A second live test forced the PUT to succeed but slowly, then switched to a different real channel via an in-app click (not a page reload) while the upload was still in flight — confirmed via the actual sandbox database afterward (queried directly, since the production-only MCP tool cannot see sandbox data — a distinction that cost real time to work out mid-session) that the message landed with its attachment in the ORIGINAL channel only, zero contamination in the channel switched to.

**Not fixed here, filed separately (confirmed pre-existing, not caused by this session's changes):** `replyTo` and `editing` share the identical un-scoped-across-threads bug class as `pendingFiles`/`cachedAttachments` did — switching channels mid-reply or mid-edit can target the wrong conversation. Same fix shape as above (extend the new thread-switch-clearing effect), just not bundled into this urgent pass.

Dev job `ae61b03c`.)_
_Prior: 2026-09-01 — Claude (**The 2026-08-31 attachment-retry fix (below) was declared "fixed and live" without ever being confirmed against a real recurrence — it wasn't fixed. The identical error recurred in production the very next day (`system_errors` id `c0675247`, 2026-09-01 14:01 UTC, same host, same route). Root cause: a widen-the-retry fix is not automatically safe, and a 5-reviewer council pass (senior-engineer, ai-architect, bug-hunter, system-counselor, project-director) on the actual fix plan caught real problems the naive version would have introduced.** Live data first: the affected user's connection was not broken (uploaded fine 6 minutes before the failure, and many times over the prior week) — this is a genuine but low-frequency transient network hiccup, not a persistent block, confirming the ORIGINAL diagnosis was right; the fix just didn't cover enough ground. What the council found wrong with "just widen the retry": (1) [senior-engineer, blocker] the old retry only reacted to a THROWN fetch error — a STALLED connection (hangs, never throws) was never retried at all, and left `uploading` stuck `true` forever, permanently disabling Send; widening the window alone would have made this worse, not better. (2) [bug-hunter, blocker] a longer retry window gives more time for the user to type a NEW message while the OLD one silently retries — the failure handler unconditionally restores the OLD text, clobbering whatever the user typed in the meantime. (3) [ai-architect + senior-engineer + bug-hunter, independently] `Promise.all` across multiple files means ONE failure discards ALL of them, including ones that already succeeded — forcing a full re-upload; worse, if the final send POST fails AFTER uploads succeed, those uploads get silently re-done on retry, orphaning duplicate Storage objects (no cleanup job exists for `team-chat/` paths). (4) [ai-architect, blocker] the original fix lived only in `lib/team/attachment.ts` — literally the same copy-paste-drift pattern that left the client-facing portal chat (`lib/portal/chat-attachment.ts`) with ZERO retry protection at all, confirmed still true.

**Fix, this pass:** new shared `lib/chat/upload-with-retry.ts` — `fetchWithNetworkRetry` now wraps every attempt in its own `AbortController` with a hard per-attempt timeout (6s default), converting a stall into a retryable thrown error instead of an infinite hang; 4 attempts, ~30s worst-case total window (was ~2.4s across 3 attempts, not the "~5s" this doc previously implied). `lib/team/attachment.ts` now imports this instead of defining its own copy — closing the exact drift risk that caused the portal-chat gap. `app/(dashboard)/team-chat/page.tsx`: the composer textarea is now `disabled` while `sending || uploading`, closing the text-clobber race outright rather than trying to detect it after the fact. `handleSend` now uses `Promise.allSettled` + a new `cachedAttachments` state: a partial multi-file failure holds the whole send, keeps already-succeeded uploads out of the retry (never re-uploaded), and re-stages only the file(s) that actually failed; a failure on the final send POST (uploads already done) retries just the POST with the cached attachments — never re-uploads, never orphans a duplicate object.

**Test added specifically to prove the hang case, not just exercise the happy path:** `tests/unit/upload-with-retry.test.ts` mocks a `fetch` that never resolves and never rejects on its own — only the `AbortSignal` firing settles it — and confirms the attempt is cut off and a second attempt actually runs. Also fixed 2 existing tests in `tests/unit/team-attachment-upload.test.ts` that hard-coded the old 3-attempt count (now 4) — caught by a real test failure, not assumed.

**Deliberately NOT done this pass, needs Antonio's own explicit go:** porting this same protection to the client-facing portal chat (`components/portal/portal-chat.tsx`, `components/contacts/contact-detail.tsx`, `app/(dashboard)/portal-chats/page.tsx` — 3 real client-visible screens share `lib/portal/chat-attachment.ts`, confirmed via live code read). That surface has the identical `Promise.all`-all-or-nothing pattern AND zero retry logic at all — worse than what team-chat had even before today's fix — and it also does not re-stage picked files on ANY failure today (team-chat's does). Real clients hitting this same transient hiccup get a worse experience than staff did. Scoped out here deliberately because it touches real client-facing surface, not because it's lower-priority. Dev job `62a64f2b` (child). Full unit suite green (10,036 tests), build clean, sandbox only as of this entry — production ship pending Antonio's word.)_
_Prior: 2026-08-31b — Claude (**Main Team Workspace page now re-syncs the instant the tab wakes or the network returns — it was the one surface missing this, while the floating chat widget (`components/team-chat/floating-chat.tsx`) already had it.** Same-day follow-up to the attachment-retry fix below, after Antonio asked how the system could stay "alive" instead of going stale after standby. The existing 8s/20s poll already covers most staleness but can lag up to that long right when someone returns to a backgrounded tab; this makes the catch-up immediate instead of waiting for the next tick. New effect in `app/(dashboard)/team-chat/page.tsx` listens for `visibilitychange` (fires only on becoming visible, matching the floating widget) and the `online` event, and on either calls the page's own `loadThreads()` + `loadMessages(selectedIdRef.current, { silent: true })` — the exact same functions the poll already uses, no new data-fetching path introduced. Deliberately did NOT touch the realtime WebSocket subscription itself (force-recreating it on every visibility change was considered and rejected — supabase-js already has its own reconnect logic, and tearing down/resubscribing a live channel on a timer this frequent risks duplicate-handler or race-condition bugs for uncertain benefit over what the poll + this resync already cover together). No test added — this page has no existing component-test harness (confirmed: no `.tsx` test file exists for either `team-chat/page.tsx` or `floating-chat.tsx`), consistent with house convention of covering this class of UI wiring via browser QA rather than a new per-page test rig. Verified live in the browser (sandbox) and via build + full unit suite (unchanged, since no new lib/ function was added). Sandbox only as of this entry — production ship pending Antonio's word.)_
_Prior: 2026-08-31 — Claude (**Attachment uploads quietly retry on a network-level failure, and both halves of an upload failure are now reported instead of vanishing. Origin: Luca (td-bug) hit a raw browser "Failed to fetch (ydzipybqeebtpcvsbtvs.supabase.co)" toast sending a reply with 3 screenshots — the message and files never saved (confirmed live: 0 reply rows under that thread), retried and got the identical error. Investigation (live DB + code, cross-checked by an independent system-counselor pass) traced it to the ONE step in the whole send flow that talks to Supabase directly from the browser: the signed-URL PUT in `uploadTeamAttachment` (`lib/team/attachment.ts`) — the message-save itself goes to our own API, never to `*.supabase.co` directly, so it structurally cannot be the source of an error naming that host. Ruled out along the way, each via a live check: a channel-specific permission gap (none exists — one blanket staff-only RLS policy, no per-channel scoping); a code regression (no team-chat files changed in the days between Luca's last successful attachment send and this failure); the `assets` bucket's config (public, 100MB limit, no mime restriction — unchanged); the dashboard PWA's service worker (`public/dashboard-sw.js` — its `fetch` handler explicitly ignores every non-navigation request, so it cannot be intercepting the storage PUT); and a general "Luca's environment is broken" theory (he uploaded successfully via Portal Chats ~80 minutes earlier the same day). The exact live cause was never pinned down beyond that — no Vercel/Sentry log access from this session, and Sentry's own `ignoreErrors` list explicitly drops `'Failed to fetch'`/`'NetworkError'` globally (`sentry.client.config.ts`), so even an uncaught instance of this class of error was never going to reach us there.

**Fix, scoped to the two fetches inside `uploadTeamAttachment`:** a network-level failure (the browser's own thrown `TypeError`, not a completed response) is now retried up to 3 attempts with a short backoff (800ms, 1600ms) before giving up — most such failures are a passing blip, and the composer already preserves the typed text + staged files across a failed send, so a transparent retry means most people never see an error at all. A completed-but-bad-status response (413 too-large, a real 500, etc.) is a genuine answer from the server and is deliberately NOT retried — unchanged behavior, own message. Only after retries are exhausted does the user see anything, and now it is an actionable message ("...your message and files are still here — press Send to try again") instead of the raw browser text.

**Visibility, the other half of the fix:** this whole class of failure happens entirely client-side against Supabase directly and never reached our own error tracking in any form — not `system_errors`, not (per the ignoreErrors list) Sentry. Two additions close that: (1) client-side, an exhausted retry now fires a best-effort report to `POST /api/system-errors/report` (mirrors `components/offers/create-offer-dialog.tsx`'s `reportDialogError` — same fire-and-forget pattern, never blocks or breaks the send on its own failure — see the "reporting failure never surfaces to the caller" test); (2) server-side, `POST /api/team/upload-url` failing to mint the signed URL previously only did `console.error` — invisible on `/system-health` — now also calls `reportSystemError`, matching the pattern already used at 15+ other routes.

**Tests:** `tests/unit/team-attachment-upload.test.ts` — first-try success, recovers from a mid-retry network blip (both the mint call and the PUT), exhausts retries and reports + throws the friendly message, does NOT retry a completed 413, and confirms a failing report call never changes what the caller sees. Fake timers drain the retry backoff so the suite stays fast. Sandbox only as of this entry — production ship pending Antonio's word. Dev job `5817969a`'s sibling investigation (the ShoppyVerse invoice content of Luca's original draft message) is unrelated and tracked separately.)_
_Prior: 2026-08-23 — Claude (**Fixed a real, live bug this session's 2026-08-14 fix (below) accidentally introduced: `@claude`'s attach-scope paragraph told the model that `search_documents` "includes Drive-stored client records" — false. `search_documents` only returns files already filed in our own document index; it never queries Drive. This is the root cause behind a real incident (Achievers Group LLC 2024 tax return, dev job `77bd0e93`, Luca): the model trusted the false claim, never called `drive_search`, and gave up asking Luca where the file was — even though the file genuinely existed in Drive and `drive_search` was available to find it the whole time. Corrected the paragraph to say plainly that `search_documents` is our own filing, not live Drive, and to require checking `drive_search` before concluding a document does not exist. One real limit stated honestly rather than papered over: a file found ONLY via `drive_search` (never filed with us) still cannot be attached directly today — no `attach_ref` exists for a raw Drive result — so the model is told to say so and ask the staff member to post the file into the thread themselves. (Attaching a Drive-found file directly is tracked separately, dev job `20392707` — a security-boundary decision, not folded into this prompt fix.) Identical fix applied to the Portal Chats twin, documented in `ai-agent.md`. 5 new regression tests pin both. Sandbox only as of this entry — production ship pending Antonio's word.)_
_Prior: 2026-08-14 — Claude (**`@claude`'s attach-scope paragraph (`lib/team/claude-trigger.ts`'s CONTEXT CORRECTION block) was contradicting `send_email`'s own real capability. dev job `5e87b099` (Luca, "the worker acts like a judge, not an assistant" — full root-cause writeup in `ai-agent.md` 2026-08-14). Sandbox only.** The paragraph traces back to 2026-08-03 ("FILES POSTED IN A TEAM THREAD ARE NOW EMAILABLE", entry below) — a deliberate, scoped feature at the time, correctly worded then. `send_email` was loosened the SAME day to also accept any `search_documents` result (Drive included), but this restated copy of the rule was never touched, so the worker was being told two contradictory things about what it could attach — a plausible driver of the hedging Luca saw ("checking other things" instead of just doing the mechanical task). Rewritten to state both real sources: thread-posted files AND documents found via `search_documents` this turn. The Portal Chats surface (`lib/ai-agent/inbox-worker-prompt.ts`) carried the identical stale restriction — found by bug-hunter review, fixed the same way, documented in `ai-agent.md`.)_

_Last verified against code: 2026-08-04c — Claude (**The Confirm card in Team Chat can now ADD and REMOVE files before you send. Antonio, 2026-08-04: "I want the upload button on the card as well."** Two things matter specifically HERE, because this card is a permanent, channel-visible message rather than an ephemeral panel one: (1) the file list is BAKED INTO the message, so an edit updates the stored message in the same request — otherwise the card would show fewer files than the email actually carries, which is the exact failure the previous rounds removed; (2) the owner check nearly shipped broken on this surface. Drafts record their owner as `<surface>:<who>`, and Team Chat records the sender's DISPLAY NAME while every panel records an EMAIL — so an email-only comparison locked a person out of their own card here, on the very screen the button was asked for. Ownership now matches on either shape (email or team display name, resolved through the team directory) and fails closed on an unattributed draft. Editing is offered only while the card is still live; once sent or cancelled the controls disappear rather than promising something that cannot work. Rules and tests in `lib/inbox/prepared-attachment-edit.ts`; full detail in `docs/systems/inbox.md` (2026-08-04e).)_

_Last verified against code: 2026-08-04 — Claude (**Card fixes reaching Team Chat: file sizes now read B/KB/MB instead of "0.0 MB" for anything under a megabyte, and an email whose files belong to more than one client is flagged on every tile with both owners named. Also a QA-PROCESS NOTE worth keeping: during this round a Vercel build triggered from a push to main took the `td-operations-sandbox.vercel.app` ALIAS and silently replaced a CLI deploy, so a browser QA pass was run against the wrong build and a rendered warning appeared to be missing when the code was fine. Confirm the alias points at your own deployment (`vercel inspect <alias>`) before trusting any sandbox QA result. Full detail in `docs/systems/inbox.md` (2026-08-04).**)_

_Last verified against code: 2026-08-04b — Claude (**CLIENT CONVERSATIONS NO LONGER NOTIFY — neither the in-CRM toast nor the phone push. dev job `2662241e`, sandbox only, NOT on production.** Antonio: *"every time Luca do something on his pc I receive the notification. i want to turn them off."* His screenshot showed three stacked toasts — `Conversation · Luca` (Luca instructing the worker inside a client conversation) and `Conversation · Claude` (the worker answering) — i.e. one teammate's ordinary work interrupting the other all day, on screen and on the phone. **THE CAUSE IS THE PARTICIPANT RULE, NOT THE VOLUME:** participation in a `discussion` is derived from an `internal_thread_reads` row, which is written when you merely OPEN a thread, so glancing at a client conversation once enrolled him in it permanently. **The fix** is one pure client-safe predicate, `conversationNotifiesParticipants()` in `lib/team/channel-notify.ts` (same file and same reason as `channelNotifiesStaff`: the phone and the screen must not be able to disagree), consulted by all THREE sites that could notify for a conversation — `components/dashboard/realtime-notifications.tsx` (toast), `app/api/team/threads/[id]/messages/route.ts` (a person posting), `lib/team/post-message.ts` (Claude/worker posting). The branches are GUARDED, not deleted: the participant model is still the intended shape, and Antonio's rejected-for-now option (b) — notify only for conversations you actually POSTED in or were shared into, plus a per-conversation Mute — drops straight into that predicate if the silence proves too much. **UNCHANGED, deliberately:** an @mention inside a conversation still notifies and its branch sits ABOVE the conversation branch at both send sites (test-pinned by source order — reordering it would swallow the only remaining way to reach someone in a silent thread); a deliberate SHARE into a conversation still notifies (`app/api/team/share/route.ts` — one person addressing another on purpose, not chatter); `claude-trigger.ts` still answers the asker; DMs, work-channel bug posts, portal client messages and payment/signature alerts are all untouched; and `countTeamNotifications` still counts conversations, so the Team Chat unread badge still ticks — a quiet number was judged not to be an interruption. **THE COST, STATED:** a teammate who writes "Antonio, look at this" in a conversation WITHOUT @naming him now reaches nobody in real time. Tests in `team-channel-notify.test.ts` (4), mutation-proven — removing the guard from one send path fails two of them. Prior notes retained below.**)_

_Prior: 2026-08-04 — Claude (**Card fixes reaching Team Chat: file sizes now read B/KB/MB instead of "0.0 MB" for anything under a megabyte, and an email whose files belong to more than one client is flagged on every tile with both owners named. Also a QA-PROCESS NOTE worth keeping: during this round a Vercel build triggered from a push to main took the `td-operations-sandbox.vercel.app` ALIAS and silently replaced a CLI deploy, so a browser QA pass was run against the wrong build and a rendered warning appeared to be missing when the code was fine. Confirm the alias points at your own deployment (`vercel inspect <alias>`) before trusting any sandbox QA result. Full detail in `docs/systems/inbox.md` (2026-08-04).**)_

_Last verified against code: 2026-08-03b — Claude (**Touch only: the Team Chat Confirm card's `files` entries now also carry a per-file `warning`, rendered in red by the shared card renderer — an internal-only document, or one belonging to a different client than the screen. Still no URL on the card. Why it matters here specifically: this card is a permanent, channel-visible row, so a flagged file has to be flagged on the card itself and not merely in the worker's reply text, which anyone scrolling back later will not re-read. Full detail in `docs/systems/inbox.md` (2026-08-03b).**)_

_Last verified against code: 2026-08-03 — Claude (**FILES POSTED IN A TEAM THREAD ARE NOW EMAILABLE — the "@claude attach this and send it" refusal Luca reported twice is gone. dev job 7e486d0a, sandbox only, NOT on production.** `emailSendPrep.sendable` was `[]` here on purpose, with a surface-prompt paragraph saying so ("FILES IN THIS THREAD CANNOT BE EMAILED") — honest, and added precisely to stop the false-capability class, but it refused the ordinary way staff work in a channel. **Now:** the same refs the READER takes from the carrier message (`attachmentRefsFromChatRow` — the @mention itself, or the nearest earlier human message with files) are minted into `sendable` as `chat_asset` entries, so **what the worker can read here it can attach**, by construction — the two cannot drift into "I've read the EIN letter but I can't send it". The prompt paragraph is replaced in the SAME change (fill one without the other and the feature ships dead in one direction: a filled list with the old prompt refuses, a new prompt with an empty list produces "Attachable now: none"). Team-chat files live in the PUBLIC `assets` bucket, so the bytes are COPIED into the private worker bucket when the draft is frozen — the permanent card then approves bytes we hold, not a public object anyone with the link can replace. **The card renders the files** (shared `ConfirmAttachments`): image inline, document as a tile, click to open, each labelled with where it came from. `teamChatCardForFrozenDraft` now carries a `files` array and its subtitle is the SUBJECT ALONE — the filenames used to be joined into that subtitle, which put a bare string in front of a human as if it were something they could check. **The card still carries NO url**, deliberately and test-pinned: this is a permanent, channel-visible row, so a baked signed URL would be either a dead tile living forever in the scrollback or a long-lived bearer link to a client document; the renderer builds a staff-authenticated path from `entity_id` + position instead, and that path re-authorises on every open. A nameless attachment keeps its position rather than vanishing from the card. Tests in `team-chat-portal-card-suppression.test.ts` (files carried, nameless kept, no URL anywhere in the card). Suite 7,852 green.)_

_Last verified against code: 2026-08-02 — Claude (**One guard added to the Claude reply path: a frozen PORTAL-CHAT draft is now SKIPPED by the Team Chat Confirm-card writer (dev job d2024649, sandbox only). Why it matters here — Team Chat cards are permanent, channel-visible messages, unlike the ephemeral panel cards elsewhere. A portal row physically cannot carry a recipient address or subject (the database forbids it), so without the guard the card would be written into the channel reading "Confirm email to null" and would stay there for anyone to click. Team Chat has no client picker and no language dropdown, so it cannot host a portal card at all — suppression, not adaptation. Made explicit rather than relying on the nullable type, because `strict: false` in tsconfig means the type alone would not have caught it at build time. Prior note retained below.**)
_Prior: 2026-07-29f — Claude (**SLACK IS REMOVED — Team Workspace is the only internal chat. Antonio: "we don't use Slack anymore… we built team workspace to replace Slack."** Deleted: the Claude bot's event webhook, its slash-command endpoint, its interactive/button endpoint, the `slack-claude-worker` cron (and its `vercel.json` + `cron-coverage` entries), the Slack mirror (`slack-mirror.ts`, `slack-mirror-classify.ts`) with its `/api/team/slack/*` routes and the `slack_mirror_enabled` setting + accessor, the dormant 6-digit approval rail (`slack-approval.ts` — its one live consumer, `isSixDigitCode`, now lives in `team-approval.ts`), the permalink helper (`slack-link.ts`) and the `read_slack_link` agent tool with its three allow-list entries, and `slack-thread-follows.ts`. Four unit-test files for the deleted modules went with them; suite 7,481 green. **NOT touched, deliberately:** the shared WORKER ENGINE is still named after Slack (`SLACK_WORKER_SYSTEM_PROMPT` in `slack-claude.ts`, `slack-file-reader.ts`, `slack-staff.ts`, the `enableSlackSend` flag) and is imported by ALL FOUR live surfaces — Team Chat, Inbox, Portal Chats, sidebar. Deleting by keyword would break every surface at once; the rename is a separate mechanical change. **STILL SLACK-COUPLED, awaiting Antonio's decision:** the Client-Threads routes/crons fetch, close and reopen threads that live IN Slack, and the CRM Conversations page calls them — removing Slack ends that feature's data source, which is a product call, not a cleanup. `slack-claude-worker.md` is marked RETIRED with the same detail.)_

_Last verified against code: 2026-07-29b — Claude (**"ON BEHALF OF" — a Claude-sent message no longer notifies the human who dictated it. dev job 8537adf9, council-reviewed (4 core, all blockers folded in).** Antonio's report: replying through Claude (Claude Code MCP `team_chat_send`, or @claude in a thread) buzzed HIS phone/toast for his own words, because sender is the Claude sentinel and every exclusion is sender-only. **Data:** migration `20260729-1900-team-chat-on-behalf-of.sql` adds nullable `internal_messages.on_behalf_of_user_id` (no FK — root_id precedent; no backfill) and rebuilds `get_team_threads` + `list_followed_unread_threads` from their LIVE PROD defs (drift-checked 2026-07-29: sandbox bodies identical after comment-strip) so every `sender_id <> p_user_id` unread/mention exclusion also excludes rows the caller dictated. **Choke-point (`lib/team/post-message.ts`):** new `on_behalf_of` input (uuid or email) → `resolveActingUser` against `listTeamMembers` — **COUNCIL RULE, test-pinned (`team-acting-user.test.ts`): any ambiguity → null → everyone notified; never guess** (a wrong stamp silences the wrong person — the bug inverted). Stamped at insert with a PGRST204/42703 retry-WITHOUT-the-column (prod deploy lands before Antonio runs the DDL in the SQL editor; the send must survive that window — remove the shim once prod DDL is confirmed). Mentions resolve with the ACTING user as the excluded sender (typed-message parity: a self-mention neither stores nor pushes, and an emptied mention list falls through to the channel broadcast so Luca is never silenced — SE finding). Push branches all exclude the acting user (channel via `sendPushToStaffExcept` alsoExclude; DM other-participant check; discussion participant filter). Acting user is auto-FOLLOWED on a dictated reply (`internal_root_follows` upsert, human-route parity — matters in the silent td-worker-bug channel). **⛔ NO READ POINTERS ARE WRITTEN** — three reviewers independently showed pointer advancement wipes unread state for messages the user never saw and fakes "seen by" receipts; unread is fixed at READ time instead: the two RPCs above + `computeThreadMeta` (`ReplyRow.on_behalf_of_user_id`, fed by the thread GET's projection with a column-missing fallback) treat "dictated by me" as "mine". **Toast:** `realtime-notifications.tsx` skips `row.on_behalf_of_user_id === me` (realtime delivers full rows; verified no column allow-list). **Who stamps:** MCP `team_chat_send` — per-request auth context (`lib/mcp/auth-context.ts`, ALS set in the transport route's withAuth): static Claude Code key → `MCP_TEAM_CHAT_ACTOR_EMAIL` (default antonio.durante@tonydurante.us); an OAuth connector session → THAT token's oauth-user email, never the env default (architect blocker — defaulting an identified session would misattribute Luca's sends); @claude trigger (`claude-trigger.ts`) → `prompt.sender_id` (the real auth uuid; the `sendActor` display-name label is NEVER parsed for identity). **KNOWN GAP (named, safe):** the Slack worker sets no identity → its team-chat posts stay unstamped → everyone notified (today's behavior). What is deliberately UNCHANGED: notify-all-staff-except-sender in work channels (Antonio's 2026-07-24 design), and there is still no "waiting for the other person" thread indicator (separate feature if wanted). Tests: acting-user rules (12), thread-meta on-behalf (2), send-context wiring (2).)_

_Last verified against code: 2026-07-30a — Claude (**THE CROSSED-DRAFTS HOLE THAT SHIPPED WITH THE CONFIRM CARD IS CLOSED. dev job f55ea3bb; found by the independent verifier re-reading the pass-4 finding, which had been fixed only halfway.** 2026-07-29h claimed the pairing bug was fixed by "every pending row minus the pre-turn id snapshot, oldest-first". **It was not.** Both overlapping turns snapshot an EMPTY set, so both then see both new rows and both `.find()` the older one: turn A's answer carries turn B's recipient, B's card is a duplicate of the same row, and A's own frozen email gets no card at all and dies at its TTL. `cancelDraftsFrozenThisTurn` had the mirror bug — an unscoped "pending minus my snapshot" cancelled a teammate's LIVE draft the moment this turn hit an error path. Reachable wherever the conversation is shared: `thread_uuid` is the whole CHANNEL in Team Chat, and the email thread / the client on the Inbox and client-chat panels (both shared between staff); the sidebar's thread is per-user so it was never exposed there. **The missing half is the ACTOR.** The frozen row records who froze it, every surface attributes a distinct actor per staff member (`team-chat:<sender>`, `crm-inbox:<email>`, `crm-portal:<email>`, `crm-sidebar:<email>`), and `prepareWorkerEmailSend` already supersedes per-actor — so the picker and the freeze now agree on scope: **this actor's pending rows, minus this actor's pre-turn snapshot.** Two people can no longer collide; the same person's overlapping turns are still collapsed by the supersede. **Three copies became one:** `snapshotPendingPreparedIds` / `findPreparedFrozenThisTurn` / `cancelPreparedFrozenThisTurn` in `lib/inbox/worker-email-send.ts`, used by the Team-Chat trigger and both routes — the local copies (and `cancelDraftsFrozenThisTurn`) are deleted, so the next surface to share a thread cannot reintroduce this for free. Unreadable snapshot still fails to NO CARD and cancels nothing. Tests: `worker-prepared-turn-pairing.test.ts` drives the real helpers against a fake table that honours the filters — 8 cases incl. the two-people-at-once pairing and the colleague's-draft cancel; **both halves mutation-proven** (dropping either `.eq("actor", …)` fails exactly one test). Suite 7,640 green; build clean.)_

_Last verified against code: 2026-07-29h — Claude (**FOURTH council pass on the Team-Chat Confirm card — 4 majors + 3 minors, all fixed. dev job f55ea3bb.** Two would have reached a client. (1) **A SUPERSEDED DRAFT STAYED CLICKABLE AND COULD SEND A SECOND, CONTRADICTORY EMAIL.** Redrafting ("no, say we need his numbers first") freezes a new row each pass; on the panels the old card is ephemeral, but here it is a PERMANENT chat message that stays amber. Fix in the shared `prepareWorkerEmailSend`: freezing a draft now CANCELS every earlier pending row on the same conversation, so only the newest payload can ever leave — on every surface. An old card now clicks through to an honest "already cancelled". Tested. (2) **ATTACHMENT REGRESSION into the false-capability class.** Team Chat sets `sendable: []` while the worker happily READS thread files, so "send this PDF to Smit" hit the executor's *"drop the file into the panel"* — a panel that does not exist on this screen (before the card, it hit the honest "not available here" branch). Fix: the Team-Chat surface prompt now states plainly that files here are readable but NOT attachable, that the attach flow exists only on the panels, and that it must never mention a panel or claim a file is attached. (3) **The card could be paired with a DIFFERENT turn's draft** — `thread_uuid` is the whole CHANNEL and two people can have overlapping @claude turns, so "newest pending row" could hand turn A's answer turn B's recipient while A's own email went invisible. Now: every pending row minus the pre-turn id snapshot, oldest-first — the card can only ever be this turn's own. (4) **The card was only ASSUMED server-authored.** The message POST accepted a client-supplied card verbatim, so a crafted one could show harmless text beside a real frozen id and get a teammate to dispatch something else. `email_confirm` cards are now REFUSED at the POST — only the @claude trigger stamps them. (5+6) **Silent failures that promised a confirmation.** A worker throw AFTER a freeze, a failed card write, or an unusable snapshot all left the reply saying "frozen for the staff member to confirm" with no card and the email dying at its TTL. Now: this turn's frozen rows are cancelled, a SERVER-authored line is appended to the reply ("could NOT be prepared… nothing will be sent"), and the failure is reported to system-errors. (7) The card write is guarded against landing on a row that never received the answer, twin to the answer write. Suite 7,538 green; lint clean.)_

_Last verified against code: 2026-07-29g — Claude (**THE CONFIRM STEP NOW EXISTS ON TEAM CHAT TOO — it is on all four surfaces. dev job f55ea3bb. Antonio: "I want the confirm step everywhere. that must be present."** Team Chat had been left WITHOUT the step (2026-07-29e) for a real reason: it had no control to render a Confirm into, so an exempt list there could only ever produce a refusal. The control now exists. **Server (`claude-trigger.ts`):** the email rail carries `emailConfirmExempt` (the linked client's own addresses, if the thread has a client, plus `TD_MAILBOXES`) and an `emailSendPrep` whose `threadUuid` is the TEAM thread's uuid, so a new recipient FREEZES instead of sending. Prior pending rows are id-snapshotted BEFORE the worker call (never a clock comparison — skew would drop this turn's card or resurface an old one), and a failed snapshot suppresses the card rather than risking a stale confirm. After the answer is written, a row created THIS turn is stamped onto Claude's own reply as a card. **Card (`workspace.ts` + team-chat types):** new kind `email_confirm`, carrying the frozen row id in `entity_id` and the exact outgoing text in `body` — so Confirm approves a MESSAGE, not just an address, exactly like the panels. Server-authored only. **UI (`team-chat/page.tsx::EmailConfirmCard`):** amber card with recipient, subject + file names, the full body in a scroll box, and Confirm & send / Cancel calling the SAME `/api/inbox/worker-chat/confirm-send` endpoint the Inbox and client-chat panels use — one payload, one code path, single-use row (a second click cannot send twice), and the server's real error surfaced verbatim (R099: "already sent", "not authorized for this mailbox", expiry). **So the rule is now uniform on Inbox, client chat, sidebar AND Team Chat:** an already-known address sends on the staff member's go-ahead; any NEW address freezes and one human sees the recipient before it leaves. Test: `team-workspace.test.ts` pins the card kind — if the validator ever rejects it, the card write fails and a frozen email would sit with no way to send it. Suite 7,535 green.)_

_Last verified against code: 2026-07-29e — Claude (**@claude EMAIL WORKS ON EVERY TEAM THREAD, not just client-linked ones. dev job f55ea3bb, pass 3.** The rail was gated on `thread.account_id || thread.contact_id`, and channel/ad-hoc threads carry NULL client ids — so "@claude email our accountant about MFCompany" inside #td-taxreturn got no `send_email` tool at all, while the verbatim Slack prompt it runs still says to call send_email (a false capability on the channel where the work happens). Now unconditional: `{ enableEmailSend: true, forceMailbox: 'support' }` on every team thread. Also recorded: this surface passes no `agent_messages` id, so `claimWorkerSend`'s cross-run dedup cannot engage — a turn killed between the Gmail send and the placeholder rewrite could be re-sent by `rescueStuckClaudeReplies`. Pre-existing, blast radius widened from client-addresses-only to any address; flagged, not fixed.)_

_Prior: 2026-07-29d — Claude (**@claude EMAIL FROM A CLIENT THREAD REACHES ANY ADDRESS — no confirm step here, deliberately. dev job f55ea3bb.** The first fix round briefly gave this surface a confirm-a-new-recipient list; a verification pass caught that it had NO `emailSendPrep`, so nothing could be frozen and every third-party address hit the refusal branch — re-creating the exact MFCompany/Smit bug the work was reported for, while the comment claimed it was fixed. Team Chat now carries `{ enableEmailSend: true, forceMailbox: 'support' }`: no address restriction, no confirm step, because (a) the prompt here is STAFF-AUTHORED — a teammate typing "@claude email our accountant" IS the decision, which is Antonio's rule exactly — and (b) this surface has no prepared-send UI, so a confirm step could only ever produce a refusal. `forceMailbox` still applies: no mailbox-authorisation check exists here, so `from: 'antonio'` is never honoured. The confirm-once step lives on the two surfaces that carry attacker-authored text and have a card to render (Inbox, client-chat panel) — see inbox.md 2026-07-29j.)_

_Prior: 2026-07-29c — Claude (**CLIENT-THREAD EMAIL UNPINNED — staff decide the recipient. dev job f55ea3bb, Antonio's explicit direction (verbatim: "You just have to unlock it because he has to do what we decide").** The @claude worker on a client DISCUSSION thread had `pinnedEmailRecipients` = the account's contact emails (the 2026-07-19 safety floor), which hard-refused the everyday case: Luca asking it to email the ACCOUNTANT (MFCompany → smit@adasglobus.com, 2026-07-29). The pin is REMOVED on this surface: `emailSendRail` is now `{ enableEmailSend: true }` with NO pin when the thread is client-linked (`claude-trigger.ts`), the same unpinned model Slack has always had — because the @claude prompt is STAFF-authored, the draft→explicit-"send it" discipline remains the actual control, and `sendActor` audit-attributes every send. TRADE-OFF ANTONIO ACCEPTED (named to him before building): with no pin, a poisoned document attempting to redirect a send is caught only by staff reviewing the draft. The Inbox surface's thread-participant pin and the Portal-Chats client pin are UNTOUCHED. The July-20 `account_contacts` join fix note (previous entry below) is now historical for this rail — the address list is no longer built at all.)_

_Prior: 2026-07-29b — Claude (**"ON BEHALF OF" — a Claude-sent message no longer notifies the human who dictated it. dev job 8537adf9, council-reviewed (4 core, all blockers folded in).** Antonio's report: replying through Claude (Claude Code MCP `team_chat_send`, or @claude in a thread) buzzed HIS phone/toast for his own words, because sender is the Claude sentinel and every exclusion is sender-only. **Data:** migration `20260729-1900-team-chat-on-behalf-of.sql` adds nullable `internal_messages.on_behalf_of_user_id` (no FK — root_id precedent; no backfill) and rebuilds `get_team_threads` + `list_followed_unread_threads` from their LIVE PROD defs (drift-checked 2026-07-29: sandbox bodies identical after comment-strip) so every `sender_id <> p_user_id` unread/mention exclusion also excludes rows the caller dictated. **Choke-point (`lib/team/post-message.ts`):** new `on_behalf_of` input (uuid or email) → `resolveActingUser` against `listTeamMembers` — **COUNCIL RULE, test-pinned (`team-acting-user.test.ts`): any ambiguity → null → everyone notified; never guess** (a wrong stamp silences the wrong person — the bug inverted). Stamped at insert with a PGRST204/42703 retry-WITHOUT-the-column (prod deploy lands before Antonio runs the DDL in the SQL editor; the send must survive that window — remove the shim once prod DDL is confirmed). Mentions resolve with the ACTING user as the excluded sender (typed-message parity: a self-mention neither stores nor pushes, and an emptied mention list falls through to the channel broadcast so Luca is never silenced — SE finding). Push branches all exclude the acting user (channel via `sendPushToStaffExcept` alsoExclude; DM other-participant check; discussion participant filter). Acting user is auto-FOLLOWED on a dictated reply (`internal_root_follows` upsert, human-route parity — matters in the silent td-worker-bug channel). **⛔ NO READ POINTERS ARE WRITTEN** — three reviewers independently showed pointer advancement wipes unread state for messages the user never saw and fakes "seen by" receipts; unread is fixed at READ time instead: the two RPCs above + `computeThreadMeta` (`ReplyRow.on_behalf_of_user_id`, fed by the thread GET's projection with a column-missing fallback) treat "dictated by me" as "mine". **Toast:** `realtime-notifications.tsx` skips `row.on_behalf_of_user_id === me` (realtime delivers full rows; verified no column allow-list). **Who stamps:** MCP `team_chat_send` — per-request auth context (`lib/mcp/auth-context.ts`, ALS set in the transport route's withAuth): static Claude Code key → `MCP_TEAM_CHAT_ACTOR_EMAIL` (default antonio.durante@tonydurante.us); an OAuth connector session → THAT token's oauth-user email, never the env default (architect blocker — defaulting an identified session would misattribute Luca's sends); @claude trigger (`claude-trigger.ts`) → `prompt.sender_id` (the real auth uuid; the `sendActor` display-name label is NEVER parsed for identity). **KNOWN GAP (named, safe):** the Slack worker sets no identity → its team-chat posts stay unstamped → everyone notified (today's behavior). What is deliberately UNCHANGED: notify-all-staff-except-sender in work channels (Antonio's 2026-07-24 design), and there is still no "waiting for the other person" thread indicator (separate feature if wanted). Tests: acting-user rules (12), thread-meta on-behalf (2), send-context wiring (2).)_

_Prior: 2026-07-29 — Claude (**@claude NO LONGER RUNS ON THE SLACK KEY, AND ITS FAILURES ARE NO LONGER INVISIBLE. dev job 6ffd2d5e.** The 2026-07-29 outage: `claude-trigger.ts` hardwired `apiKeyOverride: process.env.SLACK_WORKER_ANTHROPIC_KEY`, so when the Slack key was disabled at Anthropic — Slack itself being retired — every @claude request in Team Chat errored ("⚠️ I hit an error working on that"), and because the override was PRESENT (not empty) the shared-key fallback never engaged. Worse, the catch swallowed it: `system_errors` had ZERO rows while Luca hit the wall for hours; we learned from his bug report, not from /system-health. NOW: (1) the key is per-surface CONFIG — `surfaceApiKeyOverride('team_chat')` from `lib/ai-agent/surface-api-key.ts` reads `WORKER_KEY_TEAM_CHAT`, unset ⇒ shared `ANTHROPIC_API_KEY`; the legacy `SLACK_WORKER_ANTHROPIC_KEY` name feeds ONLY the `slack` surface, never team_chat — pinned by `tests/unit/surface-api-key.test.ts` ("THE OUTAGE" case); (2) the worker-call catch now calls `reportSystemError` (route `team-chat/claude-trigger`) BEFORE replying, so a repeat outage shows up in /system-health and the 15-min audit cron instead of only in a teammate's patience. ALSO same day: `worker-bug-report.ts` gained a third wall kind, `partial_read_shipped` — fired when an assistant answer ships over a file it did not finish reading (the reply itself carries a server-authored disclosure; see ai-agent.md "READ-TO-THE-END IS ENFORCED").)_

_Prior: 2026-07-26 — Claude (**READ-RECEIPT / "WHOSE TURN" BADGE ON EVERY THREAD LIST + THE CHANNEL RE-CLICK NOW RETURNS TO THE LIST. Sandbox; NO migration (reuses `internal_root_reads`). dev job 4fdfa31e.**

**Nav fix.** Clicking the channel you are ALREADY in now returns to its Threads panel and closes the open thread. Root cause: the sidebar channel row called `setSelectedId(sameId)` — a React no-op — so the selection effect (the ONLY place that resets `openRootId`/`showThreadsPanel`) never re-ran, and the click appeared dead while a thread was open (Antonio: "I click td-bug and it doesn't go back in the list"). New `onSelectThread` in `page.tsx`: same-id + `thread_type==='channel'` → `setOpenRootId(null)` + `setShowThreadsPanel(true)`; a DIFFERENT id takes the normal `setSelectedId` path, so the selection effect's deep-link / board-open-pending guards are untouched. Wired onto the channel/general/DM sidebar rows.

**Read receipt (per viewer, per thread):** 'waiting for you' (someone else wrote last and you haven't opened it) · 'sent · waiting for {name}' → 'seen' (you — or the AI on the team's behalf — wrote last; flips once the other teammate opens it) · or nothing. ONE shared calculation surfaced on ALL FOUR thread-list surfaces — the channel Threads panel, the in-stream "replies" line, the cross-channel board, the Later list — so they can never disagree. Pure core `lib/team/thread-turn.ts` (`computeThreadTurn`) + server gatherer `lib/team/thread-turn-server.ts` (`enrichThreadTurn`) + shared `components/team-chat/turn-badge.tsx`; wired into the three list endpoints (`threads/[id]`, `all-threads`, `later-threads`). The `[id]` route also folds it into `thread_meta` for the in-stream line.

**⛔ ROSTER-FREE BY DESIGN, AND IT IS LOAD-BEARING.** "The other side" is NOT `listTeamMembers`. Verified 2026-07-26 the active team is exactly two humans (Antonio `b0da5d9c…`; Luca `deb9becc…`, whose id also posts as 'support' / 'Imported from Slack') plus the AI (`CLAUDE_SENDER_UUID`), and the auth directory ALSO carries dormant/QA accounts. Keying 'seen' off that directory would (a) leave every outgoing thread stuck at 'not seen' the moment a dormant account is in the list, and (b) hammer `listAllAuthUsers` (pages ALL auth users incl. every portal client) on the 8s message poll. Instead: internal threads are staff-only, so 'seen' = ANY read pointer that is not the viewer's and not the AI's reached the last message. The AI counts as OUR side (Antonio: "when I tell the AI to reply to Luca I have to see the status").

**KNOWN LIMITATION (accepted, both council reviewers):** a non-teammate staff login that opens a thread — the QA admin during testing most of all — counts as "the teammate" and can show 'seen' before Luca actually reads. Correct in daily 2-person use; QA the flip with Luca's REAL account, not the QA admin.

**`enrichThreadTurn` FAILS SILENT-BUT-LOGGED, NEVER WRONG.** supabase-js returns `{data:null,error}` (it does not throw), so each of its three reads (root last-messages, replies, read pointers) checks `error` → `console.error` + returns `{}` (no badge = honest "unknown") rather than computing a LYING receipt from partial data — the exact opposite trap the sibling `stateErr` guard warns about. Chunked `.in()` at 100 (the board asks for up to 300 roots). Timestamps compared as PARSED EPOCH instants, not strings: read pointers are written 'Z' by JS while messages serialize '+00:00' from Postgres, and lexical order of those two shapes is not chronological.

**Latency (disclosed to Antonio):** the 'seen' flip is NOT instant — the page realtime feed does not subscribe to `internal_root_reads`, so it lands on the ~8s message poll. The Later list has no poll, so its receipts refresh on its own reloads. Both accepted.

**THREAD OPENS FULL-WIDTH (drill-in), the resizable split pane is GONE.** Antonio, 2026-07-26: on a wide screen the old layout kept the channel stream beside the thread and squeezed the thread into a narrow, drag-resizable right column — cramped. Now opening a thread hides the channel stream on desktop too (mobile already did this) and the thread takes the whole main area, with a ← back arrow in the header returning to the channel's Threads panel (`backFromThread`: `setOpenRootId(null)` + `setShowThreadsPanel(true)` for channels). The drag divider and ALL its machinery are deleted — `paneWidth` state, `startPaneResize`/`resetPaneWidth`, `paneRowRef`, the localStorage width restore, and the `lib/team/pane-width` imports (the lib file itself is left in place). The thread body is capped to a centered `max-w-3xl` reading column so it doesn't stretch edge-to-edge on a big monitor. Deliberately IN-PLACE, not a separate route/URL — keeps the existing realtime + data with no new plumbing; a genuinely separate shareable thread page was considered and rejected as heavier for the same result. Scope: channel threaded view only; DMs/general/conversations unchanged. dev job 47769340.

**RECEIPTS NOW ON DMs + CLIENT CONVERSATIONS TOO (not just channel threads), + @MENTION CURSOR FIX. dev job 8288d86b.** `enrichConversationTurn` (`lib/team/thread-turn-server.ts`) computes the same read/turn state at CONVERSATION grain (key = thread id, read pointer = `internal_thread_reads`, last message = newest message in the thread) and reuses the SAME pure `computeThreadTurn`. Wired into `/api/team/threads` for `thread_type` dm|discussion ONLY (channels/general carry per-thread receipts inside them, so a conversation-level badge there is meaningless). Rendered as a COMPACT WhatsApp-style mark in the sidebar rows (`TurnBadge compact`: Eye=waiting-for-you / single Check=sent-waiting / double CheckCheck=seen) — full text pills stay on the channel surfaces. `@mention` insertion now moves the caret to AFTER the inserted `@handle ` (`insertMention` setSelectionRange in rAF) — it used to leave the caret behind the name.
**Council fixes on this batch (senior-engineer + bug-hunter):** (a) last-message lookup fetches DESCENDING + first-wins-per-thread, not ascending — an ascending fetch under PostgREST's ~1000-row cap returns the OLDEST rows and would show a stale/wrong last message; applied to BOTH `enrichThreadTurn` (channels) and `enrichConversationTurn`. Still transfers all rows — a `DISTINCT ON (thread_id)` RPC is the bounded upgrade once volume grows (follow-up; negligible at the current few-hundred messages). (b) the conversation enrichment is wrapped best-effort in the route (like the Later lookup) so a cosmetic receipt can never 500 the whole sidebar. (c) an Antonio↔AI DM (no human on the other side) is force-set to no-badge instead of a stuck "sent · waiting".
**KNOWN SEMANTICS, decided not a bug (bug-hunter #1):** "seen" means the conversation was OPEN on the other person's device — the main team-chat page marks read on open + poll (the route doc explicitly endorses this: "you opened it, you read it"), same as WhatsApp/iMessage. It does NOT mean eyeballs-on-screen; a chat left open + idle marks incoming read. The stricter `mark_read=0`+engagement model is reserved for the floating/background surfaces. Revisit only if Antonio wants the stricter version.

**GLOBAL IN-APP BACK (app shell — see also pwa.md).** `components/dashboard/global-back-button.tsx` renders in BOTH the desktop header and the mobile app bar (the installed PWA has no browser back — the reason Antonio asked). Reads `window.history.state.idx`; `idx>0 → router.back()`, else `router.push('/')` so a fresh deep-link never exits the PWA. Steps back a PAGE; in-page drill-ins that don't change the URL (a thread inside team chat, a conversation inside portal-chats) keep their own in-surface back arrows (portal-chats' list back already existed).

**EVERY SELECTION IS NOW A REAL BACK STEP (global Back arrow).** Antonio, 2026-07-26: "I click on one chat in portal chat, and then I change chat, and then I hit the arrow to go back. It goes to home dashboard." Channel / DM / conversation, the open thread, the Board view and the Slack mirror were ALL pure React state — the URL never moved — so the global Back arrow correctly skipped the whole page and landed on the last real navigation (the dashboard). `useSelectionHistory` (`lib/hooks/use-selection-history.ts`) now records them in the query string with `window.history.pushState` (native History API — Next 14.1+ keeps the router in sync with NO server round-trip or route re-render; `router.push` would refetch and is the wrong tool) and restores them on `popstate`. Deliberately reuses the EXISTING `?thread=` / `?root=` deep-link keys, so a copied URL and a Back step mean the same thing. Same hook is used by Portal Chats.
**⛔ RESTORING A THREAD MUST GO THROUGH `openThreadInChannel`, NEVER `setOpenRootId` DIRECTLY.** The selection effect resets `openRootId` whenever `selectedId` changes, so a direct set is WIPED on the same tick; the Board's replay path (`pendingOpenRef` + its effect) also fetches a thread whose opening message is older than the loaded 500-message window. And restoring the SAME channel with no root mirrors `backFromThread` explicitly, because `setSelectedId(same)` is a React no-op — the selection effect never runs and the thread would stay open (the identical no-op trap as the channel re-click fix).
**⚠️ TWO BACK GUARDS FAILED BEFORE THE WORKING ONE — do not re-try either.** (1) `window.history.state.idx`: the App Router does NOT populate it, so the guard was always false and EVERY Back went home. (2) pathname-change only: switching chats changes just the QUERY, so the flag never flipped and Back still went home — verified in-browser, Back pushed `/` and `history.length` went UP. The working guard is an explicit shared depth counter, `lib/nav/in-app-history.ts`: route changes AND selection pushes report a move, `popstate` consumes one. No framework internals. Both dead ends are recorded in that module's header.
**QA'd end-to-end in a real browser** (sandbox, logged in): general → td-bug → open a thread → Back returns to td-bug's THREAD LIST → Back returns to general; `history.length` holds across a Back (a true pop, not a push). Portal Chats: Stefano → Uxio → Back re-selects Stefano's row.

**Invariants preserved:** `?mark_read=0`, per-root read pointers, read-on-real-engagement, the partner/non-staff exclusion at the source — all unchanged; the receipt work only READS `internal_root_reads`/`internal_thread_reads`, it never writes a new read path.)_
_Prior: 2026-07-24 — Claude (**A WORK CHANNEL NOW TELLS BOTH PEOPLE EVERYTHING, THE CHANNEL BADGE COUNTS BUGS INSTEAD OF MESSAGES, THE "EVERY REGISTERED DEVICE" BROADCAST IS DELETED, AND CLAUDE CAN ANSWER INSIDE A BUG. Sandbox; migration `20260724-1800-channel-unread-by-thread.sql` applied to sandbox, prod pending. dev job 59db1c17.**

**Antonio's report, verbatim:** td-bug "is a mess because I can't control the single bug… When I have the red notification on the debug channel and I open one, the red note disappears, so I don't have control." Plus: "There is only Luca and me, so it's not mentioned or not. I have to know everything because I work on the bugs." And: "delete the 'Tell everyone'… I don't care about Chris, and the client's browser never has to get anything about our business."

**⛔ THE BADGE BUG, exactly.** A channel's `unread_count` counted EVERY message in the channel — including replies inside individual bug threads — against ONE pointer, `internal_thread_reads.last_read_at`, which the channel GET advances. So one glance at the stream marked as read every reply in every bug he had never opened. The per-bug state survived the whole time (`internal_root_reads` is a separate pointer — that is why the blue dot on "3 replies" stayed), but nothing at channel grain reflected it. Now, for `thread_type='channel'` ONLY, `unread_count` = **the number of THREADS in that channel that are new for me**, and it does not read the channel pointer at all, so opening the channel cannot clear it — only opening each bug can. **This is the FOURTH reader of "is this thread new for me"** (with `computeThreadMeta`, `list_all_threads`, `list_followed_unread_threads`); the predicate is copied from `list_all_threads` character for character, per the standing rule in `20260718-1700`.
**WHICH ROOTS COUNT — the rule that makes the badge clearable:** only roots the Threads panel actually LISTS (has a reply, or a state row that MEANS something). Counting anything the panel does not show would produce a number with nothing to click, that can never reach zero. Plain chatter therefore does not raise the count — it still pushes and still pops up, it is just not a unit of work. Deliberately STRICTER than `list_all_threads`' own WHERE, which reads `ts.status IS DISTINCT FROM 'todo'` and so treats a root with NO state row as meaningful (pre-existing board looseness — not fixed here, not copied here).
**`general` is deliberately EXCLUDED** from thread-grain counting and stays mention-only in `countTeamNotifications`: 48 top-level messages, zero replies, no per-root read rows — root-grain counting there would light a badge nothing in the UI could clear (the stuck "48" this signal was cleaned up to remove). Regression-tested both ways.

**NOTIFICATIONS — follow/mention targeting is GONE for work channels.** A post in a work channel (new thread, reply, or a Claude answer) pushes to EVERY staff member except the sender, and pops the in-CRM toast wherever they are. The old rule pushed only @mentioned people or, for a reply, the thread's FOLLOWERS — which is why the real production state was: Luca opened 13 bug threads, Antonio follows ONE of them, so Antonio's answers reached Luca but a new bug reached Antonio only if Luca happened to @name him. Silence is decided by ONE predicate, `channelNotifiesStaff` (`lib/team/channel-notify.ts`, pure + client-safe), read by BOTH the send routes and the toast listener so the phone and the screen cannot disagree. Only `td-worker-bug` is silent — machine-written, and a channel that buzzes for every worker hiccup gets muted, which is worse than silent.
**Also: the toast is no longer suppressed on `/portal-chats`** (it was, so a team message arriving while Antonio worked there produced no signal at all — the floating chat is quiet there too). Only the team-chat page stays silent, because you are looking at it. Toasts deep-link to `&root=` when the message belongs to a thread, so the click lands on the BUG, not the stream.

**⛔ `sendPushToAdmin` AND `sendPushToAdminExcluding` ARE DELETED. DO NOT REINTRODUCE A BROADCAST.** They selected rows of `admin_push_subscriptions` by nothing except the table they were in, and carried internal team chat, staff notes on client messages, and client chat previews. That table was not a staff list: `POST /api/admin/push` required only *a* logged-in user, so a partner's browser or a client's portal login could register itself and start receiving all of it. **Production was clean — exactly two devices, Antonio's and Luca's — so nothing leaked; the door was open, not the room.** Replaced by `sendPushToStaffExcept` / `sendPushToStaff` (`lib/team/notify.ts`), which resolve real staff through `listTeamMembers` (the ONE definition that knows partners and clients are not staff) and **fail CLOSED** if that lookup errors — a directory outage must never fall back to "everyone registered". All 9 call sites converted (team chat, both internal-notes routes, portal chat + reaction, the MCP portal tool, the agent tool, approval notifications). The push-registration POST is now gated on the new `isStaffUser` — **not** `isDashboardUser`, which only excludes clients and lets a partner through. DELETE stays open to any logged-in user (removing your OWN device, scoped to your id). `/api/admin/push/test` now pushes to the CALLER only instead of buzzing the whole team.
**RESIDUAL, stated not hidden:** the staff rule is a deny-list, so a future outsider role ('contractor', 'auditor') arrives as staff until added to `NON_STAFF_AUTH_ROLES`. Adding any non-employee login MUST include that line in the same change.

**CLAUDE CAN NOW ANSWER INSIDE A BUG.** `postTeamMessage` (the one choke-point for the MCP tool AND the worker tool) takes `root_id`: the answer lands in that bug's thread instead of as a detached top-level message in the stream, and the recipient's notification opens the bug. The root is validated to belong to the resolved target (a wrong id cannot graft an answer onto another channel's bug), a reply-to-a-reply flattens to its root (same 2-level rule as the human route), and an answer into an ARCHIVED thread un-archives it (same rule as the human route — otherwise the message is accepted and pushed while the thread stays hidden). `root_id` + `dm_user_id` is refused in the pure validator: a DM has no threads inside it. `team_chat_read_thread` now prints the exact `channel` + `root_id` to answer with, because reading a bug is nearly always the step before answering it.

**THE CHANNEL OPENS ON ITS LIST OF BUGS**, not the flat stream (the panel already existed with titles/status/assignee/new-marks — it was behind a button). The stream is one click away via the panel's Close; a deep link straight to a bug still wins (`openThread` closes the panel, and the selection effect does not cover a pending deep link). DMs and client conversations still open on their messages — they are conversations, not lists of work.

**Invariants preserved:** `?mark_read=0` and read-on-real-engagement; per-root read pointers; the partner exclusion stays at the source; soft-deleted tombstones; own realtime topics.)_
_Prior: 2026-07-23b — Claude (**The floating chat gained an EXTERNAL "open this thread" entry point** so "Discuss this note" (see `staff-notes.md` 2026-07-23c) can open a specific conversation. Mechanism: a cancelable document event, name defined once in `lib/team/open-team-chat.ts` (`requestOpenTeamChat`), received by a listener in `components/team-chat/floating-chat.tsx` that opens the desktop WINDOW or the mobile SHEET, seeds the composer draft (never clobbering a half-typed one), invalidates the thread list (a just-created conversation isn't cached yet), and marks read (a deliberate click IS engagement). The listener `preventDefault`s ONLY when it can show the thread — NOT on `/team-chat` where the component renders null — so the dispatcher knows to navigate to the full page instead when the window is off or absent. No other chat behaviour changed.)_
_Prior: 2026-07-23 — Claude (**READING IS A GESTURE NOT A SCROLL, a client chat now actually notifies, there is ONE find-or-create, and parking no longer marks a thread read — ALL LIVE on production 2026-07-23.** The parking fix needed migration `20260723-1000-per-person-note-state-and-thread-later.sql`, run by Antonio in the Supabase SQL editor before the code shipped. **Migration first, code second** — always, for anything touching these tables.

**⛔ A MESSAGE WAS MARKING ITSELF READ — the exact failure the floating window exists to prevent, wired in on day one.** An arriving message changed the message count → the list auto-scrolled → that PROGRAMMATIC scroll fired the container's `onScroll`, which was wired straight to mark-read. A message landing while nobody was at the desk cleared its own badge, after the push had already gone. **Engagement is now a human GESTURE — pointer / wheel / key — never a scroll.** A timestamp window to "ignore the scroll we caused" was considered and REJECTED: it needs a tuned constant, breaks the moment anyone adds smooth scrolling, and discards a genuine read landing inside the window. `scrollIntoView` emits no pointer/wheel/key event; a human emits one every time.
**ORDER MATTERED:** opening a chat previously marked it read ONLY via that same scroll side effect, so removing the scroll signal alone would have left the badge permanently stuck. `onPickThread` now marks read explicitly, and that landed FIRST. `markRead` is also throttled per thread — gestures arrive in bursts and each call was a POST plus a full thread-list refetch (which re-reads the staff directory server-side).

**A CLIENT CHAT NOTIFIED NOBODY.** A discussion pushes to its PARTICIPANTS, and participation is simply "you hold a read row" — but creating a conversation wrote no rows at all, so starting a chat about a client pushed to no one and lit no badge. Other staff are now seeded at the EPOCH with `ignoreDuplicates` (the share route's proven shape), inside `findOrCreateConversation`, on CREATE ONLY — re-seeding a reused conversation would dump its whole history back as unread. Accepted cosmetic consequence, recorded in code: the automatic 🗂️ opening marker counts as 1 unread for the other person.

**⚠️ ONE FIND-OR-CREATE. `/api/team/conversations` used to hand-roll its own, keyed on `resolved_at IS NULL` — the exact predicate `lib/team/find-conversation.ts` documents as WRONG**, because it forks a SECOND live conversation the moment one is marked Solved, while Share reuses and reopens the first. Messages then split across two identically-labelled threads, invisibly. That helper's header says "do not add a third"; this was the third. The route now delegates to it, which fixes the fork, gives participant seeding to both callers, and exposes `forceNew`. The floating dialog no longer drops the `reused` flag: it says an open chat already exists and offers **Continue** or **Start a separate one**.

**PARKING A THREAD MARKED IT READ (pre-existing).** `later` lived on `internal_thread_reads`, whose `last_read_at` is NOT NULL DEFAULT now(), so the INSERT recording "park this" also stamped "read everything in it" — the opposite of Later, on its own stated use case. Writing the epoch into that same upsert would NOT fix it: an upsert writes the columns you send, so on an EXISTING row it drags a real read pointer back to 1970. And a read row is ALSO PARTICIPATION, so seeding one would subscribe you to that conversation's phone alerts with no way off. The flag therefore gets its own sparse table `internal_thread_later` (migration `20260723-1000-...`), mirroring `internal_root_later` (20260718-1700) which exists for this identical reason one grain down. `internal_thread_reads.later` is FROZEN — still projected by `get_team_threads`, no longer written; `GET /api/team/threads` OVERLAYS the new table so no function rewrite was needed. Dropping the column must rewrite that function in the same change.

**Also:** the chat launcher is now DRAGGABLE and its default moved off the composer band — it had been sitting on the Send button of the Portal Chats composer, which is how a client gets answered. See `staff-notes.md` (2026-07-23) for the shared drag hook and the stale-state bug found while building it.)_
_Prior: 2026-07-22c — Claude (**THE FLOATING WINDOW NOW OPENS EVERY CHAT, NOT JUST THE DM — and this is the entry that records why DM-threading was REJECTED. dev job d6466b59.**

**Antonio's model, verbatim:** "If it's something between me and Luca, it stays between me and Luca. If I'm opening a chat with Luca for a client, it could go under the conversation in Team Workspace under that client." Plus: "the staff is not a problem because the staff today is me and Luca" — privacy is NOT a requirement here; TD LLC is 2 people, so DM / conversation / channel are equally private in practice.

**What shipped:** "New chat" asks for a CLIENT (optional) + an optional subject. Client → `POST /api/team/conversations`, which already REUSES an open discussion for the same client+topic (`conversations/route.ts:67-77`) instead of forking, and already seeds a 🗂️ "Conversation started" message carrying who + when (`:117-124`). No client → the existing DM. The window lists both (`ChatList`), and the badge now counts DMs **plus live client conversations** — `windowUnreadCount` in `lib/team/chat-window-threads.ts`. Also: auto-growing composer, emoji at the caret, and a per-message ⋯ menu (reactions / copy / make-a-note / edit+delete on your own), always-visible because there is no hover at ~380px.

**⛔ DO NOT BUILD THREADS INSIDE A DM. This was proposed, fully reviewed, and rejected — do not revive it without reading this.** A 4-core council returned UNANIMOUS fix-first with 11 blockers, each verified against code:
1. **Two read pointers on one DM.** `get_team_threads` computes DM unread PURELY at conversation grain (`20260712-1600-team-threads-client-bucket.sql:49-58`); threads read at ROOT grain. Opening one chat would clear another chat's unread — the exact message-loss the floating window exists to prevent — or, mirrored, leave the badge permanently lit.
2. **`new-thread` marks the whole DM read** (`new-thread/route.ts:95-97` upserts `internal_thread_reads`). Harmless for channels, fatal in a DM.
3. **A "General" bucket cannot exist without a migration.** Every chat is IDENTIFIED by a root message id (`internal_thread_state.root_message_id` is PK + FK; follows, Later and per-root reads all key on it; deep links need both ids). A virtual General has no root, so the one chat holding ALL history is the ONLY one that could never be renamed, followed, archived or deep-linked.
4. **EIGHT enforcement sites, not three.** Beyond the route guards (`new-thread:49`, `thread-state:60`, `thread-follow:35`, `thread-remove:14`) there are LIVE SQL functions hard-filtering `thread_type IN ('channel','general')` — `list_all_threads`, `list_later_threads`, `list_followed_unread_threads` (9 occurrences across 5 migration files). Miss them and named DM chats are invisible on the Board, absent from Later, and never light the menu dot, while the UI renders all those controls.
5. **Push tags collapse** (`messages/route.ts:178`, `team-dm-${threadId}`): two chats' notifications replace each other on the lock screen.
6. **Mute would be dead**: the `dm` push branch (`:171`) precedes and shadows the follower branch (`:181`), so unfollowing a DM chat still buzzes. (This also REFUTED the pre-review worry that DM push would stop working — it would not.)
7. Badge double-count once DM follows are honoured; auto-follow has silently been writing DM rows already (`messages/route.ts:147-152`); older chats blank past the 500-message window; `filterStreamRoots` would make today's flat DM stream vanish.
**Live-source correction to the council (R093):** a reviewer asserted existing DM history already carries scattered roots from @claude/quote-replies, making a backfill dangerous. The MECHANISM is real, but production had **23 DM messages across 2 conversations with ZERO carrying a root** — the reviewers reasoned about years of history that does not exist.
**And the decisive fact, found by querying rather than reasoning:** Antonio ALREADY HAS named chats with titles — in channels, used daily (td-bug 27 msgs/14 thread-replies, td-taxreturn 25/12, td-dev 19/7), and client discussions number 91 threads / 233 messages / 64 with an explicit topic, against 2 DM threads. The mess was one unstructured room next to structured ones, not a room lacking sub-rooms.

**Invariants this pass preserves (do not regress):** every message fetch still passes `?mark_read=0` and read is marked only on real human engagement; own realtime topic with INSERT **and** UPDATE; soft-deleted messages render the tombstone, never the retracted text; per-conversation drafts; partner exclusion stays at the source (`isStaffAuthRole`) and a caller-side role filter is NOT a second line of defence; own crash guard that degrades VISIBLY.
**Badge grain rule, decided explicitly:** a badge counts exactly what its surface can OPEN. Counting less leaves a message unread with no badge; counting more sends the user hunting for something that is not there.)_
_Prior: 2026-07-22b — Claude (**⛔ PARTNERS WERE BEING TREATED AS STAFF — fixed at the source. Found in production the same day the floating chat shipped, BY the floating chat: its person picker offered "cristian", a partner in TD Communication.**

**Root cause, and it was never the chat window:** `lib/team/directory.ts::listTeamMembers` filtered out only `role === 'client'` and then COERCED every survivor to `'admin' | 'team'`. A managed partner authenticates with `app_metadata.role='partner'` (see `lib/partner-auth.ts::isPartner`, which already read it correctly) and is confined to `/collab` by middleware — but this list handed him back RELABELLED AS STAFF.

**Blast radius — every caller of `listTeamMembers` treats it as the staff list:** @mention autocomplete AND mention push targeting, `POST /api/team/dms` validation, thread assignee (`thread-state`), `/api/team/share`, and — the worst one — **staff sticky-note sharing** (`app/api/crm/staff-notes/route.ts`), which would hand a note the author marked PRIVATE to a partner and push its body to his phone. Note that route filters `admin|team` *believing* that excludes partners; it never did, precisely because of the coercion. **A caller-side role filter is NOT a second line of defence here — the role is derived in the directory. Keep the exclusion at the source.**

**Fix:** new pure, client-safe `isStaffAuthRole` + `NON_STAFF_AUTH_ROLES` in `lib/team/workspace.ts`, consumed by the directory AND by the chat window's picker so server and UI cannot drift. **Deliberately a DENY-LIST, not an allow-list:** an absent/unknown role still counts as staff, preserving behaviour for legacy accounts whose role predates the field (the old rule was simply `role !== 'client'`), so this closes the partner hole and changes nothing else. **Residual risk, stated in the code and here:** a FUTURE outsider role ('contractor', 'auditor') would again arrive as staff until added to `NON_STAFF_AUTH_ROLES` — adding a non-employee auth role MUST include that line in the same change.

**⚠️ WHY THIS SHIPPED — the transferable lesson, worth more than the fix.** There WAS a unit test asserting a member with `role: 'partner'` is dropped, and it passed. But the real directory never emitted `'partner'` — the coercion meant that branch was UNREACHABLE in production while the actual partner sailed through labelled `'team'`. **A test whose fixture is shaped by what you assume the caller sends, rather than what it actually sends, tests your assumption.** The real rule is now tested against the genuine production role values (`tests/unit/team-workspace.test.ts`) and MUTATION-VERIFIED: removing `'partner'` from the list fails 3 tests. The old test keeps its assertions but is re-headed to say what it does and does not prove.

**Production auth reality when fixed (verified, not assumed):** exactly 4 non-client users — antonio.durante (admin), qa-staff (admin), support (team), cristian@sirioos.design (**partner**).

**Also in this pass:** the floating chat launcher was a small pill and Antonio could not find it at all ("I don't see anything anywhere to activate a message") — it rendered correctly the whole time but read as a status chip. Now a 56px circular button with a ring and a badged unread count. **Deliberate, documented trade-off:** the sticky-notes layer documents that the toast stack owns bottom-right (which is why the notes pill went bottom-LEFT); this button takes bottom-right anyway, because that is where users look for a chat launcher and discoverability was the reported failure. Toasts are transient; the button is permanent. If the overlap annoys in daily use, move it UP, not back into a corner nobody checks.)_
_Prior: 2026-07-22 — Claude (**FLOATING CHAT WINDOW — a green window onto staff DMs, on every dashboard page (SANDBOX ONLY; NOT on prod). dev job 774d68e7.** Antonio wanted the sticky-note experience for chatting with Luca: floats, follows you across pages, and any message converts to a note. Built as a WINDOW ONTO the existing `thread_type='dm'` threads — no new table, no new send path, so push/history/search keep working; a separate store would have made a second inbox. Council (5 reviewers) returned FIX-FIRST with 8 blockers; all fixed before the window was written.

**⛔ THE RULE THAT SHAPES THIS FEATURE — READING IS A WRITE.** `GET /api/team/threads/[id]` upserts `internal_thread_reads.last_read_at` AND clears `manual_unread` as a SIDE EFFECT OF THE FETCH, and `get_team_threads` derives `unread_count` from that pointer. So ANY surface that renders a thread destroys its unread state. For the Team Chat page that is fine (you opened it). For a window that can auto-open, poll, or sit minimized it is fatal: the badge clears for a message nobody saw, and the push has already fired and gone. **New `?mark_read=0` opt-out** (default unchanged → every existing caller byte-identical). The floating window passes it on EVERY fetch and calls the pre-existing `POST .../read` only on real engagement (composer focus / list scroll / send), never on mount, never on poll, never while minimized. **Any future surface that displays a thread must do the same.** NOTE: a council reviewer claimed no per-thread mark-read route existed and one had to be built — WRONG, `POST /api/team/threads/[id]/read` already existed and does exactly this. Verified before building.

**Files:** `components/team-chat/floating-chat.tsx` (the window), `chat-error-boundary.tsx`, and four pure modules — `lib/team/chat-autopop.ts` (`decideAutoPop` → `'open' | 'ignore' | 'refresh'`), `chat-window-position.ts`, `chat-window-threads.ts`, `chat-messages.ts`. 71 unit tests; the two load-bearing ones are MUTATION-VERIFIED.

**Invariants (each one is a council blocker, do not regress):**
- **Own crash guard, and it must stay VISIBLE.** Mounted in `app/(dashboard)/layout.tsx`; `app/(dashboard)/error.tsx` is a PAGE-segment boundary and does NOT catch layout throws, and there is no global boundary — unguarded, one throw white-screens the whole CRM on every page. Deliberately NOT a copy of the notes boundary, which renders `null` forever: for a chat that reads as "nobody is writing to me". Degrades to a "Chat had a problem / Reopen" pill.
- **Own realtime topic** (`floating-chat-dm`) — never `team-workspace`, which the page owns. Subscribes to INSERT **and UPDATE**, or a soft-deleted message keeps its retracted text here after it is gone on the page.
- **Own localStorage position key.** The notes layer prunes its shared map to live NOTE ids every ~60s, so a chat position stored there is wiped within a minute. Clamp against the MEASURED element box — note-position's fixed 0.92 cap puts a 360px window's composer off-screen and persists it.
- **`decideAutoPop` returns `'refresh'`, not `false`, for an unknown thread.** The DM-id set refreshes on a timer, so the FIRST message of a brand-new DM is not in it — and realtime replays nothing, so dropping it loses that message forever. Unknown = "I don't know yet": refresh the list once (guarded against looping) and re-ask. Also: a NULL current-user id must return `'ignore'`, never "not mine → open".
- **Auto-pop is desktop-only, and suppressed on `/team-chat`** (two panes fighting one read pointer) and while an overlay is open or the user is typing. Deliberately NOT suppressed on `/portal-chats` — the global toast listener bails there, which is why a DM arriving while Antonio works in Portal Chats currently produces no signal at all.
- **Person switcher is STAFF ONLY and excludes self.** `GET /api/team/threads` returns `listTeamMembers()` UNFILTERED (the notes API filters to admin|team and re-checks on share) — a partner (Cris) would otherwise appear in a staff DM picker. Self-exclusion is not cosmetic: `POST /api/team/dms` will create a you-and-you thread that can never deliver anything. **Both are client-side only today — the endpoint still permits a partner DM and a self-DM and should reject them server-side.**
- **The pill counts DM unread ONLY** and is labelled so. Do NOT reconcile it with the sidebar dot, which mixes grains. And NEVER count from `internal_messages.read_at` — the send route stamps it at insert, so that count is permanently zero.
- **z-46**: above the notes layer (45) so a dragged note cannot cover the composer, below AI panel (55) / palette (60) / note dialogs (70/80) so it can never trap a modal's buttons. Anchored bottom-right-of-centre — bottom-left is the notes pill, far bottom-right is toasts. Mobile is pill + sheet, no drag, no auto-pop.
- **Per-conversation drafts**, because the window changes subject on its own when a message arrives. Failed send keeps the text and surfaces the server's error (R099).
- **Notes made from a message take the client from the CURRENT PAGE**, never the conversation: a DM thread has NO `account_id`/`contact_id` (`findOrCreateDm` writes only type/key/title/creator). The originally-promised "note tied to the conversation's client" is impossible on DMs.

**Known weakest point:** there is no global "a modal is open" signal in this app, so the interruption guard is a DOM probe for dialog-ish overlays. Expect to tune it after real use.
**NOT BUILT YET (Ship 2 remainder):** whole-conversation note, clickable note back-links (notes have stored `origin_url` since day one and NOTHING has ever rendered it — only the dev board does, via `isSafeInternalUrl`), colour picker + the missing PATCH `color` action (colour is settable only at creation today). The colour CHECK constraint was DROPPED by decision: the renderer already degrades safely, and it would turn a harmless unknown value into a live 500 during a hand-run prod migration.)_
_Prior: 2026-07-20c — Claude (**Thread "Copy link" + a Claude Code read tool (SANDBOX; NOT on prod). dev job e08ab690.** Antonio: he keeps having to re-explain a bug (e.g. the Aumianna LLC / VictoriamRoas LLC thread) to every new Claude Code session instead of just pointing at it. The permanent deep-link format (`/team-chat?thread=<channel>&root=<root>`) already existed for internal navigation (notifications, board cards) but was never user-copyable — opening a thread never updated the browser address bar either, so there was no way to hand-copy one. **Build:** (1) a "Copy link" row in the thread ⋯ menu (`ThreadActionsMenu`, both the Threads-panel row and the open thread pane header) that writes `${origin}/team-chat?thread=<channelId>&root=<rootId>` to the clipboard — `channelId` threaded down from the currently-selected conversation at both call sites, no new prop plumbing beyond that. (2) new MCP tool `team_chat_read_thread` (`lib/mcp/tools/team-chat.ts`, alongside the existing `team_chat_send`) — READ-ONLY, staff-only by nature of who runs Claude Code. Takes the pasted link, parses it with the pure `lib/team/thread-readout.ts::parseThreadLink` (accepts a full URL or bare query string; rejects a link missing either id — e.g. the plain page URL with nothing open), fetches the channel + root message + replies + thread-management state (status/assignee/title) from the existing tables (no schema change), resolves the title through the SAME `resolveThreadTitle` the UI/board use (so a soft-deleted opening message renders its tombstone, never a leaked body) and the assignee id through `listAllAuthUsers`, then formats it with the pure `formatThreadReadout` — channel, title, status, assignee, then every message `[timestamp] Sender: body`, deleted messages as `(message deleted)`, attachments noted by count only (never their contents/URLs, so a stray sensitive attachment can't ride into the model's context invisibly). **Deliberately NOT built:** resolving a thread from a description ("the Aumianna bug") — only from an actual copied link; a link only exists once "Copy link" has been clicked on that specific thread. 15 new unit tests (parseThreadLink edge cases + formatThreadReadout tombstone/attachment/assignee rendering) + full suite (6502) green, build green. Sandbox first; Antonio QAs + ships.)_
_Last verified against code: 2026-07-20b — Claude (**BUGFIX: Threads panel status pill (Open/Working/Pending/Done) showed threads of OTHER statuses too (Antonio reported: clicking Working showed threads whose own pill said Open). Root cause: the 2026-07-17d filter design deliberately unioned every named-status filter with "OR unread" so a new reply could never be hidden — `t.status === stage || t.unread`. That union is what actually put wrong-status threads under the wrong pill: any unread thread appeared under every filter regardless of its real status. Fixed to a strict match, `t.status === stage` — a status pill now shows only that status. Unread visibility is unaffected: the dedicated "New" pill still shows unread-only, and unread rows still render bold + blue dot everywhere. `app/(dashboard)/team-chat/page.tsx` ThreadsPanel filter, no server/DB change.**)_
_Last verified against code: 2026-07-20 — Claude (**The @claude email rail was silently DEAD for every account-scoped thread.** `claude-trigger.ts` resolved the thread's addresses by filtering `contacts` on an `account_id` column that DOES NOT EXIST — the link is the `account_contacts` join table. PostgREST errored, only `data` was destructured so the error vanished, and the empty address list is deliberately treated as "refuse every address": it failed CLOSED but invisibly, so nobody noticed no email could ever go out from here. Now resolved through `account_contacts`. Two other places had the identical bug (the CRM sidebar rails and Drive folder naming); a repo-wide test — `tests/unit/contacts-account-link.test.ts` — now fails if ANY file filters `contacts` by `account_id`, and it found the third instance itself. Nothing else about Team Workspace changed: the send rails, the atomic placeholder claim and the rescue windows are untouched.**)_
_Last verified against code: 2026-07-19c — Claude (**Full catalog reach is now ON for the @claude Team Chat worker (dev job 74701b48).** Reach was one global variable shared with Slack and the CRM panels; it is now per-surface via `fullReachEnabledFor('team_chat')` in `lib/ai-agent/full-reach.ts`, defaulting ON like every other surface — Antonio: the worker has the same capability wherever it is. Reach is DISCOVERY only: what may actually RUN is decided per call by the reviewed allow-list in `tool-risk.ts` (57 of 216 auto-run; everything else asks, and with the action rail off it is refused outright). Switch this surface off on its own with `WORKER_FULL_REACH_TEAM_CHAT=false`. No other Team Workspace behaviour changed in this pass — the send rails, the atomic placeholder claim and the two rescue windows from 2026-07-19a are unchanged.**)_
_Last verified against code: 2026-07-19a — Claude (**WORKER SAFETY FLOOR (dev job 17459c25) — five live defects found by a full council pass on the capability-parity plan, fixed BEFORE any capability work. (1) THE CLIENT BOUNDARY WAS DEAD CODE: `clientScope` was built by the Portal Chats route and gated on in `executeWorkerTool`, but the `sendContext` literal in `callWorker` never copied it across, so the gate read `undefined` and NEVER FIRED — the a6c3d75b control had never once run in production. It shipped green because the route spreads `sendRails` as a VARIABLE (no TS excess-property check) and `client-scope.test.ts` only exercises the pure functions. FIX: context construction extracted to exported `buildWorkerSendContext()` so the WIRING is testable; `tests/unit/worker-send-context.test.ts` asserts each control survives the handoff AND refuses a foreign client through the real `executeWorkerTool` entry point (verified: 4 of 8 cases fail without the fix). ANY new control field on `WorkerSendContext` must be copied there and asserted there. (2) TEAM CHAT SENT WITH NO PINS: `claude-trigger.ts` passed `enableEmailSend`/`enableSlackSend` with no `pinnedEmailRecipients`, no `pinnedPortalRecipient` and no `sendActor` — so the recipient check was skipped entirely, portal sends used MODEL-supplied ids, and the language guard + send latch (both conditioned on a pin existing) were INERT. Now: portal rail only when the thread is client-linked, hard-pinned to that client; email rail only when the linked client has addresses, pinned to exactly those (`[]` stays a real refuse-all pin, never `undefined`); rails OFF entirely on an unlinked thread; `sendActor` always set. NOTE the earlier planning claim that the INBOX lacked client context was WRONG — the Inbox is the one surface with a hard fail-closed pin. (3) SIDEBAR ACTED WITHOUT ASKING: with `WORKER_SIDEBAR_ENABLED` off (its default) the sidebar runs the legacy `callAgent` engine, which dispatches straight to `executeTool` — `send_email` reached real clients with no approval. Legacy path is now READ-ONLY via `LEGACY_AGENT_BLOCKED_TOOLS` (filtered from the tool list AND refused at dispatch); memory/log writes deliberately still allowed. NOT fixed by flipping the flag: the worker path is gated `&& !attachment` and `runSidebarWorker` forwards no images/documents, so flipping it would silently REMOVE working attachment support. (4) BUG REPORTS LEAKED CLIENT PII: wall reports quoted staff message + draft reply verbatim into `td-worker-bug` with no approval and no fencing, and team-chat history is fed to the worker RAW — so a stranger's injected text could reach a surface with more reach than the one that read it. Now redacted via pure `lib/team/redact-identifiers.ts` (split out because `worker-bug-report.ts` is `server-only` and therefore untestable) and wrapped in evidence fences. (5) TEAM WORKSPACE DOUBLE-RAN: the rescue cron re-fired any placeholder still `…` after 45s, and the read-then-check guard passed while the first run was STILL RUNNING — both runs executed every tool call. Now an atomic compare-and-set claim (`…` → `⋯`), with TWO rescue windows: unclaimed after 45s (nothing started), claimed after 10min (processor died). The team-chat UI treats both markers as the thinking spinner. 6327 unit tests pass; build clean.**)_
_Last verified against code: 2026-07-18f — Claude (**"Bring forward" + "Mark unread" on a single THREAD and MESSAGE (SANDBOX; NOT on prod). dev_task 680a9895.** Antonio: "I want to put any thread or message forward and read or unread" — clarified via AskUserQuestion that "forward" means BUMP BACK TO THE TOP (a personal resurface marker), not forward-to-someone. **Verified before planning:** both actions already existed but ONLY at the WHOLE-CONVERSATION grain (`internal_thread_reads.later` / `.manual_unread`, set from the sidebar kebab); neither reached a thread inside a channel or a single message. Forward-to-someone does not exist at all (only `/api/team/share`, which pulls INTO team chat from Inbox/Portal Chats — the opposite direction) and is OUT OF SCOPE per his answer. **TWO DIFFERENT HOMES, deliberately (migration `20260718-1700-thread-later-and-manual-unread.sql`, transaction-wrapped, sandbox-applied; prod pending):** LATER gets its OWN sparse table `internal_root_later(root_message_id → internal_messages ON DELETE CASCADE, user_id, PK both)` + per-user index + staff RLS + realtime — mirroring `internal_root_follows` and for the SAME reason: `internal_root_reads.last_read_at` DEFAULTS to now(), so a flag stored there would mark a never-opened thread's replies READ. MANUAL_UNREAD is genuinely read state, so it IS a new column on `internal_root_reads` (creating that row to mark a never-opened thread unread is harmless — it is already unread). **⚠️ THE HAZARD THIS JOB WAS BUILT AROUND: thread unread is READ IN THREE PLACES** — `computeThreadMeta` (TS: panel + in-stream affordance), `list_all_threads` (board), `list_followed_unread_threads` (menu dot + dropdown). A flag honoured in only some means the dot and the lists disagree — same class as the 2026-07-18e archive bug. ALL THREE updated together, and the dropdown's count uses `GREATEST(computed, manual?1:0)` so a hand-marked row is never labelled "0". **New:** `list_later_threads(p_user_id)` (own function — a personal, short, cross-channel list; folding it into the 300-row board query to render a handful would be wasteful), excluding archived threads server-side. **Server:** `POST thread-later {root_id, later}` (root-only guard, presence=flagged), `POST thread-unread {root_id, from_message_id?}` — with a message id it is Slack's MARK UNREAD FROM HERE, rewinding the pointer to 1ms before that message, and the message is validated to belong to THIS thread (root or one of its replies) so a caller cannot rewind using an unrelated timestamp; `thread-read` now CLEARS `manual_unread` on open (mirrors the conversation-level read route); `GET /api/team/later-threads`. **Client:** "Bring forward" / "Remove from Later" + "Mark unread" in the ⋯ thread menu; "Mark unread from here" on every message INSIDE a thread pane (desktop hover row + touch menu); marking unread CLOSES the pane (leaving it open would instantly re-read the thread and undo the request); the sidebar Later section now renders brought-forward threads under the existing conversation rows, each labelled `#channel · Stage`. **NAMED RISK, told to Antonio up front:** the Later list now holds TWO GRAINS side by side — whole conversations and single threads. Mixing grains is exactly what made the Board confusing on 2026-07-18; mitigated by the channel label on every thread row. **PUSHED BACK ON and NOT built:** a personal saved-messages list — pin, Later, thread stage, follow and unread are already five flags, and a sixth place to look is where this stops being useful; to keep one message, bump its thread. **Verified on sandbox with a live harness:** baseline read → not unread anywhere; hand-marked → unread on the board AND the menu dot together; Later list returns the thread with its channel label and unread state; board exposes the `later` flag; opening the thread clears it everywhere; zero leftover rows. 6223 unit + build green.)_
_Last verified against code: 2026-07-18e — Claude (**BUGFIX: an archived thread stayed in the CHANNEL (prod, reported by Antonio minutes after 2026-07-18d shipped). dev_task ea635437.** Archiving worked and the panel showed it correctly attributed, but the thread kept rendering in the channel stream with its reply count — in BOTH the normal and the archive view. **Root cause, self-inflicted during the council fix pass:** hiding a root from the stream needs the COMPLETE set of archived roots. The GET returned exactly that (`archived_roots`); a reviewer flagged it as "two representations of one fact" alongside `threads[].archived` and I accepted the simplification, deriving the hidden set from `threadsList` instead. But `threads[]` is a FILTERED view — archived rows are dropped from it unless `include_archived=1` — so with the archive view OFF the derived set was EMPTY and hid nothing, and with it ON the filter short-circuited to null. They are NOT the same fact: one is a filtered list, the other is the full set. **Fix:** `archived_roots` restored (with a comment naming this incident so it is not folded away again) + the stream filter extracted to the pure, unit-tested `lib/team/thread-meta.ts::filterStreamRoots(messages, archivedRootIds, showArchived)` — replies always dropped, archived roots hidden when the archive view is off, everything shown when it is on so an archived thread can be opened and restored. 5 regression tests including the explicit "an empty set hides nothing, so the caller MUST pass the full set" case. The archive action now also moves `archivedRoots` optimistically, so the thread leaves the channel on the click rather than on the next refetch. **Lesson (recorded because it is the transferable part): a reviewer's "this is duplicated state" is a hypothesis about behaviour, not a fact — two fields that look redundant can differ in FILTERING, and accepting the simplification without re-testing the behaviour shipped a feature that never worked in either state.** 6210 unit + build green.)_
_Last verified against code: 2026-07-18d — Claude (**Thread RENAME + ARCHIVE + DELETE, and touch-reachable message actions (SANDBOX; NOT on prod). dev_task ea635437.** Antonio: "I can't delete or edit any threads anywhere." Offered delete-vs-archive he chose **Archive/hide (reversible)**, with true delete reserved for a thread nobody else has posted in. **⚠️ DEPLOY ORDER: SQL FIRST, THEN CODE.** Migrations `20260718-1400-thread-rename-archive.sql` (transaction-wrapped; adds `internal_thread_state.archived_at` + `archived_by` + partial index, rewrites both list RPCs, DROPs+recreates `list_all_threads` with a 3rd `p_include_archived` arg) and `20260718-1500-thread-delete-atomic.sql` (`delete_thread_if_sole_author`). Code-first fails LOUDLY by design now (see below) but the board and thread details still break until the SQL lands. **Rename:** `title` on the state row; `POST thread-state` accepts `title` (blank clears → falls back to the opening message); ONE resolver mirrored in `lib/team/thread-title.ts::resolveThreadTitle` + both SQL lists, with unit tests asserting the exact shared literals ('Attachment', 'Message deleted') after a real drift ('📎 Attachment' in TS vs 'Attachment' in SQL) was caught in review. **Archive:** positive `archived_at` marker (never row-absence — absence resurrects on the next reply and has no restore path); hides the thread from the channel STREAM, the Threads panel, the board, and the followed-unread dot; **a reply auto-UNARCHIVES** (send route) so a message can never land somewhere nobody can see it; archive attribution (`archived_by` + date) is rendered on the archived row because an archive removes a thread from EVERYONE's view; restore from the panel's Archived toggle OR the board's Archived view (cross-channel restore path). **Delete:** `DELETE thread-remove` → `delete_thread_if_sole_author`, which does the guard and the delete IN ONE TRANSACTION — the first cut checked in JS then deleted in a second round-trip, a window wide enough to destroy a teammate's reply. Refuses `not_author` / `not_a_thread` / `has_other_replies`; counts other people's SOFT-DELETED replies as "someone else" (their preserved row IS the R100 audit trail); NULL sender counts as someone else; replies scoped to the root's own `thread_id`; a straggler reply from the sub-ms race is PROMOTED to a top-level message rather than deleted or orphaned. Admins get NO override — an admin deleting a colleague's replies is the data-loss case, not an exception. Hard (not soft) delete is correct here and does NOT erode R100: R100 is scoped to CLIENT-visible content and explicitly permits hard delete on internal-only tables, and a soft-deleted root would leave a permanent "Message deleted" tombstone — the exact artifact the user clicked Delete to remove. **STRUCTURAL FIX — the sparse-delete rule is GONE.** `thread-state` used to DELETE the row on revert-to-default to keep the table sparse; that caused three bugs (created thread vanishing, rename reverting, archive undoing itself), each patched by another term in a growing "don't delete if…" guard that a concurrent write could lose a race against anyway. Rows are now KEPT and every list asks whether the row is MEANINGFUL (`threadStateIsMeaningful` + the matching WHERE clause), so an all-default row is indistinguishable from no row — which also fixes archive→restore stranding a stray message on the board as a phantom thread. The route now writes ONLY the fields the caller sent, so a status change can't blank someone else's rename. **Touch:** message actions were hover-only (`opacity-0 group-hover`) and unreachable on Antonio's ~380px PWA — desktop keeps the hover row, touch gets an always-visible ⋯ opening ≥44px rows, with ONE menu open at a time (per-row state meant row A's full-screen backdrop swallowed the tap on row B), a dismiss layer on the reaction picker (no hover-out on touch), and a confirm step on every delete. **Two pre-existing bugs fixed:** the panel's root query omitted `sender_id`, so your OWN new thread read as unread to you; and `list_followed_unread_threads` titled rows from the message body, leaking the OLD text into the menu dropdown after a rename. **R099:** the board loader and the channel GET's state query now FAIL LOUDLY — both previously returned an empty/200 result on error, which would have made a missing database change look like "the workspace forgot every thread name" with no error anywhere. **Council:** 4 core reviewers, all fix-first; every cited blocker closed; the one contested claim (does a PostgREST upsert reset omitted columns?) was settled EMPIRICALLY against sandbox — it preserves them, so archive can't wipe a rename. **Verified on sandbox (SQL + a live guard harness):** archive hides from board + followed-unread and restores cleanly; renamed titles appear in BOTH lists; and all five delete cases behave — not_author / has_other_replies / soft-deleted-other-reply still refused / target-is-a-reply refused / solely-mine deleted with zero leftover rows. 6168 unit + build green. **DEFERRED (named, not done):** attachment blobs of a deleted thread are left in Storage (matches R100's opt-in Phase 2); archived threads remain searchable (deliberate — archive ≠ delete).)_
_Last verified against code: 2026-07-18c — Claude (**RESIZABLE thread pane (SANDBOX; NOT on prod). dev_task c1361309.** Antonio, from a screenshot: the thread pane was a hard-coded 384px (`md:w-96`), so a long @claude reply wrapped into a tall one-word-per-line strip. **Build (UI only — no schema, no endpoint, no data risk):** a 6px divider between the channel stream and the pane, `hidden md:block` (mobile pane is full-width — untouched, and Antonio runs the CRM as a ~380px PWA). Drag = **pointer** events, not mouse (trackpad/mouse/stylus) with `setPointerCapture` so the drag survives the cursor outrunning the 6px strip; `touch-none` stops the mobile scroll gesture stealing it; `document.body.style.userSelect='none'` for the drag duration (restored on up/cancel) so dragging over messages doesn't select the page. The pane is on the RIGHT so a NEGATIVE x-delta widens it. Double-click the divider resets to the default. Width persists in `localStorage` under `td-team-thread-pane-width` — read in an **effect after mount**, never in `useState` (reading storage during render desyncs hydration). Applied via a CSS variable + `md:w-[var(--thread-pane-w)]` (verified present in the built stylesheet) so the mobile `flex-1` full-width path stays intact; the pane's `md:border-l` was removed since the divider now IS the separator. **Geometry is a pure unit-tested module** (`lib/team/pane-width.ts`, `tests/unit/team-pane-width.test.ts`, 11 cases): `clampThreadPaneWidth(width, containerWidth)` enforces TWO floors — the pane's own 320px AND a 360px floor for the channel stream beside it (max is derived from the live container width, not a constant) — with the pane floor winning when the container can't honour both; a non-finite/absent/garbage stored value falls back to the default rather than throwing, so a hand-edited or stale key can never break the chat. 6150 unit + build green. Size-gated below a council call (visual-only, no data path).)_
_Last verified against code: 2026-07-18c — Claude (**#td-worker-bug wall reports (dev job a6c3d75b).** New `lib/team/worker-bug-report.ts` posts a THREAD into the existing `td-worker-bug` channel when the worker hits a wall only code can fix: (a) it was about to say something isn't in the system without any SUCCEEDED lookup (caught by the answer-guard), or (b) `assertsCannotDo` — a flat capability refusal ("I can't access external URLs or Slack links"). NOT on ordinary corrections: Antonio rejected that first version — "it's a part of the learning process" — and it would have produced tens of threads a day. Posts a root message + `internal_thread_state` row (own title, `created_as_thread`), deduped per thread+kind+day, best-effort. LOOP-SAFE: inserted directly as `CLAUDE_SENDER_UUID`, NOT through the team send route, so it cannot trigger `triggerClaudeReply` (which only fires on human-authored messages via that route). Never creates the channel — missing channel = skip. `claude-trigger` also now passes `surface: 'team_chat'` + `clientName` for labelling. Full detail in `ai-agent.md` 2026-07-18e.**). Prior: 2026-07-17e — Claude (**BUSINESS BRAIN wiring (dev job 203cda1a): the @claude team worker (`lib/team/claude-trigger.ts`) now CAPTURES a lesson after each reply via the shared `lib/ai-agent/lesson-capture.ts` — if the staff message corrected the prior reply, it saves it (raw prompt + prior reply only). Client-linked thread → CLIENT-SCOPED; general thread → GLOBAL + scrubbed. Separately, the CRM 🧠 reaction (`chat-memory-reaction.ts`, used by Team + Portal) now saves GLOBAL + SCRUBBED via `distillMarkedMessage` (was client-scoped raw) — Antonio: "🧠 = make it global". Full detail in `ai-agent.md` 2026-07-17e.**)._
_Last verified against code: 2026-07-18b — Claude (**"+ New thread" + the Board REPLACED with an all-threads summary (SANDBOX; NOT on prod). dev_task c1361309.** Origin: Antonio opened the Threads panel, found general empty and a td-bug thread marked Working, then filtered the BOARD to #td-bug and got four empty columns. Diagnosis (not a bug): the Board's cards were whole `internal_threads` rows (one card per channel/conversation) filtered on `parent_channel_id`, a DIFFERENT GRAIN from the message-threads inside a channel — so a channel's threads were never on it, and a channel filter could only ever show filed conversations (td-bug had none). Antonio's instinct (go to the board, filter by channel, expect my thread) settled the design: **the Board now tracks THREADS**. **Data (migration `20260718-1100-thread-title-and-all-threads.sql`, sandbox-applied; prod pending):** `internal_thread_state` gains `title` + `created_as_thread`; new `list_all_threads(p_user_id, p_limit=300)` returns every thread across all channel/general threads (channel label, resolved title, status, assignee, reply_count, last_activity, unread, following), bounded newest-activity-first + LIMIT so it can't become an unbounded scan. **Council blockers closed:** (1) a created thread's EXISTENCE can't be "has a non-default status row" — the sparse rule DELETEs that row on revert-to-Open, so the thread would vanish; `created_as_thread` now suppresses that delete (`thread-state` route). (2) A thread's NAME can't be the root message body (editing renames it, deleting makes it "Message deleted" forever) — explicit threads carry their own `title`, derived ones still fall back to the body (one resolver, mirrored in TS + SQL). (3) Unread now counts the ROOT message too, so a brand-new thread shows **bold + dot** for teammates before any reply. (4) Board rows write status/follow to the ROW's channel (`channelId` param), not the selected one. (5) `POST /new-thread` posts ONE opening message (title + optional note) and deliberately does NOT fire the @claude worker from a title, while still resolving @mentions (targeted push deep-linked to the pane), bumping activity, seeding the creator's follow, and registering the thread. (6) Cross-channel open: `openThreadInChannel` parks the request in a ref and a replay effect opens the pane once the channel loads, fetching that one thread via `GET /thread-messages?root=` when its root is older than the newest-500 window; `loadMessages` also PRESERVES an open out-of-window thread across refreshes. **BUG FOUND + FIXED in QA (pre-existing):** the `?thread=` deep-link effect re-asserted the URL's channel on EVERY threads refresh, so any later channel switch snapped back — made cross-channel open look broken. Now one-shot (ref guard), same as the `&root=` link. **Retired:** the whole-conversation kanban setter (`setWorkStatus`); `internal_threads.work_status` + its PATCH remain but are no longer reachable from the UI. **Browser-verified on sandbox:** New-thread dialog creates "Problem with Inbox" which appears instantly with 0 replies and auto-opens its pane; Board lists threads from general + td-bug with channel labels, bold+dot on unread, stage columns; filtering to #td-bug now shows BOTH its threads (the original complaint); clicking a #general card from td-bug switches channel and opens that thread's pane. 6105 unit + build green.)_
_Last verified against code: 2026-07-18 — Claude (**Team Chat MENU dot switched ON for followed threads (SANDBOX; NOT on prod). dev_task c1361309.** Antonio asked for it after living with the in-channel signal; the council had deferred it as the "noisy 48" risk, so it is deliberately scoped to FOLLOWED threads only — ordinary channel chatter still lights nothing. **Data (migration `20260718-1000-followed-unread-threads.sql`, sandbox-applied; prod pending):** `list_followed_unread_threads(p_user_id)` — its OWN function, NOT a widening of `get_team_threads` (follow + unread live at the ROOT-MESSAGE grain; the RPC is whole-thread grain). It replicates the four guards the rest of the unread model uses: the **per-root** read pointer (`internal_root_reads`, never the channel pointer — so opening the channel can't clear an unopened thread), `deleted_at IS NULL`, `sender <> caller`, and `thread_type IN (channel,general)`. Returns one row per followed thread with unread replies (root id, thread id, label, title w/ tombstone, unread count, last reply) so the SAME query drives both the dot and its dropdown — they can never disagree. Partial index `idx_internal_messages_root_only` keeps the badge poll cheap. **Wiring:** `app/api/dashboard/badges/route.ts` adds `rows.length` to `teamChat` (the sidebar renders `teamChat > 0` as a DOT, not a number — so no cross-grain count reconciliation / mention double-count is needed, which was the council's blocker); `app/api/team/notifications/route.ts` appends the rows as items; `TeamNotifItem.kind` gained `'thread'` and the sidebar dropdown renders a sky **THREAD** badge, deep-linking `?thread=<ch>&root=<root>`. Both wirings are best-effort (try/catch) so a failure never breaks the badges payload or the list. **Browser-verified on sandbox:** the Team Chat nav item shows the dot; clicking it opens "NEW FOR YOU" with a THREAD-badged row naming the channel + thread and deep-linking to its pane. 6105 unit + build green.)_
_Last verified against code: 2026-07-17d — Claude (**Thread FOLLOW + stage FILTER + bold/dot new-activity (SANDBOX; NOT on prod). dev_task c1361309 (child of d53174c8).** Antonio: the Threads panel needs to filter by stage, per-person FOLLOW a thread, and show new activity clearly. **Design (council-approved, all blockers folded in):** **(1) Follow is the SINGLE source of truth for the channel thread-reply push** — it REPLACES the old "push every prior participant" heuristic (additive would make Unfollow a lie). Replying AUTO-FOLLOWS (Slack-style); a bell follows/unfollows anything. **(2) Own sparse table** `internal_root_follows(root_message_id PK → internal_messages ON DELETE CASCADE, user_id, PK both)` — presence = following; migration `20260717-2200-team-thread-follows.sql` (+realtime), with a BACKFILL seeding existing participants (root authors + repliers of channel/general roots, minus the Claude sentinel) so live threads keep pinging after the switch. Deliberately NOT a flag on internal_root_reads (that table's `last_read_at DEFAULT now()` would mark a followed-but-unopened thread READ). Follow = INSERT ON CONFLICT DO NOTHING; Unfollow = DELETE; never touches read state. **(3) Top-level Team Chat menu dot DEFERRED** (the "noisy 48" risk) — the new-activity signal lives on the panel + the Threads button (+ bold rows), scoped to FOLLOWED-unread, until proven calm. **Server:** send route reply push targets followers − sender − Claude (`app/api/team/threads/[id]/messages/route.ts`); replier auto-follows (upsert ignoreDuplicates). New `POST /api/team/threads/[id]/thread-follow {root_id, follow}` (INSERT/DELETE, channel/general only). GET `threads[]` returns per-row `following`. **Client:** panel stage filter (All/New/Open/Working/Pending/Done) where **unread is NEVER hidden by the filter** (a status filter shows that status OR unread) + a **Following-only** toggle + a **Follow bell** per row and in the pane header; **unread threads render BOLD + a blue dot** (Antonio's ask — instant identification) in the panel AND on the in-stream "N replies" line; the Threads-button dot re-scoped to followed-unread; realtime on internal_root_follows. **BUG FOUND + FIXED during QA (real, not the follow feature):** deep-linking `?thread=<channel>` auto-selects general first then jumps to the channel — two message loads race, and general's response could land last, showing the WRONG channel's threads/messages under the right header. Fixed with a stale-response guard in `loadMessages` (ignore a response whose thread ≠ the currently-selected one). **Browser-verified on sandbox:** correct per-channel threads after the fix; filter bar + Following toggle; follow bell toggles and the unfollow DELETEs the row (verified in DB); backfill seeded followers. 6105 unit + build green. **DEFERRED (named):** the top-level menu dot; a per-thread push throttle for very active threads (minor). Sandbox first; Antonio runs prod DB change + ships.)_
_Last verified against code: 2026-07-17c — Claude (**Thread MANAGEMENT panel — per-channel thread status (SANDBOX; NOT on prod). dev_task d53174c8 (child of the threads job a358fa9b).** Antonio's driver: the reason to leave Slack is threads get lost — so each in-channel thread needs a visible state. **Design (council-approved, blockers folded in):** each thread (a root message) can be set **Working / Pending / Done** by hand + optionally **assigned**; **"New" is an ORTHOGONAL BADGE, not a status** (a Done thread that gets a new reply resurfaces with a New badge and sorts to the top — never hidden; this was the key council fix, mirroring the existing resolution/work_status split). Shown in a **"Threads" panel** that overlays the channel (toggle in the channel header; channels/general only), each row = status pill (click to change) + title + reply count + last reply + assignee + New badge; click a row → opens that thread's pane. **Data (migration `20260717-2100-team-thread-state.sql`, sandbox-applied; prod pending):** sparse `internal_thread_state(root_message_id PK → internal_messages ON DELETE CASCADE, thread_id, status CHECK todo|in_progress|waiting|handled default todo, assignee_id, updated_at, updated_by)` — a row exists only once set off default (untriaged = no row = Open, no pill); staff RLS; added to the realtime publication (live status so two people don't grab the same thread). **ONE vocabulary:** `TEAM_WORK_STATUS_LABELS` relabeled to Open/Working/Pending/Done + new `TEAM_STATUS_COLORS` in `lib/team/workspace.ts` — one source for board + StatusDot + the panel pill (no third drifting vocabulary; PD ship-condition). **Server:** GET route returns per-channel `threads[]` (status/assignee + **title from a NARROW root-id query, never the 500-msg window**; a soft-deleted root renders a **'Message deleted' tombstone** title, never its body) and folds status into `thread_meta`. New `POST /api/team/threads/[id]/thread-state {root_id, status?, assignee?}` — **PARTIAL** write (setting assignee never clobbers status), assignee validated against the staff directory, `thread_type IN (channel,general)` guard, reverting to default+no-assignee **deletes** the row (keeps it sparse). Panel ordering is the pure unit-tested `lib/team/thread-meta.ts::sortPanelThreads` (unread-first → Working/Pending/Open/Done → newest; `hideDone` drops only READ done). **Browser-verified on sandbox:** panel opens, thread lists with status pill/reply-count/assign, setting Open→Working persists (DB `in_progress`), pill updates live. 6105 unit + build green. **DEFERRED (named):** out-of-window root click (busy channel >500 msgs) → load-around not built (low-volume channels unaffected); StatusDot/board still have their own inline colors (panel uses the central map; full board migration to it is a follow-up). Sandbox first; Antonio runs prod DB change + QAs + ships.)_
_Last verified against code: 2026-07-17b — Claude (**@claude PERMANENT MEMORY (council redo WS1.4, dev job a9477d06).** The Team-Chat @claude worker was handed a `threadId` (the team thread uuid) so `buildThreadContext` + the `recall_thread` tool could work — but NOTHING was ever written to `agent_messages` for team threads, so that store was ALWAYS EMPTY and the assistant remembered nothing beyond the injected last-12-message window (`recall_thread` returned "no earlier turns" — a tool that lied to the model). `lib/team/claude-trigger.ts::processClaudeReply` now records each exchange after the reply: a `done` `agent_messages` row (`sender='crm'`, `recipient='worker'` — isolated from the Slack/Hermes crons; `status='done'` so it never engages the CRM per-thread in-flight lock) storing the RAW prompt + reply (NEVER the enriched userBody, which already carries the last-12 recap — storing that would nest recap-in-recap). Best-effort: a memory-write failure never blocks the reply. Minor, accepted overlap: the recent @claude turn appears both in the last-12 `contextBlock` and in `buildThreadContext` next turn — same info, not conflicting. Prior: 2026-07-17 — Claude (**🧠 SAVE-TO-MEMORY on Team Chat messages (council redo WS1.1, dev job a9477d06).** Reacting 🧠 on a Team Workspace message now saves it to the assistant's memory — Antonio's Slack behavior, brought into the CRM. `app/api/team/messages/[id]/react/route.ts` (already `isDashboardUser` staff-only) calls the shared `lib/ai-agent/chat-memory-reaction.ts::saveChatMessageAsMemory` on a 🧠 ADD (not remove): it resolves the thread's `account_id`/`contact_id` for client scope, attributes the real reactor, and is idempotent per message (`source_type='crm_reaction'`). Best-effort — a memory-write failure never breaks the reaction toggle. The same helper backs the Portal Chats staff 🧠 (gated to staff only there, since clients can also react). Full detail in `slack-claude-worker.md` (2026-07-17e).)_
_Prior: 2026-07-17 — Claude (**Composer attachments: drag-and-drop + paste + failed-send retention (SANDBOX, deployed to td-operations-sandbox.vercel.app; NOT on prod).** Origin: staff Slack report that files (esp. CSV) "can't be uploaded" in td-bug/td-dev. Investigation (live sandbox) proved the paperclip already accepts ALL normal types incl. CSV (30MB CSV → HTTP 200; storage bucket has no mime restriction; send route only host-guards the URL; the block-list in `lib/portal/chat-attachment.ts` bans only active-content/executables). The real gaps were: no drag-and-drop anywhere, no paste, and a silent data-loss bug where a failed send dropped the staged files. **Build:** (1) new pure `prepareChatFiles(incoming, currentCount, cap=5)` in `lib/team/attachment.ts` (+ `CHAT_ATTACHMENT_MAX_COUNT`) — validates at ADD-time via the shared block-list, rejects empty/0-byte (dropped-folder) entries, synthesizes a `pasted-<ts>.<ext>` name for nameless clipboard blobs, and caps against already-staged count; unit-tested (`tests/unit/team-attachment-intake.test.ts`, 8 cases). (2) `app/(dashboard)/team-chat/page.tsx`: one `addPendingFiles` intake used by the paperclip onChange, drag, and paste — surfaces a toast for rejected/overflow files (never silent); `pendingFilesRef` mirror avoids a stale-closure cap race; a final `.slice(0,cap)` on the set is the concurrency safety net. (3) Drag-and-drop by REUSING `components/chat/worker-dropzone.tsx` (added an optional `label` prop, default-compatible) wrapping the messages+composer subtree with `flex-1 flex flex-col min-h-0` so the list still scrolls; `disabled={sending||uploading||!!editing}`. (4) `onPaste` on the textarea — only `preventDefault`+attach when the clipboard carries FILES and no `text/plain`/`text/html` (won't hijack spreadsheet/rich-text paste); short-circuits while uploading/editing. (5) failed-send catch now restores `pendingFiles` via MERGE `[...files, ...prev].slice(0,cap)` (not overwrite) so a mid-flight staged file isn't clobbered and there's no double-append. Type policy UNCHANGED — all three intake paths funnel into the SAME `uploadTeamAttachment` → block-list → public `assets` bucket → send-route host guard (Security council reviewer: strictly equivalent to the paperclip path, no new surface). Council: 5 reviewers, GO, 0 "don't build" blockers; all edge cases folded in. **Browser-verified on sandbox:** drag overlay "Drop to attach" covers the conversation; drop a CSV → stages; drop .exe/.svg → rejected toast, not staged; paste a nameless image → staged as `pasted-*.png`. 6039 unit tests + build green. Follow-ups (separate cards, NOT done): rename WorkerDropZone→ChatDropZone + converge portal-chat's own drag handlers onto it; shared cap constant; block-list defense-in-depth (svgz, empty-ext+empty-mime); team attachments still live on a PUBLIC bucket (pre-existing). dev_task 4bc2d9f3.)_
_Prior: 2026-07-16 — Claude (**Share→Dev-Board target upgraded (shipped with the dev-tracker plain-cards feature)**: `POST /api/team/share` with `{ target: { dev_board: { channel } } }` now (a) INSERTS the card FIRST and lets the AI summarizer patch the plain-English fields (`summary_plain`/`business_impact`/`simple_next_step` + `plain_generated_at`) in a second update — a share click can never hang on AI latency or lose the card to an AI outage; (b) stores the back-link in the new `dev_tasks.origin_url` column (guarded to in-app relative links via `isSafeInternalUrl` — protocol-relative `//` rejected too) instead of only inside description text; (c) exports `maxDuration=60` for the AI patch phase. Everything else about the share endpoint (DM / conversation targets, buildShareCards, push scoping) is UNCHANGED. Full contracts in `dev-tracker.md` (2026-07-16).)_
_Last verified against code: 2026-07-17b — Claude (**Slack-style message THREADS on channels (SANDBOX, deployed to td-operations-sandbox.vercel.app; NOT on prod). dev_task a358fa9b.** Antonio's directive: the Team Workspace CHANNELS (td-dev/td-bug) must thread like the real Slack #td-dev — each message can carry a reply thread; the channel stream shows only the original messages + "N replies"; replies live in a thread pane that opens INSIDE Team Workspace (right column on desktop, full-width on mobile — never a new nav tab). **Scope:** channels + general only. DMs and client discussions keep the flat inline layout + their existing @claude continuation (unchanged). **Council:** the naive "client-side grouping, no schema" MVP was REJECTED (4-core, 2 blockers each) — reply_to_id doesn't always point at a root (Claude/approval inserts point at the prompt), client grouping over the capped load is wrong (the GET even loaded the OLDEST 500, a real pre-existing bug), and one channel read pointer hid unread replies. The shipped design resolves all of them. **Data (migration `20260717-1730-team-chat-slack-threads.sql`, sandbox-applied; prod pending):** `internal_messages.root_id` (plain uuid, NULL = the message IS a root; NO FK, so a future hard-delete of a root can't null its replies into loose top-level messages) stamped at insert on EVERY write path = `parent.root_id ?? parent.id` (send route, claude-trigger placeholder, approval insert) + backfill of existing replies via recursive walk-to-root; `internal_root_reads(root_message_id, user_id, last_read_at, PK both)` = per-user per-thread read pointer (staff-only RLS mirroring the siblings). **Server:** send route stamps root_id + notifies only the thread's participants (root author + prior repliers via `id=root OR root_id=root`, minus sender + Claude) on a channel REPLY — top-level channel posts stay silent (Slack parity); GET loads NEWEST 500 (fixed) + returns `thread_meta` per root (reply_count / last_reply_at / per-user `unread`) computed by the pure unit-tested `lib/team/thread-meta.ts::computeThreadMeta` over ALL replies (not the window) with unread driven by the per-root pointer ONLY (opening the channel never clears an unopened thread's unread); new `POST /api/team/threads/[id]/thread-read` advances the per-root pointer when a pane opens; deep-link `?thread=&root=` is one-shot (ref-guarded, no re-open on poll). **@claude stays in-thread** (placeholder carries root_id); discussion auto-continue left thread-wide (discussions render flat in v1). **Client (`app/(dashboard)/team-chat/page.tsx` only):** channel stream = ROOTS only (incl. soft-deleted roots as tombstones so replies stay attached) + a "💬 N replies · last reply · unread dot" affordance; the shared composer routes to the open thread (`reply_to_id = quoted ?? openRootId`) with a "Replying in thread" bar; realtime reply INSERT bumps the root's count + unread (refs avoid stale closures). **Browser-verified on sandbox (desktop + mobile):** existing Claude quote-replies backfilled into threads; posting root+replies flattens correctly; the pane opens inside Team Workspace, reply posts stay in-thread, "N replies" updates live; tombstone roots preserved. 6045 unit tests + build green. **DEFERRED (phase 2, named):** thread attachments in the pane composer use the shared composer (works), but a very busy channel (>500 messages) can push a thread's replies past the load window — deep-link "load-around" not built; converge portal-chat's own drag handlers is separate. **NOTE:** the stale-message contamination seen during QA was a test-harness artifact (rapid tool-driven `?thread=` SPA nav + a fetch monkeypatch), not a code bug — a clean load shows the correct per-channel content.)_
_Prior: 2026-07-12b — Claude (**Conversations BUCKETS + lead→client AUTO-MOVE (SANDBOX, branch claude/conversations-bucket-automove-f03cb9; NOT on prod) + Slack #td-support IMPORT (DONE on PROD). dev_task be582c5e.**  **Buckets (Phase 1, sandbox):** the Conversations sidebar is split into 8 collapsible top-level sections by a server-computed `client_bucket` — Active clients / Leads / Partners / Individuals (open by default) + Suspended / Cancelled / Off-boarded / Internal (collapsed) — and each conversation shows a status badge. Bucket is computed in `get_team_threads` (migration `20260712-1600-team-threads-client-bucket.sql`, DROP+CREATE adding `client_bucket` + `lead_status`) from the client's real state: partner-type company or `contacts.is_partner` → partner; company by `accounts.status` (Active/Suspended/Cancelled/Closed+Offboarding→off-boarded); a contact who owns ≥1 company (via `account_contacts` ∪ `contacts.primary_company_id`) is bucketed by that company's status, else 'individual'; lead → lead (+ `lead_status`). Pure vocabulary + grouping in `lib/team/conversation-buckets.ts` (unit-tested); sidebar render in `page.tsx` (`convSections` = `groupIntoSections(clientGroups)`, `expandedBuckets` state). "Off-boarded" = Closed+Offboarding; Cancelled kept separate (Antonio). **Auto-move (Phase 2, sandbox):** `lib/team/reanchor-conversations.ts::reanchorLeadConversations(leadId, accountId)` (idempotent: sets account_id, clears lead_id, only rows still `account_id IS NULL`; never throws) is called at ALL FIVE lead→account conversion sites so a lead's conversations follow it into the Clients bucket on conversion: `auto-create.ts` ensureMinimalAccount, `onboarding-setup.ts`, `audit-chain` admin action (all 3 already recorded the lead), `formation-materialize.ts` (**also added the previously-missing `converted_to_account_id`+`converted_at` write**), and `tax-return-intake.ts` (no lead in hand → resolves the origin lead via `leads.converted_to_contact_id = contact_id`). **Import (Phase 3, PROD):** the 55 real #td-support threads were imported as `internal_threads` discussions filed under support@ — 54 imported (Silvia Baldini set aside, not in system), anchored account/contact/lead per an Antonio-approved match (compound overrides: Dragos→Clearview, Luca Comaggi→Everboost, Matteo Angioni→Profit Harbor; Claudio Fiore→contact, Nicolo Parodi→account), topic+topic_slug set, 41 solved / 13 open, each seeded with one write-up message `sender_name='Imported from Slack'` bodied `[Imported from Slack #td-support] …`. Idempotency = `description` marker `Imported from Slack #td-support · ts:<slack_ts>`. **PROD-WRITE GOTCHAS (carved-in-stone):** a multi-row `internal_messages` insert using `JOIN internal_threads … WHERE NOT EXISTS(… internal_messages …)` **times out** — use a plain `INSERT … SELECT FROM (VALUES …)` with explicit `thread_id`; readable write-up text trips the execute_sql `"UPDATE her"` safety guard, and hand-pasting long text corrupts — so messages were inserted base64-encoded (`convert_from(decode(b64,'base64'),'UTF8')`) with a per-row `WHERE md5(v.msg)=v.emd5` self-guard (transit corruption drops the row, never stores wrong client text). Generators: `scratchpad/gen_import.js` + `gen_messages.js`. **PENDING:** ship Phase 1+2 to prod (run migration `20260712-1600` on prod + the branch code) — buckets/badges do NOT show on prod until then, but the imported conversations already group by client+topic on prod via the live Phase-0 view.)_
_Last verified against code: 2026-07-12 — Claude (**CLIENT-CONVERSATION UPGRADE S1–S5 IS NOW LIVE ON PRODUCTION — this CORRECTS the "SANDBOX-only / prod DDL pending" notes in the dated entries below.** Reconciled 2026-07-12 against live sources (R093): production `internal_threads` has all the added columns (`resolution`, `resolved_by`, `topic`, `topic_slug`, `lead_id`) AND the `get_team_threads` function, and the **prod function definition is byte-identical to sandbox** (both 3460 chars, both project `client_key` / `topic` / `client_label`). The slice commits are all on `origin/main` (= production): S1 `2f838a0e`, S2 `e733f007`, S3 `7fd34da9`, S4 `7703ebf4`, S5 `fd2165ed`. So the grouped/topic Conversations sidebar, Solved/Closed, the participant ring+dot, and the topic/state filters are ALL live on prod; migrations `20260710-1900 / -2000 / -2100` were promoted. The S1–S5 dated entries below that say "SANDBOX-only / prod DDL pending" were true WHEN WRITTEN but are now HISTORICAL. **STILL PENDING (unchanged):** the `'Laeds'→'Leads'` prod `catalog_entries` typo fix (a one-row `catalog_update` + `topic_slug` backfill on prod). Verified while scoping the Conversations buckets job `be582c5e`.)_
_Last verified against code: 2026-07-11 — Claude (**Share box gained a "Dev Board" target** (SANDBOX, prod ship pending): `POST /api/team/share` now accepts `{ target: { dev_board: { channel: 'td-bug' | 'td-dev' } } }` and creates a `dev_tasks` board card (bug / new-implementation) with a relative back-link to the client/message, instead of a team thread — reuses `buildShareCards`; available on both the Inbox and Portal Chats surfaces via the shared `ShareToTeamDialog`. Full detail in `dev-tracker.md`. Prior: 2026-07-10 — Claude (**@claude worker: action rail + code-task rail SWITCHED OFF (reversible), Antonio**: the team-chat @claude worker no longer queues actions (`propose_action`) or launches/ships coding jobs — `enableCodeTasks` forced `false` in `claude-trigger.ts` (was Antonio-only per R111), and the in-thread 6-digit approval interception in `app/api/team/threads/[id]/messages/route.ts` is gated on the new reversible switch `lib/ai-agent/worker-actions-switch.ts` (env `WORKER_ACTIONS_ENABLED`, default OFF) — a 6-digit message is now just normal chat while the rail is off. The worker still SENDS (email/portal/team on "go") and investigates on "investigate it"; it lays out steps for anything else. Full detail in `agent-bridge.md` (2026-07-10). Prior: 2026-07-10 — Claude (**@claude can now SEE files shared in a team thread.** This closes the "Slice 2 (tracked, not built): attachments/images from team messages into the worker" item below. Team Chat has always uploaded attachments fine (`lib/team/attachment.ts` → `/api/team/upload-url` → PUBLIC `assets` bucket → `internal_messages.attachments` jsonb, host-validated on write) — but `processClaudeReply` selected only `sender_name, message, created_at` and so **dropped every attachment before the worker existed**. It now selects `attachments` (on the triggering message AND on the 12-message history window) and resolves them through the shared `lib/ai-agent/attachment-reader.ts`: images/scanned-PDFs ride the user turn as blocks (the worker SEES them), extracted text is appended to `userBody` under a `--- FILES SHARED IN … ---` block. **Carrier fallback**: when the @mention itself carries no file, the nearest earlier HUMAN message with attachments is used — staff post a screenshot and then @claude in the next message, exactly as they do in Slack (where the worker harvests thread history for the same reason); Claude's own messages are excluded as carriers. Downloads go through `fetchTrustedStorageBytes`, which re-checks the Supabase host allow-list even though the send route already validated the URL (defense-in-depth; the URL sits in a jsonb column). `callWorker` → `callWorkerWithAttachments` (one text-only retry on a media 400), so a bad image degrades to a text reply instead of a dead "…" placeholder. Attachment reading is wrapped in try/catch — it can never block the answer. NOTE: team attachments remain in the PUBLIC `assets` bucket (pre-existing exposure, NOT widened here); the new worker-panel uploads deliberately use a private bucket instead — see `inbox.md`. Sandbox-verified; **NOT yet on production**.)_
_2026-07-10d (**Client-conversation upgrade — S5 (topic + state filters). SANDBOX, same branch. FEATURE COMPLETE (S1–S5).** The Conversations sidebar section gained two filters: a **topic** dropdown (populated from the distinct topics present) + a **state** selector (All / Open / Solved / Closed). Pure client-side over the already-loaded threads; when any filter is active every matching client group auto-expands so narrowed results show without clicking. Predicate extracted to pure `lib/team/conversation-filter.ts::matchesConversationFilter` (unit-tested, 7 cases). Global message search (`search_team_messages`, the existing `searchQ` box) already covers finding a conversation by its content — not rebuilt. **TYPO NOT FIXED (prod-only, needs Antonio):** the 'Laeds' topic-template row (should be 'Leads') is a PRODUCTION `catalog_entries` row (`catalog_id='topic_templates'`); SANDBOX doesn't have it, so it can't be sandbox-tested. It splits dedup (`laeds` slug vs a free-typed `Leads`→`leads`). Fix = a one-row `catalog_update` on prod (display_name + slug) + backfill any `laeds` topic_slug on existing threads → deferred to Antonio's prod pass. No new migration for S5. 5385 unit + build green. **Whole feature S1–S5 is sandbox-only on branch claude/invoice-cancel-reactivate-ca89c2; prod ship needs migrations 20260710-1900 + -2000 + -2100 run in order first.**)_
_Prior: 2026-07-10c (**Client-conversation upgrade — S4 (scoped ring + dot for participants). SANDBOX, same branch.** A client conversation you're PART OF now rings (Web-Audio chime + web push) and lights the Team Chat dot on new activity — without resurrecting the "noisy 48". **Participant model** (no new table, no hardcoded team size): you're a participant of a thread iff you have an `internal_thread_reads` row for it (created when you OPEN it, POST in it, or are SEEDED as a share recipient). Migration `20260710-2100-team-threads-participant.sql` (sandbox-applied; **prod DDL pending**) does a `get_team_threads` DROP+CREATE adding `is_participant boolean = (r.thread_id IS NOT NULL)`. `countTeamNotifications` (the sidebar dot) now adds a discussion's `unread_count` when `is_participant` (unread already includes mentions, so no double count); a non-participant discussion still contributes only `mention_count`. `buildTeamNotifications` gains a `'conversation'` item kind (green "Client" badge in the sidebar dropdown, sorted DM→conversation→mention). `RealtimeNotifications` chimes+toasts for a message in a participant conversation (`myConversationThreadIdsRef`, refreshed 60s alongside the DM set). The message-send route pushes a discussion's PARTICIPANTS (read-row holders, minus sender + Claude sentinel) — was silent for discussions. The share route SEEDS each recipient a read row (`last_read_at` = epoch, NOT null — the column is NOT NULL; **this was a real bug the live test caught**: a null seed silently violated the constraint so the recipient never became a participant) so the first share pings Luca in-app, not just web-push. Live test 13 cases (+ participant flag + seed→unread). Remaining: S5 topic+state filters + 'Laeds'→'Leads'.)_
_Prior: 2026-07-10b (**Client-conversation upgrade — S3 (client-grouped sidebar). SANDBOX, same branch.** The "Discussions" sidebar section is renamed **Conversations** and now **groups by client**: each client is a collapsible row (chevron + Building2 icon + client name + a red dot when any of its conversations has unread activity + a count), expanding to its topic rows newest-first (each row labelled by TOPIC — "Billing" — with a small red/grey unread dot). Migration `20260710-2000-team-threads-grouping.sql` (sandbox-applied; **prod DDL pending**) does another `get_team_threads` DROP+CREATE adding `lead_id`, `topic`, `client_key` (`'account:<id>'|'contact:<id>'|'lead:<id>'|'internal'`, discussions only) and `client_label` (`'Internal / No client'` when unanchored). Grouping is computed SERVER-SIDE (review: never group on the display label). UI groups `unfiledDiscussions` by `client_key` in a `useMemo` (RPC returns newest-activity-first, so newest client + newest topic fall out of insertion order); `expandedClients` Set mirrors the existing `expandedChannels` pattern. `TeamThread` gained `lead_id`/`topic`/`client_key`/`client_label`. **DELIBERATE DEFERRAL (named):** a contact-anchored conversation groups under `contact:<id>`, NOT merged under the contact's parent account (needs the account_contacts join + a multi-account rule; rare for this account-first shop). Channel-filed discussions are unchanged (still under their channel folder). Live test extended (11 cases; added account client_key + label + topic assertion). Remaining: S4 scoped ring+dot, S5 filters + 'Laeds'→'Leads'.)_
_Prior: 2026-07-10 (**Client-conversation upgrade — S1 + S2 (SANDBOX, branch claude/invoice-cancel-reactivate-ca89c2; NOT on prod). Plan adversarially reviewed by 2 subagents before building.** **S1 — share into a conversation:** the Share dialog (Inbox + Portal Chats, `ShareToTeamDialog`) gained a **Conversation** target (default for a single item; bulk stays DM) — client search (`/api/team/client-search`) + topic dropdown (`/api/portal/chat/topic-templates`) + notes. `POST /api/team/share` accepts a 3rd target `{ conversation: { client, topic, force_new? } }` → lands the shared item(s)+card in a client discussion, web-pushes the support person (Luca) by default (admins if the sharer IS support). The DM targets (`'support'` | `{user_id}`) are unchanged. NEW single find-or-create path `lib/team/find-conversation.ts::findOrCreateConversation` — the ONE dedup grain (reuse open+same `topic_slug`; `forceNew` = "start a new one" hatch; seeds the 🗂️ opening marker). The modal route (`/api/team/conversations`) still has its own copy of the create logic + channel filing — reconcile later, but the dedup keys match so no grain divergence. **S2 — Solved / Closed:** review said do NOT overload `work_status` (the kanban lane). New orthogonal columns `internal_threads.resolution` (`null|'solved'|'closed'`, CHECK) + `resolved_by` (migration `20260710-1900-team-conversation-resolution.sql`, sandbox-applied; **prod DDL pending Antonio**). Open = `resolved_at IS NULL`; Solved = `resolution='solved'`; Closed = `resolution='closed'`. `PATCH /api/team/threads/[id]` accepts `{ resolution: 'solved'|'closed'|null }` → stamps `resolution`+`resolved_at`+`resolved_by` and moves the kanban card to `handled`; reopen (null) clears all three back to `todo`. The conversation header shows **Solved / Closed** buttons (was a single Open/Resolved toggle); either badge reopens on click. `findOrCreateConversation` reuse now **skips Closed** (dropped → fresh thread) and **reopens Solved** on new activity (deliberately NOT keyed on `resolved_at IS NULL`, which would fork a duplicate). `get_team_threads` DROP+CREATE adds `resolution` to its projection (prod & sandbox defs were identical pre-change). Live sandbox test `tests/live/team-share-conversation.live.test.ts` (10 cases). Remaining slices: S3 client-grouped sidebar+dots (needs `get_team_threads` lead_id + resolving-account key), S4 scoped ring+dot for participants (`internal_thread_reads` ∪ mentions), S5 topic/state filters + fix the 'Laeds'→'Leads' catalog typo (a live dedup splitter).)_
_Prior 2026-07-09f — Claude (**"Post to team chat AS Claude" send capability — for code (MCP) + the AI worker**, branch `claude/ecstatic-franklin-c201ec`. Antonio wanted the AI assistant able to post into Team Chat (announce a fix, flag/ask a teammate to test) from both Claude Code and the worker. New choke-point `lib/team/post-message.ts::postTeamMessage({ channel | thread_id | dm_user_id, message, card? })`: resolves the target (channel slug/name or "general" room; explicit thread id; or a DM to a staff user via `findOrCreateDm(CLAUDE_SENDER_UUID, ...)`), inserts one `internal_messages` row AS the CLAUDE sentinel (`CLAUDE_SENDER_UUID`/`CLAUDE_SENDER_NAME` — same identity that answers @claude), bumps `last_activity_at`, resolves `@mentions` (`resolveMentions`) → targeted push to mentioned staff else broadcast-excluding-Claude. Deliberately **does NOT fire the @claude trigger** (Claude posting must not invoke Claude — loop safety) and **advances no human read pointer** (recipients' unread stays correct). Pure validators split into `lib/team/post-message-validate.ts` (client-safe, unit-tested `tests/unit/team-post-message.test.ts`). Two callers share the choke-point: **(1) MCP tool `team_chat_send`** (`lib/mcp/tools/team-chat.ts`, registered in `app/api/[transport]/route.ts`) for Claude Code / the connector; **(2) worker tool `team_chat_send`** (`TEAM_CHAT_SEND_TOOL` in `lib/ai-agent/worker-tools.ts` — NOT to be confused with the pre-existing per-client `send_team_message` agent tool, which posts to Portal Chats > Team tab; deliberately renamed to avoid that collision) gated by new `CallWorkerOptions.enableTeamChatSend` — set ONLY by the Slack worker (`processSlackEvent`) and the @claude Team-Chat trigger (`processClaudeReply`); **kept OUT of the Hermes research worker (R108)**, with an `availableNames` executor re-check (defense-in-depth). Both run the same **draft → explicit "send it"** approval discipline as `send_email`/`send_portal_message` (stated in the tool descriptions; for Claude Code the draft is shown to Antonio first). Verified: full suite 5268 green, build green; sandbox data-path E2E (replicated the exact Claude insert into `qa-ops-channel` → `get_team_threads` surfaced it unread to a staff user → deleted). **SESSION CAVEAT:** MCP tool lists are fixed at session start, so a freshly-added tool is only callable from a NEW session / the worker runtime.)_
_2026-07-09e — Claude (**Notifications scoped to DM/@mention** (sandbox, pending prod ship): both the in-app chime+toast (`RealtimeNotifications`) and the system web-push (`/api/team/threads/[id]/messages`) now fire ONLY for a DM to you (the send route pushes just the other DM participant, read from `dm_key`; the client checks the message's thread is one of your DM threads, cached + refreshed 60s) or an @mention of you (`mentioned_user_ids`). Ordinary channel/discussion chatter no longer buzzes anyone — matches the DM/@mention dot. The toast/push deep-link to the exact `/team-chat?thread=<id>` and the notification title shows "DM · <sender>" / "@mention · <sender>". Sound already existed (Web-Audio chime in-app; OS sound for push — notifications are not `silent`); this pass fixed the SCOPE + click routing (see pwa.md for the SW click fix). `sendPushToAdminExcluding` no longer used by the team send route.)_
_Prior 2026-07-09d — Claude (**Team Chat notification dropdown** (sandbox, pending prod ship): the main-sidebar Team Chat dot is now CLICKABLE (`TeamNotifDot` in `components/dashboard/sidebar.tsx`) — it opens a small fixed-positioned popover listing the caller's unread **DMs** (labelled with the other person) and **@mentions** (labelled with the channel/discussion, e.g. "td-dev"), each deep-linked to `/team-chat?thread=<id>`, so you can triage priority without opening Team Chat. Backed by `GET /api/team/notifications` (per-user via `get_team_threads` → pure `buildTeamNotifications` in `lib/team/workspace.ts`, unit-tested) — same DM+mention scope as the dot, so it can never show another user's private DMs.)_
_Prior 2026-07-09c — Claude (**Share full-text + notification dots** (sandbox, pending prod ship): (1) a share now embeds the ENTIRE email/portal-message text in the chat message body (`composeShareMessage` in `lib/team/share.ts`, capped at `SHARE_BODY_CAP`=4000 chars; inbox fetches + strips the email HTML, portal passes `msg.message`); the card stays the titled link. (2) **Team Chat menu signal FIXED + narrowed to a DOT** — it was always 0 (counted `internal_messages.read_at IS NULL`, but every message is inserted with read_at=now()); now `/api/dashboard/badges` + `layout.tsx` compute a per-user signal via `get_team_threads(user)` → `countTeamNotifications()` (pure, in `lib/team/workspace.ts`, unit-tested) = **unread DMs + @mentions ONLY** (ordinary channel/discussion unread is deliberately excluded — that was the noisy "48" Antonio flagged). The main-sidebar renders it as a plain **dot** (`NavItem.dotBadge`), not a count; the realtime handler REFETCHES the count (debounced) instead of blind-+1 (a blind increment would light the dot for channel chatter). Team signal is its OWN item (no longer folded into Portal Chats). (3) in-chat **"Direct Messages" section header shows a red dot** when any DM is unread (`SectionHeader dot` prop).)_
_Prior 2026-07-09b — Claude (**Desktop whole-page-scroll fix**: both `/team-chat` roots (list view + board view) now use `h-full lg:h-[calc(100%-3.5rem)] overflow-hidden` to subtract the sticky 56px desktop `DashboardHeader` — the chat sidebar + message pane now scroll internally within the viewport instead of the whole page scrolling. Same fix pattern as the inbox (finance dashboard got it too). Mobile unchanged. Part of the "sweep the other pages" pass.)_
_Prior 2026-07-09 — Claude (**Share to team chat** from Inbox + Portal Chats: new `POST /api/team/share` + `ShareToTeamDialog` + `findOrCreateDm` helper + `support_person_user_id` setting. See "Share to team chat" section below. **LIVE ON PRODUCTION** — commit `1a02c4ef`; prod `support_person_user_id` seeded to the support@ team account `deb9becc-01d5-4d76-a29a-195a6d5b2857` (the operational "Luca" account; sandbox labels the same account "Luca").)_
_Prior 2026-07-08b — Claude (**Mobile pane-collapse** (PWA mobile UX pass, dev_task `e1f28dce`): on <768px `/team-chat` now shows the thread sidebar full-width until a conversation is opened, then the main pane full-width with an "All conversations" back bar (`md:hidden`) that clears `selectedId`/`selectedSlackId`. Desktop (`md+`) unchanged — same fixed `w-64` rail + main pane. CSS/JSX-only in `app/(dashboard)/team-chat/page.tsx`; no data-flow, API, or realtime changes.)_
_Prior 2026-07-08 — Claude (LIVE ON PRODUCTION through the Slack-parity pass, commit `97363891`; invitation gate added same day. Goal per Antonio: replace Slack entirely so the team has ONE place to work — no extra app/fee. This is the staff-only internal chat, distinct from the client portal chat in `portal.md`.)_

## What it is
The internal team chat, rebuilt from a single "Team General" room into a full workspace: **named channels**, **direct messages**, **client discussions**, and a **general** room — all staff-only, never client-visible. Reactions, edit-with-history, pins, message search, @mentions with targeted push, colored rich cards, and an **@claude** AI-worker trigger. Backed by the same `internal_threads` / `internal_messages` tables that already powered the old team chat + `portal_team_send` (so nothing pre-existing broke — the change is additive).

Two UI surfaces read these tables and both keep working:
- `/team-chat` — the new **Team Workspace** page (this subsystem).
- CRM **Portal Chats → Team tab** (`app/(dashboard)/portal-chats/page.tsx`, `view=internal`) — the legacy per-client internal-thread view, untouched. `portal_team_send` still writes here.

## Data model
`internal_threads` — one row per channel / DM / discussion / general room. `thread_type` ∈ `general | channel | discussion | dm` (CHECK). Channel cols: `channel_name`, `channel_slug` (partial-unique), `description`, `color`. DM col: `dm_key` = sorted `"idA:idB"` (partial-unique → one thread per pair). `resolved_at` (discussions), `archived_at`, `last_activity_at` (sidebar sort). Legacy cols retained: `account_id`, `contact_id`, `source_message_id`, `title`, `created_by`.

`internal_messages` — `reactions` jsonb `[]`, `edited_at` + `original_message` (edit history), `pinned_at`/`pinned_by`, `mentions` jsonb (matched handles), `card` jsonb (rich card). Legacy retained: `attachments`, `reply_to_id`, `deleted_at`/`deleted_by` (R100 soft-delete), `read_at`/`seen_at` (legacy — superseded for unread by the reads table).

`internal_thread_reads` — **per-user read pointer** (`thread_id`, `user_id`, `last_read_at`, PK both). This is the unread model. Staff-only RLS matching the sibling tables (prod has RLS on; sandbox off — a green sandbox test does NOT prove RLS).

Realtime publication members: `internal_messages` (pre-existing) + `internal_threads` + `internal_thread_reads` (added Phase 1).

## Read-side RPCs (avoid N+1)
- `get_team_threads(p_user_id)` → every visible thread + **per-user `unread_count`** (`msgs where created_at > COALESCE(last_read_at,'-infinity') and sender_id<>user and deleted_at is null`) + last-message preview. DMs filtered to those whose `dm_key` contains the user id; channels/general/discussions visible to all staff. Excludes `archived_at`.
- `search_team_messages(p_user_id, p_query)` → ILIKE search across visible messages (same DM rule), cap 50.
- `toggle_internal_message_reaction(msg, emoji, reactor_id, reactor_name)` → atomic add/remove keyed on (emoji, reactor). Mirror of `toggle_message_reaction` (that one is bound to `portal_messages` — do NOT reuse it here).

## New conversation (native, Slack-independent — the Client-Threads modal, reimagined)
The "New conversation" entry on the **Client Discussions** sidebar header opens a modal (mirrors the Slack Client-Threads modal) — pick a **client** (account/contact/lead search), an optional **topic** (from the `topic_templates` catalog or free-typed), and optionally a **channel** to also drop a card into. It creates (or reuses) a `thread_type='discussion'` thread anchored to that client + topic. Purely native — no Slack.
- `GET /api/team/client-search?q=` — accounts (`company_name`) + contacts (`full_name`) + leads (`full_name`), staff-only, returns `{value:"<kind>:<uuid>", label, sublabel, kind}` (mirrors the Slack modal's 3-table search). Topics come from the existing `GET /api/portal/chat/topic-templates`.
- `POST /api/team/conversations` — body `{ client:"account|contact|lead:<uuid>", topic?, channel_id? }`. Parses the client ref (`lib/team/conversations.ts::parseClientRef`), resolves the display name, **finds-or-reuses** an OPEN discussion for the same client+topic (dedup on `(clientCol, topic_slug)`), else creates one titled `"<client> · <topic>"` + seeds a "🗂️ Conversation started" message. Optional `channel_id` drops a rich card (`kind:client_message`, link `/team-chat?thread=<id>`) into that channel.
- Schema: `internal_threads` gained `topic`, `topic_slug`, `lead_id` (migration `20260708-0100`). The page honors `?thread=<id>` to deep-select (used by the channel card + push links).

## API routes (`app/api/team/*`, all staff-only via `isDashboardUser`)
- `GET /threads` — sidebar payload: `get_team_threads` + staff directory (`listTeamMembers`) + current user.
- `POST /channels` — create channel (slug from name, unique). `POST /dms` — find-or-create DM by `dm_key`.
- `GET /threads/[id]` — messages (+ reply previews) and **advances the caller's read pointer**. `PATCH /threads/[id]` — rename/recolor/resolve/archive. `POST /threads/[id]/read` — advance read pointer only (badge clear).
- `POST /threads/[id]/messages` — send. Resolves @mentions → targeted push (`sendPushToAdminUsers`) for mentioned staff, else broadcast-excluding-sender; validates card + attachment host; fires the **@claude** trigger when `@claude`/`@ai` is present.
- `PATCH /messages/[id]` (edit, author-only, preserves `original_message`) / `DELETE` (soft-delete, author or admin). `POST /messages/[id]/react` / `/pin`.
- `GET /search?q=` — `search_team_messages`.
- `POST /upload-url` — signed direct-to-Storage URL (100MB) → `assets` bucket `team-chat/<threadId>/`, bypassing the serverless body limit (the flaw the old `/api/internal/threads/[id]/upload` had at 10MB).
- `POST /claude/process` — internal, CRON_SECRET-authed, `maxDuration=300`. Runs the shared worker and rewrites the placeholder.

## @claude FULL SLACK PARITY (2026-07-08, after the Tamás Fazekas incident)
Antonio's prod test exposed half-parity: a conversation created with a channel selected was NOT filed under it, and "@claude send it in the thread" produced an approval code instead of sending (and that code was un-completable in Team Chat). A code-verified PARITY MATRIX was extracted from the Slack worker (processSlackEvent + webhook rails + slack-approval.ts) and the Team worker brought to parity:
- **Channel = home**: `POST /api/team/conversations` now sets `parent_channel_id` (files the conversation under the chosen channel, like Slack's /client modal); reuse with a channel selected re-files; channel validated up-front; the card in the channel remains as the "conversation started" marker. Modal label updated.
- **Worker call parity** (`processClaudeReply`): `systemPromptOverride = SLACK_WORKER_SYSTEM_PROMPT` (+ a context-correction line + the approved-templates block) — the full discipline stack (SOURCES FIRST, TWO GEARS, DRAFTS, send-after-explicit-"send it") now applies; `threadId` = the TEAM thread uuid (worker thread memory + `approval_queue.thread_id` linkage); `enableEmailSend`, `enableSlackSend`, `enableThreadRecall`, `enableFullToolReach` (env-gated), `maxIterations: 20`, `apiKeyOverride = SLACK_WORKER_ANTHROPIC_KEY`. `messageId` is deliberately NOT passed — `approval_queue.source_message_id` FKs `agent_messages(id)` (Slack-only). Claude's answer also fires a push to the asker (Slack posts answers as new messages → push; our in-place placeholder rewrite doesn't, so the push is explicit).
- **In-thread approval codes** (`lib/team/team-approval.ts` + interception in the messages POST): an ADMIN message that is exactly a 6-digit code completes the pending proposal linked to this thread (`approval_queue.thread_id` match) — atomic approve → existing executor → outcome posted as a Claude reply; never reaches the LLM (mirrors slack-approval.ts incl. ambiguity/error guards + APPROVAL_RAIL_ENABLED gate).
- **Incident cleanup**: the stale un-completable approval (code 985040) was voided (double-send risk) and the Tamás thread filed under td-support.
- **Verified**: filing E2E (conversation created under the channel), interception E2E (planted proposal → typed code → "Approved" outcome), send E2E on deployed sandbox (draft → "send it" → "Sent. ✅", direct tool, no code). Slack worker untouched (its call sites unchanged; slack-approval tests green).
- **Slice 2 (partly built)**: ~~attachments/images from team messages into the worker~~ **DONE 2026-07-10** (see header). Still tracked, not built: referenced-thread capture, Stop button, animated thinking ticks, reaction rails, ask-antonio code-task answer routing.

## @claude invitation gate — no re-mention in discussions (2026-07-08)
Antonio (Giuseppe Cirino · Billing): *"everytime I have to @claude for the worker to answer. is it normal?"* It wasn't Slack parity: in Slack, thread replies flow to the worker once the parent mentioned Claude, and `/client` conversation threads process ALL replies mention-free. Team Chat required `@claude` on every message.
- **Rule (shipped)**: in a `discussion` thread where Claude has ALREADY participated (any `internal_messages` row with the Claude sentinel sender — i.e. it was @mentioned once), every subsequent plain TEXT message auto-triggers the worker. Channels / general / DMs still require an explicit `@claude` per ask (Slack top-level parity).
- **Where**: pure predicate `shouldAutoContinueWithClaude` in `lib/team/workspace.ts` (unit-tested); the messages POST checks prior Claude participation (one indexed lookup, only for plain messages in discussions) and calls `triggerClaudeReply({ force: true })`; `triggerClaudeReply` gained a `force` param that bypasses its own `mentionsClaude` guard. The processor/cron-rescue never re-check the mention, so rescued forced prompts survive.
- **Ordering preserved**: the 6-digit approval interception still runs (and returns) BEFORE the trigger block; attachment/card-only sends (empty text) never auto-trigger; loop-safety unchanged (Claude-sentinel messages never reach the send route's trigger).
- **Deliberate scope**: a human-only discussion (nobody ever mentions Claude) is never auto-invoked. A channel 30-min continuation window was offered and deferred by Antonio ("ship rule 1 now").

## @claude reliability — middleware exception + cron rescue (2026-07-08)
Antonio's first prod @claude got stuck on "thinking…". ROOT CAUSE: `/api/team/claude/process` was not in `middleware.ts` `PUBLIC_PREFIXES`, so the session gate 401'd (SESSION_EXPIRED) the server-to-server direct fire BEFORE the route's own CRON_SECRET auth — deterministic, every time (found via sandbox E2E; handler tests bypass middleware, only a real request catches it — same lesson as the e-sign signer page). FIXES:
- `middleware.ts`: `/api/team/claude/process` added to `PUBLIC_PREFIXES` (route self-auths via CRON_SECRET Bearer, same as `/api/cron/*`).
- **Cron rescue net** (same pattern as the Slack worker): the placeholder now stores `reply_to_id = the prompt message` (also renders as a quoted reply); `rescueStuckClaudeReplies()` in `claude-trigger.ts` finds "…" placeholders older than 45s (batch 3), recovers the prompt from `reply_to_id` (legacy fallback: nearest earlier human message), re-derives the Antonio-only code-task gate via an auth lookup, and processes them. The process route gained GET + scan mode; registered in `vercel.json` at `*/2 * * * *` (Vercel sends the CRON_SECRET bearer).
- `/client` slash command in the composer opens the native New-conversation modal (Slack muscle-memory parity). Extended same day: typing just **`/`** pops a command MENU above the composer — `/client` New conversation, `/channel`, `/dm`, `/board` — filtered as you type; Enter runs the first match, Esc dismisses (Antonio: easier than typing the full command).
Verified in sandbox: a stuck placeholder (direct fire fails locally by design) was found and rewritten by the scan; `/client` opens the modal.

## @claude adapter (`lib/team/claude-trigger.ts`)
A human `@claude` mention → `triggerClaudeReply` inserts a placeholder message (sender = `CLAUDE_SENDER_UUID` sentinel, body `…`) and fires `/api/team/claude/process` (bounded direct-trigger, like the Slack worker). `processClaudeReply` reuses `callWorker` from `lib/ai-agent/worker-tools.ts` (same brain as Slack/Hermes), builds context from recent thread messages + client linkage (`clientKey`/`clientName` when the thread has an account/contact — the per-client brain), then rewrites the placeholder with the answer (TOCTOU-guarded on the placeholder body).
- **Rails:** research/read on for everyone (`enableDbRead/DocReads/CallReads/Calendly/ClientThreadRead/WebSearch`). **Code-task rail Antonio-only** (`enableCodeTasks = isAdmin`, R111). **Send rails ON since the 2026-07-08 parity pass** (`enableEmailSend`/`enableSlackSend` + full discipline prompt — see "@claude FULL SLACK PARITY" above; the earlier research-only stance was the Tamás incident's root cause).
- **Loop-safety:** only human sends reach the send route; `processClaudeReply` refuses a prompt authored by the Claude sentinel. Claude's own posts never re-trigger.

## Pure helpers (`lib/team/workspace.ts`, unit-tested)
`parseMentionHandles` (ignores emails), `mentionsClaude`, `dmKey`, `channelSlug`, `validateHexColor`, `validateTeamCard`, `TEAM_COLORS`, `CLAUDE_SENDER_UUID`/`CLAUDE_SENDER_NAME`/`CLAUDE_MENTION_ID`. `lib/team/directory.ts` = staff directory + `resolveMentions` (handle→user id). `lib/team/attachment.ts` = client-side signed-URL upload (reuses `validateChatAttachment` so team + client chat share size/type policy).

## Slack channel mirror (read-only feed — ships DORMANT)
A read-only view of the Slack channels the bot is in, inside the workspace, so the team can watch Slack from the CRM during the parallel-run before Slack is decommissioned. Gated on the `slack_mirror_enabled` app_setting (default **false** — dormant; prod has no row → off).
- **Tables:** `slack_channels` (id/name/is_member/last_message_at…) + `slack_messages` (`(channel_id, ts)` PK, thread_ts, author, text, subtype, deleted, posted_at, raw). Staff-only RLS. Migration `20260708-0300`.
- **Two feeds into the mirror:** (1) LIVE — the production Slack webhook (`app/api/webhooks/slack-claude/route.ts`) calls `ingestSlackMessageEvent(event)` for every channel `message` event, ADDITIVELY before the worker routing (worker behaviour unchanged; best-effort; no-op when the flag is off). (2) BACKFILL — `POST /api/team/slack/sync` runs `syncSlackChannels()` (conversations.list) + `backfillChannelHistory()` (conversations.history) — internal app is exempt from the 2025 Slack rate limits.
- **Module:** `lib/team/slack-mirror.ts` (server) + pure `lib/team/slack-mirror-classify.ts` (`classifySlackEvent` handles new/edit(message_changed)/delete(message_deleted); `resolveSlackMentions`; `KNOWN_SLACK_USERS`). Idempotent upsert on `(channel_id, ts)` for Slack retries.
- **Read APIs:** `GET /api/team/slack/channels`, `GET /api/team/slack/channels/[id]/messages` (Open-in-Slack via `buildSlackThreadDeepLink`), `POST /api/team/slack/sync`. All staff-only + kill-switch-gated.
- **UI:** a "Slack" sidebar section (only when enabled) → click a channel → read-only feed with per-message + channel "Open in Slack"; footer "reply in Slack" (no CRM→Slack posting in this slice).
- **Scope note:** the app has `channels:read` + `channels:history`. Human display names use a 3-person `KNOWN_SLACK_USERS` stopgap; full name resolution for everyone needs the `users:read` scope + a users.info cache in the sync. Private channels need `groups:read`/`groups:history` + `message.groups`. CRM→Slack posting needs `chat:write.customize`.
- **Verified in sandbox** (2026-07-08): synced 11 real channels + 183 messages via local bot token; feed renders real #td-dev history with names + mentions resolved + working deep links. Live event flow is **prod-only** (webhook is the prod URL) — verify after switch-on there.

## Organizer layer — channel-folders, kanban board, mark-unread + Later (2026-07-08)
Turns the workspace into a lightweight work tracker (Antonio's request).
- **Channels are FOLDERS that hold threads.** `internal_threads.parent_channel_id` points at the channel a discussion/topic is filed under (null = unfiled). The sidebar renders each channel as an expandable folder containing its filed threads; the ⋯ menu on a thread has "Move to channel" (any channel or "No channel"). Move = PATCH `/api/team/threads/[id]` `{ channel_id }`.
- **One kanban board of all work threads.** `internal_threads.work_status` ∈ `todo|in_progress|waiting|handled` (CHECK; default `todo`; backfill: resolved→handled). The **List | Board** toggle (`ViewToggle`) in the header switches the whole pane to `app/(dashboard)/team-chat/board.tsx` — 4 columns, each thread a draggable card (reuses `@hello-pangea/dnd`, same as the tasks kanban), filterable by channel, DMs excluded. Drag → PATCH `{ work_status }`. **`handled` IS the done state**: setting it stamps `resolved_at`, moving off it clears `resolved_at` (kept in sync so the resolve toggle + anything reading `resolved_at` stays consistent).
- **Mark-unread + Later are PER-USER** (`internal_thread_reads.manual_unread`, `.later`). `POST /api/team/threads/[id]/mark-unread` forces the badge on until the thread is next opened (GET/`/read` clear `manual_unread`). `POST /api/team/threads/[id]/later` `{ later }` pins the thread to a "LATER" sidebar section (amber clock indicator). `get_team_threads` returns `work_status`, `parent_channel_id`, `later`, and `unread_count = GREATEST(computed, manual_unread?1:0)`.
- Pure helpers `TEAM_WORK_STATUSES`/`TEAM_WORK_STATUS_LABELS`/`isValidWorkStatus` in `lib/team/workspace.ts` (unit-tested). Migrations `20260708-1400` (schema) + `20260708-1500` (RPC — DROP+CREATE, signature changed).
- Verified in sandbox browser: move a discussion into a channel (nests under the folder), mark-unread (badge returns), Later (LATER section), board renders + status change re-groups the card. Physical drag can't be automated (library limitation) but the operation it triggers is verified.

## Mentions inbox, status dots, chat search, sidebar scale fix (2026-07-08, from Luca's Slack proposal)
Luca proposed (Slack #td-dev 1783524778.142989): column layout per channel, client search, follow-up dots, and a Mentions section. Panel review mapped half to already-shipped features (folders, @mention push, kanban) and approved three additions + one scale fix:
- **Mentions inbox**: `internal_messages.mentioned_user_ids uuid[]` (resolved USER IDS stored at send time — handles in `mentions` stay display-only; GIN index). "Pending mention" = mentioned in a message newer than my `last_read_at` in that thread → clears naturally on open (reuses the per-user read model, no new table). `get_team_threads` returns per-caller `mention_count`; the UI shows a violet **MENTIONS** section (badge = total) above LATER.
- **Status dots** (`StatusDot`): the kanban `work_status` surfaces in every list row — blue = in_progress, amber = waiting; todo intentionally undotted (noise), handled shows via strikethrough.
- **Chat name search**: the sidebar search panel now has a **Chats** group (client-side label match, top 8, with status dots) above the **Messages** content results.
- **Sidebar scale fix**: `get_team_threads` computes `label` server-side (LEFT JOIN accounts/contacts/leads) — replaced the threads route's per-discussion N+1 lookups (2 queries × N discussions per load/poll) that would degrade first as clients grow. Migration `20260708-1800`.
- **Deferred with a trigger**: Luca's Finder-style column layout — revisit when a channel folder no longer fits one screen (~15–20 clients) or total threads pass ~100.

## Share to team chat — from Inbox & Portal Chats (built 2026-07-08, LIVE ON PROD 2026-07-09 commit `1a02c4ef`)
Antonio's request: from a specific email in the CRM **Inbox**, or a specific client message in **Portal Chats**, share it into the team chat as a **DM** — either to a chosen teammate (to discuss) or to a configured **support person** (a support intake queue). A multi-select share to Support posts **one message per item** (never merged). Each shared post carries the sharer's **notes** (message body) + a **card** linking back to the source.
- **One endpoint** — `POST /api/team/share` (`app/api/team/share/route.ts`). Body `{ target: 'support' | { user_id }, note?, items[] }`. Resolves the recipient (support person via `getSupportPersonUserId()`, or the passed teammate — both validated against `listTeamMembers`), find-or-creates the DM, inserts **one `internal_messages` row per item** (note as `message`, item as `card`), bumps `last_activity_at`, advances the sender's read pointer. **Notifies ONLY the recipient** via `sendPushToAdminUsers([recipientId])` — deliberately NOT the normal send route, which broadcasts DM/thread activity to the whole staff (`sendPushToAdminExcluding`); reusing it would ping everyone on every share. Self-share (recipient === sender) skips the push.
- **DM find-or-create is shared** — `lib/team/dm.ts::findOrCreateDm(a, b)` (deduped by `dm_key`, race-safe). Both `POST /api/team/dms` and the share route use it — never hand-roll a DM insert.
- **Full source text in the body** — each shared message's body = the sharer's note + the item's ENTIRE text (`composeShareMessage(note, body, cap=SHARE_BODY_CAP)`, capped at 4000 chars with a truncation marker). Inbox single-share fetches `/api/inbox/messages/<id>` and strips the email HTML to plain text (`stripEmailHtml` in `inbox-shell.tsx`); portal passes `msg.message`; bulk email share stays snippet-only (no per-item fetch). The card remains the compact titled link.
- **Card building is pure + unit-tested** — `lib/team/share.ts::buildShareCards(rawItems, max=25)` returns `{ cards, error }`: defaults `kind` to `client_message`, requires a **relative** in-app `url` (rejects external links), runs each through `validateTeamCard`, fails closed on the first bad item. Tests in `tests/unit/team-share.test.ts`. (Note: a discriminated-union `{ok}` result shape tripped an anomalous TS narrowing failure only in this project's compile context — the flat `{cards,error}` shape is the deliberate workaround.)
- **Support person is configurable** — `support_person_user_id` app-setting (`lib/settings.ts::getSupportPersonUserId()`) stores the **actual auth user UUID** (no runtime name-resolution — brittle). Seeded to the support@ team account in both envs: **prod** `deb9becc-01d5-4d76-a29a-195a6d5b2857` (name "support"), **sandbox** `55ac2225-42ad-4215-b79b-9d610fefc6c5` (name "Luca") — same operational account, different label. If unset, the endpoint returns a 409 "no support person configured" rather than guessing. Change it with a single app-setting update.
- **Shared dialog** — `components/team/share-to-team-dialog.tsx` (`ShareToTeamDialog`): target toggle (Support | teammate picker from `GET /api/team/threads` `members`, self excluded), notes box, item preview, success toast with an "Open" link to `/team-chat?thread=`. Optional `onShared` callback (Inbox uses it to clear a bulk selection). Reused by BOTH surfaces — do not fork.
- **Inbox surface** (`components/inbox/inbox-shell.tsx`): a per-thread "Share to team chat" header button (email → `kind:'link'` card, subject as title, sender + snippet as subtitle) + a **bulk "Share to team"** button in the selection bar (resolves `selectedIds` → conversation objects from the `inbox-conversations` react-query cache). Card links back via a **new inbox deep-link**: `/inbox?thread=gmail:<id>&mailbox=support|antonio`, hydrated on mount from `window.location` (the inbox had no addressable single-email URL before — added here; it fetches `/api/inbox/messages/...` for the header subject/sender).
- **Portal Chats surface** (`app/(dashboard)/portal-chats/page.tsx`): a "Share to team chat" item in the per-message kebab (next to "Discuss with Team"). Card = `kind:'client_message'`, title = client/account name, subtitle = message text, back-link `/portal-chats?account=<id>&message=<id>` (or `?contact=`). Single-message only for v1 (that page has no multi-select; deferred).

## Business rules / invariants
- **Staff-only, never client-visible.** All routes gate on `isDashboardUser`; RLS is staff-only.
- **Unread is per-user** via `internal_thread_reads`. Do NOT reintroduce logic based on the single `read_at` column for unread (it made counts always 0 — the pre-Phase-1 bug, because sends stamped `read_at=now()`).
- **@claude trigger scope**: an explicit mention anywhere, PLUS plain text messages in a `discussion` where Claude has already participated (invitation gate, 2026-07-08). It never listens ambiently in channels/general/DMs, and never in a discussion it hasn't been invited into. Code tasks Antonio-only; send tools run on the Slack discipline rail (draft → explicit "send it").
- **Attachments** must live on our Storage host (send route rejects off-host URLs); 100MB via signed URL, active-content block-list (shared with portal chat).
- **Who gets interrupted is decided by TWO shared predicates, never by a per-site list** (`lib/team/channel-notify.ts`): `channelNotifiesStaff` (work channels notify everyone; the machine-written worker-bug channel does not) and `conversationNotifiesParticipants` (client conversations notify NOBODY, since 2026-08-04). Every notifying site — both send paths and the in-CRM toast listener — must read them, so the phone and the screen cannot drift apart. **@mention is handled BEFORE the conversation branch at both send sites and must stay there**: it is the only remaining way to reach a teammate inside a client conversation. **Notification ≠ unread:** the badge (`countTeamNotifications`) still counts conversations on purpose — Antonio wants to see that one moved without being pulled out of his work.

## How to verify current state
- `get_team_threads(<a staff user id>)` returns rows with correct `unread_count` (own messages don't count; another user's messages do until they GET the thread or POST /read).
- Confirm columns: `SELECT column_name FROM information_schema.columns WHERE table_name IN ('internal_threads','internal_messages','internal_thread_reads')`.
- Confirm realtime: `SELECT tablename FROM pg_publication_tables WHERE pubname='supabase_realtime' AND tablename LIKE 'internal_%'` → all three.
- @claude needs `ANTHROPIC_API_KEY` + `CRON_SECRET` on the environment; push needs VAPID keys. Sandbox blocks outbound email but push/worker work.
- Migrations: `scripts/migrations/20260707-1900-team-workspace-phase1.sql` (schema) + `20260707-2000-team-workspace-read-rpcs.sql` (RPCs). Applied to sandbox; NOT promoted to prod.

## Not yet built (later phases)
Slack ingestion/mirror + post-from-CRM (behind kill-switch), colored Send-to-Team cards from account/invoice/doc/task pages, client-message → discuss/task/Slack bridges, PWA mobile hardening, Slack history import + decommission. See dev_task "Team Workspace — replace Slack".
