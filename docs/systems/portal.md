# Client Portal
_Last verified against code: 2026-08-11c — Claude (**The portal OA signing wrapper is part of the MMLLC Operating Agreement link-security change — full rules in `lease-oa.md` (2026-08-11).** The embedded OA signing iframe now carries a short-lived, agreement-bound pass minted by the contact-gated wrapper, so it skips the email gate WITHOUT the spoofable `?portal=true` flag; a logged-in member whose OWN link has expired has it silently re-stamped on open; a member whose link was revoked by a roster change sees an "ask the owner / support" message instead of a signing pad; and a client-facing "Re-send signing links" button lets the account owner re-issue expired co-signer links himself (email-only, rate-limited 1/hr). Files: `app/portal/sign/oa/page.tsx`, `app/portal/sign/oa/portal-oa-client.tsx`. See `lease-oa.md`.)_
_Prior: 2026-08-11b — Claude (**The formation dashboard's “Action required” card now carries the lead link when the client has exactly ONE in-progress formation. Dev job ca788354, found by Antonio's own QA pass.** The two formation branches in `app/portal/page.tsx` (formation-account and post-PR1 contact-scoped) rendered `FormationDashboard` WITHOUT `formationLeadId`, so the card linked to the BARE wizard path; the wizard page only enters formation scope via `?lead=`, so a returning client landed on a BLANK new wizard, re-typed everything, and submitted an unattributable formation — the ambiguous safety net caught it live during QA (nothing minted, staff flagged). Fix: `getInProgressFormations` is now fetched once, unconditionally, in that block; with exactly one in-progress formation its `leadId` is passed to both branches (`soleInProgressLeadId`); with several the bare path stays, because the company-switcher cookie branch already carries the lead per selection. The cookie branch was always correct and is untouched.)
_Prior: 2026-08-10 — Claude (**The formation wizard now LOCKS once we begin work — it was editable forever, and that is how the setup chain got re-run 15 times. Dev job `ca788354`.** `isLocked` in `app/portal/wizard/page.tsx` was computed for `wizardType === 'tax'` ONLY. For formation it therefore stayed `false` permanently, `wizard-client.tsx` kept `isResubmitMode` true for any submitted formation, and every re-submit sent `allow_resubmit: true` — which is exactly the flag that skips the submit route's duplicate check. 8 contacts re-ran `formation_setup` 15 extra times between 2026-03-23 and 2026-08-06, all client-driven from the portal; Pasztor and Covelli each received the "Formation data received!" notification three times, and Turcanu received a whole phantom Company Formation for a company formed six weeks earlier. **The lock is tax-style, NOT blanket read-only at submit** (Antonio's ruling, 2026-08-10): editable at "Payment Confirmed" and "Wizard Submitted", LOCKED from "Filed with State" onward, once the formation has materialized into a real company, once it is finished, when the stage cannot be read, and when identification is ambiguous. A client correcting a wrong passport number or date of birth *before* we file keeps that path — the same reasoning as the tax review loop; changes after the lock go through portal chat. **Which formation a wizard view belongs to is resolved by `identifyFormation` in `lib/portal/formation-resubmit-gate.ts` — the SAME helper the background job uses**, keyed on the originating OFFER (resolved `offers.token` via the lead), never on the contact. That matters because ~11% of contacts own more than one company: a contact-keyed lock would render a repeat client's SECOND formation read-only because their FIRST had been filed. Ambiguous identification locks rather than guessing which formation is open. **The submit route was also resetting reviewed submissions:** `buildSubmissionRecord` hardcodes `status: "completed"` and the upsert keys on `token`, which is stable for the same person + lead + calendar year — so a re-submit silently undid a completed review. `preserveReviewedStatus` (`lib/portal/submission-record.ts`) now prevents the downgrade before the upsert in `app/api/portal/wizard-submit/route.ts`. That is the ONLY route-side change: the refusal itself deliberately lives downstream in the job handler, because Antonio's ruling requires the client's correction to still reach their contact record and staff to still be emailed — so `wizard-submit-nonblocking` does NOT flip. Full behaviour, the phantom-delivery half, and the retired Luca WhatsApp task are documented in `formation.md` (same date).
_Prior: 2026-08-09b — Claude (**Two portal signing surfaces changed by the OA "never fewer signers than members" rule** — full detail in `docs/systems/lease-oa.md`, nothing portal-specific is decided here. (1) **Generate Documents** now REFUSES to create a multi-member Operating Agreement when any owner cannot be sent a signature request, and shows the client which owner and what is missing. That message is the client's only guidance, so the error element was widened from `text-xs` right-aligned to readable left-aligned body text — it now carries a multi-sentence explanation, not a two-word failure. (2) **`app/portal/sign/oa/page.tsx`** decides whether to attach a per-member `?signer=` code from the EXISTENCE of `oa_signatures` rows, not from `total_signers > 1`; with one expected signature it previously emitted no code and the document page then accepted a signature from anyone holding the shared access code. Live exposure was Full Throttle Media LLC, which had no agreement yet.)_
_Prior: 2026-08-09 — Claude (**Formation wizard renders the RIGHT company shape; the signed contract decides and it never guesses** — dev job `fc69557f`. The wizard showed a single-member form to multi-member clients and then refused to submit (ITIN count resolved lead-independently, entity type did not, and defaulted to SMLLC). Fixed: contract → offer → ask the client one question; `account.entity_type` ignored for formation so a returning client’s old company cannot shape the new one; the guess is no longer persisted (NULL, and only on formation_submissions — the other three submission tables are NOT NULL). A member with no email now keeps their ownership row so the table totals 100. Present since the wizard’s first commit, 2026-03-21.)_
_Prior: 2026-08-08 — Claude (**WS-A backfill guard (shared with formation) — dev job `c0a61e44`.** The portal's account auto-create backfill no longer sweeps an UNSPENT credit note onto a newly created account; spent credits carry over freely. Person-scoped credit stays with the person until a human decides otherwise)_
_Prior: 2026-08-07 — Claude (**Portal chat admin send-scope invariant — cross-company leak fix, Phase 2** (dev job `4bad3094`, council-reviewed full tier). Context: 28 staff replies about one client's new company were auto-stamped with `account_id` = his OTHER company (the staff inbox defaulted person-thread replies to the contact's first open company, `app/(dashboard)/portal-chats/page.tsx` old lines 300/1912/1467) and the server inserted any admin-supplied `account_id` unchecked — readable by that company's other member for two months. Data containment ran 2026-08-07 (`scripts/migrations/20260807-2000-portal-chat-leak-retag-cm.sql`). **The rule now (one shared module, `lib/portal/admin-send-scope.ts`):** (1) `decideAdminSendScope` — a staff send carrying BOTH `contact_id` and `account_id` is REJECTED unless it explicitly declares `sender_context='company'`; (2) `isContactLinkedToAccount` — a declared company must be one of that person's companies (`account_contacts`), verified server-side, FAILS CLOSED; (3) `accountAudience` — members + active chat-capable portal teammates, for the composer warning. **Wired surfaces:** `POST /api/portal/chat` (admin branch), MCP `portal_chat_send` (which also now uses the shared deterministic `resolveAdminReplyContact` — moved into the module — instead of its old arbitrary `.limit(1)` member pick), the topic starter (`topic/create`), correspondence upload (`app/api/accounts/correspondence`, checked BEFORE the Drive upload), and the AI worker's portal send (`lib/ai-agent/worker-tools.ts`). `admin-actions/upload-document` needs no guard — it derives the account FROM the contact's own links. Client and teammate sends keep their pre-existing gates. **Staff inbox:** person-thread replies now default to PERSONAL scope (`selectedCompanyId` starts null; no auto-selected company); sending as a company requires an explicit chip click (chip "No tag" renamed "Personal"); the send body declares `sender_context` explicitly ('person'|'company' — these now render as the PR-2 badges client-side). An amber audience banner (`GET /api/portal/chat/audience`, staff-only) shows before any company-tagged send whose audience is >1 person — INCLUDING a solo company with chat-capable teammates. The six read-only side panels (AI assistant, Discuss-with-Team, Issues, To-Do/tag account attribution, notes, solo-company realtime arm) kept their company context via the new derived `panelCompanyId` (explicit selection ?? first open company) — send target and panel context are now SEPARATE values. Unit tests: `tests/unit/admin-send-scope.test.ts`.)_
_Prior: 2026-07-28b — Claude (**Push-adoption card + Gmail label for chat notification emails** (dev job `1fc9fac3`, Antonio's email-noise request). Context: the client-facing chat notification email (`notifyClientOfAdminMessage`) is skipped for clients with an active push subscription — but only 8 push subscriptions existed (7 real clients), so ~everyone fell back to email and Sent filled up. The staff-facing chat email was ALREADY off (app_settings `portal_admin_email_on_client_message`=false since 2026-05-21; toggle in Dev Tools → Maintenance) — do not re-remove it. **(1) `components/portal/enable-push-card.tsx`**, mounted in `app/portal/layout.tsx` next to `DashboardInstallBanner`: one-tap "Turn on notifications" card, `/portal` home only, **standalone (installed PWA) only**, mobile-only visually (`lg:hidden` — desktop keeps the header compact `PushToggle`, which is `hidden lg:flex`). Visibility rules are the pure `shouldShowEnablePushCard` in `lib/portal/push-card-visibility.ts` (unit-tested): standalone && push supported && permission≠denied && no subscription && not snoozed (30-day localStorage snooze, key `enable-push-card-dismissed`). Shows when permission=granted-but-unsubscribed (the SUBSCRIPTION is what stops the emails). Enable flow mirrors `push-toggle.tsx` including the mandatory `PORTAL_SW_SCOPE` (scope-'/' double-registration incident). Never coexists with the install card (that one hides in standalone). **(2) Gmail label "Portal chat notifications"** (`lib/gmail-labels.ts`: `getOrCreateLabelId` cached per user+name, `addLabelToMessage`, never-throwing `labelPortalChatNotification`): `notifyClientOfAdminMessage` files each sent copy under the label right after send (no-op in sandbox — sandbox send returns no message id). Backlog: `scripts/retro-label-portal-chat-emails.ts` (dry-run by default, `--apply` to label; add-only, idempotent). Filters are NOT possible: scopes are readonly/compose/modify only, and Gmail filters never touch sent mail anyway. **Council pass (4 core) fixes:** (a) `Notification.requestPermission()` now runs FIRST in the card's tap handler, before any await — iOS WebKit ties the prompt to the tap's transient activation and the card is standalone-iOS-targeted (do NOT copy push-toggle.tsx's await-then-prompt order into new mobile surfaces); (b) if saving the subscription server-side fails, the card unsubscribes the browser-side subscription — otherwise the device is stuck invisible-to-server forever (still getting emails) while the card hides itself; (c) **account-level push suppression is now trusted only for single-recipient notifications** (`recipients.length === 1` in `notifyClientOfAdminMessage`): on a multi-member account, one member's unattributable account-keyed subscription must not cut the other members/teammates off from both push AND email — mutation-tested regression in notify-client-admin-message.test.ts; (d) label-create race across serverless instances re-lists and adopts the winner.)_
_Prior: 2026-07-28 — Claude (**Tax wizard: the SS-4 Responsible Party picker and its "exactly one" rule no longer run on the tax questionnaire.** A multi-member client could complete every step and be refused at submit by a toast naming a tick box several steps back, with no link to it. The picker belongs to the EIN-application wizards only — `wizardRequiresSs4Signer` (formation + onboarding) is now the gate, strictly narrower than `wizardCollectsOwnerMembers`, which tax KEEPS because it also enforces the members-ownership 100% rule the K-1s depend on. Also on the tax members step: a per-person **US green card** question and, for company members, **country of formation** — both feed the derived foreign-partner answer, see tax-returns.md.)_
_2026-07-27 — Claude (**Bank Applications is now catalog-driven, and the portal home's "Partner Banks" box is RETIRED.** `app/portal/banks/page.tsx` no longer carries its own `RECOMMENDED_BANKS` array — it reads enabled rows from `bank_referrals` in `sort_order`, so Antonio adds/edits/reorders/removes banks from the CRM (Trackers → Banking Fintech) with no code change. The home-page "Partner Banks" section in `app/portal/page.tsx` was deleted along with its `getBankReferralsForAccount` fetch: it read the SAME catalog, so once the page became catalog-driven the client would have seen every bank twice. **Bank Applications is now the only client-facing bank surface**; staff still see per-account click status on the CRM account-detail page (`components/accounts/bank-referrals-panel.tsx`, untouched). Tiles: `managed` banks (Relay, Payset — "we submit for you") open the internal `/portal/wizard?type=banking_…` form same-tab and are not click-tracked; self-service banks route through `/portal/apply/bank/<slug>` so the click is recorded and the bank rep notified — which also fixed the page's Sokin tile, previously a bare untagged `sokin.com` link whose clicks were never credited to TD. **Wise removed** (Antonio); the EIN-issued client message (`app/api/flows/[id]/save-ein/route.ts`, EN+IT) no longer names any bank or points at wise.com — it just sends the client to Bank Applications in the portal. If the catalog is unreachable the page falls back to `FALLBACK_BANKS` (Relay + Payset) so it is never blank — so ship the migration BEFORE the code in any environment. Full detail in referrals-circleback.md.)_
_Prior: 2026-07-26b — Claude (**Client-generated signed documents now land in the client's Documents folder** (dev job `3c4ac6d0`). When a client generates AND signs a self-service document in `app/portal/documents/generate` (a distribution certificate / tax statement — NOT the OA, which keeps its own multi-signer flow), the browser now sends the signed PDF to `POST /api/portal/generated-documents/save-signed`, which persists it via the canonical `autoSaveDocument` (Drive + a `portal_visible` `documents` row with a real `drive_link`). Product rule: it SURFACES as "new" but the MAKER is not alerted — `lib/portal/save-generated-document.ts` stamps `client_notified_at`, pre-inserts a `portal_document_views` row for the maker (so no pulse/alert for them), and alerts only the OTHER account contacts (co-owners) per-contact, honoring the `new_document_alert_enabled` kill switch. Deliberately does NOT call `notifyClientsOfNewDocument` (account-scoped → would alert the maker). Tax statements file under category 3 (Tax), others category 1 (Company). Best-effort: on failure the server records a system error and the client shows a visible "downloaded but couldn't file it" note. Reviewed by full council + bug-hunter; both majors (missing drive_link; silent failure) fixed.)_
_Last verified against code: 2026-07-26 — Claude (**Sign Documents tab now blinks "new" for anything awaiting signature** (dev job `0dc29dc4`). The sign page's document assembly moved out of `app/portal/sign/page.tsx` into shared `lib/portal/signable-documents.ts` (`getSignableDocuments` + `getToSignCount`; `skipLegacyDrive` skips the always-signed Drive fallback for the count) so the tab count can never drift from the list. `app/portal/layout.tsx` computes `toSignCount` for the selected account in parallel with `unreadDocsCount` and passes it to `PortalSidebar`, which pulses + shows a numeric badge on the Sign tab (mirrors the Documents-tab pulse; clears while on `/portal/sign`). A draft Form 8832 is now guarded from rendering as signable (same class as the SS-4 draft guard). Clients are also alerted (in-portal chat + bell/push + email) the moment a lease is ready to sign — see `notifyLeaseReadyToSign` in `lib/portal/action-required.ts`, fired from `sendLeaseToPortal`.)_
_2026-07-24c — Claude (**The personal ITIN wizard was permanently un-submittable for multi-member-LLC clients** (dev job `abe6060f`; reported by Adam Mihaly / LUMA Beauty Global). The shared portal wizard (`app/portal/wizard/wizard-client.tsx`) gated the formation-only SS-4 Responsible-Party signer requirement — and the members-ownership-sum rule — on `isMMLLC = entityType === 'MMLLC' && !isBankingWizard`. That blocklist turned the check ON for EVERY multi-member wizard except banking, including the personal ITIN wizard, whose config (`ITIN_STEPS`/`ITIN_FIELDS`) has no owner/members step and therefore no signer picker. Result: on Submit the chosen-signer count was always 0, so it threw *"Select exactly one person as the SS-4 Responsible Party."* with no way to satisfy it — a permanent deadlock for the ITIN (and, latently, for the equally step-less `company_info` and `closure` wizards on a multi-member account). The ITIN belongs to the PERSON and has nothing to do with the company's member structure, so the check never belonged there. **Fix:** replaced the blocklist with an ALLOWLIST — new pure helper `wizardCollectsOwnerMembers(wizardType)` in `components/portal/wizard/wizard-configs.ts` returns true only for `formation`/`onboarding`/`tax`/`tax_return` (the wizards that actually render an owner + members roster), and `isMMLLC` is now `entityType === 'MMLLC' && wizardCollectsOwnerMembers(wizardType)`. This keeps the signer + ownership rules exactly as before for formation/onboarding/tax multi-member, and makes them inert for itin/banking/company_info/closure/td_communication. Allowlist over blocklist on purpose: a newly-added wizard type is now safe-by-default (no signer demand) instead of deadlock-by-default. `getWizardConfig` keeps its own internal MMLLC flag for step selection — untouched, decoupled from this one. Server side has no signer gate for ITIN, so the deadlock was purely client-side and the one-line change fully clears it. New unit test `tests/unit/wizard-signer-scope.test.ts` pins the allowlist. Verified end-to-end on a sandbox deploy as a real multi-member portal client: before = the exact error + stuck; after = "Data submitted successfully" + row marked submitted; formation multi-member still shows and enforces the SS-4 signer; company_info/closure multi-member load clean with no signer demand. Council (bug-hunter + senior-engineer + ai-architect) could not break the implemented change.)_
_2026-07-24c — Claude (**Wizard "clarify" fix — a blank required field no longer produces a silent, unexplained dead button.** Origin: the reopened MMLLC clients (Luca's list) + Matteo hit newer required questions the old form never had; the wizard blocked forward movement with a DISABLED grey "Next"/"Submit" (`canProceed={validateStep()}` → `disabled`) and NO per-field indication — a silent wall the client can't diagnose (exactly what made Matteo message us). Antonio: "the page shouldn't allow the client to go forward, but should just highlight the problem… instead of just giving an error, explain what the error is." **FIX** (`app/portal/wizard/wizard-client.tsx` + `components/portal/wizard/wizard-shell.tsx`): the boolean `validateStep` is now DERIVED from a new `getStepErrors(): Record<fieldKey, localizedMessage>` that mirrors the exact same rules (required-empty / below-min / repeater ≥1-row + required sub-fields / members-ownership-totals-100 / prepare-acknowledged) — ONE source of truth, so a valid step still advances byte-identically. `labelForKey()` turns a flattened key (`member_i_x`, `repeater_i_sub`, top-level, `__members_ownership`) into the field's localized label. `raiseStepErrors()` publishes the errors and scrolls to the first `[data-field-key]`. The Next/Submit buttons are now ENABLED (validity reflected via `aria-disabled`, not `disabled`): a FORWARD step change or a submit with errors highlights each blocking field red (WizardField already renders its `error` prop; wired at all three render sites — members, repeater, top-level), lists them by plain label in a red summary panel ("To continue, complete these fields:"), and STAYS on the step; backward / earlier-tab clicks clear the errors; editing a field clears its own error (existing `handleFieldChange`). Reuses the existing `FieldError[]` state + summary panel (was server-400-only; now also client-side). IT/EN copy from `locale`. **VERIFICATION:** full suite 7072 green, build + eslint + tsc clean; logic derived from the existing validator so the normal happy-path flow is unchanged (no regression). Deployed to sandbox (portal 200 / wizard 307). **Note (honest):** the visible highlight/scroll behaviour is a browser interaction not provable by unit tests — click-through: leave a required field blank → Next highlights it + names it + does not advance; fill it → clears + advances. Dev job `7829cc87`.)_
_2026-07-24b — Claude (**Portal chat push no longer uses the deleted "every registered device" broadcast** (td-bug channel change, dev job `59db1c17`). The two portal-chat notification writers — a new client message and a reaction — plus the "Team:" internal-note push from the `portal_chat_send` MCP tool now go through `sendPushToStaff` / `sendPushToStaffExcept` (`lib/team/notify.ts`), which resolve real staff through the one directory that excludes partners and clients and FAIL CLOSED on error. This replaces `sendPushToAdmin` / `sendPushToAdminExcluding`, both DELETED. Behaviour is unchanged for a two-person staff (both devices were staff) — the fix is that a client's or partner's browser can no longer be a push recipient. The MCP tool's dead pre-check on `admin_push_subscriptions` (which could swallow the push on a query error) was removed at the same time. No change to who SEES a portal message or to the unread model — only which devices receive the OS notification. Full rationale in `docs/systems/team-workspace.md` 2026-07-24.)_
_2026-07-24b — Claude (**Portal-side OA fixes (part of the OA post-ship sweep — full detail in `lease-oa.md` same date).** Two portal surfaces changed: (1) the formation dashboard no longer shows a "sign your Operating Agreement" call-to-action for a `voided` or `draft` agreement — the portal's own to-sign list already hides both, but the dashboard banner and the pending-signature nudge were still surfacing them and sending the client to a page that then said the agreement was outdated (21 voided agreements exist in prod). (2) The in-portal OA signing frame (`app/portal/sign/oa/portal-oa-client.tsx`) now verifies the origin of the "signed" message before flipping its banner, and no longer announces "signed and saved" to a PARTIAL multi-owner signer — the signing page reports whether the agreement is actually complete. Dev job `6b28ef2b`.)_
_Prior: 2026-07-24 — Claude (**The tax wizard now re-opens WITH the client's previous answers and documents — and an edit can no longer lose an attachment.** Companion to the flow-button fix in `tax-returns.md` / `flows.md` (same date); full root cause there. Two portal-side changes. (1) **EDIT-MODE PREFILL** (`app/portal/wizard/page.tsx`): `savedData` was loaded ONLY from `wizard_progress`. The ~64 clients who submitted through the EXTERNAL tax form have NO `wizard_progress` row — their answers live only on `tax_return_submissions.submitted_data` — so when staff reopened them for changes the wizard rendered essentially BLANK (contact/account prefill only), and the review-mode submit would then overwrite their real answers with whatever partial data they retyped. Now: when the wizard is `tax`, there is NO draft (`!progressId`), and the latest submission's `review_status` is client-editable, the page loads that submission's `submitted_data` as `savedData` and sets `wizardSubmitStatus='submitted'` (so the UI is in re-submit mode). `savedStep = 0` deliberately — he walks EVERY step, so a question the form gained since his submission (notably the $25k Form-5472 related-party gate, absent from every legacy `submitted_data`) is shown BLANK rather than skipped by a `current_step=99` jump straight to the documents step. Guarded on `!progressId`, so a normal portal client with a real draft is never prefill-clobbered, and on the editable review state, so a locked/under-review submission is never surfaced. (2) **DOCUMENT PRESERVATION on the review-mode resubmit** (`app/api/portal/wizard-submit/route.ts`): the branch used to replace `upload_paths` wholesale with `collectUploadPaths(data)`, which only returns WIZARD-PREFIXED paths — so an external-form attachment (Carasso's EIN letter, path `carasso-consulting-llc-2025/…`, matching no wizard prefix) had its reference silently destroyed on the first resubmit while the file sat on in storage. Now the write carries forward only prior NON-wizard paths (NEW `isWizardUploadPath` in `lib/portal/wizard-uploads.ts`) unioned with the newly collected wizard set. **The first cut of this unioned ALL prior paths and was caught by the bug-hunter**: every upload gets a fresh unique path, so a client REPLACING a wrong bank statement would have had the old one resurrected beside the corrected one, and a deliberately REMOVED file would come back — a regression for the normal review client, who previously got a clean overwrite. Splitting on the wizard prefix is what makes both true at once: the form can't represent an external path (the client can neither see nor delete it, so it must be preserved), while everything the form DOES show is fully client-controlled. Antonio's explicit scope decision, verbatim: *"we don't need a copy of his old submission. if he decides to change something is because the old one was wrong"* — so there is deliberately NO snapshot of the prior `submitted_data`; only attachments are protected. Guards: `tests/unit/wizard-uploads.test.ts` pins both union rules (external kept / replaced + removed not resurrected). Sandbox E2E `scripts/sandbox-qa/carasso-edit-button-e2e.mts` covers keep/replace/remove and the prefill lookup. Suite 7023 green. Dev job `84c8220b`.)_
_Last verified against code: 2026-07-24 — Claude (**Staff reply now clears the staff unread dot — WhatsApp semantics (dev job `335cbf3e`, Antonio: "when I answer to that client I shouldn't have any red dot from my own message... As WA works").** The red pill per thread in `app/(dashboard)/portal-chats` is the RPC's `unread_count` = `portal_messages` with `sender_type='client' AND read_at IS NULL` (verified against the LIVE prod `get_portal_chat_threads_v2` body — admin/system are NEVER counted, so a staff send can't create a dot; every dot Antonio saw was a genuine unread client message). **The real defect was that replying didn't reliably CLEAR it.** Clearing lived only in the browser: the dashboard POSTed `/api/portal/chat/read` in the send's `onSuccess` with a CONTACT-scoped body, and the staff contact branch of that route deliberately EXCLUDES multi-member accounts (the 2026-07-08c "one message, one staff thread" rule) — so on an MMLLC the client's message stayed flagged after staff answered. It also never ran at all for sends from any other surface (mobile edge cases, Inbox, MCP `portal_chat_send`, the AI assistant). **Fix:** new shared `lib/portal/mark-thread-read.ts` — pure, unit-tested planner `buildStaffReplyReadPlan` + executor `markClientMessagesReadForStaffReply`, called SERVER-SIDE from the reply write itself so it cannot be skipped. Scope mirrors the thread the reply lands in: `account_id` present ⇒ clear every client message on that account (solo OR multi-member — replying in the company thread means you read it; this is what fixes the MMLLC case, and it deliberately does NOT touch personal-NULL DMs); else contact thread ⇒ contact-tagged rows minus multi-member accounts + company-only legacy rows on non-multi-member linked accounts (identical to the staff branch of `/read`). **Topic filter deliberately dropped** — a reply means the whole conversation was seen, which also fixes named-topic messages that used to linger because open-thread mark-read only cleared the `topic IS NULL` general tab. `client_kept_unread` rows are never touched. Wired at three write sites: `app/api/portal/chat/route.ts` (awaited before the response so the sender's refetch already sees it cleared), the `portal_chat_send` MCP tool, and the assistant's `sendPortalMessageFromWorker`. **NOT wired** to the automated system/workflow notification writers — an auto-notice going out does not mean a human read the client, and marking those read would wrongly clear dots. Sandbox-verified end-to-end by Antonio on all three thread shapes; the untouched third thread proved no over-clear.)_
_2026-07-23d — Claude (OA "read-before-signing" reached the portal: the Sign-documents OA link now carries the selected `account` so a client with more than one company lands on the right agreement (`lib/portal/queries.ts`), and the signing page shows a DRAFT download + "I signed by hand" (optional scan upload) inside the portal iframe — full detail in `docs/systems/lease-oa.md` under "OA draft download & sign-by-hand".)_
_2026-07-23c — Claude (**Clients with a company never saw their PERSONAL notifications.** The bell's read query was a strict either/or — account_id if present, else contact_id — and the portal layout always passes the selected account, so for any active-tier client the contact branch was unreachable. Measured on PRODUCTION before the fix: **262 unread notifications hidden from 55 clients** — 128 chat alerts, 71 service updates, **51 form reminders** (chase-ups asking a client to complete a form they were never shown), 5 decision requests, 4 action-required, 2 new-document. Oldest April, newest the previous day, so it was ongoing. Tell-tale that this was an oversight and not a decision: the mark-as-read path in the SAME file already accepted BOTH scopes. Now the read ORs the two. **SECURITY:** the OR uses the contact id derived server-side from the session, never the query param — both halves are the caller's own data (the account is verified as theirs, the contact is them). A foreign account id or contact id is still rejected 403, verified after the change. **TEAMMATES keep account-only scope** — they must not see the owner's personal items. **THE TRAP, found in sandbox testing and NOT in review:** the first version of this fix used a bare `contact_id.eq.X` as the personal half. Most notifications carry BOTH an account and a contact (39 of 56 for the QA fixture), so that matched every company's items for that person — selecting company B listed company A's notifications. Not a cross-client leak, but it merges companies, which is exactly the model the portal moved AWAY from when chat was re-scoped per-company in 2026-06-24. The personal half MUST also require `account_id IS NULL`: "personal" means addressed to the person and tied to no company. Verified after the correction: zero cross-company items, personal items present under both companies, company scoping preserved.)_
_2026-07-23b — Claude (**The notification bell now catches up on wake too.** It was the last screen left behind by the wake work: a bare 30s `setInterval` with no wake handling, so a client returning to the portal saw a CURRENT chat badge sitting beside a bell up to 30 seconds stale — two unread indicators disagreeing on the same screen. **Why the page-level wake could not cover it:** `components/portal/portal-wake-refresh.tsx` calls `router.refresh()`, which re-runs SERVER components; the bell is a client component holding its own fetch state, so the refresh cannot reach it. Exactly the same reason `lib/hooks/use-portal-chat.ts` needed its own wake handling — if you add another client-fetched widget to the portal, it needs the hook too, and the page-level refresh will silently not cover it. The 30s poll DELIBERATELY stays: it is the fallback for anything the wake signal cannot see, and browsers freeze it while the app is backgrounded, which is precisely the gap the wake fills. **Testing note worth keeping:** the first version of the guard asserted `toContain('useWakeSignal')`, which passed on the IMPORT line alone — deleting the actual call left the test green. Caught by poisoning it. Assert the CALL shape, not the identifier; this is the same weak-guard mistake that let a Gmail-backed query sit undetected in the wake list a round earlier. **Known, NOT fixed here (out of scope, flagged):** the bell queries by account OR contact, never both — an active-tier client with a company selected never sees contact-scoped notifications.)_
_2026-07-23 — Claude (**The portal now catches up by itself when the client returns to it.** One primitive: on wake, `router.refresh()`. The portal is server-rendered, so that single call re-runs the layout AND the page — Documents, Sign Documents, Invoices, My Company, Addresses and the sidebar counts all come current at once. It is the same call `components/portal/pull-to-refresh.tsx` already makes on pull-down; only the trigger is new (`components/portal/portal-wake-refresh.tsx`, mounted in BOTH branches of `app/portal/layout.tsx` — the teammate branch returns early, and mounting only in the main branch would have silently excluded teammates). **Signing routes are deliberately excluded**: a refresh re-runs the page's server code, and a transient DB error or a staff action mid-session can flip the branch and replace an open signing document with "no agreement found" — while the client is signing. Nothing on a signing page goes usefully stale anyway. **Chat** is client-fetched, so `router.refresh()` cannot reach it; it catches up via its own subscribe-status resync, which now also marks the messages read — without that, a client returning to an open chat would see the new message on screen AND an unread badge for it on their phone's home-screen icon (a two-change interaction no single review surfaced). Its refetch REPLACES the list and sizes itself to what is already loaded: replace rather than merge because a merge cannot express an ABSENCE, so a message soft-deleted during an outage would linger on the client's screen forever (R100). **The unread badge was deliberately LEFT ALONE.** An earlier draft replaced its local counter with a server-value-plus-refresh; all four reviewers rejected it — it would not clear when the client opens chat (the layout does not re-render on soft navigation), it would silently drop the 2nd and 3rd of messages sent in a burst under a leading-edge throttle, and it would break mark-as-unread. The drift it was "fixing" is already handled by the prop-sync effect. **No custom reconnect logic**: supabase-js rejoins on its own (verified in the installed source — the channel schedules a rejoin timer on error/timeout, the socket reconnects with stepped backoff, and a rejoin re-fires SUBSCRIBED on the same channel because its receive hooks survive the resend). A previous attempt added a retry ladder that FOUGHT the library — tearing the channel down on each retry can leave the socket manually disconnected forever, which is where its dropped-message bug came from. The wake trigger exists because the socket's own heartbeat can take ~25-50s to notice it died, and timers are frozen while an app is suspended. **Cost, stated honestly:** one wake re-runs the layout — roughly a dozen awaited steps, and the nav-visibility check alone is ~10 queries. Hence the gate: only fire after a real absence (20s+), and never from "we have no record of them leaving" (a zero baseline reads as away-since-1970 and would fire on every desktop alt-tab). Council: 4 reviewers across 2 plan rounds; this is the 4th design and the first all four passed.)_
_2026-07-22g — Claude (**The Generate Documents page was styled for a DARK theme inside a light portal — the page title was invisible.** Found by E2E browser QA, not by eye: a programmatic contrast sweep on the live sandbox measured the `h1` "Generate Documents", the section heading, and "Document History" at **1.1:1** — near-white text (`text-zinc-100`) on the portal's white background. Not merely ugly: unreadable. The three document cards rendered as dark grey slabs (`bg-zinc-800/50`) with washed-out descriptions, and the form fields were dark-on-dark. 44 dark-theme classes in one file — the page appears to have been authored against a dark shell the portal no longer uses. **This is the page Lorenzo Cassi was told to use to generate his Operating Agreement, and the washed-out headings are visible in the screenshot he sent** — very likely part of why he struggled. Converted to the portal's own light palette, taken from the sibling `app/portal/sign/sign-documents-client.tsx` rather than invented: headings `text-zinc-900`, body `text-zinc-700`, secondary `text-zinc-500`, borders `text-zinc-200`, cards white, read-only fields `bg-zinc-50`. Neutral action buttons deliberately KEPT dark (`bg-zinc-700` + `text-white`) — that is ~7:1 on a light page and reads as a button, not a bug. Accent hovers moved from the -400 shades (correct on dark, too light on white) to -600. **If you touch this page, verify contrast programmatically** — this class of bug is invisible in a code review and easy to miss in a screenshot.)_
_2026-07-22e — Claude (**"Sign Documents" is now ALWAYS present for company clients — never gated on whether they have a pending document.** Antonio's rule, verbatim: *"this tab must be there even though there is nothing. It doesn't make sense to have a tab that disappears if there is no document, or if there is a document."* Origin: Lorenzo Cassi generated his own Operating Agreement, support told him "go to Sign Documents", and the entry was not in his sidebar. The old `navVisibility.pendingSignatures` gate (`components/portal/portal-sidebar.tsx`, fed by `getPortalNavVisibility` in `lib/portal/queries.ts`) was wrong in FOUR independent ways, only the first of which was the reported symptom: (1) **STALE** — computed once per full page load in the portal ROOT layout, and the create call is a Route Handler, which unlike a Server Action does not invalidate the client Router Cache, so a client who generated a document mid-session never saw the entry appear; (2) **FAIL-CLOSED** — every count discarded its error and coerced to 0, so a transient PostgREST failure silently removed the client's only route to their documents with no log line; (3) **INCOMPLETE** — it counted 3 document families while `/portal/sign` renders SEVEN (also MSA, Form 8832, `signature_requests` e.g. the 8879, and e-sign envelopes), so a client whose only pending item was an 8879 could not reach the page from the nav at all; (4) **MISMATCHED** — it counted ALL rows per family while the page renders only the NEWEST row per family. Plus the mirror-image failure nobody had noticed: once a client signed everything the count hit zero and the entry VANISHED, taking away the route to their own signed documents. The entry is now tier-gated only; the page already owned a bilingual empty state. **Counts now FAIL OPEN** — `countOrFailOpen` logs and returns 1 (section VISIBLE) instead of 0, because hiding a section is the expensive failure and showing an empty one is cheap; it must never throw, since `getPortalNavVisibility` is awaited inside the portal root layout and a throw blanks the whole portal. **Also: completion is the refresh signal** (Antonio's own design call) — finishing "Create & Send for Signing" calls `router.refresh()` so the entry appears immediately, rather than any timer or broadcast, which on a document screen would wipe a half-filled form. It sits OUTSIDE the try block: a throw there would flip the UI to red after the agreement was created and the signing links were already sent, and the client's natural retry deletes and recreates the agreement. **REJECTED during review:** aligning the counter's predicate to the sign page's — the AI Architect showed it would have made the reported bug PERMANENT, because nothing on the client's path ever moves a portal-created OA off `draft`. Council: 4 core reviewers, all four returned blockers across two rounds. Live-updates work (global refetch-on-focus, shared realtime reconnect hook) was deliberately SPLIT OUT of this ship — see the dev-board job; it touches every staff screen and its Gmail-quota exposure cannot be sandbox-tested.)_
_2026-07-22d — Claude (**TD's office address now lives in ONE place — `lib/td-address.ts` — with its three PURPOSES kept apart.** It had been hand-written in ELEVEN source files, and the comment in `app/portal/flows/[id]/page.tsx` claimed it lived in two others and NAMED THE WRONG ONES. On the day the office moves, whoever trusts that comment updates a few sites, misses the rest, and a client posts their ORIGINAL wet-ink W-7 plus passport copies to a dead address — unrecoverable. **Deliberately NOT one constant.** Three named exports off one source value, because they answer different questions and are equal only by coincidence today: `MAILING_DESTINATION_*` (where CLIENTS post to us — both portal surfaces + the two ITIN email templates in `lib/mcp/tools/itin-form.ts`), `CAA_IDENTITY_ADDRESS` (TD's address as Certified Acceptance Agent on the W-7, beside the EIN/PTIN/office code — a regulated identity block, not a delivery address), and `CLIENT_ADDRESS_FALLBACK` (stands in for the CLIENT's company address on the SS-4 when their account has none — semantically the client's address, borrowing ours). Collapsing them would make a new document-receiving office silently move the CAA identity block too. **SS-4 precedence untouched:** the client's mailing address wins, then their physical address, and only then the fallback (`lib/operations/ss4.ts`). Also replaced in `lib/mcp/instructions.ts` — prose the assistant repeats to staff and clients. New guard `tests/unit/td-address-single-source.test.ts` fails if anyone hardcodes it again; it strips comments first, since an example in a doc comment is not a delivery path. Test fixtures deliberately KEEP the literal — a tripwire that fires if the value changes. **KNOWN GAP, not fixable from code:** the staff workspace's mailing note at the ITIN Client Signing stage stores the address inside `pipeline_stages.stage_layout` — a DB row. It will NOT follow the constant and must be changed by hand if the office moves, and it is client-facing instruction text, so it is the riskiest remaining copy. Suite 6747 green. Dev job `0d5c7695`.)_
_2026-07-22b — Claude (**The client's ITIN was split across two pages that contradicted each other — put back in one.** Origin: Antonio, viewing the portal as Pietro De Pellegrino, could not find the ITIN at all. **The damaging half:** at Client Signing the client saw the tracking form in the `Client Signing` card on this page, while the button that actually moved the application — "I have mailed the documents" — lived on `/portal/itin-documents`, which has NO tracking field. `setServiceDeliveryShipping` (`lib/operations/service-delivery.ts`) wrote four columns and stopped: no stage change, no notification, no revalidate. And the action-required dispatch for `ITIN::Client Signing` (`lib/portal/action-stage-registry.ts`) deep-links to `/portal/flows/{sd_id}` — the page that could NOT advance. So a client could do everything asked, on the page we sent them to, and from the staff side it looked like they never responded. **Now one action does both:** `components/portal/itin-shipping-form.tsx` records the shipment, then calls the SAME server action the other page used (`confirmItinMailed`, `app/portal/itin-documents/actions.ts` — contact-scoped, ownership-checked, workflow path with legacy fallback). Order is deliberate: the tracking POST runs FIRST and is harmless alone, so a failed advance leaves a saved tracking number and a retryable button rather than a half-advanced application, and surfaces the server's real reason (R099). A pre-existing tracking number means a later Edit is a CORRECTION, not a second confirmation — it does not re-advance. **Also here:** (1) the CAA warning "do NOT mail your original passport — colour photocopies only" existed ONLY on `/portal/itin-documents`; a client following the emailed link never saw it, and mailing an original passport is unrecoverable — now on the card via a new optional `passportWarning` on `ShippingCard`. (2) A "Complete your ITIN application" CTA at the FIRST journey step, which previously had NO button — the client read "we need your personal information and passport to start" with nothing to press, the form being reachable only via a sidebar item labelled "Complete Setup" naming neither service nor task. (3) `buildItinShippingCard` is now gated to `stage === 'Client Signing'`; it was built for EVERY ITIN stage (only the inner form was gated), so a client at Data Collection could open "Print, sign & mail your documents" onto an empty document list. **Keying:** the CTA is POSITIONAL (`journey[0].state === 'current'`), never the stage NAME — stage names are editable from the service editor, and `replaceStagesForService` (`lib/services/stages.ts`) DELETEs and re-inserts stage rows without `client_label_it`/`icon`/`stage_layout`, so a name is not a safe key. Positional also makes the dangerous case impossible: three clients have applications already AT THE IRS (verified in production), and telling any of them to "start your application" would be worse than silence. `tests/unit/flow-progress.test.ts` pins all four directions incl. a renamed first stage. **NOT done, deliberately:** `/portal/itin-documents` still exists and still works — retiring a live client surface needs its own decision; the two now overlap. **Flagged, not fixed:** the client can advance to Documents Received before anything reaches the office; the TD mailing address is duplicated across ELEVEN non-test source files while a comment in this page names only two; `client_description` is EN-only so an Italian client reads an Italian stage title over an English body. Council reviewed the PLAN (4 core, all four returned blockers — the dashboard-card variant was REJECTED: the account-scoped already-submitted check cannot see a contact-keyed ITIN row, so the card would never disappear and would invite re-opening a filed IRS application). Suite 6739 green. NOT verified in a browser by Claude — the shared sandbox session had switched users; Antonio confirmed the client view himself. Dev job `0d5c7695`.)_
_2026-07-22 — Claude (**A client with no company yet was promised a "Tax Return" form and served "Company Information".** Found by the bug hunt on the label work; PRE-EXISTING, traced to `3e15d369` which added the tax eligibility gate to the account-scoped action-item builder only. **CORRECTION to how this was first reported:** it is NOT a wrong destination. `getPortalActionItemsByContact` runs ONLY on the no-account branch (`app/portal/page.tsx` ~381), its SD query filters `account_id IS NULL`, and `decideTaxWizardEligibility` returns `company_info` for EVERY accountless subject unconditionally (`lib/tax/wizard-eligibility.ts` ~144) — so `app/portal/wizard/page.tsx` (~382-385) rewrites `?type=tax` to `company_info` and the client lands on the CORRECT form. The defect was the card's promise, not where it led. **FIX:** in that loop a `tax` wizard type is mapped to `company_info` before the dedup and the card is built (`lib/portal/queries.ts` ~1773). Deliberately NOT the account-scoped builder's `if (wt === 'tax' && !taxWizardActionable) continue` — suppressing the card here would leave these clients with NO entry point at all. Mapping also FIXES THE DEDUP, which was asking whether a `tax` wizard was in progress when the wizard they actually fill is `company_info` (that mismatch could stack two cards; it cannot happen today only because the CHECK constraint still rejects `company_info` rows — see the separate carded job). **Exposure at time of fix: ZERO** — a production query for active contact-scoped Tax Return SDs returned no rows, so no client is currently hitting it. **Test infrastructure note:** `tests/unit/portal-queries-by-contact.test.ts` previously could not test item generation at all (its mock returned the same fixture for every branch of the builder's `Promise.all`, and only settled on `.limit()`, which several branches never call — the file said so in a comment, and this defect hid in exactly that blind spot). The harness is now per-table and thenable, and two behavioural tests cover the mapping. Mutation-verified: removing the mapping fails the test naming the wrong link.)_
_2026-07-21c — Claude (**Wizard labels, round 3 — THE NAME COMES FIRST, plus three more leaks a bug-hunt found.** Round 2's Italian was grammatically correct but created a WORSE bug on mobile. **(1) MOBILE COLLISION (self-inflicted, now fixed):** the card title is `truncate`d in a ~196px column at 380px — and the entire portal/CRM is used as a phone app. Leading with the verb gave "Completa il modulo per il Conto Bancario Payset", clipping to "Completa il modulo per il C…", so a client holding BOTH bank forms (TFC Management LLC does) saw TWO CARDS THAT READ THE SAME. Measured live: 322px of text in a 196px box. Titles now lead with the NAME — `${label} — completa il modulo` / `${label} — complete your form` (wording chosen by Antonio). Verified at 380px: "Conto Bancario Payset —" vs "Conto Bancario Relay — comp". **This also DELETED the `itOf` field:** with the name no longer inside a prepositional phrase there is no per-label preposition to get wrong, so that whole bug class is gone rather than merely tested against. **(2) THE FEED STILL LEAKED, on offers:** round 2 fixed the wizard events in `account-activity.ts` and walked past the offer events two functions below — `Offer created — ${o.contract_type}` and the "Contract signed" sub-line. Production counts 2026-07-21: renewal 162, formation 59, onboarding 14, tax_return 4, itin 1 — the most common offer in the business was showing clients an internal code. Now via NEW `offerTypeLabel()`, which returns NO suffix for an unknown type rather than the raw string. **(3) ITIN RENEWAL clients were told to start an ITIN *Application*** — both services open the `itin` wizard, so the home card and the wizard tab disagreed by construction (the tab had its own hardcoded renewal label). NEW `SERVICE_LABEL_OVERRIDES` keyed by `service_deliveries.service_type` is read by BOTH; the Start-card sites now pass `sd.service_type` through. **(4) THE STAFF ACCOUNT PAGE had its own label list** (`components/accounts/account-journey.tsx`) missing `company_info` + `td_communication`, so staff read raw codes — and round 2's "one list" claim was false. Staff wording stays deliberately more specific ("Banking (Payset EUR)" tells Luca the currency), but the map is now `Record<WizardType, string>` so the compiler forces completeness. Guards (15 tests) mutation-verified: restoring verb-first titles fails a phone-collision test that NAMES the colliding pair; dropping the renewal override or the renewal offer label each fails. A THIRD pre-existing test was found pinning a raw code ("Offer created — formation") — these tests were protecting the bug. 6,656 unit tests green; build clean. **NOT fixed, pre-existing, filed separately — both worse than the label bug:** (a) an ACCOUNTLESS tax client is shown "Tax Return — start your form" and lands on the **Company Information** wizard, because the tax eligibility gate exists on the account-scoped action-item path and was never added to the contact-scoped copy; (b) a tax client can get TWO cards for the same form under two different names ("Tax Return — start your form" and "Complete Tax Information — 2025"), because the second card's dedup only checks in-progress wizard rows, not SD-surfaced ones. Sandbox — Antonio ships.)_
_2026-07-21b — Claude (**Wizard labels, round 2 — the round-1 fix was incomplete and forked the vocabulary. QA caught both.** Round 1 (below) consolidated four label maps but (a) left a FIFTH client-facing renderer leaking, (b) invented a service name, and (c) broke the Italian. All three found by E2E QA + adversarial review, all fixed here. **(1) The leak was still live:** `lib/operations/account-activity.ts` built `Wizard started — ${w.wizard_type}` / `Wizard submitted — …`. That feed is CLIENT-VISIBLE — `'wizard'` is whitelisted in `lib/portal/journey-events.ts` PORTAL_VISIBLE and rendered verbatim by `components/portal/chat/log-tab.tsx` in the portal chat "Log" tab — so the same three clients read "Wizard started — banking_payset". Now goes through `wizardLabelFor`. (The feed is English-only for EVERY event type, not just wizard; localizing it is a separate job, deliberately not done here.) **(2) Vocabulary fork:** round 1 renamed closure to "LLC Closure"/"Chiusura LLC" while the services catalog, the guide, the wizard page and the services list all said "Company Closure"/"Chiusura Società" — a fork created by the commit meant to END forks. Reverted, and formation aligned to the catalog's "LLC Formation"/"Costituzione LLC". **NEW GUARD:** a test asserts every wizard label equals the `SERVICES_STATIC` catalog entry (R106 = catalog is source of truth). It immediately caught a second fork nobody had noticed — onboarding's Italian is "Onboarding LLC esistente" in the catalog, not "Onboarding". **(3) Italian grammar:** `Completa il modulo ${it}` produced "Completa il modulo Costituzione" (wrong — the old copy correctly had "di"), and the sibling "Start" card still said "Inizia il modulo di …", so an Italian client saw two grammars in ONE list. Labels now carry a third field `itOf` — the fragment after "il modulo", per-label because the preposition differs ("di Costituzione LLC" vs "per il Conto Bancario Payset") — and BOTH sentence builders (`completeWizardFormTitle` + NEW `startWizardFormTitle`) live in the label module and consume it, so they cannot drift again. **(4) Wizard page tab strip** (`app/portal/wizard/page.tsx`) was a SIXTH hardcoded English-only map ("Payset (EUR)", "LLC Formation"): a client clicked "Complete your Payset Bank Account form" and landed on a tab reading something else, in English even with the portal in Italian. Now `label`/`labelIt` from the shared map + locale-aware render; the ITIN Renewal vs ITIN Application distinction is preserved deliberately (different services), and the merged Banking picker tab is localized. Guards: `tests/unit/wizard-labels.test.ts` (13 tests), mutation-verified — reintroducing the grammar bug, the invented name, or the activity-log leak each fails. Two pre-existing tests were updated because they PINNED the old strings — one was asserting the raw code "Wizard started — formation", i.e. the test was locking the bug in. 6,654 unit tests green; build clean. **STILL OPEN / Antonio's call:** the tax form's Italian is "Dichiarazione Fiscale" in the catalog (followed here) but "Dichiarazione dei Redditi" in the tax progress tracker + guide — both correct Italian, needs one choice made everywhere. Sandbox — Antonio ships.)_
_2026-07-21 — Claude (**Wizard labels: ONE compiler-checked map — the internal-code leak that shipped twice.** On 2026-07-20 the reminder cron emailed a client "Complete your banking_relay form" (no label for the banking types); it was fixed inside the cron's own private map. On 2026-07-21 the **portal home** was found leaking identically — `getPortalActionItems` and `getPortalActionItemsByContact` each built their card title from a byte-identical inline ternary naming only `formation`/`onboarding`, everything else falling through raw — and `labelForWizardType`/`labelForWizardTypeIt` in the same file knew `itin`/`closure` but not the banking types. **Verified against production that day: three real clients were reading "Complete banking_payset Form" on their portal home — TFC Management LLC (payset + relay), PTBT Holding LLC, LC Marketing Consulting LLC** (each has an in-progress banking wizard AND an active Banking Fintech SD, so they pass the `WIZARD_SD_REQUIRED` gate). Four independent label maps existed; patching them one at a time is precisely what produced the second incident. **NOW:** `lib/portal/wizard-labels.ts` is the single source — `WIZARD_LABELS` is typed **`Record<WizardType, {en,it}>`** keyed off `VALID_WIZARD_TYPES`, so adding a wizard type without a label is a **COMPILE ERROR**, not a client-visible code. `wizard-reminder-copy.ts` re-exports from it (cron unchanged); both `queries.ts` switch helpers delegate; both action-item builders call the new `completeWizardFormTitle(type, lang)`, which also aligns the card wording to the reminder email's ("Complete your ⟨Label⟩ form") so a client who gets the email and then opens the portal reads the same words. `tax_return` (the service slug, which leaks into wizard-shaped call sites) resolves via an alias map rather than polluting the type. Guard: `tests/unit/wizard-labels.test.ts` — mutation-verified both ways (reintroducing a raw-code label fails 3 tests; reverting one call site to a hand-rolled ternary fails 2). NOT a fix for the separate `wizard_progress` save-progress defect (`company_info` rows are still rejected by the CHECK constraint) — that is carded. Council-reviewed. Sandbox — Antonio ships.)_
_2026-08-02b — Claude (**View-as now leaves NO trace on the client at all — it also restores `recovery_sent_at`.** Antonio, 2026-08-02: *"when I click view as client is for technical stuff. It shouldn't do anything anywhere. It's just for us to help the client and guide me according to what he sees in his portal."* That was already the design — `app/portal/view-as/route.ts` captures `last_sign_in_at` before minting and puts it back after (migration `20260615-2100`), so a staff visit never counts as the client logging in. But it missed a SECOND stamp: the session is minted with `generateLink({type:'magiclink'})`, and GoTrue writes `auth.users.recovery_sent_at` when it mints that link. **PROVEN CAUSALLY in sandbox, not inferred from correlation:** `qa-e2e-pnl@tonydurante.us` had `recovery_sent_at` NULL → one magiclink mint at 22:53:04.232Z → immediately after, `recovery_sent_at` = 22:53:04.243512Z. **Why it mattered enough to fix:** that column is the obvious place to look when asking "did we send this client a reset email?", and View-as was filling it with false positives — on production, 100 users carried the stamp and **96 fell within 120 SECONDS of a `portal_view_as_enter` row**. Reading it as "clients requested resets" burned most of a session while diagnosing Chiara Fazzini. New `viewas_restore_auth_stamps(p_user_id, p_last_sign_in, p_recovery_sent)` (migration `20260802-2300`, same SECURITY DEFINER + service_role self-guard model as its predecessor; a NEW function rather than a third argument, which would have created a Postgres overload beside the old 2-arg one). Round trip verified in sandbox: NULL → stamped by mint → restored to NULL with `last_sign_in_at` untouched. **This stops View-as ADDING noise; it does NOT make the column trustworthy** — a real successful self-serve reset on PRODUCTION left it NULL anyway, and prod/sandbox disagree. The authoritative record is the `action_log` row written by `lib/portal/password-reset.ts`. Dev job `fb4bcae4`. NOT on production; Antonio ships (migration needs promoting too).)_

_Prior: 2026-08-02 — Claude (**The "forgot password" email now comes from US, in the client's language — and every attempt is recorded.** Trigger: Chiara Fazzini (CF Consulting LLC) could not recover her password and no email ever reached her. The client-facing surface changed in three ways. (1) **`app/portal/forgot-password/page.tsx` no longer talks to Supabase from the browser** — it POSTs `/api/portal/password-reset`, which mints the link server-side and sends a TD-branded bilingual mail through our own Gmail (full mechanics + the `recovery_sent_at` trap in `auth-oauth.md` 2026-08-02). The old page printed "Check your email for a password reset link" as the `else` branch of `if (error)` and **never showed WHICH address it used** — so a client who mistyped, or who has a second login (`auth-oauth.md` documents that login email ≠ contact email is normal), waited forever for mail that was never going to be sent. It now echoes the exact address back. It also had **no try/catch and no timeout**: a rejected request left the button stuck on "Sending…" permanently with no message — now an AbortController 15s timeout + R099 error surfacing. (2) **`app/portal/reset-password/page.tsx` is bilingual via a `?lang=` marker on the link, NOT via `LocaleProvider`.** This is deliberate and load-bearing: the page is reached LOGGED OUT, `app/portal/layout.tsx:66-68` renders logged-out children with **no `LocaleProvider`**, and `getLocale` reads `user_metadata.portal_language` which **requires a session** — so an Italian client always landed on an English "New Password" card (Antonio reproduced this on 2026-08-02). Since we now mint the link, the sender is the only place that still knows the client's language, so it rides on the URL. Anything arriving without the marker falls back to English. Copy lives in a local `COPY` const — `lib/portal/i18n.ts` has **zero** keys for login/forgot/reset, so there was nothing to extend. Also added: real `htmlFor`/`id` label association and `autoComplete="new-password"` (phone keychains could not offer to save the new password), and `role="alert"` on the forgot-password error. (3) **Language is resolved from the OLDEST matching contact, not `.maybeSingle()`** — live sandbox data has `tdtest@tonydurante.us` on two contacts, one Italian one English, and `maybeSingle()` errors on multi-row and hands back null → silent English downgrade for exactly the duplicated clients. **NOT fixed, deliberate:** the reset page still renders inside the full portal shell when the browser already carries a session (sidebar + banner + "logged in as X" behind the card) — cosmetic, and fixing it means changing how the layout decides to draw its frame. **Verified live, not assumed:** request → Italian branded mail from `Tony Durante <support@tonydurante.us>` delivered 22:31:02 → reset page rendered "Nuova Password" / "Verifica del link in corso…". Mixed-case padded input (`"   TdTeSt@TONYDURANTE.us   "`) matched and logged clean; an unknown address returned a byte-identical response. Dev job `fb4bcae4`. SANDBOX/local only — NOT on production; Antonio ships.)_

_Prior: 2026-07-21 — Claude (**A standalone ITIN is now reachable for a client who ALREADY owns a company — the portal was looking for it on the wrong owner.** Origin: Antonio — Pietro De Pellegrino (DeP Consulting LLC) bought an ITIN months ago; Luca added the ITIN service by hand in the CRM on 07-21 and the client had NO way to start it, so the To-Do board card read "Send the link to the client" with no link that exists. **Root cause:** `createSD` (`lib/operations/service-delivery.ts:221-247`) strips `account_id` from EVERY ITIN service delivery — "the ITIN belongs to the person, not the company" — but the portal READ side never matched that write rule. `app/portal/wizard/page.tsx` discovery was an either/or: account-scoped SDs when the client has an account, contact-scoped only when they do not, with a closure-only exception list (`FLEXIBLE_SERVICE_TYPES_BY_WIZARD`). `lib/portal/wizard-visibility.ts` (the sidebar "Complete Setup" entrance) repeated the same shape. So every ITIN client this had ever worked for arrived via a formation and had NO account yet; the first buyer who already owned a company was structurally invisible. **Fix — the person-ownership rule is declared ONCE** in `lib/portal/wizard-map.ts` as `PERSON_OWNED_WIZARD_TYPES` / `PERSON_OWNED_SERVICE_TYPES_BY_WIZARD` (`itin` → `["ITIN"]`), read by BOTH discovery surfaces through the new shared accessor `getContactScopedDiscoveryServiceTypes()` (flexible ∪ person-owned) — the two hand-copied unions had already drifted (differing row limit + status filter) and this stops a third divergence. Deliberately a THIRD category, not an addition to `FLEXIBLE_WIZARD_TYPES`: flexible means "either scope depending on the SD", which is false for ITIN (always the person). Company Formation stays out by construction — it is routed by the company switcher / `?lead=`, and unioning it would leak a second company's formation into the portal of a client operating a different one (the `bb54680b` class, prevented identically in `lib/portal/queries.ts:657-659`). **Scoping now follows the same rule:** `resolveWizardProgressScope` + `accountIdForWizardSubmission` (`lib/portal/wizard-scope.ts`) key ITIN progress AND submission on the CONTACT. Without this, two members of one LLC who each buy an ITIN share ONE company-keyed row — person B loads person A's passport/DOB prefilled and B's own submit returns "Already submitted" as a silent no-op. **This is live today: LUMA Beauty Global LLC has TWO contacts (Adam Mihaly, Peter Marton Nemeskeri) each holding an ITIN, both at Data Collection, both with their own portal login** — pinned by a unit test. ITIN takes its own scope branch with `restrictToNoLead: FALSE`; inheriting formation's flag would hide a lead-stamped ITIN row (an ITIN sold inside a formation offer) and re-offer a filed wizard. **NO migration needed — verified against production, not assumed:** all 8 `wizard_progress` type-`itin` rows and all 8 `itin_submissions` already carry `contact_id` (4/8 and 5/8 respectively ALSO carry an `account_id`, which is now simply ignored on read), and ZERO carry a `lead_id`. The council had unanimously demanded a hand migration; the data said otherwise. **Two consequences of the re-keying, both handled:** (1) `app/api/itin-form-completed/route.ts` resolved the client's Drive folder and company label from `submission.account_id` — now null — which would have filed every ITIN package of a company-owning client into `Leads/{name}/` instead of the company folder; it now resolves a `driveAccountId` from the submission's account when present, else the contact's primary linked account. (2) `canSubmitWizard` (`lib/portal/wizard-submit-access.ts`) gates teammates on `accountId` alone, so a null account would have passed unconditionally — a portal teammate could have filed a W-7 in another person's name and written DOB/citizenship/passport onto their contact row. Teammates are now DENIED person-owned wizards outright (they have no contact identity); threaded through both `wizard-submit` and `wizard-progress`. **Sidebar nag fixed:** an ITIN SD stays `active` for the months the IRS takes, so "active ITIN exists" no longer means "still owes us the form" — `computeHasWizardPending` suppresses person-owned SDs whose wizard is already submitted (closure behaviour unchanged, asserted by a test). **DELIBERATELY EXCLUDED — "ITIN Renewal":** `createSD` forces contact scope for `service_type === "ITIN"` EXACTLY, so a renewal SD keeps its `account_id` (pinned by `tests/unit/operations-service-delivery.test.ts`). Listing it would discover nothing (the query filters `account_id IS NULL`) AND route renewal clients into the ITIN-application chain, which would mint a NEW ITIN application SD while the real renewal sat untouched, or 23505-crash against `uq_itin_sd_active_per_contact`. Bringing renewals onto this rail is a separate, deliberate change. **Also rejected by the council and NOT built:** dropping the either/or for a blind union (formation leak); provisioning a `wizard_progress` row at CRM activation (wizard availability is DERIVED from the SD — nothing reads such a row to decide availability, so it would change nothing and create a second truth); gating discovery on the stage name "Data Collection" (stage names are editable from the workflow editor, so a rename would silently hide the form from every ITIN client, and it locks out a client whose doc generation failed after submit); and adding auto-task + portal-user provisioning to the CRM "add service" route (that route ALREADY auto-creates a TD invoice, so one staff click would both bill a client and email them a temp password — plus it would push a mid-formation client to tier `active`; filed as its own job, NOT done here). Unit tests 6654 green (wizard-scope +6, wizard-visibility +3, wizard-submit-access +4); **negative control performed** — disabling the person-owned map made exactly 5 of the new tests fail, restoring it returned them to green, so they are not passing vacuously. Lint + build green. Dev job `0d5c7695`. SANDBOX ONLY — NOT on production; Antonio ships.)_
_2026-07-16 — Claude (**Tax wizard is eligibility-gated on every portal surface — the PTBT wrong-year incident.** A company formed 2026-03-02 self-served the tax wizard (home "Start Tax Return Form" card had NO stage gate; its `?type=tax` link bypassed the wizard page's gate via `forcedType`; no server-side check) and the submission was silently stamped tax_year=2025 by a DB default. NOW: ONE resolver — `lib/tax/wizard-eligibility.ts` — is consulted by (1) `getPortalActionItems` (`lib/portal/queries.ts`: the in-progress tax card, the SD-driven Start card, AND the tax_returns-driven "Complete Tax Information" sibling card all require mode open/review), (2) `app/portal/wizard/page.tsx` on ALL THREE resolution paths (derived picker, `?type=` forced, offer-fallback) — genuinely-closed states render a friendly bilingual "not available" card, locked review states (under_review/confirmed) keep the read-only view, and an eligible tax wizard now shows a **tax-year chip** so the client sees the filing year, and (3) the submit route (the real gate — 409 + typed reason BEFORE any state write). Contact-scoped standalone tax clients resolve to `company_info` mode (intake preserved). Full rules + handler/migration detail in `tax-returns.md` §"Wizard eligibility". Council-reviewed (dev job 8cc8e1c8); sandbox — Antonio ships. **SAME-DAY ADDITIONS (Antonio-approved after the MMLLC E2E walk):** (1) **Submission-token collision fix** — fresh portal submissions now mint `portal-{slug}-{period}-{scope8}` via NEW `lib/portal/submission-token.ts::buildSubmissionToken` (period = `ty{taxYear}` for tax, else calendar year; scope8 = account → lead → contact id prefix). The legacy name+calendar-year token let ONE OWNER'S SECOND COMPANY hijack+overwrite the first company's submission through the `onConflict: 'token'` upsert — PROVEN LIVE during the walk (QA Solo Owner's submit reassigned Uxio Test LLC's existing row). Applies to all six upserting submission tables (formation/onboarding/tax/company_info/itin/closure); review-loop edits update by row id and never touch tokens, so legacy tokens on existing rows stay valid; retries rebuild the identical token (idempotent). Tests: `tests/unit/submission-token.test.ts`. (2) **Default-company rule unified** — `app/portal/wizard/page.tsx` now derives its no-cookie default from `getPortalAccounts` (primary first, alphabetical, Active/Suspended only), the same list the layout + home use; it previously picked the first row of an unordered, status-unfiltered link query, so a two-company client saw home scoped to one company and the wizard scoped to the other.)_
_2026-07-13 — Claude (**Activation's account-create now moves a lead's Team Chat conversations to the new account.** `ensureMinimalAccount` (`lib/portal/auto-create.ts`, the payment/activation account-create path) calls `reanchorLeadConversations(leadId, account.id)` right after it links the lead (`converted_to_account_id`), so any Team Chat conversation opened on the lead follows it into the "Active clients" bucket. Additive/best-effort; no change to portal/activation behavior. Part of dev_task `be582c5e` (Conversations buckets + auto-move); full detail in `team-workspace.md`. Shipped to prod 2026-07-13.)_
_2026-07-09b — Claude (**Bank application (Relay/Payset) no longer inherits company-formation's MMLLC rules.** **Bug (Adam Mihaly / LUMA Beauty Global LLC):** the in-portal Relay intake (`/portal/wizard?type=banking_relay`, generic `WizardClient`) is prop-drilled the client's real `entityType`; for a multi-member LLC that flipped `isMMLLC` on, which activated formation-only behavior on a BANK form — the owner-step "SS-4 Responsible Party" radio AND the submit-time "additional members' ownership must total >0% and <100%" check. That check reads `member_*_member_ownership_pct` keys the Relay form never has (it collects `equity_pct`/`partner_equity_pct`), so its sum was always 0 → EVERY multi-member Relay/Payset submit was blocked with "…currently 0%…". Surfaced by the 2026-07-08d tile reconnection, which put multi-member clients back on the in-portal form. **Fix (surgical, 1 file):** `app/portal/wizard/wizard-client.tsx` — `isMMLLC = entityType === 'MMLLC' && !isBankingWizard` (`isBankingWizard = wizardType === 'banking_relay' || 'banking_payset'`). One boolean gates both the SS-4 radio and the ownership/signer submit block, so both formation leftovers drop off the bank form at once; formation/onboarding/tax paths are untouched (they're not banking). Server already accepted banking (`validateWizardData` default → valid), so no server change. **E2E QA'd on sandbox** as a real MMLLC portal client (Uxio Test LLC): SS-4 question gone from the owner step; Owner 85% / Partner 15% Relay submit returned 200 "Application submitted!" with no ownership error. 93 wizard/validation unit tests green; typecheck clean. LIVE ON PRODUCTION.)_
_2026-07-09 — Claude (**Referral page now shows each referred client's LIVE funnel status.** On `/portal/referrals` (`app/portal/referrals/page.tsx` + `components/portal/referral-page.tsx`), a referral tied to a lead used to show only the coarse reward status (Pending → Converted → Credited → Paid). It now shows the referred **lead's real stage** — Call Done → Offer Sent → Paid → **Lost** (stays Lost, per Antonio) — via the pure `leadStatusBadge()` in `lib/portal/referral-aggregate.ts`, shown whenever the referral is tied to a lead and not cancelled (a cancelled referral still shows "Cancelled"). The page batch-fetches referred-lead statuses. Driven by the new lead-stage referrer picker (staff assign a referrer on the lead → pending referral keyed to that lead → this page reflects the lead's journey). Full detail in `referrals-circleback.md`. E2E QA'd on sandbox. LIVE ON PRODUCTION — branch `claude/brave-cohen-8425cc`.)_
_Prior: 2026-07-08d — Claude (**Bank Applications: Relay + Payset tiles reconnected to the intake forms** — branch `claude/lucid-tu-39fcb9`, SANDBOX — Antonio ships. **Bug (LUMA Beauty Global LLC):** on `/portal/banks` (the active-tier "Bank Applications" page created 2026-06-20, `4fbab5af`), the **Relay** and **Payset** tiles were plain external `<a target=_blank>` links to `relayfi.com` / `payset.io` — a client clicking them landed on the bank's own site instead of TD's "we submit for you" intake form. The older BankingPicker forms (`/portal/wizard?type=banking_relay|banking_payset`, built 2026-04-07) still existed and still fully work (verified: `?type=banking` bypasses SD/tier gating at `wizard/page.tsx:162,169`, reaches `BankingPicker`; submit at `app/api/portal/wizard-submit/route.ts` step 4b builds the PDF→Drive, posts a portal-chat confirmation, and creates the **High-priority CRM task for Luca** "Review banking application" — SD-independent, so the retired Banking Fintech SD-advance + `banking_submissions` update simply no-op) — they were just disconnected from the menu when formation moved to self-service banking. **Fix (in-place, minimal blast radius):** `app/portal/banks/page.tsx` — `BankOption` gains a `managed?: boolean` flag; Relay + Payset set `managed:true` with `url` now the internal `/portal/wizard?type=banking_relay|banking_payset` (same-tab nav, no `target=_blank`), a "We submit for you" / "Ce ne occupiamo noi" badge, and "Start application →" (`ArrowRight`) CTA; ordered first. Mercury / Sokin / Wise unchanged (external self-service). The page's guidance (never share the signed SS-4, describe your business clearly) is preserved. **Rejected** the alternative of repointing the sidebar `nav.bankApplications` link to the picker — it would have discarded that guidance, orphaned `/portal/banks`, and silently changed the provider set. Antonio confirmed the team still files Relay/Payset applications on the client's behalf. Build + 5,186 unit tests green; sandbox-deployed for browser QA.)_
_2026-07-08c — Claude (**Portal chat: one message, one staff thread — MMLLC duplicate fix** — branch `claude/ecstatic-bun-022a8b`, SANDBOX, PROD DDL PENDING (migration `20260708-2000-portal-chat-one-thread-per-message.sql`, Antonio runs in Supabase SQL editor before the code push). **Bug (Adam Mihaly / LUMA Beauty Global LLC):** every MMLLC client message showed in — and bumped the unread badge of — BOTH the company thread and the member's person thread, because the live `get_portal_chat_threads_v2` had drifted from the repo migration (an `OR EXISTS` branch re-emitted a person thread for members with any personal message, and its laterals used the unrestricted `pm.contact_id=c.id` superset; prod and sandbox each carried a DIFFERENT drifted body). **New rule:** a message belongs to exactly ONE staff thread — where the client wrote it. Multi-member accounts (STRUCTURAL: 2+ distinct `account_contacts` links) own their company-scoped messages exclusively; a person thread carries only personal (account_id NULL) + solo-owned-company messages; a member with no personal/solo messages gets no person-thread row at all. **Five enforcement sites, all sharing `lib/portal/thread-scope.ts` (`multiMemberAccountIds` + `contactThreadOrFilter(…, excludeAccountIds)`):** (1) the RPC (migration above); (2) staff message view `GET /api/portal/chat` contact branch — STAFF ONLY, client legacy callers keep the full superset (their scoping is `buildChatQueryPlan`); (3) staff contact-thread mark-read (`/api/portal/chat/read`) no longer clears multi-member-account messages staff never saw; (4) What's New purple dots + notes panel (`bucketWhatsNewCounts` takes a multi-member set: those notes light ONLY the account thread); (5) the CRM inbox realtime handlers gate live INSERTs with the same predicate (`belongsToThisThread` in `portal-chats/page.tsx`) — without it a business message was live-patched into the person thread until refetch. **Reply routing:** admin send with only account_id now tags the member deterministically (`resolveAdminReplyContact` in `app/api/portal/chat/route.ts`: reply-to author → last client sender → primary/first link) instead of the old arbitrary `limit(1)` pick that tagged the wrong MMLLC member; replies from a person thread stay personal **[CORRECTED 2026-08-07: this was NEVER true — the inbox auto-selected the contact's first open company as the send target and stamped it on person-thread replies, which is exactly how the Conversion Monsters cross-company leak happened (dev job `4bad3094`). True only since the 2026-08-07 send-scope fix, see the top entry]** — safe now that business messages no longer render there (the 2026-07-08 "replied on the personal chat" complaint came from the duplicate). **MCP parity:** `portal_chat_inbox` was hand-rolling its own thread list (per-account + contact-only) and double-showed MMLLC threads; it now calls the same RPC; `portal_chat_read` contact reads use the shared scoped filter (personal + solo-company, was personal-only). **CLIENT-SIDE PRIVACY UNTOUCHED:** `lib/portal/chat-scope*.ts` (company thread = account-tagged only; personal-NULL rides along only when sole-owned; realtime mirror) not modified — verified before shipping. Sandbox QA fixtures `QA Test MMLLC Co`/`QA Test Solo LLC`/`QA Test Member A/B` (uuid prefixes aaaaaaaa-/bbbbbbbb-/cccccccc-) prove: MMLLC thread unread=2, member A person thread = personal+solo only with solo-only companies pill, member B emits no row. Unit tests in `tests/unit/thread-scope.test.ts`.)_
_2026-07-08b — Claude (**Clients can now answer country/period location cards in the P&L review** — branch `claude/lucid-pascal-809464`, SANDBOX, PROD DDL PENDING. The "🌍 Time away from home base" section of the tax-financials review (country cards "Spain, whole year — N transactions", travel-period cards, one-tap All business / All personal with confirm modal + undo) was staff-only; it now renders for the client too, served from their books. Client answers are attributed (actor_role='client'), reversible (undo of OWN answers only — staff/system batches show "Message us to change it"), reset the attestation, and a full-year country answer becomes the standing policy replayed automatically next year. Owner-only routes POST /api/portal/tax-financials/period-answer(+/undo) with post-confirm lock. Browser-verified E2E on worktree-dev as a real portal client. Full design + DDL + gotchas in tax-returns.md 2026-07-08b.)_
_2026-07-08 — Claude (**Portal sign-out is now LOCAL-scope + P&L publish/questions notifications** — branch `claude/lucid-pascal-809464`, SANDBOX — Antonio ships. **(1) Sign-out scope:** `portal-sidebar.tsx::handleLogout` and both rejected-login sign-outs in `app/portal/login/page.tsx` called `supabase.auth.signOut()` — supabase-js DEFAULT scope is GLOBAL, revoking every refresh token, so a client tapping "Esci" on a shared computer (or a role-rejected login attempt!) logged them out of their phone and every other device. All three now pass `{scope:'local'}` (this browser only). Verified against the local sandbox auth stack: dual sessions → local sign-out leaves the second session's refresh ALIVE; the old global call kills it ("Refresh Token Not Found"). Found while clearing a real client's leftover session from the office Chrome (2026-07-07). View-as exit was already local-scope by design — unchanged. **(2) Notifications:** saving a P&L workspace to a client and the AI-categorization chain finishing with open questions now announce themselves via the action-required engine (chat+bell+email, bilingual, year-scoped links that land on the year-picker tab) — full design in tax-returns.md 2026-07-08.)_
_2026-07-07f — Claude (**Portal tax-financials year picker** — branch `claude/lucid-pascal-809464`, SANDBOX. `app/portal/tax-financials/page.tsx` used to pick ONLY the single newest `tax_returns` row with `data_received=false`, so a client with two open years (e.g. a 2024 return opened alongside the live 2025 one — the Dynamiq collaboration case) could never reach the older year. Now the page loads ALL open years, selects via NEW pure helper `lib/portal/open-year.ts::pickOpenYear(openYears, ?year param)` (requested year must itself be open, else newest; no open years → redirect to /portal as before), and renders a "Tax year: 2025 | 2024" pill nav above the review when more than one year is open (links `?year=YYYY`, bilingual label, aria-current). Non-open/garbage `?year` falls back to newest — no crash, no access to closed years. 5 unit tests (`tests/unit/open-year.test.ts`); browser-verified on worktree-dev with a two-year fixture: default=newest, ?year=2024 renders the 2024 review with its own totals, ?year=1999 falls back. Seeding gotcha: `tax_returns.company_name` AND `deadline` are NOT NULL — omit either and a supabase-js insert fails silently. NOT on production — Antonio ships._
_2026-07-07e — Claude (**Distribution Resolution / Tax Statement "two-line document" fix** — branch `claude/optimistic-dhawan-f7595e`, SANDBOX. For accounts with no `members` rows, `app/portal/documents/generate/page.tsx` fell back to the account's owner contact via a CASE-SENSITIVE `eq('role','owner')` — but 20+ `account_contacts` rows store 'Owner'/'Sole Member', so the members list came back EMPTY and both templates (`distribution-resolution-template.tsx`, `tax-statement-template.tsx`) silently skipped their entire `managingMember`-guarded body: the client downloaded a legally useless PDF containing only letterhead + title + one closing line (Infinity Commerce Group 2026-07-06, Outriders 2026-04-21; lowercase-'owner' accounts were unaffected). Fix: fetch ALL contact links and pick owner-ish → member-ish → first, case-insensitively; plus a client-side guard that blocks Preview with an explanatory bilingual error when the members list is empty — an incomplete legal document must never generate silently.)_
_2026-07-07d — Claude (**MMLLC entity-type normalization across portal OA surfaces** — branch `claude/optimistic-dhawan-f7595e`, SANDBOX. `accounts.entity_type` stores "Multi Member LLC" (long form) but the portal self-service OA route (`app/api/portal/operating-agreement/create/route.ts`), the OA signing page (`app/operating-agreement/[token]/[code]/page.tsx`), both `/portal/sign` pages and `generate-documents-client.tsx` compared raw strings against `'MMLLC'` — false for EVERY account — so every portal-generated OA was created single-member with the primary contact at 100%, silently ignoring the `members` table (Umberto Moretti / Azarexa incident; Datavora SIGNED a wrong OA 2026-05-25). All five surfaces now use `normalizeEntityType()` (`lib/portal/entity-type.ts`); the create route additionally validates ownership totals 100%, prefers member-row addresses, and stores the short code in `oa_agreements.entity_type`. Full invariant: lease-oa.md.)_
_2026-07-07c — Claude (**Identity & Tax card on /portal/profile** — branch `claude/practical-einstein-ce1e5c`, SANDBOX. Luca's request (td-dev thread, Martin Csordas follow-up): clients couldn't look up their ITIN without opening the CP565 letter. `app/portal/profile/page.tsx` now renders a ShieldCheck card between Personal Info and Company Info when `contacts.itin_number` is set: ITIN number, issue date, and renewal-due date (labelled "ITIN Renewal Due" — our computed June-1 +3y convention, deliberately NOT "expiration" which would imply an IRS-printed date), plus a note pointing at Documents for the letter. Pure read of the already-loaded contact row — no new queries. New i18n keys profile.identityTax/itinNumber/itinIssueDate/itinRenewalDate/itinLetterNote (EN+IT, `lib/portal/i18n.ts`). Hidden for contacts without an ITIN and for teammates (profile page requires a contact id). NOT on production — Antonio ships._
_2026-07-07b — Claude (**Wizard duplicate-submission guard + upload-failure error-audit** — branch `claude/objective-cohen-b75f61`, SANDBOX. LT Program incident #2: after her first tax submit marked wizard_progress 'submitted', the client reloaded the wizard, which put it in re-submit mode (`isResubmitMode` → `allow_resubmit=true`), so each of her 4 retries BYPASSED the step-1 dedup by design and enqueued an identical `tax_form_setup` job — 5 total, each re-copying every file to Drive (5× Tax/2025 copies) and re-emailing the team. Fix 1: `lib/portal/wizard-job-dedupe.ts` — wizard-submit now stamps every job payload with a `dedupe_key` (sha256 of wizard type + subject + submitted data) and reuses a recent (<24h) non-failed job with the same key instead of enqueueing a twin; an EDITED resubmission hashes differently and enqueues normally, so the legit allow_resubmit flow is preserved. Lookup failure → enqueue (never drop a submission). Fix 2 (why she retried at all): a wizard file-upload failure was previously only a browser-console error + generic "Upload failed" — `handleFileUpload`'s catch now also fire-and-forgets `/api/system-errors/report` (route `portal-wizard-file-upload`, context: field/file/size/type) so staff see WHY "non me li carica" happened; mint-path non-guided failures funnel into the same catch. Companion Drive-side dedup guards in documents.md (same date). NOT on production — Antonio ships._
_Prior 2026-07-07 — Claude (**Personal (contact-scoped) documents now visible to LLC-owning clients + storage-aware portal download** — branch `claude/practical-einstein-ce1e5c`, SANDBOX. Martin Csordas: his ITIN letter (documents.account_id NULL — the ITIN flow is contact-scoped by design) was INVISIBLE on `/portal/documents` because the page's account branch filtered EVERY query by the selected account; only no-LLC clients ever saw personal docs. Fix in `app/portal/documents/page.tsx`: the account branch additionally queries (a) the viewer's person-level non-flow docs (account_id NULL + contact_id + portal_visible) into My Documents, and (b) the viewer's person-level flow-stamped docs into the flow groups — same `isClientSafeFlowDoc` curation applies (an ITIN letter passes because the finalize writes it portal_visible=true; internal ITIN docs stay hidden). Teammates unaffected (no contact id → personal queries skipped, unchanged). SECOND fix: `app/api/portal/documents/[id]` streamed ONLY from Google Drive, so a `storage:<path>` doc (any contact-scoped flow upload) 500'd on download/preview even when listed — it now handles the storage shape exactly like the staff preview route (access check unchanged: personal docs only for the owning contact). NOT on production — Antonio ships._
_2026-07-06c — Claude (**notifyClientActionRequired: optional `ctaLabel`** — branch `claude/practical-einstein-ce1e5c`, SANDBOX. The engine's email CTA button was hardcoded "Take action"/"Vai all'azione"; informational dispatches read oddly. New optional `ctaLabel?: LocalizedText` param on `ActionRequiredParams` (default unchanged — every existing caller behaves identically). First consumer: the ITIN Approved letter finalize ("View document"/"Vedi documento", flows.md 2026-07-06). No other engine behavior changed._
_Last verified against code: 2026-07-15 — Claude (**Wizard money fields: domain minimum support** — `FieldConfig.min` on wizard fields; `min: 0` on the four tax money fields + `rpt_amount` in `wizard-configs.ts`; input `min` attr + inline EN/IT error in `wizard-field.tsx`; `belowFieldMin` step-gate enforcement in `wizard-client.tsx` across top-level, member-indexed and repeater sub-fields. Companion to the tax-form hardening after the Rocco Papotti -0.03 incident — full story in tax-returns.md 2026-07-15c. Sandbox — Antonio ships.)_
_Prior: 2026-07-06b — Claude (**Flows/services access: contact-match now requires account_id null** — follow-up to the createSD person-link hygiene (formation.md 2026-07-06): company SDs may now carry a `contact_id` alongside `account_id`, so `app/portal/flows/[id]/page.tsx` and `app/api/portal/services/[id]/route.ts` grant contact-match access ONLY for contact-ONLY SDs (`account_id` null = ITIN-style personal). Company SDs are gated by account membership alone — a contact later unlinked from the company keeps no access via the stale SD tag. ITIN access unchanged. SANDBOX — Antonio ships.)_
_Prior: 2026-07-06 — Claude (**What's New scope fix: company-only system notes now reachable from person threads** (Prowave LLC incident: the member-info submission note was visible as an amber pill in the staff Messages feed but never in What's New and never lit the purple dot, because it was tagged `account_id`-only while the What's New feed filtered strictly `contact_id` on person-level threads). **(1) NEW `lib/portal/thread-scope.ts`** — the person-thread superset rule (contact-tagged rows PLUS company-ONLY rows on the contact's linked accounts; both-tagged rows belong to their tagged contact only) as shared helpers: `contactThreadOrFilter` (PostgREST `.or()` string), `linkedContactIdsByAccount` (batch reverse lookup), `bucketWhatsNewCounts` (pure dot-count bucketing — company-only notes fan out to every linked contact's bucket; `total` counts distinct notes once). `app/api/portal/chat/route.ts` (Messages feed, previously inline) and `app/api/crm/admin-actions/whats-new/route.ts` (notes feed + counts) BOTH consume these helpers, so the two thread definitions can no longer drift; contact→accounts lookup reuses `lib/portal-auth.ts::getClientAccountIds`. **(2) Emitter dedup hardened** (`lib/portal/chat-events.ts`): idempotency now matches the `<!-- chat-event: -->` marker ALONE (unique by kind+source), never additionally recipient tags — a webhook retry spanning the dual-tagging deploy must dedup against an older account-only row. **(3) Dual-tagging for the 3 hard-coded account-only emitters:** member-info submit passes the request's stored `contact_id`; contact-request submit tags the RECIPIENT contact (not the freshly added one) alongside the account; portal document upload tags the authenticated uploader via `getClientContactId` (staff uploads stay account-only). Audit note: dispatcher/payment/SS-4/decision emitters already pass contact_id when the source row has it — layer (1) covers rows where it's null. Antonio's decision: historical company-only unhandled notes SURFACE (no backfill-handled migration) — expect purple dots on person threads for genuine untriaged backlog. Side effect (accepted): on multi-member accounts a company-only note lights every member's person-thread dot; handled state is per-note so one tick clears all. Unit tests: `tests/unit/thread-scope.test.ts`, `tests/unit/chat-events-dedup.test.ts`. SANDBOX — Antonio ships.)_
_Prior: 2026-07-04c — Claude (**Unsent-draft rule extended to the portal Expenses tab** (the follow-up flagged in 2026-07-04b, Antonio approved). `createTDInvoice` writes the `client_expenses` mirror AT DRAFT TIME, so an unsent draft still showed as a "Tony Durante LLC — Pending" expense (Kasabi's EXP-000337). New pure `filterClientVisibleExpenseMirrors` in `lib/portal/payment-visibility.ts` + private `hideUnsentDraftMirrors` in `queries.ts`: `getPortalExpenses`/`getPortalExpensesByContact` now look up the linked payments for `source='td_invoice'` rows and drop mirrors whose payment is Draft+Pending. FAIL-OPEN: a mirror with a missing payment row stays visible (never hide a real expense on data drift); uploads/manual expenses never touched. Verified the Expenses tab is the ONLY client list surface for `client_expenses` (portal expense-actions already refuse edit/delete on td_invoice rows). Unit-tested (5 new tests in `payment-visibility.test.ts`). SANDBOX — Antonio ships.)_
_Prior: 2026-07-04b — Claude (**Unsent draft invoices hidden from the client's Payment History** (Antonio's decision after the Kasabi Ocean Global incident: the June cron's staff-review Draft INV-002172 showed to the client as "Pending $1,000" on the portal home although nobody ever sent it). New pure helper `lib/portal/payment-visibility.ts::isClientVisiblePayment` — hides EXACTLY `invoice_status='Draft' AND status='Pending'` (an unsent draft); a Draft paid anyway stays visible, legacy `invoice_status=NULL` rows untouched (createTDInvoice marks credit-covered invoices `Paid`, never Draft, so no paid history can be hidden). Applied in `getPortalPayments` + `getPortalPaymentsByContact` (`lib/portal/queries.ts`) — the only feeders of the home PaymentHistory widget; portal action items were already Sent/Overdue-only. Unit-tested (`tests/unit/payment-visibility.test.ts`). KNOWN FOLLOW-UP (flagged, awaiting Antonio's decision): the draft's `client_expenses` mirror row (e.g. EXP-000337) is still visible in the portal Expenses tab via `getPortalExpenses` — same unsent invoice, different surface. Sibling change: Finance → All Invoices gained a Send button on Draft rows (see billing-invoicing.md same date). SANDBOX — Antonio ships.)_
_Prior: 2026-07-04 — Claude (**Tax wizard: declared related entities** (Phase 3R slice 4). Both LLC financials configs in `components/portal/wizard/wizard-configs.ts` gain one optional field `other_owned_companies` ("Do you or the members own or control any other companies — even with no transactions?"), placed with the existing related-party section (SMLLC: after the Form-5472 RPT repeater; MMLLC: before comp_anything_else). Persists via the normal `submitted_data` jsonb — NO DDL, no wizard-client changes. These names + the existing `rpt_company_name` answers now FEED CATEGORIZATION: `lib/tax/declared-entities.ts` extracts them and `computeRecategorizationUpdates` flags matching bank rows `is_related_party` + note — flag-only, never a category (a $183k transfer to the client's own other company can no longer silently read as a vendor expense; the review/answer decides the booking). Full Phase 3R detail in tax-returns.md.)_
_Prior: 2026-07-02d — Claude (**Action-Required Phase C: Tax + ITIN + new-invoice-to-pay**, branch `claude/focused-stonebraker-a5f331`, SANDBOX-tested, ZERO DDL. **(1) NEW `lib/portal/action-stage-registry.ts`** — code-side registry of pipeline stages requiring a CLIENT action on entry (`Tax Return::Wizard Available` → `/portal/wizard`; `ITIN::Client Signing` → `/portal/flows/{sd_id}`, copy migrated verbatim from the retired bespoke 8c block). `advanceServiceDelivery` step **8-pre** dispatches `notifyClientActionRequired` for registered stages and SUPPRESSES the generic notification (8), raw stage push (8a) and `notify_client_email` stage email (8b) — one message, three channels, never doubles (E2E-verified: exactly 1 email on the Tax Wizard Available advance despite the stage's email flag being ON). **(2) Tax review actions** (`app/api/crm/tax-review/action/route.ts`): `request_changes` AND `approve` (the click-Confirm gate) now dispatch the full action-required package (were chat-only); `reopen` keeps the chat-only notice; responses carry `client_notified`. **(3) Financials-ready** (`lib/jobs/ingest-complete-notify.ts`) upgraded from chat-only to the full package (link `/portal/tax-financials`); its own idempotency (last-job gate + `financials_meta.ready_notified`) retained. **(4) INVOICES:** `sendTDInvoice` (`lib/invoice-auto-send.ts`) — the single choke point for every invoice send path — now also drops the package with **`skipEmail: true`** (the rich PDF invoice email already went out — never a second email) and a per-invoice link `/portal/invoices?inv=<payment_id>` so consecutive invoices each notify (helper dedups by link); PORTAL audience only (`no_portal` recipients pay via the /pay token link in their email). Helper gained `skipEmail` param. Sandbox E2E: all 3 flows one-email-exactly, IT/EN per recipient, deep links correct; fixtures cleaned. 4443 unit tests green. NOT on production — Antonio ships.)_
_Prior same day: 2026-07-02c — Claude (**TD Communication wizard AI-assist wiring** — `app/portal/wizard/wizard-client.tsx` gains a `handleAiAssist` callback that POSTs to `/api/td-communication/ai/generate` and is passed to `WizardField` ONLY when `wizardType==='td_communication'` (inert for every other wizard). Client half of the Phase 11 "Generate with AI" brand-audit feature (✨ button on brand-audit textareas, DB-driven via `td_comm_questions.ai_assist`). Full detail in td-communication.md (Phase 11 — AI features). Shipping to main.)_
_Prior same day: 2026-07-02c — Claude (**"Client paid INV-…" events no longer render in the STAFF Messages thread.** Antonio (Umberto Moretti thread): the payment_received chat-event pill showed inline in the CRM portal-chats conversation — it belongs in What's New only. Verified before fixing: the CLIENT never saw these (client reads filter ALL `<!-- chat-event:` rows at `app/api/portal/chat/route.ts` GET + the realtime hook `use-portal-chat.ts:139`; the prod INV-002254 row carries the marker + sender_type='system'); the same event already feeds a What's New card (`payment_received` in `lib/notifications/whats-new-defaults.ts`). Fix: the admin/staff branch of the chat GET now excludes `%<!-- chat-event: kind=payment_received%` (surgical — other event kinds keep their inline pills; pattern verified vs sandbox data: hits exactly the 8 payment events, spares the 172 others). KNOWN FOLLOW-UP (flagged, not fixed): the thread-LIST preview (`get_portal_chat_threads_v2` RPC) can still show "Client paid…" as a thread's last message + bump its ordering — fixing that needs a DB-function migration._
_Prior same day: 2026-07-02b — Claude (**Action-Required Phase B: client reminders + staff stale-draft alert**, branch `claude/focused-stonebraker-a5f331`, SANDBOX-tested, ZERO DDL. New daily cron `/api/cron/action-required-reminders` (vercel.json `0 13 * * *` ≈ 9 AM ET, registered in `lib/cron-coverage.ts`) backed by `lib/portal/action-required-reminders.ts`. **(1) Client reminders:** SS-4s still `awaiting_signature` get re-notified through the Phase A channels (chat + immediate email + bell/push) after `REMIND_AFTER_DAYS=3` of silence, max `MAX_REMINDERS=2` after the initial; resolution-aware (signing removes the row from the sweep). STATE = the `portal_notifications` rows themselves (`type='action_required'`, `link='/portal/sign/ss4'`, counted per signer) — no new tables. Stragglers with ZERO notifications (pre-Phase-A sends) get the INITIAL once quietly awaiting >3d — first prod run will message Clearview Global (awaiting since Apr 21; Antonio informed pre-ship). `is_test` accounts skipped. **(2) Staff stale-draft alert:** formation SDs parked at "SS-4 Prepared" whose SS-4 is still `draft` — or MISSING (auto-generation failed) — past `STAFF_ALERT_AFTER_DAYS=3` email support@ with the workspace link (the Michele Cotti failure mode); throttle marker = `action_log` row `details.kind='ss4_stale_draft_alert'` per SD, repeats every 3d until resolved. Decision logic is PURE (`decideSs4ClientReminder`/`decideStaleDraftAlert`, explicit `now` — time-travel tested in `tests/unit/action-required-reminders.test.ts`). Sandbox E2E: 6-scenario fixture matrix all passed, second run fully throttled. NOT on production — Antonio ships.)_
_Prior same day: 2026-07-02 — Claude (**Client "Action Required" notification engine (Phase A) + canonical language normalizer**, branch `claude/focused-stonebraker-a5f331`, dev_task `735adcf9`, SHIPPED to production (main `99978661`). **(1) NEW `lib/portal/action-required.ts::notifyClientActionRequired()`** — THE dispatch point for "the client must DO something" (vs. passive status updates). One call → (a) clickable portal CHAT message: direct `portal_messages` insert (NOT the chat route, so the R103 chat-email does not double-fire), body ends with the absolute action URL which `portal-chat.tsx` auto-linkifies into an in-app link, stamped `service_delivery_id` so staff see it in the workspace flow chat, sender = the system admin id; **VISIBLE-THREAD RULE**: if the account's status is not Active/Suspended (hidden by `getPortalAccounts`) the chat is tagged to the client's PERSONAL thread (`account_id` NULL) — a message in an invisible thread is as bad as no message; (b) IMMEDIATE branded bilingual email (Antonio's decision 2026-07-02 — action moments email now, not via digest), RFC 2047 subject, CTA deep link; (c) bell + web push via `createPortalNotification` with NEW `suppressDigestEmail` param (row born with `email_sent_at` set → the portal-digest cron can never send a second email). Dedup: same type+link+recipient within 10 min → skipped (double-click/race guard; deliberate later re-sends still notify). Per-channel try/catch, never throws, `action_log` audit with per-channel results. Unit-tested (`tests/unit/action-required.test.ts`). First consumer: SS-4 ready-to-sign (see formation.md same-date). Phase B (reminders + staff stale-draft alert) and Phase C (catalog-driven stage actions for Tax/ITIN/Closure) are separate dev tasks. **(2) NEW `lib/locale.ts`** — canonical `localeFromLanguage`/`isItalian` (contacts.language is messy free text: "Italian" ×210, "Italiano", "Italian - englis", null…). Switched: `notifyClientOfAdminMessage` + `notifyClientOfStageAdvance` (both used a broken strict `=== 'it'` — Italian clients were getting ENGLISH chat/stage emails), greeting.ts, auto-create.ts reads, welcome-message.ts, wizard-failure-notify.ts (re-export kept), ITIN advance message in service-delivery.ts. FLAGGED not switched (follow-up cleanup): task-waiting-with-optional-message.ts:45, chain-send-client-message.ts:31, document-alerts.ts:142, itin-processing-reminder.ts:68. **(3)** Generic SD-advance portal notification now shows the stage's `client_label`/`client_label_it` (per contact language) instead of the internal stage name. 4376 unit tests green. NOT on production — Antonio ships.)_
_Prior: 2026-07-01 — Claude (**TD Communication real brand-audit questions (Cris's 30).** The portal brand-audit wizard (`app/portal/wizard/page.tsx`, `app/portal/wizard/wizard-client.tsx`) no longer uses placeholder questions — it now renders Cris's real 30 questions from the SUPERNOVA + SIRIUS forms, DB-driven via the `td_comm_questions` table (Cris-editable from the CRM TD Communication admin). Options for colors/shapes are bilingual (EN/IT) with psychological descriptions. Both `new_brand` and `rebrand` now use one unified 4-step intake. Question→field mapping in `lib/td-communication/question-to-field.ts`; pipeline/questions/types updated accordingly. Seed migration `20260701-1200-td-comm-real-questions-seed.sql` applied to both sandbox and production. Full feature detail in td-communication.md. SHIPPED to main.)_
_Last verified against code: 2026-07-01 — Claude (**Internal chat readers label staff messages "TD Team" — never a member name.** The MCP `portal_chat_read`/`portal_chat_inbox` tools (`lib/mcp/tools/portal.ts`) joined the message's `contact_id` to a full name and appended it to BOTH client AND admin/system rows. But on an admin send carrying only an `account_id`, `contact_id` is a ROUTING TAG — an arbitrary linked member picked via `.limit(1)` in `app/api/portal/chat/route.ts:270-279` so the message lands in the client's contact-scoped thread — NOT the author. Result: a staff reply rendered as `Admin (Gaia Pellegrinelli)`, a company member who never wrote it (verified: AI Venture Labs' `account_contacts LIMIT 1` = Gaia = the tag on Antonio's message). Fixed at the DISPLAY layer only — the tagging is load-bearing for multi-member thread visibility and is untouched. New pure helpers `staffChatSenderLabel` + `formatMcpChatSenderLabel` in `lib/portal/chat-sender-name.ts`: admin → "TD Team", system → "TD Team (auto-reply)", client → "Client (Name)". Applied to the MCP reader + inbox `lastBy`, and the Slack/Hermes worker's readers (`lib/ai-agent/tools.ts`, which showed `Admin (TD)`) for consistency. Client-facing portal was already correct ("Tony Durante Team" via `portal-chat.tsx`/`i18n.ts`); the CRM dashboard shows no name on admin bubbles. Unit-tested (`chat-sender-name.test.ts`); 4343 unit tests green. Sandbox — Antonio ships.)_
_Last verified against code: 2026-07-01 — Claude (**Staff CRM inbox: block sending to a CLOSED account the client can't see.** A contact who owns >1 account shows ONE contact-level thread in `app/(dashboard)/portal-chats` with a "Send as" company picker listing ALL their companies — including closed ones (the `get_portal_chat_threads_v2` RPC's `companies` array has no status filter). But the client portal HIDES Closed/Cancelled/Delinquent/Pending-Formation accounts (`lib/portal/queries.ts::getPortalAccounts` → `status IN ('Active','Suspended')`), so a staff message tagged to a closed account (`account_id`=closed) is NEVER seen by the client. Root cause of the silent pile-up: the picker defaulted `selectedCompanyId` to `companies[0]`, and the RPC orders companies alphabetically, so a closed company sorting first (e.g. "Giulio Sembinelli Trainer LLC" before "Next To Prime LLC") became the DEFAULT send target — type-and-send went to the dead account (live: closed acct had 28 admin msgs + 0 client replies; active acct had all 65 client msgs). Fix (no migration): `app/api/portal/chat/threads/route.ts` enriches each company with `closed` (+ thread-level `account_closed` for account-level multi-member threads) via one `accounts.status` fetch; `app/(dashboard)/portal-chats/page.tsx` (a) defaults "Send as" to the first NON-closed company (null → "No tag" if all closed — an untagged `account_id IS NULL` message still reaches the client's sole-owned-company + personal views), (b) marks closed companies red + "Closed" in the picker, (c) shows a red warning banner + DISABLES the send button + guards `handleSend` when the current target is closed. Build + 4304 unit tests green. Sandbox.)_
_Last verified against code: 2026-06-30 — Claude (**View-as on mobile: hamburger menu was hidden behind the red banner.** The mobile nav bar (`components/portal/portal-sidebar.tsx`, `fixed top-0 z-40 h-14 lg:hidden`) and the View-as banner (`components/portal/view-as-banner.tsx`, `sticky top-0 z-[100]`) both occupied the same top strip, so in View-as on a phone the banner covered the hamburger and the menu was unreachable (content also clipped). Fix WITHOUT hard-coding a height (the banner wraps to two lines on narrow screens): `ViewAsBanner` is now a client component that MEASURES itself (useLayoutEffect + ResizeObserver) and publishes its height as CSS var `--portal-vb-h` on `<html>` (reset to `0px` on unmount); the mobile nav bar drops `top-0` for `style={{ top: 'var(--portal-vb-h, 0px)' }}` so it stacks just below the banner. No-op for normal clients (banner unmounted → var falls back to 0px → bar at top as before) and for desktop (bar is `lg:hidden`). Verified live in sandbox: View-as mobile menu reachable + drawer opens + content not clipped; normal-client mobile unchanged; desktop sidebar rail unchanged. SHIPPED to main.)_
_Last verified against code: 2026-06-29 — Claude (**8-stage formation: `faxedAt` added to formation tracker.** `getFormationTracker` (`lib/portal/queries.ts`) now returns `faxedAt: string | null` alongside `filedAt` — extracted from `stage_history` `to_stage='SS-4 Sent to IRS'` `advanced_at` (falls back to `stage_entered_at` while AT that stage). `buildFormationTrackerSteps` (`lib/portal/formation-progress.ts`) accepts a 5th `faxedAt?` param and attaches it to the "SS-4 Sent to IRS" step when completed/current. `FormationProgressTracker` renders "· Faxed Jun 29, 2026" bilingual (FAXED_LABEL en/it). All 4 `buildFormationTrackerSteps` call sites in `app/portal/page.tsx` forward `tracker.faxedAt`. See formation.md for the full 8-stage migration.)_
_Latest 2026-06-29 — Claude (**TD Communication concept reveal** — Phase 7, branch `feat/td-comm-logo-reveal`, SHIPPED to main. `app/portal/td-communication/page.tsx` now, when the client's active enrollment is at `concept_ready` or `approved`, renders `<ConceptReveal>` (a click-to-accept legal disclaimer gate → cinematic logo reveal of the released concept) INSTEAD of the marketing landing; otherwise unchanged (Phase 9 landing + Phase 5/6 footer). No concept image URL is passed from the server — the reveal fetches signed preview URLs from `/api/portal/td-communication/concept` only after the disclaimer is accepted. New client-only API routes under `app/api/portal/td-communication/` (concept GET, disclaimer POST, approve POST). Acceptance logged to new table `td_comm_disclaimers`. Full feature in td-communication.md (Phase 7).)_
_Latest 2026-06-29 — Claude (**Tax MMLLC wizard fixes** — `app/portal/wizard/wizard-client.tsx`. (1) "Before You Start" first step now carries a stronger attention message + a REQUIRED "I have read this" checkbox that gates Next (`formData.prepare_acknowledged`). (2) Tax MMLLC ownership must total EXACTLY 100% (the members list is the full roster — no separate owner step); fixed the submit validation + added an early members-step gate and a live ownership-total readout. (3) The wizard bank lookup ("Show me how to download the CSV") now calls the new `/api/portal/bank-guide` and works for ANY bank. Full detail + rationale in tax-returns.md (2026-06-29 entry).)_
_Latest 2026-06-29 — Claude (**Brand-audit wizard** — TD Communication Phase 6, branch `feat/td-comm-wizard`, SHIPPED to main. New portal wizard type `td_communication` (New-Brand 3-step / Rebrand 2-step, bilingual). `app/portal/wizard/page.tsx` resolves it via `forcedType` (`?type=td_communication`) and sets `entityType` from the client's active enrollment `client_type` so `getWizardConfig` picks the right path; on submit the route writes into the existing `td_comm_enrollments` row (no submission table / no job — inline branch). `app/portal/td-communication/page.tsx` upgrades from a static teaser to enrollment-aware: shows a "Start your brand audit" CTA → `/portal/wizard?type=td_communication` when the client has an active enrollment whose form isn't submitted, a "received" note once submitted, else the original Coming-Soon teaser. No DB/migration. Full feature in td-communication.md (Phase 6).)_
_Latest 2026-06-29 — Claude (**TD Communication tab** — Phase 4, branch `feat/td-comm-portal-tab`, SHIPPED to main. New static client-facing "Coming Soon" page at `app/portal/td-communication/page.tsx` teasing the upcoming branding service (logos / landing pages / brand identity); bilingual EN/IT via `getLocale`, server component mirroring `app/portal/banks/page.tsx` (auth guard → locale → inline copy), no DB/queries. Sidebar entry in `components/portal/portal-sidebar.tsx` `companyItems` (key `nav.tdCommunication`, `Palette` icon, `tierOnly: ['active']`) — active-tier clients only, hidden for lead/onboarding/formation, teammates (default-deny), and partners. Label key in `lib/portal/i18n.ts`. Full 9/9 sandbox browser QA. Feature detail in td-communication.md.)_
_Latest 2026-06-28 — Claude (**Partner login admission** — TD Communication Phase 2, branch `feat/td-comm-pipeline`, SANDBOX. `app/portal/login/page.tsx` previously hard-rejected any non-`client` role, so a `role='partner'` user (Cris) could authenticate but never get in — there was no working door to the partner dashboard. Now the email-login path admits `client` OR `partner` and routes a partner to `/collab`. A partner must ALSO have a NON-EMPTY `client_partners.partner_scope` — checked server-side via the `partnerLoginAllowed()` server action because scope lives in the RLS-protected `client_partners` table — so a scopeless/unlinked partner is rejected ("This account does not have portal access"). Middleware keeps partners on `/collab`; `/collab` still gates on the `td_communication` scope. Also fixed an invalid non-async export in the `'use server'` actions file that 500'd every partner login (and would have broken teammate username login when invoked). Full feature in td-communication.md.)_
_Latest 2026-06-27c — Claude (**Security fix: e-sign To-Sign cross-client leak.** `app/portal/sign/page.tsx` matched e-sign signers with `.or(contact_id.eq.<id>,email.ilike.<login>)` — a login email containing `_`/`%` acted as a SQL LIKE wildcard and surfaced ANOTHER client's envelope (name + token) in the list. Replaced with two separate queries (contact_id `.eq` + login email `.ilike` with `%`/`_`/`\` escaped so it matches literally), merged + de-duped. The embed page's ownership check was already exact-match (unaffected). Found by adversarial stress-QA; engine + the other 3 fixes in `esign.md`.)_
_Latest 2026-06-27b — Claude (**E-sign To-Sign now matches by `contact_id`.** When staff add a signer as a CRM client (new editor picker), the portal "To sign" list (`app/portal/sign/page.tsx`) + the `/portal/sign/esign` embed match that signer by **`contact_id` OR email** (was email-only) — robust against login/contact email drift. Delivery is channel-routed at send (CRM client w/ portal login → portal + a `sign_document` notification; else email). Full engine + dispatcher in `esign.md`. Ships with the e-sign signer-picker change.)_
_Prior 2026-06-27 — Claude (**In-portal e-sign signing**, LIVE ON PRODUCTION (2026-06-27, `main` @ `f398ee24`). The portal "To sign" list (`app/portal/sign/page.tsx`) now also surfaces **TD e-sign envelopes** where the logged-in client is an invited signer — matched by their **login email** (`auth.users.email = esign_signers.email`, because TD-first signers carry an email but no `contact_id`), restricted to active envelopes (`sent`/`in_progress`) where it's the signer's turn (`status` sent/viewed). Each item links to the new `app/portal/sign/esign?token=...` page, which re-checks the email owns the signer, then embeds the public signer page `/sign/{token}/{code}?portal=true` via `PortalDocumentClient` (same-origin URL built from request headers so it works on sandbox preview AND prod; the signer page posts `document-signed` on completion, same-origin). Full engine in `esign.md`.)_
_Last verified against code: 2026-06-26b — Claude (Fatture Sales row menu now follows the standard invoice lifecycle — client's own tool, no TD-accounting impact: Edit + Void allowed at any stage incl. Paid/Partial; Send only advances Draft→Sent (never downgrades a paid invoice); Reminder stays Sent/Overdue; Split parents excluded. Full design in `billing-invoicing.md`.)_
_Last verified against code: 2026-06-26 — Claude (DUAL-ROLE portal: a person who is BOTH a client (account membership) AND a partner (a `client_partners` row) — e.g. Auralba — switches between views using the **EXISTING `CompanySwitcher`**, which gains a **"Partner — My Referrals"** entry for dual-role users (NOT a separate toggle — Antonio: the client must use the same switcher). Effective view is cookie-driven (`portal_mode`); single-role users are unchanged. `lib/portal/portal-mode.ts::resolvePortalMode` (DB: client_partners existence) + pure `decidePortalMode` (dual→cookie, else locked to capability) — unit-tested. `app/portal/layout.tsx` computes the effective role passed to the sidebar. `components/portal/company-switcher.tsx` holds the Partner entry + sets the `portal_mode` cookie on select (selecting a company drops out of partner mode); `components/portal/portal-sidebar.tsx` renders this one switcher at the top for dual-role in BOTH views (and suppresses the buried duplicate); the dual-role switcher passes `partnerHref="/portal/partner/referrals"` so the **"Partner — My Referrals"** entry actually lands on My Referrals (the component default `partnerHref` is `/portal/partner/clients`, which mismatched the label — fixed 2026-06-26 during sandbox QA). `app/portal/page.tsx` redirects to the partner section only when mode='partner'. Auralba keeps `portal_role='client'` (base identity). NOTE: 'View as partner' (CRM Partners page) lands on this dual-role portal. PRIOR same chain: partner-portal page **My Referrals** `/portal/partner/referrals` (partner role only; nav added to `portal-sidebar.tsx` partnerItems). Lists the partner's referrals from their OFFERS with a CRM-derived progress stepper for the ONE-TIME acquisition funnel (Call done → Offer sent → Client signed → Client paid); **annual renewals are shown SEPARATELY** as a recurring section driven by the actual **installment invoices** (R106 billing cycle, NOT a funnel stage). Per renewal year (after formation), each installment (1 Jan / 2 Jun) shows `Invoice issued → Paid` from `payments` (payment_category `installment_1`/`installment_2`, year from the `annual-installment:ACCT:N:YEAR` key) and the partner's OWN amount only — never the client's invoice amount (Antonio 2026-06-26). The agreed renewal amount is paid IN FULL on **each installment** (NO split, NO %; Antonio sets the amount per referral → two payouts/year); each is a separate payout (reference `renewal:<acct>:<year>:<n>`) created when its installment is paid (`onFirstInstallmentPaid`→#1, `onSecondInstallmentPaid`→#2) and self-serve **requestable** then. Setup payout still shows its own request form (USD bank details + optional invoice upload). Full design in `partners-team.md`. SANDBOX, branch `feat/partner-deal-config`.)_
_Latest 2026-06-26 — Claude (**Suspend client portal login** (admin Suspend / Unsuspend), branch `feature/portal-suspend-access`, SHIPPED. New `suspend`/`unsuspend` actions in `app/api/crm/admin-actions/contact-portal/route.ts` ban/unban the contact's auth user (`ban_duration` `'876600h'`/`'none'`), set `app_metadata.suspended`, send the client a bilingual notice on BOTH suspend & restore, and write `action_log`. Admin UI = red **Suspend Login** / green **Unsuspend Login** in the contact Portal tab `PortalTab` (`components/contacts/contact-detail.tsx`); the confirm dialog lists every linked company (the blast radius), and the Portal Status "Account" row shows red **Suspended** when blocked (from `portalAuth.suspended`, computed from `banned_until` in `app/(dashboard)/contacts/[id]/page.tsx`). **Suspend is LOGIN-level** — one auth user per email, so it blocks the person across ALL their companies; it is NOT a tier change and NOT an `accounts.status` change (that enum's own `Suspended` value is unrelated CRM lifecycle). `restore_access` (per-company membership restore) is GUARDED — it will NOT lift a deliberate suspension (`app_metadata.suspended===true`), so restoring one company can't silently un-suspend the login. Login-side enforcement (instant kick-out + message) lives in `middleware.ts` + `app/portal/login/page.tsx` — see auth-oauth.md. Verified live in sandbox end-to-end (normal client unaffected; suspend→blocked+message→unsuspend→restored; both via the real dashboard buttons + audit log). **Follow-up (same day):** the contact **header** badge (next to the name, `contact-detail.tsx`) now shows red **Suspended** in place of the green tier badge when `portalAuth.suspended` — tier is intentionally unchanged by suspend, so the header previously kept showing "active" after a suspension, which read as a bug.)_
_Latest 2026-06-26 — Claude (**MMLLC formation wizard now collects the SS-4 Responsible Party (signer)**, branch `feat/mmllc-workspace`, SANDBOX. `app/portal/wizard/wizard-client.tsx` adds a `SignerRadio` on the owner step ("I will be the SS-4 Responsible Party") and one per additional member on the members step; all share a single `signerIndex` state (-1=owner, 0+=member) so **exactly one** is selectable. On change it stamps flat `owner_is_signer` / `member_{i}_is_signer` booleans into formData, which persist verbatim into `wizard_progress.data` + `formation_submissions.submitted_data` (wizard-submit writes `data` untouched; the formation validator ignores extra keys). Submit is gated (MMLLC only) on exactly one signer chosen AND additional members' ownership summing to >0% and <100% (owner takes 100−Σ at materialization). Removing the selected member clears the choice. Entirely inert for SMLLC / non-formation wizards (the members step + owner radio only render when `entityType==='MMLLC'`). Downstream: `extractMembersFromWizardData` now surfaces `is_signer`, `formation-materialize.ts` writes it to `members.is_signer` (owner from `owner_is_signer`, each member from its flag) instead of hardcoded false, and the workspace `members_panel` displays/overrides it (see flows.md). **Server-side backstop (added 2026-06-26):** `validateFormationData` (`lib/jobs/validation.ts`) now ALSO enforces the same two rules when called with an MMLLC `entityType` — exactly one signer across `owner_is_signer` + `member_{i}_is_signer`, and additional members' ownership summing to >0% and <100% — returning field-level errors (`is_signer` / `member_ownership_pct`) that `/api/portal/wizard-submit` surfaces as a 400 so a direct API hit that bypasses the client toast is still rejected. Gated on the entityType param the wizard-submit route passes; the background `formation-setup` re-validation calls it WITHOUT entityType, so legacy in-flight MMLLC jobs are unaffected. NOT on production — Antonio ships.)_
_Last verified against code: 2026-06-25c — Claude (Referral page `/portal/referrals` now surfaces a contact's PERSONAL referrals PLUS referrals earned by a company where they are the **primary** contact (sole member ⇒ primary; leak-safe for multi-member LLCs), and renders "earned" **per-currency** (USD `$` + EUR `€` coexist). Pure logic in `lib/portal/referral-aggregate.ts`; files `app/portal/referrals/page.tsx`, `components/portal/referral-page.tsx`. Full design + the paired activation auto-credit change are documented in `referrals-circleback.md`. Branch `claude/sweet-albattani-8859f9`, SANDBOX.)_
_Last verified against code: 2026-06-16 — Claude (WIZARD SUBMIT IS RESILIENT TO LOST RESPONSES — Luca/Dynamiq hit "Submission failed" on the MMLLC wizard even though the server SUCCEEDED (wizard_progress=submitted, submission=completed, job=completed, CSVs ingested). Root cause: the submit returns 200 fast (async), but a transient delivery failure — a lost 200 OR a one-off empty-body 500 (reproduced once against the live endpoint) — made the client show a hard "Submission failed", leaving isSubmitted=false so the success screen + its P&L/Balance-Sheet link never rendered → the user was STRANDED with no path to the next page despite the submission going through. Fix (wizard-client.tsx onSubmit): idempotent retry loop (up to 3 attempts, 700ms·attempt backoff). The route already marks wizard_progress 'submitted' before anything and returns {success,'Already submitted'} on a repeat WITHOUT re-enqueuing (verified: 3× allow_resubmit=false → 200 'Already submitted', no duplicate job), so retrying a lost/5xx response is safe — attempt 1 carries the caller's allow_resubmit, retries force allow_resubmit=false to hit the dedup. A real validation error (400+fields) is NEVER retried (shows field errors immediately). After 3 genuine failures, the toast tells the user to refresh and check rather than implying lost work. NOTE: the wizard link is session-scoped (resolves the account from the logged-in user, not the URL) — to test Dynamiq, log in as worldresltd@gmail.com, not uxio74.)_
_Prior: 2026-06-12 — Claude (WIZARD SUBMIT IS NOW TRULY ASYNC, Antonio's report "submission failed": the tax_form_setup job used to run INLINE in /api/portal/wizard-submit, holding the browser response 15-45s+; on Vercel both of his attempts died ("No response is returned from route handler") while every server-side step had succeeded — data saved, job completed, SD advanced — so the client saw failure and resubmitted, duplicating review tasks. Fix: the inline claim+execute block is REMOVED; the route returns right after enqueue (~1.6s measured locally, was 41s) and the job runs in /api/jobs/process via enqueueJob's existing fire-and-forget trigger (~1s pickup) with the 5-min process-jobs cron as safety net — the same path formation/onboarding always used. Success screen's "we are preparing your P&L" copy covers the async window; the financials page recomputes on load.)_
_Prior: 2026-06-12 — Claude (MMLLC tax wizard §14 redesign: 6 steps with a REAL members step (the old config never rendered members — machinery keys on step id 'members'); members pre-filled from CRM in wizard page.tsx ("confirm, don't type"); members-repeater remove now shifts later members down + writes member_count; every field carries plain-language hint EN/IT. See tax-returns.md §14 entry for the full redesign.)_
_Prior: 2026-06-12 — Claude (WIZARD CONDITIONAL CHAIN FIX, Antonio's QA repro: a conditional field is now visible only if its WHOLE ancestor chain holds (isFieldVisible, module-level in wizard-client.tsx, used by BOTH validateStep and the render filter). Before: child checked only its direct parent — crypto=Yes → 1099=No (stored in draft) → crypto flipped to No left the required CSV upload demanded by an invisible chain and blocked Next. Verified live: stale-child state reproduced, flip to No → all three descendants vanish, Next advances.)_
_Prior: 2026-06-11 — Claude (Slice 9: tax-financials review screen gains the YEAR-COVERAGE section (per-bank month-gap questions, two-button answers, 'had_activity' guides to replace the file; attest button blocked while any coverage question is open) and the TaxBanner shows 'Check and confirm your Profit & Loss and Balance Sheet →' linking /portal/tax-financials for MMLLC/Corp returns with a submission (showFinancialsLink prop set in app/portal/page.tsx). See tax-returns.md Slice 9 for the engine + handoff.)_
_Prior: 2026-06-11 — Claude (Tax financials review screen, Slice 8: NEW /portal/tax-financials (server page resolves account via portal_account_id cookie + open tax year from tax_returns data_received=false) + components/portal/tax-financials-review.tsx (client: gates checkmarks, P&L/BS cards, K-1 member table, question groups with plain-language answer chips EN/IT, file cards with delete, Excel download, attestation checkbox+button hard-blocked by canConfirm). All API routes OWNER-ONLY (lib/portal/owner-access.ts) — tax financials are non-delegable to portal teammates. R099 error surfacing on every fetch. See tax-returns.md Slices 7-8 for the engine + routes.)_
_Prior: 2026-06-11 — Claude (Tax wizard prior-return matrix, Slice 6 master plan §5: MMLLC docs step replaces the optional prior_year_return file with a required `prior_return_case` select (we_filed / filed_elsewhere / first_year / never_filed) + conditional fields: PDF-only upload for filed_elsewhere (server guard in both wizard upload routes), back-filing interest select + under-responsibility declaration checkbox for never_filed (declaration wording PENDING Antonio/lawyer review). Case A is preselected by server prefill when tax_returns has 'TR Filed' at year-1; the submit route re-verifies the claim and fire-and-forgets lib/tax/prior-return-case.ts::processPriorReturnCase — see tax-returns.md for the full §5 pipeline.)_
_Prior: 2026-06-11 — Claude (Wizard AUTOSAVE: debounced 2.5s silent save on any change (typed fields, repeater rows, uploaded file paths — everything funnels through handleFieldChange's dirty flag), single-flight guard, failed silent saves re-mark dirty and retry on next edit, 'Saved HH:MM' indicator in WizardShell (autosaveStatus prop). Manual Save draft unchanged (loud). Step-change fire-and-forget save pre-existed. Closes the lost-upload-reference gap found in Slice 1 QA.)_
_Prior: 2026-06-11 — Claude (Tax wizard Slice 1, master plan b2115fd3: MMLLC/Corp documents step now uses CSV-ONLY per-bank repeater `bank_accounts` (free-text bank name + account label + checking/credit-card type + .csv-only upload, gift-framing copy EN/IT) replacing the single `bank_statements` field — legacy drafts still read. Wizard repeater infra fixed: file sub-fields get the upload callback (silently stored filenames before), remove-row reindexes (orphaned higher rows before), `repeaterRequired` step gating, hint rendering. CSV-only server guard on both wizard upload routes for `bank_accounts_*_statements`.)_
_Prior: 2026-06-11 — Claude (wizard field UX: per-field help is now ALWAYS visible (the click-to-reveal "i" tooltip in `wizard-field.tsx` was removed — clients never clicked it); new `warningOnValue` shows an amber callout on a specific answer; a required `repeater` with a `conditional` is gated + enforced by `validateStep`. Powers the SMLLC tax intake — full writeup in `tax-returns.md`. PRs #106/#107.)_
_Last verified against code: 2026-06-15 — Claude (View-as no longer counts as the client's login. The entry route (`app/portal/view-as/route.ts`) captures `auth.users.last_sign_in_at` BEFORE minting the client session and restores it AFTER via the SECURITY-DEFINER fn `public.viewas_restore_last_sign_in(uuid, timestamptz)` (self-guarded to service_role; migration `20260615-2100`). Before this, View-as's verifyOtp stamped last_sign_in_at = now(), which polluted "Last login" AND wrongly HID the "Resend Welcome" credentials button on the contact page — the gate is `showResendWelcome = portalAuth.exists && !portalAuth.lastLogin` (`contact-detail.tsx`), so a view-as-stamped login made the system think the client had logged in. Surfaced by Marco Barbiero "can't access portal"; his polluted timestamp was corrected back to null.)_
_Last verified against code: 2026-06-16 — Claude (ITIN "Client Signing" card on the flow detail page now has a shipping-tracking form (`components/portal/itin-shipping-form.tsx`) below the document downloads — courier dropdown (FedEx/DHL/UPS/USPS/Other) + tracking number → POST `app/api/portal/flows/[id]/shipping`. Once saved it collapses to a read-only summary (courier + tracking + a courier tracking link via `lib/flows/courier.ts`) with Edit. Shown only when the SD is AT Client Signing (`shippingForm` set on the `ShippingCard` only then). Bilingual EN/IT. Staff see the same data via the `shipping_info` workspace component.)_
_Last verified against code: 2026-06-23 — Claude (international office clock: `components/portal/office-clock.tsx` mounted in the portal top bar on every page (client + teammate branches of `app/portal/layout.tsx`) + holiday-aware `lib/portal/office-hours.ts` (`getOfficeStatus`/`usFederalHolidays`/`isOfficeHoliday`); live-chat/support hours = Mon–Fri 9 AM–3 PM ET excl. US federal holidays — see "Office hours / international clock" under Chat/notifications. Office-open 8 AM ≠ chat-open 9 AM, intentional.)_
_Last verified against code: 2026-06-23 — Claude (office clock "YOUR TIME" now reads the **client's** timezone from `contacts.address_country` (new `lib/portal/client-timezone.ts::countryToTimeZone` — normalized free-text + alias table for spellings/casing/accents; multi-tz country → main tz; empty/unknown → null → device/browser fallback). `app/portal/layout.tsx` resolves the viewed contact's country server-side and passes `clientTimeZone`/`clientTimeZoneLabel` to `OfficeClock`. Works under View-as because the View-as route mints the client's OWN session, so `getClientContactId` = the viewed client. Teammate branch keeps the device fallback. See "Office hours / international clock" under Chat/notifications.)_
_Last verified against code: 2026-06-25 — Claude (ITIN visibility for formation-tier clients + wizard cookie-trust fix, branch `fix/itin-workspace`, SANDBOX. **(1) ITIN shows alongside FormationDashboard.** A formation-tier client who also has a contact-scoped ITIN never saw it: `app/portal/page.tsx` returns `<FormationDashboard>` early in all four formation branches, before the account/lead "Service Status" sections. New `components/portal/flow-status-section.tsx` (`PortalFlowStatusSection`, extracted from the existing no-account Service-Status markup) is now rendered ABOVE FormationDashboard in every formation return, fed by a `loadItinFlows()` helper (`getPortalFlows('', locale, contactId)` filtered to `flow_type==='ITIN'`). Verified in the running app: "SERVICE STATUS / ITIN 2026 / Active" renders above "We're forming your LLC". **(2) `/portal/wizard` no longer resolves to the wrong (tax) wizard via a stale cookie.** `app/portal/wizard/page.tsx` initialized `accountId = cookieAccountId || ''` and only overrode it when the contact had `account_contacts` links — so a logged-in contact with NO account link (an in-progress-formation / ITIN-only client) kept a stale `portal_account_id` cookie's FOREIGN account, then resolved the wizard type from THAT account's service deliveries. A Tax Return SD on the unrelated account → the 4-step tax wizard ("Financial Information") for a client with no tax return (Daniel Pasztor symptom). Fix: `accountId = contactId ? '' : (cookieAccountId || '')` and the cookie now only SELECTS among `account_contacts`-verified owned accounts — never grants an unowned one. Reproduced + fixed against the sandbox "Daniel Pasztor QA" fixture: pre-fix wizardType=`tax` (foreign accountId); post-fix wizardType=`formation` with `itin` as the second option, `accountId=''`. **Caveat:** the sandbox repro surfaced the cookie-trust path; if a client legitimately OWNS an account that carries a Tax Return SD, the contact-scoped ITIN is still invisible to the account-scoped wizard query (only Closure is unioned today via `getFlexibleServiceTypes()`) — flagged, not changed. Sandbox only; not on production.)_
_Prior: 2026-06-24 — Claude (PER-COMPANY CHAT SCOPING, branch `feat/portal-chat-scoping`. Multi-company clients previously saw ONE merged chat (the hook threaded by `contact_id` only — PR 2 Step 6); switching companies in the sidebar did nothing. Now the chat is scoped to the selected entity. **Privacy boundary (the whole point):** a company thread is shared by everyone linked to that account (members + chat teammates), so it shows ONLY `account_id=X` messages. A contact's personal/untagged (`account_id IS NULL`) messages ride along in a company view ONLY when that account is **sole-owned by the viewer** — exactly one linked contact == them — decided SERVER-SIDE via `mayIncludePersonalNull` (structural; NOT the messy free-text `account_contacts.role`, since 61 MMLLC accounts carry an `owner`-role contact and would leak). So: SMLLC (sole) view = account + personal; MMLLC view = account only, never personal; formation/no-account view = `account_id IS NULL AND contact_id=me`; clients with no sole-owned company get a synthetic **Personal** switcher entry. **Pieces:** pure `lib/portal/chat-scope.ts` (`mayIncludePersonalNull`, `buildChatQueryPlan`, `messageVisibleInPlan` — unit-tested) + server `lib/portal/chat-scope-server.ts` (`resolvePersonalNullInclusion`); GET/read `app/api/portal/chat/route.ts` + `/read/route.ts` gain `scope=company|personal` (absent ⇒ legacy/admin path UNCHANGED — admin CRM `portal-chats` untouched); `lib/hooks/use-portal-chat.ts` takes a `ChatScope`, subscribes broad (account_id and/or contact_id) then drops cross-company rows via `messageVisibleInPlan`; `lib/portal/queries.ts::getChatEntities` builds the chooser list; `app/portal/chat/page.tsx` resolves the selected entity from the `portal_account_id`/`portal_formation` cookies (+ `'personal'` sentinel) and passes a `scope`; `components/portal/portal-chat.tsx` replaces the old Person/Company toggle with a first-send **popup** ("Which company is this message about?", multi-entity only, warns when the company is shared) + a prominent always-visible **banner** at the top of the chat card ("CHATTING ABOUT / <entity>" + Switch button; accent color by entity kind — blue company / amber formation / zinc personal; replaced the original small bottom pill per Antonio's UI feedback); view-follows-choice (popup switches the cookie). Single-entity clients auto-tag, no popup. Verified in sandbox by seeding Michele Cotti (Whalecot SMLLC sole-owned + Conversion Monsters MMLLC 2-member + personal NULLs): SMLLC view=8 (5 company+3 personal, no CM leak), MMLLC view=6 (4 his+2 other member, ZERO personal leak), personal view=3; build + 3660 unit tests green. **Known follow-up (out of scope, flagged):** the sidebar unread badge + MMLLC shared-thread `read_at` are still single-valued/contact-keyed — a member sees shared messages in the thread but the badge/read-state aren't per-member yet. Sandbox only.)_
_Prior: 2026-06-23 — Claude (per-company scoping of action items + banners, branch `fix/portal-entity-scoping`, dev_task bb54680b. Multi-company clients (or an active company + a new in-progress formation) saw one context's items/banners leak into all views. **(1) Action items** — `getPortalActionItems` (`lib/portal/queries.ts`) queried in-progress + submitted wizards with `account_id=X OR contact_id=Y`, so a contact-scoped Company Formation wizard (`account_id NULL`, for a NEW company) and wizards from the contact's OTHER accounts surfaced under the selected company (e.g. "Complete Formation Form" under Scaledge). Both wizard sub-queries are now scoped to `account_id = selected account` only; the formation/no-account view keeps its own contact-scoped path (`getPortalActionItemsByContact`). The per-member OA-signature `contact_id` check and the intentional contact-scoped Company Closure surfacing are unchanged. **(2) "Set up your new company" banner** — `formationInProgress` in `app/portal/page.tsx` was queried by `user.email` on an offer left at `account_id NULL`, so it fired on every active company and lingered after the company was formed (showed under Whalecot). REMOVED from the active dashboard — the in-progress formation is now a switchable entity (company switcher → `FormationDashboard` with the Continue-Setup CTA), the correct per-entity path. The `FormationInProgressBanner` component file is now unused (dead). **(3) Stale tax card** — the "Complete Tax Information — <year>" item was gated only on `tax_returns.data_received=false`, a flag flipped by a downstream job (`tax-form-setup`) that can lag/fail, so the card persisted for months after submission. Now also suppressed YEAR-ACCURATELY when a `tax_return_submissions` row exists for the account + `tax_year` (a prior year must not suppress the current year). **(4) Service Status flow leak** — found during browser QA: `getPortalFlows` merges `CONTACT_FLOW_TYPES = ['ITIN','Company Formation']` (contact-scoped, `account_id NULL`) into every account's Service Status, so a new company's in-progress formation showed as a "COMPANY FORMATION <year>" stepper under an unrelated active company (same leak class as #1, pre-existing). Fixed: `getPortalFlows` now EXCLUDES contact-scoped Company Formation when a real `accountId` is selected (formation is a separate switchable entity → `FormationDashboard`); ITIN still rides along; no-account/ITIN-only callers (`accountId===''`) unaffected. Browser-verified on the sandbox production deploy (`td-operations-sandbox.vercel.app`) with a seeded multi-company + in-progress-formation contact (Matteo Angioni): action items "All caught up" on the active company, no formation banner, formation context renders its own tracker, and Uxio Test LLC's stale tax card is gone. Sandbox only.)_
_Prior: 2026-06-23 — Claude (office clock "YOUR TIME" now reads the **client's** timezone from `contacts.address_country` (new `lib/portal/client-timezone.ts::countryToTimeZone` — normalized free-text + alias table for spellings/casing/accents; multi-tz country → main tz; empty/unknown → null → device/browser fallback). `app/portal/layout.tsx` resolves the viewed contact's country server-side and passes `clientTimeZone`/`clientTimeZoneLabel` to `OfficeClock`. _Also 2026-06-23 — Claude (Formation "Filed with State" copy + date, branch `fix/portal-filing-banner`. **(1) Waiting banner reworded.** The blue waiting card (only shown at SD stage "Filed with State") said "Your LLC is being filed / we submitted your Articles" — present tense, but it's ALREADY filed and we're waiting on the state to confirm. Now: "Your LLC has been filed with the Secretary of State on {date}. Processing time varies by state — typically 1–4 weeks. We'll update you as soon as the state confirms." (EN + IT; date interpolated into the bold title via `waitingStateTitleDated`, falls back to the undated title when unknown). **(2) Filing date in the progress tracker.** The "Filing with the state" step (stage "Filed with State") now shows "· Filed Jun 18, 2026" once reached. Date source = new `filedAt` returned by `getFormationTracker` (`lib/portal/queries.ts`): prefers the durable `stage_history` `to_stage='Filed with State'` `advanced_at` (survives advancing past the stage), falls back to `stage_entered_at` only while AT that stage, else null (legacy SDs with no recorded transition show no date — graceful). `filedAt` threaded → `buildFormationTrackerSteps(…, filedAt)` attaches it to the Filed-with-State step (completed/current only) → `formation-progress-tracker.tsx` renders the compact locale date; also passed to `formation-dashboard.tsx` for the banner. All 4 FormationDashboard render sites in `app/portal/page.tsx` forward `filedAt={tracker?.filedAt}`. Sandbox only.)_
_Also 2026-06-23 — Claude (international office clock: `components/portal/office-clock.tsx` mounted in the portal top bar on every page (client + teammate branches of `app/portal/layout.tsx`) + holiday-aware `lib/portal/office-hours.ts` (`getOfficeStatus`/`usFederalHolidays`/`isOfficeHoliday`); live-chat/support hours = Mon–Fri 9 AM–3 PM ET excl. US federal holidays — see "Office hours / international clock" under Chat/notifications. Office-open 8 AM ≠ chat-open 9 AM, intentional.)_
_Also 2026-06-20 — Claude (Formation dashboard "Your LLC is being filed / we submitted your Articles" banner was PREMATURE. `formation-dashboard.tsx` computed `waitingForState = wizardSubmitted && !stateConfirmed`, so it fired at the "Wizard Submitted" stage — where we're still reviewing the LLC name and nothing has been filed yet. Now gated on the SD stage via the existing `sdStage` prop: `sdStage ? sdStage === 'Filed with State' && !stateConfirmed : <legacy>`. The legacy heuristic remains only as a fallback when no stage signal is available. Surfaced during a test-contact reset: a fresh "Wizard Submitted" client wrongly saw the Articles-filed banner. Also note for test resets: a raw `contacts.portal_tier` write does NOT update the auth user's `app_metadata.portal_tier` (only `syncTier` does both) — a stale `active` auth tier can leak Bank Applications / wrong routing; reset it via the auth admin API too.)_
_Last verified against code: 2026-06-20 — Claude (Formation QA, branch `fix/formation-workspace-bugs`. **(1) New `/portal/banks` "Bank Applications" page** (`app/portal/banks/page.tsx`) — self-service guidance to open a business bank account after formation completes: what you need (EIN + Articles), a prominent "never share your signed SS-4" warning, business-description guidance, and a curated provider list (Relay/Mercury/Sokin/Payset/Wise, edit `RECOMMENDED_BANKS`). Replaces the old Banking Fintech wizard/SD (removed — formation ends at the EIN). Added to the sidebar (`portal-sidebar.tsx`) under Company as `nav.bankApplications`, **active-tier only** (a formation-tier client with EIN pending can't open an account yet); the old `nav.wizard`→"Bank Applications" relabel was retired (it was Banking-SD-driven), so the wizard item is always "Complete Setup". **(2) "Complete Formation Details" CTA gating** — `formation-dashboard.tsx` takes a new `sdStage` prop (from `getFormationTracker.currentStage`, passed at all 4 render sites in `app/portal/page.tsx`); the wizard CTA hides once the formation SD is past "Payment Confirmed", fixing the card persisting when the `wizard_progress` row is contact-keyed (`account_id` NULL) and the account-scoped lookup returned nothing. **(3) Portal Documents** shows formation Articles + EIN letter but NEVER the signed SS-4 (internal). See formation.md + flows.md for the full set.)_
_Prior: 2026-06-19 — Claude (Formation dashboard routing no longer depends on the stale auth-metadata tier. The no-account branch of `app/portal/page.tsx` previously showed `FormationDashboard` only when `user.app_metadata.portal_tier === 'formation'`; if that auth tier was stale (e.g. left at `active` after a prior formation completed, or never set) the client fell through to the old `WelcomeDashboard` (Review Proposal → Sign Contract → Make Payment). Now the gate is `authTier === 'formation' OR an active contact-scoped Company Formation SD exists` — the SD (`getInProgressFormations(contactId)`, which short-circuits after 1 query when none) is the authoritative signal, so the 7-stage tracker shows at ANY formation stage including "Payment Confirmed". Surfaced live by Marinela Marku, whose auth tier was stuck at `active`. Reuses the contact-scoped FormationDashboard render path unchanged — only the gate condition broadened.)_
_Prior: 2026-06-18 — Claude (Pending Client Decision Requests now surface PORTAL-WIDE, not just on the flow detail page. `components/portal/pending-decisions.tsx` (mounted once in `app/portal/layout.tsx` above `{children}`, for any contact) fetches `GET /api/portal/my-decisions` and renders the client's pending requests as actionable `DecisionCard`s under an "Action required" heading; self-hiding (renders nothing when there are none). Fixes the case where a no-account / lead-or-formation client (e.g. Marinela Marku) received an LLC-name approval but had no path to the flow page where the card used to live exclusively. Tier-agnostic — works on every dashboard branch.)_
_Prior: 2026-06-18 — Claude (Client Decision Requests reach the client on the flow detail page. `app/portal/flows/[id]/page.tsx` loads the SD's `client_decision_requests` (newest first) and renders `components/portal/decision-card.tsx`: the newest pending one is actionable (approval Yes/No, choice radios, or text field; EN/IT via message_it), older ones collapsed read-only. Submits to `POST /api/portal/decisions/[id]/respond` (portal auth + ownership). Full system: client-decision-requests.md.)_
_Prior: 2026-06-17 — Claude (Company Formation portal tracker rewritten to read SD pipeline stages. `getFormationTracker()` (`lib/portal/queries.ts`) resolves the formation SD's current stage (cascading sdId → account-scoped → contact-scoped) + the 7 Company Formation `pipeline_stages` (client_label/client_label_it); the pure `buildFormationTrackerSteps` (`lib/portal/formation-progress.ts`) + `FormationProgressTracker` (`components/portal/formation-progress-tracker.tsx`) render the journey with an amber "Action required" glow on the CURRENT client-action stage (Payment Confirmed → wizard link, SS-4 Prepared → sign link), EN/IT. Wired into all 4 `FormationDashboard` render sites in `app/portal/page.tsx` via a new optional `trackerSteps` prop; the legacy signal-derived milestone list remains the fallback when no formation SD/stages resolve. Part of Workspace v2 — see flows.md/formation.md.)_
_Prior: 2026-06-17 — Claude (NEW staff page `/portal/team/approvals` (`app/portal/team/approvals/page.tsx` + `app/api/portal/team/approvals/route.ts` + `components/portal/approvals-list.tsx`): a phone-friendly list of the AI assistant's queued action-proposals with Approve/Reject. Staff-only — gated on a Supabase user whose `app_metadata.role !== 'client'` (same staff distinction as `lib/portal/team/gate.ts`); clients/teammates see "not available". Approve atomically flips `approval_queue` pending→approved (guarded on status='pending'; no 6-digit code — the per-card button on an authed staff session is the binding the code provides in chat); the async executor then runs it. No sidebar link yet — reachable by URL.)_
_Prior: 2026-06-17 — Claude (SMLLC tax wizard documents step — removed the "Bank Statements (optional)" + "Financial Statements (optional)" file uploads (Antonio). Single-member clients enter figures in the "Financial Information" step; both uploads were optional, never read by any code, pure confusion. Step now = prior-year return (optional) + confirm checkbox. Surgical single-member-only production ship; multi-member/Corp wizard untouched. See tax-returns.md for detail.)_
_Prior: 2026-06-16 — Claude (ITIN "Client Signing" card on the flow detail page now has a shipping-tracking form (`components/portal/itin-shipping-form.tsx`) below the document downloads — courier dropdown (FedEx/DHL/UPS/USPS/Other) + tracking number → POST `app/api/portal/flows/[id]/shipping`. Once saved it collapses to a read-only summary (courier + tracking + a courier tracking link via `lib/flows/courier.ts`) with Edit. Shown only when the SD is AT Client Signing (`shippingForm` set on the `ShippingCard` only then). Bilingual EN/IT. Staff see the same data via the `shipping_info` workspace component.)_
_Earlier 2026-06-16 — Claude (ITIN "Client Signing" card on the flow detail page now EMBEDS the actual prepared documents (W-7 / 1040-NR / Schedule OI) as download links, right under the mailing address + checklist — `buildItinShippingCard()` takes the SD's client-safe docs and `FlowStageJourney`'s `ShippingCard` renders them as `<a href="/api/portal/documents/[id]">` (auth-gated proxy, Content-Disposition: attachment). Replaces the old "listed under Documents below" text. The SD-linked portal-visible forms are exactly those three. NOTE: in sandbox Damiano's SD has no generated forms (service_delivery_id NULL on all his docs), so the embedded list is empty there until the forms are generated/seeded; in production they're SD-linked and render.)_
_Earlier 2026-06-16 — Claude (Flow DETAIL page (`app/portal/flows/[id]/page.tsx`) is now usable for ITIN. Two fixes: (1) the type gate accepted only `FLOW_TYPES`, so an ITIN flow returned `notFound()` on both envs — changed to `ALL_FLOW_TYPES`; (2) ITIN stages carry `client_description` (and, on prod, `client_label`), so the page now renders the clickable `FlowStageJourney` (vertical, each stage expands to its description) for contact flows, with a print/sign/mail card on the "Client Signing" stage — TD document-receiving office **Seminole, 11125 Park Blvd Suite 104-153** (NOT the Largo CMRA business address) + a 2× W-7 / 2× 1040-NR+OI / 2× passport-copy checklist. Account flows still use the horizontal `FlowProgressTracker`. The W-7/1040-NR/Schedule OI PDFs (portal_visible, SD-linked) show in the Documents section below.)_
_Earlier 2026-06-16 — Claude (ITIN-only clients (no account/LLC) now see their ITIN flow too. The no-account branch of `app/portal/page.tsx` (the WelcomeDashboard path) computes `getPortalFlows('', locale, contactId)` and renders a "Service Status" section above the welcome dashboard when the contact has contact-scoped flows. `getPortalFlows` now SKIPS the account query when `accountId` is falsy (passing `''` would otherwise be an invalid-uuid error on `.eq('account_id','')`), so it safely returns only the contact-scoped (ITIN) flows. Covers the formation-tier early-return path NO (an ITIN-only client isn't formation tier). FORWARD-LOOKING: sandbox currently has no ITIN contact lacking an Active/Suspended account, so verified by build + mechanism, not live data.)_
_Earlier 2026-06-16 — Claude (Portal "Service Status" now surfaces contact-scoped ITIN flows. `getPortalFlows(accountId, locale, contactId?)` gained a `contactId` param and runs a SECOND query for contact-scoped SDs (`contact_id = contactId AND account_id IS NULL`, filtered by `CONTACT_FLOW_TYPES = ['ITIN']` — NOT `FLOW_TYPES`, which stays account-only by design) merged with the account-scoped results. `app/portal/page.tsx` passes the already-resolved `contactId`. ITIN stages currently have no `client_label`, so ITIN renders as the neutral "Active" card (links to `/portal/flows/[id]`) rather than a stepper until labels are added. NOTE: only reaches clients who have an active account (no-account ITIN-only clients are routed to the WelcomeDashboard before this section). Did NOT add ITIN to `FLOW_TYPES` (the literal task asked to) — used the existing `CONTACT_FLOW_TYPES` separation instead.)_
_Earlier 2026-06-15 — Claude (View-as no longer counts as the client's login. The entry route (`app/portal/view-as/route.ts`) captures `auth.users.last_sign_in_at` BEFORE minting the client session and restores it AFTER via the SECURITY-DEFINER fn `public.viewas_restore_last_sign_in(uuid, timestamptz)` (self-guarded to service_role; migration `20260615-2100`). Before this, View-as's verifyOtp stamped last_sign_in_at = now(), which polluted "Last login" AND wrongly HID the "Resend Welcome" credentials button on the contact page — the gate is `showResendWelcome = portalAuth.exists && !portalAuth.lastLogin` (`contact-detail.tsx`), so a view-as-stamped login made the system think the client had logged in. Surfaced by Marco Barbiero "can't access portal"; his polluted timestamp was corrected back to null.)_
_Last verified against code: 2026-06-15 — Claude (Portal Addresses page: new `app/portal/addresses/page.tsx` (+ `nav.addresses` sidebar item under My Company, EN/IT i18n) shows three read-only cards: (1) Tony Durante's mailing address — the TD-provided `business_mailing` row from the `addresses` table (`is_td_provided=true`); (2) the account's Registered Agent (`accounts.registered_agent_address` + `registered_agent_provider`); (3) the account's Mailing/CMRA address (the FK-joined `business_mailing_address_id` → `addresses`). Uses `formatAddressString` from `lib/addresses`. Account-scoped (contact → their accounts; teammate → granted account). The CRM-side `/addresses` CRUD + `/api/addresses` + the `addresses` table already existed — this only adds the client-facing read view.)_
_Prior: 2026-06-14 — Claude (Sign page shows ONLY actionable items: `sign-documents-client.tsx` now renders only non-signed docs — the "Already signed" collapsed section was REMOVED (signed docs hidden entirely). AND `page.tsx` no longer surfaces **`draft`/`voided` OA & Lease**: an OA/Lease is created `draft` and only becomes `sent` when sent for signature (`lib/mcp/tools/oa.ts`), so a draft was never sent to the client and must not appear as "awaiting" (the prior `status==='signed' ? 'signed' : 'awaiting'` wrongly treated unsent drafts as pending — the real cause of "the sign page shows everything"). NOTE: MSA (`annual_agreements`) has no `sent` state — a draft MSA IS the pending state — so MSA draft is NOT excluded. Verified vs sandbox: Profit Harbor's draft OA/Lease excluded, MSA + signed items hidden, leaving only the pending Tax Return signature_request.)_
_Prior: 2026-06-14 — Claude (Sign page grouped pending vs signed: `app/portal/sign/sign-documents-client.tsx` now splits the documents list into a prominent **"To sign"** section at top (status ≠ signed — OA/Lease/SS-4/MSA/8832/generic signature_requests incl. the flow Tax Return) and a **collapsed "Already signed"** `<details>` below, so a client arriving from "Sign your tax return" sees only what needs signing. Card markup extracted into a `DocCard` component reused by both sections; React key changed from `doc.type` to `doc.href` (fixes a key collision when multiple generic signature_requests exist). The server page (`page.tsx`) is unchanged — it still gathers the full list; the grouping is presentation-only.)_
_Prior: 2026-06-14 — Claude (Portal tax banner recognises flow-workspace SD stages: `app/portal/page.tsx` now passes `tr.sd_stage` into `TaxBanner`, which maps it via `lib/tax/flow-banner.ts::flowStageBannerState` to the right client action — "Sign your tax return" (→ `/portal/sign`) at "Sent for Signature"/"Tax Return Prepared", "Complete your tax form" at "Wizard Available", "Under review" (no button) at "Data Submitted"/"Under Review", "Changes requested — edit your submission" at "Revision Requested", and Signed/Filed/Completed status. **Precedence:** `review_status` block still runs FIRST (production unaffected); the SD-stage block runs only when there's no `review_status`, before the legacy "Edit submission" fallback. Fixes flow Tax Returns wrongly showing "Edit submission" at later stages. CAVEAT: at "Tax Return Prepared" the signature request isn't created until staff click Send for Signature, so /portal/sign may be empty until the SD reaches "Sent for Signature".)_
_Prior: 2026-06-14 — Claude (Password show/hide toggle on the portal login (`app/portal/login/page.tsx`): a `showPassword` state flips the field between `type="password"`/`"text"` with an `Eye`/`EyeOff` (lucide) button positioned inside the input; `type="button"` + `tabIndex={-1}` so it never submits the form or steals tab focus. Same toggle added to the CRM login (`app/login/page.tsx`). Pure UI, no auth-flow change.)_
_Earlier: 2026-06-14 — Claude (Per-stage custom client email: `notifyClientOfStageAdvance` (`lib/portal/notifications.ts`) now accepts an optional `custom_message`; the new pure, unit-tested `buildStageAdvanceCopy` helper uses it as the email headline (dropping the generic secondary line) or falls back to the standard bilingual "service moved to stage" copy. `advanceServiceDelivery` (`lib/service-delivery.ts`, step 8b) passes the target stage's new `pipeline_stages.client_notification_message` column (migration `20260614-2000`, sandbox-only until promoted — same casting pattern as `notify_client_email`). Set on Tax Return "Wizard Available" so reaching it emails the client to submit their tax info. Override is locale-agnostic (one text for all languages) — an `_it` variant is a future follow-up if Italian clients need a translated message.)_
_Earlier 2026-06-14 — Claude (Client flow detail page: new `app/portal/flows/[id]/page.tsx` ([id]=service_delivery_id) — a read-only per-flow view showing the full progress stepper (`FlowProgressTracker` over `buildFlowSteps`), the flow's curated client-safe documents (`isClientSafeFlowDoc`), and the flow's chat messages (read-only, soft-deleted + `<!-- chat-event:` notes excluded, link to `/portal/chat`). Strictly access-gated: the SD must belong to an account the client can access (`getClientAccountIds`) or be the contact's own contact-scoped SD; teammates need account-scope + `documents`; otherwise `notFound()`. The home "Service Status" cards now link here (`FlowProgressTracker` gained an optional `href`; the neutral CMRA "Active" card is a `Link`).)_
_Earlier 2026-06-14 — Claude (Curated flow-document visibility: the per-flow documents grouping on `app/portal/documents/page.tsx` no longer filters `portal_visible=true` in SQL — flow docs are written `portal_visible=false` by default, so that gate showed nothing. It now applies `isClientSafeFlowDoc(serviceType, flow_stage, portal_visible)` (`lib/flows/flow-doc-visibility.ts`, pure + unit-tested): an admin-published doc (`portal_visible=true`) is always shown; otherwise the doc's `flow_stage` must be in a per-service-type CURATED allowlist of client-safe stages (receipts / confirmations / signed outputs). The **unsigned "Tax Return Prepared" draft is intentionally excluded** so it never leaks to the client; the signed return reaches them via the e-signature flow. The allowlist in that file is the single curation point — add a stage there to expose its docs.)_
_Earlier 2026-06-14 — Claude (Client-facing flow progress: the active dashboard (`app/portal/page.tsx`) now renders a **"Service Status"** card driven by `getPortalFlows(accountId, locale)` in `lib/portal/queries.ts` — the account's ACTIVE `service_deliveries` of the four recurring flow types (Tax Return / State Annual Report / State RA Renewal / CMRA), each rendered as a **visual progress stepper** (`components/portal/flow-progress-tracker.tsx`, a generalisation of `tax-progress-tracker.tsx`) — title (`buildFlowTopic`, e.g. "Tax Return 2025") + a dot-per-stage row with the current stage highlighted and stage labels. Steps come from the pure `lib/flows/flow-progress.ts::buildFlowSteps` (labelled-stage membership + completed/current/future state, current = highest labelled stage ≤ the SD's stage; catalog `icon` carried through); `computeFlowProgress` remains for the count/label. A flow with no client-labelled stages (CMRA) shows a neutral "Active"/"Attivo" card. Tax Return is dropped from Service Status when the dedicated Slice-5 `TaxProgressTracker` is already showing (no double stepper). Stage labels read `client_label`/`client_label_it`. The **documents page** (`app/portal/documents/page.tsx`) now groups docs stamped with `documents.service_delivery_id` into per-flow sections above Company/My (queried independently of category, then de-duplicated out of Company/My; `service_delivery_id` is missing from generated Supabase types so those queries cast through `unknown`/`as any`).)_
_Last verified against code: 2026-06-15 — Claude (Admin read-only "View as client": an admin opens a client's portal exactly as the client sees it, locked read-only, to reproduce "I don't see the wizard"/visual bugs. Button on the account + contact detail pages (`components/accounts/view-as-client-button.tsx`), rendered only when `isAdmin(user)` AND the contact has a portal login. Flow: `POST /api/admin/view-as` (admin-gated) mints a short-lived HMAC token (`lib/portal/view-as.ts`); `GET /portal/view-as?t=` verifies it, mints the CLIENT's session server-side via `generateLink`+`verifyOtp` (no email sent), sets the session cookies + a signed `td_view_as` marker cookie on the portal domain, audits to `action_log`, redirects to `/portal`. While the marker is present, `middleware.ts` BLOCKS every mutating request (POST/PUT/PATCH/DELETE) to `/portal` + `/api/portal` with 403 — the single read-only lock (server actions + API writes; the wizard storage upload is gated because its slot-mint is a POST). `ViewAsBanner` shows a persistent red "READ ONLY — Exit" bar (`app/portal/layout.tsx`). `GET /portal/view-as/exit` signs out **local-scope only** (never global — global would log the real client out of all their own devices) + clears the marker + audits. Relies on portal & CRM being different domains in prod (separate cookie jars) so the admin's CRM session is untouched; in single-domain sandbox the dual-login helper `scripts/sandbox-dual-login.sh` works around it. Admin-only, fully audited; not sabotage-proof against the admin who holds the button — prevents accidental writes, not deliberate cookie tampering.)_
_Prior: 2026-06-14 — Claude (Password show/hide toggle on the portal login (`app/portal/login/page.tsx`): a `showPassword` state flips the field between `type="password"`/`"text"` with an `Eye`/`EyeOff` (lucide) button positioned inside the input; `type="button"` + `tabIndex={-1}` so it never submits the form or steals tab focus. Same toggle added to the CRM login (`app/login/page.tsx`). _Prior: 2026-06-11 — Claude (wizard field UX: per-field help is now ALWAYS visible (the click-to-reveal "i" tooltip in `wizard-field.tsx` was removed — clients never clicked it); new `warningOnValue` shows an amber callout on a specific answer; a required `repeater` with a `conditional` is gated + enforced by `validateStep`. Powers the SMLLC tax intake — full writeup in `tax-returns.md`. PRs #106/#107.)_
_Prior: 2026-06-11 — Claude (new-document alerts unified: dispatch moved to the `updateDocument` choke point so ALL share paths alert (toggle, process-and-share, signature webhook); chat-on-share flag `new_document_chat_message_enabled` default ON; portal-digest renders document file names (lib/portal/digest-render.ts, `show_body` + `portal_digest_type_labels` override) and marks unresolvable recipients' notifications instead of recycling them every 5 min — skip counts in cron_log.)_
_Prior: 2026-06-10 — Claude (contact reuse in autoCreatePortalUser/createFromEmail is now case-insensitive + alias-aware via findContactIdByEmail — no duplicate contact/login for a case/alias email variant; see Gotchas. Prior same-day: Slice 5 tax progress tracker: `components/portal/tax-progress-tracker.tsx` on the portal home below the TaxBanner — 13 catalog-driven steps from `pipeline_stages` (`client_label` / new `client_label_it`, migration `20260610-1900`), position = SD stage overlaid with `review_status` (the SD parks at "Data Submitted" through the review loop; the overlay moves the dot to Under Review/Revision Requested/Approved/Confirmed so banner and tracker agree). Pure logic in `lib/tax/progress-tracker.ts::buildTrackerSteps` (null = hide: pre-1st-installment, terminated, legacy/unknown stage names). Auto-scrolls the current step into view. Catalog query: `getTaxTrackerCatalogStages` in `lib/portal/queries.ts`.)_
_Prior: 2026-06-09 — Claude (Slice 3 tax review banner: `components/portal/tax-banner.tsx` is now `'use client'` and has 7 states driven by `review_status` (null/submitted/under_review/revision_requested/approved/confirmed/reopened); includes a Confirm button (`POST /api/portal/tax-confirm`). `app/portal/page.tsx` TaxBanner now receives `reviewStatus`+`submissionId` from `getPortalTaxReturns` which joins the latest `tax_return_submissions` row. Banner filter changed from `!data_received` to "not terminal status". Legacy `dataReceived`/`sentToAccountant` fallback kept for pre-Slice-2 submissions._
_wizard file uploads → multi-file + any type + direct-to-storage signed URL, dev_task 64bfcdd9: new app/api/portal/wizard-upload-url, lib/portal/wizard-uploads.ts, wizard-field.tsx multi-file UI, wizard-client.tsx uploadToSignedUrl, extractUploadPaths + formation-setup.ts array-aware); 2026-06-08 — Claude (formation wizard routing for returning-active clients: getInProgressFormations now resolves leadId; FormationDashboard CTA + wizard/page.tsx carry/honor ?lead= / portal_formation cookie; prior 2026-06-06 new-document alerts; 2026-06-01 read of tier-config.ts, sync-tier.ts, auto-create.ts, queries.ts, notifications.ts, portal/team/*, resolve-portal-identity.ts, app/portal/layout.tsx, wizard-map.ts, wizard-visibility.ts, app/portal/wizard/page.tsx, app/portal/page.tsx, components/portal/formation-dashboard.tsx)_

> Formation-tier render path note: formation-tier clients without a materialized account go straight to `FormationDashboard` (skipping `ActionItems`). `FormationDashboard` has hard-coded CTAs for wizard/SS-4/OA/Lease, plus an optional `closureData` prop that surfaces a Closure CTA when a contact-scoped active Company Closure SD exists alongside the formation. Pattern shipped 2026-06-01 for Patrick Covelli (new NM formation + external DE LLC closure).

> Returning-active-client new-formation routing (2026-06-08): a new-company formation is **lead-anchored** — the wizard page only enters formation scope via `?lead=` (PR #75 / dev_task 21fd1f4a). A client who already owns an account and buys a new formation reaches `FormationDashboard` via the `portal_formation` switcher cookie, but a wizard CTA that omits `?lead=` falls through to the account context and opens that account's wizard (Tax/Banking) — the contact-scoped Company Formation SD (`account_id IS NULL`) is invisible to the account-id SD lookup. Fix: `getInProgressFormations` resolves each formation's `leadId` (via the formation offer linked to the SD; SD `notes` carry `from offer <token>` to disambiguate multiple formations — `extractOfferTokenFromNotes`), `FormationDashboard` carries `?lead=` on its CTA, and `wizard/page.tsx` also honors the `portal_formation` cookie (resolving the selected formation's lead) so the sidebar "Complete Setup" + direct-nav entry points route correctly. Fresh clients are unaffected (no account to fall through to). Surfaced live by Alessandro Federici (existing Scaledge LLC + new MMLLC NM).

## What it is
The client-facing app at **portal.tonydurante.us** where clients log in to see their services, documents, invoices, deadlines, chat with the team, and complete data-collection wizards. This is NOT the internal CRM dashboard — "portal" always means the client app.

## Tiers — the master access control (R102)
A client's portal experience is gated by their **tier**. There are exactly **4** (`lib/portal/tier-config.ts`, ordered):
| Tier | When | Sees |
|---|---|---|
| `lead` (0) | Offer sent, not paid | View/sign/pay offer, chat, profile, guide |
| `formation` (1) | Paid for formation (no EIN yet) | + data wizard, documents, signatures |
| `onboarding` (2) | Existing LLC going through onboarding | + upload docs, fill wizard |
| `active` (3) | Data reviewed / company live | + services, invoices, deadlines |

- Each tier **includes** the previous tiers' features (`TIER_FEATURES`).
- **A feature shows only if BOTH gates pass:** the tier allows it AND the data exists (`getPortalNavVisibility`). Tier alone is not enough.
- **Account type** is a second axis: **Client** (annual management — full feature set per tier) vs **One-Time** (standalone service — limited; no billing/invoicing/customers/deadlines).
- The value `full` is **removed** — never use it (R102).

### How tier is set — `syncTier()` only
- **ALL tier writes go through `syncTier()`** (`lib/operations/sync-tier.ts`). Never write `accounts.portal_tier` / `contacts.portal_tier` directly (R102).
- **Downgrade guard:** `syncTier` refuses to lower a tier unless `allowDowngrade=true` — it returns **success as a silent no-op**. This stops offer-publish (which runs at `lead`) from clobbering an existing `active` client.
- **Contact tier is computed, not set:** a contact's tier = the **highest** tier across all their linked accounts (`computeContactTier` / `maxTier`). Setting an account tier propagates to its contacts.
- `tierForContract(contractType)`: `formation`→`formation`, `onboarding`→`onboarding`, everything else→`active` (`lib/portal/auto-create.ts`).

## Chat, notifications, documents
- **Office hours / international clock.** Source of truth for office hours is `lib/portal/office-hours.ts`: **Mon–Fri 9 AM–3 PM Eastern (`America/New_York`), excluding U.S. federal holidays**. `getOfficeStatus(now)` returns `{open, reason}` (`open|before_hours|after_hours|weekend|holiday`); `isOfficeOpen()` = `getOfficeStatus().open`; `usFederalHolidays(year)`/`isOfficeHoliday()` compute observed federal holidays (Sat→Fri, Sun→Mon). Two consumers: (1) the chat **out-of-office auto-reply** (`lib/portal/auto-reply.ts`, fired from `app/api/portal/chat/route.ts` when a client messages while closed — now also fires on holidays); (2) the **`OfficeClock`** widget (`components/portal/office-clock.tsx`), a `'use client'` live clock mounted in the portal top bar (`app/portal/layout.tsx`, both client + teammate branches) on every page — shows ET office time + Open/Closed badge + the **client's own time**, bilingual, mount-guarded against hydration mismatch. **"YOUR TIME" is the client's timezone, NOT the device's:** the layout resolves the viewed contact's `contacts.address_country` → IANA tz via `lib/portal/client-timezone.ts::countryToTimeZone` and passes `clientTimeZone`/`clientTimeZoneLabel` to the clock, so it reflects the client even when staff open the portal via View-as (the device tz would otherwise be the staff member's). Empty/unknown country → falls back to the device/browser tz (Antonio's choice). `countryToTimeZone` normalizes free-text country (casing/accents) + an alias table (Italia→Italy, UAE, USA…); multi-tz countries → their main tz (refine by state later). **Office vs chat (do not "reconcile" these — they are two different things, confirmed by Antonio 2026-06-23):** the physical **office opens 8 AM ET** (what the signed service-agreement contracts in `app/offer/[token]/contract/*.tsx` state — correct, NOT a typo); **live chat/support opens 9 AM ET**. The clock + auto-reply track **chat/support (9 AM)** because their purpose is the client's expectation of getting a *reply*. The clock is labeled "Live chat open/closed" + "US Eastern" (not "Office") so the two don't get confused.
- **Chat** lives in `portal_messages`. Admin sends (dashboard or `portal_chat_send`) → the client gets an **automatic email** (`notifyClientOfAdminMessage`, throttled **1 email/conversation/2h**, bilingual — R103). Messages are **soft-deleted** (`deleted_at`/`deleted_by`, R100) and update over realtime. Tools: `portal_chat_inbox/read/mark_read/send`.
- **Emoji reactions** (2026-06-30) — react to a single message with any emoji (instead of a reply), in BOTH the client portal chat and the CRM staff chat, both directions. Stored ON the message row as a JSONB array `portal_messages.reactions` (`{emoji, reactor_id, reactor_type:'client'|'staff', reactor_name, created_at}`), so reactions ride the EXISTING realtime UPDATE handler (`{...m,...updated}` in `use-portal-chat.ts` and `portal-chats/page.tsx`) and the GET `select('*')` — no new subscription, no JOIN. Toggle is atomic via the Postgres fn `toggle_message_reaction` (row `FOR UPDATE` → no lost-update race); Slack-style (one reactor may add several distinct emojis; re-tap removes only their own). Endpoint `POST /api/portal/chat/message/[id]/react` (auth mirrors the pin route: staff=any message, client=own conversation, teammate=own account+`chat` cap). Shared UI `components/chat/message-reactions.tsx` = pills (emoji+count, viewer's own highlighted) + an add-reaction (smiley-plus) button that opens the full emoji picker; the picker's own recently-used row surfaces the last pick for fast re-use. (An earlier per-message "last-pick shortcut" button was **removed 2026-06-30** — it rendered the remembered emoji under EVERY message, which read as if every message had been auto-reacted; the picker's recent row covers reuse without the clutter.) **Notifications: in-app + push ONLY, never email** (Antonio's choice) — staff→client reaction makes a `type='reaction'` portal_notification (push + bell; **explicitly excluded from the digest email** in `portal-digest`), client→staff reaction sends `sendPushToAdmin`; only on ADD (not removal), throttled per message/direction. **System messages are NOT reactable** (the office-closed auto-reply etc.): the CRM renders them as centered pills with no strip, so the portal also suppresses the strip on `sender_type==='system'` for parity. Migration `20260630-1200-portal-messages-reactions.sql`. Helpers `lib/portal/reactions.ts` (`summarizeReactions`, `isValidReactionEmoji`).
- **Email is skipped when the client has active push** (PWA installed + notifications on) — they get the Web Push instead, so the email would be redundant. Per recipient, `notifyClientOfAdminMessage` checks `push_subscriptions` first by `contact_id`, then by `account_id` (account-level fallback covers teammates and any subscription not keyed to a contact); if either has ≥1 row, the email is suppressed for that recipient. The portal "Enable notifications" endpoint (`app/api/portal/push`) now records `contact_id` (from `getClientContactId`) alongside `user_id`/`account_id` so the per-contact check has data — **pre-existing subscriptions have a null `contact_id` until the client re-subscribes**, but they're still caught by the account-level check (they carry `account_id`). Caveat: the account-level fallback is coarse — in a multi-member LLC where only one member installed the PWA, members **without** push can have their email suppressed too until subscriptions carry `contact_id`.
- **Chat attachments** upload **directly to Storage via a signed URL** (`app/api/portal/chat/upload-url` + `lib/portal/chat-attachment.ts`), NOT through a serverless function — this bypasses the platform request-body limit that silently failed large passport photos / PDF scans ("Upload failed try again"). Shared helper `uploadChatAttachment`/`validateChatAttachment` is the single source of truth used by all three senders (client `portal-chat.tsx`, staff CRM inbox `portal-chats/page.tsx`, staff `contact-detail.tsx`). Policy: any normal file, **block-list** for active-content/executables (HTML, SVG, JS, exe, …); **100 MB** cap enforced client-side AND at the `assets` bucket `file_size_limit`. Files land in the **public** `assets` bucket under `chat-attachments/<accountId|contactId>/`. The old streaming route `app/api/portal/chat/upload` was removed.
- **Wizard file uploads** (formation/onboarding/tax/banking/itin/closure/company_info) upload **directly to Storage with a RESUMABLE (TUS) upload** (`tus-js-client` → `{ref}.supabase.co/storage/v1/upload/resumable`, helper `lib/portal/resumable-upload.ts`), NOT through the serverless `wizard-upload` route — to escape the ~4.5MB platform request-body cap that silently failed merged bank-statement PDFs (dev_task 64bfcdd9, 2026-06-09). `app/api/portal/wizard-upload-url` only **mints the path** (server owns the scheme); the browser then TUS-uploads in 6MB chunks authenticated with the **client's own session token** (`supabase.auth.getSession()`), with auto-retry on network blips + a progress bar. Every `type:'file'` field is **multi-file** and accepts **any file type** (no `accept` restriction unless a field sets one). The field value is now an **array of storage paths** (legacy single-string values still render). Path scheme is preserved — `{wizardType}/{identifier}/{fieldName}_{unique}_{filename}` — so all downstream matching by field name still works. `lib/portal/wizard-uploads.ts` (`collectUploadPaths`/`firstUploadPath`/`normalizeUploadValue`) is the single source of truth that flattens string|array; `wizard-submit`'s `extractUploadPaths` and `formation-setup.ts`'s owner-passport copy both go through it. Downstream Drive copy / passport OCR / bank-statement→P&L read the flat `upload_paths` list (unchanged); `formation-materialize.ts` already falls back to it. **Storage:** the upload uses the client's JWT, so `onboarding-uploads` needs an INSERT RLS policy — prod already had `Allow public read/upload onboarding`; sandbox was missing the bucket + all policies (migration `20260609-1210-onboarding-uploads-bucket-and-policies.sql` brings it to parity). **Max file size is the Supabase project's global upload limit** (a dashboard setting), which caps standard and resumable alike — resumable's win is reliable chunked transfer + resume, not a higher ceiling.
- **Notifications:** `createPortalNotification`, `notifyClientOfStageAdvance`, `getUnreadNotificationCount` (`lib/portal/notifications.ts`); `/portal/notifications` page.
- **New-document alerts (admin → client)** (`lib/portal/document-alerts.ts`, 2026-06-06; unified 2026-06-11): when a document becomes client-visible, the client is alerted, the Documents sidebar tab **pulses** with an unopened count, and the doc shows a **"New" pill + indigo-tinted row** until that person opens it. Dispatch is owned by the **`updateDocument()`/`updateDocumentsBulk()` choke point** (`lib/operations/document.ts`): an actual `portal_visible` false→true transition fires `notifyClientsOfNewDocument(documentId)` fire-and-forget — covering the file-manager toggle, process-and-share, the signature webhook, and future callers (singles alert by default; **bulk defaults to NOT alerting** — portal-transition migrations; opt in with `clientAlert: true`). The two admin upload routes (`app/api/crm/admin-actions/upload-document`, `upload-account-document`) call the module directly (raw insert path). It's idempotent via `documents.client_notified_at` (TOCTOU-guarded), respects the per-doc `documents.notify_client` toggle and the global kill switch `app_settings.new_document_alert_enabled` (default true), creates a `type='new_document'` portal_notification + web push, and (since 2026-06-11, Antonio: ON) also posts a localized **portal chat message** ("A new document has been added to your folder: <name>") behind `app_settings.new_document_chat_message_enabled` (default true; account-scoped — personal docs with no account skip chat). **Delivery rule mirrors chat:** PWA clients get the push; non-push clients get the batched digest email — the `portal-digest` cron skips the email for `new_document` when the recipient has a push subscription (no double-notify). **Digest rendering (2026-06-11):** section building lives in `lib/portal/digest-render.ts` — `document`/`new_document` items render the file name under the title (`show_body`); per-type icon/labels/show_body are overridable at runtime via `app_settings.portal_digest_type_labels`. **Unresolvable recipients** (contact without email/portal, account without `portal_account` or company email) get their notifications **marked `email_sent_at` and counted in cron_log `details.skipped`** instead of recycling through the digest query every 5 minutes forever (pre-fix: the LUMA June-9 row retried indefinitely). **"New" is per-CONTACT** via `portal_document_views(document_id, contact_id, viewed_at)` so each owner of a multi-member LLC clears their own state; opening a doc records the view (`POST /api/portal/documents/[id]/view` + the download proxy as backstop). **Baseline:** pre-feature docs keep `client_notified_at = NULL` and never show as "new" (no day-one flood; no backfill). Sidebar count via `getUnopenedDocsCount`; the tab stops pulsing once the client is on `/portal/documents`.
- **Home announcement banners** are client components rendered on the active-tier home dashboard (`app/portal/page.tsx`), each dismissed per-device via a versioned `localStorage` key read in a `useEffect` (no hydration mismatch): `GuideAnnouncementBanner`, `TeamAccessAnnouncementBanner` (key `td-team-access-announce-v1`, also drives the sidebar "new" badge), and `WhatsNewBanner` (key `td-whats-new-v1`, bilingual "What's New / Novità" feature announcement). Bump the key's version suffix to re-show after a major change. Lead/onboarding/formation tiers render different dashboard components and do not show these.
- **Documents:** auto-saved (`auto-save-document.ts`), templated (`document-templates.ts`); pages for general docs, `tax-documents`, `itin-documents`. Legal docs are view-only for non-signers.
- **Sales invoices (client's own tool):** the `/portal/invoices` Sales list has a per-row 3-dot action menu (View · Download PDF · Edit · Send · Reminder · Void) following the standard invoice lifecycle. These are the **client's own invoices** — no TD-accounting/tax impact — so Edit + Void are allowed at any stage incl. Paid; Send only advances Draft→Sent (never downgrades a paid invoice); Reminder is Sent/Overdue only; Split parents excluded. Full design + gating in `billing-invoicing.md` ("Portal Fatture per-row actions").
- **Invoices/payments:** `/portal/billing`, `/portal/invoices`; the **Pay modal is the only payment entry point** (R092) — invoice emails point here, never embed Stripe/wire. The expense list shows the **net amount due** (`client_expenses.amount_due`, the original `total` struck through when a credit was applied) and the Pay modal charges `amount_due`, so a credited invoice (e.g. a referral credit reconciled in — see `billing-invoicing.md` / `referrals-circleback.md`) shows the client the true balance. `getPortalExpenses` selects `amount_due`/`amount_paid` (added 2026-06-01).

## Team Access — teammates (Option B, independent identity)
An **active client's account-admin** can invite their employees ("teammates") into that **one company's** portal, choosing per-section what each may see.
- **Identity model (Option B):** a teammate is an **independent identity** — its own row in `portal_team_members`, **NOT** a contact and **NOT** in `account_contacts` (so billing/ops flows never target a teammate). Auth lives in `auth.users` with `app_metadata.role='client'` + `kind='team_member'` + `account_id` + `team_member_id` markers, and a synthetic email (`tm-<token>@teammate.portal.tonydurante.us`). Login is **username + password** (`app/portal/login`).
- **Who can manage:** only the **account-admin** ("main person"). Resolver precedence (`lib/portal/team/account-admin.ts`): persisted override (`accounts.portal_admin_contact_id`) → MMLLC SS-4 signer → signer name-match → owner-role `account_contact` → sole contact. SMLLC = the stored owner.
- **Capabilities are owner-chosen, server-enforced, default-deny** (`lib/portal/team/capabilities.ts`): `documents`, `invoices_billing`, `chat`, `company_services`, `bank_applications`, `sales_customers`, `company_data_form`, `announcements`. Empty = see nothing beyond always-on (overview/guide). Owner-only/non-delegable sections (team, sign, request service, referrals, profile, offer) are **never** delegable.
- **Chat notifications include teammates** — `notifyClientOfAdminMessage` (the single admin→client chat-email chokepoint) emails account contacts AND active teammates who have the `chat` capability + a real email on file (placeholder emails live only on auth.users, so they're skipped). Revoked teammates and no-chat-cap teammates are excluded; recipients are deduped by email. Teammates have no language column, so they **follow the owner's language** (the `role='owner'` account contact, else any contact, else English).
- **The gate** (`lib/portal/team/gate.ts`): `canAccessAccount(user, accountId, capability)` is **default-deny** — it replaces the leaky `if (contactId) { check }` pattern that silently skipped the check for a null-contact (teammate) user. Staff (non-`client` role) bypass. Pages use `getTeammateScopeOrNull(cap)`; the identity seam is `resolvePortalIdentity` (`contact | teammate | none`).
- **Production safety:** prod RLS denies a teammate every **direct/realtime** read (their JWT has no `contact_id`, so `get_client_account_ids()`/`get_client_contact_id()` return empty). Granted sections still work because pages fetch **server-side** via `supabaseAdmin`. Owner accepts a **responsibility disclaimer** at invite time.
- **Manage:** `/portal/team` (admin-only tab) — invite/edit/revoke/reset-password; revoke also bans the auth user. Optional teammate email enables notifications + self-serve password reset. The invite dialog explains email is optional (only for notifications).
- **Feature announcement** — account-admins see a one-time dismissible violet banner on the portal home (`TeamAccessAnnouncementBanner`, gated by `canManageTeam` computed via `isAccountAdmin`) + a "NEW" badge on the Team nav item; both clear via the shared `localStorage` key `td-team-access-announce-v1` (on banner dismiss/click or first Team-page visit). The "Invite Your Team" guide article (id `team-access`, EN+IT) lives in `app/portal/guide/page.tsx`. A broader broadcast can be posted to all clients via the existing `portal_announcements` system (CRM Config → Announcements).
- **Teammate chat attribution** — a teammate has no contact, so their chat messages stamp `portal_messages.sender_name` with the teammate's **display name** at send time (chat route teammate branch). The read route resolves the display name via `pickChatSenderName(contactName, storedSenderName)` (`lib/portal/chat-sender-name.ts`): contact name for client/owner messages, stored display name for teammates, null → UI's generic label. So staff/owner see *which* teammate wrote. Admin/system messages still render "Tony Durante Team".
- **Teammate chat threading** — the chat page (`app/portal/chat/page.tsx`) and `usePortalChat` thread by `contact_id` for normal clients but by `account_id` for teammates (no contact_id; their messages are stored by account with `contact_id` NULL). The page resolves the teammate's account via `getTeammateScopeOrNull('chat')` instead of the old `if (!contactId) redirect('/portal')` that silently bounced teammates out of Chat. The Journey Log tab is hidden for teammates (contact-oriented).

## Business rules
- **R102** — 4 tiers; `syncTier()` is the only writer; `formation` = no EIN; never advance tier manually; `full` is forbidden.
- **R103** — admin portal message auto-emails the client (throttled 2h, bilingual).
- **R100** — client-visible content (chat, notifications, docs) uses soft-delete; clients never see deleted rows, staff see a tombstone.
- **R092** — clients pay via the portal Pay button only.
- **R099** — client-side fetches must surface the server's real error, not a generic toast.
- **No support@ fallback** — the portal IS the channel; don't tell clients to email support@ as an alternative.

## How it's built
### Key files
- `lib/portal/tier-config.ts` — canonical tiers + `TIER_FEATURES` + `maxTier`.
- `lib/operations/sync-tier.ts` — `syncTier()`, `computeContactTier()`, `syncContactTiersForAccount()`.
- `lib/portal/auto-create.ts` — `tierForContract()`, `autoCreatePortalUser()`, `ensureMinimalAccount()`.
- `lib/portal/queries.ts` — everything the portal reads (`getPortalAccounts`, `getPortalNavVisibility`, `getPortalTier`, services/payments/expenses/deadlines, ITIN-at-signing).
- `lib/portal/notifications.ts` — client notifications + email.
- `lib/portal/team/*` — Team Access: `capabilities.ts` (per-section flags + nav map), `account-admin.ts` (admin resolver), `provision.ts` (create teammate auth + row), `server.ts` (list/update/revoke/reset), `gate.ts` (`canAccessAccount` default-deny + `getTeammateScopeOrNull`). `lib/portal/resolve-portal-identity.ts` — `contact | teammate | none` seam.
- `lib/mcp/tools/portal.ts` — `portal_create_user`, `portal_chat_*`, `portal_invoice_create/send`, `portal_messages`, `portal_team_send`, `portal_transition_*`.
- `app/portal/*` — the pages (login/auth, dashboard, wizard, documents, billing/invoices, chat, deadlines, services, members, settings, …).
- Wizards: `wizard-map.ts`, `wizard-scope.ts`, `wizard-visibility.ts`. Multi-company: `select-entity.ts`, `getPortalAccounts` (a contact can hold several accounts).
  - **Field rendering (`components/portal/wizard/wizard-field.tsx` + `wizard-client.tsx`, 2026-06-11):** every field's help text is **always visible** under the field — the click-to-reveal "i" tooltip was removed (clients never clicked it), so this applies to ALL wizards. A field may carry `warningOnValue` to show an amber callout when a specific answer is chosen (e.g. the SMLLC related-party "No" → $25k-penalty notice). A `type:'repeater'` field honors `conditional` (renders only when its gate matches) and, when `required`, `validateStep` blocks the step until ≥1 row exists with every required sub-field filled. See the SMLLC tax intake writeup in `tax-returns.md`.
  - Wizard types fall into three categories: **contact-scoped** (`formation` — lives on the contact until the LLC materializes), **account-scoped** (everything else by default — onboarding, banking, tax, company_info), and **flexible** (`closure` today — can run contact-scoped for an external LLC OR account-scoped for an existing managed LLC). Discovery (`wizard-visibility.ts`, `app/portal/wizard/page.tsx`) unions contact + account queries for flexible types so a managed-account holder can still see a contact-scoped Closure.
  - Action items (`getPortalActionItems` / `getPortalActionItemsByContact`) surface a `Start ⟨Wizard⟩ Form` card per active wizard-eligible SD that has no `wizard_progress` row yet, deep-linked to `/portal/wizard?type=⟨wt⟩`. In-progress wizard cards also deep-link so refreshes return the client to the same wizard.

### Tables
`accounts`/`contacts` (`portal_tier`; `accounts.portal_admin_contact_id` = team-admin pointer), `account_contacts`, `portal_messages` (`deleted_at`, `deleted_by`, pin fields, `reactions` JSONB), `portal_notifications`, `portal_team_members` (teammate identity + `capabilities` JSONB, RLS-on/service-role-only), portal auth users (`auth.users`), `payments`/`client_invoices`/`client_expenses`, `service_deliveries`, `deadlines`.

## Gotchas, invariants & past bugs
- **⚠️ FORMATION ENTITY TYPE: the signed contract decides, and the wizard NEVER guesses (2026-08-09, dev job `fc69557f`).** The formation wizard rendered a SINGLE-member form to MULTI-member clients and then refused to submit — it demanded as many ITIN applicants as the client had bought while showing a form on which only one person could exist. Alessandro Della Bianca hit the deadlock after paying €4,000. **Cause: two lookups with different fallbacks.** `itinCount` resolved lead-independently (offer matched by client email), while the entity type came ONLY from the `?lead=` scope and otherwise fell to a hard `'SMLLC'`. **Nearly every entry point drops that lead** — the formation dashboard button, the services-page `?type=formation` link (which also skips the cookie rescue), the post-payment portal notification, the reminder cron, the sidebar. Present since the wizard's first commit (`a3f5eeed`, 2026-03-21); `contracts.llc_type` has held the client's own choice since 2026-03-15 and the wizard never read it. **The rule now:** signed contract → the formation offer (live statuses only, case-insensitive email match) → **ASK THE CLIENT one plain question** ("Just me" / "Me and other owners"). Never wall (a hard stop recreates the dead end) and never guess. **Placement is load-bearing:** after `wizardType` is final and the old account is dropped, and BEFORE `normalizeEntityType` — that helper returns `'SMLLC'` for empty input and would silently reinstate the default. **`account.entity_type` is IGNORED for formation** — a returning client's existing SMLLC was deciding the shape of their NEW MMLLC. Corporation short-circuits (asking an LLC ownership question would be nonsense and its answer would be written as SMLLC/MMLLC).
- **Never persist a guessed entity type (2026-08-09)** — formation materialization reads `formation_submissions.entity_type` back as one of its entity-type sources, so a fabricated `'SMLLC'` could later confirm itself as evidence. `buildSubmissionRecord` now writes NULL, **and only for `formation_submissions`** — `entity_type` is NOT NULL on `onboarding_submissions`, `tax_return_submissions` and `company_info_submissions` (verified in prod + sandbox), where NULL is a 23502 → false "submission failed" toast AND a skipped auto-chain enqueue.
- **A member with no email is still a member (2026-08-09, Antonio)** — on the filing, in the OA, ownership counted; they simply get no contact and no portal access. Their `members` row used to be skipped entirely (the whole write sat inside `if (contactId)`) while their percentage was still deducted from the owner's share, so the ownership table summed to LESS than 100 and the person was missing from the SS-4 and the OA. The row is now written unconditionally with `contact_id: null`, the insert's error is captured (supabase-js RETURNS errors — the enclosing try/catch cannot see them, so the step used to claim "ownership recorded" about a row that never wrote), and the step message distinguishes "no email supplied" from "contact creation failed". Who SIGNS is a separate question — see `docs/systems/lease-oa.md` (signing set).
- **NEVER hand-write a wizard label (2026-07-21)** — every client-facing name for a wizard type comes from `lib/portal/wizard-labels.ts` (`wizardLabelFor` / `completeWizardFormTitle`). That map is `Record<WizardType, …>`, so the compiler refuses a new wizard type without a label. Do NOT loosen it to `Record<string, …>` and do NOT add a local ternary/switch "just for this surface" — four such local maps are what leaked `banking_payset` to three real clients on the portal home the day after the same bug was fixed in the reminder cron. The CRM staff view (`components/accounts/account-journey.tsx`) deliberately keeps its own more detailed labels ("Banking (Payset EUR)") — it is staff-facing and complete; do not merge it into the client map.
- **Wizard number fields support a domain minimum (`FieldConfig.min`, 2026-07-15)** — set `min: 0` on any money amount; `wizard-field.tsx` renders the input `min` attr + an inline EN/IT "cannot be negative" error, and `wizard-client.tsx`'s `belowFieldMin` blocks the step gate (top-level, member-indexed AND repeater sub-fields). Added after the Rocco Papotti incident: the external tax form (and the wizard, which was worse — default step 1 means one ArrowDown from 0 writes **-1**) accepted negative money values; -0.03/-0.04 landed in a real accountant-facing submission. The four tax money fields + `rpt_amount` carry `min: 0` in `wizard-configs.ts`. NEVER add a money field without `min: 0`. Full rule + shared helpers: see `docs/systems/tax-returns.md` (2026-07-15c) and `lib/types/tax-form.ts`.
- **A failed downgrade looks like success** — `syncTier` silently no-ops a downgrade and returns `success:true`. Intentional (protects clients); don't "fix" it into an error.
- **Never set a contact's tier directly** — it's computed as the max across linked accounts.
- **Tier gate AND data presence** both required to show a feature — a tier change won't reveal a section that has no data.
- **One-Time accounts stay limited even at `active`** (no billing/invoicing/customers/deadlines).
- **ITIN-at-client-signing is allowed at every tier** — the data (an active ITIN SD at "Client Signing") is the real gate, not the tier.
- **Teammates have no `contact_id`** — never gate portal access on `if (contactId)`; that silently skips the check for a teammate and leaks the resource. Use `canAccessAccount` / `getTeammateScopeOrNull` (default-deny). Teammates are never in `account_contacts` and must never be targeted by billing/ops flows.
- **Contact reuse is case-insensitive + alias-aware** (2026-06-10) — `autoCreatePortalUser`/`createFromEmail` resolve an existing contact via `findContactIdByEmail()` (`lib/operations/find-contact-by-email.ts`), NOT exact `.eq('email', …)`. A differently-cased or post-merge-alias email must reuse the real person, never spawn a duplicate contact + duplicate portal login (the Michele Cotti orphan bug). The helper matches `email` case-insensitively (ilike narrows, exact compare in JS to dodge the `_` wildcard) + folded `alt_emails`, and never returns a merged tombstone (`merged_into IS NOT NULL`). Never reintroduce a raw `.eq('email', …)` contact lookup in the activation/portal-creation path.
- Per-company chat isolation is the other in-flight multi-tenant project — verify current chat behaviour against code before assuming.

## How to verify current state
- Read `lib/portal/tier-config.ts` (the 4 tiers + features), `lib/operations/sync-tier.ts` (`syncTier` + downgrade guard + `computeContactTier`), `lib/portal/queries.ts` (`getPortalNavVisibility`, `getPortalTier`).
- A client's tier: `SELECT portal_tier FROM accounts WHERE id='<id>';` and `SELECT portal_tier FROM contacts WHERE id='<id>';` (contact should equal the max of their accounts).
- Note (R096): sandbox via sandbox MCP / `psql`; production `execute_sql` hits production.
