# Client Portal
_Last verified against code: 2026-06-26 — Claude (DUAL-ROLE portal: a person who is BOTH a client (account membership) AND a partner (a `client_partners` row) — e.g. Auralba — switches between views using the **EXISTING `CompanySwitcher`**, which gains a **"Partner — My Referrals"** entry for dual-role users (NOT a separate toggle — Antonio: the client must use the same switcher). Effective view is cookie-driven (`portal_mode`); single-role users are unchanged. `lib/portal/portal-mode.ts::resolvePortalMode` (DB: client_partners existence) + pure `decidePortalMode` (dual→cookie, else locked to capability) — unit-tested. `app/portal/layout.tsx` computes the effective role passed to the sidebar. `components/portal/company-switcher.tsx` holds the Partner entry + sets the `portal_mode` cookie on select (selecting a company drops out of partner mode); `components/portal/portal-sidebar.tsx` renders this one switcher at the top for dual-role in BOTH views (and suppresses the buried duplicate); the dual-role switcher passes `partnerHref="/portal/partner/referrals"` so the **"Partner — My Referrals"** entry actually lands on My Referrals (the component default `partnerHref` is `/portal/partner/clients`, which mismatched the label — fixed 2026-06-26 during sandbox QA). `app/portal/page.tsx` redirects to the partner section only when mode='partner'. Auralba keeps `portal_role='client'` (base identity). NOTE: 'View as partner' (CRM Partners page) lands on this dual-role portal. PRIOR same chain: partner-portal page **My Referrals** `/portal/partner/referrals` (partner role only; nav added to `portal-sidebar.tsx` partnerItems). Lists the partner's referrals from their OFFERS with a CRM-derived progress stepper for the ONE-TIME acquisition funnel (Call done → Offer sent → Client signed → Client paid); **annual renewals are shown SEPARATELY** as a recurring section driven by the actual **installment invoices** (R106 billing cycle, NOT a funnel stage). Per renewal year (after formation), each installment (1 Jan / 2 Jun) shows `Invoice issued → Paid` from `payments` (payment_category `installment_1`/`installment_2`, year from the `annual-installment:ACCT:N:YEAR` key) and the partner's OWN amount only — never the client's invoice amount (Antonio 2026-06-26). The agreed renewal amount is paid IN FULL on **each installment** (NO split, NO %; Antonio sets the amount per referral → two payouts/year); each is a separate payout (reference `renewal:<acct>:<year>:<n>`) created when its installment is paid (`onFirstInstallmentPaid`→#1, `onSecondInstallmentPaid`→#2) and self-serve **requestable** then. Setup payout still shows its own request form (USD bank details + optional invoice upload). Full design in `partners-team.md`. SANDBOX, branch `feat/partner-deal-config`.)_
_Latest 2026-06-26 — Claude (**Suspend client portal login** (admin Suspend / Unsuspend), branch `feature/portal-suspend-access`, SHIPPED. New `suspend`/`unsuspend` actions in `app/api/crm/admin-actions/contact-portal/route.ts` ban/unban the contact's auth user (`ban_duration` `'876600h'`/`'none'`), set `app_metadata.suspended`, send the client a bilingual notice on BOTH suspend & restore, and write `action_log`. Admin UI = red **Suspend Login** / green **Unsuspend Login** in the contact Portal tab `PortalTab` (`components/contacts/contact-detail.tsx`); the confirm dialog lists every linked company (the blast radius), and the Portal Status "Account" row shows red **Suspended** when blocked (from `portalAuth.suspended`, computed from `banned_until` in `app/(dashboard)/contacts/[id]/page.tsx`). **Suspend is LOGIN-level** — one auth user per email, so it blocks the person across ALL their companies; it is NOT a tier change and NOT an `accounts.status` change (that enum's own `Suspended` value is unrelated CRM lifecycle). `restore_access` (per-company membership restore) is GUARDED — it will NOT lift a deliberate suspension (`app_metadata.suspended===true`), so restoring one company can't silently un-suspend the login. Login-side enforcement (instant kick-out + message) lives in `middleware.ts` + `app/portal/login/page.tsx` — see auth-oauth.md. Verified live in sandbox end-to-end (normal client unaffected; suspend→blocked+message→unsuspend→restored; both via the real dashboard buttons + audit log). **Follow-up (same day):** the contact **header** badge (next to the name, `contact-detail.tsx`) now shows red **Suspended** in place of the green tier badge when `portalAuth.suspended` — tier is intentionally unchanged by suspend, so the header previously kept showing "active" after a suspension, which read as a bug.)_
_Latest 2026-06-26 — Claude (**MMLLC formation wizard now collects the SS-4 Responsible Party (signer)**, branch `feat/mmllc-workspace`, SANDBOX. `app/portal/wizard/wizard-client.tsx` adds a `SignerRadio` on the owner step ("I will be the SS-4 Responsible Party") and one per additional member on the members step; all share a single `signerIndex` state (-1=owner, 0+=member) so **exactly one** is selectable. On change it stamps flat `owner_is_signer` / `member_{i}_is_signer` booleans into formData, which persist verbatim into `wizard_progress.data` + `formation_submissions.submitted_data` (wizard-submit writes `data` untouched; the formation validator ignores extra keys). Submit is gated (MMLLC only) on exactly one signer chosen AND additional members' ownership summing to >0% and <100% (owner takes 100−Σ at materialization). Removing the selected member clears the choice. Entirely inert for SMLLC / non-formation wizards (the members step + owner radio only render when `entityType==='MMLLC'`). Downstream: `extractMembersFromWizardData` now surfaces `is_signer`, `formation-materialize.ts` writes it to `members.is_signer` (owner from `owner_is_signer`, each member from its flag) instead of hardcoded false, and the workspace `members_panel` displays/overrides it (see flows.md). **Server-side backstop (added 2026-06-26):** `validateFormationData` (`lib/jobs/validation.ts`) now ALSO enforces the same two rules when called with an MMLLC `entityType` — exactly one signer across `owner_is_signer` + `member_{i}_is_signer`, and additional members' ownership summing to >0% and <100% — returning field-level errors (`is_signer` / `member_ownership_pct`) that `/api/portal/wizard-submit` surfaces as a 400 so a direct API hit that bypasses the client toast is still rejected. Gated on the entityType param the wizard-submit route passes; the background `formation-setup` re-validation calls it WITHOUT entityType, so legacy in-flight MMLLC jobs are unaffected. NOT on production — Antonio ships.)_
_Last verified against code: 2026-06-25c — Claude (Referral page `/portal/referrals` now surfaces a contact's PERSONAL referrals PLUS referrals earned by a company where they are the **primary** contact (sole member ⇒ primary; leak-safe for multi-member LLCs), and renders "earned" **per-currency** (USD `$` + EUR `€` coexist). Pure logic in `lib/portal/referral-aggregate.ts`; files `app/portal/referrals/page.tsx`, `components/portal/referral-page.tsx`. Full design + the paired activation auto-credit change are documented in `referrals-circleback.md`. Branch `claude/sweet-albattani-8859f9`, SANDBOX.)_
_Last verified against code: 2026-06-16 — Claude (WIZARD SUBMIT IS RESILIENT TO LOST RESPONSES — Luca/Dynamiq hit "Submission failed" on the MMLLC wizard even though the server SUCCEEDED (wizard_progress=submitted, submission=completed, job=completed, CSVs ingested). Root cause: the submit returns 200 fast (async), but a transient delivery failure — a lost 200 OR a one-off empty-body 500 (reproduced once against the live endpoint) — made the client show a hard "Submission failed", leaving isSubmitted=false so the success screen + its P&L/Balance-Sheet link never rendered → the user was STRANDED with no path to the next page despite the submission going through. Fix (wizard-client.tsx onSubmit): idempotent retry loop (up to 3 attempts, 700ms·attempt backoff). The route already marks wizard_progress 'submitted' before anything and returns {success,'Already submitted'} on a repeat WITHOUT re-enqueuing (verified: 3× allow_resubmit=false → 200 'Already submitted', no duplicate job), so retrying a lost/5xx response is safe — attempt 1 carries the caller's allow_resubmit, retries force allow_resubmit=false to hit the dedup. A real validation error (400+fields) is NEVER retried (shows field errors immediately). After 3 genuine failures, the toast tells the user to refresh and check rather than implying lost work. NOTE: the wizard link is session-scoped (resolves the account from the logged-in user, not the URL) — to test Dynamiq, log in as worldresltd@gmail.com, not uxio74.)_
_Prior: 2026-06-12 — Claude (WIZARD SUBMIT IS NOW TRULY ASYNC, Antonio's report "submission failed": the tax_form_setup job used to run INLINE in /api/portal/wizard-submit, holding the browser response 15-45s+; on Vercel both of his attempts died ("No response is returned from route handler") while every server-side step had succeeded — data saved, job completed, SD advanced — so the client saw failure and resubmitted, duplicating review tasks. Fix: the inline claim+execute block is REMOVED; the route returns right after enqueue (~1.6s measured locally, was 41s) and the job runs in /api/jobs/process via enqueueJob's existing fire-and-forget trigger (~1s pickup) with the 5-min process-jobs cron as safety net — the same path formation/onboarding always used. Success screen's "we are preparing your P&L" copy covers the async window; the financials page recomputes on load.)_
_Prior: 2026-06-12 — Claude (MMLLC tax wizard §14 redesign: 6 steps with a REAL members step (the old config never rendered members — machinery keys on step id 'members'); members pre-filled from CRM in wizard page.tsx ("confirm, don't type"); members-repeater remove now shifts later members down + writes member_count; every field carries plain-language hint EN/IT. See tax-returns.md §14 entry for the full redesign.)_
_Prior: 2026-06-12 — Claude (WIZARD CONDITIONAL CHAIN FIX, Antonio's QA repro: a conditional field is now visible only if its WHOLE ancestor chain holds (isFieldVisible, module-level in wizard-client.tsx, used by BOTH validateStep and the render filter). Before: child checked only its direct parent — crypto=Yes → 1099=No (stored in draft) → crypto flipped to No left the required CSV upload demanded by an invisible chain and blocked Next. Verified live: stale-child state reproduced, flip to No → all three descendants vanish, Next advances.)_
_Prior: 2026-06-11 — Claude (Slice 9: tax-financials review screen gains the YEAR-COVERAGE section (per-bank month-gap questions, two-button answers, 'had_activity' guides to replace the file; attest button blocked while any coverage question is open) and the TaxBanner shows 'Check and confirm your Profit & Loss and Balance Sheet →' linking /portal/tax-financials for MMLLC/Corp returns with a submission (showFinancialsLink prop set in app/portal/page.tsx). See tax-returns.md Slice 9 for the engine + handoff.)_
_Prior: 2026-06-11 — Claude (Tax financials review screen, Slice 8: NEW /portal/tax-financials (server page resolves account via portal_account_id cookie + open tax year from tax_returns data_received=false) + components/portal/tax-financials-review.tsx (client: gates checkmarks, P&L/BS cards, K-1 member table, question groups with plain-language answer chips EN/IT, file cards with delete, Excel download, attestation checkbox+button hard-blocked by canConfirm). All API routes OWNER-ONLY (lib/portal/owner-access.ts) — tax financials are non-delegable to portal teammates. R099 error surfacing on every fetch. See tax-returns.md Slices 7-8 for the engine + routes.)_
_Prior: 2026-06-11 — Claude (Tax wizard prior-return matrix, Slice 6 master plan §5: MMLLC docs step replaces the optional prior_year_return file with a required `prior_return_case` select (we_filed / filed_elsewhere / first_year / never_filed) + conditional fields: PDF-only upload for filed_elsewhere (server guard in both wizard upload routes), back-filing interest select + under-responsibility declaration checkbox for never_filed (declaration wording PENDING Antonio/lawyer review). Case A is preselected by server prefill when tax_returns has 'TR Filed' at year-1; the submit route re-verifies the claim and fire-and-forgets lib/tax/prior-return-case.ts::processPriorReturnCase — see tax-returns.md for the full §5 pipeline.)_
_Prior: 2026-06-11 — Claude (Wizard AUTOSAVE: debounced 2.5s silent save on any change (typed fields, repeater rows, uploaded file paths — everything funnels through handleFieldChange's dirty flag), single-flight guard, failed silent saves re-mark dirty and retry on next edit, 'Saved HH:MM' indicator in WizardShell (autosaveStatus prop). Manual Save draft unchanged (loud). Step-change fire-and-forget save pre-existed. Closes the lost-upload-reference gap found in Slice 1 QA.)_
_Prior: 2026-06-11 — Claude (Tax wizard Slice 1, master plan b2115fd3: MMLLC/Corp documents step now uses CSV-ONLY per-bank repeater `bank_accounts` (free-text bank name + account label + checking/credit-card type + .csv-only upload, gift-framing copy EN/IT) replacing the single `bank_statements` field — legacy drafts still read. Wizard repeater infra fixed: file sub-fields get the upload callback (silently stored filenames before), remove-row reindexes (orphaned higher rows before), `repeaterRequired` step gating, hint rendering. CSV-only server guard on both wizard upload routes for `bank_accounts_*_statements`.)_
_Prior: 2026-06-11 — Claude (wizard field UX: per-field help is now ALWAYS visible (the click-to-reveal "i" tooltip in `wizard-field.tsx` was removed — clients never clicked it); new `warningOnValue` shows an amber callout on a specific answer; a required `repeater` with a `conditional` is gated + enforced by `validateStep`. Powers the SMLLC tax intake — full writeup in `tax-returns.md`. PRs #106/#107.)_
_Last verified against code: 2026-06-15 — Claude (View-as no longer counts as the client's login. The entry route (`app/portal/view-as/route.ts`) captures `auth.users.last_sign_in_at` BEFORE minting the client session and restores it AFTER via the SECURITY-DEFINER fn `public.viewas_restore_last_sign_in(uuid, timestamptz)` (self-guarded to service_role; migration `20260615-2100`). Before this, View-as's verifyOtp stamped last_sign_in_at = now(), which polluted "Last login" AND wrongly HID the "Resend Welcome" credentials button on the contact page — the gate is `showResendWelcome = portalAuth.exists && !portalAuth.lastLogin` (`contact-detail.tsx`), so a view-as-stamped login made the system think the client had logged in. Surfaced by Marco Barbiero "can't access portal"; his polluted timestamp was corrected back to null.)_
_Last verified against code: 2026-06-16 — Claude (ITIN "Client Signing" card on the flow detail page now has a shipping-tracking form (`components/portal/itin-shipping-form.tsx`) below the document downloads — courier dropdown (FedEx/DHL/UPS/USPS/Other) + tracking number → POST `app/api/portal/flows/[id]/shipping`. Once saved it collapses to a read-only summary (courier + tracking + a courier tracking link via `lib/flows/courier.ts`) with Edit. Shown only when the SD is AT Client Signing (`shippingForm` set on the `ShippingCard` only then). Bilingual EN/IT. Staff see the same data via the `shipping_info` workspace component.)_
_Last verified against code: 2026-06-23 — Claude (international office clock: `components/portal/office-clock.tsx` mounted in the portal top bar on every page (client + teammate branches of `app/portal/layout.tsx`) + holiday-aware `lib/portal/office-hours.ts` (`getOfficeStatus`/`usFederalHolidays`/`isOfficeHoliday`); live-chat/support hours = Mon–Fri 9 AM–3 PM ET excl. US federal holidays — see "Office hours / international clock" under Chat/notifications. Office-open 8 AM ≠ chat-open 9 AM, intentional.)_
_Last verified against code: 2026-06-23 — Claude (office clock "YOUR TIME" now reads the **client's** timezone from `contacts.address_country` (new `lib/portal/client-timezone.ts::countryToTimeZone` — normalized free-text + alias table for spellings/casing/accents; multi-tz country → main tz; empty/unknown → null → device/browser fallback). `app/portal/layout.tsx` resolves the viewed contact's country server-side and passes `clientTimeZone`/`clientTimeZoneLabel` to `OfficeClock`. Works under View-as because the View-as route mints the client's OWN session, so `getClientContactId` = the viewed client. Teammate branch keeps the device fallback. See "Office hours / international clock" under Chat/notifications.)_
_Last verified against code: 2026-06-25 — Claude (ITIN visibility for formation-tier clients + wizard cookie-trust fix, branch `fix/itin-workspace`, SANDBOX. **(1) ITIN shows alongside FormationDashboard.** A formation-tier client who also has a contact-scoped ITIN never saw it: `app/portal/page.tsx` returns `<FormationDashboard>` early in all four formation branches, before the account/lead "Service Status" sections. New `components/portal/flow-status-section.tsx` (`PortalFlowStatusSection`, extracted from the existing no-account Service-Status markup) is now rendered ABOVE FormationDashboard in every formation return, fed by a `loadItinFlows()` helper (`getPortalFlows('', locale, contactId)` filtered to `flow_type==='ITIN'`). Verified in the running app: "SERVICE STATUS / ITIN 2026 / Active" renders above "We're forming your LLC". **(2) `/portal/wizard` no longer resolves to the wrong (tax) wizard via a stale cookie.** `app/portal/wizard/page.tsx` initialized `accountId = cookieAccountId || ''` and only overrode it when the contact had `account_contacts` links — so a logged-in contact with NO account link (an in-progress-formation / ITIN-only client) kept a stale `portal_account_id` cookie's FOREIGN account, then resolved the wizard type from THAT account's service deliveries. A Tax Return SD on the unrelated account → the 4-step tax wizard ("Financial Information") for a client with no tax return (Daniel Pasztor symptom). Fix: `accountId = contactId ? '' : (cookieAccountId || '')` and the cookie now only SELECTS among `account_contacts`-verified owned accounts — never grants an unowned one. Reproduced + fixed against the sandbox "Daniel Pasztor QA" fixture: pre-fix wizardType=`tax` (foreign accountId); post-fix wizardType=`formation` with `itin` as the second option, `accountId=''`. **Caveat:** the sandbox repro surfaced the cookie-trust path; if a client legitimately OWNS an account that carries a Tax Return SD, the contact-scoped ITIN is still invisible to the account-scoped wizard query (only Closure is unioned today via `getFlexibleServiceTypes()`) — flagged, not changed. Sandbox only; not on production.)_
_Prior: 2026-06-24 — Claude (PER-COMPANY CHAT SCOPING, branch `feat/portal-chat-scoping`. Multi-company clients previously saw ONE merged chat (the hook threaded by `contact_id` only — PR 2 Step 6); switching companies in the sidebar did nothing. Now the chat is scoped to the selected entity. **Privacy boundary (the whole point):** a company thread is shared by everyone linked to that account (members + chat teammates), so it shows ONLY `account_id=X` messages. A contact's personal/untagged (`account_id IS NULL`) messages ride along in a company view ONLY when that account is **sole-owned by the viewer** — exactly one linked contact == them — decided SERVER-SIDE via `mayIncludePersonalNull` (structural; NOT the messy free-text `account_contacts.role`, since 61 MMLLC accounts carry an `owner`-role contact and would leak). So: SMLLC (sole) view = account + personal; MMLLC view = account only, never personal; formation/no-account view = `account_id IS NULL AND contact_id=me`; clients with no sole-owned company get a synthetic **Personal** switcher entry. **Pieces:** pure `lib/portal/chat-scope.ts` (`mayIncludePersonalNull`, `buildChatQueryPlan`, `messageVisibleInPlan` — unit-tested) + server `lib/portal/chat-scope-server.ts` (`resolvePersonalNullInclusion`); GET/read `app/api/portal/chat/route.ts` + `/read/route.ts` gain `scope=company|personal` (absent ⇒ legacy/admin path UNCHANGED — admin CRM `portal-chats` untouched); `lib/hooks/use-portal-chat.ts` takes a `ChatScope`, subscribes broad (account_id and/or contact_id) then drops cross-company rows via `messageVisibleInPlan`; `lib/portal/queries.ts::getChatEntities` builds the chooser list; `app/portal/chat/page.tsx` resolves the selected entity from the `portal_account_id`/`portal_formation` cookies (+ `'personal'` sentinel) and passes a `scope`; `components/portal/portal-chat.tsx` replaces the old Person/Company toggle with a first-send **popup** ("Which company is this message about?", multi-entity only, warns when the company is shared) + a prominent always-visible **banner** at the top of the chat card ("CHATTING ABOUT / <entity>" + Switch button; accent color by entity kind — blue company / amber formation / zinc personal; replaced the original small bottom pill per Antonio's UI feedback); view-follows-choice (popup switches the cookie). Single-entity clients auto-tag, no popup. Verified in sandbox by seeding Michele Cotti (Whalecot SMLLC sole-owned + Conversion Monsters MMLLC 2-member + personal NULLs): SMLLC view=8 (5 company+3 personal, no CM leak), MMLLC view=6 (4 his+2 other member, ZERO personal leak), personal view=3; build + 3660 unit tests green. **Known follow-up (out of scope, flagged):** the sidebar unread badge + MMLLC shared-thread `read_at` are still single-valued/contact-keyed — a member sees shared messages in the thread but the badge/read-state aren't per-member yet. Sandbox only.)_
_Prior: 2026-06-23 — Claude (per-company scoping of action items + banners, branch `fix/portal-entity-scoping`, dev_task bb54680b. Multi-company clients (or an active company + a new in-progress formation) saw one context's items/banners leak into all views. **(1) Action items** — `getPortalActionItems` (`lib/portal/queries.ts`) queried in-progress + submitted wizards with `account_id=X OR contact_id=Y`, so a contact-scoped Company Formation wizard (`account_id NULL`, for a NEW company) and wizards from the contact's OTHER accounts surfaced under the selected company (e.g. "Complete Formation Form" under Scaledge). Both wizard sub-queries are now scoped to `account_id = selected account` only; the formation/no-account view keeps its own contact-scoped path (`getPortalActionItemsByContact`). The per-member OA-signature `contact_id` check and the intentional contact-scoped Company Closure surfacing are unchanged. **(2) "Set up your new company" banner** — `formationInProgress` in `app/portal/page.tsx` was queried by `user.email` on an offer left at `account_id NULL`, so it fired on every active company and lingered after the company was formed (showed under Whalecot). REMOVED from the active dashboard — the in-progress formation is now a switchable entity (company switcher → `FormationDashboard` with the Continue-Setup CTA), the correct per-entity path. The `FormationInProgressBanner` component file is now unused (dead). **(3) Stale tax card** — the "Complete Tax Information — <year>" item was gated only on `tax_returns.data_received=false`, a flag flipped by a downstream job (`tax-form-setup`) that can lag/fail, so the card persisted for months after submission. Now also suppressed YEAR-ACCURATELY when a `tax_return_submissions` row exists for the account + `tax_year` (a prior year must not suppress the current year). **(4) Service Status flow leak** — found during browser QA: `getPortalFlows` merges `CONTACT_FLOW_TYPES = ['ITIN','Company Formation']` (contact-scoped, `account_id NULL`) into every account's Service Status, so a new company's in-progress formation showed as a "COMPANY FORMATION <year>" stepper under an unrelated active company (same leak class as #1, pre-existing). Fixed: `getPortalFlows` now EXCLUDES contact-scoped Company Formation when a real `accountId` is selected (formation is a separate switchable entity → `FormationDashboard`); ITIN still rides along; no-account/ITIN-only callers (`accountId===''`) unaffected. Browser-verified on the sandbox production deploy (`td-operations-sandbox.vercel.app`) with a seeded multi-company + in-progress-formation contact (Matteo Angioni): action items "All caught up" on the active company, no formation banner, formation context renders its own tracker, and Uxio Test LLC's stale tax card is gone. Sandbox only.)_
_Prior: 2026-06-23 — Claude (office clock "YOUR TIME" now reads the **client's** timezone from `contacts.address_country` (new `lib/portal/client-timezone.ts::countryToTimeZone` — normalized free-text + alias table for spellings/casing/accents; multi-tz country → main tz; empty/unknown → null → device/browser fallback). `app/portal/layout.tsx` resolves the viewed contact's country server-side and passes `clientTimeZone`/`clientTimeZoneLabel` to `OfficeClock`. _Also 2026-06-23 — Claude (Formation "Filed with State" copy + date, branch `fix/portal-filing-banner`. **(1) Waiting banner reworded.** The blue waiting card (only shown at SD stage "Filed with State") said "Your LLC is being filed / we submitted your Articles" — present tense, but it's ALREADY filed and we're waiting on the state to confirm. Now: "Your LLC has been filed with the Secretary of State on {date}. Processing time varies by state — typically 1–4 weeks. We'll update you as soon as the state confirms." (EN + IT; date interpolated into the bold title via `waitingStateTitleDated`, falls back to the undated title when unknown). **(2) Filing date in the progress tracker.** The "Filing with the state" step (stage "Filed with State") now shows "· Filed Jun 18, 2026" once reached. Date source = new `filedAt` returned by `getFormationTracker` (`lib/portal/queries.ts`): prefers the durable `stage_history` `to_stage='Filed with State'` `advanced_at` (survives advancing past the stage), falls back to `stage_entered_at` only while AT that stage, else null (legacy SDs with no recorded transition show no date — graceful). `filedAt` threaded → `buildFormationTrackerSteps(…, filedAt)` attaches it to the Filed-with-State step (completed/current only) → `formation-progress-tracker.tsx` renders the compact locale date; also passed to `formation-dashboard.tsx` for the banner. All 4 FormationDashboard render sites in `app/portal/page.tsx` forward `filedAt={tracker?.filedAt}`. Sandbox only.)_
_Also 2026-06-23 — Claude (international office clock: `components/portal/office-clock.tsx` mounted in the portal top bar on every page (client + teammate branches of `app/portal/layout.tsx`) + holiday-aware `lib/portal/office-hours.ts` (`getOfficeStatus`/`usFederalHolidays`/`isOfficeHoliday`); live-chat/support hours = Mon–Fri 9 AM–3 PM ET excl. US federal holidays — see "Office hours / international clock" under Chat/notifications. Office-open 8 AM ≠ chat-open 9 AM, intentional.)_
_Also 2026-06-20 — Claude (Formation dashboard "Your LLC is being filed / we submitted your Articles" banner was PREMATURE. `formation-dashboard.tsx` computed `waitingForState = wizardSubmitted && !stateConfirmed`, so it fired at the "Wizard Submitted" stage — where we're still reviewing the LLC name and nothing has been filed yet. Now gated on the SD stage via the existing `sdStage` prop: `sdStage ? sdStage === 'Filed with State' && !stateConfirmed : <legacy>`. The legacy heuristic remains only as a fallback when no stage signal is available. Surfaced during a test-contact reset: a fresh "Wizard Submitted" client wrongly saw the Articles-filed banner. Also note for test resets: a raw `contacts.portal_tier` write does NOT update the auth user's `app_metadata.portal_tier` (only `syncTier` does both) — a stale `active` auth tier can leak Bank Applications / wrong routing; reset it via the auth admin API too.)_
_Last verified against code: 2026-06-20 — Claude (Formation QA, branch `fix/formation-workspace-bugs`. **(1) New `/portal/banks` "Bank Applications" page** (`app/portal/banks/page.tsx`) — self-service guidance to open a business bank account after formation completes: what you need (EIN + Articles), a prominent "never share your signed SS-4" warning, business-description guidance, and a curated provider list (Relay/Mercury/Sokin/Payset/Wise, edit `RECOMMENDED_BANKS`). Replaces the old Banking Fintech wizard/SD (removed — formation ends at the EIN). Added to the sidebar (`portal-sidebar.tsx`) under Company as `nav.bankApplications`, **active-tier only** (a formation-tier client with EIN pending can't open an account yet); the old `nav.wizard`→"Bank Applications" relabel was retired (it was Banking-SD-driven), so the wizard item is always "Complete Setup". **(2) "Complete Formation Details" CTA gating** — `formation-dashboard.tsx` takes a new `sdStage` prop (from `getFormationTracker.currentStage`, passed at all 4 render sites in `app/portal/page.tsx`); the wizard CTA hides once the formation SD is past "Payment Confirmed", fixing the card persisting when the `wizard_progress` row is contact-keyed (`account_id` NULL) and the account-scoped lookup returned nothing. **(3) Portal Documents** shows formation Articles + EIN letter but NEVER the signed SS-4 (internal). See formation.md + flows.md for the full set.)_
_Prior: 2026-06-19 — Claude (Formation dashboard routing no longer depends on the stale auth-metadata tier. The no-account branch of `app/portal/page.tsx` previously showed `FormationDashboard` only when `user.app_metadata.portal_tier === 'formation'`; if that auth tier was stale (e.g. left at `active` after a prior formation completed, or never set) the client fell through to the old `WelcomeDashboard` (Review Proposal → Sign Contract → Make Payment). Now the gate is `authTier === 'formation' OR an active contact-scoped Company Formation SD exists` — the SD (`getInProgressFormations(contactId)`, which short-circuits after 1 query when none) is the authoritative signal, so the 7-stage tracker shows at ANY formation stage including "Payment Confirmed". Surfaced live by Marinela Marku, whose auth tier was stuck at `active`. Reuses the contact-scoped FormationDashboard render path unchanged — only the gate condition broadened.)_
_Prior: 2026-06-18 — Claude (Pending Client Decision Requests now surface PORTAL-WIDE, not just on the flow detail page. `components/portal/pending-decisions.tsx` (mounted once in `app/portal/layout.tsx` above `{children}`, for any contact) fetches `GET /api/portal/my-decisions` and renders the client's pending requests as actionable `DecisionCard`s under an "Action required" heading; self-hiding (renders nothing when there are none). Fixes the case where a no-account / lead-or-formation client (e.g. Marinela Marku) received an LLC-name approval but had no path to the flow page where the card used to live exclusively. Tier-agnostic — works on every dashboard branch.)_
_Prior: 2026-06-18 — Claude (Client Decision Requests reach the client on the flow detail page. `app/portal/flows/[id]/page.tsx` loads the SD's `client_decision_requests` (newest first) and renders `components/portal/decision-card.tsx`: the newest pending one is actionable (approval Yes/No, choice radios, or text field; EN/IT via message_it), older ones collapsed read-only. Submits to `POST /api/portal/decisions/[id]/respond` (portal auth + ownership). Full system: client-decision-requests.md.)_
_Prior: 2026-06-17 — Claude (Company Formation portal tracker rewritten to read SD pipeline stages. `getFormationTracker()` (`lib/portal/queries.ts`) resolves the formation SD's current stage (cascading sdId → account-scoped → contact-scoped) + the 7 Company Formation `pipeline_stages` (client_label/client_label_it); the pure `buildFormationTrackerSteps` (`lib/portal/formation-progress.ts`) + `FormationProgressTracker` (`components/portal/formation-progress-tracker.tsx`) render the journey with an amber "Action required" glow on the CURRENT client-action stage (Payment Confirmed → wizard link, SS-4 Prepared → sign link), EN/IT. Wired into all 4 `FormationDashboard` render sites in `app/portal/page.tsx` via a new optional `trackerSteps` prop; the legacy signal-derived milestone list remains the fallback when no formation SD/stages resolve. Part of Workspace v2 — see flows.md/formation.md.)_
_Prior: 2026-06-17 — Claude (NEW staff page `/portal/team/approvals` (`app/portal/team/approvals/page.tsx` + `app/api/portal/team/approvals/route.ts` + `components/portal/approvals-list.tsx`): a phone-friendly list of the AI assistant's queued action-proposals with Approve/Reject. Staff-only — gated on a Supabase user whose `app_metadata.role !== 'client'` (same staff distinction as `lib/portal/team/gate.ts`); clients/teammates see "not available". Approve atomically flips `approval_queue` pending→approved (guarded on status='pending'; no 6-digit code — the per-card button on an authed staff session is the binding the code provides in chat); the async executor then runs it. No sidebar link yet — reachable by URL.)_
_Prior: 2026-06-17 — Claude (SMLLC tax wizard documents step — removed the "Bank Statements (optional)" + "Financial Statements (optional)" file uploads (Antonio). Single-member clients enter figures in the "Financial Information" step; both uploads were optional, never read by any code, pure confusion. Step now = prior-year return (optional) + confirm checkbox. Surgical single-member-only production ship; multi-member/Corp wizard untouched. See tax-returns.md for detail.)_
_Prior: 2026-06-16 — Claude (ITIN "Client Signing" card on the flow detail page now has a shipping-tracking form (`components/portal/itin-shipping-form.tsx`) below the document downloads — courier dropdown (FedEx/DHL/UPS/USPS/Other) + tracking number → POST `app/api/portal/flows/[id]/shipping`. Once saved it collapses to a read-only summary (courier + tracking + a courier tracking link via `lib/flows/courier.ts`) with Edit. Shown only when the SD is AT Client Signing (`shippingForm` set on the `ShippingCard` only then). Bilingual EN/IT. Staff see the same data via the `shipping_info` workspace component.)_
_Earlier 2026-06-16 — Claude (ITIN "Client Signing" card on the flow detail page now EMBEDS the actual prepared documents (W-7 / 1040-NR / Schedule OI) as download links, right under the mailing address + checklist — `buildItinShippingCard()` takes the SD's client-safe docs and `FlowStageJourney`'s `ShippingCard` renders them as `<a href="/api/portal/documents/[id]">` (auth-gated proxy, Content-Disposition: attachment). Replaces the old "listed under Documents below" text. The SD-linked portal-visible forms are exactly those three. NOTE: in sandbox Damiano's SD has no generated forms (service_delivery_id NULL on all his docs), so the embedded list is empty there until the forms are generated/seeded; in production they're SD-linked and render.)_
_Earlier 2026-06-16 — Claude (Flow DETAIL page (`app/portal/flows/[id]/page.tsx`) is now usable for ITIN. Two fixes: (1) the type gate accepted only `FLOW_TYPES`, so an ITIN flow returned `notFound()` on both envs — changed to `ALL_FLOW_TYPES`; (2) ITIN stages carry `client_description` (and, on prod, `client_label`), so the page now renders the clickable `FlowStageJourney` (vertical, each stage expands to its description) for contact flows, with a print/sign/mail card on the "Client Signing" stage — TD document-receiving office **Seminole, 11125 Park Blvd Suite 104-153** (NOT the Largo CMRA business address) + a 2× W-7 / 2× 1040-NR+OI / 2× passport-copy checklist. Account flows still use the horizontal `FlowProgressTracker`. The W-7/1040-NR/Schedule OI PDFs (portal_visible, SD-linked) show in the Documents section below.)_
_Earlier 2026-06-16 — Claude (ITIN-only clients (no account/LLC) now see their ITIN flow too. The no-account branch of `app/portal/page.tsx` (the WelcomeDashboard path) computes `getPortalFlows('', locale, contactId)` and renders a "Service Status" section above the welcome dashboard when the contact has contact-scoped flows. `getPortalFlows` now SKIPS the account query when `accountId` is falsy (passing `''` would otherwise be an invalid-uuid error on `.eq('account_id','')`), so it safely returns only the contact-scoped (ITIN) flows. Covers the formation-tier early-return path NO (an ITIN-only client isn't formation tier). FORWARD-LOOKING: sandbox currently has no ITIN contact lacking an Active/Suspended account, so verified by build + mechanism, not live data.)_
_Earlier 2026-06-16 — Claude (Portal "Service Status" now surfaces contact-scoped ITIN flows. `getPortalFlows(accountId, locale, contactId?)` gained a `contactId` param and runs a SECOND query for contact-scoped SDs (`contact_id = contactId AND account_id IS NULL`, filtered by `CONTACT_FLOW_TYPES = ['ITIN']` — NOT `FLOW_TYPES`, which stays account-only by design) merged with the account-scoped results. `app/portal/page.tsx` passes the already-resolved `contactId`. ITIN stages currently have no `client_label`, so ITIN renders as the neutral "Active" card (links to `/portal/flows/[id]`) rather than a stepper until labels are added. NOTE: only reaches clients who have an active account (no-account ITIN-only clients are routed to the WelcomeDashboard before this section). Did NOT add ITIN to `FLOW_TYPES` (the literal task asked to) — used the existing `CONTACT_FLOW_TYPES` separation instead.)_
_Earlier 2026-06-15 — Claude (View-as no longer counts as the client's login. The entry route (`app/portal/view-as/route.ts`) captures `auth.users.last_sign_in_at` BEFORE minting the client session and restores it AFTER via the SECURITY-DEFINER fn `public.viewas_restore_last_sign_in(uuid, timestamptz)` (self-guarded to service_role; migration `20260615-2100`). Before this, View-as's verifyOtp stamped last_sign_in_at = now(), which polluted "Last login" AND wrongly HID the "Resend Welcome" credentials button on the contact page — the gate is `showResendWelcome = portalAuth.exists && !portalAuth.lastLogin` (`contact-detail.tsx`), so a view-as-stamped login made the system think the client had logged in. Surfaced by Marco Barbiero "can't access portal"; his polluted timestamp was corrected back to null.)_
_Last verified against code: 2026-06-15 — Claude (Portal Addresses page: new `app/portal/addresses/page.tsx` (+ `nav.addresses` sidebar item under My Company, EN/IT i18n) shows three read-only cards: (1) Tony Durante's mailing address — the TD-provided `business_mailing` row from the `addresses` table (`is_td_provided=true`); (2) the account's Registered Agent (`accounts.registered_agent_address` + `registered_agent_provider`); (3) the account's Mailing/CMRA address (the FK-joined `business_mailing_address_id` → `addresses`). Uses `formatAddressString` from `lib/addresses`. Account-scoped (contact → their accounts; teammate → granted account). The CRM-side `/addresses` CRUD + `/api/addresses` + the `addresses` table already existed — this only adds the client-facing read view.)_
_Prior: 2026-06-14 — Claude (Sign page shows ONLY actionable items: `sign-documents-client.tsx` now renders only non-signed docs — the "Already signed" collapsed section was REMOVED (signed docs hidden entirely). AND `page.tsx` no longer surfaces **`draft`/`voided` OA & Lease**: an OA/Lease is created `draft` and only becomes `sent` when sent for signature (`lib/mcp/tools/oa.ts`), so a draft was never sent to the client and must not appear as "awaiting" (the prior `status==='signed' ? 'signed' : 'awaiting'` wrongly treated unsent drafts as pending — the real cause of "the sign page shows everything"). NOTE: MSA (`annual_agreements`) has no `sent` state — a draft MSA IS the pending state — so MSA draft is NOT excluded. Verified vs sandbox: Profit Harbor's draft OA/Lease excluded, MSA + signed items hidden, leaving only the pending Tax Return signature_request.)_
_Prior: 2026-06-14 — Claude (Sign page grouped pending vs signed: `app/portal/sign/sign-documents-client.tsx` now splits the documents list into a prominent **"To sign"** section at top (status ≠ signed — OA/Lease/SS-4/MSA/8832/generic signature_requests incl. the flow Tax Return) and a **collapsed "Already signed"** `<details>` below, so a client arriving from "Sign your tax return" sees only what needs signing. Card markup extracted into a `DocCard` component reused by both sections; React key changed from `doc.type` to `doc.href` (fixes a key collision when multiple generic signature_requests exist). The server page (`page.tsx`) is unchanged — it still gathers the full list; the grouping is presentation-only.)_
_Prior: 2026-06-14 — Claude (Portal tax banner recognises flow-workspace SD stages: `app/portal/page.tsx` now passes `tr.sd_stage` into `TaxBanner`, which maps it via `lib/tax/flow-banner.ts::flowStageBannerState` to the right client action — "Sign your tax return" (→ `/portal/sign`) at "Sent for Signature"/"Tax Return Prepared", "Complete your tax form" at "Wizard Available", "Under review" (no button) at "Data Submitted"/"Under Review", "Changes requested — edit your submission" at "Revision Requested", and Signed/Filed/Completed status. **Precedence:** `review_status` block still runs FIRST (production unaffected); the SD-stage block runs only when there's no `review_status`, before the legacy "Edit submission" fallback. Fixes flow Tax Returns wrongly showing "Edit submission" at later stages. CAVEAT: at "Tax Return Prepared" the signature request isn't created until staff click Send for Signature, so /portal/sign may be empty until the SD reaches "Sent for Signature".)_
_Prior: 2026-06-14 — Claude (Password show/hide toggle on the portal login (`app/portal/login/page.tsx`): a `showPassword` state flips the field between `type="password"`/`"text"` with an `Eye`/`EyeOff` (lucide) button positioned inside the input; `type="button"` + `tabIndex={-1}` so it never submits the form or steals tab focus. Same toggle added to the CRM login (`app/login/page.tsx`). Pure UI, no auth-flow change.)_
_Earlier: 2026-06-14 — Claude (Per-stage custom client email: `notifyClientOfStageAdvance` (`lib/portal/notifications.ts`) now accepts an optional `custom_message`; the new pure, unit-tested `buildStageAdvanceCopy` helper uses it as the email headline (dropping the generic secondary line) or falls back to the standard bilingual "service moved to stage" copy. `advanceServiceDelivery` (`lib/service-delivery.ts`, step 8b) passes the target stage's new `pipeline_stages.client_notification_message` column (migration `20260614-2000`, sandbox-only until promoted — same casting pattern as `notify_client_email`). Set on Tax Return "Wizard Available" so reaching it emails the client to submit their tax info. Override is locale-agnostic (one text for all languages) — an `_it` variant is a future follow-up if Italian clients need a translated message.)_
_Earlier 2026-06-14 — Claude (Client flow detail page: new `app/portal/flows/[id]/page.tsx` ([id]=service_delivery_id) — a read-only per-flow view showing the full progress stepper (`FlowProgressTracker` over `buildFlowSteps`), the flow's curated client-safe documents (`isClientSafeFlowDoc`), and the flow's chat messages (read-only, soft-deleted + `<!-- chat-event:` notes excluded, link to `/portal/chat`). Strictly access-gated: the SD must belong to an account the client can access (`getClientAccountIds`) or be the contact's own contact-scoped SD; teammates need account-scope + `documents`; otherwise `notFound()`. The home "Service Status" cards now link here (`FlowProgressTracker` gained an optional `href`; the neutral CMRA "Active" card is a `Link`).)_
_Earlier 2026-06-14 — Claude (Curated flow-document visibility: the per-flow documents grouping on `app/portal/documents/page.tsx` no longer filters `portal_visible=true` in SQL — flow docs are written `portal_visible=false` by default, so that gate showed nothing. It now applies `isClientSafeFlowDoc(serviceType, flow_stage, portal_visible)` (`lib/flows/flow-doc-visibility.ts`, pure + unit-tested): an admin-published doc (`portal_visible=true`) is always shown; otherwise the doc's `flow_stage` must be in a per-service-type CURATED allowlist of client-safe stages (receipts / confirmations / signed outputs). The **unsigned "Tax Return Prepared" draft is intentionally excluded** so it never leaks to the client; the signed return reaches them via the e-signature flow. The allowlist in that file is the single curation point — add a stage there to expose its docs.)_
_Earlier 2026-06-14 — Claude (Client-facing flow progress: the active dashboard (`app/portal/page.tsx`) now renders a **"Service Status"** card driven by `getPortalFlows(accountId, locale)` in `lib/portal/queries.ts` — the account's ACTIVE `service_deliveries` of the four recurring flow types (Tax Return / State Annual Report / State RA Renewal / CMRA), each rendered as a **visual progress stepper** (`components/portal/flow-progress-tracker.tsx`, a generalisation of `tax-progress-tracker.tsx`) — title (`buildFlowTopic`, e.g. "Tax Return 2025") + a dot-per-stage row with the current stage highlighted and stage labels. Steps come from the pure `lib/flows/flow-progress.ts::buildFlowSteps` (labelled-stage membership + completed/current/future state, current = highest labelled stage ≤ the SD's stage; catalog `icon` carried through); `computeFlowProgress` remains for the count/label. A flow with no client-labelled stages (CMRA) shows a neutral "Active"/"Attivo" card. Tax Return is dropped from Service Status when the dedicated Slice-5 `TaxProgressTracker` is already showing (no double stepper). Stage labels read `client_label`/`client_label_it`. The **documents page** (`app/portal/documents/page.tsx`) now groups docs stamped with `documents.service_delivery_id` into per-flow sections above Company/My (queried independently of category, then de-duplicated out of Company/My; `service_delivery_id` is missing from generated Supabase types so those queries cast through `unknown`/`as any`).)_
_Last verified against code: 2026-06-15 — Claude (Admin read-only "View as client": an admin opens a client's portal exactly as the client sees it, locked read-only, to reproduce "I don't see the wizard"/visual bugs. Button on the account + contact detail pages (`components/accounts/view-as-client-button.tsx`), rendered only when `isAdmin(user)` AND the contact has a portal login. Flow: `POST /api/admin/view-as` (admin-gated) mints a short-lived HMAC token (`lib/portal/view-as.ts`); `GET /portal/view-as?t=` verifies it, mints the CLIENT's session server-side via `generateLink`+`verifyOtp` (no email sent), sets the session cookies + a signed `td_view_as` marker cookie on the portal domain, audits to `action_log`, redirects to `/portal`. While the marker is present, `middleware.ts` BLOCKS every mutating request (POST/PUT/PATCH/DELETE) to `/portal` + `/api/portal` with 403 — the single read-only lock (server actions + API writes; the wizard storage upload is gated because its slot-mint is a POST). `ViewAsBanner` shows a persistent red "READ ONLY — Exit" bar (`app/portal/layout.tsx`). `GET /portal/view-as/exit` signs out **local-scope only** (never global — global would log the real client out of all their own devices) + clears the marker + audits. Relies on portal & CRM being different domains in prod (separate cookie jars) so the admin's CRM session is untouched; in single-domain sandbox the dual-login helper `scripts/sandbox-dual-login.sh` works around it. Admin-only, fully audited; not sabotage-proof against the admin who holds the button — prevents accidental writes, not deliberate cookie tampering.)_
_Prior: 2026-06-14 — Claude (Password show/hide toggle on the portal login (`app/portal/login/page.tsx`): a `showPassword` state flips the field between `type="password"`/`"text"` with an `Eye`/`EyeOff` (lucide) button positioned inside the input; `type="button"` + `tabIndex={-1}` so it never submits the form or steals tab focus. Same toggle added to the CRM login (`app/login/page.tsx`). _Prior: 2026-06-11 — Claude (wizard field UX: per-field help is now ALWAYS visible (the click-to-reveal "i" tooltip in `wizard-field.tsx` was removed — clients never clicked it); new `warningOnValue` shows an amber callout on a specific answer; a required `repeater` with a `conditional` is gated + enforced by `validateStep`. Powers the SMLLC tax intake — full writeup in `tax-returns.md`. PRs #106/#107.)_
_Prior: 2026-06-11 — Claude (new-document alerts unified: dispatch moved to the `updateDocument` choke point so ALL share paths alert (toggle, process-and-share, signature webhook); chat-on-share flag `new_document_chat_message_enabled` default ON; portal-digest renders document file names (lib/portal/digest-render.ts, `show_body` + `portal_digest_type_labels` override) and marks unresolvable recipients' notifications instead of recycling them every 5 min — skip counts in cron_log.)_
_Prior: 2026-06-10 — Claude (contact reuse in autoCreatePortalUser/createFromEmail is now case-insensitive + alias-aware via findContactIdByEmail — no duplicate contact/login for a case/alias email variant; see Gotchas. Prior same-day: Slice 5 tax progress tracker: `components/portal/tax-progress-tracker.tsx` on the portal home below the TaxBanner — 13 catalog-driven steps from `pipeline_stages` (`client_label` / new `client_label_it`, migration `20260610-1900`), position = SD stage overlaid with `review_status` (the SD parks at "Data Submitted" through the review loop; the overlay moves the dot to Under Review/Revision Requested/Approved/Confirmed so banner and tracker agree). Pure logic in `lib/tax/progress-tracker.ts::buildTrackerSteps` (null = hide: pre-1st-installment, terminated, legacy/unknown stage names). Auto-scrolls the current step into view. Catalog query: `getTaxTrackerCatalogStages` in `lib/portal/queries.ts`.)_
_Prior: 2026-06-09 — Claude (Slice 3 tax review banner: `components/portal/tax-banner.tsx` is now `'use client'` and has 7 states driven by `review_status` (null/submitted/under_review/revision_requested/approved/confirmed/reopened); includes a Confirm button (`POST /api/portal/tax-confirm`). `app/portal/page.tsx` TaxBanner now receives `reviewStatus`+`submissionId` from `getPortalTaxReturns` which joins the latest `tax_return_submissions` row. Banner filter changed from `!data_received` to "not terminal status". Legacy `dataReceived`/`sentToAccountant` fallback kept for pre-Slice-2 submissions._
_wizard file uploads → multi-file + any type + direct-to-storage signed URL, dev_task 64bfcdd9: new app/api/portal/wizard-upload-url, lib/portal/wizard-uploads.ts, wizard-field.tsx multi-file UI, wizard-client.tsx uploadToSignedUrl, extractUploadPaths + formation-setup.ts array-aware); 2026-06-08 — Claude (formation wizard routing for returning-active clients: getInProgressFormations now resolves leadId; FormationDashboard CTA + wizard/page.tsx carry/honor ?lead= / portal_formation cookie; prior 2026-06-06 new-document alerts; 2026-06-01 read of tier-config.ts, sync-tier.ts, auto-create.ts, queries.ts, notifications.ts, portal/team/*, resolve-portal-identity.ts, app/portal/layout.tsx, wizard-map.ts, wizard-visibility.ts, app/portal/wizard/page.tsx, app/portal/page.tsx, components/portal/formation-dashboard.tsx)_

> Formation-tier render path note: formation-tier clients without a materialized account go straight to `FormationDashboard` (skipping `ActionItems`). `FormationDashboard` has hard-coded CTAs for wizard/SS-4/OA/Lease, plus an optional `closureData` prop that surfaces a Closure CTA when a contact-scoped active Company Closure SD exists alongside the formation. Pattern shipped 2026-06-01 for Patrick Covelli (new NM formation + external DE LLC closure).

> Returning-active-client new-formation routing (2026-06-08): a new-company formation is **lead-anchored** — the wizard page only enters formation scope via `?lead=` (PR #75 / dev_task 21fd1f4a). A client who already owns an account and buys a new formation reaches `FormationDashboard` via the `portal_formation` switcher cookie, but a wizard CTA that omits `?lead=` falls through to the account context and opens that account's wizard (Tax/Banking) — the contact-scoped Company Formation SD (`account_id IS NULL`) is invisible to the account-id SD lookup. Fix: `getInProgressFormations` resolves each formation's `leadId` (via the formation offer linked to the SD; SD `notes` carry `from offer <token>` to disambiguate multiple formations — `extractOfferTokenFromNotes`), `FormationDashboard` carries `?lead=` on its CTA, and `wizard/page.tsx` also honors the `portal_formation` cookie (resolving the selected formation's lead) so the sidebar "Complete Setup" + direct-nav entry points route correctly. Fresh clients are unaffected (no account to fall through to). Surfaced live by Alessandro Federici (existing Scaledge LLC + new MMLLC NM).

## What it is
The client-facing app at **portal.tonydurante.us** where clients log in to see their services, documents, invoices, deadlines, chat with the team, and complete data-collection wizards. This is NOT the internal CRM dashboard — "portal" always means the client app.

## Tiers — the master access control (R102)
A client's portal experience is gated by their **tier**. There are exactly **4** (`lib/portal/tier-config.ts`, ordered):
| Tier | When | Sees |
|---|---|---|
| `lead` (0) | Offer sent, not paid | View/sign/pay offer, chat, profile, guide |
| `formation` (1) | Paid for formation (no EIN yet) | + data wizard, documents, signatures |
| `onboarding` (2) | Existing LLC going through onboarding | + upload docs, fill wizard |
| `active` (3) | Data reviewed / company live | + services, invoices, deadlines |

- Each tier **includes** the previous tiers' features (`TIER_FEATURES`).
- **A feature shows only if BOTH gates pass:** the tier allows it AND the data exists (`getPortalNavVisibility`). Tier alone is not enough.
- **Account type** is a second axis: **Client** (annual management — full feature set per tier) vs **One-Time** (standalone service — limited; no billing/invoicing/customers/deadlines).
- The value `full` is **removed** — never use it (R102).

### How tier is set — `syncTier()` only
- **ALL tier writes go through `syncTier()`** (`lib/operations/sync-tier.ts`). Never write `accounts.portal_tier` / `contacts.portal_tier` directly (R102).
- **Downgrade guard:** `syncTier` refuses to lower a tier unless `allowDowngrade=true` — it returns **success as a silent no-op**. This stops offer-publish (which runs at `lead`) from clobbering an existing `active` client.
- **Contact tier is computed, not set:** a contact's tier = the **highest** tier across all their linked accounts (`computeContactTier` / `maxTier`). Setting an account tier propagates to its contacts.
- `tierForContract(contractType)`: `formation`→`formation`, `onboarding`→`onboarding`, everything else→`active` (`lib/portal/auto-create.ts`).

## Chat, notifications, documents
- **Office hours / international clock.** Source of truth for office hours is `lib/portal/office-hours.ts`: **Mon–Fri 9 AM–3 PM Eastern (`America/New_York`), excluding U.S. federal holidays**. `getOfficeStatus(now)` returns `{open, reason}` (`open|before_hours|after_hours|weekend|holiday`); `isOfficeOpen()` = `getOfficeStatus().open`; `usFederalHolidays(year)`/`isOfficeHoliday()` compute observed federal holidays (Sat→Fri, Sun→Mon). Two consumers: (1) the chat **out-of-office auto-reply** (`lib/portal/auto-reply.ts`, fired from `app/api/portal/chat/route.ts` when a client messages while closed — now also fires on holidays); (2) the **`OfficeClock`** widget (`components/portal/office-clock.tsx`), a `'use client'` live clock mounted in the portal top bar (`app/portal/layout.tsx`, both client + teammate branches) on every page — shows ET office time + Open/Closed badge + the **client's own time**, bilingual, mount-guarded against hydration mismatch. **"YOUR TIME" is the client's timezone, NOT the device's:** the layout resolves the viewed contact's `contacts.address_country` → IANA tz via `lib/portal/client-timezone.ts::countryToTimeZone` and passes `clientTimeZone`/`clientTimeZoneLabel` to the clock, so it reflects the client even when staff open the portal via View-as (the device tz would otherwise be the staff member's). Empty/unknown country → falls back to the device/browser tz (Antonio's choice). `countryToTimeZone` normalizes free-text country (casing/accents) + an alias table (Italia→Italy, UAE, USA…); multi-tz countries → their main tz (refine by state later). **Office vs chat (do not "reconcile" these — they are two different things, confirmed by Antonio 2026-06-23):** the physical **office opens 8 AM ET** (what the signed service-agreement contracts in `app/offer/[token]/contract/*.tsx` state — correct, NOT a typo); **live chat/support opens 9 AM ET**. The clock + auto-reply track **chat/support (9 AM)** because their purpose is the client's expectation of getting a *reply*. The clock is labeled "Live chat open/closed" + "US Eastern" (not "Office") so the two don't get confused.
- **Chat** lives in `portal_messages`. Admin sends (dashboard or `portal_chat_send`) → the client gets an **automatic email** (`notifyClientOfAdminMessage`, throttled **1 email/conversation/2h**, bilingual — R103). Messages are **soft-deleted** (`deleted_at`/`deleted_by`, R100) and update over realtime. Tools: `portal_chat_inbox/read/mark_read/send`.
- **Email is skipped when the client has active push** (PWA installed + notifications on) — they get the Web Push instead, so the email would be redundant. Per recipient, `notifyClientOfAdminMessage` checks `push_subscriptions` first by `contact_id`, then by `account_id` (account-level fallback covers teammates and any subscription not keyed to a contact); if either has ≥1 row, the email is suppressed for that recipient. The portal "Enable notifications" endpoint (`app/api/portal/push`) now records `contact_id` (from `getClientContactId`) alongside `user_id`/`account_id` so the per-contact check has data — **pre-existing subscriptions have a null `contact_id` until the client re-subscribes**, but they're still caught by the account-level check (they carry `account_id`). Caveat: the account-level fallback is coarse — in a multi-member LLC where only one member installed the PWA, members **without** push can have their email suppressed too until subscriptions carry `contact_id`.
- **Chat attachments** upload **directly to Storage via a signed URL** (`app/api/portal/chat/upload-url` + `lib/portal/chat-attachment.ts`), NOT through a serverless function — this bypasses the platform request-body limit that silently failed large passport photos / PDF scans ("Upload failed try again"). Shared helper `uploadChatAttachment`/`validateChatAttachment` is the single source of truth used by all three senders (client `portal-chat.tsx`, staff CRM inbox `portal-chats/page.tsx`, staff `contact-detail.tsx`). Policy: any normal file, **block-list** for active-content/executables (HTML, SVG, JS, exe, …); **100 MB** cap enforced client-side AND at the `assets` bucket `file_size_limit`. Files land in the **public** `assets` bucket under `chat-attachments/<accountId|contactId>/`. The old streaming route `app/api/portal/chat/upload` was removed.
- **Wizard file uploads** (formation/onboarding/tax/banking/itin/closure/company_info) upload **directly to Storage with a RESUMABLE (TUS) upload** (`tus-js-client` → `{ref}.supabase.co/storage/v1/upload/resumable`, helper `lib/portal/resumable-upload.ts`), NOT through the serverless `wizard-upload` route — to escape the ~4.5MB platform request-body cap that silently failed merged bank-statement PDFs (dev_task 64bfcdd9, 2026-06-09). `app/api/portal/wizard-upload-url` only **mints the path** (server owns the scheme); the browser then TUS-uploads in 6MB chunks authenticated with the **client's own session token** (`supabase.auth.getSession()`), with auto-retry on network blips + a progress bar. Every `type:'file'` field is **multi-file** and accepts **any file type** (no `accept` restriction unless a field sets one). The field value is now an **array of storage paths** (legacy single-string values still render). Path scheme is preserved — `{wizardType}/{identifier}/{fieldName}_{unique}_{filename}` — so all downstream matching by field name still works. `lib/portal/wizard-uploads.ts` (`collectUploadPaths`/`firstUploadPath`/`normalizeUploadValue`) is the single source of truth that flattens string|array; `wizard-submit`'s `extractUploadPaths` and `formation-setup.ts`'s owner-passport copy both go through it. Downstream Drive copy / passport OCR / bank-statement→P&L read the flat `upload_paths` list (unchanged); `formation-materialize.ts` already falls back to it. **Storage:** the upload uses the client's JWT, so `onboarding-uploads` needs an INSERT RLS policy — prod already had `Allow public read/upload onboarding`; sandbox was missing the bucket + all policies (migration `20260609-1210-onboarding-uploads-bucket-and-policies.sql` brings it to parity). **Max file size is the Supabase project's global upload limit** (a dashboard setting), which caps standard and resumable alike — resumable's win is reliable chunked transfer + resume, not a higher ceiling.
- **Notifications:** `createPortalNotification`, `notifyClientOfStageAdvance`, `getUnreadNotificationCount` (`lib/portal/notifications.ts`); `/portal/notifications` page.
- **New-document alerts (admin → client)** (`lib/portal/document-alerts.ts`, 2026-06-06; unified 2026-06-11): when a document becomes client-visible, the client is alerted, the Documents sidebar tab **pulses** with an unopened count, and the doc shows a **"New" pill + indigo-tinted row** until that person opens it. Dispatch is owned by the **`updateDocument()`/`updateDocumentsBulk()` choke point** (`lib/operations/document.ts`): an actual `portal_visible` false→true transition fires `notifyClientsOfNewDocument(documentId)` fire-and-forget — covering the file-manager toggle, process-and-share, the signature webhook, and future callers (singles alert by default; **bulk defaults to NOT alerting** — portal-transition migrations; opt in with `clientAlert: true`). The two admin upload routes (`app/api/crm/admin-actions/upload-document`, `upload-account-document`) call the module directly (raw insert path). It's idempotent via `documents.client_notified_at` (TOCTOU-guarded), respects the per-doc `documents.notify_client` toggle and the global kill switch `app_settings.new_document_alert_enabled` (default true), creates a `type='new_document'` portal_notification + web push, and (since 2026-06-11, Antonio: ON) also posts a localized **portal chat message** ("A new document has been added to your folder: <name>") behind `app_settings.new_document_chat_message_enabled` (default true; account-scoped — personal docs with no account skip chat). **Delivery rule mirrors chat:** PWA clients get the push; non-push clients get the batched digest email — the `portal-digest` cron skips the email for `new_document` when the recipient has a push subscription (no double-notify). **Digest rendering (2026-06-11):** section building lives in `lib/portal/digest-render.ts` — `document`/`new_document` items render the file name under the title (`show_body`); per-type icon/labels/show_body are overridable at runtime via `app_settings.portal_digest_type_labels`. **Unresolvable recipients** (contact without email/portal, account without `portal_account` or company email) get their notifications **marked `email_sent_at` and counted in cron_log `details.skipped`** instead of recycling through the digest query every 5 minutes forever (pre-fix: the LUMA June-9 row retried indefinitely). **"New" is per-CONTACT** via `portal_document_views(document_id, contact_id, viewed_at)` so each owner of a multi-member LLC clears their own state; opening a doc records the view (`POST /api/portal/documents/[id]/view` + the download proxy as backstop). **Baseline:** pre-feature docs keep `client_notified_at = NULL` and never show as "new" (no day-one flood; no backfill). Sidebar count via `getUnopenedDocsCount`; the tab stops pulsing once the client is on `/portal/documents`.
- **Home announcement banners** are client components rendered on the active-tier home dashboard (`app/portal/page.tsx`), each dismissed per-device via a versioned `localStorage` key read in a `useEffect` (no hydration mismatch): `GuideAnnouncementBanner`, `TeamAccessAnnouncementBanner` (key `td-team-access-announce-v1`, also drives the sidebar "new" badge), and `WhatsNewBanner` (key `td-whats-new-v1`, bilingual "What's New / Novità" feature announcement). Bump the key's version suffix to re-show after a major change. Lead/onboarding/formation tiers render different dashboard components and do not show these.
- **Documents:** auto-saved (`auto-save-document.ts`), templated (`document-templates.ts`); pages for general docs, `tax-documents`, `itin-documents`. Legal docs are view-only for non-signers.
- **Invoices/payments:** `/portal/billing`, `/portal/invoices`; the **Pay modal is the only payment entry point** (R092) — invoice emails point here, never embed Stripe/wire. The expense list shows the **net amount due** (`client_expenses.amount_due`, the original `total` struck through when a credit was applied) and the Pay modal charges `amount_due`, so a credited invoice (e.g. a referral credit reconciled in — see `billing-invoicing.md` / `referrals-circleback.md`) shows the client the true balance. `getPortalExpenses` selects `amount_due`/`amount_paid` (added 2026-06-01).

## Team Access — teammates (Option B, independent identity)
An **active client's account-admin** can invite their employees ("teammates") into that **one company's** portal, choosing per-section what each may see.
- **Identity model (Option B):** a teammate is an **independent identity** — its own row in `portal_team_members`, **NOT** a contact and **NOT** in `account_contacts` (so billing/ops flows never target a teammate). Auth lives in `auth.users` with `app_metadata.role='client'` + `kind='team_member'` + `account_id` + `team_member_id` markers, and a synthetic email (`tm-<token>@teammate.portal.tonydurante.us`). Login is **username + password** (`app/portal/login`).
- **Who can manage:** only the **account-admin** ("main person"). Resolver precedence (`lib/portal/team/account-admin.ts`): persisted override (`accounts.portal_admin_contact_id`) → MMLLC SS-4 signer → signer name-match → owner-role `account_contact` → sole contact. SMLLC = the stored owner.
- **Capabilities are owner-chosen, server-enforced, default-deny** (`lib/portal/team/capabilities.ts`): `documents`, `invoices_billing`, `chat`, `company_services`, `bank_applications`, `sales_customers`, `company_data_form`, `announcements`. Empty = see nothing beyond always-on (overview/guide). Owner-only/non-delegable sections (team, sign, request service, referrals, profile, offer) are **never** delegable.
- **Chat notifications include teammates** — `notifyClientOfAdminMessage` (the single admin→client chat-email chokepoint) emails account contacts AND active teammates who have the `chat` capability + a real email on file (placeholder emails live only on auth.users, so they're skipped). Revoked teammates and no-chat-cap teammates are excluded; recipients are deduped by email. Teammates have no language column, so they **follow the owner's language** (the `role='owner'` account contact, else any contact, else English).
- **The gate** (`lib/portal/team/gate.ts`): `canAccessAccount(user, accountId, capability)` is **default-deny** — it replaces the leaky `if (contactId) { check }` pattern that silently skipped the check for a null-contact (teammate) user. Staff (non-`client` role) bypass. Pages use `getTeammateScopeOrNull(cap)`; the identity seam is `resolvePortalIdentity` (`contact | teammate | none`).
- **Production safety:** prod RLS denies a teammate every **direct/realtime** read (their JWT has no `contact_id`, so `get_client_account_ids()`/`get_client_contact_id()` return empty). Granted sections still work because pages fetch **server-side** via `supabaseAdmin`. Owner accepts a **responsibility disclaimer** at invite time.
- **Manage:** `/portal/team` (admin-only tab) — invite/edit/revoke/reset-password; revoke also bans the auth user. Optional teammate email enables notifications + self-serve password reset. The invite dialog explains email is optional (only for notifications).
- **Feature announcement** — account-admins see a one-time dismissible violet banner on the portal home (`TeamAccessAnnouncementBanner`, gated by `canManageTeam` computed via `isAccountAdmin`) + a "NEW" badge on the Team nav item; both clear via the shared `localStorage` key `td-team-access-announce-v1` (on banner dismiss/click or first Team-page visit). The "Invite Your Team" guide article (id `team-access`, EN+IT) lives in `app/portal/guide/page.tsx`. A broader broadcast can be posted to all clients via the existing `portal_announcements` system (CRM Config → Announcements).
- **Teammate chat attribution** — a teammate has no contact, so their chat messages stamp `portal_messages.sender_name` with the teammate's **display name** at send time (chat route teammate branch). The read route resolves the display name via `pickChatSenderName(contactName, storedSenderName)` (`lib/portal/chat-sender-name.ts`): contact name for client/owner messages, stored display name for teammates, null → UI's generic label. So staff/owner see *which* teammate wrote. Admin/system messages still render "Tony Durante Team".
- **Teammate chat threading** — the chat page (`app/portal/chat/page.tsx`) and `usePortalChat` thread by `contact_id` for normal clients but by `account_id` for teammates (no contact_id; their messages are stored by account with `contact_id` NULL). The page resolves the teammate's account via `getTeammateScopeOrNull('chat')` instead of the old `if (!contactId) redirect('/portal')` that silently bounced teammates out of Chat. The Journey Log tab is hidden for teammates (contact-oriented).

## Business rules
- **R102** — 4 tiers; `syncTier()` is the only writer; `formation` = no EIN; never advance tier manually; `full` is forbidden.
- **R103** — admin portal message auto-emails the client (throttled 2h, bilingual).
- **R100** — client-visible content (chat, notifications, docs) uses soft-delete; clients never see deleted rows, staff see a tombstone.
- **R092** — clients pay via the portal Pay button only.
- **R099** — client-side fetches must surface the server's real error, not a generic toast.
- **No support@ fallback** — the portal IS the channel; don't tell clients to email support@ as an alternative.

## How it's built
### Key files
- `lib/portal/tier-config.ts` — canonical tiers + `TIER_FEATURES` + `maxTier`.
- `lib/operations/sync-tier.ts` — `syncTier()`, `computeContactTier()`, `syncContactTiersForAccount()`.
- `lib/portal/auto-create.ts` — `tierForContract()`, `autoCreatePortalUser()`, `ensureMinimalAccount()`.
- `lib/portal/queries.ts` — everything the portal reads (`getPortalAccounts`, `getPortalNavVisibility`, `getPortalTier`, services/payments/expenses/deadlines, ITIN-at-signing).
- `lib/portal/notifications.ts` — client notifications + email.
- `lib/portal/team/*` — Team Access: `capabilities.ts` (per-section flags + nav map), `account-admin.ts` (admin resolver), `provision.ts` (create teammate auth + row), `server.ts` (list/update/revoke/reset), `gate.ts` (`canAccessAccount` default-deny + `getTeammateScopeOrNull`). `lib/portal/resolve-portal-identity.ts` — `contact | teammate | none` seam.
- `lib/mcp/tools/portal.ts` — `portal_create_user`, `portal_chat_*`, `portal_invoice_create/send`, `portal_messages`, `portal_team_send`, `portal_transition_*`.
- `app/portal/*` — the pages (login/auth, dashboard, wizard, documents, billing/invoices, chat, deadlines, services, members, settings, …).
- Wizards: `wizard-map.ts`, `wizard-scope.ts`, `wizard-visibility.ts`. Multi-company: `select-entity.ts`, `getPortalAccounts` (a contact can hold several accounts).
  - **Field rendering (`components/portal/wizard/wizard-field.tsx` + `wizard-client.tsx`, 2026-06-11):** every field's help text is **always visible** under the field — the click-to-reveal "i" tooltip was removed (clients never clicked it), so this applies to ALL wizards. A field may carry `warningOnValue` to show an amber callout when a specific answer is chosen (e.g. the SMLLC related-party "No" → $25k-penalty notice). A `type:'repeater'` field honors `conditional` (renders only when its gate matches) and, when `required`, `validateStep` blocks the step until ≥1 row exists with every required sub-field filled. See the SMLLC tax intake writeup in `tax-returns.md`.
  - Wizard types fall into three categories: **contact-scoped** (`formation` — lives on the contact until the LLC materializes), **account-scoped** (everything else by default — onboarding, banking, tax, company_info), and **flexible** (`closure` today — can run contact-scoped for an external LLC OR account-scoped for an existing managed LLC). Discovery (`wizard-visibility.ts`, `app/portal/wizard/page.tsx`) unions contact + account queries for flexible types so a managed-account holder can still see a contact-scoped Closure.
  - Action items (`getPortalActionItems` / `getPortalActionItemsByContact`) surface a `Start ⟨Wizard⟩ Form` card per active wizard-eligible SD that has no `wizard_progress` row yet, deep-linked to `/portal/wizard?type=⟨wt⟩`. In-progress wizard cards also deep-link so refreshes return the client to the same wizard.

### Tables
`accounts`/`contacts` (`portal_tier`; `accounts.portal_admin_contact_id` = team-admin pointer), `account_contacts`, `portal_messages` (`deleted_at`, `deleted_by`, pin fields), `portal_notifications`, `portal_team_members` (teammate identity + `capabilities` JSONB, RLS-on/service-role-only), portal auth users (`auth.users`), `payments`/`client_invoices`/`client_expenses`, `service_deliveries`, `deadlines`.

## Gotchas, invariants & past bugs
- **A failed downgrade looks like success** — `syncTier` silently no-ops a downgrade and returns `success:true`. Intentional (protects clients); don't "fix" it into an error.
- **Never set a contact's tier directly** — it's computed as the max across linked accounts.
- **Tier gate AND data presence** both required to show a feature — a tier change won't reveal a section that has no data.
- **One-Time accounts stay limited even at `active`** (no billing/invoicing/customers/deadlines).
- **ITIN-at-client-signing is allowed at every tier** — the data (an active ITIN SD at "Client Signing") is the real gate, not the tier.
- **Teammates have no `contact_id`** — never gate portal access on `if (contactId)`; that silently skips the check for a teammate and leaks the resource. Use `canAccessAccount` / `getTeammateScopeOrNull` (default-deny). Teammates are never in `account_contacts` and must never be targeted by billing/ops flows.
- **Contact reuse is case-insensitive + alias-aware** (2026-06-10) — `autoCreatePortalUser`/`createFromEmail` resolve an existing contact via `findContactIdByEmail()` (`lib/operations/find-contact-by-email.ts`), NOT exact `.eq('email', …)`. A differently-cased or post-merge-alias email must reuse the real person, never spawn a duplicate contact + duplicate portal login (the Michele Cotti orphan bug). The helper matches `email` case-insensitively (ilike narrows, exact compare in JS to dodge the `_` wildcard) + folded `alt_emails`, and never returns a merged tombstone (`merged_into IS NOT NULL`). Never reintroduce a raw `.eq('email', …)` contact lookup in the activation/portal-creation path.
- Per-company chat isolation is the other in-flight multi-tenant project — verify current chat behaviour against code before assuming.

## How to verify current state
- Read `lib/portal/tier-config.ts` (the 4 tiers + features), `lib/operations/sync-tier.ts` (`syncTier` + downgrade guard + `computeContactTier`), `lib/portal/queries.ts` (`getPortalNavVisibility`, `getPortalTier`).
- A client's tier: `SELECT portal_tier FROM accounts WHERE id='<id>';` and `SELECT portal_tier FROM contacts WHERE id='<id>';` (contact should equal the max of their accounts).
- Note (R096): sandbox via sandbox MCP / `psql`; production `execute_sql` hits production.
