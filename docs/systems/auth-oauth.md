# Auth & OAuth
_Last verified against code: 2026-08-06 — Claude (**`/portal/install` added to `PUBLIC_PREFIXES`** — the public PWA install landing page (QR/link target, dev job `8f38add1`). Carries no client data and no session; a QR is scanned by a logged-OUT phone, so without the prefix every scan 307s to the portal login and the funnel is dead. It is a bare `startsWith` match (no trailing slash): any future sibling route beginning with `/portal/install` would silently become public — don't create one. The page lives in route group `app/(portal-install)/` outside the portal shell; see `pwa.md` → "Portal install page".)_
_Prior: 2026-08-02 — Claude (**Self-serve password reset moved off the browser and onto our own Gmail; `/api/portal/password-reset` added to `PUBLIC_PREFIXES`.** Origin: client Chiara Fazzini (CF Consulting LLC), 2026-08-02 — "non mi arriva nessuna e-mail per impostare nuova password". **Root cause was NOT a broken mailer.** `app/portal/forgot-password/page.tsx` called `supabase.auth.resetPasswordForEmail` straight from the BROWSER, so (a) the mail arrived from **"Supabase Auth" `noreply@mail.app.supabase.io`** — plain, English, unbranded, a sender no client has ever seen, trivially spam-filed or unrecognised — and (b) the request never touched our servers, so there was **no Vercel log, no `action_log` row, no trace at all** that a client had tried. **⚠️ DO NOT diagnose this from `auth.users.recovery_sent_at` — it is a TRAP and cost this session hours.** Empirically, on 2026-08-02: a COMPLETE successful self-serve reset on PRODUCTION (uxio74@gmail.com, Antonio) left `recovery_sent_at` NULL, no `auth.one_time_tokens` row and no `auth.audit_log_entries` row (that table is EMPTY, 0 rows, project-wide). Meanwhile `supabaseAdmin.auth.admin.generateLink` DOES stamp it — and `app/portal/view-as/route.ts:43` mints a magiclink on EVERY admin "View as client", so **96 of the 100 stamped production users correlate within 120 SECONDS of a `portal_view_as_enter` row**. The column measures admin-minted links, not client resets. Worse, the two environments DISAGREE: in sandbox `resetPasswordForEmail` DID stamp it, on production it did not — do not reason from it in either direction. **New shape:** `lib/portal/password-reset.ts` (`normalizeResetEmail`, `buildResetEmailContent`, `runPasswordResetAttempt`) + `app/api/portal/password-reset/route.ts`. The token is still minted by GoTrue via `admin.generateLink({type:'recovery'})` — **we deliberately did NOT build our own reset-token scheme** (the council rejected it: no new table, no owned crypto, no bespoke expiry/single-use). **PROVEN BY LIVE TEST, not assumed:** `generateLink({type:'recovery'})` does **NOT** send the provider's email — sandbox, 22:05:58 mint produced zero mail, while a control `resetPasswordForEmail` at 22:07:02 delivered one within the same second to the same mailbox. So there is no double-send. Also measured: a **~60s per-user cooldown** ("you can only request this after N seconds") — a second request 82s later succeeded. **Every attempt now writes an `action_log` row** (`action_type='portal_password_reset_requested'`) recording the attempted address, matched/unmatched, and the real outcome — including `sandbox_blocked`, because `gmailPost` short-circuits on `SANDBOX_MODE=1` (`lib/gmail.ts:142`) and returns `{sandbox:true}`: **never derive "sent" from "gmailPost didn't throw"**. The HTTP response is byte-identical for matched and unmatched addresses (no account enumeration) and the rate-limit budget is charged on BOTH branches, or a 429 becomes a perfect enumeration oracle. **Known limitation, stated not hidden:** `lib/portal/rate-limit.ts` is an in-memory Map, per serverless instance — a best-effort speed bump, NOT a guarantee; this route is an outbound-mail amplifier until a DB-backed limiter exists. **Language rides on the link** (`?lang=`) because the reset page is reached LOGGED OUT, where the portal has no `LocaleProvider` and `getLocale()` needs a session — see `portal.md`. **Contact lookup uses `.limit(1)` + `order(created_at)`, NOT `.maybeSingle()`** — found in live sandbox data: `tdtest@tonydurante.us` is on TWO contacts (one Italian, one English); `maybeSingle()` errors on a multi-row match and returns null, silently downgrading a duplicated Italian client to an English email. Live end-to-end proof: the Italian branded mail arrived from `Tony Durante <support@tonydurante.us>` at 22:31:02. Dev job `fb4bcae4`. NOT on production; Antonio ships.)_

_Prior: 2026-08-01 — Claude (middleware PUBLIC_PREFIXES: added `/api/offer/` — the new token-gated contract-PDF route `GET /api/offer/[token]/contract-pdf` verifies the offer's `access_code` server-side and returns a short-lived signed URL to the client's own signed contract; it is public (not session-gated), exactly like the existing `/api/lease/` and `/api/operating-agreement/` entries. Without the prefix a client-facing request 401s SESSION_EXPIRED — **and this was caught only by a real sandbox deploy, not by tests: the FIRST deploy restored the build cache and did not rebuild the middleware bundle, so the new prefix was missing and every contract download 401'd; a clean rebuild fixed it. Verify `/api/offer/` is public post-deploy before relying on it.** It is a directory prefix — `startsWith('/api/offer/')` — distinct from the separately-listed `/api/offers` (offer checkout); any future route under `/api/offer/` is public by default and must self-gate. Part of storage-lockdown Stage 2; full detail in `offers.md` 2026-08-01, dev job `97177e49`.)_
_Prior: 2026-07-24c — Claude (middleware PUBLIC_PREFIXES: added `/api/lease/` — the new token-gated lease read route `GET /api/lease/[token]/fetch` verifies the lease access code server-side and is public (not session-gated), exactly like the existing `/api/operating-agreement/` entry. Without the prefix a client-facing request 401s SESSION_EXPIRED. The prefix does NOT match `/api/lease-signed` or `/api/lease-regen-drive` (different separator) — those stay as they are. Part of the lease security lockdown; full detail in `lease-oa.md` 2026-07-24c, dev job `8ffd1122`.)_
_Prior: 2026-07-24b — Claude (**New `isStaffUser` helper — a STRICTER staff check than `isDashboardUser`** (`lib/auth.ts`, td-bug channel change, dev job `59db1c17`). `isDashboardUser` only excludes clients, so a managed partner (Cris, `role='partner'`, confined to `/collab`) passes it. `isStaffUser` delegates to the single `isStaffAuthRole` deny-list (`lib/team/workspace.ts`) — the one place that knows partners and clients are not staff — so server and UI can't drift. Used to gate the admin push-registration endpoint (a partner or client browser could previously register itself to receive internal staff notifications) and its self-test. Use `isStaffUser`, not `isDashboardUser`, for anything that carries internal business to a person. Residual, stated in code: the deny-list means a future outsider role must be added to `NON_STAFF_AUTH_ROLES` in the same change. Full rationale in `docs/systems/team-workspace.md` 2026-07-24.)_

_Last verified against code: 2026-07-22 — Claude (**`/api/operating-agreement/` added to `PUBLIC_PREFIXES`.** The new OA fetch route is token+access-code gated and holds the service key; it verifies the code SERVER-side and returns a field whitelist. Without the prefix it 307'd to the staff login and both public signing pages broke — caught only by a real request, not by tests. Same class as the `/api/sign/*` and `/sign-document` entries above. Note it is a directory prefix: any future route added under `/api/operating-agreement/` is public by default, so it must self-gate. Related, same change: the shared signing access-code guard now fails closed on a blank stored code — see `esign.md`. **Progress on the `023c7d06` exposure tracked in the 2026-07-20 entry above:** `oa_agreements` + `oa_signatures` carried `USING (true)` policies for role `public` (one named "Allow all for authenticated" while actually being role `public`, cmd ALL) with anon holding SELECT — one unauthenticated request returned every agreement's access code, EIN, member emails/addresses and every co-signer's personal signing code, with tokens derived from a public company name. The pages now read through the server route; the accompanying migration `20260722-0100-oa-close-public-read.sql` revokes table SELECT from anon+authenticated, grants column-level SELECT on `id` only (so the retained `UPDATE ... WHERE id` still resolves — dropping the SELECT POLICY makes the row invisible and the write silently matches ZERO rows while PostgREST returns 204), keeps a visibility-only SELECT policy, and scopes the UPDATE grant to the columns the pages actually write. That last part is load-bearing: table-wide UPDATE let an attacker overwrite `access_code` and then call the service-key route with their own code — a write turned into a read. NOT closed: signed PDFs remain readable from their bucket by token with no code.)_
_Last verified against code: 2026-07-20 — Claude (**APPLIED TO PRODUCTION: unauthenticated access to all 15 `public` views REVOKED.** Migration `scripts/migrations/20260720-2210-revoke-anon-from-public-views.sql`, run by Antonio in the Supabase SQL editor (the MCP `execute_sql` path CANNOT run `REVOKE` — `exec_sql` only classifies INSERT/UPDATE/DELETE/WITH/CREATE/ALTER as mutations, so a REVOKE falls to the read branch and errors with `syntax error at or near "SELECT"`; `CREATE POLICY`/`ALTER POLICY`/`ALTER VIEW` DO work there). **The hole:** every view in `public` is owned by `postgres`, which carries `rolbypassrls`, and NONE set `security_invoker` — so a view executed with the OWNER's rights and RLS on the underlying tables DID NOT APPLY. `anon` held SELECT on all 15. One unauthenticated PostgREST request against `v_client_full` returned `itin_number` / `citizenship` / `residency` / `kyc_status` / `email` / `phone` for EVERY contact; `v_account_detail` returned EIN + banking; `v_overdue_payments` contact emails + amounts; `v_tax_return_tracker` the whole tax book. No token, no path guessing, no row limit. Found by the Security reviewer in the Ship 2 council pass and independently verified against production before acting; it was absent from the original Ship 2 plan entirely. **Why the revoke was safe** (verified BEFORE applying): it targets ONLY `anon` — staff and portal users act as `authenticated`, server routes as `service_role`, both untouched (confirmed post-apply: anon=false, authenticated=true, service_role=true on all 15). A grep across `app/ lib/ components/` showed 11 of the 15 views are referenced ONLY in the generated `lib/database.types.ts` — nothing queries them; the 4 real consumers are all SERVER-side (`v_messaging_inbox` → `app/(dashboard)/layout.tsx` + `lib/mcp/tools/messaging.ts`; `service_catalog` → `app/api/service-catalog/route.ts`, `app/api/crm/admin-actions/create-service/route.ts`, `app/(dashboard)/service-catalog/actions.ts`), and ZERO public token-gated pages or portal pages read any view. Post-apply production browser check: Service Catalog renders all 19 services, Dashboard renders live To-Do cards and sidebar counts. **NOT yet fixed — `security_invoker` is still OFF**, so an AUTHENTICATED caller (including a client-role portal login) still bypasses RLS through these views. Turning it on requires first checking each view's underlying-table policies so staff reads don't break; tracked on dev job `023c7d06`. **Related and still open on the same job:** `payments` carries role `{public}` policies whose predicate `COALESCE(auth.jwt()->'app_metadata'->>'role','') <> 'client'` evaluates TRUE for anon (auth.jwt() is NULL → COALESCE gives `''` → `'' <> 'client'`), so payments are anon read/update/delete TODAY — and the SAME broken predicate shape is deployed with no `TO` clause on `internal_messages`, `internal_threads`, the Slack-mirror and team-thread tables. The correct form (precedent: `scripts/migrations/20260503-2200-address-registry-prod-backfill.sql:52-55`) is `USING (auth.role() = 'authenticated' AND COALESCE(...) <> 'client')` **plus** `TO authenticated`. Never write the bare COALESCE predicate again — it excludes logged-in clients only, never the public.**)_
_Prior: 2026-07-08b — Claude (**`/dashboard-sw.js` + `/portal-sw.js` added to `PUBLIC_PREFIXES`** — PWA mobile UX pass, dev_task `e1f28dce`. The `config.matcher` extension exclusion list has no `.js`, so both service-worker scripts were auth-gated: registration worked only because it always happens from an authenticated page, but the browser's own SW refetch and `use-sw-update.ts`'s 60s `reg.update()` poll got a 307→login HTML after session expiry and the update silently failed (swallowed by `.catch(()=>{})`), leaving installed PWAs on a stale worker. Both scripts are push-display-only (no secrets, no caching logic at this date). Same class as the manifest prefixes directly above. Same pass, second commit: **`/offline` added to `PUBLIC_PREFIXES`** — the PWA offline fallback page (`app/offline/page.tsx`) that `dashboard-sw.js` precaches at install; a fresh SW fetches it without a session, so it must be public. Full subsystem doc: `pwa.md` (new).)_
_Latest 2026-06-28 — Claude (**Partner login + middleware allowlist** — TD Communication Phase 2, branch `feat/td-comm-pipeline`, SANDBOX. `middleware.ts` partner-confinement branch (`role==='partner'`) now also allows `/api/td-communication*` alongside `/collab` + `/api/conversations` (everything else still bounces to `/collab`). The login page (`app/portal/login/page.tsx`) now admits `role='partner'` (was client-only) and requires a non-empty `partner_scope` via the `partnerLoginAllowed()` server action (scope is in the RLS-protected `client_partners` table, so the check can't run client-side). Feature in portal.md + td-communication.md.)_
_Last verified against code: 2026-07-08 — Claude (**`/api/team/claude/process` added to `PUBLIC_PREFIXES`** — the Team Workspace @claude worker runner. Server-to-server only (direct fire from the team send route + a `*/2` Vercel cron rescue in vercel.json); it self-authenticates via `CRON_SECRET` Bearer inside the route, same pattern as `/api/cron/*`. Without the prefix the session gate 401'd (SESSION_EXPIRED) every server-to-server call BEFORE the route's own auth — the root cause of @claude's stuck "thinking…" placeholders on production, found via sandbox E2E 2026-07-08. See `team-workspace.md`. Prior entry: **Expired-session API calls now return 401 JSON `code: SESSION_EXPIRED`** instead of redirecting background fetches to the login page — see the "No user" bullet + `error-auto-audit.md`. Prior entry: **Phase 16: `/site/` added to `PUBLIC_PREFIXES`** — public per-client TD Communication landing pages `/site/<slug>`. TRAILING SLASH (unlike `/portfolio`) pins the `startsWith` match to `/site/<slug>` so it can never whitelist a sibling like `/sitemap.xml`. The page self-gates on the `landing_builder_enabled` kill-switch + `published` (shows "coming soon" + `robots noindex` otherwise); all editor/publish routes live under the SEPARATE self-authenticating `/api/td-communication/projects/[id]/landing-site` prefix. Without the public prefix a logged-out visitor 307s to `/login`. See `td-communication.md` → Phase 16. Prior entry: **Phase 14: `/portfolio` + `/api/td-communication/portfolio` added to `PUBLIC_PREFIXES`** — public TD Communication showcase; single literal routes, curator routes live under the separate self-authenticating `/api/td-communication/admin/portfolio` prefix; see the Public paths line + `td-communication.md`. Prior entry: **E-sign signer page `/sign/[token]/[code]` + `/api/sign/*` + pdfjs worker `/esign/pdf.worker.min.mjs` added to `PUBLIC_PREFIXES`** — token+code gated, no Supabase auth; without them external signers were bounced to the staff login. Caught by real-browser E2E since API-handler tests bypass middleware. Prior entry: **Suspended portal logins blocked at the gate**, branch `feature/portal-suspend-access`. When an admin suspends a client (see portal.md), their auth user is banned. `middleware.ts` already calls `getUser()` per request, and it returns `banned_until` reliably even on a still-valid access token — so a new check: if `banned_until` is in the future, clear the `sb-*` session cookies and redirect to `/portal/login?reason=suspended`. This bounces a suspended client on their VERY NEXT request (instant kick-out) instead of waiting ~1h for the access token to expire. It is a **no-op for normal users** (`banned_until` is null → branch skipped) and adds **no DB/network call** (reuses the existing `getUser()`); proven live in sandbox (a normal client navigated the portal unaffected). The login page (`app/portal/login/page.tsx`) shows "Your login has been suspended by the administrator." both on the `?reason=suspended` redirect (read client-side in a `useEffect`) and when a suspended client submits the form (`signInWithPassword` → `code: 'user_banned'`, verified empirically in sandbox: fires on correct AND wrong password). Shipped to production.)_
_Prior: 2026-06-15 — Claude (portal LOGIN email now follows the CONTACT email: any contact-email change syncs the auth login email via `lib/operations/portal-login-email.ts` — by contact_id, conflict-guarded, client-notified. See "Portal login email vs contact email" below + crm-core.md.)_
_Prior: 2026-06-15 — Claude (added the read-only "View as client" lock to middleware.ts — see the bullet + Gotchas; feature writeup lives in portal.md)_
_Prior: 2026-05-29 — Claude (read middleware.ts, lib/auth.ts, lib/oauth.ts)_

## What it is
Two completely separate authentication systems — don't confuse them:
1. **Web app auth (Supabase session)** — gates the CRM dashboard and the client portal in the browser, via `middleware.ts` + RBAC roles.
2. **MCP server auth (dual: Bearer + OAuth 2.1)** — gates the MCP tool server (`app/api/[transport]/route.ts`): a static **Bearer token** for Claude Code, and **OAuth 2.1 + PKCE** for the Claude.ai custom connector.

## Web app auth & RBAC
- Roles live in `user.app_metadata.role`: **admin** / **team** / **client** (`lib/auth.ts`: `isAdmin`, `isTeam`, `isClient`, `isDashboardUser`, `getCrmRole`).
  - **admin** (Antonio, `antonio.durante@tonydurante.us`) — full access: financials, settings, all pages.
  - **team** (support@, staff) — operational only: tasks, services, accounts.
  - **client** — portal only.
- **`middleware.ts` route gating:**
  - **Public paths** (no auth): `/portal/login`, forgot/reset password, `/portal/auth/callback`, `/api/webhooks`, cron, the referral landing `/invitation`, the token+code contact-request form, `/api/portal/announcements`, the `/pay/<token>` redirect, `/.well-known`, `/oauth`, the PWA manifest, and (Phase 14) the **public TD Communication portfolio** — the `/portfolio` page + its read-only `/api/td-communication/portfolio` GET API (both single literal routes; every mutating/curator route lives under the SEPARATE prefix `/api/td-communication/admin/portfolio` and self-authenticates, so the `startsWith` public match never exposes a writer — verified by an unauthenticated call to the admin list route being redirected to login), and (Phase 16) the **public per-client landing pages** — `/site/` (trailing slash, so the match is confined to `/site/<slug>`); the page self-gates on the `landing_builder_enabled` kill-switch + `published`, and every editor/publish route is under the SEPARATE self-authenticating `/api/td-communication/projects/[id]/landing-site` prefix. Token+code-gated signing flows are also public: the legacy `/sign-document` + `/api/signature-request*`, and the **generic e-sign signer page `/sign/[token]/[code]` + `/api/sign/*` + its pdfjs worker `/esign/pdf.worker.min.mjs`** (per-signer token + access code, no Supabase auth — these MUST be in `PUBLIC_PREFIXES` or external signers get bounced to the staff login).
  - No user → **background API fetches get 401 JSON; navigations get the login redirect** (2026-07-07). On `/api/*` paths, when the request is NOT a browser navigation (`Sec-Fetch-Mode` ≠ `navigate` AND `Accept` lacks `text/html`), middleware returns `401 { error, code: 'SESSION_EXPIRED' }` so UI fetches fail with a machine-readable cause. Previously the unconditional 307→login made the browser re-POST the login PAGE, which answered 405 + HTML — surfaced as a bare "Unknown error" toast in the offer dialog (the incident that created the error-auto-audit system; contract + client-side reference implementation in `error-auto-audit.md`). Page navigations and direct links to `/api/` downloads keep the redirect to `/portal/login` (portal paths) or `/login` (dashboard). The banned-user branch follows the same split: background API calls get `401 code: 'ACCOUNT_SUSPENDED'` + `sb-*` cookies cleared; navigations keep the `?reason=suspended` redirect.
  - **Portal paths** require role `client` (admins are allowed through for debugging).
  - **Dashboard paths**: a `client` is bounced to `/portal`; admin-only paths (`/dev-tools`, `/team-management`) require admin email/role, else redirected home with `?denied=admin_only`.
  - **Sandbox guard**: `SANDBOX_MODE=1` blocks all `/api/webhooks` (503) so external traffic can't mutate sandbox data.
  - **Read-only "View as client" lock**: when a valid signed `td_view_as` marker cookie is present, middleware returns **403** for any mutating method (POST/PUT/PATCH/DELETE) on `/portal` + `/api/portal` — the single chokepoint that makes an admin's client view read-only (blocks API writes AND server actions). `/portal/view-as` is a public path (token-gated; mints/tears down its own session). Full feature in `portal.md`. Marker signing/verify: `lib/portal/view-as.ts` (HMAC over `API_SECRET_TOKEN`, edge+node safe).
- `isDashboardUser()` is the staff guard reused by API routes (e.g. `requireStaff()` in the message-actions route).

## Portal login email vs contact email
- The **login email** (`auth.users.email`) is the credential a client signs in with; the **contact email** (`contacts.email`) is the CRM/business address. They can legitimately differ.
- **The login follows the contact email automatically.** Any contact-email change — `crm_update_record`, the core `updateContact`, or the inline contact/account email field — calls `syncPortalLoginEmail` (`lib/operations/portal-login-email.ts`): it resolves the login **by `contact_id`** (never by matching emails), and if the new email is free it updates the login (Supabase admin `updateUserById` migrates users+identities, so the client signs in with the new email and the **same password**) and emails the client their new login.
- **Conflict guard:** if the new email already belongs to **another** login, the sync is **skipped + flagged** (the contact email still updates) — identities are never merged. Because some clients log in with an email different from their contact email on purpose (and some have two logins), a blanket "force every login = contact email" is unsafe — backfills are reviewed per-client.

## MCP server auth (Claude Code + Claude.ai)
- `lib/oauth.ts` implements **OAuth 2.1 with PKCE** for the Claude.ai connector; **Bearer token stays active for Claude Code**.
- Tables: `oauth_clients`, `oauth_codes`, `oauth_tokens`, `oauth_users`.
- TTLs: access token **7 days**, refresh **90 days**, auth code **10 min**.
- Discovery endpoint: `/.well-known/oauth-authorization-server`. `OAUTH_ISSUER` = `NEXT_PUBLIC_APP_URL` (defaults to `td-operations.vercel.app`).
- Middleware **excludes** `/api/oauth/*` and `/.well-known/*` from session auth.

## Business rules
- **Never change `OAUTH_ISSUER`** off `td-operations.vercel.app` — it would break the Claude.ai connector (CLAUDE.md architecture note). (`QB_REDIRECT_URI` also points here, but QuickBooks is **DEAD/decommissioned since 2026-05-23** — that reason is moot.)
- The **public-path list in `middleware.ts` is security-sensitive** — adding a path there removes authentication from it. Change only deliberately.
- `SANDBOX_MODE=1` must never be set in production (it would 503 real webhooks); it exists to protect sandbox.

## How it's built
### Key files
- `middleware.ts` — the route gate (public paths, role routing, sandbox webhook block). Excludes `/api/oauth` + `/.well-known`.
- `lib/auth.ts` — RBAC helpers (`isAdmin`/`isTeam`/`isClient`/`isDashboardUser`/`getCrmRole`/`getUserDisplayName`), `ADMIN_EMAILS`, `ADMIN_ONLY_PATHS`.
- `lib/oauth.ts` — OAuth 2.1 + PKCE (token/codes/clients, `verifyCodeChallenge`, `registerClient`, `createAuthCode`, `exchangeAuthCode`), TTLs, `OAUTH_ISSUER`.
- `app/api/oauth/*`, `app/.well-known/oauth-authorization-server` — endpoints.
- `app/api/[transport]/route.ts` — the MCP entry point (Bearer + OAuth verification).

### Tables
`oauth_clients`, `oauth_codes`, `oauth_tokens`, `oauth_users`, plus Supabase `auth.users` (role in `app_metadata`).

## Gotchas, invariants & past bugs
- **Two auth worlds**: a browser session (Supabase) vs an MCP token (Bearer/OAuth). A change to one does not affect the other.
- **Don't touch the OAuth issuer domain** — auth breaks silently for the Claude.ai connector. (The QB redirect points here too, but QuickBooks is decommissioned/DEAD — no longer a reason.)
- **The public-path allowlist is the attack surface** — anything listed there is unauthenticated. Audit additions.
- **Admins are NOT blocked from the portal** (intentional, for debugging) — so "admin can see the client view" is expected, not a bug.
- **Login email ≠ contact email is normal** — resolve a client's login by `contact_id`, NEVER by matching emails. An email-match check wrongly hid the View-as button for clients whose login email had drifted (e.g. Michele Cotti: contact `michelecotti@whalecotconsulting.com`, login `cotti_michele@libero.it`). `syncPortalLoginEmail` now keeps them aligned on contact-email change.
- **MCP tool calls bypass the web sandbox guards entirely** (see `hooks-guardrails.md` / R096) — the `mcp__af7d85f2-*` connection hits production regardless of `SANDBOX_MODE`/`.env.local`.

## How to verify current state
- Read `middleware.ts` (public-path list + role routing), `lib/auth.ts` (RBAC), `lib/oauth.ts` (PKCE + TTLs), `app/api/[transport]/route.ts` (MCP auth).
- OAuth state: `SELECT count(*) FROM oauth_tokens WHERE expires_at > now();` and `SELECT client_id, name FROM oauth_clients;`
- Note (R096): sandbox via sandbox MCP / `psql`; production `execute_sql` hits production.
