# Documents & Storage
_2026-07-24 — Claude (**Drive-write helpers hardened + a sandbox can now do REAL Drive work in isolation.** Two changes under the Google-Drive layer, in service of the tax-archival reliability fix (full detail in `docs/systems/tax-returns.md` 2026-07-24b). (1) `saveFormToDrive` / `copyUploadsToDrive` (`lib/form-to-drive.ts`): ABORT-ON-UNRESOLVED — if the target subfolder / year folder can't be resolved the function now returns with errors and uploads NOTHING, instead of falling through with `targetFolderId` still = the account ROOT and dumping the package there while reporting a truthy `summaryFileId` (a silent misfile that read as "ok"); and it aborts if the folder listing fails, because a null name-map turned the stable-name UPSERT into a blind CREATE (duplicate PDFs/files on a retry). Also NEW optional `resolveBucket(path)` so a single submission's files are downloaded from the RIGHT bucket per-path — portal-wizard uploads in `onboarding-uploads`, legacy external-form uploads in `tax-form-uploads` — a fixed bucket copied 0 external files. (2) `lib/google-drive.ts`: the 12 `SANDBOX_MODE === '1'` mock checks are now one `driveMocked()` helper = `SANDBOX_MODE==='1' && GOOGLE_DRIVE_LIVE!=='1'`. This DECOUPLES the Drive mock from the global sandbox flag (which also gates email/webhooks): setting `GOOGLE_DRIVE_LIVE=1` makes Drive do REAL work in sandbox WITHOUT re-enabling email/webhooks, provided `GOOGLE_SHARED_DRIVE_ID` points at a SEPARATE test Shared Drive (a dedicated isolated one, `0ABz0eJKly9bkUk9PVA`, was created — never the prod drive `0AOLZHXSfKUMHUk9PVA`). FAIL-SAFE: any misconfig (either var unset/empty) degrades to MOCKED — never a production Drive write; production leaves both unset so nothing changes. Proven via `scripts/sandbox-qa/verify-sandbox-drive-live.mts` (real folder/file created + listed + trashed in the isolated drive). Suite 7043 green.)_
_2026-07-23 — Claude (The server OA renderer (`lib/pdf/operating-agreement-pdf.ts`) gained a DRAFT mode: `draft: true` stamps every page and rewrites the preamble + IN WITNESS WHEREOF recital to their unexecuted form, so the client-facing "download a copy to read" is a clearly-marked draft that can never be passed off as signed. Filing behaviour is unchanged — the executed instrument still goes through the signed-PDF path. Full behaviour in `docs/systems/lease-oa.md` under "OA draft download & sign-by-hand".)_
_2026-07-22d — Claude (**TD's office address now lives in ONE place — `lib/td-address.ts` — with its three PURPOSES kept apart.** It had been hand-written in ELEVEN source files, and the comment in `app/portal/flows/[id]/page.tsx` claimed it lived in two others and NAMED THE WRONG ONES. On the day the office moves, whoever trusts that comment updates a few sites, misses the rest, and a client posts their ORIGINAL wet-ink W-7 plus passport copies to a dead address — unrecoverable. **Deliberately NOT one constant.** Three named exports off one source value, because they answer different questions and are equal only by coincidence today: `MAILING_DESTINATION_*` (where CLIENTS post to us — both portal surfaces + the two ITIN email templates in `lib/mcp/tools/itin-form.ts`), `CAA_IDENTITY_ADDRESS` (TD's address as Certified Acceptance Agent on the W-7, beside the EIN/PTIN/office code — a regulated identity block, not a delivery address), and `CLIENT_ADDRESS_FALLBACK` (stands in for the CLIENT's company address on the SS-4 when their account has none — semantically the client's address, borrowing ours). Collapsing them would make a new document-receiving office silently move the CAA identity block too. **SS-4 precedence untouched:** the client's mailing address wins, then their physical address, and only then the fallback (`lib/operations/ss4.ts`). Also replaced in `lib/mcp/instructions.ts` — prose the assistant repeats to staff and clients. New guard `tests/unit/td-address-single-source.test.ts` fails if anyone hardcodes it again; it strips comments first, since an example in a doc comment is not a delivery path. Test fixtures deliberately KEEP the literal — a tripwire that fires if the value changes. **KNOWN GAP, not fixable from code:** the staff workspace's mailing note at the ITIN Client Signing stage stores the address inside `pipeline_stages.stage_layout` — a DB row. It will NOT follow the constant and must be changed by hand if the office moves, and it is client-facing instruction text, so it is the riskiest remaining copy. Suite 6747 green. Dev job `0d5c7695`.)_
_Last verified against code: 2026-07-20 — Claude (**SHIP 1 of the storage/Drive remediation (dev job `527b2377`): the generic signed-upload endpoint is now STAFF-ONLY + destination-pinned, and the account Documents tab stops double-listing. **LIVE ON PRODUCTION 2026-07-20** (PR #190, merge `38630c64`) — sandbox-QA'd first, then shipped on Antonio's explicit go and VERIFIED ON PRODUCTION: a staff session gets a signed URL for a real caller prefix and 400 for `worker-attachments` / `signed-contracts` / a `..` traversal / an unlisted prefix; unauthenticated is 401; and Automatiko LLC's Documents tab renders "Not in Google Drive: 2" above the real Drive folder tree with zero duplication. The decisive client-role 403 was proven on SANDBOX ONLY — a real client session was minted there through the admin view-as flow against a synthetic fixture contact, and that was deliberately NOT repeated on production because it would mean minting a session for a real client.** Origin: Luca reported the account Documents tab showing two document sections. Investigating it surfaced far more, and a full council pass (4 core + Security/Data-Migration/QA/Web) found the worst item independently: **`app/api/storage/upload/route.ts` took `{bucket, path}` straight from the body and returned a SERVICE-ROLE `createSignedUploadUrl` for it with NO authorization of its own.** Middleware proves only that a session EXISTS — `isDashboardPath` returns false for anything under `/api` (`middleware.ts:141`), so the role block at `:324-342` never runs for API routes, and an authenticated **client portal user** (`role='client'`) passed the `!user` gate and could obtain service-role write access to ANY bucket at ANY path: overwrite another client's signed contract, write into the private `worker-attachments` bucket. FIX = two layers, both required: the route now calls `isDashboardUser` (403), and the new pure `lib/storage/upload-guard.ts::validateStorageUploadTarget` pins the request to bucket `onboarding-uploads` + the five prefixes the real callers use (`flow-uploads/`, `crm-account-uploads/`, `crm-dba-uploads/`, `crm-uploads/`, `articles/`), rejecting traversal/absolute/backslash/NUL/over-long paths BEFORE the prefix test. All five callers verified dashboard-only, so nothing client-facing breaks; adding a caller means adding its prefix here AND a test case (the test asserts the prefix list and the caller cases stay in sync). **`documents` tab double-listing:** `AccountDocumentsList` rendered EVERY documents row above the Drive-only `FileManager`, so any Drive-backed doc appeared twice — once flat, once in its folder (introduced 2026-06-20 to surface Storage-fallback rows, "list ALL" from its first version, not a later regression). It now filters via pure `lib/documents/list-visibility.ts` and is retitled **"Not in Google Drive"**. The rule is "carries only what has nowhere else to appear": a sentinel pointer (`storage:…`, `ss4-live:…`, or anything with a colon — a Drive file id never has one, so future sentinels are covered without a code change), a null/blank pointer, **OR the account has no `drive_folder_id` at all** — that last clause is load-bearing, because `FileManager` then renders only its "No Google Drive folder" empty state and a sentinel-only filter would hide those rows from BOTH views (a council reviewer caught this; the production count for that class was 0 at ship time but it is handled by construction, not by trusting the snapshot). **NOTE for future authors:** this repo compiles with `strict: false`, where narrowing a discriminated union on a boolean `ok` discriminant does NOT work — `upload-guard.ts` deliberately returns a FLAT `{error, status, bucket, path}` shape and callers branch on `error`. 37 new unit tests; 6524 total + build green. **Still open from the same council pass (NOT fixed here):** 47 anon/public RLS policies with `qual='true'` on `tax_return_submissions`/`ss4_applications`/`itin_submissions`/`banking_submissions`/`formation_submissions`/`contracts`/`offers`/`lease_agreements`/`oa_agreements`/`action_log` and more — a single unauthenticated PostgREST request dumps SSN/ITIN/tax data and the audit log, and PATCH rewrites them; buckets `assets`/`formation-uploads`/`public-assets`/`td-operations` are `public=true` (passports + chat attachments on permanent public URLs); ~17 client-page upload paths still write browser-direct with the anon key and six signing uploads never check `res.ok`; the `storage:` sentinel has TWO grammars (e-sign writes bucket-qualified `storage:signed-documents/…` — `app/api/documents/[id]/preview` already mis-parses it today).**)_
_Prior: 2026-07-19d — Claude (**NEW: free-form text → PDF (`lib/pdf/letter-pdf.ts`, tool `pdf_create`, dev job 0c8897f5).** Until now every PDF here FILLED A KNOWN TEMPLATE — W-7, 1040-NR, SS-4, invoice, lease, intercompany agreement. Nothing could take arbitrary text and produce a printable page, which is why Luca had to reformat the IRS name-change letter by hand on 10 July. The new renderer does letterhead, date line, reference lines, `##`/`**` headings, `- ` bullets, word wrap and automatic page breaks, with EMBEDDED Unicode fonts — the default PDF fonts cannot encode Italian accents and throw, so `embedUnicodeFonts` is mandatory for anything client-facing. Deliberately NOT a layout engine: no tables, columns or images. If a document needs those, add a real template under lib/pdf/ rather than extending this. **WHERE THE OUTPUT GOES — read this before wiring anything to it:** the generated file lands in the PRIVATE worker-attachments bucket and is handed back as a 24-hour SIGNED link. It is NOT filed to Drive and NOT attached to the client record, deliberately: those are client-visible acts and belong behind an approval, and these documents routinely carry an EIN and a home address, so a permanent public URL would be a quiet leak. Filing a generated PDF into the client's Drive folder is still the normal documents path (uploadBinaryToDriveUpsert + an autoSaveDocument row so it appears in the CRM — a Drive upload WITHOUT the index row is invisible in the account Documents tab, the failure that produced 171 dead 'No link' rows). 7 renderer tests cover multi-page flow, accents, empty body and a 4000-char unbroken token.**)_
_Last verified against code: 2026-07-18f — WINDOWED OCR: documents longer than Google's per-call page limit are now readable. Document AI's synchronous endpoint refuses a PDF over 15 pages (non-imageless; 30 needs an allowlist we are not on), so a 35-page scanned tax return failed WHOLESALE — and that same failure ran in the STORE path too (`processFile` → `extractTextFromFile` → `ocrDriveFile`), so `documents.ocr_text` was never written for long scans and every later read found nothing saved. THREE changes, all additive: (1) `ocrDriveFile(fileId, opts?)` gained optional `pages` (an inclusive 1-based range), `pageLimit` and `maxBytes`. When `pages` is set AND the file is a PDF, it splits locally with pdf-lib and sends ONLY that window. OPT-IN by design — all 14 existing call sites pass nothing and execute the same bytes as before; a windowing DEFAULT would silently feed truncated documents to passport/ITIN identity writeback and to the stored-text pipeline. Raster images (TIFF/GIF/JPEG/PNG/BMP/WEBP) NEVER go through the splitter — pdf-lib cannot load them — so fax receipts and ID photos keep their exact path. (2) The duplicated Document-AI response parser (previously verbatim TWICE, in `ocrDriveFile` and `ocrRawContent`) is now one pure `parseDocaiDocument` + one `processWithDocai`. Anchors are resolved INSIDE the response that produced them and never across a concatenation — slicing merged text with per-response anchors silently returns another page's content. (3) `OcrResult` gained `documentPageCount` (the TRUE length from the local split) and `windowStart` (absolute page of `pages[0]`). `pageCount` still means pages OCR'd; conflating the two makes a reader report '15 pages' for a 35-page return. SIZE: `DOCAI_INLINE_MAX_BYTES` (15MB, the per-REQUEST ceiling, unchanged default) is now distinct from `DOCAI_SPLIT_MAX_BYTES` (40MB, only for a windowed read) — the inline limit constrains what we SEND, not the file we split. The pre-download metadata check is KEPT (raised, not removed): an oversize file must be a returned error, never an OOM kill, which no catch block can observe. The phrase 'too large' in that error is load-bearing — `lib/itin/finalize-approval.ts` string-matches it. Encrypted PDFs are NOT loaded with `ignoreEncryption` (which yields blank pages that OCR to nothing and read as 'poor scan'); the load is allowed to throw so 'locked' stays distinguishable from 'blank'. STORE PATH: `extractTextFromFile` keeps its single whole-document call as the fast path and falls back to a bounded window loop (`MAX_STORE_WINDOWS` = 8 → 120 pages) ONLY on a page-limit error, sequentially (the client throws on the first non-2xx with no backoff, so parallel windows would turn a rate-limit into the very failure being fixed). New pure module `lib/docai-windows.ts` holds all the arithmetic + the coverage contract; `STORED_PAGE_DELIMITER` is now the single source of truth shared by the writer (`lib/mcp/tools/doc.ts`) and the readers. `MAX_OCR_TEXT` stays 50k — DECIDED, not inherited: raising it is blocked behind fixing the document lister, which SELECTs the whole column just to slice a 500-char preview. Safe meanwhile because the cut is no longer silent (see the coverage contract in `ai-agent.md`). Tests: `tests/unit/docai-windows.test.ts` (34) + `tests/unit/docai-window-read.test.ts` (11, ground truth — a 35-page PDF whose page n is uniquely identifiable by width, a fake Document AI that reports back the widths it actually received, and assertions on identity/conservation/boundaries/call-count; negative control: an always-first-window regression fails 7 of 11). Council pass 2, dev job `ee4d1df6-fa26-4e3d-904e-a59532d1665c`. Prior: 2026-07-17 — Claude (**Drive folder listing now reports truncation, and year-folder creation refuses to duplicate.** `listFolder` (`lib/google-drive.ts`) adds `nextPageToken` to its fields mask — it was stripped, so callers could not tell a full folder from a truncated one; ~60 call sites all read `.files` only, so the extra key is purely additive. `findOrCreateYearFolder` now lists with 100 (was the 50 default) and THROWS on a page token instead of creating a second `{year}` folder — a duplicate year folder is durable corruption (writer archives into the invisible twin, readers resolve the other, the client's workbook becomes unfindable). Both came out of the accountant-hand-off wrong-year fix, where a truncated listing made a safety guard pass by accident; full story in tax-returns.md 2026-07-17. See the two new bullets under "Gotchas".)_
_Prior: 2026-07-14 — Claude (**Invoice PDF now prints the REQUIRED PAYMENT REFERENCE.** `lib/pdf/invoice-pdf.ts` renders "Payment Reference (required): INV-xxxxxx" under the bank-details block, with a one-line instruction to include it in the transfer. Only change to this subsystem. **Why it matters:** for a WIRE, the reference the client types is the ONLY identifying signal that exists — there is no payer email, no card metadata, nothing else. Without it a wire can be matched on its amount alone, which is how payments end up unreconciled or, worse, one click from being credited to a different client with a similar balance. The portal Pay modal already demanded the reference; the emailed invoice already carried it; the PDF — the thing clients actually open to read the bank details — did not. Part of the payment-reconciliation rebuild; full rationale in **banking-bankfeed.md** (2026-07-14).)_
_Prior: 2026-07-07c — Claude (Drive↔documents SYNC AUDIT + "No link" fix. Audit of all 4,736 index rows + Drive existence check on 224 sampled ids (45-day recent + all LT Program rows): 0 missing drive_file_id, 0 duplicate index rows, 0 ids pointing at hard-deleted files. FIXED: (1) `autoSaveDocument` never wrote `drive_link`, and the CRM account Documents tab renders a clickable link ONLY from `drive_link` → 171 rows showed a dead "No link" (Banking/Lease/SS-4/ITIN, new ones still appearing in July). Writer now derives `drive_link` from the Drive file id — but NEVER for sentinel ids (`storage:<path>`, `ss4-live:<token>` — those carry their own signed/relative links); 171 legacy rows backfilled in prod (batched, verified 0 remaining). The banking summary-PDF insert in wizard-submit now routes through autoSaveDocument (gets the link + drive_file_id idempotency). (2) 2 rows pointed at TRASHED Drive files: Damiano Mocellin's portal-visible Schedule OI repointed to its live replacement; an orphaned stale signed-SS-4 index row (superseded by a good row) deleted. NOTE: `ss4-live:` sentinel rows are BY DESIGN (live-rendered unsigned SS-4, relative drive_link to the render route — lib/operations/ss4.ts). Known remaining noise: 61 old status=error rows (Document AI OCR limits, ≤April) + 78 client-unlinked rows, mostly the same batch — files fine on Drive, not sync breaks.)_
_Prior same day: 2026-07-07b — Claude (FULL Drive-upload dedup sweep, Antonio's directive "fix everything related to google drive": every AUTOMATED re-runnable upload path now carries a duplicate guard. New `fileExistsInFolder(folderId, name)` single-file check in `lib/google-drive.ts` (error → exists:false = default-to-upload). VERBATIM copies (skip-if-name-exists, prior run also did OCR/documents insert so whole block skips): onboarding-setup doc_copy + member passports, formation-setup owner passport, formation-materialize owner+member passports, tax-return-intake doc_copy, ITIN MCP passport copies. GENERATED/stable-name artifacts (uploadBinaryToDriveUpsert — overwrite in place): W-7/1040-NR/Schedule OI (route + MCP), P&L Excel (tax-form-setup, tax-form-completed, bank_statement_pnl MCP), form-summary PDFs, signed-PDF webhooks (ss4/lease/oa/8832/offer/signature-request/esign — webhook retry refreshes, never duplicates), lease regen routes, renewal receipts (file-renewal), closure letters, HC delivery downloads, Gmail-attachment saves (MCP + agent). DELIBERATELY untouched: interactive one-shot upload endpoints (portal/CRM/flows/dba/workflows uploads, drive_upload* MCP tools, agent generic upload) — a human re-uploading a same-named file may intend a new copy; and `lib/copy-uploads-to-drive.ts` which has ZERO importers (dead code, flagged for separate cleanup). Prior same-day entry below.)_
_Prior 2026-07-07 — Claude (Drive duplicate-upload guard after the LT Program incidents: `lib/google-drive.ts` gained `folderFileNameMap` (paginated name→id map of a folder, null on failure), `updateBinaryFile` (binary files.update PATCH), and `uploadBinaryToDriveUpsert` (overwrite-in-place when a same-name file exists, else create — for regenerated artifacts with stable names: P&L Excel, form-summary PDFs). `copyUploadsToDrive` (`lib/form-to-drive.ts`) now SKIPS file names already present in the destination folder (upload names embed a per-upload hash, so name-equality = same object) and returns a `skipped` array; `saveFormToDrive` upserts the summary PDF. Failure posture everywhere: if the existence check fails, default to uploading — a stray duplicate beats a silently missing file.)_
_Earlier 2026-07-02 — Claude (`lib/pdf/itin-data-summary.ts` deleted — dead code, zero callers; see "PDF generation" section note. No behavior change to any live document path.)_
_Earlier 2026-06-25 — Claude (`autoSaveDocument` (`lib/portal/auto-save-document.ts`) `confidence` was hardcoded to the numeric `1.0`, which VIOLATES the production `documents_confidence_check` CHECK constraint (`confidence IN ('high','medium','low')`) — so EVERY autoSaveDocument insert in production (ITIN W-7/1040-NR/Schedule OI, signed OA / lease / contract) failed and the error was swallowed by callers, the root cause of "docs in Drive but not in the documents table" (Daniel Pasztor). Sandbox has NO such constraint, which masked it across all prior testing. Fixed: `confidence: 'high'`. Verified against prod: constraint def = `CHECK (confidence = ANY (ARRAY['high','medium','low']))`, existing rows are 3670 'high' + 383 'low'. The companion `app/api/itin-form-completed` change (shipped in `fix/itin-workspace`) now checks autoSaveDocument's return and logs the real error instead of reporting "ok".)_
_Earlier 2026-06-20 — Claude (CRM **account Documents tab** now lists ALL `documents` rows for the account via a new `AccountDocumentsList` (`components/accounts/account-documents-list.tsx`), rendered above the Drive-only `FileManager`. Reason: the new Company Formation flow uploads via the Storage fallback (`onboarding-uploads`, `drive_file_id='storage:…'`, signed-URL `drive_link`) and never creates a Drive folder — so an account could show a doc count (e.g. "4") while `FileManager` rendered only its "No Google Drive folder" empty state. The list links each row to `drive_link` (Drive URL in prod, signed Storage URL for fallback uploads) and shows portal-visibility. Data was already fetched by the account page; no new query. Note: fallback `drive_link` is a 1-year signed URL — long-term re-signing is a separate follow-up.)_
_Earlier 2026-06-16 — Claude (ITIN IRS-form fillers updated: the Certified Acceptance Agent mailing address printed on the W-7 (`lib/pdf/w7-fill.ts` `AGENT`) and the preparer address + `CAA_HOME` on the 1040-NR (`lib/pdf/1040nr-fill.ts`) — where the IRS routes ITIN correspondence — now use TD's document-receiving office **Seminole, 11125 Park Blvd Suite 104-153, FL 33772** (was the old Largo address). SS-4/8832 fillers and PDF footers were intentionally left on the existing address.)_
_Earlier 2026-06-16 — Claude (`autoSaveDocument` (`lib/portal/auto-save-document.ts`) gained an optional `serviceDeliveryId` param, written to `documents.service_delivery_id` on insert (untyped — column not in generated types). Lets flow-generated docs link to their service_delivery so they surface on the flow workspace / portal flow page (which query by `service_delivery_id`). Used by the ITIN doc generator (`app/api/itin-form-completed`) for W-7/1040-NR/Schedule OI. Prior 2026-06-11: unified share-path alerting via updateDocument choke point + chat-on-share flag + document_reprocess self-heal job + OCR audit CHECK 40; 2026-06-06 new-document client alerts.)_

## What it is
How files are stored, processed, classified, indexed, and generated. Three storage surfaces + a processing pipeline + PDF generation.

## The three storage surfaces
1. **Google Drive** — the primary document store: the **"Tony Durante LLC" Shared Drive** (ID `0AOLZHXSfKUMHUk9PVA`). Access via a **Service Account + Domain-Wide Delegation impersonating `support@tonydurante.us`** (`lib/google-drive.ts`; SA key in `GOOGLE_SA_KEY` base64 → JWT → token). Tools: `drive_upload` (**text**), `drive_upload_file` (**binary** — PDF/images), `drive_list_folder`, `drive_search`, `drive_move`, `drive_rename`, `drive_delete`, `drive_create_folder`, `drive_read_file`, `drive_get_file_info`, `drive_file_id`, `drive_folder_id`.
2. **Supabase Storage** — the `td-operations` bucket for app/internal files (SOPs, project files). Tools: `storage_read/write/list/move/delete`. **Storage files are auto-mirrored to Google Drive.**
3. **`documents` table** — the CRM **index** of processed documents (not the bytes — Drive holds those). Columns include `account_id`, `contact_id`, `file_name`, `document_type_name`, `category`, `drive_file_id`, `status`, `confidence`, `processed_at`, `portal_visible`.

## The document-intelligence pipeline
`lib/mcp/tools/doc.ts`: **Drive file → OCR → classify → store in `documents`**.
- `doc_process_file` (one), `doc_process_folder` (batch flat), `doc_process_client` (recursive client tree, subfolders 1–5), `doc_bulk_process` (all docs for a CRM account, auto-resolves the Drive folder).
- `doc_search` / `doc_list` / `doc_get` / `doc_stats`.
- `doc_map_folders` — links orphan documents to CRM accounts via Drive folder ancestry.
- `doc_compliance_check` / `doc_compliance_report` — required-vs-present docs per client.
- `doc_mass_process`, `doc_update_health`.

### Classification
`lib/mcp/tools/classify.ts` + `lib/classifier.ts`: **DocAI OCR** (scanned PDFs/images) + **40+ regex rules** (ported from `gdrive-file-classifier.py`). Categories: **Company, Contacts, Tax, Banking, Correspondence**. Tools: `classify_document`, `classify_text`, `classify_list_rules`, `docai_ocr_file`.

## PDF generation (`lib/pdf/`)
Form fillers and generators: `ss4-fill` (EIN), `w7-fill` (ITIN W-7), `8832-fill`, `1040nr-fill`, `tax-form-pdf`, `invoice-pdf`, `intercompany-agreement-pdf`; helpers `sanitize`, `unicode-fonts`, `wrap-text`. (`itin-data-summary` was DELETED 2026-07-02 — dead code with zero callers since April; the live ITIN data summary is generated by `lib/form-to-drive.ts` `FORM_CONFIGS.itin`, and the dead file embodied the fixed-column label/value pattern that caused the 2026-07-02 accountant-PDF overlap bug. Never draw a field value at a hardcoded x — see `drawFieldRow` in `lib/form-to-drive.ts`.)
- **`invoice-pdf` renders negative line items with a leading minus** (e.g. a `Credit applied` line shows `-$500.00`, not `$500.00`) so a credit reads as a deduction, not a charge — matches the same fix in the invoice detail dialog (2026-06-02).
- **`letter-pdf` letterhead has THREE states, and `null` means BARE (2026-07-29, dev job `6ffd2d5e`).** Absent ⇒ the firm default `Tony Durante LLC`; `''` **or** `null` ⇒ no letterhead; a string ⇒ that string. It previously used `opts.letterhead ?? DEFAULT_LETTERHEAD`, which folds `null` into the default, so only `''` suppressed the header — the more obvious way to say "no letterhead" produced one. **The `pdf_create` handler change is inseparable from this:** it used to pass `letterhead: letterhead ?? null`, and under the new meaning that converts "caller said nothing" into "caller asked for none", stripping the header off EVERY generated document. It now passes the value through unchanged. `renderLetterPdf` has exactly one caller, which is why the meaning of `null` is pinned in the renderer rather than at the call site — if a second caller ever appears, re-read this before wiring it. Verify by rendering and re-extracting the text for all four cases, not by reading the code — `tests/unit/letterhead-contract.test.ts` does exactly that, and also source-guards the handler against the `?? null` edit, because the previous suite asserted only `getPageCount() === 1`, which is true of every possible implementation including "header always dropped". **The `pdf_create` schema is `z.string().nullable().optional()`** — a bare `.optional()` REJECTS explicit null, so an assistant reaching for `null` to mean "no header" failed validation and the header survived, a symptom indistinguishable from the original bug.

## Auto-save & portal visibility
`lib/portal/auto-save-document.ts` inserts a `documents` row (e.g. when an offer is signed) — deduped by `drive_file_id`, `status='classified'`, and a **`portal_visible`** flag (default false) that controls whether the client sees it in the portal. `lib/portal/document-templates.ts` generates templated docs.

**New-document client alerts (2026-06-06, unified 2026-06-11):** when a document becomes client-visible, the client gets a portal notification + push (email for non-push users via the digest), an optional portal **chat message** ("A new document has been added to your folder: <name>", flag `new_document_chat_message_enabled`, default ON per Antonio 2026-06-11), and the doc shows as "New" in the portal until opened. **Dispatch is owned by the `updateDocument()`/`updateDocumentsBulk()` choke point** (`lib/operations/document.ts`): any patch that actually transitions `portal_visible` false→true fires `notifyClientsOfNewDocument()` fire-and-forget — covering the file-manager toggle, process-and-share, the signature webhook, and every future caller. Single updates alert by default (`clientAlert: false` to opt out); bulk defaults to NOT alerting (portal-transition migrations would flood clients). The two admin upload routes call the module directly (raw insert, not updateDocument). Extra `documents` columns: `notify_client` (per-doc opt-out, default true) + `client_notified_at` (idempotency + baseline). Per-contact opened state lives in `portal_document_views`. Full behavior in `portal.md`.

**OCR-outage resilience (2026-06-11, after the Document AI billing outage sat unnoticed 2 days):** `process-and-share` on a `status='error'` doc still shares it (client first) but keeps the honest error status — it NEVER fakes `classified` — and queues a **`document_reprocess` job** (`lib/jobs/handlers/document-reprocess.ts`, max 12 attempts ≈ 1h of 5-min cron retries, dedup on pending jobs per document) that re-runs OCR+classification when the backend recovers. Longer outages surface via **audit-health-check CHECK 40** (`recent_document_ocr_errors`, P1): documents with errors in the last 24h appear in the daily 9am alert email.

## Business rules
- **`drive_upload` = text, `drive_upload_file` = binary** (PDF/images). Using the wrong one corrupts binary content.
- **`portal_visible` gates client visibility** — only flagged documents appear in the portal; client-visible document deletion uses soft-delete (R100).
- The Service Account impersonates `support@tonydurante.us` (DWD) for all Drive/Gmail access.

## How it's built — key files
`lib/google-drive.ts` (SA/DWD + `SHARED_DRIVE_ID`), `lib/docai.ts` (OCR), `lib/classifier.ts` (rules), `lib/pdf/*` (generation), `lib/mcp/tools/{doc,classify,docai,drive,storage}.ts`, `lib/portal/auto-save-document.ts`, `lib/portal/document-templates.ts`. Tables: `documents` (index) + Google Drive (bytes) + the Supabase `td-operations` bucket.

## Gotchas, invariants & past bugs
- **`listFolder` does NOT paginate — it returns at most 100 files, and it now TELLS you when it truncated** (2026-07-17). The fields mask requests `nextPageToken` alongside `files(...)`; without it in the mask Drive omits the token entirely, and a caller cannot distinguish "exactly 100 files" from "100 of many". Any code that reasons about the COMPLETE set of files in a folder (rather than just finding one) must check `nextPageToken` and refuse rather than guess — counting rows is not a substitute (a folder of exactly 100 is complete, and a short page can still have more behind it). Precedent: the accountant hand-off's "two files match this year → stop and ask a human" guard silently passed BY ACCIDENT on a truncated listing, which is worse than having no guard (`lib/mcp/tools/tax.ts`, see tax-returns.md). `folderFileNameMap` is the reference implementation of a real pagination loop.
- **`findOrCreateYearFolder` refuses rather than create on a truncated listing** (2026-07-17) — a duplicate `{year}` folder is durable corruption: the writer archives into the invisible twin while every reader resolves the other, so the client's file becomes unfindable, and no downstream check can detect it. It lists with 100 and throws when a page token is present. Any other find-then-create folder helper should do the same.
- **`documents` is an INDEX, not the file store** — `drive_file_id` points at the real bytes in Drive. Deleting a `documents` row doesn't delete the Drive file (and vice-versa).
- **Wrong upload tool corrupts binaries** — never `drive_upload` a PDF; use `drive_upload_file`.
- **Folder ancestry matters** — `doc_map_folders` links docs to accounts by Drive folder tree; misfiled documents won't auto-link.
- **Shared Drive ID + SA key are fixed config** — `0AOLZHXSfKUMHUk9PVA` / `GOOGLE_SA_KEY`; a bad SA key fails all Drive ops silently-ish (auth error).
- **Supabase Storage auto-mirrors to Drive** — a `storage_write` also lands in Drive; don't double-upload.
- **Drive uploads must be duplicate-guarded (LT Program incidents 2026-06-16 + 2026-07-07)** — Drive happily stores many files with the same name, so any re-runnable code path that uploads to a client folder needs a guard: verbatim client uploads → skip when the name already exists (`folderFileNameMap` + skip, as in `copyUploadsToDrive` / onboarding `doc_copy`); regenerated artifacts with stable names (P&L, form-summary PDFs) → `uploadBinaryToDriveUpsert` (overwrite in place). A failed existence check defaults to uploading — never silently omit. The 2026-06-16 runaway re-ran onboarding-setup every ~30s for ~2h and piled 200+ copies of every wizard upload into LT Program LLC's folder; nobody noticed until the client's tax season.

## How to verify current state
- Read `lib/google-drive.ts` (`SHARED_DRIVE_ID`, SA/DWD auth), `lib/mcp/tools/doc.ts` (the pipeline tool list), `lib/classifier.ts` (the rules + categories).
- A client's indexed docs: `SELECT file_name, category, document_type_name, portal_visible, drive_file_id FROM documents WHERE account_id='<id>' ORDER BY processed_at DESC;`
- Note (R096): sandbox via sandbox MCP / `psql`; production `execute_sql` hits production.
