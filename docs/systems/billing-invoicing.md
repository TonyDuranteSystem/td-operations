# Billing & Invoicing

_Last verified against code: 2026-08-30e — Claude (**Finance summary card — 9-reviewer full council pass, then every real blocker fixed same day.** Before committing the card described in the entry below, Antonio asked for a full council review ("run it"). Ran all 5 core reviewers + Finance-Auditor/Business-Analyst/Security/QA-Tester specialists against the real diff. Result: every reviewer that looked at the money math found real, independently-corroborated defects, and the System Counselor issued 3 cited MISMATCH-stops backed by live production queries — verdict FIX-FIRST, not shippable as-is. Fixed and re-verified live in sandbox against real data before this entry was written (not just re-reading the code): (1) **money math** — a Draft/Partial invoice past due used to match neither the overdue nor pending filter and silently read as "$0 owed" (reproduced live on `Uxio Test LLC`: the exact real Draft/past-due invoice this bug hid is now correctly counted); a partially-paid invoice was summed at its full original total instead of the remaining balance; a credit note (money owed BACK to the client) was counted as a receivable — excluded now by its `CN-` number prefix specifically because a credit tied to a split/installment-plan invoice is not necessarily issued right away (Antonio's correction, mid-review) so it can't be relied on to already carry `invoice_status='Credit'`; mixed-currency invoices were summed together under one currency's symbol — now grouped per currency, live-proven on a real sandbox account (`Active Acres Properties LLC`) that genuinely holds both EUR and USD invoices, a case a reviewer had flagged as "can't confirm in production." (2) **Control-bypass** — `sendAutopayEnrollmentLink` was the one autopay-enrollment surface in the entire codebase that didn't check the global kill switch, so a staff click could arm an account while the feature was supposed to be switched off system-wide; now checks it first, and `saveAutopayCard` independently refuses to flip the flag while the switch is off as a second backstop on the webhook-completion side — both live-verified by flipping the sandbox switch off and confirming the refusal. (3) **The client-facing message** claimed "no 5% card fee" without qualifying that the waiver only applies going forward — checked against every real outstanding production invoice, all of which were already fee-pinned at creation, meaning the original wording was false for exactly the accounts it would be sent to; reworded to say "future invoices" explicitly, and now sends the Italian variant when the resolved contact's language is Italian (roughly two-thirds of the real client base, per a live query), matching every other client-facing system string. (4) Added: a confirmation step before sending (previously only "Turn off" had one), a same-24h duplicate-send refusal, an already-enrolled refusal, a no-linked-contact refusal (previously reported false "sent" with nobody to notify), the card no longer offers autopay on non-`'Client'` account types (Vendor/Partner/One-Time), a multi-company contact no longer renders an empty card for every company with nothing to show, the contact page's own Invoices tab now uses the same due-date-aware overdue rule as the Overview card instead of contradicting it on the same screen, and both staff actions now call `router.refresh()` so the card can't read stale regardless of which page (account or contact) it was actioned from. **Deliberately deferred, flagged not silently resolved:** whether the auto-charge cron should also reach `'Overdue'`-status invoices (today it only considers `'Sent'`, so enrolling frequently does not make an account's headline "Outstanding" figure collectible) is a money-behavior decision for Antonio; the enrollment message going to the whole company's shared chat (not one specific person) needs a capability-gate extension bigger than this fix pass. Also corrected a stale fact in `.claude/skills/council/SYSTEM-KNOWLEDGE.md` the Counselor flagged: it still said the kill switch was off pending a go-live decision that had already happened. 740 test files / 10047 tests green (12 new), build clean, still on the feature branch — awaiting Antonio's review before merge.)_
_Last verified against code: 2026-08-30d — Claude (**Two updates in one entry — a stale-doc catch-up plus a new feature.** (1) **Catch-up (doc was stale, not the system):** the entries below still read "kill switch OFF" / "SANDBOX ONLY" for card autopay Phase 1 — that was true when written, but later the SAME session shipped it to production end-to-end: code pushed to `main`, the `20260830-0900-card-autopay.sql` migration applied to production (Antonio ran it by hand in the Supabase SQL editor per the standing DDL rule), the live Stripe webhook (`app.tonydurante.us/api/webhooks/stripe`) briefly gained `charge.refunded`/`charge.dispute.created` then had them explicitly removed again on Antonio's "I don't want refunds and disputes" (back to its original 2 events: `checkout.session.completed`, `payment_intent.payment_failed`), and finally `app_settings.card_autopay_config.enabled` was flipped to `true` in production on Antonio's "turn it on" — confirmed live via a fresh query the moment this doc entry was written, not assumed from memory. The section heading and kill-switch bullet below are corrected to say ON/production-live. (2) **NEW — Finance summary card + staff autopay controls (dev job `10995181` follow-up, SANDBOX ONLY, uncommitted — not yet shipped).** Antonio: "add under account and contact in overview a page 'Finance' as summary of the invoices... payment and if Auto pay is on or off. when a client activate it we have to receive a message in what's new" — then corrected twice: it's a CARD on the existing Overview tab, not a new nav item, and staff need a real toggle, not just a read-only display. Built: `lib/billing/finance-summary.ts::summarizeInvoicesForFinanceCard()` (pure, mirrors the Payments tab's own Overdue/Pending/Paid classification exactly, against a structural `InvoiceLike` type so it accepts both `Payment` (account page) and the contact page's own local `ContactInvoice` shape without either importing the other) renders via `components/shared/finance-summary-card.tsx::FinanceSummaryCard`, dropped into both `PanoramicaTab` (account Overview) and contact `OverviewTab` — one card per linked company on the contact side (a contact can belong to 2+ accounts with independent autopay states), plus a "Personal (no company)" card for contact-direct invoices with the whole autopay row hidden (`showAutopay={false}` — autopay is account-scoped only, there is no contact-level concept). **Staff actions, asymmetric by design:** "Turn off" is a REAL immediate action (`disableAccountAutopay` server action → the same `disableAutopayCard()` the client's own self-service disable calls, actor-tagged `dashboard:<name>` instead of `"client"` so the action_log/What's New wording differs correctly) — but there is no staff-side "Turn on": TD never sees or stores a client's card, so `sendAutopayEnrollmentLink` instead posts the client the same on-session Stripe `mode:"setup"` Checkout Session link they'd generate themselves, as an admin portal chat message (reusing `resolveAdminReplyContact` + `createPortalNotification`/`notifyClientOfAdminMessage`, the same primitives `portal_chat_send` uses). **New What's New event** `card_autopay_enabled` (`emitCardAutopayEnabledEvent`/`retireCardAutopayEnabledNote` in `lib/portal/chat-events.ts`) fires from `saveAutopayCard()` (the real webhook enrollment path) — the retire call fires unconditionally from `disableAutopayCard()` so a disable-then-re-enroll cycle notifies staff again instead of deduping forever against the first note. **A real bug found and fixed via mandatory browser QA (R079 earned its keep here):** the first version of `sendAutopayEnrollmentLink` set `portal_messages.sender_id` to `getDashboardActor()`'s `"dashboard:name"` audit-log string — but that column is `uuid NOT NULL`. Live in sandbox this failed with `invalid input syntax for type uuid` the first time the button was actually clicked, never caught by unit tests (which mocked the insert and never asserted the exact value) or by reading the code. Fixed to use the real auth user id from `createClient().auth.getUser()`, matching `app/api/portal/chat/route.ts`'s own `sender_id: user.id` — redeployed and re-verified live. Full sandbox browser QA: account-page toggle both directions (Turn off exercised via a direct sandbox-only call since this browser tool disables native `confirm()`; Send enrollment link click-tested live, no confirm gate), a 3-linked-company contact rendering 3 correctly-scoped cards, a 0-company contact rendering the Personal card with autopay hidden, the What's New note's "AUTOPAY" label + "Open" deep link (`deepLinkFor`'s new `'accounts'` case in `thread-whats-new-panel.tsx`) landing on the right account page. New migration `20260830-2200-seed-whats-new-card-autopay-event.sql` (a `catalog_entries` data seed, not DDL) applied to sandbox only. 740 test files / 10033 tests green, build clean — awaiting Antonio's review before commit/ship.)_
_Last verified against code: 2026-08-30c — Claude (**A SECOND, UNGUARDED CLIENT PAY ROUTE FOUND AND FIXED — same dev job `10995181`.** The claim system (`lib/operations/autopay-claim.ts`) shipped in the entry below was wired into exactly one route, `create-invoice-checkout` — the one the PORTAL Pay button calls. An automated review pass raised a specific concern that this left the *actual* Jan/June client population's real payment path unguarded. Investigated exhaustively rather than taking the claim at face value (or dismissing it): the literal claim was WRONG — active clients (the real Jan/June population) resolve to the "portal" audience and their only card-payment route already was the guarded one; a second theory (the renewal-signing page's own separate checkout call) is structurally dead code for current renewals (renewals live in `annual_agreements`, that call looks up the retired `offers` table, 404s, the button never even renders). **But the underlying instinct was right anyway — chasing it down surfaced a real, different second gap:** `app/pay/[token]/route.ts`, the OLDER email-link "Pay Invoice" route (sent to clients whose `portal_tier` resolves to `resolveInvoiceAudience()`'s "no_portal" case — no portal access), independently creates a Stripe Checkout Session against an existing invoice with ZERO claim-system integration — it was never touched when the claim system shipped. On its own this was a narrow edge (the intended Jan/June population doesn't use it), but it stopped being narrow the moment autopay's scope widened to "any payment" two entries below: any invoice for an autopay-enrolled account is now a cron target, and an account can enroll while portal-tier, then later receive some OTHER invoice that resolves "no_portal" for unrelated reasons (e.g. a status change) — landing on this exact unguarded route while still cron-eligible. **Fixed:** `app/pay/[token]/route.ts` now claims the payment (`claimPaymentForCharge`) before creating its Stripe session, releases on every exit path, and records the session id for the cron to expire — the identical pattern already proven in `create-invoice-checkout`. This closes the double-charge race across every currently-known client-facing payment-creation route, not just the one used by the population this feature was originally scoped to. Re-verified: grepped every caller of the shared Stripe-Checkout-Session helper across the repo — the only two routes that create a session against an EXISTING `payments` row are these two, both now guarded; the other two call sites (`offers/create-checkout` for brand-new contract signing, and card-autopay's own `mode:"setup"` enrollment session) never compete for an existing invoice's money and correctly have no claim. Full suite re-verified green (732/9960) after this fix; redeployed to sandbox.)_
_Last verified against code: 2026-08-30b — Claude (**CARD AUTOPAY WIDENED TO "ANY PAYMENT" + a pay-moment nudge, same dev job `10995181`, same session as the entry below.** Antonio, right after the entry below shipped: "The autopay must work in any payment they will do... When they receive a recurring invoice for any service and they click to pay it, a message must inform them to activate the autopay." Two changes: (1) **scope widened from Jan/June-only to every invoice type** — the auto-charge cron's candidate query dropped the `payment_category IN ('installment_1','installment_2')` filter entirely (now excludes only `'credit'`, since a credit note is money TD owes the client, plus a defensive `amount > 0` guard); Antonio explicitly confirmed, when asked, that a payment-plan tranche invoice (`setup_tranche`) — which an earlier 2026-08-27 council pass had rejected for auto-charge on narrower grounds no longer fully applicable now that a saved card + failure path exist — should be included too. The fee waiver was similarly de-scoped from two hand-wired call sites to the SINGLE choke point every TD invoice already goes through: `createTDInvoice`'s existing `card_fee_rate` resolution now calls a new `resolveCreationCardFeeRate()` (`lib/payments/card-fee-config.ts`) that checks `isAccountAutopayEnabled()` (`lib/operations/card-autopay.ts`) whenever the caller passes no explicit rate — so EVERY invoice-creation path benefits automatically, not just the two the feature started with; the two now-redundant per-call-site overrides in `agreement-signed`/`annual-installments` were removed. (2) **A "activate Autopay" nudge now shows inside the Pay dialog itself** (`components/portal/td-pay-modal.tsx`), not only on the static Expenses-tab card — a new `GET /api/portal/autopay/eligibility?payment_id=X` resolves the invoice's account and tells the modal whether to show it (global switch on AND this account not yet enrolled); clicking it starts the same enrollment flow as the Expenses-tab button. **Stripe → sandbox notifications, the other half of Antonio's ask ("fix Stripe to notify our sandbox server"):** created a NEW Stripe test-mode webhook destination (`td-operations-sandbox`, 4 events: `checkout.session.completed`/`payment_intent.payment_failed`/`charge.refunded`/`charge.dispute.created`) pointed at the sandbox deployment — the PRE-EXISTING test-mode destination pointing at `app.tonydurante.us` was left untouched (R015). Two more real bugs found and fixed while wiring this up, neither caught by the first round's QA because it never got far enough to exercise them: (a) the enrollment endpoint's Stripe redirect (`success_url`/`cancel_url`) pointed at a page (`/portal/payment-settings`) that doesn't exist and appended `?autopay=...` onto a `returnUrl` that can already carry its own `?tab=expenses` query string, producing a malformed double-`?` URL — fixed: correct path (`/portal/invoices?tab=expenses`) + a `?`-vs-`&` joiner in `createAutopaySetupCheckoutSession`. (b) Stripe's POST to the sandbox webhook was blocked with a 401 by Vercel's own Deployment Protection (SSO wall) — same class of block as `[[reference_sandbox_instant_job_trigger_blocked]]` — fixed by fetching the project's "Protection Bypass for Automation" secret from the Vercel Projects API (not visible via `vercel env ls`; lives under the `protectionBypass` key) and appending it as `?x-vercel-protection-bypass=<secret>` on the Stripe destination's URL. **After both fixes, a THIRD, deliberate blocker surfaced that is NOT a bug:** the request now reaches the sandbox app and is rejected with a `503 "Service Unavailable (sandbox mode)"` — this is `SANDBOX_MODE=1`'s own middleware guard (documented in CLAUDE.md's Sandbox Environment section: "`SANDBOX_MODE=1` middleware blocks all `/api/webhooks/*` with 503"), a standing, intentional safety boundary that applies to every webhook, not something specific to this feature. **Conclusion, re-verified from first principles rather than assumed:** card-autopay ENROLLMENT COMPLETION (the webhook step) cannot be tested end-to-end in sandbox by design — not a gap to close, a boundary to respect. Everything upstream of that boundary (auth gating, the kill switch, real Stripe Customer + `mode=setup` Checkout Session creation with the correct pre-filled email, the enabled/disabled portal UI, self-service turn-off with `action_log` audit) was independently browser-verified live in sandbox, twice, including through both bug-fix redeploys. Full loop closure is only provable in production, behind the still-OFF kill switch.)_
_Last verified against code: 2026-08-30 — Claude (**CARD AUTOPAY — PHASE 1 (card only), SANDBOX ONLY, dev job `10995181`, kill switch OFF by default.** Antonio asked for clients to be able to save a card and have future invoices charge automatically — targeted, on his own clarification, at the January/June annual-renewal installments specifically (`payment_category` `installment_1`/`installment_2`), not the rarely-used payment-plan/tranche system. A 2026-08-27 council pass had already REJECTED unattended card auto-charge for the tranche case (see the `offers.md` era marker) — this build does not reverse that; it targets a different, narrower surface and closes the specific blocker (a real double-charge race) that sank the earlier design. **The hard problem, closed on the third design pass:** the client's own "Pay Invoice" click (`app/api/workflows/create-invoice-checkout/route.ts`) and this new unattended cron can both act on the same invoice. Fix is two-part: (1) `payments.charge_claimed_until` — an atomic claim, `lib/operations/autopay-claim.ts::claimPaymentForCharge`, a single conditional `UPDATE ... WHERE (charge_claimed_until IS NULL OR < now()) AND status != 'Paid' RETURNING id` — checked and re-verified in the SAME statement, so a webhook flipping the row Paid mid-check can't slip through. Whichever caller's UPDATE returns a row may touch Stripe; the other must not. (2) Stripe enforces a 30-MINUTE MINIMUM on how soon a Checkout Session can expire, so the DB claim alone cannot stop a client's already-open payment page from completing after the cron has already charged — `payments.stripe_checkout_session_id` records the client's live session, and the cron ACTIVELY calls `stripe.checkout.sessions.expire()` on it before charging (catching the case where the client's session raced to `complete` first, in which case the cron backs off and lets the client's own webhook settle it). **New columns** (migration `20260830-0900-card-autopay.sql`, sandbox-applied, NOT yet promoted): `accounts.autopay_card_enabled`/`autopay_stripe_customer_id`/`autopay_stripe_payment_method_id`/`autopay_card_last4`; `payments.charge_claimed_until`/`stripe_checkout_session_id`. **Enrollment** is on-session, Stripe's own recommended pattern: `POST /api/portal/autopay/setup-checkout` (`lib/operations/card-autopay.ts`) creates/reuses a Stripe Customer then a Checkout Session in `mode: "setup"`; the Stripe webhook's NEW branch intercepts `checkout.session.completed` on `session.mode === "setup"` BEFORE the existing `handleCheckoutCompleted` (which assumes every completed session is a $-bearing payment and, left unguarded, would have tried to process a $0 "payment" and could even have matched it to an unrelated `pending_activations` row by email — a real latent bug this build closes as a side effect, not the point of the change) — retrieves the SetupIntent's PaymentMethod and saves it via `saveAutopayCard()`. Self-service turn-off: `POST /api/portal/autopay/disable` best-effort detaches the Stripe PaymentMethod and always clears the DB flags either way. **The auto-charge cron** (`app/api/cron/card-autopay-charge/route.ts`, daily 11:00 UTC) finds Sent/unpaid/due installment invoices for enrolled accounts, claims, expires any live client session, creates an off-session PaymentIntent with a deterministic Stripe Idempotency-Key (`card-autopay:{payment_id}`), books the fee (if any) via the existing `bookCardFee()` choke-point, and settles via the canonical `confirmPayment()` so the pre-existing installment-1/2 side effects still fire unchanged. A decline or a 3DS challenge that can't complete off-session releases the claim, raises a staff task, and notifies the client via `createPortalNotification` (which the portal-digest cron also emails) — never silent. **Incentive:** the 5% card fee is waived (`card_fee_rate: 0` passed into `createTDInvoice`) for an enrolled account's Jan/June invoice, evaluated fresh at invoice-creation time in both `agreement-signed`'s webhook (Jan) and `annual-installments`'s cron (June) — not retroactive, matches how `card_fee_rate` is pinned everywhere else. **Kill switch:** `lib/payments/card-autopay-config.ts::isCardAutopayEnabled()` reads `app_settings.card_autopay_config.enabled`, DEFAULTS FALSE (unlike the card-fee switch, which defaults true) — a missing config row must mean "not live," never "on," since this is genuinely new unattended-charging infrastructure. Checked by both the enrollment endpoint and the cron. The portal UI card (`components/portal/autopay-card.tsx`, on the Expenses tab of `/portal/invoices`) is itself hidden while the switch is off, unless the viewing account is already enrolled. **Browser-QA'd live in sandbox** (kill switch flipped on there only): enrollment correctly gated by the switch, a real Stripe test-mode Customer + `mode=setup` Checkout Session created with the resolved client email pre-filled, the enabled-state UI and self-service disable both verified end-to-end (DB flags cleared, `autopay_stripe_customer_id` correctly PRESERVED for re-enrollment, `action_log` entry written). **NOT verifiable in sandbox and NOT yet done:** the webhook completion step, because `STRIPE_WEBHOOK_SECRET` is not configured on `td-operations-sandbox` at all (a pre-existing sandbox gap, not something this build introduced or fixed) — `verifyStripeSignature` fails closed with no secret, so no Stripe event of any kind can complete there today. A real end-to-end enrollment (save a card and see `autopay_card_enabled` flip via the webhook) can only be proven once this ships to production behind the still-OFF kill switch, or if a sandbox Stripe test-mode webhook endpoint is deliberately set up first. **Explicitly deferred, not built:** ACH autopay (Phase 2) and its $20 enrollment discount — separate mechanism (Stripe `us_bank_account`, NACHA mandate collection, days-later settlement/reversal risk) needing its own design pass.)_
_Last verified against code: 2026-08-23 — Claude (**THE `client_expenses` MIRROR IS NOW KEPT IN SYNC BY THE DATABASE ITSELF, NOT APPLICATION CODE — dev job `0dcb0a18`, LIVE ON SANDBOX (production pending Antonio's word).** Confirmed real production drift (Domenico Pio Cristiano / DOM Consulting LLC, INV-002457: mirror showed `amount_due` 2500 against a real invoice fully paid at 0 due) traced back to the same root cause as every prior entry below: scattered application-code writers (`syncTDInvoiceMirror`, `syncTDInvoiceStatus`, a direct edit in `finance/actions.ts`) that each had to remember to fire, and periodically didn't. **FIX (council-reviewed across 3 rounds — senior-engineer/ai-architect/bug-hunter each round; a first VIEW-based design was rejected outright: it would have broken a real foreign key from `client_expense_items`, broken invoice creation, reintroduced the credit-note double-count bug, and needed a second untested copy of the status-mapping logic):** migration `20260823-1200-client-expenses-auto-sync-trigger.sql` adds a Postgres `AFTER UPDATE` trigger on `payments` (watching `total, amount, amount_due, amount_paid, status, invoice_status, paid_date, due_date, description, subtotal`) that recomputes the matching `client_expenses` row automatically, inside the same transaction, for every writer present or future — no application code calls a sync function anymore. A second `BEFORE UPDATE` trigger on `client_expenses` refuses any direct write to those fields on a `source='td_invoice'` row (gated on `pg_trigger_depth() <= 1` = direct write, `>= 2` = the sanctioned cascade from the first trigger) — this ALSO closes a previously-unguarded gap where `app/portal/invoices/expense-actions.ts`'s `markExpensePaid` (unlike its siblings `updateExpense`/`deleteExpense`) had no `source==='td_invoice'` check at all. **`syncTDInvoiceMirror` (`lib/portal/td-invoice-mirror.ts`) is now DIAGNOSTIC-ONLY — it reads and reports drift, it no longer writes** (its former write is what the guard trigger would now block). **`syncTDInvoiceStatus` (`lib/portal/td-invoice.ts`) now ONLY fires the "client paid" chat notification** — the notification deliberately stayed OUT of the trigger (round-3 finding): it would duplicate message/marker wording `lib/portal/chat-events.ts` already owns as the single source (risking the reversal path silently failing to match a drifted marker), it can't distinguish a genuine cash settlement from a credit-note settlement (which must never fire it), and it would flood clients with false "you just paid" notes on a future bulk data-correction script. The redundant direct `client_expenses` write in `finance/actions.ts`'s `updateInvoice` is removed (now redundant — the trigger covers `due_date`/`description`/`subtotal` too). **Proven with 17 live scenarios against a real database** (not mocks) covering: plain sync, settled-invoice-owes-zero, due_date/description propagation, credit-note no-mirror-row safety, the guard blocking direct writes while allowing unrelated columns, a single-statement multi-row batch update, a CAS-guarded reversal, a partial-column update (card-fee shape), a Stripe-orphan-row (no mirror at all) update, an untracked-column update correctly NOT cascading, and a multi-invoice waterfall with one invoice deliberately left untouched. Deployed to the cloud sandbox: both triggers confirmed live, zero remaining drifted mirrors in real sandbox data after the one-time heal. **Deliberately NOT touched, different shape, confirmed in review:** the `client_invoices`↔`payments` legacy `portal_invoice_id` link (some `client_invoices` rows have no upstream `payments` row at all — no clean 1:1 to project from; see the 2026-08-18/07-08 entries below for the scattered fixes still living there) — flagged as its own future project, not folded into this one.)_
_Prior: 2026-08-18b — Claude (**Two more post-ship fixes, dev job `9e1e46a6`, found during the `ea5751ef` production verification pass.** (1) **The internal `payments.payment_method` label was hardcoded to a currency-based guess** ("Wire Transfer (Mercury (USD))" / "(Airwallex (EUR))") whenever a specific `settings_bank:<uuid>` bank was picked from the dynamic Invoice Settings dropdown, completely ignoring which of the actual configured banks was chosen — proven live on production (`INV-002471`: Chase JP Morgan picked, label showed Mercury). `createUnifiedInvoiceDraft` (`app/(dashboard)/finance/actions.ts`) now resolves the real bank name via `fetchSettingsBanks()`/`selectSettingsBank()` (the same pair already used correctly at send time in `invoice-auto-send.ts`; `fetchSettingsBanks` was private, now exported for reuse) — confirmed live: the same test now stores `"Wire Transfer (Mercury USD)"`, the bank's actual configured name, not the old hardcoded guess. The functional bank ROUTING was never wrong — `bank_preference` (a separate field, resolved by id at send time via `resolveBankDetails()`) was always correct; only this display label was. (2) **Antonio reported a wrong Airwallex bank address "in the contract and in the system."** Investigated exhaustively before touching anything: NO contract render, invoice PDF, or invoice email anywhere in the codebase actually displays a bank street address at all (confirmed via `InvoicePdfInput`'s type shape and every consumer of `bank-defaults.ts` — none read a `.address` field) — so no client was ever shown this wrong value live. The real, live bug was knowledge-base article "Bank Account Details — EUR and USD" (`a25c8daf-f419-47a3-908f-98e494ba60c0`), which had an unlabeled `Address:` line showing Tony Durante LLC's own Florida office address positioned directly in the Airwallex/EUR block, ahead of a separate, correctly-labeled `Bank address:` line further down showing the real Banking Circle S.A. address in Copenhagen — easy to misread as the bank's own address. Relabeled to `Account holder address:` (matching the clearer, already-correct convention used in the sibling KB article "Bank Details — Tony Durante LLC"). Also corrected the same wrong value sitting unused/dead in `bank-defaults.ts`'s `AIRWALLEX_EUR.address` constant for data hygiene — confirmed no current renderer reads it, so this was latent, not live. Left `RELAY_USD`/`MERCURY_USD`/`REVOLUT_USD`'s own `.address` fields untouched — those correctly hold the ACCOUNT HOLDER's (TD's own) address, the expected convention for a US ACH wire's beneficiary address, distinct from the bank's own address that only matters for an international SWIFT wire like Airwallex.)_
_Prior: 2026-08-18 — Claude (**RECURRING INVOICING SHIPPED TO PRODUCTION, then 6 post-ship defects found in live QA and fixed — dev job `ea5751ef`, child of `4a854806`.** Antonio ran a real production create-and-verify pass right after the first ship and asked for a full E2E; that surfaced 5 real defects in the shared `createTDInvoice()` path (used by 15+ real callers, not just recurring) plus a 6th found independently by the Bug Hunter during the review of the fix plan. **All six, fixed and re-verified live with real created-and-deleted records, not just read from code:** (1) **UTC-vs-Eastern date bug** — every spot that asked "what day is it today" via `new Date().toISOString().split('T')[0]` (the dialog defaults, `createTDInvoice`, the recurring reactivation/creation catch-up logic, the cron's own eligibility check) read TOMORROW's date for the last ~4-5 hours of the ET business day. Fixed by a new exported `getOfficeDateString()` in `lib/portal/office-hours.ts`, reusing the exact Eastern-time formatter already proven for office-hours logic — deliberately NOT touching the ~150 other unrelated occurrences of the same raw pattern elsewhere in the codebase (different subsystems, never reported broken, out of scope). (2) **The staff-picked Issue Date was silently discarded on EVERY invoice** — one-time, credit notes, AND recurring cycles — `createTDInvoice` always stamped server-today regardless of what was picked. `TDInvoiceInput` gained an optional `issue_date`, used when given, else defaults to (1)'s corrected today; wired through the one-time action, the credit-note action, AND the recurring generator (`issue_date: runDate`, the template's own cycle date, on every cycle including cycle 1). **A second, independently-broken copy of this same bug was found live in production QA after the first fix looked like it hadn't worked**: the Finance page's OWN "New Invoice" button doesn't call the one-time action above at all — it calls a separate wrapper, `createUnifiedInvoiceDraft` (`app/(dashboard)/finance/actions.ts`, used by both `all-invoices-tab.tsx` and `clients-invoices-tab.tsx`), which had the identical gap on its own and needed its own fix. Neither the original review nor any of the 5 council reviewers caught this second path since none were pointed at Finance's own wrapper — found by testing the actual page, not by re-reading the code. A future-dated recurring schedule also needed a guard (skip immediate cycle-1 generation when the picked date is in the future — the cron picks it up for real when the date arrives) plus a messaging fix (the toast said "did not generate" — sounding like a failure — for a schedule that was actually working exactly as designed; now says when the first bill will actually go out). (3) **A real, currently-live, unrelated bug found along the way:** the main New Invoice screen (Payments/Account/Contact pages, via `createInvoice` in `invoice-actions.ts`) collected "Mark as Paid", Payment Method, and Bank Account but never forwarded them to `createTDInvoice` — the UI reported success as Paid-with-chosen-bank while the database saved Draft/unpaid/auto-bank. Fixed in the same object literal already being touched for (2). (4) **`is_test` never propagated from the account/contact onto the invoices generated for them** — `payments.is_test` already existed and was already read by two live, previously-dormant production filters (the bank-feed matcher, the owner P&L income calc), but nothing had ever set it, so both filters were silently no-ops; 7 real payments + 4 real client_expenses rows already sat on already-flagged-test accounts uncounted. `createTDInvoice` now looks up the account's (account wins if both set) or contact's `is_test` via a fail-open `.maybeSingle()` lookup (mirrors the existing pattern in `service-delivery.ts`) and stamps both `payments.is_test` and a NEW `client_expenses.is_test` column (migration `20260818-1500-client-expenses-is-test.sql` — that table had no such column at all, confirmed independently by 3 reviewers). Callers that already know the answer (the June annual-installments cron, which already filters test accounts before its loop) pass an `is_test` override to skip the redundant lookup. **Existing rows backfilled on production** (not just forward-only) via a one-time UPDATE matching the same account-wins-over-contact precedence — Antonio's explicit call. (5) **No edit or delete for an existing recurring schedule** — added both. Edit (`updateRecurringInvoiceTemplate`, dedicated `RecurringEditDialog` component) covers content fields only (label/description/line items/frequency/due-date-offset/payment method/bank/notes) — deliberately NOT `next_run_date`/`active`, which stay owned by the cron's ordering invariant and the pause toggle. **Editing frequency recomputes the next bill date immediately** if the old date was still in the future (reset to today so the new cadence takes effect right away) or via the existing `fastForwardToNextOccurrence` catch-up if it was already overdue — left un-recomputed, this silently froze a schedule at the OLD cadence for up to a full cycle (independently found by 2 reviewers). Delete (`deleteRecurringInvoiceTemplate`) is gated on whether the template has ever actually fired: a template with `last_generated_at IS NULL` gets a real hard delete; one that has already generated a real invoice gets deactivated instead (same effect as the pause toggle) — `payments` has no back-reference to `recurring_invoice_templates` at all, so a fired template's invoices would otherwise lose their only queryable explanation. (6) **Misleading "release it" wording on a credit-covered cycle** — when a recurring bill comes back fully settled by an existing account credit (confirmed via a real credit note + a real fully-covered $0 invoice, live), the staff notification now says "already settled, nothing to send" instead of asking someone to release a bill that doesn't need sending; wording stays generic (not credit-specific) since the same code path can rarely also return an already-Paid real invoice via the idempotency short-circuit, with no credit involved. **Process note:** this fix set went through a real 5-reviewer council pass (Bug Hunter, Senior Engineer, AI Architect, Project Director, System Counselor) against the concrete plan BEFORE any code was written, catching 2 real blockers (the missing `client_expenses` column; the wrong assumed delete-safety mechanism) and multiple majors that reshaped the plan — not just a self-review.)_
_Prior: 2026-08-17b — Claude (**RECURRING INVOICING GETS A NEW-INVOICE-SCREEN TOGGLE + A FINANCE MANAGEMENT TAB — dev job `4a854806`, SANDBOX ONLY, awaiting Antonio's production sign-off.** Antonio, same day: "I want a fucking option to create a recurring invoice that [has] nothing to do with installments or other stuff that works today" — the backend engine below already existed; this adds the staff-facing surface. **`components/payments/invoice-dialog.tsx`** gets a One-time/Recurring switch, force-hidden whenever the dialog is opened for a payment-plan "tranche" part or an installment (`defaultValues?.tranche`/`defaultValues?.installment` — the same two flags `components/accounts/account-detail.tsx`'s Raise and Installment buttons already set), so the two can never mix. Recurring mode hides discount/installment-tag/already-paid/send-now and shows a Repeats picker (Weekly/Every 2 weeks/Monthly/Yearly — quarterly stays supported by the engine, just not offered here). Submitting calls a brand-new, deliberately separate action `createRecurringInvoice` (`app/(dashboard)/payments/recurring-invoice-actions.ts`) — its own Zod schema (`lib/schemas/recurring-invoice.ts`) shares nothing with the one-time `createInvoiceSchema`, and unit price must be `> 0` at input time rather than only caught after the fact. **The cron's per-cycle logic was extracted into `lib/billing/recurring-invoice-generate.ts`** so cycle 1 (created immediately by the dialog) and every later cycle (the cron) run through the exact same function — never two paths that could drift. `createTDInvoice` gained an optional `description` override (one atomic field, not a follow-up UPDATE) that still yields to the credit-covered "$0 due" explanation when both apply. New Finance **"Recurring"** tab (`app/(dashboard)/finance/recurring-tab.tsx`, non-admin, matches the other invoice-management tabs) lists every schedule — company, description, frequency, amount, on/off toggle — as a stacked card list (a cramped grid table failed real mobile-phone testing at the ~380px width Antonio actually runs the CRM at). **Three bug-hunter passes on this surface, all fixed:** (1) design-time — the switch had to be unreachable from the tranche/installment flows, and cycle 1 had to share the cron's exact generator, both closed before any code was written; (2) post-build code review — backdating the New Invoice screen's Issue Date (ordinary practice on a one-time bill) reproduced the exact backlog-dump bug already fixed for re-activating a paused schedule, since `createRecurringInvoice` never fast-forwarded a stale date the way `toggleRecurringInvoice` does — fixed by reusing `fastForwardToNextOccurrence` after a successful first cycle (never after a failed one, which must stay due); the dialog's Payment Method field was collected, validated, and then silently discarded on every generated cycle (`recurring_invoice_templates` had no column for it) — added the column and wired it through; the two Supabase update calls inside the per-cycle generator could let a raw network-level throw (not a returned `{error}`) escape past their own error handling and mislabel a just-created real invoice as failed — both now wrapped in their own try/catch; a due date before the issue date now returns a real validation error instead of silently clamping to a zero-day offset. (3) Live re-verification in the browser against the real deployed build: created a real weekly schedule end-to-end through the actual screen, confirmed the first Draft invoice and the Finance tab row both appeared correctly, toggled a schedule off and on, then deliberately backdated a new schedule's issue date by 40 days and confirmed in the database that it generated exactly one invoice at the historical date and then correctly resumed on a real future date — no backlog. Along the way, recovered from the sandbox alias being stolen by a concurrent deploy (same documented failure mode as before — re-promoted via the same `vercel alias set` command the standard deploy script uses) and fixed a wording bug ("a new one automatically every monthly" → "every month"). Full section below now describes the create-and-manage surface, not just the engine.)_
_Prior: 2026-08-17 — Claude (**GENERAL-PURPOSE RECURRING INVOICING FOR TD'S OWN BILLING — dev job `4a854806`, SANDBOX ONLY, awaiting Antonio's production sign-off.** New table `recurring_invoice_templates` holds billing SCHEDULES (account/contact, `line_items` jsonb, currency, frequency monthly/quarterly/yearly, `next_run_date`, optional `end_date`, `due_date_offset_days`, `active`) — deliberately NOT columns on `payments` and NOT a template row living inside `payments`. Why: the existing recurring pattern on `client_invoices` (`recurring_frequency` etc. + `app/api/cron/portal-recurring-invoices`) only works because its template row is a genuinely inert `client_invoices` row that `createUnifiedInvoice` never mirrors into `payments`/QB/credit-netting. That shape is UNSAFE on `payments`: the dunning cron auto-escalates any `invoice_status IN ('Sent','Partial')` row past its due date to Overdue and queues a chase email, and the bank-feed matcher's matchable set is every non-terminal `payments` row — both would treat an inert template as a real bill. So a template here is NEVER a `payments` row. **The new daily cron `app/api/cron/recurring-invoices`** reads active, due templates and calls `createTDInvoice()` fresh each cycle — Draft-only, same idempotency-key dedup/credit-netting/invoice-number handling as every other TD invoice, natural key `recurring-template:{template_id}:{next_run_date}`. **Ordering invariant (AI Architect review, the one blocker raised):** `next_run_date` advances ONLY after a successful generation — never before, never unconditionally — enforced structurally by two pure functions in `lib/billing/recurring-invoice-schedule.ts` (`buildTemplateSuccessUpdate` is the only place that ever sets `next_run_date`; `buildTemplateFailureUpdate` structurally cannot). A failed cycle leaves the template due so the cron retries the same charge tomorrow, recording `last_run_status`/`last_error` either way. **Month-end/leap-day clamping:** naive date math silently rolls Jan 31 + 1 month to Mar 3 — `advanceRecurringDate` clamps to the target month's actual last day (also correctly retreats a Feb 29 yearly anchor to Feb 28 in a non-leap year). **Staff notification:** a new `recurring_invoice_generated` chat-event (`emitRecurringInvoiceGeneratedEvent` in `chat-events.ts`, catalog-seeded in `whats_new_events`) fires per generated invoice — "Recurring invoice created — release it," modeled on the existing `plan_referrer_ready_to_release` pattern. Deployed-level sandbox QA: 4 fixture templates (due+active, due+inactive, due+past-end-date, not-yet-due) proved the DB filter excludes exactly the 3 negative cases; a real run created a genuine Draft invoice + mirror + notification and advanced `next_run_date` by one clamped month while the other 3 templates stayed untouched; a same-day reset + re-run proved double-fire safety (`createTDInvoice`'s own idempotency guard returned the identical invoice number, no duplicate note). Template CREATION has no UI or MCP tool yet — v1 is insert-only (direct DB write); Antonio scoped the build to "the schedule table + daily job," not a creation surface — flag if a creation UI/tool becomes wanted. Full section: "Recurring invoice templates (TD's own billing)" below.)_
_Prior: 2026-08-11 — Claude (**A TD INVOICE CAN NOW BE ONE PART OF AN OFFER'S PAYMENT PLAN — WS-C item 2, dev job `c0a61e44` — **SHIPPED TO PRODUCTION 2026-08-11, PR #286 (merge `1bbc7ef5`)**; standing watch card `97d24951`: the first REAL production plan signing must be database-verified (the deployed webhook is the one join the gate could not exercise — sandbox blocks `/api/webhooks/*`, so the gate drove it via a local server on the same code).** `createTDInvoice` accepts `tranche_offer_token` + `tranche_seq` (both or neither — the database enforces the pair) and a part gets its own billing category `setup_tranche`, **never `installment_1`/`installment_2`**: paying an invoice categorised as those fires the paid-instalment handler, which lifts a client's accountant hand-off gate, advances their tax card, and feeds the June cron and the instalment badge — a formation client paying part two of a setup fee must trigger none of it. Client-facing wording is "Partial Payment" / "Pagamento Parziale" (both approved by Antonio verbatim), never "instalment" or "rata"; enforced by a word-boundary test, because a substring ban fired on the ordinary Italian word *separatamente*. **The dedup rule CHANGES when lineage is present:** an ordinary manual invoice is deduped on a content hash, but a plan part is deduped on the PART ITSELF (`offer-tranche:{token}:{seq}`) — keying on content would let part two be raised twice by editing the amount between clicks, which is the exact double-bill this prevents. A **partial unique index** (`uq_payments_one_invoice_per_tranche`) is the hard guarantee, and it excludes `Cancelled`/`Voided`/`Credit` so a withdrawn part can be raised again — the first version excluded only `Cancelled`, which locked a voided part's slot for ever (fixed, live on production, migration `20260810-0940`). Invoice DESCRIPTIONS stay English even for an Italian client, deliberately: every other description in the system is English and localising one line would make a single row of their invoice list disagree with the rest. **Dunning is safe by status, not by the missing due date** — `lib/billing/dunning.ts` filters `invoice_status IN ('Sent','Partial')` BEFORE the due-date comparison, so do not rely on omitting a due date for safety. But note a Draft is also invisible to the WIRE MATCHER (`Sent`/`Overdue` only), so a raised-but-unsent part will not auto-match a client's transfer. Full subsystem: `offers.md`.)_
_Prior: 2026-08-08b — Claude (**Credit stranded by currency is now REPORTED at invoice creation (dev job `c0a61e44`).** The netting engine has always computed which credit a client holds in a DIFFERENT currency to the bill — correctly never applied, because credit does not convert and we will not invent an FX rate — but nothing read that field, so the fact died. An Italian client who paid EUR257 for a strategy call would get a USD renewal at full price with nobody told. Invoice creation now raises a staff notice naming the stranded amount, because that is the moment the money is decided AND because the renewal cron never passes through offer creation, which was the only other place that warned. Fires only when something is genuinely stranded, so a same-currency bill produces no noise — both directions are pinned by live cells and mutation-proven)_
_Prior: 2026-08-08 — Claude (**WS-A CREDIT NETTING AT INVOICE CREATION — dev job `c0a61e44`.** The netting gate widened from account-only to `(account_id || contact_id)`, which is what makes a person-scoped paid-call credit reach the signing invoice. Atomic claim → create → confirm with unwind on every failure path; `payments.credit_consumed_by` is a TRANSIENT LOCK, not a tombstone — a partially-used credit returns to the pool, and only an exhausted one keeps the invoice stamp. New `releaseStaleCreditClaims` runs at the head of the netting path: a claim abandoned by a process that died mid-invoice is released on that client's next bill, self-healing rather than a cron. `unspentCreditByCurrency` reports LOCKED credit separately from available and THROWS on a read error rather than returning empty — an empty list must mean "they hold nothing", never "we could not look")_
_Prior: 2026-07-29 — Claude (**The overdue chaser now PAUSES on an invoice whose payment is sitting in the bank awaiting review.** `suppressWhilePaymentAwaitsReview` (`lib/billing/dunning.ts`, pure + unit-tested) drops any eligible invoice that a `needs_review` bank transaction is pinned to, before reminders are enqueued. Why it became necessary in the same change: the bank-feed matcher now PARKS a payment it cannot attribute with certainty, and un-matching restores an invoice to its true Sent/Overdue state instead of hiding it as a Draft — so without this, a client who has genuinely paid gets emailed "Payment Overdue" the next night. It is a PAUSE, not a cancellation: `reminder_count` is untouched, so chasing resumes exactly where it left off once the review is resolved. Full rationale in **banking-bankfeed.md** (2026-07-29b).)_
_Last verified against code: 2026-07-28b — Claude (**The un-mark shipped hours earlier was SILENTLY BROKEN on its first production run — root-caused, healed, and hardened.** The cron reported `unmarked_future_dated: 1` while Shoppyverse stayed Overdue. Why: `syncInvoiceStatus('payment', …)` wrote BOTH status columns in one update, passing the lifecycle value straight into `payments.status` — but that column is the ENUM `payment_status` (`Pending/Paid/Overdue/Delinquent/Waived/Refunded/Not Invoiced/Cancelled`) which has **no `Sent`, `Partial` or `Draft` labels**, so Postgres rejected the ENTIRE update (22P02) — and the writer never checked the error. The returns-not-throws class, again, on a NEW surface: **an ENUM column is invisible to the code↔DB contract gate, which reads CHECK constraints** (flagged as a gap, not fixed here). Only the separate `reminder_count` reset landed, which is what made the failure diagnosable. **Fixes:** (1) `toPaymentEnumStatus` (exported, pure, test-pinned) maps `Sent/Partial/Draft → 'Pending'` — verified against live rows: `invoice_status='Sent'` invoices carry `status='Pending'`; (2) the payments update now CHECKS its error and THROWS (dunning catches per-invoice into `errors[]`, so a rejection is loud, counted, and never reported as success); (3) `toExpenseStatus` (exported, pure, test-pinned) in the TD-invoice mirror gains `Sent/Draft → 'Pending'` — `client_expenses.status` has a CHECK allowing only `Pending/Paid/Overdue/Cancelled`, so the unmapped fall-through was a second silent rejection waiting. Tests: `tests/unit/invoice-status-mapping.test.ts`. **Data:** INV-002295 healed by hand on prod (Sent/Pending, mirror Pending, due 2026-09-01, reminder_count 0).)_
_Last verified against code: 2026-07-28 — Claude (**Overdue is no longer one-way.** For its whole life the daily dunning pass (`lib/billing/dunning.ts`) flipped Sent/Partial → Overdue when past due, and NOTHING ever flipped one back — a renegotiated payment date (Luca moved Shoppyverse INV-002295 to September, td-bug thread 2026-07-28) left the label stuck forever: misleading in the client portal, inflating the Finance overdue totals, and keeping the invoice under daily reminder consideration (it never actually SENDS — `daysPastDue` goes negative and no threshold fires — but the label was wrong). NOW: **step 1b `unmarkFutureDatedInvoices`** runs in `runDunning` after marking and BEFORE reminders are enqueued — any `invoice_status='Overdue'` with `due_date >= today` flips back (Partial when `amount_paid > 0`, else Sent) via the SAME status-only `syncInvoiceStatus('payment', …)` writer step 1 uses (both status columns + the client-expense mirror stay in sync; that writer REFUSES money — the 2026-07-14 rule). `DunningSummary` gains `unmarked_future_dated`. **`reminder_count` resets on the flip**: a renegotiated date starts a fresh reminder cycle, otherwise an invoice that already used its two reminders would never be chased again when the NEW date passes. **Boundary:** un-mark is `>= today`, mark is strictly `< today` — the two partition cleanly, a due-today invoice cannot flap. ALSO: the Finance edit dialog (`updateInvoice` in `finance/actions.ts`) flips immediately when a due date is edited to today-or-future on an Overdue invoice, so the editor sees the change instead of waiting for the nightly cron; every other edit surface is healed by the cron within a day.)_
_2026-07-14b (**`PAYMENT_CATEGORIES` is now a GATED contract, not a promise.** `lib/billing/payment-classification.ts` declares the payment categories and its comment claimed the list was "kept in sync with the CHECK constraint on `payments.payment_category`". **Nothing had ever verified that, and it was NOT in sync**: the database and live code both used `td_communication`, which the list omitted. A loaded gun rather than a fired one (`isPaymentCategory` has zero callers, so no live harm), found on the first run of the new code↔database contract check. The value is added, and the column is now REGISTERED in `scripts/check-db-constraints.ts` — so the promise in the comment is enforced by the pre-push gate and by CI. **The rule this establishes: a code-side vocabulary that mirrors a database CHECK must be registered in the contract check. A comment promising sync is a note; the check is the gate.** Full incident in `banking-bankfeed.md` + `hooks-guardrails.md` (2026-07-14).)_
_Prior: 2026-07-14 (**`confirmPayment` no longer writes money itself — there is now ONE money writer, and both of its branches were broken.** Full rationale in **banking-bankfeed.md** (2026-07-14). What changed here: `confirmPayment` (`lib/operations/payment.ts`) delegates to `applyMoneyToInvoice` (`lib/finance/apply-payment.ts`) for BOTH branches. **The bugs it had:** (a) the `portal_invoice_id` branch called `syncInvoiceStatus('invoice', …)`, which only touches `client_invoices` — it **never wrote the `payments` row at all**, yet `confirmPayment` returned `outcome:"paid"`. Because the idempotency guard reads `payment.status` — the very column that branch never wrote — the function was **not idempotent on the Stripe path**: a retried webhook re-fired the installment handler and re-sent the paid receipt. (b) The direct branch wrote `status` + `amount_paid` but **never `invoice_status` and never `amount_due`** — this is the source of the **64 production invoices that are `status='Paid'` while still reading as Sent/Overdue with a non-zero balance**. They looked open in Finance, and (because the matcher filtered on `invoice_status` alone) they stayed **live auto-match targets that could be credited a second time**. **New behaviour:** one writer refuses terminal invoices (returns a reason instead of silently doing nothing while reporting success), accumulates + caps `amount_paid`, always writes the coherent tuple (`status` + `invoice_status` + `amount_paid` + `amount_due` + `paid_date`), mirrors `client_expenses` + `client_invoices`, and logs to `action_log`. **New `outcome:"partial"`** — a part-payment is recorded but is NOT a settlement: no installment handler, no paid receipt. **`amount_paid` is NOT defaulted to the invoice total** when the caller omits it in a partial context — the Stripe webhook passes the amount actually charged, and defaulting would silently convert a part-payment into a full credit (mode `settle_full` is used only when the caller asserts full settlement by passing no amount). **Also:** the invoice PDF (`lib/pdf/invoice-pdf.ts`) now prints the **required payment reference** (the invoice number) under the bank details — the emailed invoice already did, and for a wire that typed reference is the only identifying signal that exists. **The 64 half-closed rows are NOT bulk-repaired** — forward-only fix; Antonio reviews them per record. SANDBOX-verified (`scripts/qa-payment-reconciliation.ts`, 15/15) + 5,643 unit tests green.)_
_Prior: 2026-07-10b (**Live E2E QA of cancel↔reactivate found two more bugs — both fixed.** A live harness (`tests/live/invoice-reactivate.live.test.ts`, 19 scenarios) drives the REAL server actions against the sandbox DB. **(e) `voidInvoice` left the client's mirror claiming the full balance.** It called `syncTDInvoiceStatus` (status-only) and never the authoritative `syncTDInvoiceMirror`, so a cancelled invoice's portal copy kept `amount_due` at the old figure — the client saw a Cancelled badge next to money still owed. Now both run. **PRODUCTION RESIDUE: 19 `client_expenses` rows are Cancelled with a non-zero `amount_due`, totalling $20,404.90** (`SELECT ... FROM client_expenses e JOIN payments p ON p.id=e.td_payment_id WHERE e.source='td_invoice' AND e.status='Cancelled' AND e.amount_due <> 0`). The portal expense query does NOT filter Cancelled, so these rows are client-visible. A repair sweep (re-project each through `syncTDInvoiceMirror`) is pending Antonio's approval. **(f) A cancelled CREDIT NOTE came back as an ordinary Draft invoice.** The derived path knew nothing about credit notes: a −$200 CN- row reactivated to `invoice_status='Draft'`. Worse, `credit_remaining` (how much of the credit is unspent) is unrecoverable once cancelled. Fixes: `capturePreVoidState` now also snapshots `credit_remaining`; new pure `reactivateBlocker()` REFUSES to reactivate a credit note that has no snapshot (issue a new one instead) and still blocks Split parents; a snapshot-backed credit note restores its `Credit` status and remaining balance. `reactivateInvoice` only writes `credit_remaining` when the target actually carries one. **Known, accepted:** `safeAction` writes `action_log` fire-and-forget, so an instant cancel→reactivate could in principle miss its own snapshot and fall back to derivation (verified as passing today; the fallback is safe, never corrupting). Also note `safeAction` runs its audit insert INSIDE the try, so an audit failure reports the whole action as failed even though the write landed — pre-existing, affects every dashboard action, not fixed here.)_
_Prior: 2026-07-10 (**Reactivate a cancelled invoice + two bugs fixed on the way.** Origin: VictoriamRoas LLC — the correct 2nd-installment invoice (INV-002218) was cancelled in error, and NOTHING in the codebase could bring it back; the repair had to be done by hand on production. **(a) Reactivate.** `voidInvoice` now stamps a `PreVoidState` snapshot (`status`, `invoice_status`, `amount_due`, `amount_paid`, `paid_date`) into `action_log.details.pre_void_state` — safeAction already accepted a `details` payload, it was just never used. New `reactivateInvoice(paymentId)` + `reactivateInvoicePreview(paymentId)` (`app/(dashboard)/finance/actions.ts`) restore that snapshot EXACTLY. For invoices cancelled BEFORE this shipped there is no snapshot, so the pure `resolveReactivateTarget` DERIVES the honest state, in this precedence: fully covered by cash → Paid; **past due → Overdue (even if never emailed — the debt is real; Antonio's explicit call)**; part-paid → Partial; was emailed → Sent; else Draft. `amount_paid` is real cash and is carried through untouched; the balance is recomputed from it rather than trusting the possibly-stale `amount_due` on the cancelled row. `payments.status` is a DB ENUM with no Draft/Sent/Partial member — those map to `status='Pending'` + the free-text `invoice_status`. TOCTOU-guarded (`.eq('invoice_status','Cancelled')` on the UPDATE). Re-syncs the portal copy via `syncTDInvoiceMirror`. Deliberately does **NOT** relink bank feeds — which suggestion was right is unrecoverable and guessing would silently mis-reconcile money. Blocked on Split parents and on non-cancelled invoices. **(b) The reminder-burst trap.** Reactivating a long-overdue invoice whose `reminder_count` is 0 satisfies BOTH dunning thresholds at once, so the next two nightly passes fire back-to-back chase emails — the dunning pass keys ONLY off `invoice_status='Overdue'` + days-past-due + reminder count, and never checks whether the invoice was ever actually emailed. New pure `projectedReminderCount()` + `daysPastDue()` in `lib/billing/dunning.ts` (co-located with `shouldRemindNow` so they cannot drift) power a **warning in the reactivate preview dialog** stating exactly how many emails the client will get. **(c) `voidInvoice` bank-feed bug.** It set `status='unmatched'` on EVERY row pointing at the invoice. But a link can also be an unconfirmed *suggestion* on a row already triaged — so voiding resurrected rows marked `ignored` (deliberately dismissed) and `outgoing` (money leaving, never an invoice payment) back into the review queue. Real: INV-001244 had exactly those two. Now only a CONFIRMED `matched` row returns to `unmatched` (pure `partitionFeedsForUnlink`); every other row just loses its stale pointer, keeping its status. **(d) Finance → By Client row actions were wired to the WRONG TABLE.** That tab is fed from `payments` (finance/page.tsx maps `invoice_status`→`status`) but its Download / Send / Remind / Mark Paid called the CLIENT's own sales-invoice endpoints (`/api/portal/invoices/[id]/pdf|send`, `markInvoiceAsPaid`), which look up `client_invoices` by id. With a payments id they matched nothing: Download and Send errored, Remind silently called the SEND route (full invoice email, not a reminder), and **Mark Paid updated ZERO rows while toasting "Invoice marked as paid" — a false success.** All four repointed at the TD routines the All Invoices tab already uses. Void + Reactivate added to that tab too (it had neither). Pure helpers unit-tested (`tests/unit/invoice-reactivate.test.ts`). SANDBOX — Antonio ships.)_
_Prior: 2026-07-08c (**ROOT CAUSE of the mirror drift = RLS, not just scattered writes.** `client_expenses` has RLS ENABLED with ZERO policies → only `service_role` (supabaseAdmin) can write it; a staff/user-scoped client is silently denied (0 rows, no error). `regenerateInvoice` runs with `createClient()` (staff session): it CAN write `payments` (6 policies, 2 for writes) but its `client_expenses` mirror write hits RLS and never lands. That — not merely "non-transactional partial writes" — is why Giuseppe's INV-002233 payment dropped to $700 while the mirror stayed $1,150, and why the 2026-07-08b fix didn't take on prod (it inherited the staff client; sandbox masked it because sandbox client_expenses RLS differs and allowed the write). FIX: `syncTDInvoiceMirror` now ALWAYS uses `supabaseAdmin` (no client param) so the mirror write bypasses RLS; `applyAvailableCreditToInvoice` + `reconcileAccountCredits` call it with no client. Verified on prod via `pg_class.relrowsecurity` + `pg_policies`. Also: `applyAvailableCreditToInvoice` returns `mirror_synced`; the Finance Regenerate toast now says "Client portal copy synced" (was the misleading "No available credit to apply" when re-running on an already-credited invoice — the case where no NEW credit applies but the mirror still needs syncing). Lesson: sandbox/prod RLS divergence means service-role scripts can't prove a user-scoped-client path — test with the same auth context as prod.)_
_Prior: 2026-07-08b (**Client-mirror single source of truth — drift fix (Giuseppe INV-002233 "accreditato where").** A TD invoice lives in TWO tables: `payments` (source of truth) + `client_expenses` (source='td_invoice', the client-facing PROJECTION shown in the portal Fatture/Expenses). They were kept in sync by scattered hand-rolled partial UPDATEs at each write site — non-transactional, no reconciliation — so they drifted: a $450 referral credit applied to INV-002233 reduced the payment to $700 but the mirror stayed at $1,150, so the client's portal showed $1,150 owed while Finance showed $700. Audit found 3 OPEN invoices with a client-facing wrong balance (INV-002233, INV-002248, INV-002084) + ~86 harmless Paid rows (client correctly sees "$0.00 due"). FIX: new leaf module `lib/portal/td-invoice-mirror.ts::syncTDInvoiceMirror(paymentId, client?)` — the ONE authoritative projection that rebuilds the mirror's full financial state (total, amount_due, amount_paid, status, paid_date) from the payment. Idempotent. DERIVES the balance from status (settled Paid/Cancelled → amount_due 0; never blind-copies payments' sometimes-stale amount_due, which would show "$2000" next to a Paid badge). Kept as a leaf module (deps: db + supabase-admin) to avoid the td-invoice ↔ credit-netting circular import. Routed through it: `applyAvailableCreditToInvoice` + `reconcileAccountCredits` (credit-netting.ts) replace their partial mirror UPDATEs; `reconcileTDInvoiceMirror` (the CRM "Sync Mirror" button) now heals AMOUNTS not just status. Repair sweep: `scripts/reconcile-td-mirror-drift.ts` (dry-run default, `--apply`) re-projects every drifted TD mirror — idempotent, sandbox-verified (26 healed → re-run 0). Pure helpers `mapPaymentStatusToExpense` + `mirrorDiffers` unit-tested. Sibling: portal Expenses list (`components/portal/expense-list.tsx`) now renders a credit note (total<0) as "−$200" green + a "Credit"/"Credito" badge instead of "$0.00 Paid". Business rule (Antonio): a referral credit REDUCES the client's open/overdue invoice. SANDBOX — Antonio ships; prod needs the sweep run + the 3 open invoices verified. Follow-up flagged: the portal "Totale Spese/Pagato" tiles still net credit-note negatives (pre-existing, not changed here).)_
_Prior: 2026-07-08 (**Credit-note numbering hardened against malformed CN- rows** — `generateCreditNoteNumber` (`lib/portal/invoice-number.ts`) no longer trusts the lexicographic max: LIKE's `_` matches ANY character, so a malformed row like `CN-QA0001` lex-sorts above every numeric CN-, parsed NaN → maxSeq 0 → always `CN-000001` → permanent unique-violation retry loop (caught in referral-fix sandbox QA). It now scans a 50-row page and takes the highest ALL-DIGITS suffix via pure `maxNumericSuffix()` (unit-tested). `generateInvoiceNumber` (INV-, both tables) had the SAME bug and it was NOT just latent — real `INV-MMQA-*` rows in sandbox had silently broken ALL invoice creation there (the top lex row hid the true numeric max → permanently colliding candidate). Fixed the same way in the same push; browser+script QA verified INV-002397 generated correctly past the poison rows and click-to-apply consumed $165 of credit against it. Sibling change in the same push: Finance → By Client → **Credit Notes tab now reads `payments` `invoice_status='Credit'` rows** (Amount/Remaining/Available-vs-Applied, applied-to invoice number) instead of the never-written `client_credit_notes` table — see referrals-circleback.md 2026-07-08 for the full referral-scoping fix it belongs to. SANDBOX — Antonio ships.)_
_Prior: 2026-07-04 (**Send button on Draft rows in Finance → All Invoices** — the Kasabi Ocean Global incident: Luca could not send the June cron's draft INV-002172 because the All Invoices row actions (`app/(dashboard)/finance/all-invoices-tab.tsx::InvoiceActions`) offered NO Send for Draft (only Mark Paid / Void / Regenerate / Edit); Resend + Send Reminder were gated to Sent/Overdue/Partial, while the By Client tab already had Send on Draft. Draft rows now show a Send Invoice button (confirm → `sendNewInvoice` server action → `sendTDInvoice`, same backend as By Client). Sibling change: unsent drafts (Draft+Pending) are now hidden from the client portal Payment History via `lib/portal/payment-visibility.ts` — see portal.md 2026-07-04b. Follow-up flagged, not done: the draft's `client_expenses` mirror is still client-visible in the portal Expenses tab. SANDBOX — Antonio ships.)_
_Prior: 2026-07-03 (Visible invoice notes + client-level dated reminder pause — Luca's request after the KG Wolf incident: a batch reminder went to a client who had promised to pay by September) — Claude. **(a) Note visibility:** `payments.notes` (which already existed, editable via the row Edit dialog) now shows as an amber note icon next to the status badge in Finance → All Invoices, Finance → By Client, and the account Payments tab (`components/payments/invoice-note-dot.tsx`, portaled popover — HelpDot pattern). Bulk "Send Reminders" warns pre-send when selected invoices carry notes. **(b) Dated pause:** new `accounts.dunning_pause_until DATE` + `dunning_pause_reason TEXT` (migration `20260703-1745`, sandbox applied; prod DDL pending) — "client promised to pay by X". While active (today ≤ date, pure `isReminderPaused`/`isAccountReminderPaused` in `lib/billing/reminder-snooze.ts`, unit-tested), ALL the account's invoices are skipped by the dunning enqueue AND by bulk; the pause is ALSO re-checked at send time INSIDE `sendInvoiceReminder` (a deliberate exception to that module's no-policy rule — jobs enqueued before a pause was set must still be stopped; this also closed the hole where the single-row Send Reminder bypassed even the boolean pause). Manual single-send warns "paused until X — send anyway?" and passes `force: true` on confirm; auto/bulk never force. Expiry = reminders resume automatically (Antonio's decision — no staff task). The pause NEVER suppresses Overdue marking or badges. Set from account page → Payments tab → Payment Reminder Settings ("Paused until" + "Pause reason" fields, `updateAccountField` whitelist + date validation); paused rows show "⏸ Paused until <date>" in All Invoices. **(c) Internal notes stay internal:** `updateInvoice` and `createTDInvoice` no longer copy `payments.notes` into the `client_expenses` mirror (client-domain table; the edit dialog promises "not visible to client"). Existing mirror rows keep whatever notes they already have (client's own field). SANDBOX — Antonio ships._ `sendTDInvoice` (`lib/invoice-auto-send.ts`) — the single choke point behind agreement-signed auto-send, the CRM send button and finance actions — now also dispatches the action-required package after the send: clickable portal chat ("We've issued invoice INV-… — pay in your portal") + bell/push, deep link `/portal/invoices?inv=<payment_id>` (per-invoice, so consecutive invoices each notify). **`skipEmail: true`** — the rich PDF invoice email is the only email, never a second one. PORTAL audience only (`no_portal` recipients keep their /pay-token email path, R092 untouched). Draft creation stays silent (2nd-installment drafts still staff-reviewed); the overdue dunning system is untouched. Engine + E2E in portal.md (2026-07-02d). SANDBOX — Antonio ships._
_Last verified against code: 2026-07-15 — Claude (card fee LIVE on prod, deployed OFF; Finance-dashboard kill-switch button + merge-safe rate setter + offer→invoice pin inheritance, Council-approved Phase A. See "Card processing fee" under How it's built.)_
_Prior: 2026-06-26 — Claude (portal Fatture row menu now follows the standard invoice lifecycle — client's own tool, no TD-accounting impact: Edit + Void allowed on Paid/Partial too; Send only advances Draft→Sent and never downgrades a paid invoice; Reminder stays Sent/Overdue; Split parents excluded. See "Portal Fatture per-row actions" under How it's built.)_
_Prior: 2026-06-22 — Claude (dunning reminders now QUEUED via the job system: runDunning enqueues `invoice_reminder` jobs (priority 8, deduped, bulk via enqueueJobs) and the process-jobs worker delivers them gradually — no timeout/blast. Per-pass cap UI-configurable up to 1000 (clampCap). New handler lib/jobs/handlers/invoice-reminder.ts. dev_task d2af38a1.)_
_Prior: 2026-06-19 — dunning pass extracted to lib/billing/dunning.ts::runDunning, shared by cron + "Run reminders now" admin button; auto-send UI-controlled via app_settings.dunning_autosend toggle (env var retired); pure shouldRemindNow() unit-tested._
_Prior: 2026-06-19 — reminder email rebranded: brand logo header + bilingual "How to pay" block, NO link — portal-on-desktop-or-phone-app instruction. buildReminderEmail in lib/billing/invoice-reminder.ts._
_Prior: 2026-06-19 — payment reminders unified into `lib/billing/invoice-reminder.ts::sendInvoiceReminder`, bilingual EN/IT; dunning cron now calls it in-process — fixed the VERCEL_URL self-fetch auth-wall bug that sent 0 reminders; `resolvePaymentRecipient` now also returns `language`. dev_task `d2af38a1`._
_Prior: 2026-06-18 — invoice recipient resolution centralized in `resolvePaymentRecipient()`; owner-role match is CASE-INSENSITIVE and all three send paths — CRM resend route, sendTDInvoice, sendPaidReceipt — delegate to it. Fixes the ADWise lowercase-"owner" resend failure, dev_task `63431fc8`._
_Prior: 2026-06-16 — bank_preference references a configured bank by STABLE ID `settings_bank:<uuid>`, resolved by id via selectSettingsBank(); each invoice_settings.bank_account carries an `id` auto-assigned on add + server backfill; DB CHECK constraint allows null / 5 legacy enum / `settings_bank:<uuid>`. Replaced the fragile positional `settings_bank_N` scheme, which had an active-filter index-mismatch bug._

## What it is
Everything about money on invoices. There are **four separate money "worlds"** that look similar but must never be mixed up — confusing them is the #1 source of billing bugs:

1. **TD billing the client** — Tony Durante LLC's invoices *to* clients (our revenue). → `payments` table.
2. **The client's own sales** — invoices the client sends to *their* customers (their business, not ours). → `client_invoices` table.
3. **The client's expenses** — what the client owes, including a *mirror* of our TD invoices so they see them in the portal. → `client_expenses` table.
4. **TD's own operating costs** — vendor bills, filing fees, software TD pays. → `td_expenses` table.

## Business rules
- **R027** — `client_invoices` is for the client's sales invoices ONLY. **TD systems NEVER write to it.** (CLAUDE.md)
- **R092** — Invoice emails to clients must send them to the **portal Pay button** to pay. NEVER embed Stripe checkout links, wire details, or payment credentials in the email body. (CLAUDE.md)
- **R098** — Invoice-number generation is race-safe via a DB unique index + caller retry, NOT a code retry/timestamp fallback. Never write `invoice_number` directly — always go through the helper functions. (CLAUDE.md)
- **Credit netting (at invoice-creation only)** — a real (positive, unpaid) TD bill automatically applies the account's outstanding credit notes (oldest first, same currency, capped at the bill) **at invoice-creation time** (`computeCreditApplication`/`consumeCredits`). This is the ONLY remaining automatic credit application; for invoices that already exist, credits apply via click-to-apply Regenerate (below). Rule lives in code: `lib/operations/credit-netting.ts`.
- **Credit application = CLICK-TO-APPLY (2026-06-03)** — credits are NOT auto-applied to existing invoices. A new credit note / referral credit sits as available `credit_remaining` on the account and lands on a SPECIFIC invoice only when staff click **Regenerate** on it (see below). This replaced the old auto-reconcile-onto-oldest behaviour, which made a credit earned in June silently reduce an overdue January invoice (the Wise Strategies bug). `reconcileAccountCredits()` / `allocateCredits()` remain in `credit-netting.ts` but are **NO LONGER CALLED** — do not re-wire them into credit creation.
- **Regenerate invoice document = applies available credit (2026-06-03)** — `regenerateInvoice(paymentId)` (`app/(dashboard)/payments/invoice-actions.ts`) applies the account's available credit to THIS invoice (capped at what it still owes), shows it as a `Credit applied −$X` line, drops `amount_due`, and consumes the credit (stamping `credit_for_payment_id` = this invoice). **The invoice you click is where the credit lands.** In place — same invoice number, no money moved; `amount_paid` tracks REAL cash only (credit is shown purely as the line, never folded into `amount_paid`). Idempotent: a re-click with no remaining available credit re-renders the same document. Generic for ANY account-scoped invoice/service. Pure helpers in `lib/portal/invoice-regenerate.ts` (`computeClickToApplyCredit` etc.), unit-tested; applies/consumes via `computeCreditApplication`/`consumeCredits`. No-op when there is no existing or available credit. UI: Regenerate button on Draft/Sent/Overdue/Partial invoices in the Payment Tracker dialog (`invoice-detail-dialog.tsx`) AND both Finance views (`clients-invoices-tab.tsx`, `all-invoices-tab.tsx`).

## How it's built
### The two creator functions (single entry points)
- **`createTDInvoice()`** in `lib/portal/td-invoice.ts` — TD → client. Writes **`payments`** (primary, staff-facing) + **`payment_items`** + a **`client_expenses`** mirror row (vendor "Tony Durante LLC", its own `EXP-NNNNNN` internal ref) so the client sees the bill as an incoming expense in the portal. Sets `qb_sync_status='pending'` — a **legacy/inert column: QuickBooks is DEAD (decommissioned 2026-05-23), nothing ever reads or advances it**. **Never writes `client_invoices`.** Accepts optional `issue_date` (defaults to today in Eastern time via `getOfficeDateString()` when omitted) and optional `is_test` (looked up from the account/contact when omitted, stamped onto both `payments` and `client_expenses`) — see the `ea5751ef` changelog entry for why both were added.
- **`createUnifiedInvoice()`** in `lib/portal/unified-invoice.ts` — client → their customer. Writes **`client_invoices` only**. No `payments` mirror, no QB sync. Resolves/creates a `client_customers` row.

### Invoice numbers
- `generateInvoiceNumber()` in `lib/portal/invoice-number.ts` — one **global** sequence `INV-NNNNNN`, computed as max+1 across **both** `payments` and `client_invoices` (strict `LIKE 'INV-______'` so legacy oddities don't poison the max). It is intentionally simple and **not race-safe by itself**.
- `generateCreditNoteNumber()` (same file) — separate **`CN-NNNNNN`** sequence (payments-scoped) for **credit notes**, so a credit never reads as an invoice. `createTDInvoice` picks CN- automatically whenever `grossTotal <= 0` (a credit note); INV- otherwise. (Added 2026-06-01.)
- Race safety = partial unique indexes `uq_payments_invoice_number` / `uq_client_invoices_invoice_number` + a **10-attempt retry loop** in the creator functions that regenerates on a 23505 unique-violation. `isUniqueViolation()` detects the specific constraint. **No timestamp-suffix fallback** (that produced the `INV-NNNNNN-XXXXXXXX` scars deleted after the April-12 collision incident — R098).

### Idempotency (content-level dedup)
Both creators accept an optional `idempotency_key`; if a row with that key exists, the existing invoice is returned (no duplicate). `payments` also has `uq_payments_idempotency_key` as a concurrent-insert guard. Standard keys: `offer-signed:TOKEN:CONTACT_ID`, `annual-installment:ACCT:N:YEAR`, `manual-crm:ACCT:HASH`.

### Status & sync
- `payments.status` = Pending/Paid (+ Partial/Overdue/Cancelled/Split); `payments.invoice_status` = Draft/Paid.
- **The `client_expenses` mirror is kept in sync by a DATABASE TRIGGER, not application code (dev job `0dcb0a18`, 2026-08-23).** Any UPDATE to a TD invoice's money/status/date fields on `payments` automatically recomputes the matching `client_expenses` row in the same transaction — for every writer, present or future. A second trigger on `client_expenses` refuses any other direct write to those fields on a `source='td_invoice'` row. `syncTDInvoiceMirror()` is now diagnostic-only (reports drift, never writes); `syncTDInvoiceStatus()` now only fires the "client paid" portal-chat notification. `reconcileTDInvoiceMirror()` / the CRM "Sync Mirror" button are now a health check, not a repair tool — there should be nothing left for them to fix.
- `installment` uses `payment_type_enum`: Setup Fee, Installment 1 (Jan), Installment 2 (Jun), Annual Payment, One-Time Service, Custom.
- Currency USD/EUR; `bank_preference` is a free-form string. Resolution in `lib/invoice-auto-send.ts::resolveBankDetails` via the pure helper `selectSettingsBank()`: (1) `settings_bank:<uuid>` — the bank whose stable `id` matches in `invoice_settings.bank_accounts` (preferred; robust to reorder/delete); (2) `auto`/null — `is_default` bank from invoice_settings for matching currency, then first bank of that currency, then Mercury/Airwallex fallback; (3) legacy enum (`relay/mercury/revolut/airwallex`) → hardcoded constants. The legacy positional `settings_bank_N` form is still parsed as a harmless fallback but is no longer produced by the dialog nor accepted by the DB constraint. `is_default` is scoped per currency (radio button in Invoice Settings UI). DB guard `payments_bank_preference_check` allows null / the 5 enum values / `^settings_bank:[0-9a-fA-F-]{36}$`.

### Cancel ↔ Reactivate (2026-07-10)
- **Cancel** (`voidInvoice`) → `payments.status`/`invoice_status` = `Cancelled`, mirror synced, bank feeds unlinked, **and a `pre_void_state` snapshot written to `action_log.details`**.
- **Reactivate** (`reactivateInvoice`) → restores the snapshot when present, else derives via `resolveReactivateTarget` (`lib/billing/invoice-reactivate.ts`, pure + unit-tested). Preview (`reactivateInvoicePreview`) warns how many automatic chase emails the client will receive.
- **Where the buttons live:** Finance → All Invoices, Finance → By Client, account page → Payments tab. Cancel is hidden on Paid and on already-Cancelled invoices (Antonio's rule: *"if an invoice is paid there is no reason to void it; if a payment is matched but I need to change the invoice I can edit instead"*). Reactivate shows ONLY on `Cancelled` — never on `Waived`/`Voided`, which are different lifecycle ends.
- **Never relink bank feeds on reactivate.** The correct match is not recoverable; re-match from the Bank Feed tab.
- **Never point the By Client tab at `/api/portal/invoices/*` or `app/portal/invoices/actions`.** Those operate on `client_invoices` (the client's own sales ledger). This tab renders `payments`.

### Key files
- `lib/billing/invoice-reactivate.ts` — `capturePreVoidState` / `parsePreVoidState` / `resolveReactivateTarget` / `partitionFeedsForUnlink` (all pure, unit-tested)
- `lib/portal/td-invoice.ts` (creates the initial mirror row; `syncTDInvoiceStatus` now only fires the paid-notification, per Status & sync above) · `lib/portal/unified-invoice.ts` · `lib/portal/invoice-number.ts` (`generateInvoiceNumber` + `generateCreditNoteNumber`) · `scripts/migrations/20260823-1200-client-expenses-auto-sync-trigger.sql` (the mirror-sync + write-guard triggers)
- `lib/operations/credit-netting.ts` — `computeCreditApplication`/`consumeCredits` (at-creation) + `reconcileAccountCredits`/`allocateCredits` (existing-invoice reconcile) · `lib/portal/invoice-audit.ts`
- `lib/billing/installment-defaults.ts` · `lib/billing/renewal-guard.ts`
- `lib/invoice-auto-send.ts` — `resolveBankDetails()` (bank resolution at send time), `selectSettingsBank()` (pure id/legacy matcher, unit-tested), `fetchSettingsBanks()` (all banks from invoice_settings), `sendPaidReceipt()`, `sendInvoiceEmail()`
- `lib/portal/resolve-payment-recipient.ts` — `resolvePaymentRecipient()`: the SINGLE source of truth for "who does an invoice email go to". Used by the CRM resend route (`app/api/invoices/[id]/send/route.ts`), `sendTDInvoice`, `sendPaidReceipt`, the pay-link route, create-invoice-checkout, and the reminder module. Returns `{ email, name, language }` (language from the resolved contact; null when resolved via `communication_email`). Unit-tested.
- `lib/billing/invoice-reminder.ts` — `sendInvoiceReminder(paymentId)`: the SINGLE source of truth for payment-reminder emails. Resolves recipient via `resolvePaymentRecipient`, builds a **bilingual (EN/IT by `contacts.language`)** branded HTML email via the pure `buildReminderEmail()`, sends (safeSend ordering), bumps `payments.reminder_count` / `last_reminder_at`, returns a structured `ReminderResult` (never throws). Called DIRECTLY by the dunning cron, the `POST /api/invoices/[id]/remind` route, and the manual "Send Reminder" finance action. Unit-tested (`tests/unit/invoice-reminder-email.test.ts`). The email carries the brand **logo** (`APP_BASE_URL/images/logo.jpg`) and a **"How to pay" block with NO link** — it tells the client to log in to the portal on desktop or the TD Portal phone app, then Invoices → Pay (R092: portal-only, no embedded links/creds). No link by deliberate decision (email links can't reliably open an installed PWA, esp. on iOS).
- MCP tools: `portal_invoice_create`, `portal_invoice_send`. CRM: Finance pages, `/payments`, `/invoice-aging`, `/invoice-settings`.
- **Portal Fatture per-row actions (client → their customer, `client_invoices`)** — the portal Sales list (`components/portal/invoice-list.tsx`) renders a 3-dot menu per row (`components/portal/invoice-row-actions.tsx`, same `createPortal` flip-above pattern as the CRM `PaymentRowActions`). **These invoices are the client's own tool** — they never touch TD's books, accounting, or any tax record (clients are non-US residents owing no US tax; they may not invoice at all). So the client has full control over their own invoices at every stage. The menu follows the standard invoice lifecycle (Draft → send → Sent/Overdue → pay → Paid), like QuickBooks. Endpoints — no new backend: **Send** → `POST /api/portal/invoices/[id]/send` (emails the invoice + PDF; **only advances Draft → Sent, never downgrades** a Sent/Overdue/Paid invoice), **Reminder** → `POST /api/portal/invoices/[id]/remind` (route hard-requires `Sent`/`Overdue`), **Edit** → `/portal/invoices/[id]/edit` (page allows any status except Split/Cancelled), **Void** → `voidInvoice()` server action in `app/portal/invoices/actions.ts` (sets `Cancelled`, audit-logged; allowed on any status except already-Cancelled and Split). **View** and **Download PDF** (`GET /api/portal/invoices/[id]/pdf`, no status gate) are **always shown on every status**. Status-dependent actions are gated by the pure helper `availableInvoiceActions(status)` in `lib/portal/invoice-row-actions-policy.ts` (unit-tested): **edit + void on Draft/Sent/Overdue/Paid/Partial**, **send on Draft**, **remind on Sent/Overdue**. **Reminder is excluded on Paid** (a "please pay" reminder on a settled invoice is meaningless). **Split parents are excluded from edit/void** (structurally tied to their installment children). Bilingual via `useLocale()` (`invoices.*` keys). The same actions also live on the invoice detail page (`app/portal/invoices/[id]/page.tsx`).
- `app/(dashboard)/invoice-settings/page.tsx` — Invoice Settings UI with is_default radio buttons per currency
- `components/payments/invoice-dialog.tsx` — dynamic bank dropdown loaded from `/api/invoice-settings`
- **Overdue rollup for UI badges (2026-06-30)** — `lib/billing/overdue.ts` exports pure, DB-free helpers (`isOverduePayment`, `daysPastDue`, `summarizeOverdue`) that classify a `payments` row as overdue: `status ∈ {Overdue, Delinquent}` OR (`status === 'Pending'` AND `due_date < today`), excluding `is_test` rows, no age cap. This mirrors — but does not call — the runtime check already used in `lib/portal/queries.ts` and `app/api/crm/dashboard-attention/route.ts`; it does **not** touch `payments.status` or trigger reminders (that's the dunning pass above). First consumer: `app/api/portal/chat/threads/route.ts` queries `payments` for the threads' account/contact ids and calls `summarizeOverdue()` per account (and per contact for contact-direct invoices with no `account_id`) to attach a `{count, maxDays, totalDue}` summary the portal-chats UI renders as a red badge next to the contact's name and each company they own. Unit-tested (`tests/unit/overdue.test.ts`).

### Recurring invoice templates (TD's own billing, dev job `4a854806`, LIVE ON PRODUCTION since `ea5751ef`)
General-purpose recurring invoicing for TD → client billing — NOT the Jan/June installment-only cron, and NOT the same shape as the `client_invoices` recurring engine.
- **Table `recurring_invoice_templates`** holds ONLY schedule metadata: `account_id`/`contact_id` (either), `label` (internal, never client-facing), `description` (overrides the invoice's summary description on every generated cycle, mirrors the one-time flow's Service field), `line_items` jsonb (same shape as `TDInvoiceInput.line_items`), `currency`, `frequency` (`weekly`/`biweekly`/`monthly`/`quarterly`/`yearly`), `next_run_date`, optional `end_date`, `due_date_offset_days` (DB `CHECK >= 0`), `active`, optional passthroughs (`installment`, `payment_category`, `notes`, `message`, `bank_preference`, `payment_method`), and an audit trail of the last cycle (`last_generated_payment_id`, `last_generated_at`, `last_run_status`, `last_error`). RLS enabled, no anon/authenticated grants — server-only, same as `payer_client_map`/`annual_agreements`. **A template is NEVER a `payments` row** — see the changelog entry above for why that would be unsafe (dunning + bank-feed matcher would treat an inert template as a live bill).
- **Shared generator `lib/billing/recurring-invoice-generate.ts::generateRecurringInvoiceCycle`** is the ONE function that turns a template row into an invoice for a cycle — used by BOTH the daily cron AND the New Invoice dialog's Recurring path for the schedule's first cycle, so the two can never silently disagree on which fields persist.
- **Cron `app/api/cron/recurring-invoices`** (daily) selects `active=true AND next_run_date <= today AND (end_date IS NULL OR end_date >= today)`, then calls the shared generator per due template — `createTDInvoice()` with `idempotency_key = "recurring-template:{id}:{next_run_date}"`, Draft-only, inherits credit-netting/invoice-number retry/idempotency dedup for free. Sandbox-only `?dry=1` reports what would generate without writing.
- **Creation surface: New Invoice screen → Recurring toggle.** `components/payments/invoice-dialog.tsx` gets a One-time/Recurring switch, force-hidden whenever `defaultValues?.tranche` or `defaultValues?.installment` is set (the Raise-a-plan-part and Installment 1/2 flows in `components/accounts/account-detail.tsx`) — a recurring schedule can never be created for or from either. Recurring mode hides discount/installment-tag/already-paid/send-now; shows a Repeats picker (Weekly/Every 2 weeks/Monthly/Yearly — quarterly stays DB-supported, just not offered here). Submits via `createRecurringInvoice` (`app/(dashboard)/payments/recurring-invoice-actions.ts`) — its own Zod schema (`lib/schemas/recurring-invoice.ts`), deliberately sharing nothing with the one-time `createInvoiceSchema`; unit price must be `> 0` at input time. Inserts the template row, then calls the shared generator for cycle one immediately (so the first bill appears right away, same UX as a normal invoice).
- **Management surface: Finance → Recurring tab.** `app/(dashboard)/finance/recurring-tab.tsx` (non-admin, same visibility as the other invoice tabs) lists every schedule — company/contact name, description, frequency, amount, on/off toggle — as a stacked card list (NOT a grid table: a grid was unreadable at the ~380px phone width Antonio actually runs the CRM at). Toggle action `toggleRecurringInvoice`: off just flips `active` (does not touch already-generated invoices); ON re-activation fast-forwards a stale `next_run_date` via `fastForwardToNextOccurrence`.
- **Ordering invariant — the ONE thing that must never regress:** `next_run_date` advances ONLY on a successful `createTDInvoice()` call. This is enforced by construction, not by a runtime check: `buildTemplateSuccessUpdate` (`lib/billing/recurring-invoice-schedule.ts`) is the ONLY function that ever sets `next_run_date`, and `buildTemplateFailureUpdate` has no such field at all. A failed cycle (transient DB error, retry exhaustion) leaves the template due, so the cron retries the same charge tomorrow instead of silently skipping it forever. **This invariant extends to CREATION too:** a backdated Issue Date on a brand-new schedule gets fast-forwarded to a real future `next_run_date` immediately after a successful first cycle (never after a failed one) — otherwise the exact same backlog-dump bug re-activation was hardened against would reopen through the create path (bug-hunter finding, fixed same day).
- **Month-end / leap-day clamping, day-based vs month-based frequencies.** `advanceRecurringDate` branches explicitly on two lookup tables (`DAY_FREQUENCIES`: weekly=7/biweekly=14 days; `MONTH_FREQUENCIES`: monthly=1/quarterly=3/yearly=12 months, clamped) — NOT a ternary chain, which would silently route an unlisted frequency into the wrong branch (mutation-tested: this exact regression was deliberately reintroduced and confirmed the test suite catches it). Month-based clamping: Jan 31 + 1 month lands on Feb 28 (Feb 29 in a leap year), not Mar 3; a Feb 29 yearly anchor correctly retreats to Feb 28 in a non-leap target year.
- **Staff notification.** On success, `emitRecurringInvoiceGeneratedEvent` (`lib/portal/chat-events.ts`) fires a `recurring_invoice_generated` chat-event — same `emitClientChatEvent` primitive and idempotency marker (dedup on `payments:{id}`) as every other What's New note, catalog-seeded in `whats_new_events` with display text "Recurring invoice created — release it." Modeled directly on `plan_referrer_ready_to_release`. Non-fatal: a notification failure never blocks or un-does the already-created invoice.
- **Edit & delete (dev job `ea5751ef`).** `updateRecurringInvoiceTemplate` (content fields only — `next_run_date`/`active` stay system-owned) recomputes `next_run_date` on a frequency change per the ordering-invariant rule above. `deleteRecurringInvoiceTemplate` hard-deletes only a template that has never fired (`last_generated_at IS NULL`); a fired template is deactivated instead, since `payments` has no back-reference to this table and a hard delete would leave its real invoices with no queryable trail. Both live in `app/(dashboard)/payments/recurring-invoice-actions.ts`; edit UI is `components/payments/recurring-edit-dialog.tsx`.
- **`is_test` propagation.** `createTDInvoice` stamps `is_test` (account wins over contact when a caller sets both) onto every invoice it creates — recurring, one-time, and credit notes alike, not just this feature. See the changelog entry above for the two live filters this activates and the production backfill.
- **How to verify current state:** `SELECT * FROM recurring_invoice_templates WHERE active` for live schedules; `SELECT * FROM catalog_entries WHERE catalog_id='whats_new_events' AND slug='recurring_invoice_generated'` for the notification label. Verified end-to-end against the real deployed sandbox build with real created-and-deleted records (dev job `ea5751ef`) — see the top changelog entry for the exact scenarios proven, including the credit-covered case and the future-dated-schedule case.

### Card autopay (Phase 1 — card only, dev job `10995181`, LIVE ON PRODUCTION, kill switch ON since 2026-08-30)
See the top changelog entry for the full design rationale (the double-charge race + its fix, the setup-mode webhook guard, the fee waiver). Quick reference for a session picking this up:
- **Kill switch:** `lib/payments/card-autopay-config.ts` (`app_settings.card_autopay_config.enabled`, defaults **false** — but is explicitly set **true in production** since 2026-08-30, confirmed live). Per-account trust flag is `accounts.autopay_card_enabled` — the ONLY field any charging code may trust.
- **The claim (the safety mechanism the whole feature rests on):** `lib/operations/autopay-claim.ts` — `claimPaymentForCharge`/`releasePaymentClaim`/`recordCheckoutSessionId`. Called from THREE places before any of them may create a Stripe object for a given `payments` row: `app/api/workflows/create-invoice-checkout/route.ts` (the portal Pay button), `app/pay/[token]/route.ts` (the older email pay-link, added 2026-08-30c after being found unguarded), and `app/api/cron/card-autopay-charge/route.ts` (unattended). **If a future session adds a fourth way for a client to reach Stripe for an existing invoice, it needs this claim too** — grep for callers of `createStripeCheckoutSession`/`stripe.checkout.sessions.create` against an existing `payments` row before assuming coverage is complete.
- **Enrollment:** `lib/operations/card-autopay.ts` (`getOrCreateStripeCustomerForAccount`, `createAutopaySetupCheckoutSession`, `saveAutopayCard`, `disableAutopayCard`) + `app/api/portal/autopay/{setup-checkout,disable}/route.ts` + the webhook's setup-mode branch in `app/api/webhooks/stripe/route.ts`.
- **Charging:** `app/api/cron/card-autopay-charge/route.ts`, daily 11:00 UTC (`vercel.json` + `lib/cron-coverage.ts`). Scope is EVERY `payments` row except `payment_category='credit'` — not just Jan/June — per Antonio, 2026-08-30b.
- **Fee waiver:** centralized in `createTDInvoice` itself via `resolveCreationCardFeeRate()` (`lib/payments/card-fee-config.ts`) — every invoice-creation call site gets the waiver automatically when the account is enrolled; do not re-add a per-call-site override.
- **UI:** `components/portal/autopay-card.tsx` (Expenses tab) + a nudge inside `components/portal/td-pay-modal.tsx` (backed by `GET /api/portal/autopay/eligibility`), rendered whenever the invoice's account isn't yet enrolled and the kill switch is on.
- **Sandbox webhook testing — a real, deliberate dead end, don't re-attempt it expecting a different result:** `SANDBOX_MODE=1`'s middleware returns 503 on every `/api/webhooks/*` call, so `checkout.session.completed` can never complete in sandbox regardless of Stripe/Vercel config (both the Vercel deployment-protection bypass and a correct `STRIPE_WEBHOOK_SECRET` are ALSO required and were set up 2026-08-30b, but 503 is the wall behind them). Verify enrollment's Stripe-facing half (Customer + Checkout Session creation, correct email, correct redirect) live in sandbox; verify the webhook-completion half only in production behind the kill switch.
- **How to verify current state:** `SELECT value FROM app_settings WHERE key='card_autopay_config'` for the kill switch; `SELECT id, autopay_card_enabled, autopay_card_last4 FROM accounts WHERE autopay_card_enabled` for enrolled accounts; `SELECT id, invoice_number, charge_claimed_until, stripe_checkout_session_id FROM payments WHERE payment_category != 'credit' AND charge_claimed_until IS NOT NULL` should normally be empty (a non-null claim outside an in-flight charge attempt means a prior run didn't release it — investigate, don't just clear it).

### Finance summary card (account + contact Overview tabs, dev job `10995181` follow-up, SANDBOX ONLY)
A read-mostly invoice summary + autopay status card, NOT a new page/tab — sits inside the existing Overview tab's grid on both `components/accounts/account-detail.tsx` (`PanoramicaTab`) and `components/contacts/contact-detail.tsx` (`OverviewTab`, one card per linked company with content + a no-autopay "Personal" card for contact-direct invoices). **Went through a 9-reviewer full council pass (2026-08-30) before shipping** — the first build had 6+ independently-corroborated blockers, all fixed and re-verified live in sandbox against real data before this doc was written. Treat the pre-fix version as a cautionary example, not a design to imitate: rolling every invoice into ONE headline "Outstanding" number (unlike `PagamentiTab`'s separate, individually-labeled sections) means a classification gap here is far more visible/costly than the same gap on a tab that shows each bucket separately.
- **Shared logic:** `lib/billing/finance-summary.ts::summarizeInvoicesForFinanceCard(invoices, today)` — pure, takes the structural `InvoiceLike` type (not `Payment` directly) so both pages' differently-shaped invoice arrays satisfy it without a cross-import. Returns `{ byCurrency: [...] }` — one entry per currency actually present (sorted alphabetically, not by query order), because a real sandbox account (`Active Acres Properties LLC`) turned out to hold both EUR and USD invoices, live-proving the mixed-currency case reviewers flagged as "unconfirmed in production" was real. Classification is **exhaustive by default**: anything invoiced that isn't `Paid`/`Cancelled`/`Voided`/`Split` counts as outstanding (a Draft or Partial invoice past due — a routine state, not a rare glitch — used to match neither the overdue nor pending filter and silently read as $0 owed). A **credit note is excluded by its `CN-` number prefix**, not only by `invoice_status='Credit'` — a credit tied to a split/installment-plan invoice is not necessarily issued right away (Antonio, 2026-08-30), so it can sit briefly at `invoice_status='Draft'` before being retagged; excluding by the number assigned at creation closes that window too. The outstanding sum uses the **remaining balance** (`amount_due`, falling back to `total − amount_paid`), never the original face amount, so a partially-paid invoice no longer shows as fully owed.
- **Presentational component:** `components/shared/finance-summary-card.tsx::FinanceSummaryCard`. `accountId`/`autopayEnabled`/`autopayLast4`/`isAdmin`/`onDisable`/`onSendLink` are all optional — a "Personal (no company)" card passes none of them plus `showAutopay={false}` to hide the whole row instead of showing a misleading "Off" for an account that doesn't exist. `showAutopay` is ALSO false for a non-`'Client'`-type account (Vendor/Partner/One-Time) — the first build offered card enrollment on those too. "Send enrollment link" now confirms before sending, matching "Turn off"'s existing confirm; both call `router.refresh()` on success so the card can't read stale after a write from either host page (a server-action `revalidatePath` only refreshes the ONE route it names — the account page's own revalidate left the contact page's copy of the same card stuck on the old state).
- **Staff actions** (`app/(dashboard)/accounts/actions.ts`): `disableAccountAutopay(accountId)` calls the SAME `disableAutopayCard()` the client's own self-service disable uses, actor-tagged from the dashboard session — a real, immediate write; also passes the real auth user id through so the retired What's New note's `deleted_by` correctly names the staff member, not the system actor. `sendAutopayEnrollmentLink(accountId)` creates a Stripe `mode:"setup"` Checkout Session (same helpers the client-facing enrollment flow uses) and posts it as an admin portal chat message — this is the ONLY "enable" path from the CRM, because TD never sees or stores a client's card. It now: (1) checks `isCardAutopayEnabled()` FIRST — the first build was the one autopay-enrollment surface that didn't, so a staff click could arm an account while the global switch was off (`saveAutopayCard()` itself also refuses to flip the flag while the switch is off, as a second, independent backstop for the webhook-completion side); (2) refuses if the account is already enrolled; (3) refuses if the account has no linked contact to send to, instead of reporting a false "sent"; (4) refuses a repeat send within 24 hours (checked via an existing admin message containing a `checkout.stripe.com` link — survives future wording changes); (5) sends the Italian variant when the resolved contact's `language` is Italian (`lib/locale.ts::isItalian`), matching every other client-facing system string; (6) says **"future invoices"** explicitly — the fee waiver and the auto-charge cron both apply forward from enrollment only, never to an invoice already created (its fee rate is pinned at creation, R098-adjacent convention) — the first wording implied otherwise and was checked against every real outstanding invoice in production, all of which were already fee-pinned. **`portal_messages.sender_id` is `uuid NOT NULL`** — it must be the real dashboard auth user id (`createClient().auth.getUser()`), never `getDashboardActor()`'s `"dashboard:name"` string (that string is for `action_log.actor` only) — a real bug here shipped past `tsc`/lint/mocked-unit-tests and was only caught by clicking the actual button in sandbox (R079).
- **Notification:** `card_autopay_enabled` What's New event (`emitCardAutopayEnabledEvent`/`retireCardAutopayEnabledNote`, `lib/portal/chat-events.ts`) — fires from `saveAutopayCard()` (real webhook enrollment), retired unconditionally from `disableAutopayCard()` so a later re-enrollment notifies again instead of deduping forever. Catalog-seeded via `scripts/migrations/20260830-2200-seed-whats-new-card-autopay-event.sql` (sandbox only). Deep link in `thread-whats-new-panel.tsx` routes to `/accounts/<id>` (the Overview tab, where the card lives).
- **Deliberately NOT fixed in this pass — flagged, not silently resolved:** (1) the enrollment message is posted to the WHOLE company's shared portal chat, not one specific person — any linked member (or team-chat-capable teammate) could complete enrollment with their own card; closing this needs the same `invoices_billing` capability gate the client-facing route enforces, extended to admin sends, which is a messaging/permissions change bigger than this fix pass. (2) The auto-charge cron only considers `invoice_status='Sent'` invoices — most of a real account's currently-outstanding balance can sit at `'Overdue'` instead, meaning enrolling frequently does NOT make the card's headline "Outstanding" figure collectible by autopay. Whether the cron's candidate query should include `'Overdue'` too is a money-behavior decision for Antonio, not something changed here. (3) `is_test` invoices are still uncounted/unfiltered on this card, same as the pre-existing Payments tab — flagged as a pre-existing gap, not a regression.
- **How to verify current state:** same account/kill-switch queries as the card-autopay section above; `SELECT id, message FROM portal_messages WHERE sender_type='system' AND message LIKE '%card_autopay_enabled%' AND deleted_at IS NULL` for live (non-retired) notification notes.

### Tables
`payments`, `payment_items`, `client_invoices`, `client_invoice_documents`, `client_expenses`, `client_expense_items`, `td_expenses`, `td_expense_items`, `client_customers`, `client_vendors`, `recurring_invoice_templates`.

## Gotchas, invariants & past bugs
- **Never write `client_invoices` from any TD/staff flow** (R027) — it's the client's own sales ledger.
- **Never set `invoice_number` directly or add a timestamp fallback** — go through the two creator functions (R098). The April-12 collision came from a fallback format; it was deleted on purpose.
- **`createTDInvoice` is the ONLY entry for new TD invoices** — it deliberately uses raw Supabase (lint rule disabled inline) because the retry loop needs raw error codes that the `dbWrite` wrapper strips.
- **Credit netting is automatic** on real unpaid bills. To create a credit note itself (or any invoice that must not net), pass `skip_credit_netting: true` — otherwise a credit can wrongly net into it. A bill fully covered by credit is marked **Paid, $0 due** with an explicit "service − credit = $0" description.
- **The `client_expenses` mirror can no longer be updated by hand — a database trigger both keeps it in sync AND refuses any other direct write** to its money/status/date fields on a `source='td_invoice'` row (dev job `0dcb0a18`). A new writer of `payments` needs no mirror-sync call at all; a new writer that tries to touch `client_expenses` directly will get refused. `syncTDInvoiceStatus`/`syncTDInvoiceMirror` no longer write the mirror (see Status & sync above).
- **Invoice emails → portal Pay only** (R092). No Stripe/wire in the email body.
- **Card processing fee — CHARGED, booked at payment time, one authoritative column** (2026-07-15, dev_task `6ec6872a`, SANDBOX/branch `claude/card-fee-charge-6ec6872a`). The card surcharge (today 5%) was displayed everywhere but never collected on Stripe (regressed when Stripe replaced Whop). Now: both checkout routes charge `base × (1 + pinned rate)`; the Stripe webhook books the fee ONTO the invoice from the ACTUAL charge BEFORE the settle (`bookCardFee` in `lib/finance/card-fee-booking.ts` → then `applyMoneyToInvoice`), because the settle caps `amount_paid` at the invoice total. `bookCardFee` is ALL-OR-THROW + idempotent (one fee line via the partial unique index `uq_payment_items_one_fee_per_payment`; `total = charged` is a SET) → a failure returns non-200 and Stripe retries from a non-terminal invoice. Fee math lives ONLY in `lib/payments/card-fee.ts` (`computeCardTotal` for display+charge; `deriveFeeFromCharge` for booking — never recompute the rate at booking, book `charged − base`). **The money-truth is `payments.card_fee_amount`; the fee LINE (`payment_items.item_type='fee'`) is a projection kept equal. Invariant (live-tested): `card_fee_amount = sum(fee lines) = total − base`.** `base = sum(item_type<>'fee')`. **Every `payment_items` writer MUST preserve `item_type`** — `buildRegeneratedLineItems` + the credit-netting reinsert now do; `updateInvoice` is Draft-only (a fee only exists once Paid) so it never sees one; voids fully cancel (invariant untouched). A base/charge mismatch books NO fee and raises a staff `portal_issues` "!" alert (overage review); a paid-but-activation-failed case raises one too (deduped on the gateway payment id). Rate config in `app_settings.payment_fee_config.card_rate`, read ONLY at creation, PINNED onto `offers.card_fee_rate` / `payments.card_fee_rate` so editing it never re-prices a signed deal. **LIVE ON PROD since 2026-07-15 (PR #158, main @ 46cf4e1b) — deployed with the fee OFF per the go-live runbook.**
- **Global kill switch + Finance UI button (Council-approved Phase A, 2026-07-15).** `payment_fee_config.enabled` (default ON when missing) is checked by `resolveChargeRate()` at BOTH checkout routes: OFF → effective rate 0, overriding every per-deal pin, no redeploy. Admin-only switch card on Finance → Overview (`app/(dashboard)/finance/card-fee-switch.tsx` → `toggleCardFee` server action in `finance/actions.ts`): the role check lives INSIDE the action (`isAdmin`), not just page placement; two-step inline confirm; copy states the ~60s propagation (per-instance config cache) and that already-issued payment links keep their price. Flips are audit-logged to `action_log` (`card_fee_enabled_changed`, actor `finance-ui:<email>`).
- **Two hardening invariants (Council blockers, 2026-07-15):** (1) `setConfiguredCardFeeRate` MERGES into the `payment_fee_config` JSONB (`{ ...current, card_rate }`) — a whole-object write would delete `enabled:false` and silently re-arm the fee. (2) The offer-signed webhook passes the OFFER's pinned rate into `createTDInvoice` via `pinnedRateForInheritance()` (`lib/payments/card-fee-config.ts`) so the sign-time invoice INHERITS the deal's pin instead of re-reading live config — prerequisite for any per-deal waiver (a waived offer must not mint a 5% invoice). `agreement-signed` is deliberately NOT changed: it is an offer-less renewal root and correctly pins the configured rate. Tests: `tests/unit/card-fee-config.test.ts`, `tests/unit/card-fee-switch-action.test.ts`.
- **ASSUMPTION the card-fee refund policy rests on — do not break silently:** "refund keeps the 5%" (Antonio's decision) currently holds ONLY because **there is NO automatic refund path** — refunds are MANUAL credit notes (`createCreditNote`), staff-entered, so nothing auto-returns the fee. If anyone ever builds an auto-refund (e.g. a Stripe `charge.refunded` webhook → credit note), it MUST credit the BASE only (a −base line, `card_fee_amount=0` on the credit note) and leave the original invoice's fee line + `card_fee_amount` intact — else it silently hands back the service amount AND orphans a now-unmatched fee. (dev_task `6ec6872a`.)
- **The dunning PASS is `lib/billing/dunning.ts::runDunning` — shared by the daily `invoice-overdue` cron AND the dashboard "Run reminders now" button (`POST /api/invoices/run-dunning`, admin).** It does TWO jobs: (a) mark Sent/Partial invoices Overdue when past due — ALWAYS runs; (b) send due reminders — **queued to the background job system, not sent inline.** `enqueueDueReminders()` bulk-enqueues an `invoice_reminder` job (handler `lib/jobs/handlers/invoice-reminder.ts` → `sendInvoiceReminder`) per due invoice at **priority 8** (below operational priority-5 jobs, so a reminder burst never delays formation/onboarding), deduped against already-pending jobs in one query, via `enqueueJobs()` (single bulk insert + one worker trigger). The shared `process-jobs` worker (~10/5min, retries, Exception Center) delivers them — no function timeout, no email-reputation spike. The cap now **bounds enqueues per pass** (`getDunningCap()` reads `app_settings.dunning_autosend.cap`, default `DUNNING_RUN_CAP`=40, clamped 1–`DUNNING_CAP_MAX`=1000 via pure `clampCap()`; UI "Max per run" field). Eligibility = pure `shouldRemindNow()` (1st at r1, 2nd at r2, cap 2; unit-tested). At send time `sendInvoiceReminder` re-checks the invoice STATUS (so an invoice paid between enqueue and send is skipped, not emailed) and the ACCOUNT PAUSE (boolean `dunning_pause` + dated `dunning_pause_until`, 2026-07-03) — but NOT the 2-reminder cap (that is enqueue-time only). **Automatic sending is controlled from the UI, NOT an env var:** `isAutoSendEnabled()` reads `app_settings` key `dunning_autosend` (value `{enabled:boolean, cap:number}`, default OFF/40); the ON/OFF toggle + "Max per run" field + "Run reminders now" button live at the top of **Finance → All Invoices** (admin-only). The old `DUNNING_AUTOSEND_ENABLED` env var is retired. With auto-send off, invoices are still flagged Overdue so the dashboard list + manual/bulk "Send Reminder" (independent of the cron) keep working. Per-account `dunning_reminder_1_days` default 7, `_2_days` default 14, cap 2, `dunning_pause`; the `/api/invoices/[id]/remind` route and the manual "Send Reminder" finance action use the same function. **The cron MUST call it in-process — never via an internal `fetch` to `process.env.VERCEL_URL`.** That self-HTTP pattern hit the Vercel "Authentication Required" deployment-protection wall and sent ZERO reminders while logging ~119 errors/day (discovered 2026-06-19, dev_task `d2af38a1`). Reminder emails are bilingual EN/IT by `contacts.language`. Cadence is per-account configurable from the **account page → Payments tab → "Payment Reminder Settings"** (edits `dunning_reminder_1_days` / `_2_days` / `dunning_pause` via `updateAccountField`, which whitelists + range-validates them 1–365). Each invoice row in Finance → All Invoices also shows a reminder badge (count + last sent + auto/manual) and supports bulk reminders (`sendBulkReminders`, cap/pause-aware). `dunning_escalation_email` and a global default are not yet wired (read-but-unused / code-default 7/14).
- **Invoice recipient resolution is centralized in `resolvePaymentRecipient()` — never hand-roll a `role = 'Owner'` lookup.** Priority: `payment.contact_id` → owner-role contact (CASE-INSENSITIVE: "Owner"/"owner"/"Co-Owner" all qualify) → any linked contact with an email → `accounts.communication_email`. The old code duplicated an exact-case `.eq('role','Owner')` match in three send paths; accounts whose owner link was stored lowercase `"owner"` resolved zero rows, so resend errored "No contact email found" even though the contact had a valid email (the **ADWise incident, 2026-06-18**, dev_task `63431fc8`). All three paths now delegate to the resolver. NOTE: the *first* send for ADWise (INV-002346) still went out fine — only the account-anchored **resend** (payment with `contact_id` null) hit the brittle owner branch.
- **Banks are referenced by stable id, not list position.** An invoice stores `settings_bank:<uuid>`; each bank in `invoice_settings.bank_accounts` carries an `id` (assigned on add in the settings UI via `crypto.randomUUID()` + backfilled server-side in `app/api/invoice-settings/route.ts`). This replaced the old positional `settings_bank_N` (2026-06-16) which had a latent bug: the dialog indexed the active-filtered list while `resolveBankDetails` indexed the unfiltered list, so an inactive bank could put the wrong bank on an invoice. The DB CHECK constraint (`payments_bank_preference_check`) was widened to accept `settings_bank:<uuid>` via `scripts/migrations/20260616-1410-widen-payments-bank-preference-constraint.sql` (production DDL applied via Supabase dashboard — the MCP tool blocks DDL).

## How to verify current state
- Read the three creators: `lib/portal/td-invoice.ts` (`createTDInvoice`), `lib/portal/unified-invoice.ts` (`createUnifiedInvoice`), `lib/portal/invoice-number.ts`.
- Confirm the race-safety indexes exist:
  `SELECT indexname FROM pg_indexes WHERE tablename IN ('payments','client_invoices') AND indexname LIKE '%invoice_number%';`
- Confirm the idempotency guard: same query with `LIKE '%idempotency%'` on `payments`.
- Confirm the four worlds are distinct tables: `payments`, `client_invoices`, `client_expenses`, `td_expenses`.
- Note (R096): use the **sandbox** MCP / `psql` for sandbox; production `execute_sql` hits production.
