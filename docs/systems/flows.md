# Service Flow Workspaces

_Last verified against code: 2026-09-04 — Claude (**Confirmed and enforced: the ITIN "Client Signing" mailing instructions (forms + mail-to-CAA instructions, embedded via `buildItinShippingCard` in `app/portal/flows/[id]/page.tsx` and rendered through `FlowStageJourney` → `ItinShippingForm`) are the ONLY real client-facing surface for this step — a leftover pre-workspace duplicate page (`/portal/itin-documents` + its own confirm-mailed button) was found still live in the sidebar and deleted.** Verified two ways before touching anything: live production query (3 real ITIN service_deliveries at "Client Signing" today, none of them routed anywhere near the old page) and the actual current wording of the `itin_form_*` MCP tool docs (`lib/mcp/tools/itin-form.ts`), which describe the workspace + a portal chat notification as the sole delivery mechanism with no mention of a second page. The old page's own `confirmItinMailed` server action was a real, load-bearing dependency of `ItinShippingForm` (the workspace's live confirm-mailed button imported it directly) — moved to `lib/portal/itin-mailing.ts` first, both callers repointed and verified working, THEN the old page was deleted, so this was never at risk of breaking the real client-facing flow. If you're looking for "where does a client confirm they mailed their ITIN forms," `ItinShippingForm` + `confirmItinMailed` is it — do not resurrect a second page for this.)_
_Prior: 2026-09-03 — Claude (**The client dashboard's service-status stepper can now glow amber on a step that's genuinely the client's turn — reusing the SAME registry that already drives the client email/chat/bell dispatch (`action-stage-registry.ts`), so the two surfaces can never disagree about what counts as "your turn." Requested by Antonio after seeing the ITIN step look identically passive whether it was waiting on the client, on staff, or on the IRS.** `buildFlowSteps` (`lib/flows/flow-progress.ts`) now takes the flow's service_type and the SD's id, and computes `isActionRequired` + `actionHref` for the CURRENT step only, by looking it up in `action-stage-registry.ts` — never by stage position, never a blanket "whatever's current." `FlowProgressTracker` (`components/portal/flow-progress-tracker.tsx`) renders that as an amber pulsing dot + an "Action required" caption + makes that one step a direct link to the action, reusing the same visual language already live on the Company Formation tracker (`formation-progress-tracker.tsx`) rather than inventing a new one. Registered ITIN's wizard-start stage (`'ITIN::Data Collection'`) in the registry for the first time — it already existed for ITIN's later signing stage but never for the very first step, so the client's first "your turn" moment on the dashboard never lit up before this. Confirmed live in sandbox (a temporary service-delivery fixture on the designated QA account, deleted after): the ITIN wizard-start step glows and links correctly, and sibling waiting-on-us/waiting-on-the-IRS stages (RA Renewal Due, Annual Report Due, etc.) correctly stayed plain blue — confirming the registry-gated design doesn't create false alarms on stages nobody can act on. Companion fix — the dashboard's own pending-items card had a matching gap for this exact moment — is in `portal.md`, same date. Side-fix made in passing: sandbox's ITIN pipeline stages had empty client-facing text where production has the real wording — copied production's text into sandbox in the same pass so this and future ITIN portal work is actually testable there; this alone was silently breaking the whole ITIN stepper in sandbox before now.)_
_Prior: 2026-09-01 — Claude (**`GET /api/flows/[id]/submission` (backs the Workspace's "Client Details" panel, formation branch) gained a fallback to `formation_submissions` when no `wizard_progress` row exists — dev job `9a9c5cf5`, round 4.** Live production incident: staff saw "No submitted data found for this client yet" for a client (Francesco Lussignoli) who had genuinely submitted — his `wizard_progress` write had silently failed (2026-08-27 missing-column incident, full root cause in `formation.md`). The companion "LLC Name Approval" panel's one-time initializer (`lib/operations/formation-name-checks.ts::getOrInitNameChecks`) had the identical gap and got the same fallback — see `formation.md`'s round-4 entry for the full incident writeup, this is a pointer.)_
_Prior: 2026-08-28 — Claude (**`app/api/flows/[id]/save-ein/route.ts` recorded the EIN and notified the client correctly but never re-triggered the `welcome_package_prepare` job — the ONLY step that seeds a company's banking-application record, which staff notifications for a later banking-wizard submission depend on to have anything to key off of.** Confirmed via 90 days of `action_log`: this is the ONE screen staff have actually used for every real EIN entry this quarter (the older `lib/operations/ein-received.ts` path, which DOES re-trigger the job, saw zero real usage in the same window) — so every account processed here was silently missing its banking setup. Real incident: BRIXEL LLC submitted a bank application through the portal and staff got no internal alert at all (dev job c3efa6cb). Fixed with a best-effort `enqueueJob({ job_type: 'welcome_package_prepare', ... })` call added right after the EIN save succeeds, mirroring the working legacy path exactly — a failure here never blocks the EIN save itself. Full root-cause chain, the shared `getOrCreateBankingSubmission` fix, and the affected-account backfill are documented in `whats-new.md`. NOT fixed in this pass (flagged, confirmed near-zero real usage so lower urgency): the sibling `lib/tasks/workflow-handlers/formation-confirm-ein-received.ts` handler has the identical gap.)_
_Prior: 2026-08-21 — Claude (**`app/api/flows/[id]/members/route.ts` had zero caller-auth — any authenticated session, including an ordinary portal client, could read another company's member roster or flip who's flagged as the SS-4 signer.** Found in the same security sweep that fixed 15 other account/contact-scoped CRM routes with the identical gap (dev job `9d80395e-cef4-4c76-998b-c23a5f99684b`; full account of the sweep in `docs/systems/crm-core.md`). The route's own header comment claimed "Staff-only (gated by middleware — /api/flows is not in the public allowlist)" — that was FALSE: middleware only checks role on dashboard PAGE navigations (`isDashboardPath` excludes everything under `/api`), so any `/api/flows/**` route inherits "is logged in" only, not "is staff," unless it checks for itself. Both GET and POST now call the codebase's canonical staff gate, `requireStaffRoute()` (`lib/auth/require-staff-route.ts`, already used in 22+ other routes), as their first statement. **Not fixed in this pass, flagged as a known follow-up:** the Bug-Hunter reviewer flagged that the same missing-check pattern is very likely present across the ~15 other sibling routes under `app/api/flows/[id]/*` (`activate-ra`, `advance`, `generate-ss4`, `save-ein`, `send-ss4`, `set-stage`, `revert`, etc.) — none of those were audited or touched in this pass; treat their "gated by middleware" comments (if present) with the same suspicion until each is actually checked.)_
_Prior: 2026-08-13 — Claude (**The tax room becomes the staff surface: a visible door, and the client's WHOLE conversation (card c5ff8b4d Phase 1, Antonio's binding ruling: the workspace is the ONLY staff surface for tax returns).** Two flow-component changes shipped with the 4a39e0fd fix wave. **(1) The door** — new `components/flows/tax-workspace-banner.tsx`: a prominent top-of-page banner on the ACCOUNT page linking to the account's Tax Return flow workspace, mirroring the FormationWorkspaceBanner pattern, so staff reach the room from where they already are (Antonio: "a piece you have not seen on screen is labeled not-verified" — this one was browser-verified in his Test 2 pass, 2026-08-12). **(2) Whole-conversation chat** — `components/flows/flow-chat.tsx` + the room's chat route now return the client's ENTIRE account conversation, each message tagged `in_flow`: messages belonging to THIS return's thread render as before; the client's OTHER messages render QUIETER (dashed border / lighter blue) with an explicit "· other conversation" label. The defect this kills: a client answering about their tax return in the general chat was invisible to the person working the return. Empty-state copy changed accordingly ("No messages yet with this client"). Staff replies from the room still post into the flow's own thread — display widened, send-scope unchanged. Layout note from the same wave: sandbox's 17 tax `stage_layout` rows were EMPTY copies (rooms rendered blank) — production layouts copied in 2026-08-12; the standing practice is now to check the stage's layout EXISTS in the environment you claim a room works in (227 real CMRA clients with blank rooms → card `72ac5d42`, unassigned).)_
_Prior: 2026-08-10 — Claude (**SS-4 card gains an always-visible responsible-party block + signer picker — dev job `833562d7` (ACE Marketing Group LLC).** `components/flows/ss4-panel.tsx` now shows WHO signs the SS-4 at the top of the card, always — the default signer is auto-picked from a role hint at creation, so a wrong default must be catchable at a glance during review. Below it, a native full-width select (phone-PWA-usable) lists EVERY contact linked to the account with their role shown; roles inform, they never restrict (Antonio: the responsible party is decoupled from ownership by design). Choosing a person POSTs `{set_signer: contact_id}` to the flows generate-ss4 route → shared core `lib/operations/ss4-set-signer.ts`, which rewrites the party as a set, resets awaiting_signature→draft, rotates the access code (previous signing link dies), repoints the internal SS-4 documents row, syncs `members.is_signer` when member rows exist, and clears the old signer's chat/bell. The GET on that route now also returns `candidates` + the current `contact_id`/`responsible_party_name`. Signed = select disabled, immutable. Default-pick rule + signing-page hardening + Antonio's binding rulings: `formation.md` (2026-08-10). Council-fix addendum same day: PICK WINS — a non-member pick survives every refresh/regenerate (members flags not consulted while a non-member is the signer); the panel re-loads server state after Regenerate (was blanking the signer block); route maps infrastructure errors to 500. Round-3 (2026-08-11): the flow Members panel now surfaces the SS-4 auto-refresh outcome in an amber banner (kept-pick note / flag alerts / orphan alert — "Set as signer" is never a silent no-op), the regenerate response carries the kept-pick note, and the SS-4 card's draft toast no longer claims the client was notified. Round-5 (2026-08-11): send-ss4 and resend-ss4 run the shared promotion gate (`assertSs4PartyPromotable`) before any flip to awaiting — an orphaned responsible party (deleted/repointed member, never picked) is refused with the picker-pointing message instead of being re-armed with a fresh link; and a member-flag signer swap through the members panel now revokes the old signer's link, resets to draft and clears their prompts (full picker semantics). Detail in `formation.md` addenda.)_
_Prior: 2026-08-06 — Claude (**FORMATION STATE ON THE WORKSPACE NOW RESOLVES THE FULL CHAIN — WS-B scope amendment, dev job `c0a61e44`.** For an account-less formation SD, the workspace page (`app/(dashboard)/flows/[id]/page.tsx`) used to derive the state from the client's WIZARD data only and silently fall back to New Mexico — so the SOS filing links (`resolveFormationFilingLink`, "Check/File on SOS") pointed staff at the NM portal on a signed-Wyoming deal, at the exact step where the legal filing happens. It now resolves **wizard answer → formation-form submission → the SIGNED offer's pinned state** (pure `resolveFormationStateCode` in `lib/formation/states.ts` + `formationStateForClient` lookup) and yields the display NAME, or **null when nothing decided** — the filing-link component already renders a visible "Defaulted to New Mexico" note + badge for null, so undecided is SHOWN as undecided, never silently asserted. The account row's own state still wins when an account exists (unchanged ordering). Related same-change behavior: advancing to "Articles Received" passes a state to materialization ONLY when a tier actually decided it — an undecided deal now errors loudly there instead of filing NM (architect-approved; see `formation.md` 2026-08-06).)_
_Prior: 2026-08-04 — Claude (**THE "SS-4 IS INTERNAL ONLY" RULE IS REVERSED** — Antonio, 2026-08-04: *"the SS4 visible to the client is ok."* Entries below still describe the old rule and are kept for the history; do not re-derive it from them. **What changed:** the worker no longer warns when an SS-4 is attached to an outbound email. **What did NOT change, deliberately:** this subsystem's client-visibility allowlist. The signed SS-4 (flow stage "Signed") is still absent from the client-safe stages and the sign handler still writes it not-visible, so the client PORTAL behaves exactly as before — reversing a warning is one line; changing what 704 SS-4 documents do on the portal is a behaviour change nobody asked for and is its own decision. Detail in `docs/systems/inbox.md` (2026-08-04d).)_

_2026-07-28 — Claude (**Workspace surfaces stop swallowing Company-Formation materialization failures.** Companion to `formation.md` (same date, dev job `9636a59f`) — the Covelli/DoctorGut case: three "successful" advances into "Articles Received", no account created, failure visible nowhere. Workspace-side changes: **(1)** the flow advance route passes a new staff `entity_type` (SMLLC/MMLLC) through to `advanceServiceDelivery`, returns the structured `materialization` outcome, and maps a runtime materialization failure into the existing `warning` channel (`ActionButtons` already renders warnings — no component change needed there). A DETERMINISTIC failure (no data / no filed name / unresolvable LLC type) now refuses the advance up-front with an actionable error instead of succeeding silently (gate lives in `advanceServiceDelivery` §4b, so the action buttons, the stepper's forward path, workflow-card `chain.advance_sd_stage` actions, and MCP `sd_advance_stage` all get it). **(2)** `upload-document` route: accepts `entity_type`, forwards `formation_date`+`entity_type` to the advance, includes `advance.materialization` in its response (previously discarded server-side — the exact reason Luca saw green), and folds refusals/failures into `detail` with a ⚠️. **(3)** `document-upload.tsx` Articles modal: on open it now calls new `GET /api/flows/[id]/materialize-preflight` (read-only dry-run) in parallel with the date OCR — when the LLC type is unresolvable the modal REQUIRES a Single-/Multi-Member select before commit (Antonio: ask at upload time); an unresolvable-by-staff blocker (no filed name / no formation data) disables Confirm with the reason; post-commit, a failed company creation renders an AMBER warning, never the green success line. SANDBOX — Antonio ships._
_2026-07-27 — Claude (**EIN-received client message no longer names banks.** The Company Formation workspace's "Record EIN Received" action (`app/api/flows/[id]/save-ein`) sends the client a long bilingual message; step 3 used to list the providers by name — "you can apply at: Relay, Mercury, Sokin, Payset, or Wise… For Wise, go directly to wise.com and apply there" (the copy recorded in the 2026-06-20 2nd-QA-round entry below, now SUPERSEDED). That hardcoded roster drifted the moment the bank list changed, and it sent clients OFF-portal to a provider TD no longer recommends. Step 3 now just points at the portal — EN: "Go to Bank Applications in your portal sidebar and start your application from there — you'll find the banks we recommend, with instructions for applying. You can apply to more than one."; IT equivalent. **No bank is named in this message any more, by design** (Antonio 2026-07-27): the roster lives in the `bank_referrals` catalog he edits from the CRM, and the portal page is the single place it is stated. Wise was removed from that catalog in the same change. If you ever need to change which banks clients are told about, edit the catalog — do NOT re-add names here. Full detail in referrals-circleback.md + portal.md.)_
_Prior: 2026-07-26 — Claude (**Closure now archives to Google Drive DURABLY, and a pre-existing wrong-bucket bug (closure attachments NEVER reached Drive) is fixed.** The closure completion route (`app/api/closure-form-completed/route.ts`) saved best-effort with no retry/marker (as did the review path `lib/operations/closure-review.ts` + MCP tool `lib/mcp/tools/closure.ts`). It now enqueues the durable `archive_submission` job (generic engine `lib/forms/archive-*`) with folder+bucket+config+uploadPaths PINNED, plus marker columns `closure_submissions.drive_archived_at`+`drive_archive_meta` (migration `20260726-1400`) and the per-table backstop sweep (`/api/cron/forms-archive-sweep`, DRY-RUN default). FILING is unchanged, made reliable via `lib/forms/closure-folder.ts`: the company folder when an account is EXPLICITLY linked to the submission (never inferred from a contact link — a person may own several companies), else a deterministic `Leads/{client} - {llc} (Closure)` folder (`TD_CLIENTS_ROOT_ID` env-overridable for the sandbox test only). **BUG FIXED:** the closure config bucket was `onboarding-uploads` but the external closure form uploads to `closure-uploads` (`app/closure-form/[token]/[code]/page.tsx`), so every closure attachment downloaded from the wrong bucket and 0 files ever reached Drive (silent, pre-existing). Fixed the config default to `closure-uploads` AND — because closure has TWO upload origins (external form → closure-uploads; portal wizard → onboarding-uploads with `closure/`-prefixed paths) — added a per-path bucket resolver (`ArchiveRecipe.resolveBucket`; closure = `isWizardUploadPath(path) ? onboarding-uploads : closure-uploads`), applied per file inside `saveFormToDrive` via `opts.resolveBucket`. It's a code-side recipe function (not part of the JSON pin), so a pinned closure job still splits buckets per file. Bug-hunter 3 rounds, all findings CLOSED. E2E `scripts/sandbox-qa/closure-archive-e2e.mts` 17/17 vs the isolated test drive — proves BOTH upload origins copy, company-vs-Leads filing, partial-throws, resubmit-clears-marker. Tax / banking / ITIN archival untouched. Dev job `6e8a8045`.)_
_2026-07-25 — Claude (**ITIN now archives to Google Drive DURABLY and files under the PERSON, never the company's main area (Antonio's rule).** The ITIN completion route (`app/api/itin-form-completed/route.ts`) previously filed a company-owner's ITIN into the company's MAIN folder and an individual's into a `Leads/{name}` folder. Now `lib/forms/itin-contact-folder.ts::resolveItinContactFolder` resolves the CONTACT folder: company-owner (submission's account, else the contact→`account_contacts` link, that has a `drive_folder_id`) → the company's "2. Contacts" subfolder; individual (no company / no company folder) → `Individual Clients/{name} ({idSuffix})` (root id `1P7omomS8yBb8vkSmirwTj1AdcSXDraqv`, overridable via `INDIVIDUAL_CLIENTS_ROOT_ID` only so the sandbox test drive can point elsewhere). The WHOLE ITIN package moves under the contact — the data summary here AND the step-5 W-7/1040-NR/Schedule OI, which reuse `driveFolderId`. SAFETY (bug-hunter, 2 rounds, all findings CLOSED): the individual folder name is keyed to the person's identity (`shortId(contactId||leadId||token)`) so two same-name people can NEVER share a folder (their W-7/passport would overwrite → cross-client exposure); it deliberately does NOT persist `contacts.drive_folder_id` (that column is read+reused by formation's `ensureContactFolder`, so writing an Individual-Clients link there would hijack the later company contact folder); a company whose account has NO `drive_folder_id` yet THROWS (retryable + loud) rather than misfiling into Individual Clients — the backstop sweep archives it once the folder appears. Archival runs on the GENERIC engine (`lib/forms/archive-*`; banking was the first form): the route enqueues the durable `archive_submission` job with folder+bucket+config+uploadPaths PINNED (`uploadPaths = column ∪ collectUploadPaths(submitted_data)` via `deriveUploadPaths`, so a portal-wizard passport kept INSIDE `submitted_data` — the column is empty for wizard rows — is not silently lost). Marker columns `itin_submissions.drive_archived_at` + `drive_archive_meta` (migration `20260725-1200`); the per-table backstop sweep `/api/cron/forms-archive-sweep` (DRY-RUN default) discovers any un-archived ITIN. Route + recipe share `resolveItinClientName` so the inline save and the durable job resolve the SAME folder. E2E `scripts/sandbox-qa/itin-archive-e2e.mts` 20/20 vs the isolated test drive (company→"2. Contacts"/ITIN, individual→Individual Clients, file-recovered-from-data, partial-throws, resubmit-clears-marker, same-name→different-folders, company-no-folder-throws). Tax + banking archival untouched. Dev job `6e8a8045`.)_
_2026-07-24 — Claude (**The workspace's "Request Changes" moved the STAGE and nothing else — the client was told to edit and then refused.** Full root cause in `tax-returns.md` (same date). The workspace review buttons POST `/api/flows/[id]/advance`, which calls `advanceServiceDelivery` ONLY; the portal decides editability from `tax_return_submissions.review_status`, so pressing Request Changes advanced the SD to "Revision Requested", fired the generic stage notification at the client, and left him locked. **⚠️ THIS DOC WAS PART OF THE PROBLEM:** the line below — *"Review actions live on the SD stage"* — contradicted `tax-returns.md`, which states review sub-states are deliberately NOT actionable outside the What's New buttons "where the state machine is enforced". Both were half-true and neither surface kept the other in sync. **NOW:** the advance route mirrors the stage into the review state machine — on `to_stage === 'Revision Requested'` it calls `syncTaxRevisionRequest` (`lib/tax/sync-flow-revision.ts`), which writes `review_status='revision_requested'` (TOCTOU-guarded, truthful staff history round), resolves the open What's New card, and sends the client the bilingual action-required package. Best-effort by contract: a sync failure never rolls back the advance the staff member already saw succeed — instead the route returns a `warning` and `ActionButtons` renders an amber "the client's tax form was NOT unlocked — press again" notice, so a rare concurrent-write conflict can't be silent. It targets the SAME submission the wizard gate will open (oldest-open tax year's latest submission, else latest overall), so button / gate / prefill / lock act on ONE row rather than the sync blindly taking the newest and cross-contaminating a returning client's tax years. **RESUBMIT NOW CLEARS THE STAGE:** `SD_ADVANCE_FROM_STAGES` in `lib/jobs/handlers/tax-form-setup.ts` gained "Revision Requested", so the client's resubmit parks the SD back at "Data Submitted" (45). Without that the SD sat at 47 forever and `overlayEffectiveStageName` — which takes the FURTHER of SD stage and review-mapped stage — pinned both the Tax Board card and the client tracker on "Revision Requested" for the rest of the loop, i.e. the workspace kept showing *"Waiting for the client to resubmit"* after he already had. "Revision Requested" is now registered in `lib/services/protected-stage-names.ts` (a catalog rename would silently break that move; the existing guard test caught it). **BUTTON VISIBILITY, verified against production `stage_layout`:** `start_review` renders at "Data Submitted"; `approve` + `request_changes` at "Under Review"; "Revision Requested" renders a `waiting_notice` with NO buttons — so there is no re-press footgun at that stage. **STAGE-ORDER CORRECTION (R093, verified live in BOTH envs):** the "15-stage flow" list below states Data Submitted 60 / Under Review 65 / Revision Requested 67; the LIVE `pipeline_stages` rows are **45 / 46 / 47**. The live values are what the overlay and `applyConfirmedTaxSubmission` (`stage_order < 50`) actually compare — and 47 being under 50 is why a client's Confirm still releases the delivery. Treat the numbers in that list as stale. Note `start_review` / `approve` still move the stage ONLY — this change wired `request_changes` alone, the one Antonio asked for. Sandbox E2E `scripts/sandbox-qa/carasso-edit-button-e2e.mts` (29 checks incl. non-tax and contact-scoped flows correctly ignored). Suite 7023 green. Dev job `84c8220b`.)_
_2026-07-22c — Claude (**The ITIN review card was never created for a month — the wrong card answered the duplicate check.** `app/api/itin-form-completed/route.ts` spawns the `itin_review` workspace-pointer card (the What's New note telling staff a client has submitted and their W-7 / 1040-NR / Schedule OI are ready to review). It deduped on `service_delivery_id`, but the dispatcher's check was not scoped to a workflow — so the `itin_data_collection` card ("Send wizard link to client", spawned on `sd_created`, carrying the same `service_delivery_id` in its `sd_progress_v1` meta) matched, `already_spawned` was returned, and the review card was never created. The plain-task fallback in this route did NOT rescue it either: `already_spawned` sets `workflowSpawned = true`, so the fallback insert is skipped and the outcome is NO task at all. Verified in production `action_log` BEFORE any change — Marcell Bogyora (07-11, 07-15, 07-20) and Tamás Fazekas (07-14) all `task_created / skipped`, each citing an `itin_data_collection` task id already Done; exactly one `itin_review` task exists database-wide (Daniel Pasztor, 06-25, predating the send-link workflow). Fixed in the dispatcher — full detail in `workflow-engine.md` (same date): the dedup now scopes to the caller's `workflow_slug`, ignores CLOSED cards (a re-submit overwrites the generated PDFs by stable filename, so staff must re-review or they mail the IRS a stale form), and fails CLOSED on a query error. This route passes `workflow_slug: 'itin_review'`. Blast radius TWO clients, both already past that stage — no backfill. Suite 6745 green; negative control reproduces the exact symptom. Dev job `0d5c7695`.)_
_2026-07-22 — Claude (**Italian clients were getting English — the free-text `contacts.language` trap, flow client notifications.** `contacts.language` is FREE TEXT: production holds 211 rows of "Italian", 200 "English", 47 null and five one-off spellings ("Italiano", "Italian - englis", "Italiano - Ingle", "Italian / Englis", "English or Italian") — and ZERO rows of "it". Every hand-rolled `language === "it"` check therefore matched NOBODY and served English to all 211 Italian clients. `lib/locale.ts` (`localeFromLanguage` / `isItalian`) is the single normalizer and its own header records this same bug being fixed in FIVE earlier places; it then recurred three more times. **Verified live:** Pietro De Pellegrino (`language` "Italian") was sent the ENGLISH ITIN wizard message on 2026-07-21 and staff re-wrote it by hand in Italian a minute later. Here: the workspace EIN-received message (`app/api/flows/[id]/save-ein`) and the formation name-proposal message (`lib/operations/formation-name-checks.ts`) both resolved the client's language with a strict compare, so the long bilingual EIN copy and the name-availability note reached Italian clients in English. Also the ITIN IRS-processing reminder cron (`app/api/cron/itin-processing-check`), which posts the 7-11 week progress updates. **Deliberately NOT changed:** `offers` (198 "it" / 42 "en"), `itin_submissions`, `oa_agreements`, `ss4_applications` genuinely store the SHORT CODE — verified against production — so strict comparisons there are CORRECT. Two sites that already test BOTH spellings by hand work and were left alone. **KNOWN, NOT FIXED (flagged):** ~40 sites compare `=== "Italian"` exactly, which still fails for the four contacts spelled otherwise (Alessandro Marcazzan, Emerson Ierardi, Patrick Covelli, Sofia Marinoni); under Antonio's rule — legal documents and forms stay ENGLISH, only the PORTAL is bilingual — most of those are correct as-is. Also flagged: `leads.language` is MIXED (82 "Italian", 20 "English", 4 "it", 1 "en"), so any strict check on a LEAD is wrong for ~102 of 107. New guard `tests/unit/client-language-guard.test.ts` scans only files that read the free-text tables, ignores an already-normalized `locale`, and ignores a line that also handles the long spelling — precise rather than noisy; its own negative-control assertions pin both directions. E2E `scripts/e2e-client-language.ts` (19 checks, sandbox-only) drives the REAL document-alert path per spelling; **negative control performed** — reverting the fix fails 6 checks and reproduces the exact production symptom while the 200 English contacts stay unaffected. Bug-hunter counter-review of the diff: ZERO blockers; the shape-change regression I suspected in the alert fallback was traced against the old code and REFUTED (the old account-owner branch was strict too, so it returned English for every production value — the change is strictly an improvement). Suite 6697 green. Dev job `0d5c7695`.)_
_2026-07-21 — Claude (**ITIN completion no longer files the client's package into a Leads folder** — companion to the portal-side person-ownership fix, full detail in `portal.md` (same date). `app/api/itin-form-completed/route.ts` derived BOTH the company label (`displayName`) and the Drive target from `submission.account_id`. That column is now deliberately NULL for every ITIN submission — an ITIN is keyed on the PERSON so that two members of one LLC cannot share one submission row — which would have sent every ITIN package of a company-owning client into `TD Clients/Leads/{name}/` instead of their company folder, and dropped the company from the generated file naming. The route now resolves a `driveAccountId`: the submission's account when present, else the contact's primary linked account (`account_contacts`, `is_primary` first, `account_id` as the stable tiebreaker — the same precedence `resolveContactLinkedDriveFolder` in `lib/flows/flow-drive-folder.ts` already uses for contact-scoped flow uploads). The `documents` rows themselves stay CONTACT-scoped, which matches what the recent working ITIN clients already have (Marcell Bogyora, Tamás Fazekas, Daniel Pasztor — verified in production: `account_id` NULL, `contact_id` set, `portal_visible` true) — only the binary's home borrows the company folder, exactly the pattern established on 2026-07-07. No change to the SD resolution at STEP 3: it already ORs on `contact_id` and the `account_id` clause could never match an ITIN SD anyway (ITIN SDs never carry one). Unit suite 6654 green; lint + build green. SANDBOX ONLY — Antonio ships.)_
_2026-07-20e — Claude (**ACTIVATION work split off too — this branch is now ONLY Marcell's fix.** Round-4 review found a blocker plus several majors, and every one of them was in the ACTIVATION path (`activate-service`) or the deactivate loose-task REPORT — not in the duplicate-ITIN fix. Rounds 3 and 4 produced ZERO new findings against the core (per-person guard in `itin-from-wizard`, the unique index, the deterministic ITIN pick in `itin-form-completed`). So the same call made for the Inbox applies here: `lib/operations/activate-service.ts`, the new `lib/operations/per-person-quantity.ts` + its tests, and `deactivateSD`'s `unlinked_open_tasks` reporting are REVERTED from this branch and live on `claude/activation-per-person-hardening` with their open findings recorded (the shortfall warning sits inside the try so a `createSD` throw skips it entirely and a billed unit is lost silently; the shortfall ignores `existingOfferCount` so a retry over-reports and tells staff to create an already-delivered ITIN; the sentinel rows in `unlinked_open_tasks` are counted as real tasks by both consumers — wrong shape, needs separate fields; and the whole activation branch has ZERO behavioural tests). **The two round-4 findings that DID land in the core are fixed here:** (1) `reactivateSD`'s per-person precondition filtered `status='active'` — the narrow scope of the INDEX, not the LIFETIME rule the rest of the system enforces; a contact with a `completed`/`on_hold`/NULL-status ITIN plus a cancelled one could reactivate into two lifetime ITINs with the active-only index unable to catch it. Now uses `.or("status.is.null,status.neq.cancelled")` and fails CLOSED if the check itself errors. (2) The headline regression test **passed for the wrong reason** — the mock returns rows regardless of filters, so reverting to the old `notes ILIKE` guard left it green. It now records the filters actually applied and asserts the dedup keys on the PERSON and never touches `notes`; **verified by reintroducing the old guard (test fails) and restoring it (passes)**. Also fixed: the Luca follow-up guard keyed on exact `task_title`, which embeds the client's own typed name — a re-submit correcting a spelling, filling a missing surname or fixing an accent produced a different title and minted the duplicate it exists to stop; it now keys on contact + category. Suite 6545 green, E2E 16 checks green, lint + build green. NOT on production — Antonio ships._
_2026-07-20d — Claude (**Council round 3 — two more blockers found in my own round-2 code, plus the deploy-order trap. The Inbox change is SPLIT OFF.** **(1) BLOCKER:** the per-person shortfall warning in `activate-service` was pushed BEFORE the creation guards ran, so a returning client who already held an ITIN and bought "ITIN ×2" got **zero** created while the summary announced *"Created 1 for the buyer"*. Two units billed, none delivered, report claimed success. **(2) BLOCKER:** silent under-delivery at quantity **1** — "buyer already has an ITIN, buys one for their spouse" was skipped with no warning at all and the activation step read `done`. Both fixed by extracting the decision into a PURE module `lib/operations/per-person-quantity.ts` (`describePerPersonShortfall`) that reports the REAL created count and is called after the fact — with 9 unit tests, because this runs post-payment and had ZERO coverage. **(3) DEPLOY-ORDER TRAP (Project Director, the sharpest risk and one my own gap-list missed):** applying the unique index BEFORE the code makes formation-setup jobs fail on an uncaught 23505 (the pre-fix `createItinDeliveriesFromWizard` has no try/catch around `createSD`) — a portal display bug turned into broken new-client setup; and shipping the code WITHOUT the `per_person` tag leaves the activation cap, the shortfall warning and the reactivate guard all silently inert. Both migrations now carry a mandatory ordering banner (code → index → tag) plus the live pre-check to re-run at apply time. **(4)** `SERVICES_STATIC` now carries `per_person`, matching the migration — the drift test compares code against an in-repo fixture, NOT the live catalog, so it was structurally blind to a migration-added tag; both are updated by hand and the limitation is now stated in the test. **(5)** `deactivateSD`'s loose-task lookup no longer swallows its error (says "couldn't check" instead of implying "nothing there") and flags when the 20-row cap was hit. **(6) SPLIT:** the Inbox "create service from email" rework (route through `createSD`, R099 error surfacing, dropped double task) is REMOVED from this branch and moved to `claude/inbox-create-service-hardening`. It has nothing to do with the ITIN bug, it changes a staff workflow that nobody has clicked, and it still has an open finding (the dialog offers service types the route now always rejects — prod has no pipeline stages for Public Notary / Shipping / Support). Keeping it here would have held the proven ITIN fix hostage to unproven work. Tests 6547→6556. NOT on production — Antonio ships._
_2026-07-20c — Claude (**PER-PERSON services are now a catalog property, and every "one ITIN per person" refusal is LOUD.** Council round 2 on the real diff, after Antonio corrected my premise. **The correction that matters:** I had reported an "ITIN ×2 breaks" money blocker. It was WRONG and the Bug-Hunter withdrew it in writing. Antonio's rule: *a person can't apply for two ITINs — the ITIN is one for a person.* So an offer line "ITIN ×2" ALWAYS means two ITINs for two DIFFERENT PEOPLE. Verified in prod: all 3 offers with ITIN quantity>1 are `contract_type='formation'`, and the only one ever activated (`adam-mihaly-pter-nemeskri-2026`) produced two ITIN SDs on two DIFFERENT contacts (Adam Mihaly + Peter Marton Nemeskeri). activate-service SKIPS bundled pipelines for formation/onboarding, so its quantity loop never ran for them; `createItinDeliveriesFromWizard` handled them, one contact per person. **`uq_itin_sd_active_per_contact` was correct all along** — it encodes the real rule. **Changes:** (1) NEW `per_person` catalog tag on ITIN (migration `20260720-2330-itin-per-person-tag.sql`, sandbox-applied) read by `getPerPersonServiceTypes()` / `isPerPersonServiceType()` in `lib/services/index.ts` — same mechanism as `start_at_wizard`, so the next per-person service (ITIN Renewal, an individual return) inherits the protection instead of reproducing the bug. Chosen over a third hardcoded `=== "ITIN"` in activate-service (AI Architect's ruling). (2) `activate-service`: per-person + quantity>1 caps `toCreate` at 1 and pushes a dedicated `service_deliveries_needs_attention` ERROR step naming the unfulfillable units — a billed unit can no longer vanish into a count that reads like success; the per-person live check is now tag-driven, not ITIN-hardcoded; the SD step flips to status `error` when any pipeline failed; and the 23505 catch now distinguishes a genuine concurrent create (→ `existing`) from a SELF-collision within the same request (→ `error` with a plain message), because mapping the latter to "existing" is exactly how a paid unit disappears. (3) **BLOCKER FIXED — `reactivateSD`:** reactivating a cancelled ITIN while the person holds an active one hit the unique index inside `dbWrite`, which THROWS — bypassing the typed result contract so the CRM button died with NO toast (Marcell''s record is exactly this state). Now: an explicit per-person precondition returning a new `outcome:'conflict'` with plain-English copy, plus `dbWriteSafe` on the status flip so a 23505 becomes a returned error instead of an escaping exception. (4) `formation-setup`''s Luca-task guard now uses the exported `OPEN_TASK_STATUSES` — it omitted **"Waiting"**, the normal state for a follow-up, so a re-submit would still have duplicated (the exact bug the guard was added for). `OPEN_TASK_STATUSES` is exported precisely so nobody hand-rolls that list again. (5) The inbox `create-from-email` no longer inserts its own task — `createSD` already creates one, so the routing change would have given Luca TWO tasks per service; and service types with no configured pipeline (verified in prod: Public Notary / Shipping / Support have none) now get a plain-English explanation instead of a leaked internal error. (6) `noteSkippedOffer` REMOVED — the Architect was right that appending to the freetext `notes` field re-created the very pattern that caused this bug; the skip is reported in the result `detail`, and per the corrected rule "they already have one" is the CORRECT outcome, not a lost service. (7) `unlinked_open_tasks` is now rendered in the CRM deactivate toast, not only the MCP output — computing it for a surface nobody watches was the theater half. (8) Migration comments corrected: the stale contact-less-ITIN rationale, and an explicit note that the INDEX is `active`-only (race backstop) while the LIFETIME rule (`completed` also blocks) lives in the app guards — so nobody "fixes" one to match the other. Unit 6493→6547. E2E harness extended to 16 checks incl. the reactivate refusal; all green; lint + build green. NOT on production — Antonio ships._
_2026-07-20b — Claude (**The four duplicate-creation siblings, fixed + a real E2E harness** — same branch, SANDBOX. Antonio: "fix everything and do the E2E QA". The 2026-07-20 entry below fixed the ITIN guard itself; these are the sibling defects the council found around it. **(1) `activate-service.ts` Guard 1** discarded the dedup SELECT's `error` (fail-OPEN → duplicate PAID services on an activation retry) and matched only `notes ILIKE '%offer_token%'` — which cannot see an ITIN bought under a DIFFERENT offer, and Guard 2 never covers ITIN because it filters on `account_id` while ITIN SDs are contact-scoped. Now: fails closed, plus an explicit ITIN-specific per-person check before create. Its catch also treats 23505 as `existing` rather than a failed activation — REQUIRED, since the new unique index would otherwise turn a benign race into an activation error. **(2) `formation-setup.ts`** had the identical fail-open discarded-error dedup on the Company Formation SD — now throws (the job retries) rather than minting a duplicate formation. **(3) The Luca WhatsApp follow-up task** had NO idempotency guard and carried neither `contact_id` nor `delivery_id` — so Marcell's re-submit produced a second identical task, and `deactivateSD` (which cancels by `delivery_id`) could not reach it during cleanup. Now: skips when an open one already exists for that person (fail-closed on an unverifiable check), and stamps both ids. **(4) `app/api/inbox/create-from-email`** raw-inserted the SD with a literal stage `'New'` and `contact_id` NULL, bypassing `createSD` entirely — an ITIN created there was account-scoped (violating the Phase 1 rule), therefore INVISIBLE to both the per-person guard and the unique index. Now routes through `createSD` (real first stage + contact auto-resolved from the account) and returns a plain-English 409-style message on a duplicate; its dialog was the textbook R099 antipattern (`throw new Error('Failed to create service')` + `catch { toast.error(...) }`) and now surfaces the server's actual reason. **(5) `deactivateSD`** now REPORTS `unlinked_open_tasks` — open tasks on the same contact with no `delivery_id`, which it structurally cannot cancel — surfaced in the `service_deactivate` tool output. Deliberately reports rather than auto-cancels: a contact's tasks are not necessarily that service's tasks, and wrongly cancelling real work is worse than a visible loose end. **E2E harness `scripts/e2e-itin-dedup.ts`** (refuses to run outside sandbox, seeds + deletes its own fixtures) walks the real production scenario: legacy-token submit → NEW-token re-submit → mixed-case member email → raw DB duplicate attempt → cancelled-then-new. **Negative control performed:** reverted to the pre-fix code AND dropped the index in sandbox — the harness FAILED 6 checks and reproduced the exact symptom (`found 2` live ITIN services = Marcell's two client cards); restoring both returned it to all-green, so the harness is not vacuously passing. Unit tests 6491→6493 (deactivateSD unlinked-task reporting, both directions). Lint clean, build green. NOT on production — Antonio ships._
_2026-07-20 — Claude (**One live ITIN per PERSON — the duplicate-ITIN guard no longer keys on the offer token** — branch `claude/marcell-itin-status-sync-960aaf`, SANDBOX. **Origin: Antonio — Marcell Bogyora's portal showed TWO conflicting ITIN Service Status cards ("Completing ITIN wizard" AND "Print, sign & mail documents"), and the CRM contact workspace banner opened the empty one.** Root cause: he had two active contact-scoped ITIN SDs. `createItinDeliveriesFromWizard` (`lib/operations/itin-from-wizard.ts`) deduped with `.ilike("notes", '%'+offerToken+'%')`; the submission-token shape gained a per-subject `-{scope8}` suffix in `cc9c0e7b` (`lib/portal/submission-token.ts`, ~2026-07-16), so when he re-submitted the formation wizard on 07-20 the fresh token (`portal-marcell-bogyora-2026-f53451cf`) was no longer a substring of the notes written under the legacy shape (`portal-marcell-bogyora-2026`) → guard missed → duplicate SD + 2 tasks + a duplicate WhatsApp task. **Symptom surfaces:** `getPortalFlows` (`lib/portal/queries.ts`) lists every ACTIVE contact-scoped ITIN → two client cards; `resolveFlowsByContact` (`lib/flows/resolve-flows.ts`) orders `updated_at DESC` → the CRM banner pointed at the newest (empty) one; the Portal-Chats "What's New" note came from the duplicate's `itin_data_collection` workflow task. **Fixes:** (1) the guard now keys on the PERSON — `service_type='ITIN' AND contact_id=… AND (status IS NULL OR status <> 'cancelled')`. NULL-tolerant because `status` is nullable and PostgREST `neq` drops NULL rows; `completed` still BLOCKS (a person gets one ITIN for life, and re-ticking "needs ITIN" on a second company's wizard is routine) while `ITIN Renewal` is a separate `service_type` and is unaffected. (2) The dedup SELECT now captures `error` and **fails CLOSED** — the old `{data}`-only destructure turned any transient PostgREST failure into "no duplicate found". (3) NEW partial unique index `uq_itin_sd_active_per_contact` (migration `20260720-2300-itin-sd-dedup-unique.sql`, sandbox-applied) makes the DB authoritative for the read-then-insert race between the direct-fire and cron job workers — mirrors `uq_formation_sd_active_per_offer`; `createSD` failures matching 23505 are caught and reported as "already exists", any other error re-throws. (4) Contact resolution swapped from case-sensitive `.eq("email")` to `findContactIdByEmail` (case-insensitive, alias-aware, merged-tombstone-safe) — a duplicate CONTACT silently defeats any per-person guard. (5) A legitimate-but-skipped later offer now appends a dated line to the existing SD (`noteSkippedOffer`, idempotent) so staff can reconcile instead of the skip living only in a job-row counter. (6) `app/api/itin-form-completed/route.ts` — its SD `.limit(1)` had NO `.order()`, so with two active SDs the W-7/1040-NR/Schedule OI and the staff review task could be filed against the WRONG one; now `.order('created_at')` oldest-first as defence-in-depth. Unit tests `itin-from-wizard.test.ts` 5→10 (token-shape regression, fail-closed, 23505-as-existing, non-unique re-throw). Full suite 6491 green, lint clean. **Sandbox DB verification:** index blocks a second ACTIVE ITIN (23505), and correctly ALLOWS a `cancelled` ITIN and an `ITIN Renewal` on the same contact; QA rows deleted. **Production data fix already applied (Antonio's approval):** duplicate SD cancelled via `deactivateSD` (2 tasks auto-cancelled), the orphan duplicate WhatsApp task cancelled by hand (no `delivery_id`, so `deactivateSD` does not catch it), and the staff What's-New note marked handled — note the whats-new API filters on `handled_at IS NULL`, NOT task status, so cancelling the task alone would NOT have cleared the card. The second `formation_submissions` row was deliberately KEPT (the client really did re-submit). **Verified false (R093):** a review claim that the duplicate's portal chat topic was client-visible — system `<!-- chat-event: -->` notes are hidden from the client on every client surface; it is staff-only. **Blast radius checked:** Marcell was the ONLY contact prod-wide with more than one active SD of a single service type. Dev job `d6b9ee70`. NOT on production — Antonio ships the code._
_2026-07-17 — Claude (**Flow uploads can file into a Drive SUBFOLDER + auto-rename — EIN-letter fix** — branch `feat/td-comm-design-tools`, SANDBOX. **Origin: Luca — the Formation "EIN Received" upload ("Upload EIN Letter (CP 575)") saved the CP 575 into the account ROOT Drive folder instead of "1. Company", and kept the raw scan filename.** Root cause: `app/api/flows/[id]/upload-document/route.ts` always uploaded to `extractDriveFolderId(account)` (the root) and stamped `documents.file_name = fileName` (client's name), with no subfolder/rename notion. **Fix (catalog-driven, generic):** the `document_upload` stage-layout component (`lib/flows/stage-layout.ts`) gained two OPTIONAL fields — `folder` (target subfolder NAME) and `rename` (a `{token}` filename template) — carried through `parseStageLayout` (whitelist), `stage-renderer.tsx`, and `document-upload.tsx`'s POST body. The route now: (1) fetches `accounts.company_name`/`entity_type` **UNCONDITIONALLY** (outside the `SANDBOX_MODE` guard) and computes an effective filename via `deriveEffectiveFileName` (`lib/flows/flow-drive-folder.ts` — `interpolateStringStrict`, preserves the ORIGINAL extension, falls back to the uploaded name if a token is unresolved or the name is dotless), applied to BOTH the Drive file AND `documents.file_name` on every path (prod-Drive, prod-storage-fallback, sandbox); (2) resolves the named subfolder via `resolveSubfolderId` (trim + case-insensitive match, lists ≤100 children, falls back to the root folder + `console.warn` when absent/listing-fails — never blocks the upload); (3) when a rename is active, writes via `uploadBinaryToDriveUpsert` (overwrite same-named file) so a re-upload REPLACES rather than dropping a duplicate `EIN Official – X.pdf`. Both fields absent ⇒ today's behavior, so Articles / SS-4-fax-confirmation / tax / ITIN uploads are untouched. **Migration** `scripts/migrations/20260717-1500-ein-letter-company-folder-rename.sql` sets `folder="1. Company"`, `rename="EIN Official – {company_name}"` (en-dash U+2013; `company_name` already carries the entity suffix) on the Company Formation "EIN Received" `document_upload` block, located BY TYPE not array index (applied to SANDBOX; `pipeline_stages` protected — prod on Antonio's approval). Unit tests: `flow-drive-folder.test.ts` (+12: rename + subfolder resolution), `stage-layout.test.ts` (+2: whitelist). Full suite 6021 green, build green. **Scope: EIN letter only** — the Articles ("Filed with State") upload has the same root-folder behavior but is CONTACT-scoped pre-materialization (`account_id` NULL → no Drive folder → the folder mechanism no-ops), so it needs a different fix and is a SEPARATE item. **QA caveat:** sandbox mocks Drive (`SANDBOX_MODE=1` → storage fallback), so the rename is verifiable in sandbox (it lands in `documents.file_name` → shows in the workspace Documents list) but the actual "1. Company" placement can only be confirmed on production with one controlled test upload (watch the documented duplicate-`1. Company`-folder / listing-truncation hazard). Council: Project Director GO, Senior Engineer 0 blockers, AI Architect 1 (duplicate-file) + Bug Hunter 2 (sandbox-gated fetch, parser whitelist) — all folded in. NOT on production — Antonio ships._
_2026-07-09 — Claude (**In-workspace SS-4 fax panel** — Company Formation "SS-4 Signed" stage, branch `claude/ecstatic-franklin-c201ec`. New `ss4_fax_panel` stage component (`components/flows/ss4-fax-panel.tsx` + `STAGE_COMPONENT_TYPES` in `lib/flows/stage-layout.ts` + `stage-renderer.tsx`) replaces the passive `fax_irs` link for formation: staff **View** the combined signed-SS-4 + Articles package and **fax it to the IRS** from the workspace, with a pre-filled-but-editable form (domestic EIN number `IRS_EIN_FAX_DOMESTIC=855-641-6935`, Reason + Cover = internal notes), a mandatory confirm step, and a **Re-send for signature** button. State comes from `GET /api/flows/[id]/ss4-fax` (staff-only): `ss4_status`, the combined-package doc (`document_type_name='SS-4 + Articles (IRS Package)'`; `faxable`=Drive-backed, not `storage:`), and prior `fax_sent`. Send routes through the shared `/api/tools/fax/send` engine, which gained an **opt-in double-send guard** (`dedupe_service_delivery_id` + `confirm_resend` → 409 on a prior fax; general fax-tool callers unaffected). The combined package is registered by `app/api/ss4-signed/route.ts` (`autoSaveDocument`, `portal_visible:false`, SD-stamped, idempotent by `drive_file_id` so a re-sign rebuilds the same row). Stage-layout swap `fax_irs`→`ss4_fax_panel` in `scripts/migrations/20260709-1400-ss4-fax-panel-stage-layout.sql`. Panel states: awaiting_signature→waiting/disabled; no faxable package→warn+manual (never SS-4-only); signed→fax; already-faxed→confirm-again. Client-leak pinned by `tests/unit/ss4-fax-package-visibility.test.ts`; full data/logic E2E across 7 scenarios passed on sandbox. Full detail in formation.md (2026-07-09b)._
_2026-07-07b — Claude (**Flow-upload doc-insert failures feed the error-audit system** — SHIPPED (c70cb1a2). CORRECTED DIAGNOSIS (2026-07-07 evening, supersedes this entry's original text): the Martin Csordas "missing documents rows" were NOT insert failures. The Vercel request trace for the 15:55Z re-upload shows the documents insert returned **201 Created** (action_log 201 right after) — and `action_log` records `Document deleted: ITIN Official Martin Csordas.pdf` by `dashboard:support` (Luca) at 15:56:58Z and 12:22:13Z: he removed what looked like a duplicate letter from the CRM after each re-upload, via `/api/crm/admin-actions/delete-document` (properly audited). So no July-7 bug existed; the letter Martin sees is the surviving 07-06 row. The ONLY unexplained event stays the single 2026-07-01 15:20Z upload with no row and no delete audit (pre-retry era). The shipped hardening remains correct and live: insert checked + retried + staff ⚠️ warning, and the double-failure branch calls `reportSystemError` (`lib/system-errors.ts`) so any REAL recurrence is fingerprinted in `system_errors` and auto-diagnosed by the 15-min cron. Investigation lesson (R093): "row absent" ≠ "insert failed" — check the delete audit trail (`action_log action_type='delete'`) before theorizing. NO-BACKFILL decision stands: one visible letter row (07-06) serves the portal; the 07-07 Drive file remains in the company folder._
_2026-07-07 — Claude (**Flow upload: honest doc registration + Drive for contact-scoped SDs** — branch `claude/practical-einstein-ce1e5c`, SANDBOX. Luca round 2 (Martin Csordas): his re-uploaded ITIN letter produced NO documents row — `app/api/flows/[id]/upload-document` DISCARDED the insert error (intermittent; a 2026-07-01 upload has the same missing row — pre-existing, silent). Now: insert error is checked, retried once, logged, and surfaced as a ⚠️ warning in the response `detail` (file stays uploaded; staff told to re-upload). ALSO: contact-scoped SDs (ITIN, in-flight formation) always took the Supabase-Storage fallback even in production, so the letter never reached Google Drive. New `lib/flows/flow-drive-folder.ts` (`extractDriveFolderId` pure + `resolveContactLinkedDriveFolder`): when the SD has no account, the upload now files into the contact's primary linked account's Drive folder when one exists (doc row STAYS contact-scoped — only the binary's home borrows the company folder); Storage fallback only when the contact truly has no linked folder. Sandbox behavior unchanged (SANDBOX_MODE short-circuits to Storage). Unit tests `flow-drive-folder.test.ts` (8). Portal-side visibility + download fixes in portal.md (same date). NOT on production — Antonio ships._
_2026-07-06 — Claude (**ITIN Approved upload now FINALIZES the flow** — branch `claude/practical-einstein-ce1e5c`, SANDBOX. Luca (Slack td-dev, Martin Csordas): uploading the IRS approval letter (CP565) at the terminal "ITIN Approved" stage just filed the doc — no ITIN on the contact, no client message, SD stayed `active` forever (workspace banner + blue last chip: the completion rule in `lib/service-delivery.ts` only keys off stage NAMES "Completed"/"TR Filed"/renewal-"Closed"). The smart path always existed on the CRM contact-page upload (`app/api/crm/admin-actions/upload-document`) but not on the workspace door. Fix: `app/api/flows/[id]/upload-document` detects `service_type='ITIN' && flow_stage='ITIN Approved'`, inserts the doc `portal_visible: true` (the letter IS the client deliverable), skips the pointless auto-advance, and calls NEW `lib/itin/finalize-approval.ts::finalizeItinApproval`: OCR the raw bytes (`ocrRawContent` — no Drive round-trip, sandbox-safe) → shared `extractItinFromOcr` + `parseItinIssueDateFromOcr` (`lib/ocr-helpers.ts`; the extractor is now shared with both CRM OCR sites) → `writeITINFields` (stamps itin_number/issue/renewal) → NEW `completeSDInPlace` (`lib/operations/service-delivery.ts`, TOCTOU-guarded status flip WITHOUT stage change — re-advancing into the same stage would re-fire the generic stage notification) → winner-only client notify via `notifyClientActionRequired` (flow-threaded chat + bell/push + immediate bilingual email, deep link `/portal/documents`, new optional `ctaLabel` param → "View document"). OCR/extraction failures never fail the upload — warnings surface in the upload response `detail`, which `document-upload.tsx` now renders (was a hardcoded "uploaded" string). Completion drops the flow from portal Service Status (active-only query — consistent with every completed flow) and hides the CRM contact banner. Unit tests: `complete-sd-in-place.test.ts`, `ocr-helpers.test.ts`. NOT on production — Antonio ships._
_2026-07-02 — Claude (**SS-4 regenerate everywhere + member-change auto-refresh** — branch `claude/practical-chandrasekhar-6453a6`, SANDBOX. Root cause of the AI Venture Labs wrong-signer case: the SS-4 is a frozen snapshot and THREE surfaces looked like "regenerate" without regenerating — the workspace ss4_panel returned `already_existed` as a plain success, the account Documents-panel "Regenerate" button only opened the dialog, and the dialog hid the real refresh behind a failed Generate (409 → amber button). New SINGLE shared core `lib/operations/ss4-refresh.ts::refreshSS4` (pure diff via `computeSs4RefreshUpdates`, TOCTOU-guarded update, keeps token/access_code so client links survive, audited via action_log; MMLLC with ≠1 flagged signer NEVER touches the row and returns the `ss4SignerAlertMessage` staff alert; missing RA county keeps the stored Line 6; signer-change on an `awaiting_signature` record re-notifies the NEW signer via `notifySs4ReadyToSign`, drafts stay silent). Wired: (1) `POST /api/flows/[id]/generate-ss4` accepts `{regenerate:true}` and its plain-POST `already_existed` response now carries an honest "NOT refreshed" note; `ss4-panel.tsx` shows "Regenerate from account data" on draft AND awaiting_signature + result feedback. (2) CRM `generate-document` regenerate branch delegates to refreshSS4 (inline copy deleted); `generate-ss4-dialog.tsx` offers Regenerate UP FRONT via new `existingUnsignedStatus` prop (wired from `account-detail.tsx` ss4Applications). (3) AUTO-REFRESH (Antonio decision 2026-07-02) fires after every member write: client member-info form route, CRM `accounts/[id]/members` POST/PATCH/DELETE (returns `ss4_refresh` to the UI), and the workspace members-panel signer toggle (`/api/flows/[id]/members` POST). Unit tests `tests/unit/ss4-refresh.test.ts` (17). NOT on production — Antonio ships._
_2026-07-02b — Claude (**ITIN Client-Signing chat message moved to the shared action-required rail** — Phase C. The bespoke 8c block in `lib/service-delivery.ts` (Document Preparation → Client Signing portal message + R103 email) is RETIRED; the `'ITIN::Client Signing'` entry in `lib/portal/action-stage-registry.ts` now dispatches the same copy via `notifyClientActionRequired` (SD-stamped chat so it still threads into the workspace flow chat, plus bell/push and an immediate email; deep link `/portal/flows/{sd_id}`). Fires on ANY advance INTO Client Signing (was: only from Document Preparation) — dedup guards doubles. Same change wires `Tax Return::Wizard Available`; the generic advance notification/push/stage-email are suppressed for registered stages (one message, never doubles). Engine + E2E in portal.md (2026-07-02d). SANDBOX — Antonio ships._
_2026-07-02 — Claude (**SS-4 send/re-send buttons now notify the client** — branch `claude/focused-stonebraker-a5f331`, SANDBOX. `POST /api/flows/[id]/send-ss4` and `/resend-ss4` (the ss4_panel "Send to Client for Signature" / "Re-send for Signature" buttons) previously flipped `ss4_applications.status` → `awaiting_signature` and told the client NOTHING (root of the Michele Cotti silent-wait). Both now call `notifySs4ReadyToSign` (`lib/portal/action-required.ts`) after the status flip: clickable portal chat message (threads into the workspace flow chat via `service_delivery_id`) + immediate bilingual email + bell/push, deep link `/portal/sign/ss4`, targeted at the signer. Responses include `client_notified` per-channel results. Same hook fires from `createSS4(ready_to_sign)` and the MCP `ss4_update` promotion. Full engine in portal.md; formation specifics + sign-page draft-leak fix + tracker glow gate in formation.md (same-date entries)._
_2026-07-01 — Claude (**ITIN is MAIL-ONLY: removed the wrong `fax_irs` step**, branch `claude/elegant-moore-1e080b`, SANDBOX). Luca (Slack td-dev): the ITIN "Submitted to IRS" stage showed a `fax_irs` card reading "Fax the signed W-7 package to the IRS" plus a staff stage description "Mail or fax the ITIN package to IRS." W-7 / ITIN packages are submitted **by mail only** (the correct IRS ITIN Operation PO Box is already in the stage's `waiting_notice`). `fax_irs` (`components/flows/fax-irs.tsx`) is shared with SS-4 / Tax Return — which legitimately fax — and was wrongly copied onto the ITIN flow. Migration `20260701-1700-itin-remove-fax-irs-mail-only.sql` (surgical jsonb: filters the `fax_irs` element out of the ITIN "Submitted to IRS" `stage_layout.components` and rewrites the staff `description` to "Mail the ITIN (W-7) package to the IRS. Upload the mailing/tracking receipt."). Sandbox-applied + verified (fax gone, components 6→5). The **client-facing** copy (`client_description` "Your ITIN application has been mailed to the IRS.") was already correct and is untouched. `fax-irs.tsx`'s `ITIN` switch branch is now unused dead code (harmless — left in place; the component still serves SS-4 / Tax Return). NOT on production — Antonio ships._
_2026-06-29 — Claude (branch `fix/ss4-signature-required-and-resend`): `components/flows/ss4-panel.tsx` signed state now renders a **"Re-send for Signature"** button (was a dead-end confirmation), POSTing the new `app/api/flows/[id]/resend-ss4` route which re-opens a `signed`/`submitted` SS-4 to `awaiting_signature` and clears the captured signature (token/access_code preserved). Use when a client "signed" with no real signature (single-dot bug). Full rationale + the signature-required guard on the signing page are in `docs/systems/formation.md` (same-date entry). NOT on production yet._
_2026-06-25 — Claude (branch `fix/formation-date-from-articles`): `components/flows/document-upload.tsx` now has a special case for the Company-Formation Articles upload (stage **"Filed with State"**). That upload AUTO-ADVANCES into "Articles Received" and materializes the company, pinning the SS-4 formation date — so the component stages the file, OCR-prefills the filing date (`GET /api/flows/[id]/articles-formation-date?storage_path=`), and REQUIRES staff to confirm the formation date in a modal before committing the upload. The confirmed date rides the existing `POST /api/flows/[id]/upload-document` body as `formation_date` → `advanceServiceDelivery` → `materializeFormationCompany`. All other upload stages are unchanged (no modal, immediate upload). Full rationale in `docs/systems/formation.md` (same-date entry)._

_Earlier 2026-06-26 — Claude (**MMLLC formation workspace: new `members_panel` stage component**, branch `feat/mmllc-workspace`, SANDBOX. New `components/flows/members-panel.tsx` (registered in `lib/flows/stage-layout.ts` STAGE_COMPONENT_TYPES + `stage-renderer.tsx`) — a staff table of a Company Formation flow's members (name, individual/company badge, ownership %, SS-4 signer badge). **Self-hides for SMLLC.** Backed by `GET/POST /api/flows/[id]/members`: GET reads the `members` table for a materialized (account-scoped) SD, or reconstructs owner+members from the formation `wizard_progress.data` flat keys for an in-flight (contact-scoped) SD (owner % derived as 100 − Σ members; `member_id` null → read-only, toggle activates once the company exists). POST `{member_id, is_signer:true}` is a staff override that flags one member as the SS-4 Responsible Party and clears all others on the account first (single-signer invariant the `decideSs4Signer` guard needs). Added to the Company Formation **Wizard Submitted / Articles Received / SS-4 Prepared** stage_layouts via migration `20260626-1500-mmllc-members-panel.sql` (idempotent append, sandbox-applied). Browser-verified live: MMLLC SS-4-Prepared table + signer toggle + DB persistence, SMLLC self-hide, contact-scoped read-only render. The signer itself is now collected in the formation wizard (see portal.md) and written to `members.is_signer` at materialization (see formation-materialize). NOT on production — Antonio ships.)_
_Latest 2026-06-26 — Claude (**Workspace document "View" links now open — they were dead for every ITIN doc.** Luca (Slack td-dev): at the ITIN Document Preparation stage the 3 generated forms (W-7/1040-NR/Schedule OI) "are not clickable, can't review them." Root cause (verified vs prod): `autoSaveDocument` (`lib/portal/auto-save-document.ts`) writes `drive_file_id` but NEVER `drive_link`, and the workspace `document-viewer` only renders a clickable "View" when `drive_link` is set — so EVERY ITIN doc (Daniel Pasztor, Damiano Mocellin — all had `drive_link` NULL) showed a non-clickable "No link". Fix (in `app/api/flows/[id]/documents/route.ts`, not a backfill — repairs existing + future): when the stored `drive_link` is empty but a `drive_file_id` exists, the API now returns `drive_link = /api/documents/[id]/preview` — the existing service-account-backed streamer (already used by the contact-detail preview + fax history) that serves BOTH Drive ids and `storage:<path>` ids with no Google login. Working signed-storage URLs are preserved (fallback only fires when `drive_link` is null). Verified END-TO-END in the sandbox browser: seeded a real PDF (storage-backed) + a Drive-backed doc on an ITIN SD with `drive_link` NULL → the workspace showed clickable "View" (0 "No link"), opening the link returned 200 `application/pdf` and the storage PDF RENDERED inline (page 1/1). KNOWN RELATED (flagged, NOT fixed here — same null-`drive_link` root cause): the CRM account Documents tab (`account-documents-list.tsx`) also keys off `drive_link`, so these `autoSaveDocument`-created docs show "No link" there too. SANDBOX-tested; awaiting ship approval.)_
_Earlier 2026-06-26 — Claude (**ITIN review note is back in What's New (workspace-pointer), correction to the plain-task approach.** SHIPPED. The 2026-06-25 "plain task" lost the Portal Chats **What's New** notification Luca relies on (that feed needs the dispatcher's `workflow_spawned` chat-event; a plain task emits none). `itin_review` is now a `workspace_pointer` catalog workflow (empty actions, `sd_progress_v1` meta) dispatched by `itin-form-completed` — so the ITIN note shows in What's New with an **"Open in Workspace"** link (`/flows/[sd]`) and NO action buttons. The Document Preparation → Client Signing advance still posts the client the portal message (no email) and §6b now keeps the ITIN pointer card's stage fresh too. See workflow-engine.md for the full mechanics. Verified live in the sandbox browser.)_
_Earlier 2026-06-25 — Claude (**ITIN is now WORKSPACE-ONLY** — branch `fix/itin-workspace-only`, SANDBOX. The competing workflow-chain control surface was removed: the `itin_review` task is no longer a workflow card with Approve/Needs-Fix/Waiting/Recall buttons, and the "Approve & Send" handler that EMAILED the client the W-7/1040-NR/Schedule OI PDFs is gone. The workspace (`/flows/[sd_id]`) is the only way to manage an ITIN. **(1)** `itin-form-completed/route.ts` Step 6 now creates a PLAIN "Review ITIN documents — …" task whose description links to the workspace ("Open the workspace to review and advance") — no `workflow_slug`/`workflow_snapshot`, so the What's-New card has no action buttons; the internal support@ team email (Step 5) is unchanged. **(2)** NEW client-communication hook in `lib/service-delivery.ts` §8c: advancing an ITIN SD **Document Preparation → Client Signing** (the workspace's "Documents Reviewed — Send to Client" advance button, action `advance_next`) posts the client a bilingual (EN/IT, per `contacts.language`) portal chat message — "Your ITIN documents are ready in your portal… print, wet-ink sign, two color passport copies, mail to our office" — stamped `service_delivery_id` (so it threads into both the workspace flow chat AND the client portal chat), `sender_type='admin'`, sender = the system admin id, gated on `!skip_notify`. It does NOT email the documents (the old email handler's job); the portal is the delivery mechanism. The standard R103 "new portal message" notification still fires (throttled 1/2h) — same pattern as the formation EIN flow (`save-ein`). Verified live on sandbox SD `746faa3a` (Stay Legit LLC): advance returned `auto_triggers` containing "ITIN client portal message posted (en)", the `portal_messages` row was created with the English copy, and the only blocked email was the throttle digest (never the PDFs). The handler removal lives in workflow-engine.md; the five downstream ITIN catalog rows are intentionally left active for a separate cleanup; the `itin_review` catalog row was deactivated (status→draft) in sandbox. NOT on production — Antonio ships.)_
_Earlier 2026-06-25 — Claude (ITIN workspace parity fixes, branch `fix/itin-workspace`, SANDBOX. **(1) Advance button on "Document Preparation".** The ITIN `pipeline_stages` "Document Preparation" (stage_order 2) `stage_layout` was `document_viewer + info_panel + chat` with NO `action_buttons`, so staff could not advance to "Client Signing" after reviewing the auto-generated W-7/1040-NR/Schedule OI. Migration `20260625-1444-itin-document-preparation-advance-button.sql` appends an `advance_next` action ("Documents Reviewed — Send to Client" → Client Signing); idempotent (`@>` guard), applied to sandbox + verified. **(2) Document registration now reports honestly.** `app/api/itin-form-completed/route.ts` Step 4 IGNORED `autoSaveDocument`'s return (it returns `{error}`, never throws), so the action_log said `docs_generated: ok` even when the `documents` insert failed — the silent root cause of "ITIN docs in Drive but not in the table" (Daniel Pasztor). Now: each PDF whose Drive upload returned no id is skipped + reported (a null `drive_file_id` violates the documents NOT NULL constraint — verified empirically); each `autoSaveDocument` return is checked and real errors surface in a new `docs_registered` result (ok / error with the actual message). NOTE: the insert itself SUCCEEDS in sandbox with `contact_id` + `drive_file_id` (`confidence: 1.0` stores as `"1"`, not the bug); Daniel's exact prod failure couldn't be reproduced (prod-only records, no prod MCP this session) — the fix makes the true error visible on the next run. **(3) ITIN workspace banner on the CRM contact page.** New `components/flows/itin-workspace-banner.tsx` (teal, `UserCheck`), mirroring `FormationWorkspaceBanner` — rendered at the top of `app/(dashboard)/contacts/[id]/page.tsx` for any active contact-scoped ITIN SD (same prominent visibility as formation; ITIN already had the Services-tab FlowChip). Verified end-to-end against a sandbox "Daniel Pasztor QA" fixture (formation + contact-scoped ITIN at Document Preparation, docs registered). Portal-side (`app/portal/page.tsx` ITIN-alongside-FormationDashboard) + the `/portal/wizard` cookie-trust fix are in portal.md. Sandbox only; not on production.)_
_Earlier 2026-06-23 — Claude (PRODUCTION HOTFIX — workspace flow chat now reaches the client on contact-scoped SDs + filing-date display. **(1) Lost-message bug.** `POST /api/flows/[id]/chat` resolved `contact_id` ONLY when the SD had an account (`if (sd.account_id)` → `account_contacts` lookup), so contact-scoped SDs (every in-progress Company Formation: `account_id` NULL, `contact_id` set) stamped `portal_messages.contact_id = null` — the message never reached the client's contact-scoped portal thread (silently lost). The POST SD select was also missing `contact_id`. Fix: added `contact_id` to the select and a fallback `if (!contactId) contactId = sd.contact_id ?? null` after the account_contacts block. Verified E2E on deployed sandbox (POST to a contact-scoped SD → row stamped with the contact). This explains Federici Alessandro's "message didn't arrive" report. **(2) Filing date.** `formation-names.tsx` now shows the filed date (from the candidate's `name_checks` `updated_at`) next to the "📄 Filed with SOS" badge — e.g. "· Jun 18, 2026" — mirroring the existing `sent_to_client` "Sent {date}" pattern. Cherry-picked to production main (commit on `fix/decision-note-visibility`); the rest of that branch stays sandbox-only.)_
_Earlier 2026-06-22 — Claude (Client rejection NOTE now visible in the formation Name Command Center, branch `fix/decision-note-visibility`. When a client rejects a proposed name with a free-text reason, `applyNameDecisionResponse` (`lib/operations/formation-name-checks.ts`) stores it on the `NameCheck` (new optional `note` field, `lib/flows/name-checks.ts`); `formation-names.tsx` renders it under the "Rejected by client" badge as a full-width "Client note: …" line. It also posts an SD-scoped `portal_messages` flow-chat note (`Client rejected "<name>" — "<note>"`, system sender) so the reason shows in the workspace chat too. The action_log + What's New summary carry the note via `summarizeResponse` — see client-decision-requests.md. **Also (Luca, dup-window bug):** `handleNameAction` now cancels any still-pending `text_input` "New LLC Names Needed" request for the SD (`cancelPendingNewNamesRequests`) BEFORE creating a new one in both `request_new_names` and `mark_sos_rejected` — so the client never sees two name-request windows at once (an unanswered "all unavailable" request + a later SOS-rejection request). Only the latest stays pending; the prior flips to `cancelled`. `respondToDecisionRequest` already marks the answered request `responded` (that part was never broken). **Plus (Luca's feedback):** the rejection can carry a `suggested_name` — `applyNameDecisionResponse` adds it to `name_checks` as a new PENDING candidate with `source:'client_suggestion'` (deduped case-insensitively), so it appears immediately in the panel; `formation-names.tsx` tags it "(client suggested)" in blue, and the flow-chat note appends `They suggested "<name>" instead`.)_
_Earlier 2026-06-21 — Claude (Formation name-rejection recovery, branch `fix/formation-name-rejection`. **(1) SOS rejection reverts the SD to "Wizard Submitted".** `handleNameAction` `mark_sos_rejected` (`lib/operations/formation-name-checks.ts`) already set the name to `rejected_by_sos` + created a `text_input` new-names decision request, but left the SD on "Filed with State" where the name panel is read-only — so the client's resubmitted names had no action buttons (dead end). It now also calls `advanceServiceDelivery({target_stage:'Wizard Submitted', skip_tasks:true, skip_notify:true})` (best-effort) so the panel becomes actionable again AND the formation_progress task's `sd_stage` re-syncs (via §6b) — verified E2E. **(2) "Request New Names from Client" action.** New `request_new_names` NameAction (handler + `app/api/flows/[id]/name-check` route, index-optional) creates the same `text_input` new-names request but is NOT tied to a candidate — for when all names are unavailable up front (the SOS-reject path required a `filed` name first). `formation-names.tsx` shows the amber button at "Wizard Submitted" when every candidate is dead (`not_available`/`rejected_by_client`/`rejected_by_sos`, none viable); on click it sends the request and shows a "waiting for the client" confirmation. Closes the two QA gaps from the 2026-06-21 full-formation QA pass.)_
_Earlier 2026-06-20 — Claude (Formation-workspace QA fixes, branch `fix/formation-workspace-bugs`. **(1) Overview shows EIN + addresses.** `app/(dashboard)/flows/[id]/page.tsx` now loads `ein_number`, `registered_agent_address`, `physical_address`, and the mailing-address FK join (`business_mailing_address_id`) for account-scoped SDs; `info-panel.tsx` renders State of formation / EIN / Registered Agent / Mailing rows when present (new `WorkspaceAccount` fields in `types.ts`). **(2) Document viewer auto-refreshes.** `DocumentViewer` fetches in a client `useEffect` so `router.refresh()` after an upload didn't reload it; `DocumentUpload` now dispatches a `FLOW_DOC_UPLOADED_EVENT` window event (scoped by SD id) that the viewer listens for and re-fetches. **(3) SOS link collapses after filing.** `external-link.tsx` takes a `filedName` (threaded via `WorkspaceServiceDelivery.formation_filed_name`, derived from the `name_checks` filed entry in `page.tsx`); for Company Formation, once a name is filed it shows a green "✓ <Name> filed with the Secretary of State" instead of the go-file button. **(4) SS-4 preview link** enlarged from a tiny text link to a bordered button (`ss4-panel.tsx`). **(5) Portal Documents reach the client.** `flow-doc-visibility.ts` adds a `Company Formation` allowlist (Articles / EIN letter — NOT the internal signed SS-4) and `formation-materialize.ts` step 10a backfills `account_id` on SD-stamped docs so the pre-materialization Articles upload surfaces account-side. **(6) Portal "Complete Formation Details" CTA** hides once the formation SD is past "Payment Confirmed" (new `sdStage` prop on `formation-dashboard.tsx`) — fixes the card persisting when the wizard row is contact-keyed (`account_id` NULL). See formation.md for the banking-SD removal, signed-SS-4-internal, EIN-message, and `/portal/banks` changes. NOT yet deployed.)_

_Also 2026-06-20 (documents-not-showing, ROUND 2 — the real root cause) — Claude (The client `cache:'no-store'` fix below was necessary but INSUFFICIENT — docs still didn't show on the deployed sandbox while working in local dev. Real root cause: **Next.js's PRODUCTION fetch Data Cache caching the server-side `supabaseAdmin` PostgREST GET.** supabase-js calls PostgREST via the global `fetch`, which Next.js patches and caches in production (Vercel) — but NOT in `next dev` (which is why every local-dev verification passed while the deployed app served a stale/empty document list). `export const dynamic='force-dynamic'` on the route did NOT reliably prevent it. Fixes, defense-in-depth across all three cache layers: (a) **`lib/supabase-admin.ts`** now creates the admin client with a custom `global.fetch` that forces `cache:'no-store'` on every PostgREST/Storage request — a service-role admin client must never read stale data (fixes ALL server-side reads, not just documents); (b) the **documents route** sets an explicit `Cache-Control: no-store` response header (browser/CDN layer); (c) the client GET fetches already use `cache:'no-store'` (below). **Verified on a LOCAL PRODUCTION BUILD** (`next build && next start`, which DOES use the Data Cache, unlike `next dev`): deleting a doc → viewer shows 0; inserting a doc → viewer shows 1, both after reload, no staleness. Lesson: workspace doc/state bugs that "work locally but fail on sandbox" are almost always the production Data Cache — verify with `next start`, not `next dev`.)_

_Also 2026-06-20 (2nd QA round, dev_task 7cebac13) — Claude (Three fixes. **(1) DOCUMENTS NOT SHOWING — root cause: stale browser fetch cache.** Every workspace client-component GET (`document-viewer`, `ss4-panel` generate-ss4, `formation-names` name-check, `ein-entry` save-ein, `signature-status`, `flow-chat`, `data-viewer`, `signature-send`) fetched WITHOUT `cache: 'no-store'`, so the browser served a stale cached response — newly-uploaded Articles / signed SS-4 / EIN letter never appeared, and old/wrong-company docs lingered (the "CORAGEM doc on Bright Future" cross-contamination). The route's `dynamic='force-dynamic'` is a server hint and does NOT stop the browser HTTP cache. Added `cache: 'no-store'` to all 8 GET fetches. Verified live: viewer reflects the true DB state and shows an uploaded doc immediately after the auto-advance. (This is also why prior "it works" checks passed — they fetched the API with `cache:'no-store'` directly while the real component did not.) **(2) SS-4 signing now advances the SD.** `ss4-signed` updated `stage_history` but never advanced "SS-4 Prepared" → "SS-4 Signed" (staff had to do it manually). Added an idempotent `advanceServiceDelivery` (guarded to `stage==='SS-4 Prepared'`). **(3) EIN completion message** updated to the final Antonio-approved copy (adds "Go to Bank Applications in your portal sidebar", "for Wise go to wise.com", and a paragraph #5 about consulting in chat before applying) in `save-ein` (EN + IT).)_

_Also 2026-06-20 (browser QA) — Claude (Two more bugs found while walking the formation workspace stage-by-stage in the browser, both fixed. **(A) Stage stepper rendered icon NAMES as text.** `pipeline_stages.icon` holds EITHER an emoji (older flows) OR a Lucide component name (Company Formation v2: "CreditCard", "Landmark", …); `stage-stepper.tsx` rendered `{s.icon}` as raw text, so the formation stepper read "CreditCardPayment Confirmed" etc. Added a `STAGE_ICONS` name→component map (the 7 formation icons), rendering the component when mapped and falling back to the emoji/text branch otherwise. **(B) Name-proposal chat message never posted.** `sendNameProposalChatMessage` (the 2026-06-19 send_to_client heads-up) inserted `portal_messages.sender_id: null`, but that column is NOT NULL — the insert threw and the best-effort catch silently dropped it. The name-check route now threads `auth.user.id` as `actorId` → used as `sender_id`; skips (rather than nulls) when absent. Verified live: message now posts + reaches the client's portal chat.)_

_Earlier 2026-06-19 — Claude (Two formation-workspace fixes. (1) **Client name on contact-scoped flows.** In-flight Company Formation SDs (and ITIN) are contact-scoped (`account_id` NULL, no `company_name`), so the workspace was unidentifiable. The page (`app/(dashboard)/flows/[id]/page.tsx`) loads the contact's `full_name` for ANY contact-linked SD (`sd.contact_id` set — widened 2026-06-20 from the original account-less-only condition) and threads it via a new `WorkspaceServiceDelivery.contact_name` field (`components/flows/types.ts`). The Overview `info_panel` (`components/flows/info-panel.tsx`) renders a **separate "Contact" row** below "Company" when `sd.contact_name` is set — COMPANY deliberately stays "—" for contact-scoped SDs (it is NOT the company name) and shows the company for account-scoped SDs (which now also show the Contact row when they carry a `contact_id`); the header subtitle is unchanged (still "—" when there's no account). Generic to any contact-scoped SD, so ITIN benefits too. (2) **Chat message on name proposal.** `send_to_client` in `lib/operations/formation-name-checks.ts` created the approval decision request but posted no chat message. It now also posts a best-effort bilingual (EN/IT by `contacts.language`) flow chat message to `portal_messages` ("We checked your LLC names and [Name] is available in [State]. We've sent you a request to confirm — please check your portal.") stamped `service_delivery_id` + `topic` (`buildFlowTopic`/`deriveFlowYear`) + `contact_id`, and fires `notifyClientOfAdminMessage` — mirroring the EIN-save flow-chat pattern (`app/api/flows/[id]/save-ein`). `loadSd` was extended to select `service_type, due_date, stage_entered_at, created_at` for the topic/year. Best-effort: a chat failure does NOT fail the staff action (the decision request is already created). No idempotency guard added on `send_to_client` (pre-existing — double-click → 2 requests + 2 chat rows; email throttled 1/2h per topic).)_

_Earlier 2026-06-18 — Claude (Formation **Name Command Center is now stage-aware**, and the "Filed with State" layout drops a duplicate upload. (1) `formation-names.tsx` reads its `stage` prop: on "Wizard Submitted" it shows the full selection workflow (Check on SOS / Mark Available / Mark Not Available / Send to Client / File / Mark as Filed); on "Filed with State" it's read-mostly — the filed name keeps ONE corrective action and the other candidates render dimmed (`opacity-50`) with no buttons, because selection already happened on the prior stage (`selectionStage = stage === 'Wizard Submitted'`). (2) The danger button was relabeled `"SOS Rejected"` → `"Mark SOS Rejected"` — it's an action that reports a Secretary-of-State rejection, not a status (it was being misread as "this name was rejected"; the badge was always the correct blue "📄 Filed with SOS"). (3) The "Filed with State" `stage_layout` had TWO uploads — "Upload filing receipt" AND "Upload Articles of Organization"; the Articles upload was removed (it already lives on the next stage, "Articles Received"). Updated in migration `20260617-formation-workspace-v2.sql` + applied to sandbox `pipeline_stages` (now 6 components). KNOWN GAP (flagged, not changed — out of scope): the "Name Rejected — Choose New Name" action loops back to "Filed with State", but re-selection has no buttons there now; the clean fix is to loop that action to "Wizard Submitted".)_

_Earlier 2026-06-18 — Claude (Flow document upload now works for CONTACT-scoped SDs (in-flight Company Formation, `account_id` NULL). `POST /api/flows/[id]/upload-document` guarded `if (sdErr || !sd || !sd.account_id)` and returned "Flow (service delivery) not found" (404) for any SD without an account — so Antonio's "Filed with State" filing-receipt upload on Marinela's formation SD failed. Fix: select `contact_id` too, drop the `!sd.account_id` clause (`if (sdErr || !sd)`), treat `accountId`/`contactId` as nullable, and extend the existing storage fallback (`useStorageFallback = SANDBOX_MODE || !accountId`) so a no-account SD keeps the file in `onboarding-uploads` (no Drive folder to target) instead of erroring; the `documents` row now stamps `contact_id` and `action_log` passes `account_id ?? undefined`. `documents.account_id`/`contact_id` are both nullable (verified). Auto-advance behavior unchanged — the DocumentUpload component still forwards `auto_advance:false` on "Filed with State" so the upload doesn't advance the stage.)_

_Earlier 2026-06-18 — Claude (ROOT CAUSE of "File on SOS doesn't work": the NM URL was DEAD. `portal.sos.state.nm.us/BFS/online/` returns connection-refused (verified by curl; WY/FL/DE all resolve). The `<a>` + href were correct all along — the destination was broken, and Marinela's link defaults to NM (no state on her record). Fixed `FORMATION_SOS_URL.NM` in `lib/flows/state-links.ts` → `https://enterprise.sos.nm.gov/` (NM's current "SOS Enterprise" portal, verified 200). Single source of truth — fixes both the `formation_names` buttons and the `external_link` resolver. NOTE: `components/contacts/llc-name-selection-card.tsx` has the SAME dead NM URL (separate older component) — flagged, not changed here.)_

_Earlier 2026-06-18 — Claude (`formation_names` SOS links render UNCONDITIONALLY now (were gated on `sos.url &&`) and show the resolved state code — "Check on SOS (NM)" / "File on SOS (NM) →". They were already proper `<a target="_blank">` links to `resolveFormationFilingLink(state).url`, which always resolves (defaults to NM when the SD has no state — Marinela's wizard lost `state_of_formation`). Dropping the guard removes any chance the link doesn't appear and makes the target state explicit.)_

_Earlier 2026-06-18 — Claude (`formation_names` now distinguishes a SESSION LAPSE from genuinely-empty. Unauthenticated `/api/flows/[id]/name-check` 307-redirects to /login (middleware), so a fetch on an expired session followed the redirect and surfaced a confusing "Could not load" — which read as "names disappeared" even though `name_checks` is safe in the DB. The component now detects `res.redirected` / `/login` / 401 / 403 and shows an amber "Your session expired — the names are safe, reload" with a Retry, separate from the real error and the "No names submitted" empty state. Verified: view-as does NOT touch service_deliveries; Marinela's 3 names were intact + "Marinela Marku" accepted throughout. Middleware untouched (protected).)_

_Earlier 2026-06-18 — Claude (Name-proposal copy + UX polish. "Send to Client" now creates the approval request with the LLC name as the TITLE (hero; "LLC" appended if absent) and a fuller message ("…available in [State]… final approval is up to the Secretary of State…"), EN/IT, with buttons "Yes, proceed" / "No, use a different name" (localized via new `approve_label_it`/`reject_label_it` options the portal decision-card now reads). The portal `decision-card` renders the title big/bold as the hero with the message below. The staff `formation_names` row shows "Sent [date]" next to the "Waiting for client" badge. See client-decision-requests.md.)_

_Earlier 2026-06-18 — Claude (Formation **Name Command Center** replaces the staff `decision_requests` panel. NEW stage component `formation_names` (`components/flows/formation-names.tsx`, registered in `lib/flows/stage-layout.ts` + `stage-renderer.tsx`; the old `decision_requests` type + `DecisionRequestPanel` are REMOVED — decision requests are now backend plumbing, never a staff-visible component). One row per candidate LLC name with a status badge + status-driven actions (Check on SOS / Mark Available / Mark Not Available → Send to Client → File on SOS / Mark as Filed → SOS Rejected). Per-name status lives in a new `service_deliveries.name_checks` JSONB (migration `20260618-name-checks.sql`, sandbox; pure helpers + status lifecycle in `lib/flows/name-checks.ts`, server ops in `lib/operations/formation-name-checks.ts`). Driven by `GET/POST /api/flows/[id]/name-check` (staff-only; GET initializes from the formation wizard when null). "Send to Client" creates an `approval` decision request behind the scenes (options carry a `name_check` marker); "SOS Rejected" creates a `text_input` request for new names. When the client answers, `respondToDecisionRequest` calls `applyNameDecisionResponse` (gated by the marker, so decisions stay type-agnostic) to flip the name to accepted/rejected_by_client or append client-proposed names. The gated advance "Name Confirmed & Filed — Waiting for Articles" lives in `formation_names` and unlocks once a name is `filed`. Wizard Submitted layout is now `formation_names + info_panel + data_viewer + chat` (data_viewer no longer renders the names — formation_names owns them); Filed with State swaps `decision_requests`→`formation_names`. Client side unchanged: the portal decision-card on `/portal/flows/[id]` renders the approval/text_input request inline.)_

_Earlier 2026-06-18 — Claude (NEW stage component `decision_requests` — the staff side of Client Decision Requests (full system: client-decision-requests.md). `components/flows/decision-request.tsx`, registered in `lib/flows/stage-layout.ts` STAGE_COMPONENT_TYPES + `stage-renderer.tsx` (gets serviceDeliveryId + service_type + stage). Lists pending/responded requests for the SD and creates new ones (approval/choice/text_input) via `POST /api/portal/decisions/create`; on formation "Wizard Submitted" it shows a "Propose Name to Client" quick-fill (seeds an approval). Added to the Company Formation "Wizard Submitted" + "Filed with State" stage_layouts (migration 20260617-formation-workspace-v2.sql + sandbox). The client answers via `components/portal/decision-card.tsx` on `/portal/flows/[id]`.)_

_Earlier 2026-06-18 — Claude (Formation "Wizard Submitted" data_viewer FIX — show the THREE name candidates, never a "chosen" name. At this stage nothing is chosen yet (the client picks one here, via the SoS check + chat), but the old card rendered a "Chosen" badge driven by the wizard's `chosen_name` field — for a submission with only `llc_name_1` (Marinela Marku) that looked like a single "Chosen Name" field. Replaced `extractFormationNames` with the pure `formationNameChoices` (`lib/flows/formation-names.ts`): returns 3 labeled slots "Name Choice 1/2/3" from `llc_name_1/2/3` (empty slots show "Not provided" so the 3-choice structure stays explicit), or one "Proposed Name" for legacy single-name shapes — NO chosen concept. `data-viewer.tsx` renders the candidates prominently (big, boxed) and moves the rest of the submission into a collapsed `<details>` ("Client details…"); `chosen_name` + `chosen_name_final` added to `FORMATION_NAME_KEYS` so neither leaks into the grouped view. SoS link + chat are the next stage_layout components (below the names). Action label → "Name Confirmed by Client — File with State" (stage_layout, sandbox + migration). Tax-Return data_viewer (`source==='tax'`) unchanged.)_

_Earlier 2026-06-18 — Claude (Company Formation now surfaces a flow CHIP on the CRM **contact** page. In-progress formations are contact-scoped (`account_id` NULL until materialization), so `resolveFlowsByContact` — which only queried `CONTACT_FLOW_TYPES = ['ITIN']` — never returned them (Marinela Marku, sandbox, at "Wizard Submitted", showed no chip). Fix: added `'Company Formation'` to `CONTACT_FLOW_TYPES` (`lib/flows/resolve-flows.ts`) + its `FLOW_ICON` entry (Building2) in `components/flows/flow-chips.tsx` (the icon map is `Record<FlowType,…>` — a new type without an entry breaks the build). The chip links to `/flows/[sd]`. Side effects (verified): account-scoped `resolveFlows` still filters `FLOW_TYPES` only (formation never appears on the account page); `getPortalFlows` contact branch is scoped `account_id IS NULL`, so only in-progress formations surface in portal Service Status (materialized ones are account-scoped; formation-tier clients hit FormationDashboard first); the portal `/portal/flows/[id]` page accepts it as a contact flow → vertical journey. This was a port of the abandoned redesign's commit `bd088fd2` that hadn't been carried into v2.)_

_Earlier 2026-06-17 — Claude (Company Formation **Workspace v2** — 7 stages now drive `/flows/[sd]` (branch `feat/formation-workspace-v2`, migration `20260617-formation-workspace-v2.sql`, NOT yet applied; see formation.md for the full pipeline + code rewires). Flow-engine additions: (1) **`data_viewer` now reads `wizard_progress` for Company Formation SDs** — `app/api/flows/[id]/submission/route.ts` branches on `service_type==='Company Formation'` and returns the latest `wizard_progress` row for the SD's `contact_id` (formation SDs are contact-scoped, account_id NULL — the old account-scoped query returned null), with `source:'formation'`; `components/flows/data-viewer.tsx` surfaces the 3 candidate LLC names prominently (pure `lib/flows/formation-names.ts::extractFormationNames`, handles `llc_name_1/2/3`+`chosen_name` and single-name fallbacks) above the grouped fields. (2) **New `activate_ra` component** (`components/flows/activate-ra.tsx`, registered in `stage-layout.ts` + `stage-renderer.tsx`) on the "Articles Received" stage → POST `app/api/flows/[id]/activate-ra/route.ts` which mirrors `hc_sync_company` (updates the linked HC company; honestly reports when HC creation still needs jurisdiction/structure reference IDs). Disabled until the SD has an account (materialized). (3) **Formation SoS link** — `lib/flows/state-links.ts::resolveFormationFilingLink` (NM/WY/FL/DE, **defaults NM**, distinct from the annual-report `SOS_PORTAL_URL` which excludes NM); `external-link.tsx` resolves it when `serviceType==='Company Formation'` and no literal url; the workspace page injects the formation state from `wizard_progress` (no account yet). (4) **Portal formation tracker rewritten** to read SD pipeline stages — `lib/portal/formation-progress.ts::buildFormationTrackerSteps` (pure) + `components/portal/formation-progress-tracker.tsx` (amber glow + "Action required" on the CURRENT client-action stage: Payment Confirmed → wizard, SS-4 Prepared → sign; EN/IT), fed by `getFormationTracker()` in `lib/portal/queries.ts` at all 4 FormationDashboard render sites; legacy milestone list kept as fallback. (5) The CRM "What's New" panel links a `formation_progress` workflow note to `/flows/[delivery_id]` instead of rendering inline advance buttons.)_

_Earlier 2026-06-16 — Claude (ITIN shipping tracking. Client enters the courier + tracking number for the signed ITIN package they mail to TD on the portal "Client Signing" card (`components/portal/itin-shipping-form.tsx`, POSTs to `app/api/portal/flows/[id]/shipping/route.ts` — auth: SD's own contact OR account 'documents' access; writes via `setServiceDeliveryShipping()` in `lib/operations/service-delivery.ts` since service_deliveries is a protected table). Stored in 3 new `service_deliveries` columns (`shipping_courier`, `shipping_tracking_number`, `shipping_submitted_at`; migration `20260616-2300`). Staff workspace shows it via a NEW `shipping_info` component (`components/flows/shipping-info.tsx`, registered in `stage-layout.ts` STAGE_COMPONENT_TYPES + `stage-renderer.tsx`; reads from the SD row, "Awaiting shipping info from client" until submitted) — added to the ITIN Client Signing `stage_layout` after document_viewer. Courier→tracking-URL map is the pure, tested `lib/flows/courier.ts` (`COURIERS`, `courierTrackingUrl`). The portal form shows only when the SD is AT Client Signing.)_

_Earlier 2026-06-16 — Claude (Client-facing flow DETAIL page (`app/portal/flows/[id]/page.tsx`) now renders a rich, clickable per-stage journey for CONTACT flows (ITIN) via the new `FlowStageJourney` component (`components/portal/flow-stage-journey.tsx`) + pure `buildJourneySteps()` helper in `lib/flows/flow-progress.ts` (works with OR without `client_label` — prod ITIN stages have labels, sandbox doesn't). Each stage expands to its `client_description`; the "Client Signing" stage also shows a print/sign/mail card with TD's document-receiving office address (**Seminole — NOT the CMRA business address**) + a 2× W-7 / 2× 1040-NR+OI / 2× passport-copy checklist. Account flows keep the compact horizontal `FlowProgressTracker`. Gate fix: the page now accepts `ALL_FLOW_TYPES` (was `FLOW_TYPES`, which excluded ITIN → the detail page 404'd for every ITIN flow on both envs).)_

_Latest 2026-06-15 — Claude (ITIN workspace pass 2 — layouts redesigned + Purchase Origin header. All 8 ITIN `stage_layout`s rewritten (migration `20260615-2100`, sandbox): Data Collection = waiting_notice+chat; Document Preparation = document_viewer+info_panel+chat (auto-generated W-7/1040-NR/Schedule OI — REVIEW, no upload, no advance button); Client Signing = TD-office address waiting_notice + advance_next "Documents Received"; Documents Received = upload scan + advance_next CAA Review; CAA Review = advance_next Submitted to IRS; Submitted to IRS = upload tracking + IRS-address waiting_notice + `fax_irs` + advance_next IRS Processing; IRS Processing = waiting_notice + advance_next ITIN Approved; ITIN Approved = upload CP565 (terminal). All forward actions use `advance_next`. NEW **Purchase Origin card** (`components/flows/itin-origin-card.tsx`) rendered ABOVE the stepper for `service_type='ITIN'` only: the flows page (`flows/[id]/page.tsx`) matches the offer by `contact_id` OR via the contact's originating lead (`leads.converted_to_contact_id → offers.lead_id`), shows Bundled/Standalone (`bundled_pipelines` non-empty) + contract link (`/offer/[token]?preview=td`) + the contact's most recent `payments` row (invoice #, amount w/ `amount_currency`, Paid/Pending). No offer → "Manual / Legacy ITIN".)_
_Earlier 2026-06-15 — Claude (Contact-scoped flow chips for ITIN: ITIN SDs are contact-scoped (`account_id` NULL, `contact_id` set), so the account-page `FlowChips` never showed them. `resolve-flows.ts` now splits flow types — `FLOW_TYPES` (the 4 account-recurring) + new `CONTACT_FLOW_TYPES = ['ITIN']`, unioned into `ALL_FLOW_TYPES` which now defines `FlowType`. New `resolveFlowsByContact(contactId)` queries contact-scoped SDs (no scheduled placeholders). The CRM **contact** page (`contacts/[id]`) resolves these and renders `FlowChips` in the Services tab (`contact-detail.tsx`). `flow-chips.tsx` FLOW_ICON gained an `ITIN` entry (UserCheck). `getPortalFlows`/`resolveFlows(accountId)` still use `FLOW_TYPES` only — unchanged. The workspace `info-panel` already tolerates a null account (`company_name ?? '—'`; renewal rows gated to RA/AR).)_
_Earlier 2026-06-15 — Claude (ITIN workspace: filled `stage_layout` for all 8 `service_type='ITIN'` stages (Data Collection → Document Preparation → Client Signing → Documents Received → CAA Review → Submitted to IRS → IRS Processing → ITIN Approved) — migration `20260615-2100-itin-workspace-layouts.sql`, applied to SANDBOX (prod promotion pending Antonio). Each stage uses info_panel + document_upload (autoAdvance:false) / document_viewer + chat + `action_buttons` with `advance_next` objects to its explicit next stage. Two operational addresses live in `waiting_notice` labels: Client Signing → TD mailing address (11125 Park Blvd Suite 104-153, Seminole FL 33772); Submitted to IRS → IRS ITIN Operation (PO Box 149342, Austin TX 78714-9342). ITIN Approved is terminal (upload the assignment letter, no advance).)_
_Earlier 2026-06-15 — Claude (New `fax_irs` stage component: a "Send Fax to IRS" card (`components/flows/fax-irs.tsx`) added to `STAGE_COMPONENT_TYPES`/`stage-renderer.tsx`. Links to the Fax tool (`/tools/fax?to=IRS&message=…`); the fax form pre-fills the IRS fax number from `FAXAGE_IRS_NUMBER`. Added to the Tax Return "Signed" stage layout in sandbox (document_viewer → action_buttons[file_with_irs] → fax_irs → chat). Fax tool itself: `docs/systems/fax.md`.)_
_Earlier 2026-06-14 — Claude (Signed tax return now reaches the Workspace regardless of Drive: `POST /api/signature-request-signed` registered the signed PDF as a `documents` row ONLY inside the Google-Drive-upload block — gated on `account.drive_folder_id` and dependent on a successful Drive upload, whose error is swallowed. So when Drive failed (sandbox always; or any account without a folder / API hiccup) NO documents row was created, and the signed PDF never appeared in the flow Workspace document viewer — even though the SD correctly auto-advanced to "Signed" (step 4 is ungated). Added an idempotent **Drive-independent** registration (step 1b): for a flow-bound signature it inserts a `documents` row served from storage (`drive_file_id=storage:signed-documents/<path>`, `drive_link`=1-year signed URL on the signed-documents bucket), stamped `service_delivery_id` + `flow_stage="Signed"`, `portal_visible=true`. Skips if the Drive path already created the Signed doc (no prod duplicate).)_
_Earlier 2026-06-14 — Claude (Flow chat GET now matches by TOPIC too, not just SD stamp: `GET /api/flows/[id]/chat` resolves the SD's account + `buildFlowTopic` and queries `portal_messages` with `(service_delivery_id = sd OR (topic = flow_topic AND account_id = account))`, `deleted_at IS NULL`, excluding internal `<!-- chat-event:` notes. Fixes client replies typed in the portal chat under the flow topic WITHOUT a `reply_to_id` — those rows carry the topic + account but no `service_delivery_id`, so they were invisible in the Workspace thread. The portal chat POST's existing `reply_to_id` → `service_delivery_id` inheritance is unchanged (still helps when the client replies to a specific message). Client-side topic highlighting already works: `usePortalChat` derives `topics` from distinct message topics and `unreadByTopic` badges each tab, so flow topics ("Tax Return 2026") get the unread badge with no change.)_
_Earlier 2026-06-14 — Claude (2nd installment invoice on the Tax Return "Awaiting 2nd Payment" stage: the workspace page (`app/(dashboard)/flows/[id]/page.tsx`) fetches the account's `payments` row where `payment_category='installment_2'` AND `year=current calendar year` (the annual-installments cron's stamp) ONLY on that stage, and passes a `WorkspaceInvoice` (`components/flows/types.ts`) through `StageRenderer` → `InfoPanel`. `info-panel.tsx` renders an invoice block on that stage: "Invoice #INV-…", amount (`Intl` currency), a Paid/Unpaid badge (`is_paid` = paid_date set or status='Paid', else shows the invoice_status), due/paid date, and a "View invoice" link to `/api/invoices/[id]/pdf` (staff PDF). Shows a "not found yet" note when no invoice exists. Scoped to the one stage — no extra query elsewhere.)_
_Earlier 2026-06-14 — Claude (Every stage has a clear forward action: new generic **`advance_next`** action (an object `{key:"advance_next", label, target}` in `action_buttons.actions`) and a display-only **`waiting_notice`** component type. `action-buttons.tsx` now accepts `StageAction[]` (bare keys OR advance_next objects) and resolves advance_next to a forward button to its explicit `target` stage with the layout-supplied `label` (POSTs the same `/api/flows/[id]/advance`). `stage-layout.ts` parses both forms (`parseStageAction`, backward-compatible with string keys). Sandbox Tax Return layouts updated: stages 20/30/40/70 got advance_next buttons ("Proceed to Awaiting 2nd Payment" / "Mark 2nd Payment Received" / "Open Wizard for Client" / "Prepare Tax Return"); stages 50/67/85 got a `waiting_notice` (waiting on the client). Explicit targets are used (not next-by-stage_order) to avoid the Under Review→Revision Requested branch trap. AR/RA already had `complete` buttons. NOTE: production pipeline_stages still need the same layout update; "Open Wizard for Client" only advances the stage, it does not itself send the wizard to the client.)_
_Earlier 2026-06-14 — Claude (Client-facing flow surface on the PORTAL side: `lib/flows/flow-progress.ts` (pure, unit-tested) exposes `computeFlowProgress` (count + current label) and `buildFlowSteps` (full ordered completed/current/future step list, catalog `icon` carried through). `getPortalFlows()` (`lib/portal/queries.ts`) feeds the portal dashboard "Service Status" section, which renders a **visual stepper per flow** via the generic `components/portal/flow-progress-tracker.tsx` (Tax Return suppressed there when the Slice-5 tracker already shows it). The portal documents page also groups `documents.service_delivery_id`-stamped docs into per-flow sections. Staff workspace unchanged — read-only client view over the same SDs. Full writeup in `portal.md`.)_
_Last verified against code: 2026-06-14 — Claude (Clickable stepper fires FULL side effects: `stage-stepper.tsx` (client component) — clicking any stage (other than the current) moves the SD to it, forward OR backward, after a "Move to [stage]?" confirm, via `POST /api/flows/[id]/set-stage` → `moveServiceDeliveryToStage` (lib/operations/move-stage.ts). The stepper is a SHORTCUT for the action buttons + Go Back, NOT a silent move: FORWARD dispatches ONE `advanceServiceDelivery({target_stage})` (auto-tasks + client notification + completion incl. +1y renewal-date bump — single hop, one notification, never iterated); BACKWARD iterates `revertServiceDelivery` one stage at a time to the target (deletes each re-opened stage's documents + undoes the renewal bump leaving "Closed"; revert sends no client notification). Direction is decided by stage_order resolved by NAME. The earlier silent-move `setServiceDeliveryStage` helper was REMOVED. The "← Go Back" button visibility was verified already-correct: shown on every stage except the first.)_
_Earlier 2026-06-14 — Claude (Flow chat: replaced the `chat` stub with `flow-chat.tsx` — a per-SD, CLIENT-FACING message stream backed by `portal_messages` filtered by a new `portal_messages.service_delivery_id` column (migration `20260614-1700`); `GET`/`POST /api/flows/[id]/chat`. Staff send auto-stamps `service_delivery_id` + `topic` (the flow name, e.g. "Tax Return 2025") + `account_id` + `contact_id` + `sender_type='admin'`, so the message shows in the client's portal chat AND notifies them via the existing portal notification system. The portal chat POST inherits `service_delivery_id` from a replied-to flow message so client replies thread back here.)_
_Earlier 2026-06-14 — Claude (Tax Return e-signature: extended the existing portal signature system to the flow — `signature_send` (stage 80) + `signature_status` (stage 85) components, `lib/operations/signature.ts` shared helper, `/api/flows/[id]/send-for-signature` + `/signature` routes, `document_upload` auto-advance opt-out, webhook auto-advance 85→90 on sign, `signature_requests.service_delivery_id` column + sandbox `signature-requests`/`signed-documents` buckets.)_
_Earlier 2026-06-14 — Claude (Tax Return workspace: built `DataViewer` (schema-agnostic submitted_data renderer) + `/api/flows/[id]/submission`, extended `ActionButtons` to the 5 Tax Return transitions, made info-panel treat "Completed" as terminal, seeded all 15 Tax Return stage_layouts in sandbox.)_

## What it is
A per-service-delivery **Workspace** page (`/flows/[id]`, `[id]` = `service_delivery_id`) that drives the staff UI for a recurring service from a **catalog-stored layout descriptor** — no per-stage React. Each `pipeline_stages` row carries a `stage_layout` (JSONB) listing the components to render for that stage. The page reads the SD's current stage, looks up that stage's layout, and renders it. Built for the four recurring flows (`Tax Return`, `State Annual Report`, `State RA Renewal`, `CMRA Mailing Address`); Tax Return is the fully-built reference flow (15 stages).

## How it works
- **Page** `app/(dashboard)/flows/[id]/page.tsx` (server component): loads the SD, its account, and ALL `pipeline_stages` for the SD's `service_type`. Matches the current stage by **NAME** (`sd.stage` — `stage_order` on the SD is frequently NULL/stale). Renders `StageStepper` (clickable — jumps to any stage via `/set-stage`) + `StageRenderer` + `GoBackButton` (hidden on the first stage).
- **Layout schema** `lib/flows/stage-layout.ts`: `parseStageLayout()` narrows the JSONB into `{ components: [{type, label?, url?, actions?}], description? }`. Unknown/missing layouts degrade gracefully (default Overview panel).
- **Renderer** `components/flows/stage-renderer.tsx`: maps each component `type` → a React component. Every component receives the same `serviceDelivery` + `account` context and uses what it needs.

### Component types (`STAGE_COMPONENT_TYPES`)
| type | component | purpose |
|------|-----------|---------|
| `info_panel` | `info-panel.tsx` | Overview card (company, stage, time-in-stage, deadline, assignee). On the terminal stage (`Closed` for AR/RA, `Completed` for Tax Return) shows a green "Completed" banner + completed-on; "Next renewal" row only for AR/RA. |
| `document_upload` | `document-upload.tsx` | Signed-URL upload → `POST /api/flows/[id]/upload-document` → stamps a `documents` row with `service_delivery_id` + `flow_stage`, then **auto-advances** the SD to the next stage. |
| `document_viewer` | `document-viewer.tsx` | Lists the SD's documents (`GET /api/flows/[id]/documents`), newest first, with View links. |
| `data_viewer` | `data-viewer.tsx` | Renders the account's latest tax-wizard submission (`GET /api/flows/[id]/submission`) as grouped, readable cards. **Schema-agnostic** — see below. |
| `action_buttons` | `action-buttons.tsx` | Renders a button per action key in the layout; each POSTs to `/api/flows/[id]/advance` with a target stage. |
| `external_link` | `external-link.tsx` | Static/state-resolved external link (e.g. Secretary of State). |
| `signature_send` | `signature-send.tsx` | "Send for Signature" action (Tax Return stage 80). Enabled once a doc is uploaded; POSTs `/api/flows/[id]/send-for-signature` → creates a `signature_requests` row linked to the SD, notifies the client, advances to "Sent for Signature". |
| `signature_status` | `signature-status.tsx` | Shows the SD's signature request status (Waiting / Signed on date) + a staff preview link. Reads `GET /api/flows/[id]/signature`. |
| `chat` | `flow-chat.tsx` | Per-SD CLIENT-FACING message stream backed by `portal_messages` filtered by `service_delivery_id`. Loads `GET /api/flows/[id]/chat` (oldest-first); staff send via `POST` auto-stamps `service_delivery_id` + `topic` (the flow name) + `account_id` + `contact_id` + `sender_type='admin'` + `sender_id` (the auth user), so the message appears in the client's portal chat and fires the existing admin-message client notification. Client replies to a flow message inherit its `service_delivery_id` (in the portal chat POST) and thread back here. |
| `notes` | stub | Placeholder panel (not yet built). |

`document_upload` takes an optional `autoAdvance` (default true). Stage 80 sets it **false** so uploading the prepared return does NOT advance — the `signature_send` action owns the advance instead. Every other upload stage keeps the default auto-advance.

### DataViewer + submitted_data (schema-agnostic)
`lib/flows/submitted-data.ts::groupSubmittedData()` turns a flat tax-wizard `submitted_data` blob into ordered display groups **without assuming a fixed schema** (keys vary by entity type — SMLLC / MMLLC / Corp):
- Keys shaped `<base>_<index>_<field>` (e.g. `member_0_member_first_name`, `bank_accounts_1_bank_name`) regroup into per-index entity cards ("Member 1", "Bank Account 1"); a redundant repeated base prefix in the field is stripped.
- Remaining flat keys bucket by leading token: `owner_` → Owner, `comp_` → Tax Questions, `us_` → US Activity, else → Company.
- `formatValue()` normalizes for display: boolean → Yes/No, number → string, file-path arrays → "N files", empty/null → omitted.
- Group order: Company, Owner, entity cards, US Activity, Tax Questions. Pure + unit-tested (`tests/unit/submitted-data.test.ts`).

The submission is fetched per-account (newest `tax_return_submissions` by `created_at`) by `/api/flows/[id]/submission`, which resolves the SD → `account_id` first.

### ActionButtons → stage transitions
`ACTION_CONFIG` in `components/flows/action-buttons.tsx` maps each layout action key to a target stage. Each button POSTs `{ target_stage }` to `/api/flows/[id]/advance`, which calls `advanceServiceDelivery` (the single source of truth for advance side effects: stage_history, status/completion, auto-tasks, portal notify). Unknown keys are ignored.

| action key | target stage |
|------------|--------------|
| `start_review` | Under Review |
| `approve` | Review Completed |
| `request_changes` | Revision Requested |
| `file_with_irs` | Filed with IRS |
| `mark_completed` | Completed |
| `complete` | Closed _(AR/RA recurring renewal — original behavior)_ |

## The Tax Return 15-stage flow (sandbox pipeline)
`Extension Due (10)` → `Extension Filed (20)` → `Awaiting 2nd Payment (30)` → `2nd Installment Paid (40)` → `Wizard Available (50)` → `Data Submitted (60)` → `Under Review (65)` → `Revision Requested (67)` → `Review Completed (70)` → `Tax Return Prepared (80)` → `Sent for Signature (85)` → `Signed (90)` → `Filed with IRS (95)` → `IRS Receipt Uploaded (98)` → `Completed (100)`.

Review actions live on the SD stage (`Data Submitted` → Start Review → `Under Review` → Approve/`Review Completed` or Request Changes/`Revision Requested`). Document uploads auto-advance (Extension Due→Filed, Filed with IRS→IRS Receipt Uploaded). **Exception:** the `Tax Return Prepared` upload does NOT auto-advance — the **e-signature** sub-flow below owns that transition.

### E-signature sub-flow (Tax Return Prepared → Sent for Signature → Signed)
Reuses the existing portal signature system (`signature_requests` + `/sign-document/[token]/[code]` + `/portal/sign/document`), NOT a new one.
1. **Stage 80 (`Tax Return Prepared`)** — staff uploads the return (`document_upload`, auto-advance off), then clicks **Send for Signature** (`signature_send`). `POST /api/flows/[id]/send-for-signature` fetches the latest SD document's PDF bytes (Drive in prod, `onboarding-uploads` storage in sandbox — `fetchFlowDocumentPdf`), calls `createSignatureRequest` (`lib/operations/signature.ts`) which stores the PDF in the `signature-requests` bucket and inserts a `signature_requests` row stamped with `service_delivery_id`, creates a portal notification ("Your tax return is ready to sign"), and advances the SD to `Sent for Signature` with `skip_notify:true` (one tailored notice, not the generic stage-move one).
2. **Client portal** — the request auto-appears on `/portal/sign` as a "Tax Return" card (the generic `signature_requests` path already lists these) → client signs at `/sign-document/[token]/[code]` (canvas → pdf-lib overlay → `signed-documents` bucket → status `signed` → `POST /api/signature-request-signed`).
3. **Stage 85 (`Sent for Signature`)** — `signature_status` shows Waiting / Signed-on-date. The signed webhook (`/api/signature-request-signed`), seeing the request's `service_delivery_id` and the SD at `Sent for Signature`, advances it to `Signed` and stamps the signed PDF document row with the SD (idempotent: guarded on the current stage).
4. **Stage 90 (`Signed`)** — `file_with_irs` action proceeds as before.

`createSignatureRequest` sets `drive_file_id=NULL` so `/api/signature-request/[token]/pdf` serves uniformly from the `signature-requests` bucket in both environments. The MCP `signature_request_create` tool (OA/8879 production signing) is unchanged. DB: `signature_requests.service_delivery_id` (nullable FK) + the two storage buckets — migration `20260614-1500-tax-return-esignature.sql` (production already had the buckets; the column needs promoting).

> **Important divergence from the production tax pipeline.** `docs/systems/tax-returns.md` documents the PRODUCTION pipeline (Data Submitted 45 / Under Review 46 / Approved 48 / Confirmed 49 with a `tax_return_submissions.review_status` sub-state machine, SD parked at "Data Submitted" through the whole loop). The flow-workspace sandbox pipeline above is a DIFFERENT model where the review states are REAL SD stages advanced directly by the action buttons. Verified against live sandbox `pipeline_stages` on 2026-06-14. Do not assume the two pipelines match.

## How it's built
- `lib/flows/` — `stage-layout.ts` (schema), `resolve-flows.ts` (which flows an account has), `submitted-data.ts` (DataViewer grouping), `state-links.ts`, `workspace-format.ts`.
- `components/flows/` — the renderer + one component per type.
- `app/api/flows/[id]/` — `advance` (target_stage, full side effects), `set-stage` (stepper clicks → `moveServiceDeliveryToStage`: forward via advance, backward via iterative revert — FULL side effects both ways), `revert` (go back one stage), `upload-document` (upload + optional auto-advance via `auto_advance` body flag), `documents` (list), `submission` (latest tax submission), `send-for-signature` (create signature request + notify + advance), `signature` (latest request status), `chat` (per-SD client-facing `portal_messages` stream: GET oldest-first + POST staff send auto-stamping `service_delivery_id` + `topic` (flow name) + `account_id`/`contact_id` + client notification).
- `lib/operations/signature.ts` — `createSignatureRequest` (shared sig-request core, Buffer-sourced), `fetchFlowDocumentPdf` (Drive/Storage dual source), `buildSignatureToken` (pure, unit-tested).
- Layouts are stored in `pipeline_stages.stage_layout` per `(service_type, stage_name)`. Editing a layout = SQL/UPDATE on that row; no code change.
- **System tokens in layout text.** A component `label` or the stage `description` may reference a token like `{td_mailing_address}`, resolved by `parseStageLayout` from `STAGE_LAYOUT_TOKENS` (`lib/td-address.ts`) using the lenient `interpolateString`. Unknown tokens are left literal — a visible `{foo}` beats a silent blank. **Never paste the office address into a stage_layout row**: the ITIN "Client Signing" note carried its own copy and had already drifted from the code ("Suite" with no comma, "Florida" spelled out) before the token existed. Add a token to the map rather than a second copy of any value that lives in code.

## Gotchas, invariants & past bugs
- **Match the stage by NAME, not stage_order** — SD `stage_order` is frequently NULL while `stage` is set.
- **`advanceServiceDelivery` is the single source of truth for advance side effects.** Action buttons and the upload route both go through it. It fires a portal notification on every advance unless `skip_notify` (the flow advance route does NOT skip — staff stage moves notify the client, consistent with the original AR/RA "complete" button).
- **`document_upload` auto-advances to the immediate next stage** (by `stage_order`). It works for Tax Return because each upload stage's next stage is the intended target. Do not add a `document_upload` to a stage whose "next" by order isn't the desired destination.
- **Terminal-stage detection is stage-name based:** AR/RA close at `Closed`; Tax Return closes at `Completed`. `info-panel.tsx` treats both as terminal; `advanceServiceDelivery` treats `Completed` / `TR Filed` / (`Closed` for AR/RA) as completion.
- **Sandbox document storage fallback:** with `SANDBOX_MODE=1` uploads stay in the `onboarding-uploads` bucket (signed URL) instead of Google Drive (many seeded accounts have no Drive folder).
- **Every staff route locks its own door — `/api/*` is NOT covered by the middleware's client-role bounce (2026-08-13, card 4a39e0fd).** The `chat` route returns the account's WHOLE conversation by design, which is correct for staff and wrong for a client: it was missing a role check, so a portal CLIENT session holding a service-delivery id could read the full staff-facing thread (GET) or post dressed as staff, `sender_type='admin'` (POST). Fixed with `requireStaffRoute()` at the top of both handlers. "The session is logged in" is not "the session is staff" — check every route that assumes the dashboard is its only caller.

## How to verify current state
- Layouts: `SELECT stage_order, stage_name, stage_layout FROM pipeline_stages WHERE service_type='Tax Return' ORDER BY stage_order` (sandbox MCP / psql — R096).
- Open `/flows/<service_delivery_id>` for a Tax Return SD and walk the stepper.
- Submission shape: `SELECT entity_type, jsonb_object_keys(submitted_data) FROM tax_return_submissions WHERE account_id='<id>' ORDER BY created_at DESC LIMIT 1`.
- Unit tests: `tests/unit/submitted-data.test.ts`, `tests/unit/resolve-flows.test.ts`.
