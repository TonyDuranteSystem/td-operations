# Lease & Operating Agreement (OA)
_Last verified against code: 2026-08-10 — Claude (**generate-document route: SS-4 branch only — OA and Lease untouched.** The CRM Generate-SS-4 path in `app/api/crm/admin-actions/generate-document/route.ts` no longer stamps `fetchAccountAndContact`'s first-linked-contact as the SS-4 responsible party; it resolves the default via the shared role-aware `pickDefaultSs4SignerLink` (the ACE Marketing Group fix, dev job `833562d7`). DELIBERATELY scoped inside `generateSS4`: `fetchAccountAndContact` itself is unchanged, because `generateOA` and `generateLease` share it and for an SMLLC that contact is the OA's legally named sole Member — a role-aware change there would silently rename the member on Operating Agreements. Never move the SS-4 signer logic into the shared helper. Full rule set: `formation.md` (2026-08-10).)_
_Prior: 2026-08-09b — Claude (**An OA is never issued with fewer signers than members — plus the signing-gate regression that made the rule urgent.** Antonio amended the same day's rule: a multi-member agreement signed by only one owner must never exist, so an unreachable member is now a HARD STOP (`describeSigningBlock`), named to the client on the portal route and reported to the error auto-audit from the welcome-package job, which no longer inserts the agreement at all. And per-member signing is now keyed on the EXISTENCE of `oa_signatures` rows, never `total_signers > 1` — with one expected signature every per-member gate switched off at once (no `?signer=` link, shared access code could sign, submitted signer code ignored, signature recorded under the roster's first member). Live exposure was Full Throttle Media LLC; it had no agreement yet, so nothing was executed wrongly. Verified safe against existing production data. 8605 unit tests + build green.)_

_Prior: 2026-08-09 — Claude (**Membership ≠ signing: the OA signing set** — dev job `fc69557f`. `lib/members/signing-set.ts` is now the single source for who signs, shared by the portal create route and the welcome-package job. An individual with no email is a member (roster + ownership) but not a signer; a company member signs through its representative, keyed on `members.contact_id` first and the representative text only as fallback; `total_signers` counts the signing set, not the roster; the route refuses only when NOBODY can sign. One person can hold TWO capacities (Umberto Moretti is Azarexa’s 1% individual member AND the contact behind its 99% corporate member) — `app/portal/sign/oa/page.tsx` used `.maybeSingle()` and so handed him NO signing link at all; it now walks his capacities in order.)_
_Prior: 2026-07-26b — Claude (**CRM Documents-to-Sign: working lease "View" preview + "Cancel draft" button** (dev job `b18cd1d8`). (1) The lease row's preview link was broken two ways: CODELESS (`/lease/{token}?preview=td` — on the client-facing domain the staff cookie is absent, so `?preview=td` doesn't authenticate and it fell back to requiring the code → "Invalid access code"), AND hard-coded to the PRODUCTION domain `app.tonydurante.us`, so on sandbox it opened the PRODUCTION lease (different access code → "Invalid access code" — the actual symptom Antonio hit). Fixed: the code is now in the path (`/{token}/{access_code}`) and the base domain is chosen at RUNTIME — `previewBase` uses `window.location.origin` on sandbox/preview/localhost (where the CRM and the client pages are one deployment on one domain) and the server-provided `appBaseUrl` (=app.tonydurante.us) in production. **Why not an env var:** there is none correct in both — `PUBLIC_APP_BASE_URL` is EMPTY on the sandbox deployment (server `APP_BASE_URL` falls back to prod) and `NEXT_PUBLIC_APP_URL` is the internal CRM host in prod. `appBaseUrl` is threaded server→AccountDetail→DocumentsPanel. Same runtime-base fix applied to the OA + SS-4 preview links (production behaviour unchanged — still app.tonydurante.us). The shared preview control now shows a visible **"View"** label instead of a bare icon. Preview correctly does NOT bump `view_count` / flip status to `viewed` (the fetch route skips tracking whenever the `preview=td` flag is present). (2) New **Cancel draft** button (lease + `status='draft'` ONLY) → new `cancel_lease_draft` action → `cancelLeaseDraft(token)` in `lib/operations/lease.ts` (NOT the route — keeps the protected `accounts` write in the operations layer). Refuses any non-draft (pre-read guard + conditional-delete `.eq('status','draft')` TOCTOU guard + `!deleted?.length` honest re-report). **Bug-hunter-caught + fixed:** on delete it also reverts `accounts.physical_address` — `createLease` step 7 writes the account's physical_address to the suite even for a draft, and that suite is a legacy account's client-visible mailing address (portal `resolveMailingAddress` fallback); leaving it stale while `nextSuiteNumber()` (global-max+1) recycles the freed number to the next new account would show two clients the same suite. Revert recomputes from the account's REMAINING same-tenant leases (earliest suite, else null), and only when the stored address still reflects the cancelled suite (never clobbers a manual address). Admin-gated in UI (`isAdmin`); the route gate `canPerform('generate_oa')` is TEAM-tier (parity with generate/send). Verified: `lease_agreements.status` is free varchar (no check constraint) in sandbox, no FKs reference the table, draft only creates the lease row + the address update. 7146 unit tests (7 new in `operations-lease-cancel.test.ts`) + tsc + eslint green; View URL rendered end-to-end on the sandbox dev server; unauthenticated cancel rejected. Files: `app/(dashboard)/accounts/[id]/components/documents-panel.tsx`, `app/api/crm/admin-actions/generate-document/route.ts`, `lib/operations/lease.ts`. Branch `claude/lease-draft-preview-b4853f`.)_
_Prior: 2026-07-26 — Claude (**Lease date-picker + renewal auto-create/send + ready-to-sign alert + one-per-year DB rule** (dev jobs `16e6bbcb`, `0dc29dc4`). Staff pick Start/End dates in the Generate Lease dialog (`generate-lease-dialog.tsx`): Start pre-fills the account's `formation_date` only when formed THIS year, else today; End = Dec 31 of the start year (auto until hand-edited); both editable. `generateLease` (`generate-document/route.ts`) forwards `term_start_date`/`term_end_date` and derives `contract_year` from the start year; the old false "Jan 1 – Dec 31" label is gone. On a client's 1st installment, `onFirstInstallmentPaid` (`lib/installment-handler.ts`) now auto-creates the renewal lease (Jan 1–Dec 31, suite reused) and sends it to the portal — only a lease THIS run created is auto-sent — replacing the dead "create lease" task. New shared `sendLeaseToPortal` in `lib/operations/lease.ts` flips draft→sent (no email), treats sent/viewed/signed as already-in-portal, and fires `notifyLeaseReadyToSign` (in-portal chat + bell/push + email); the CRM Send button delegates to it (killed a `viewed`→`sent` knockback). DB guard: partial unique index `uq_lease_account_year_tenant (account_id, contract_year, tenant_company)` (applied to prod 2026-07-26) + a 23505-as-duplicate path in `createLease`; the duplicate CHECK stays on account+year so a renamed company's same-year re-gen is refused rather than silently re-suited/address-drifted. Reviewed by full council + 2 bug-hunter passes; 10/10 sandbox E2E.)_
_Last verified against code: 2026-07-24d — Claude (**Two lease document-correctness fixes from the security-lockdown open list (dev job `8ffd1122`).** (1) **CRM "Regenerate Lease PDF" filed a fabricated document.** `app/api/crm/admin-actions/regen-lease-pdf/route.ts` took caller-supplied `signed_date` / `start_date` / `end_date` and — because `lib/lease-pdf.ts` draws `/s/ <tenantContactName>` whenever `signedDate` is set — stamped a typed tenant signature onto the filed copy REGARDLESS of whether the lease was signed. So it could file a "signed" lease for a draft nobody executed, or with dates differing from the agreed terms, overwriting `pdf_storage_path` + the portal `documents` row + the Drive file. Now it refuses any lease that isn't genuinely signed (`status='signed'` AND `signed_at`) and rebuilds ONLY from the lease's own recorded `signed_at` / `effective_date` / `term_start_date` / `term_end_date` — no request dates. The dialog (`app/(dashboard)/accounts/[id]/components/regen-lease-pdf-dialog.tsx`) drops the editable date inputs and shows those dates read-only. Both signing pages set `status='signed'` + `signed_at` together, so genuine signed leases still regenerate. (2) **CRM "Send Lease" button lied.** `sendLease` in `app/api/crm/admin-actions/generate-document/route.ts` marked the lease `status='sent'` (which is what makes it appear in the client's PORTAL sign list — `lib/portal/queries.ts` shows leases in `['sent','viewed','awaiting_signature']`) but returned `sent_to: tenant_email`, so the UI (`documents-panel.tsx`) showed "Sent to <email>" — staff believed the client was emailed. No email is ever sent (leases reach clients through the portal). Now returns `emailed:false` + a portal `notice`, exactly mirroring the already-shipped `send_oa` fix, so the existing `emailed===false` UI branch shows "Ready — it now appears in the client's portal to sign. No email is sent." 7101 unit tests + build green. STILL OPEN on `8ffd1122`: server-side lease signing (revoke anon UPDATE), unguarded `lease_update` on a signed lease, non-Latin names crash regen, `lease_send` idempotency probe unbounded by year. Branch `claude/oa-filing-uses-recorded-path`.)_
_Prior: 2026-07-24c — Claude (**LEASE URGENT SECURITY LOCKDOWN — the lease was the pre-rebuild OA (guessable token + client-side-only gates + live anon SELECT/UPDATE + browser-screenshot signing + two unauthenticated filing endpoints). Two adversarial bug-hunter audits (dev job `8ffd1122`); this closes the READ + injection + preview-sign vectors. (1) **Anon-READ disclosure closed** (the top exposure — a guessed `${companySlug}-${year}` token returned any of 125 leases' access_code + tenant_ein + tenant_email + terms via the shipped anon key). NEW `GET /api/lease/[token]/fetch` (+ `lib/lease/public-view.ts`): verifies the access code server-side, evaluates the email gate, tracks the view with the service key, returns a WHITELIST (never access_code / tenant_email); `assertNoLeaseSecrets` guards the payload. Both signing pages (`app/lease/[token]/[code]/page.tsx` + the legacy `[token]/page.tsx`) now load through it — no more anon `select('*')` + client-side code check; view tracking moved server-side. `middleware.ts`: `/api/lease/` added to PUBLIC_PREFIXES (token-gated, not session-gated — without it the route 401s SESSION_EXPIRED; a new-public-route trap). Migration `20260724-1900-lease-close-public-read.sql` revokes anon SELECT to id-only + drops the USING(true) select policy. **DEPLOY ORDER load-bearing: CODE first, THEN the revoke** — old pages' `select('*')` breaks otherwise. (2) **`/api/lease-regen-drive`** had NO auth and filed a CALLER-SUPPLIED bucket path into the client's Drive + portal — now requires a real dashboard session (matching its gated sibling) and reads the path from the LEASE ROW. (3) **`/api/lease-signed`** ('No auth required') fired email + SD advance + task + Drive filing on a guessed token — now requires the per-lease access code, verified server-side (both pages pass it). (4) **`?preview=td` could click Sign** on a real client's lease — both pages now refuse to sign in admin preview. anon-grant contract updated: lease_agreements SELECT dropped (browser no longer reads). SANDBOX-PROVEN with the read closed: anon `select('*')` → 42501 blocked, anon UPDATE-by-id (signing write) → WORKS, deployed fetch route returns the lease with NO access_code/tenant_email. NOT CLOSED (tracked step 2 = server-side signing, mirrors the OA): anon UPDATE on lease_agreements stays until signing moves server-side, so forge-with-a-known-code is still possible; browser html2pdf screenshot signing (no certificate/IP/UA/consent/hash) unchanged; plus the document-correctness defects still open — CRM Send sends nothing, Regenerate files a fabricated-signature doc, renewal suite/address drift (nextSuiteNumber global +1 overwrites accounts.physical_address), unguarded lease_update on a signed lease. Dev job `8ffd1122`.)_
_Prior: 2026-07-24 — Claude (**Post-ship sweep of the whole OA surface — two blockers + a batch of correctness/UX fixes; found by three adversarial bug-hunter passes after the server-side-signing ship.** (1) **CRM "Recreate" was a THIRD unguarded delete door.** The `hasCollectedSignatures` rule was wired into the portal route and the MCP tool but NOT `app/api/crm/admin-actions/generate-document/route.ts`, which hard-deleted the agreement + every signature on `force_recreate` with no status check, no soft-delete, no audit (R100) — two clicks from destroying any of 74 signed agreements. Now guarded, plus a defence-in-depth signed-CHILD-row count that fails closed, plus `.order(created_at desc)` on the lookup. New test `tests/unit/oa-regenerate-guard.test.ts` scans `app/`+`lib/` for ANY unguarded `oa_agreements` delete (proven non-vacuous — finds all three doors). (2) **CRM "Generate" built MMLLC agreements as SMLLC** — it set `entity_type=MMLLC`+members JSON but never `total_signers` (DB default 1) and never created `oa_signatures` rows, so the draft AND the executed PDF rendered "the sole Member … 100%" and the client could not e-sign at all. Now sets `total_signers` + seeds the signer rows (rollback on failure). Also: signing-endpoint SMLLC insert now only treats a 23505 unique-violation as a double-submit (other errors surface, not a false "signed"); `oa_send` + CRM send refuse `voided`/`signed`/`partially_signed`; the CRM Send button no longer claims "Sent" (it does NOT email — **agreements reach clients through the PORTAL**; ~5 OA emails in 120 days vs 52 portal notifications); `formation_date` can never render the literal "null"; `oa_get` distinguishes electronic vs client-declared `by_hand` vs legacy-null signing; the Certificate of Completion PAGINATES instead of silently truncating past ~6 signers; paper-signing (Antonio: the client's document, their responsibility) keeps its behaviour but drops the false "All Members Have Signed" + always-failing Download and shows a paper-specific panel with an upload-a-copy prompt — delivered end-to-end by adding `signature_method` to `OA_AGREEMENT_SELECT` **and** `toPublicAgreement` (the first cut added it to the SELECT only, so the panel was dead code — caught by the bug-hunter). Portal self-service now refuses an unsupported state (via shared `normalizeOAState` — the first cut compared full names against 2-letter codes and would have refused 289/291 accounts; pinned by `tests/unit/oa-state-normalisation.test.ts`). Formation dashboard stops nagging on `voided`/`draft`. Hand-signed upload now sniffs magic bytes. postMessage origin derived from the embedder (was hardcoded to prod, invisible in sandbox QA). Generate-OA dialog now opens with a notice telling staff to send the client to their portal first (TD does not generate OAs for clients — the client self-generates so it carries their own data; the **lease** is the doc TD generates). E2E 25/25 + 3 bug-hunter passes green; 7014 unit tests. KNOWN-OPEN (minor, Antonio-accepted): company-as-member MMLLC (e.g. "Advertising Apex LLC 99% + Umberto Moretti 1%") cannot self-serve an OA — its own design job (needs a representative concept like SS-4 has); MCP `oa_create force_recreate` still lacks the signed-child count; `oa_send` compares raw `entity_type` without normalize; an MMLLC with exactly one member row shows a Sign button with no pad; a mixed-mode MMLLC where one owner e-signed then another declares paper sets aside the collected e-signature. Dev job `6b28ef2b`. Branch `claude/oa-filing-uses-recorded-path`.)_
_Prior: 2026-07-23c — Claude (**OA online signing moved SERVER-SIDE — server-drawn executed PDF + legal Certificate of Completion + draw/type/upload, and the anon-write hole is closed.** Was: the browser screenshotted the signing page (html2pdf) into the executed PDF and wrote `oa_signatures` + `oa_agreements` (status, pdf path, signed_count) directly with the ANON key — a token-guesser could repoint an agreement at their own PDF and mark it signed. Now: `POST /api/operating-agreement/[token]/sign` does it all on the server — verifies the access code (shared `accessCodeError` guard), resolves WHICH member from their per-member code (`resolveSignerIndex`, never a client-supplied index), captures IP + user-agent + consent, writes with the SERVICE key, gates the last signer on the atomic `increment_oa_signed_count` RPC, and on the last signature runs `lib/oa/finalize-signing.ts::finalizeOaAgreement` — renders the executed OA as real text via `generateOperatingAgreementPDF`, appends the e-sign `appendCertificatePage` (who/when/IP/device/consent + document + per-signature SHA-256), stores the PDF BEFORE flipping status (a failure leaves `partially_signed` for retry, never "signed with nothing filed"), then calls `/api/oa-signed` for the Drive filing + SD/action_log/email side-effects. Staff preview FAILS CLOSED (a `?preview=td` POST cannot sign, `isStaffPreview`). SMLLC now gets a real `oa_signatures` row (index 0) — fixes the long-standing "signed SMLLC has no signature record" gap. Reconciliation safety net: `/api/cron/oa-finalize-sweep` (every 10 min) finalizes any `partially_signed` row with `signed_count >= total_signers` (excludes `signature_method='by_hand'` in JS, not SQL — a stuck row has NULL method and `not.eq` would drop it; narrowed to `signed_count >= 1`). The signing screen (`app/operating-agreement/[token]/[code]/page.tsx`) now offers Draw / Type (signature-font canvas) / Upload (photo → canvas → PNG), all producing one PNG; a consent checkbox gates the Sign button; POSTs to `/sign`; html2pdf + anon writes removed. Migration `20260723-1600-oa-signing-audit-columns.sql` adds `oa_signatures.{last_ip,last_user_agent,consent,signature_method(drawn|typed|uploaded),signature_hash}` + `UNIQUE(oa_id, member_index)`. Per-signer `signature_method` (drawn|typed|uploaded) is distinct from `oa_agreements.signature_method` (electronic|by_hand). E2E-verified: 25/25 scenarios + certificate bytes opened (pdftotext) for SMLLC + MMLLC; sandbox deploy end-to-end signed+filed. KNOWN, not blockers: CJK member names render as tofu (DejaVu-no-CJK, whole renderer, pre-existing); `/sign` verifies the access code but not the email gate on top (by-design, matches prior posture); this closes the anon-write residual on dev_task 023c7d06. Branch `claude/oa-filing-uses-recorded-path`.)_
_Prior: 2026-07-23b — Claude (Signing controls no longer print inside the executed OA. Online signing still screenshots the signing page (html2pdf) into the executed PDF; the "Signature:" label and "Clear signature" link sat inside the captured area and were printing in the signed document. They now carry `data-hide-in-pdf` and are `display:none`'d the instant before each capture, matching the existing action-bar hide — signature image untouched. Both capture sites (SMLLC + MMLLC last-signer) in `app/operating-agreement/[token]/[code]/page.tsx`. KNOWN, deferred: the executed copy is still a raster screenshot, not server-rendered text — the real fix is to render the signed copy on the server like the draft (see the draft-download note below).)_
_Prior: 2026-07-23 — Claude (OA read-before-signing DRAFT download + "I signed by hand" path with optional scan upload — see "OA draft download & sign-by-hand" below; branch `claude/oa-filing-uses-recorded-path`)_
_Prior: 2026-07-22b — Claude (**The STAFF `oa_create force_recreate` path could destroy a fully signed Operating Agreement — one rule now guards both doors.** The earlier fix put `hasCollectedSignatures` on the client-facing portal route only; `lib/mcp/tools/oa.ts` had **no status check at all**, so `force_recreate=true` deleted the `oa_signatures` rows and the agreement itself with no soft-delete and no audit record (R100) — including a `signed` one. Production carries 73 signed OAs. It also selected the row to delete with `.limit(1)` and **no `.order()`**, while `oa_get` and `/portal/sign` both read the NEWEST — so on an account with more than one OA it could delete a *different* agreement than the one staff were looking at. Both fixed: ordered by `created_at DESC`, and the same `hasCollectedSignatures` predicate (`lib/portal/oa-regenerate-guard.ts`) now gates both writers, with a unit test that pins the shared rule explicitly so a future bespoke check on either side fails CI. **The distinction that matters — Antonio, 2026-07-22:** replacing an UNSIGNED agreement is normal and expected, a new one supersedes the old draft; destroying a SIGNED one is not, because the delete leaves no evidence the client ever signed — no signature, no date, nothing to show a bank or the IRS. The sharpest case is a multi-member LLC at 2-of-3 signed: it stays `partially_signed` until the LAST member signs, so the old `status === 'signed'` guard let a re-generate erase two executed signatures and force those members to sign again with no trace. The refusal message tells staff to VOID the old agreement (keeping the record) and create a new one. **Open question Antonio raised, NOT decided:** whether the staff tool is still needed at all now that clients self-generate — `action_log` shows staff `create` last used 2026-07-07 (174 in 120 days), quiet since portal self-service took over. Retiring it needs a dependency check first (remediation cases, clients who cannot log in, MMLLC paths the portal does not cover).)_
_Last verified against code: 2026-07-22 — Claude (**The portal self-service OA was filed as `draft` while the SAME request chatted the signing link to every member — a status the rest of the system read as "never sent".** `/portal/sign` hides `draft` and `voided`; the home Action Items exclude `draft`. So a client was sent a link to a document that was invisible everywhere in their own portal until they happened to click it, which flips it to `viewed` (`app/api/operating-agreement/[token]/fetch/route.ts`). `app/api/portal/operating-agreement/create/route.ts` now inserts `status: 'sent'`. **Do NOT unify this with the MCP `oa_create` writer** (`lib/mcp/tools/oa.ts`) — there `draft` correctly means "staff is drafting, not yet sent", and the review-then-`oa_send` workflow depends on it. **Paired change, or this breaks sending:** `oa_send`'s idempotency probe matched ANY past `email_tracking` row by recipient + `%Operating Agreement%<company>%` with no time bound and no OA linkage. Since re-generating deletes the old OA row and inserts a new one, a prior email for the same company is the NORMAL case — with OAs now born `'sent'`, the probe would have suppressed the send and left the client waiting for a link that never went out. The probe is now bounded by `.gte('created_at', oa.created_at)`. **Also here — client-data-loss guard:** re-generating hard-deletes the prior OA AND every `oa_signatures` row. The guard was `status === 'signed'` alone, which is wrong for a multi-member LLC: the OA stays `partially_signed` until the LAST member signs, so a re-generate at 2-of-3 signed destroyed two executed signatures with no soft-delete and no audit row (R100). The rule is now the pure, unit-tested `hasCollectedSignatures` (`lib/portal/oa-regenerate-guard.ts`), which blocks on `signed`, `partially_signed`, OR `signed_count > 0` — three checks rather than one, because status can lag a signature write and `signed_count` can be null on older rows. No client was exposed at the time (zero part-signed multi-owner OAs on production), but making the nav entry always visible drives more traffic through this path. Found by the Council bug-hunter + project-director.)_
_Prior: 2026-07-07b — Claude (MMLLC entity-type normalization fix + portal self-service OA + Intercompany Agreement wiring — see the OA/ICA sections below; branch `claude/optimistic-dhawan-f7595e`)_
_Prior: 2026-07-07 — Claude (Drive duplicate-upload sweep, LT Program incident class — the lease-signed and oa-signed webhooks' signed-PDF uploads (+ both lease-regen routes) switched from `uploadBinaryToDrive` to `uploadBinaryToDriveUpsert` (stable file name → a retry/re-run refreshes the ONE existing Drive file in place instead of adding a copy). No other behavior in this subsystem touched; full sweep rationale in `documents.md` (2026-07-07b). Branch `claude/objective-cohen-b75f61`.)_
_Prior: 2026-06-24 — Claude (oa-signed webhook: corrected stale v2 stage name "Post-Formation" → "Articles Received" in the post-formation milestone breadcrumb; prior full read 2026-05-29 of lib/mcp/tools/lease.ts, lib/mcp/tools/oa.ts)_

## What it is
Two client documents TD generates, sends, and tracks to signature:
1. **Lease** — the registered business-address / virtual-office lease TD provides to the client (annual, per `contract_year`).
2. **Operating Agreement (OA)** — the LLC's operating agreement (SMLLC or MMLLC), generated from CRM account data.

Both follow the same pattern: create a record → send a tokenized link via Gmail (with open tracking, through `safeSend`) → client views/signs → a signed webhook records it.

## Lease
- Tools (`lib/mcp/tools/lease.ts`): `lease_create`, `lease_get`, `lease_list`, `lease_update`, `lease_send`, `lease_agreements`.
- **Status lifecycle:** `draft → sent → viewed → signed → active → expired`.
- `lease_send` **approves + sends immediately** (not a draft) via Gmail with open tracking, sets status `sent`, requires `tenant_email`. Use `gmail_track_status` to confirm the client opened it. Uses `safeSend` (R037).
- Link: `${APP_BASE_URL}/lease/{token}/{access_code}`. Signed webhook: `app/api/lease-signed/route.ts`. PDF (re)generation: `app/api/lease-regen-drive`, `app/api/crm/admin-actions/regen-lease-pdf`.
- `leases` columns: `token`, `access_code`, `tenant_company`, `suite_number`, `status`, `contract_year`, `term_start_date`/`term_end_date`, `monthly_rent`/`yearly_rent`, `view_count`, `signed_at`, `tenant_email`.

## Operating Agreement (OA)
- Tools (`lib/mcp/tools/oa.ts`): `oa_create` (from CRM account data; SMLLC or MMLLC), `oa_get`, `oa_send` (Gmail + tracking, `safeSend`), `oa_agreements`, `oa_id`, `oa_signatures`.
- **Only supported states** — `OA_SUPPORTED_STATES` (`lib/types/oa-templates.ts`) gates which states have an OA template.
- Tokenized link + access code. Signed webhook: `app/api/oa-signed/route.ts`.
- **Three creation paths** (all insert `oa_agreements`):
  1. MCP `oa_create` (staff, free-form members params for MMLLC).
  2. CRM button (`app/api/crm/admin-actions/generate-document/route.ts::generateOA`) — MMLLC members come from the **`members` table** (real `ownership_pct`, company members included; individual address falls back to `contacts.residency`). Errors if an MMLLC has no members rows or ownership ≠ 100%. Never fabricates an even split (Datavora incident, 2026-05-25).
  3. Portal self-service (`app/api/portal/operating-agreement/create/route.ts`) — client "Create & Send for Signing" in `/portal/documents/generate`. MMLLC: reads `members` table, ownership must total 100%, creates one `oa_signatures` row **per SIGNER** (not per member — see the signing-set rule below) and portal-chats each a per-signer link.

### ⚠️ Membership ≠ signing — the signing set (2026-08-09, Antonio; amended later the same day)
`lib/members/signing-set.ts::resolveSigningSet` is the ONE place that decides who signs, and `describeSigningBlock()` is the ONE place that decides whether the agreement may be issued at all. Both writers of `oa_signatures` — the portal create route and the welcome-package job (`lib/jobs/handlers/welcome-package-setup.ts`) — call both, so they cannot drift.

- **Membership is a legal fact; portal access is not.** Every member stays in the OA roster with their ownership, whether or not they can ever log in.
- **Individual with an email → signer.** **Individual with NO email → member, counted in ownership, named in the agreement, NOT in the signing set, no portal.**
- **Company member → member AND signer, signing through its representative.** Keyed on `members.contact_id` FIRST, `representative_name`/`representative_email` only as fallback. Live reason: Advertising Apex LLC (99% of Azarexa) has blank representative text but its `contact_id` points at Umberto Moretti — the system does know who signs. A company with NEITHER a contact NOR a representative email is a member but not a signer.
- **🔒 AN AGREEMENT IS NEVER ISSUED WITH FEWER SIGNERS THAN MEMBERS (2026-08-09, Antonio — amends the same day's earlier rule).** Verbatim: *"A multi-member operating agreement signed by only one owner must never exist. The client generates it, not us, but it is a legal document and it must contain all members."* So a non-signer is a **HARD STOP**, not something to route around: `describeSigningBlock()` names the blocking member, the portal route 422s with a client-safe message on the Generate Documents screen, and the welcome-package job records an `error` step **and** calls `reportSystemError` (no screen there, so the reason has to reach a human by itself). It **can** leave a client stuck until someone supplies the missing email — Antonio: that is the correct failure for a legal document, provided the reason is visible and reaches a human. Membership itself is unchanged: the blocked member still belongs in the roster and in the ownership.
- **`total_signers` counts the SIGNING SET — which now always EQUALS the roster for an MMLLC**, because the gate above refuses to issue otherwise. (History: counting the roster while filtering the signers left `signed_count` permanently short whenever someone could not be sent a request, and the agreement sat "in progress" forever. Counting only the signers fixed that but created the blocker below.)
- **🔒 Per-member signing is decided by the EXISTENCE of `oa_signatures` rows, NEVER by `total_signers > 1`.** *Regression caught 2026-08-09, live on production, never fired.* Under the brief "refuse only when nobody can sign" rule a two-member LLC could reach exactly ONE expected signature, and every per-member gate keys off "more than one signer" — they all switched off together: `app/portal/sign/oa/page.tsx` emitted no `?signer=` code, `app/operating-agreement/[token]/[code]/page.tsx` rendered the signature pad for **anyone** holding the shared access code (skipping "each member must use their personal signing link"), and `app/api/operating-agreement/[token]/sign/route.ts` ignored the submitted signer code entirely and recorded `signed_by_name` from `oa_agreements.member_name` — the FIRST roster row — instead of the signature row. Live exposure: **Full Throttle Media LLC** (two owners, Jayesh Hans has no email on the member row *or* his contact; both member rows created in the same millisecond, so which name got printed was a tie-break). It had no agreement, so nothing was executed wrongly. Nebroo LLC matched the shape but is Closed. All four sites now key on row existence; the sign route also prefers the signature row's own name over the agreement header. **Safe against existing data** (checked on production): SMLLC agreements have no signature rows, and the only three one-signature agreements carrying rows are single-member and already signed.
- **One person can hold TWO capacities and it is NOT a duplicate.** Umberto Moretti is Azarexa's 1% individual member AND the contact behind its 99% corporate member, so he signs twice. `resolveSigningSet` maps rows 1:1 and never dedupes by contact. `app/portal/sign/oa/page.tsx` previously fetched his signature row with `.maybeSingle()`, which ERRORS on two rows — he was handed no signing link at all, silently, while legally required to sign. It now orders his rows by `member_index` and hands him the first UNSIGNED one.
- **Signature line copy:** `signerDisplayName()` renders "Rep Name (for Company LLC)" — and suppresses the suffix when the two are equal, so a company with no representative name does not print "Apex LLC (for Apex LLC)" on the executed agreement.

### ⚠️ Entity-type normalization — MANDATORY invariant
`accounts.entity_type` stores **long form** ("Single Member LLC" / "Multi Member LLC" / "C-Corp Elected") — the value 'MMLLC' NEVER appears in accounts. `oa_agreements.entity_type` stores the **short code** ('SMLLC'/'MMLLC'). ANY comparison of an entity type string against 'MMLLC' MUST go through `normalizeEntityType()` (`lib/portal/entity-type.ts`). **Precedent (2026-07-07, Umberto Moretti / Azarexa):** the portal create route compared raw `accounts.entity_type === 'MMLLC'` — false for every account — so every portal-generated OA was built single-member with the primary contact at 100%, ignoring the members table; Datavora LLC signed a legally wrong OA. Fixed in: portal create route, `app/operating-agreement/[token]/[code]/page.tsx`, `app/portal/sign/page.tsx`, `app/portal/sign/oa/page.tsx`, `generate-documents-client.tsx`.

## Intercompany Transfer Agreement (ICA)
- Generator: `lib/pdf/intercompany-agreement-pdf.ts` (operating LLC ↔ treasury/holding member company). CRM wiring: `lib/operations/intercompany.ts` — `assembleIntercompanyInput()` (pure, unit-tested) + `generateIntercompanyForAccount()`.
- **All data from CRM**: operating company from `accounts`; treasury from the `members` row with `member_type='company'` (ownership_pct, address, EIN) with fallback to the treasury company's own `accounts` row matched by name (EIN / address / state_of_formation). Missing data → hard error naming the field, never a default.
- Entry points: CRM account page → Documents to Sign panel → "Intercompany Agreement" row (only shown for MMLLC accounts) → `generate_intercompany` action in `generate-document/route.ts`. Output: PDF filed to the account Drive folder + portal-visible `documents` row.

## Business rules
- **R037** — `lease_send` / `oa_send` go through `safeSend()` (idempotency check → send → status update after).
- **R012 / R005** — client links use `APP_BASE_URL` (`app.tonydurante.us`), never the internal domain.
- **R035** — test a lease/OA via `?preview=td` before sending.
- **OA only for supported states** — an unsupported state has no template; `oa_create` will reject/skip.

## How it's built — key files & tables
- Files: `lib/mcp/tools/lease.ts`, `lib/mcp/tools/oa.ts`, `lib/types/oa-templates.ts` (`OA_SUPPORTED_STATES`), `app/api/lease-signed/route.ts`, `app/api/oa-signed/route.ts`, `app/api/lease-regen-drive/route.ts`, `app/lease`, `lib/mcp/safe-send.ts`, PDF generation in `lib/pdf/`.
- Tables: `leases`, the OA/operating-agreement table, signature/signature-request records, `accounts`, `contacts`.

## OA public pages — how data reaches the browser (2026-07-22)

**The two public OA pages MUST NOT read `oa_agreements` / `oa_signatures` with the anon key.**
They go through `GET /api/operating-agreement/[token]/fetch`, which holds the service key,
verifies the access code SERVER-SIDE, and returns the whitelist in `lib/oa/public-view.ts`.

Why (dev job 023c7d06): both pages used to `select('*')` with the anon key and compare the
access code in the BROWSER — i.e. after the row had already been delivered. Policies were
`USING (true)` for role `public` and `anon` held SELECT on both tables. Tokens are
`${companySlug}-oa-${year}`, derivable from a company name that is public in state
registries. One unauthenticated PostgREST request therefore returned, across ALL agreements:
`access_code`, `ein_number`, `member_email`, `member_address`, the members blob — and every
co-signer's personal signing code, which is the credential that authorises signing AS that
member. Reproduced and then verified closed on an isolated local stack seeded with
production's exact policy set (`scripts/migrations/20260722-0100-oa-close-public-read.sql`).

Rules that follow:
- **Never widen `toPublicAgreement` / `toPublicSignature` by spreading a row.** `assertNoSecrets`
  throws on `access_code` / `member_email` / `email` / `account_id` / `contact_id`, including
  inside nested JSONB — the `members` blob carried member emails and was caught only by
  inspecting the route's real output, not by reading the mapper.
- **`tests/unit/anon-grant-contract.test.ts` is the gate.** It derives the browser's anon
  privilege needs from the code. If it fails, the code's needs changed — reconcile it
  deliberately, and never revoke a grant on the strength of a grep.
- **Admin preview cannot sign.** `canSign` excludes `isAdmin`, and the route requires a real
  staff session via `isStaffPreview` (2026-07-21 incident) — the query flag alone proves nothing.
- **New public/token-gated routes need a `middleware.ts` PUBLIC_PREFIXES entry** or they 307 to
  the staff login. This one did, and only a real request surfaced it.

**STILL OPEN — `anon` retains UPDATE on both tables.** The signing page still writes its
signature row, the counter and the final status straight from the browser, so an attacker who
guesses a token can corrupt an agreement. Closing it means moving those writes server-side
first; the read fix does NOT close it. Separately open: the signing-integrity defects
(browser-side finalization, no server reconciliation) — see the council review on that job.

## OA draft download & sign-by-hand (2026-07-23)
A client can complete an Operating Agreement three ways from the signing page (all three render **inside the portal iframe** too — the download used to be hidden whenever `?portal=true` was set, i.e. on every route a client actually uses):
1. **Sign online** — the existing e-sign flow.
2. **Download the draft** — `GET /api/operating-agreement/[token]/pdf?code=<code>`. Renders the agreement ON DEMAND (nothing stored) and is **DRAFT ONLY, never executed**. That single rule dissolves the whole class of review blockers: it reads no signature images (so it never touches the anon-writable `signed-oa` bucket with the service key), it is not a second producer of the signed instrument, and `generateOperatingAgreementPDF({ draft: true })` stamps every page + rewrites the preamble and the IN WITNESS WHEREOF recital to their **unexecuted** form so the copy cannot be passed off as signed. Gates (all AFTER the code check): voided → 410, signed → 409, plus a per-request rate limit.
3. **"I signed it by hand"** — `POST /api/operating-agreement/[token]/hand-signed` (multipart; optional `file`). Marks the agreement `signed` with **`signature_method='by_hand'`** and leaves `pdf_storage_path` NULL (TD holds no executed instrument unless the client uploads a scan). Files the unsigned draft named "(Unsigned Copy — client signed by hand)"; when a scan is uploaded THAT is filed as the signed copy. Emails support either "scan received" or "NO SIGNED COPY ON FILE — chase it". Filing is best-effort and escalates via `reportSystemError`; the client's confirmation is never undone by a Drive hiccup.

**`signature_method`** (`electronic` | `by_hand` | NULL) distinguishes an agreement TD holds a real signature for from one the client only *declared*. NULL = signed before the distinction existed (74 legacy rows, deliberately NOT backfilled). Migration `20260723-1200-oa-signature-method.sql`.

**Design note for the next session:** the on-screen SIGNED render still uses the browser html2pdf capture (pre-existing), so the executed PDF and this server draft are still two renderers — the draft download deliberately sidesteps that by refusing signed agreements rather than re-rendering them. Retiring the browser capture (have the sign POST call `generateOperatingAgreementPDF` server-side) is the follow-up that makes it one renderer; see `lib/operations/esign.ts` `flattenEnvelopeToSignedPdf` for the pattern.

## Gotchas, invariants & past bugs
- **`lease_send` sends a real email immediately** (not a Gmail draft) — don't call it to "preview." Use `?preview=td` for review.
- **OA generation is state-gated** — if the company's state isn't in `OA_SUPPORTED_STATES`, there's no template; add the template before generating.
- **Use `safeSend`** for any new lease/OA send path (R037) — mark "sent" only AFTER the send succeeds, with idempotency first.
- Lease is annual (`contract_year`) — a new year is a new lease record, not an edit of the old one.

## How to verify current state
- Read `lib/mcp/tools/lease.ts` + `lib/mcp/tools/oa.ts` (tool contracts + statuses), `app/api/lease-signed/route.ts` / `oa-signed/route.ts` (the sign events), `lib/types/oa-templates.ts` (`OA_SUPPORTED_STATES`).
- A client's lease/OA: `SELECT token, status, contract_year, signed_at FROM leases WHERE account_id='<id>';`
- Note (R096): sandbox via sandbox MCP / `psql`; production `execute_sql` hits production.
