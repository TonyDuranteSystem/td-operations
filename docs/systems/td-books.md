# TD Books (My Finances — the owner's company books)
_Last verified against code: 2026-08-30f — Claude (**Statement filenames now carry ACCOUNT IDENTITY, and a file that doesn't say who it belongs to is REFUSED instead of guessed. Also: the 2025 rebuild was reset to zero and the off-limits year restored.** Antonio: *"if the rules will be that the file name must contain institution name, the type of account checking/credit card/saving/loan, acc number in order the transactions to be treated properly, do it. if a file is missing a proper wording the system has to give a message to rename it."* WHY IT MATTERS: the account TYPE decides the accounting (a credit card and a loan are LIABILITIES — card charges are the expense but paying the card from a bank is a TRANSFER, else every purchase counts twice; only loan INTEREST is an expense, not the principal; card balances must never be added into cash), and the account NUMBER decides IDENTITY (three First Citizens accounts — checking 5812, checking 5820, loan 7363 — pass the same $1,068.30 between them). NEW `lib/owner-statement-filename.ts` reads institution + type + number from the filename and returns a refusal with an exact rename instruction otherwise; the upload route now REJECTS an unnamed file (400, `needs_rename`) and writes the parsed label into `bank_name` and the type into `account_type`, both AUTHORITATIVE over the parser's guess (the parser labels everything except Relay "unknown" — measured across all 23 of Antonio's files: 12 Relay recognised, 11 not). `account_type` was free to repurpose — verified NULL on all 78 existing rows and read by nothing in the owner books; the parser had been putting the CURRENCY there, and currency has its own column. YEAR GUARD: the upload now takes the year being loaded and SKIPS + reports rows outside it — a loan export carrying 2026 activity previously leaked 17 rows into the year Antonio declared off limits (2026 went 78→95). CLEANUP DONE: those 17 rows plus 1 stray 2024 row deleted, 2026 verified back at exactly 78, and the 1,806 "unknown"-labelled 2025 rows purged — 2024=0, 2025=0. THREE BUGS THIS MODULE HIT, ALL FOUND BY RUNNING IT ON REAL FILENAMES RATHER THAN IMAGINED ONES: (1) `_` is a JS word character, so `/\bchecking\b/` did NOT match "Firstcitizenbank_checking_acc" and files were refused for missing a type they plainly stated — underscores are normalised to spaces before word matching; (2) first-match number detection stopped on the "Card 2025" in "Amex_Credit_Card_2025_card#51007" and called it ambiguous, never reaching the real "card#51007" — every marked candidate is now collected and years/over-long runs skipped; (3) a test asserting the RECOMMENDED convention ("Chase_checking_3920.csv") failed because the number was neither marked nor fused to a word — a standalone-digit-run fallback was added. Also note this project compiles with `strict: false` and no TS target, so the result type is one interface with optional `value`/`error` rather than a discriminated union (which does not narrow), and `matchAll` iteration is replaced by an `exec` loop. Tests: `tests/unit/owner-statement-filename.test.ts` (13, weighted to refusals). CURRENT STATE OF ANTONIO'S FOLDER (23 files): 5 pass the naming rule — Amex credit card 51007, Chase credit card 6094, Firstcitizenbank loan 7363, Firstcitizenbank checking 5820, firstcitizenbank checking 5812. 18 are refused: 12 Relay + Chase 3920 + Amex 9245 (no type), "Credi_card" Chase 9279 (typo, deliberately NOT fuzzy-matched), Mercury (`acc#45172025` — account 4517 with the year fused on, ambiguous), Stripe and Airwallex (no type or number). METHOD CHANGE (Antonio, 2026-08-30): statements are loaded ONE AT A TIME — inspect what the account actually contains, build that account's categories and rules deliberately, prove it, then the next — rather than bulk-importing and categorising afterwards.)_
_Prior: 2026-08-30e — Claude (**Five foundation defects fixed before any real 2025 statement is loaded — found by a bug-hunter pass Antonio ordered ("we are building the foundation and everything must work"), each verified line-by-line before acting.** (1) **THE UN-RANGED LEDGER QUERY — certain to fire.** `getOwnerTransactions` had no `.range()` and feeds `getOwnerPnL`, and through it the P&L tab, dashboard KPIs, Tax tab and cash-flow chart. PostgREST silently caps an un-ranged select at 1000 rows with NO error; a real year of statements clears that easily, so every headline figure would have been computed from only the newest 1000 rows while the Transactions tab — correctly ranged, `count:'exact'` — showed the true total. Two surfaces disagreeing, nothing flagged. Galling detail: the SAME FILE documents this exact trap and pages around it for `getInvoiceIncome`; the fix was applied to one query and never the other. Now paged with a `(transaction_date, id)` order and a short-page terminator. (2) **`getOwnerTransactionsPaginated` had no tie-break** — date alone has ties on any real statement and Postgres gives no stable order across separate queries, so some rows appeared on two pages and an equal number were NEVER shown, staying uncategorized forever while "Showing 51–100 of N" claimed otherwise. Now orders by `id` as well. (3) **SAME-MONEY DUPLICATES — the root defect.** The books have ONE identity (`transaction_ref`) produced by THREE unrelated generators: the feed sweep writes `feed:<uuid>`, the CSV parsers write a bank reference or a content hash over their own field set, the PDF/AI path hashes a different field set again. Those namespaces can never collide, so ref-only dedup could not see that the feed's copy and the statement's copy of one payment were one payment — it booked both under a green success toast. Same for one statement uploaded as PDF and as CSV, and for a Relay export with vs without a Balance column (`balanceRaw` flips, hash changes). NEW pure `partitionAgainstExisting()` does **MULTISET** content matching on `(date, amount·2dp, normalised bank, currency)` — deliberately NOT a plain "a match exists" test, because two separate $971 Stripe payouts on the same day are REAL: N existing + M incoming leaves max(0, M−N) inserted. Erring toward a visible duplicate over a silent drop is the deliberate choice — a duplicate can be seen and deleted, a missing transaction cannot. An exact-ref re-upload also consumes its existing row so that row cannot ALSO absorb a real twin. The pre-check query is itself paged, since a duplicate check that stops looking after 1000 rows waves through exactly the duplicates it exists to catch. Reported split by MEANING: `skipped_same_source` (you re-uploaded this file) vs `skipped_already_booked` (this money is already in your books from elsewhere) + up to 5 examples. **SCOPING, corrected against the hunter's framing:** 2025 currently has 0 rows so this does not fire on the immediate 2025 load; it fires on 2026 (78 live feed rows) and on any later feed backfill or re-export. (4) **PARSE WARNINGS WERE THROWN AWAY.** The route returned `warnings` from `parsed.errors`; the upload screen read only `error`/`imported`/`parsed_count` and never referenced them. That silently discarded *"Ambiguous date format — assumed M/D/Y (US). Verify before filing."* — raised when a D/M/Y file has no day > 12 in its first 200 rows. Guess wrong on a European statement (Antonio has EU accounts) and every date transposes; near a year boundary rows land in the WRONG TAX YEAR, since `tax_year` is derived from that same date. Now surfaced as persistent 15s warning toasts, separate from the success toast. (5) **`balance_after` was parsed and then dropped at the door** — the parsers always produced it, but the upload's row map omitted it and `OwnerImportRow` had no such field, so `getCashPosition` (which requires it NOT NULL) could only ever see bank-feed rows no matter how many statements were loaded. Now carried end-to-end; column existence re-verified live in sandbox before trusting the write. Tests: `tests/unit/owner-import-dedupe.test.ts` (15, weighted to the MUST-NOT-DROP cases) + `tests/unit/owner-books-foundation.test.ts` (12). **The hunter attacked and could NOT break:** `isOwnerOnly` on the page guard and all 12 owner API routes (count verified independently); the year-switch fix from 2026-08-30c; "apply to all" (server-scoped to `target.tax_year`, re-asserts `only_uncategorized`, TOCTOU genuinely closed); bulk chunking with honest partial counts; `transaction_ref` NOT NULL + non-blank CHECK so no NULL-dedup escape; income double-count guards on all three apply surfaces; no cross-currency summing anywhere; `NON_PNL_CATEGORIES` keeping transfers/conversions/refunds/equity out of the P&L. **STILL OPEN from the same pass, NOT fixed here:** `by_subcategory` includes income/cogs/fee/uncategorized absolute-valued so the largest INCOME renders as the largest expense category (`owner-finance.ts:405-407`, verified); raw search interpolation into a PostgREST `or()` so a comma in "STRIPE, INC" re-parses the filter (`:216`, verified); `Math.abs` on expense/cogs/distribution so a money-IN row mis-categorised as an expense INCREASES expenses; Wise filename beating the data for currency; selection surviving a filter/search change so the bulk bar can write to off-screen rows; bulk/PATCH routes carrying no `tax_year` guard (defence-in-depth); Cash Position/Runway being all-time but rendered under a year header.)_
_Prior: 2026-08-30d — Claude (**The Bookkeeper tab is now year-scoped — the last inert control on `/owner` closed.** Antonio: *"fix it first. we are building the foundation and everything must work. it's better to build the right foundation now instead of chasing issues later."* `bookkeeper-tab.tsx` took the year prop as `year: _year` and never used it, fetching `/api/owner/bookkeeper` unfiltered — so the page's year selector sat on that tab doing nothing. Same defect class as the "My Finances" menu entry shown to people the page then turned away (2026-08-30b): a control that looks like it acts and doesn't. FIX: the route accepts an optional `year` and applies `.eq('tax_year', year)`; it validates with `/^\d{4}$/` and treats anything else as NO filter — an unparseable year becoming `NaN`/`0` would filter to nothing and render as "no reviews", which reads as data loss rather than a bad input. The tab now keys its fetch effect on `year` and, on a change, ALSO clears the selected review and its loaded items: items are fetched by review id, so a review left open from the previous year would keep showing that year's questions under the new year's heading. The empty state now names the year ("No review sessions for 2025 — use the year selector above to check another year") because, once the list is year-scoped, an unqualified empty panel reads as broken instead of as "try another year". Tests: `tests/unit/owner-bookkeeper-year-filter.test.ts` (7). **This completes the year-consistency sweep of `/owner`:** Transactions and P&L re-fetch/reset on a year change (2026-08-30c), Overview/Cash Flow/Tax render straight from props and always did, and Bookkeeper now filters. Every tab honours the selector.)_
_Prior: 2026-08-30c — Claude (**INVARIANT — every `/owner` tab that holds fetched rows in state MUST re-fetch on a `year` change. The page navigates; the tabs do NOT remount.** Reported by Antonio and reproduced live in sandbox: on the Transactions tab, switching the year dropdown to 2025 left ALL 78 rows of 2026 data on screen while the header and the URL both read 2025. Mechanism: the year `<select>` does a `router.push` (`owner-dashboard.tsx`), which re-renders the force-dynamic server component and delivers fresh props — but React does NOT re-initialise `useState(initialRows)` on a preserved component instance, and `transactions-tab.tsx` had no effect watching `year`. Its `load()` already put `year` in the query string and in its deps; nothing ever CALLED it on a year change. A direct URL worked only because that is a fresh mount. **Why this was urgent, not cosmetic:** the modal, the bulk bar and "apply to all like this" all act on the rows currently in state, so categorizing from a stale list writes to the OTHER year's records while the page says otherwise — violating Antonio's binding rule that 2025 and 2026 stay separate, right before his real 2025 statements land. FIXES: (1) `transactions-tab.tsx` gains an effect on `year` that calls `load(0)` and clears selection, open modal and offset — **deliberately `load()`, not a re-seed from `initialRows`**, because the server always fetches `category='uncategorized'` while the tab owns the live filter, so re-seeding would silently drop the operator's filter and show an uncategorized-only list under a filter chip claiming otherwise; a ref skips the first run so mount does not duplicate the server fetch. (2) `pnl-tab.tsx` resets `compare`/`priorPnl` on a year change — `priorPnl` is fetched against the year at click time and the toggle relabels itself from the new `year`, so a 2025 comparison would sit under a button reading "Hide 2024 comparison": wrong year, wrong label, on money. **Verified clean and deliberately untouched:** `dashboard-tab.tsx`, `cashflow-tab.tsx`, `tax-tab.tsx` contain ZERO `useState`/`useEffect`/`fetch` — they render straight from props and follow the year correctly; a test now asserts that, so if one ever gains client state it inherits this bug and trips the tripwire. **OPEN, Antonio's call:** `bookkeeper-tab.tsx` destructures the prop as `year: _year` and never uses it — it fetches `/api/owner/bookkeeper` unfiltered and labels each row from that review's own `tax_year`, so the year selector is INERT on that tab (a dead control, not a wrong-year display). Either filter it or hide the selector there. Tests: `tests/unit/owner-year-switch.test.ts` (8).)_
_Prior: 2026-08-30b — Claude (**Two divergences closed: a nav entry stricter than its page, and expense READS looser than expense WRITES.** Found because Antonio clicked "My Finances" in sandbox and silently landed back on Home. Root cause was NOT the page guard — that worked. It was that the 2026-08-29 hardening moved the ACCESS predicate to `isOwnerOnly` without moving the VISIBILITY predicate: `components/dashboard/sidebar.tsx` still marked the item `adminOnly`, so every admin saw a link that `app/(dashboard)/owner/page.tsx` answers with `redirect('/')` — a silent dead link with no explanation. He was signed in as `admin@sandbox.tonydurante.us` (read live from the page), and the deployed sandbox build was confirmed to contain the guard (`git merge-base --is-ancestor 164e048e daa26753`), so the guard was behaving correctly and the MENU was the defect. Fixes: (1) new `ownerOnly` flag on the sidebar's NavItem + filter `if (item.ownerOnly && !isOwner) return false`; the `isOwner` value was ALREADY plumbed in from `layout.tsx` via `isProtectedAdminEmail` for the MFA dialog — no new prop was needed. (2) The Finance **Expenses** tab had the same split in a worse shape: the 5 write actions went owner-only on 2026-08-29 but the READ stayed `isAdmin`, so a non-owner admin could see Antonio's vendor bills and only discovered the limit as a raw "Forbidden" on edit. `app/(dashboard)/finance/page.tsx` now derives `userIsOwner = isOwnerOnly(user)` and withholds the rows **server-side** (`const tdExpenses = userIsOwner ? … : []`) — hiding the tab alone still ships the bills to the browser; the tab is `ownerOnly`; and the panel re-checks `isOwner` at render because `tab` seeds from the URL, so `?tab=expenses` cannot draw it. **Deliberately NOT tightened:** the card-fee master switch stays admin-level — this is about the owner's own money, not about demoting every admin capability on the Finance page (asserted by a test so a later edit can't quietly widen the change). **The general rule this establishes:** wherever a destination guards with `isOwnerOnly`, its menu entry/tab must be `ownerOnly` too — a visibility predicate looser than the access predicate is always a silent dead link. Tests: `tests/unit/owner-only-surfaces.test.ts` (9 cases; they read the real source, so loosening either side fails).)_
_Prior: 2026-08-30 — Claude (**Statement upload added to the Transactions tab.** Antonio's own instruction: reuse the client tax-financials engine's statement reader as-is, don't write a new parser. New route `app/api/owner/transactions/upload/route.ts` takes ONE file per request (CSV or PDF, any bank — deliberately not batched, to stay under the platform's request-size limit and give real per-file feedback), calls `parseBankStatement` (`lib/bank-statement-parser.ts`, the same pure function the client portal uses), maps its output to this table's row shape, and inserts via the SAME insert-once upsert semantics as everything else here. `tax_year` is derived from each transaction's own date, never assumed from the upload date. Nothing is categorized — every row lands `uncategorized`, same rule as always. The row-validate-and-insert logic is now shared (`lib/owner-transactions-import.ts`) between this route and the existing JSON import route, so they can't drift apart. Live-verified in sandbox: a real gathered statement produced the exact expected row count with real content, and re-uploading the same file produced zero duplicates.)_
_Prior: 2026-08-29 — Claude (**Access gate hardened: `/owner` + `td_expenses` writes now `isOwnerOnly`, not `isAdmin`.** `isAdmin` trusted a self-writable profile field (any staff account could grant itself access by editing its own metadata) — see `auth-oauth.md`'s 2026-08-29 entry for the full mechanism and why `isSecureAdmin` alone wasn't strict enough either (a real account, `qa-staff@`, legitimately holds admin role for other testing). Separately, and more seriously: the 5 `td_expenses` write actions (create/mark-paid/void/update/delete — the supplier-bills feature referenced below) had NO access check of any kind before this — the Finance page hid the tab from non-admins, but the actions themselves were reachable directly. Both now gate on the new `isOwnerOnly()`.)_
_Prior: 2026-07-29d — Claude (**QuickBooks-style "apply to all like this" + saved vendor rules — council-reviewed (light: senior-engineer + bug-hunter + project-director), every cited finding fixed.** (1) NEW `lib/owner-vendor-match.ts` (pure, CLIENT-SAFE — no DB imports; the tab needs it at render time): `normalizeVendorKey` (accent-folding — 'CAFFÈ'→'caffe', never a truncated false-grouping key), `isSimilarVendor` (WHOLE-TOKEN containment, ≥4-char floor — raw substring was a trap: "chase" is inside "purchase" and POS-PURCHASE wording is everywhere), `applyVendorRulesTo` (most-SPECIFIC rule wins by pattern length — alphabetical first-match let a broad "stripe" rule permanently shadow "stripe fee"; empty normalized pattern matches NOTHING, not everything). (2) NEW `GET /api/owner/transactions/similar`: same-year uncategorized look-alikes; returns ids + a PREVIEW of the matched rows (the bug-hunter's core theme: every apply surface must show the human what one click will label, never just a count) + `mixed_signs` (money-in AND money-out in one set ⇒ amber warning) + `suggested_pattern` (SHORTEST normalized key of the set — a rule saved from the long Mercury wording would never fire on the short Relay wording) + `truncated` honesty flag. (3) Modal: "N more look like this — apply to all", scrollable preview list, "Always do this" rule checkbox (disabled without subcategory; un-checks if subcategory cleared — silently dropping a checked rule was a lie); apply-to-all posts `only_uncategorized: true` and the bulk route CHUNKS ids (200/req — URL-length blowout at first-cleanup scale) + guards `.eq(category,'uncategorized')` on that path (TOCTOU: phone+desktop id-set staleness must not overwrite fresh labels) + returns the REAL updated count, toasted honestly when lower. (4) The income double-count guard now covers EVERY apply surface: modal single, modal apply-to-all (`has_positive_feed_rows`), AND the chip path (`window.confirm` before chip-accepting income on a `feed:` deposit — the chip was a warning-free side door). Chip-accept also carries the rule's `is_related_party` (was silently dropped; tax-relevant for S-corp related-party tracking). (5) `POST /api/owner/vendor-rules` validates category (`isOwnerCategory`) + match_type allowlist + non-empty normalized pattern, and UPDATES an existing same-(pattern,match_type) rule instead of inserting a duplicate (two same-pattern rules = undefined winner forever). NOTHING auto-books anywhere — rules only render click-gated suggestion chips (verified: no server-side apply call site). Live-verified on sandbox: "STRIPE - TRANSFER" finds 45 look-alikes across 3 wordings under token matching; Chase↛Amex. **DEFERRED (flagged, not built):** rules-management UI (delete/edit is API-only — a regretted rule needs the API), bulk-undo toast. **PROD SHIP GATE:** `owner_vendor_rules` does NOT exist on production (the May migration `20260504-1001` was never promoted — same gap as `bookkeeper_review_items`); Antonio must run its rules-table block in the Supabase editor BEFORE this code ships, else rule saves 500 (apply-to-all works regardless; suggestions degrade silently).)_
_Prior: 2026-07-29c — Claude (**Council pass on Phase 1b — every cited finding fixed before ship.** (1) The income fetch now PAGES the payments ledger (`INCOME_FETCH_PAGE` loop, ordered by id) — an un-ranged supabase select silently caps at 1000 rows with NO error, which would have quietly understated income the day the ledger outgrew it (275 qualifying rows today; the failure was invisible-by-design). (2) `getCashPosition` is per-currency: balances keyed (bank, currency), `totals` per currency — the Cash card no longer $-labels a EUR+USD sum (the one place this PR's own no-mixed-currency rule had been left unapplied). (3) Income anomalies are YEAR-SCOPED (a 2025 refund no longer haunts every later year's banner; dateless anomalies still show everywhere — safe side). (4) Part-paid invoices are FLAGGED (`partial_count`/`partial_attribution_count`): cumulative cash pinned to one date can misplace installment months — surfaced, not silent. (5) Uncategorized cash now reaches the MONTHLY series too, so the chart's nets sum exactly to the Net Profit KPI. (6) Wire double-count guard: choosing Income for a `feed:` row (modal AND bulk bar) shows a red warning to use "This is for a client →" instead — invoice money is already counted from the ledger. (7) Routes validate category server-side (400 with the bad value, not a raw CHECK violation) and the CSV import normalizes/validates currency (a "eur" row would have forked a separate block beside EUR; a non-ISO string crashed the formatter). (8) All-zero currency blocks are dropped; compare mode renders prior-only currencies; the route's dead variance payload is deleted; Net KPI says "provisional — categorize transactions" while uncategorized rows exist; the P&L footnote states Stripe processing fees are NOT yet booked. Deliberately accepted: created_at UTC fallback (rare + already flagged approximated); Waived-with-cash counts as income (cash method; zero such rows today). Ship checklist note: before prod code, run the same legacy-data checks on prod (`category='distribution' AND subcategory='contribution'` rows — 0 in sandbox — and Waived/Not-Invoiced with cash — 0 in sandbox).)_
_Prior: 2026-07-29b — Claude (**Phase 1b: income now comes from the INVOICE LEDGER, Stripe is a clearing account, the flat tax estimate is DEAD, and currencies are never mixed.** (1) `computeInvoiceIncome` + `getInvoiceIncome` (`lib/owner-finance.ts`): cash-method income = `payments.amount_paid` by `paid_date` — the revenue universe is the payments ledger, NEVER bank rows. Cancelled/Refunded rows with cash are anomalies (excluded + surfaced); rows with cash but no paid date fall back issue→created date, counted + flagged approximated (never dropped, never silently precise). Verified to the cent against independent SQL on sandbox: USD 235,238.02 / EUR 54,050 for 2026. (2) NEW `transfer` category (migration `20260729-1700`, sandbox-applied; prod pending): Stripe payouts + own-account moves. `NON_PNL_CATEGORIES` = transfer/conversion/refund/distribution/contribution — none touch the P&L. Live-proven single-count: categorizing a real $1,019.25 `STRIPE - TRANSFER` books row as transfer removed it from uncategorized income and added it NOWHERE (invoice income unchanged). (3) `OwnerPnL` is now per-currency `blocks[]` (`PnLBlock`) — USD first; NOTHING is ever summed across currencies (the old P&L silently added EUR+DKK+USD). Row amounts in the transactions tab render in the row's currency. (4) `estimateQuarterlyTax` DELETED with tombstone — flat 25%+SE was sole-prop math, wrong for the 1120-S filer; the Tax tab now shows real book figures (net profit, distributions, contributions) + a plain statement that personal estimates come from the CPA. STILL OPEN for a later phase: Stripe FEES as expense (needs balance-transaction ingestion — until then profit is overstated by fee amounts, CPA's 2024 figure was $694), refund/dispute ingestion, and per-rail income attribution (payment_method is free text).)_
_Prior: 2026-07-29 — Claude (Phase 1a: dedicated `td_books_transactions` table, sandbox-applied; prod pending Antonio's DDL run)_

> **The ledger rebuild programme lives in `td-books-ledger-plan.md`.** This file describes the books as they work TODAY (a flat category list). That model is being replaced by a real double-entry ledger with a chart of accounts Antonio owns — read the plan before working on the books.

## What it is
Antonio's own company books — Tony Durante LLC's money, kept separate from the client-facing Finance section. Finance holds ONLY client invoice payments (Antonio's binding rule, 2026-07-27); everything else — Stripe payouts, transfers between TD's own accounts, bank rewards, vendor spending — flows into **My Finances** (`/owner`, admin-only). This ledger is the foundation for the S-corp books project (dev job `81fb5a05`): P&L, balance sheet, equity roll-forward matching the filed 1120-S.

## Business rules
- **Privacy:** staff who work invoices in Finance must never see the owner's business activity. `/owner` is gated by `isOwnerOnly` (email-only match against `ADMIN_EMAILS`, no app_metadata fallback — deliberately stricter than `isSecureAdmin`, since a real staff account can legitimately hold admin role for unrelated purposes without that meaning it should see this), and the Finance page filters `outgoing`/`owner_ledger`/`td_expenses` server-side for non-admins.
- **Invoice-first routing (Antonio, verbatim):** *"a system that will recognize the payments that are NOT from clients for invoices… if something is wrong or the system doesn't know, put it in My Finances, with a button 'this is for client' to put it back in Finance."* Unrecognised money lands here by default; both directions are one human click.
- **Nothing is auto-booked.** Every projected row starts `category='uncategorized'`; Antonio (or a bookkeeper flow) categorizes. The S-corp plan's vocabulary target is the CPA's Statement-3 expense categories from the filed 2024 1120-S.
- **Income for the books comes from the PAYMENTS ledger, not this table** (Phase 1b): 486 of 499 paid invoices have no bank-feed link, so bank rows can never be the revenue source. This table carries expenses, transfers, distributions, and the cash side.

## How it's built
- **Table: `td_books_transactions`** (migration `20260729-1500`, retirement of the old slice `20260729-1600`). Previously the books lived as a slice of the multi-tenant `bank_transactions` (client tax data + owner rows separated only by a sentinel account UUID) — named by three council reviews as the biggest corruption risk in the books plan. Now separated; `bank_transactions` is client-tax-only again.
- **`entity_id` from day one** (Antonio's multi-entity flexibility ask): today only Tony Durante LLC = `TD_ENTITY_ID` (`00000000-0000-0000-0000-000000000001`, exported from `lib/owner-finance.ts`; `OWNER_ACCOUNT_ID` is a deprecated alias). A second company's books are an INSERT away.
- **Identity = `UNIQUE (entity_id, transaction_ref)`** — deliberately NOT the old 4-column key (date+amount in the key turns an upstream amount correction into a duplicate row). Refs: `feed:<td_bank_feeds.id>` for projected rows; import refs for CSV rows.
- **Key files:** `lib/owner-finance.ts` (queries, categories, TD_ENTITY_ID), `lib/finance/owner-ledger-projection.ts` (the router + projection — see banking-bankfeed.md for the routing rule itself), `app/api/owner/transactions/` (+ `bulk`, `import`, `to-finance`), `app/(dashboard)/owner/` (UI).
- **Data flow:** bank feeds sync → `sweepFeedsToOwnerLedger` (Step 4b of `check-wire-payments`, every 6h) routes non-invoice money here via INSERT-ONCE upsert (`onConflict: entity_id,transaction_ref`, `ignoreDuplicates: true`) → Antonio categorizes in `/owner` → (Phase 1b+) books reports.
- **Category CHECK** includes `contribution` (S-corp equity roll-forward needs it; the client vocabulary already had it).

## Gotchas, invariants & past bugs
- **INSERT-ONCE, never update-on-conflict** (architect blocker): a books row is STATEFUL once categorized — a re-sweep that updated on conflict would silently wipe Antonio's categorization. `ignoreDuplicates: true` everywhere the projection writes.
- **"This is for a client →" DELETES the books copy BEFORE restoring the feed** — leaving it would count the money twice.
- **Signed amounts:** feeds store absolute values with direction in `status='outgoing'`; the projection signs them. A raw copy books an expense as income.
- **`bookkeeper_review_items.tx_id`** FK was re-pointed to `td_books_transactions` in `20260729-1600` — ids were preserved in the copy exactly so this repoint (and any future one) is trivial.
- **The old owner P&L numbers are NOT books** — flat-25%+SE tax is wrong for an S-corp (filed return: CASH method, officer comp, AAA roll-forward, Stripe Clearing asset). Do not present them as books until Phase 1b+ ships.
- **supabase-js returns errors, doesn't throw** — every write here must check `error`.

## How to verify current state
- Table + rows: `SELECT count(*), round(sum(amount),2) FROM td_books_transactions WHERE entity_id='00000000-0000-0000-0000-000000000001'` — compare against `/owner` totals.
- Old slice retired: `SELECT count(*) FROM bank_transactions WHERE account_id='00000000-0000-0000-0000-000000000001'` must be **0**.
- Category gate (the Phase 1a "every number identical" check): `SELECT tax_year, category, count(*), round(sum(amount),2) FROM td_books_transactions GROUP BY 1,2 ORDER BY 1,2`.
- Round-trip: plant a `STRIPE - TRANSFER` feed row in sandbox, run the sweep, confirm the row lands with ref `feed:<id>`, then `sendOwnerLedgerRowToFinance` — copy gone, feed back to `unmatched`.
