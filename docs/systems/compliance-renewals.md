# Compliance, Renewals & Deadlines
_Last verified against code: 2026-08-24 — Claude (**Follow-up to the 2026-08-23c entry below: the backfill has since run in full.** A one-time `execute_sql` production run (mirroring the logic of the reusable, now-committed `scripts/backfill-renewal-date-mirrors.ts`, dry-run by default, idempotent) corrected every account+column pair a fresh live re-scan found affected: 170 missing client-facing records inserted, 62 disagreeing dates corrected. One confirmed duplicate-row account, **B&P International LLC**, was resolved by hand after Antonio's explicit "fix it" (one row Cancelled, the other corrected to the real date) — confirmed live via the CRM's "View as client" feature both before (2 duplicate rows, both 115 days overdue) and after (2 correct rows, 0 overdue). A from-scratch whole-client-base verification query (447 real account/obligation pairs) confirmed ZERO remaining exceptions after the correction. PR #341, merge commit `51c523f31879a5675ca7c0da532dbfbb89d04c25`, dev job 8bd0e51a. The "KNOWN GAP, not yet fixed" bullet below about the affected-account count and the missing backfill is now STALE — corrected in the Gotchas section in this same change.)_
_Prior: 2026-08-23c — Claude (**Round-4 full-council pass on the renewal-date sync fix below — closed the 3 remaining live write paths + several correctness/wording gaps (dev job 8bd0e51a).** The same-day fix below was reviewed a 4th time by the full council (5 core + Compliance-Deadlines-Auditor + Business-Analyst) against the actual built code, not the design — no reviewer found the overall shape wrong, but real gaps turned up: (1) **the OLD `mirrorDeadlineDate`** — still used by every intake fill, the yearly SD-completion roll-forward, AND the Compliance Calendar's own one-click record repair — did NOT exclude Cancelled from its match (only Completed/Filed), so it could silently rewrite a Cancelled record's date while the record stayed hidden from the portal; fixed by adding Cancelled to its exclusion set, matching the new function. (2) **The Compliance Calendar's one-click repair** (`renewal-problem-apply.ts`) — the actual tool staff reach for to fix an affected account — wrote the account column directly and mirrored via the OLD year-scoped helper, entirely bypassing every new protection; it now shares the SAME exported mirror primitive (`syncDeadlineRowForRenewalDate`) `setAccountRenewalDate` uses internally, while keeping its own stronger value-level checked write on the account column untouched. Its result type gained an optional `warning`, threaded all the way to a `toast.warning` in the calendar UI. (3) **`hc_sync_license_deadlines`** (pulls RA renewal dates in from Harbor Compliance) wrote straight into the `deadlines` row with zero Cancelled/duplicate protection and never touched the account column at all — confirmed live in production that it has never actually been run in write mode, ever, but it is NOT architecturally blocked from running (only defaults to a dry-run preview) — now routed through `setAccountRenewalDate` too. (4) **The corrected affected-account count, verified live in production this pass: 145, not ~67** — 118 accounts missing the client-facing record entirely, 40 with a disagreeing date, 2 with the literal duplicate-record anomaly; no backfill/remediation script exists yet for any of them — that's the next piece of work, not yet built. (5) `crm_update_record`'s renewal-column handling now validates BOTH date fields' format before writing EITHER, and a later failure's error message now discloses which field(s) already committed, instead of reporting total failure while a field silently landed (a real partial-write bug — the earlier per-field-as-you-go loop could commit one date then fail on a malformed second one with no indication anything happened). (6) The direct-write refusal message previously told staff to go correct "the account's own renewal-date field" — that field doesn't exist anywhere in the CRM UI (confirmed by grep); reworded to name the two real routes (`crm_update_record`, or the Calendar's repair action). (7) Both mirror functions' status matching now tolerates a NULL status without excluding it as "not open" — Postgres/PostgREST's `NOT IN` treats NULL as neither-in-nor-out, so a legacy NULL-status row (this table is heavily Airtable-imported) would have silently fallen through to a duplicate insert; verified live that zero `deadlines` rows currently have a NULL status, so this is a defensive fix, not a correction of a live-today bug. Same defensive fix applied to the portal page's Cancelled-exclusion query. (8) `deadline_upcoming` (staff briefing tool) now also excludes Cancelled, not just Filed, so a just-resolved record stops looking like open work. (9) Docs and code comments corrected: this is not literally the "one" writer of these columns — the SD-completion roll-forward in the separate `lib/service-delivery.ts` deliberately keeps its own year-scoped write, and that's correct, not a gap. Deliberately NOT fixed here, flagged as a separate pre-existing issue in a different, already-confirmed-clean function: a Blocked record can survive an actual filing via Mark Filed because that action's own fallback matching doesn't recognize Blocked — belongs to `file-renewal.ts`, untouched by this project. 25 live E2E scenarios now cover this (up from 21), all passing against the real per-worktree database.)_
_Prior: 2026-08-23 — Claude (**setAccountRenewalDate — the single writer for the account renewal-date columns, closing 4 real write paths that bypassed the `deadlines` mirror (dev job 8bd0e51a, 3-round council review).** Root cause: `mirrorDeadlineDate` (2026-07-30 entry below) only ran from the calendar's own roll-forward/Mark Filed flow. Four OTHER real writers touched `accounts.ra_renewal_date`/`annual_report_due_date` directly and never synced the `deadlines` row the client portal reads: the account-page inline-edit capability (present in `allowedFields` but, confirmed live, had NO actual UI caller), the generic `crm_update_record` MCP tool, `deactivateSD`'s clear-on-deactivate, and `revertServiceDelivery`'s un-roll. Result: ~67 real accounts with the account date and the portal-visible record disagreeing, some showing the client TWO disagreeing dates from a year-boundary duplicate-row bug. NEW `setAccountRenewalDate(accountId, column, value, opts)` in `renewal-dates.ts` is the one sanctioned writer now: it writes the account column (through `updateAccount`, preserving optimistic-lock + audit-detail behavior) then keeps the ONE open `deadlines` row in sync via a YEAR-AGNOSTIC match (any non-terminal row, not scoped to a specific year — this is what lets a date corrected across a year boundary fix the same row instead of forking a second one). Cancelled is now excluded from matching alongside Completed/Filed — a cancelled record is never "revived" by a later date write; a new cycle always gets a fresh record, leaving the old cancelled one as pure history. A `Blocked` record (a real, active status for an unrelated reason — waiting on the client, or a filing on hold for an unpaid invoice) IS matched and gets its date corrected; its status is left alone. More than one open record for the same obligation is never guessed at — the account column still saves, but the mirror write is skipped and a warning comes back to whoever made the edit (surfaced, not just logged — a round-3 finding: the anomaly was previously invisible at the exact screen someone would be looking at). All 4 gap sites now route through this function. Two MCP tools (`crm_update_record`'s `deadlines` branch, `deadline_update`) now refuse a DIRECT edit to a renewal record's due date, and a direct flip to Filed/Completed (which skips the account-date roll-forward) — narrowly scoped after a round-3 catch: an EARLIER, broader version of this refusal would have also blocked marking a record on-hold (Blocked) or closing out a duplicate (Cancelled) directly, which are normal actions unrelated to this bug and are the only way to resolve the exact anomaly this fix can produce — both stay directly writable. **Client-portal leak fix:** `/portal/deadlines` now explicitly excludes Cancelled records from what a client is shown/counted — previously a closed-out record's old, now-past date could render as a live "overdue" alert on an obligation staff had already resolved (this is what a deactivate-then-reactivate cycle produces under the design above; every other reader of this table already filtered correctly, only this page didn't). A lint rule (mirroring the codebase's existing `portal_tier` precedent) now flags a future raw write to either column outside `lib/operations/renewal-dates.ts` — known limitation, shared with its precedent: it can't see a write built with a variable field name, so the one live spot where that pattern existed got its own explicit code-level guard instead of relying on the rule. Verified via 21 live end-to-end scenarios against the real per-worktree database (not mocked): year-boundary correction, clear/no-op, Blocked-preserved, Cancelled-never-revived, the exact portal query proving the leak is fixed, duplicate-row detection+surfacing, both renewal columns independently, a stale-edit conflict correctly reported, all 9 direct-write-guard cases, and the real `deactivateSD` path end-to-end. Confirmed OUT of scope, deliberately: `hc_sync_license_deadlines` (Harbor Compliance sync) is a live 6th/7th direct writer to this same table, but confirmed manual-only (no cron/schedule) and already hard-blocked from autonomous AI execution — flagged as a documented follow-up, not blocking this build.)_
_Prior: 2026-08-07b — Claude (**Revert now UN-ROLLS the renewal date — on any final stage (AL Group bug).** Un-doing a completed RA/AR SD (`revertServiceDelivery`, `lib/operations/service-delivery.ts` §5) previously reversed the +1yr date roll ONLY when the final stage was literally named "Closed"; renewal pipelines whose final is "Completed" kept the rolled date, so un-doing a wrongly-recorded filing left the company a year in the future — invisible on the calendar and 'renewed' to the engine (AL Group LLC, Luca 2026-08-07: the July payment was the 2025 reinstatement, not the 2026 AR). Now: fires whenever a COMPLETED renewal SD is re-opened (status reset), on any stage name; the target is the SD's OWN cycle due_date (one anniversary back only as fallback); it never moves a date forward and skips with an audit note when the date was already repaired. DST-safe string math replaces the old local-time setFullYear here too. AL Group's data was hand-repaired the same day (AR date 2027-12-15 → 2026-12-15, audited).)_
_Prior: 2026-08-07 — Claude (**Full-council pre-ship pass (7 reviewers) — 4 blockers + majors fixed (plan 89c951a7).** (1) **Filing-year resolution hardened end-to-end:** `resolveFilingForYear` (renewal-dates.ts) — explicit caller year → the SD's own due-date year → completion year last; and `fileRenewal` now derives its cycle year from the ACCOUNT's stored date (not the filed date) when the caller sends no year — the To-Do card's Mark Done sends none, so a December cycle recorded in January used to be labeled+rolled as the new year (skipped a compliance year, mislabeled receipt/notes/notification). (2) **SD cycle attribution:** `fileRenewal` re-stamps a pre-existing SD's `due_date` to the cycle actually filed before completing it — filing an older owed year through the next cycle's cron SD used to leave that completed SD "corroborating" the NEXT cycle (laundering path) and painted a false green filed row. (3) **ONE truth for the daily email:** the `ra-renewal-check` watchdog now runs `loadRenewalStatuses` + `proposeRenewalFixes` — the email reports exactly the rail's cards; its hand-rolled date checks (different exclusions → two disagreeing surfaces) are deleted. (4) **Deadlines mirror:** `Filed` rows are history like `Completed` — repurposing a Filed row to a new cycle kept its Filed status + old receipt on the client portal. (5) Money cards show the amount still OWED (`amount_due` preferred over face amount — partial payments no longer overstate the debt) with normalized decimals. (6) Confirm-tier one-clicks now require an explicit browser confirmation; the AR derive card warns to check whether THIS year's report was filed before confirming (its derived date is next year's cycle). (7) Loader: single-account recomputes scope all child queries by account; `tax_returns` pagination gets a unique order tie-break; grid rows expose the engine cause as a tooltip; Mark-Filed year selector is a continuous clamped range (no holes, default can't 400). Counselor MISMATCH logged for Antonio: Cash Cow Consulting + Italiza (One-Time) hold 4 active renewal SDs the exclusion strands — cancel-or-reclassify decision pending; Vanallen (NM) carries a stray AR date. Follow-ups (not shipped): non-Active statuses have no obligations surface anywhere (pre-existing); money-gate scope vs the codebase's Pending-past-due overdue rule = Antonio's call; revert-from-Closed still uses old date math.)_
_Prior: 2026-08-06 — Claude (**Compliance Truth Calendar (plan 89c951a7) — full-roster status engine, problems rail, one-click record fixes, filed-year roll semantics.** (1) NEW `lib/operations/renewal-status.ts` — THE pure status engine: `computeRenewalStatus()` judges every active company's RA + AR obligation as renewed/upcoming/on_hold_unpaid/overdue/missing_data/not_applicable with a total precedence order, a plain-English CAUSE and evidence (payment ids, SD ids, cycle corroboration) on every verdict. Date-primary: `accounts.ra_renewal_date`/`annual_report_due_date` ARE the record; completed SDs only corroborate the current cycle. Money gate = SOP v7.1 computed LIVE from Overdue/Delinquent payments (test payments excluded via NOT-IS-TRUE — `.neq` would drop the NULL majority), never from stored SD 'blocked' stamps. One-Time/test/internal are off-calendar (Antonio ruling b); closing companies stay visible flagged until closure completes (ruling c). (2) NEW `lib/operations/renewal-status-loader.ts` — batched full-roster loader (paged fetches, join-in-code, no giant `.in()` id lists) + intake detection (formation SD proof / RA-switch fallback / null=never guess). (3) NEW `lib/operations/renewal-problem-proposals.ts` + `renewal-problem-apply.ts` + `components/calendar/problems-rail.tsx` + `POST /api/calendar/fix-renewal-problem` — every problem verdict maps to a proposal card (roll_forward_date / verify_filing / review_unpaid / derive_missing_date / fix_account_fields) with safety tiers (safe one-click / confirm / antonio_only for money). One-click applies revalidate against a LIVE recompute, write with a checked `.eq(col, from)` update, mirror the deadlines row, and audit to `action_log` (`renewal_record_fix`). The calendar page (`app/(dashboard)/calendar/page.tsx`) now loads the FULL roster through the engine — the year window only places dated rows on the grid; problems render in the rail regardless of viewed year (the invisible-company class is dead). (4) **Roll-forward rewrite (§10/11 `lib/service-delivery.ts`, ONE shared block): filed-year+1 semantics** via `computeRollForward(stored, filingForYear)` — a filing FOR year N moves the record to the anniversary in N+1; already-current records are never moved backwards or double-rolled (checked write on the read value); dbWriteSafe errors now SURFACE into auto_triggers (the silent discard was the prime suspect for never-rolled records). `renewal_filing_for_year` threads Mark-Filed dialog → file-renewal route → `fileRenewal` → `completeSD` → the roll; the dialog gained a "Filing for year" select defaulting to the DUE date's year, and the receipt filename/notes/deadlines/notification all use the cycle year. file-renewal's year-NULL deadlines fallback now also matches Overdue rows. (5) **Timezone bug fixed:** `plusOneYear`/`anniversaryForYear` are pure string math — the old `new Date(iso).setFullYear()` drifted a day for dates inside the DST-transition window on non-UTC machines (2026-11-07 → 2027-11-06). Feb-29 → Mar-1 convention kept. `mirrorDeadlineDate()` extracted as the shared deadlines-mirror (intake fills + one-click repairs + completion roll; `includeNullYear:false` on the roll path so a new-cycle insert never steals the old cycle's year-NULL row before file-renewal completes it.)_
_Prior: 2026-07-30 — Claude (**Renewal-date engine + watchdog + Harbor v3 reader (plan c2d97552).** (1) NEW `lib/operations/renewal-dates.ts` = single source of truth for the INITIAL `accounts.ra_renewal_date` / `annual_report_due_date` / `cmra_renewal_date` fills: RA = anniversary of the RA start (formation intake → `formation_date`+1yr; onboarding intake → `ra_switch_date`||`client_since`+1yr, matching billing's start chain); AR per state (FL May-1 / DE Jun-1 / WY formation-month, next calendar year; NM none — TD files ARs itself); CMRA = Dec-31. FILL-IF-NULL ONLY with per-column `.is(col,null)` guards, called ONLY from company-creation moments: `materializeFormationCompany`, the Company-Formation → "Articles Received" transition (`lib/service-delivery.ts` §12 — which historically never set the RA date: root cause of formations invisible on the calendar), onboarding-setup step 5b, and the onboarding review tool (whose old unconditional `ra_renewal_date = today` write was REMOVED — it clobbered dates and spawned year-early RA SDs; it now records `ra_switch_date` null-only and derives the anniversary). Yearly advancement stays owned by the SD-completion roll-forward (+1yr, Feb-29→Mar-1 convention shared by both writers). The helper also MIRRORS RA/AR fills into the `deadlines` table (the portal Deadlines page + dashboard cards read it): matches the current OPEN row (same year OR the majority year-NULL legacy rows), never touches Completed history. (2) `ra-renewal-check` cron: dedup now `.in status [active,blocked]` (was creating a NEW blocked SD daily); a TWO-COLUMN WATCHDOG runs daily even on an empty window and reports every Active Client whose RA or (non-NM) AR date is NULL or past — excluded: `is_test`, deliberate discontinuations (date NULL + cancelled SD of that type, the `service_deactivate clear_renewal_date` contract) — into the daily staff email + `cron_log.details.watchdog`. The report email itself was FIXED: `gmailPost` was being given `{to,subject,htmlBody}` which Gmail's send API rejects (needs base64url raw MIME) and the 400 was swallowed — support@ has ZERO of these reports ever delivered; now real MIME + RFC-2047 subject (R041). SIBLING NOT FIXED: `annual-report-check` still has the same broken email shape (flagged, separate item). (3) Harbor v1→v3: HC upgraded the Partner API; registrations (carrying `expiration_date` + `next_annual_report_due_date`) are NESTED under account/company in v3 while flat v1 `/licenses` returns empty. `getV3BaseUrl()` derives the v3 base (`HC_API_V3_BASE_URL` override), new `listAccountsV3`/`listCompanyRegistrationsV3`, new `hc_company_registrations` MCP tool; `hc_list_licenses` with a company_id routes via v3. PRODUCTION HC creds are live (sandbox creds are separate; HC's own sandbox tenant is empty); HC does NOT yet expose RA service-period dates — their team owes us that + paid-invoice retrieval (thread with James Gilmer/Justin Marsh/Kiana Robinson, 2026-07-29). CRM dates remain the operational truth; HC becomes a cross-check when they ship it. KNOWN-OPEN: AR first-year derives from the CURRENT year (a Dec-formation approved in Jan gets next-next-year — pre-existing convention, needs Antonio ruling); watchdog flags Client-typed accounts that never had RA service (tax-only) — scale visible after the 17-company repair review. Branch `claude/portal-chart-new-company-issues-852770`.)_
_Prior: 2026-07-07 — Claude (Drive duplicate-upload sweep, LT Program incident class — the HC delivery download-to-Drive (`lib/mcp/tools/harbor-compliance.ts`) and renewal receipts (`lib/operations/file-renewal.ts`) switched from `uploadBinaryToDrive` to `uploadBinaryToDriveUpsert` (stable file name → a retry/re-run refreshes the ONE existing Drive file in place instead of adding a copy). No other behavior in this subsystem touched; full sweep rationale in `documents.md` (2026-07-07b). Branch `claude/objective-cohen-b75f61`, SANDBOX — Antonio ships.)_
_Prior: 2026-05-29 — Claude (read harbor-compliance/client.ts, ra-renewal-check cron, service-delivery.ts)_

## What it is
Keeping clients' companies in good standing: the Harbor Compliance integration (registered agent + state filings), the recurring renewals (RA renewal, state annual report), the deadline register, and the calendar.

## Harbor Compliance (HC) integration
- HC is the external provider for **registered agent** and **state filings**. `lib/harbor-compliance/client.ts` is an OAuth2 (password-grant + refresh) REST client. Env: `HC_CLIENT_ID/SECRET/USERNAME/PASSWORD`, `HC_API_BASE_URL` (default `harborcompliance.com/api/v1`).
- Client methods: `listAccounts/Companies`, `createOrder`, `getOrder`, `updateOrder`, `listDeliveries`, `downloadDeliveryFile`, registered-agent change, license sync.
- MCP tools: `hc_list_companies`/`hc_companies`/`hc_company_id`, `hc_get_order`/`hc_orders`, `hc_list_deliveries`/`hc_deliveries`/`hc_download_delivery`, `hc_list_licenses`, `hc_submit_ra_change`, `hc_sync_company`, `hc_sync_license_deadlines`.

## Renewals (date-driven, cron-created)
Recurring obligations are driven by **account-level dates**, and a nightly cron materializes the work:
- **RA Renewal** — `accounts.ra_renewal_date`; cron `app/api/cron/ra-renewal-check` (daily 9am UTC): scans active **Client** accounts where the date is within 30 days, creates a "State RA Renewal" SD + a task for Luca (if not already created). **Skips** accounts with an active Company Closure / Client Offboarding. **Blocked if the 1st installment is unpaid** → task to Antonio (SOP RA Renewal **v7.1**: "no service if first installment unpaid", Antonio 2026-05-05, overrides the old "non-postponable" v7.0).
- **State Annual Report** — `accounts.annual_report_due_date`; cron `annual-report-check` (analogous).
- `RENEWAL_DATE_COLUMN` (`lib/operations/service-delivery.ts`) maps `service_type` → the account date column the cron reads; `isRenewalServiceType()` gates renewal-specific behaviour.
- **Annual Renewal billing** is separate: crons `annual-installments` + `annual-renewal-msa`. **Annual Renewal is a billing cycle, NOT a service delivery** (R106).

## Deadlines & calendar
- **Deadlines** — the `deadlines` table (477+ records: tax filings, annual reports, RA renewals) with status tracking. Tools: `deadline_search`, `deadline_upcoming`, `deadline_update`, `deadline_type`.
- **Calendar** — `calendar_create/update/delete/list_event`, `calendar_find_free_slots` (`lib/mcp/tools/calendar.ts`); the CRM calendar surfaces RA-renewal + annual-report dates (calendar ↔ tracker integration).

## Business rules
- **RA Renewal v7.1:** no service if the 1st installment is unpaid — the cron raises a task to Antonio instead of auto-servicing.
- **Annual Renewal ≠ SD** — it's a billing cycle (R106); don't create a service delivery for it.
- **Renewal SDs are re-created nightly** from the account date — clearing/zeroing the account date is the only way to permanently stop the cron.
- Skip renewal creation when a Closure / Offboarding is active (a leaving client shouldn't be renewed).

## How it's built — key files & tables
- Files: `lib/harbor-compliance/{client,index,types}.ts`, `lib/mcp/tools/{harbor-compliance,deadlines,calendar}.ts`, `app/api/cron/{ra-renewal-check,annual-report-check,annual-installments,annual-renewal-msa}/route.ts`, `lib/operations/service-delivery.ts` (`RENEWAL_DATE_COLUMN`, `deactivateSD` `clear_renewal_date`), `lib/operations/renewal-dates.ts` (`setAccountRenewalDate` — the ONE writer for the two account renewal-date columns; `checkDeadlineDirectWrite` — the reverse-direction guard used by `crm_update_record` and `deadline_update`), `app/portal/deadlines/page.tsx` (excludes Cancelled).
- Tables: `deadlines`, `accounts` (`ra_renewal_date`, `annual_report_due_date`), `service_deliveries` (State RA Renewal / State Annual Report), `tasks`. HC data is external (synced in).

## Gotchas, invariants & past bugs
- **A renewal SD you cancel will come back tomorrow** unless you clear the account date — `deactivateSD({ clear_renewal_date: true })` clears `ra_renewal_date`/`annual_report_due_date` so the nightly cron stops re-creating it. Deactivating without that flag = the SD reappears.
- **Unpaid 1st installment blocks RA renewal** (v7.1) — the work is NOT auto-created; a task goes to Antonio. Don't assume an overdue client got renewed.
- **Annual Renewal is billing, not an SD** — creating an SD for it is wrong (R106).
- **HC is an external API** — failures are network/auth (OAuth2 creds); `hc_sync_*` pulls HC state into the CRM, it's not the source of truth for our pipeline.
- **Any code CORRECTING or CLEARING `accounts.ra_renewal_date` / `accounts.annual_report_due_date` must go through `setAccountRenewalDate()`** (`lib/operations/renewal-dates.ts`), never a direct `.update()` — that's what keeps the client-portal-visible `deadlines` row in sync. This is NOT the only writer of these columns by design: the SD-completion roll-forward in the separate `lib/service-delivery.ts` correctly keeps its own year-SCOPED write + `mirrorDeadlineDate` call, since it always knows which cycle it's filing FOR — don't "fix" that call site into the year-agnostic writer, it would reopen an already-fixed bug (see the 2026-08-06 entry). A lint rule catches a raw literal-key write to these two columns outside `lib/operations/**`, but NOT a write built with a computed/variable key — check for that pattern by hand if adding a new writer.
- **`hc_sync_license_deadlines` (Harbor Compliance sync) also routes through `setAccountRenewalDate` now** — it used to write the `deadlines` row directly with no protection and never touched the account column; confirmed live that it has never actually run in write mode, but it was not architecturally blocked from doing so (only defaults to `dry_run:true`).
- **A direct edit to a `deadlines` row's `due_date`, or a direct flip to `Filed`/`Completed` status, is blocked** by `crm_update_record` and `deadline_update` for RA Renewal/Annual Report rows (`checkDeadlineDirectWrite`) — correct the account's own date instead (via `crm_update_record`, or the Compliance Calendar's repair action), or use the calendar's Mark Filed action for an actual filing. Cancelled and Blocked stay directly writable on purpose.
- **The client portal never shows a Cancelled `deadlines` row** — every reader of this table must filter it out (`.or('status.is.null,status.neq.Cancelled')`, NOT a bare `.neq('status','Cancelled')` — a plain `<>` is NULL, and excluded, for a NULL-status row on this heavily legacy-imported table). A closed-out record's stale due date will otherwise render as a fake "overdue" alert.
- **KNOWN GAP, not yet fixed:** a `Blocked` `deadlines` row can survive an actual filing recorded via the calendar's Mark Filed action, because that action's own year-NULL fallback matching (`lib/operations/file-renewal.ts`) doesn't include `Blocked` in its status list. Pre-existing, unrelated to this fix's own changes — flagged as a follow-up card, not fixed here.
- **RESOLVED 2026-08-24 (was "KNOWN GAP, not yet fixed" as of 2026-08-23):** the affected-account backlog identified by the round-4 review has been fully corrected — 170 missing client-facing records inserted, 62 disagreeing dates corrected, plus the B&P International LLC duplicate resolved by hand. A from-scratch whole-client-base verification query (447 real account/obligation pairs) returned zero remaining exceptions. See the entry at the top of this doc. The reusable script `scripts/backfill-renewal-date-mirrors.ts` (dry-run by default, idempotent) is committed for any future drift of this shape.

## How to verify current state
- Read `app/api/cron/ra-renewal-check/route.ts` (the 30-day scan + unpaid-installment block), `lib/operations/service-delivery.ts` (`RENEWAL_DATE_COLUMN`, `clear_renewal_date`), `lib/harbor-compliance/client.ts`.
- Upcoming renewals: `SELECT company_name, ra_renewal_date, annual_report_due_date FROM accounts WHERE ra_renewal_date <= now() + interval '30 days' OR annual_report_due_date <= now() + interval '30 days';`
- Deadlines: `SELECT deadline_type, due_date, status FROM deadlines WHERE account_id='<id>' ORDER BY due_date;`
- Note (R096): sandbox via sandbox MCP / `psql`; production `execute_sql` hits production.
