# AI Agent (in-dashboard assistant)
_Last verified against code: 2026-07-18h — PARTIAL READS NO LONGER COUNT AS 'IT LOOKED' (the safety half of windowed OCR). `read_scanned_document` now reads a long PDF one 15-page window at a time, which introduced a hazard WORSE than the bug it fixed: a partial read arrives looking like a SUCCESS. `read_scanned_document` is in `ABSENCE_EVIDENCE_TOOLS` (`answer-guards.ts:43`) and `hasSearchedForAbsence` is satisfied by any non-failed lookup, so a clean pages-1-15 read of a 35-page return would have licensed 'there is no Schedule C in this return' — with the guard CONFIRMING it had looked — when Schedule C is on page 22. Today's wholesale failure returns `lookup_failed:true` and is correctly counted as 'did NOT look'; windowing would have silently removed that protection. FIX (structural, not prose): every read now returns a machine-readable `coverage` object — `document_page_count` (true length from the local split), `pages_returned` (computed AFTER character truncation, so a response whose text stopped inside page 3 cannot claim 'pages 1-15'), `complete`, and `pages_not_read` ranges. New pure `looksLikeIncompleteRead()` keys off that data, and the ONE choke point in `runWorkerLoop` (`worker-tools.ts`, where `succeededTools` is built) now requires a lookup to have come back AND come back WHOLE. Field names deliberately avoid anything matching /error/ — an error-shaped key on a successful partial read would misclassify it via the sibling `looksLikeFailedLookup` head-scan. Prose alone was explicitly rejected: this file's own header records a prompt rule that failed four times, and the loop's exhaustion path actively tells the model to 'answer NOW with what you've found', overriding any 'ask for more pages' instruction. THE READER ALSO SERVES STORED TEXT FIRST: `documents.ocr_text` is written page-delimited, so a page or range request is answered from storage with no download, no Document AI call, no per-page billing and no page limit; OCR runs only on a miss. Tool description updated (it advertised 'omit for the whole document' and 'max 15MB', both now false) and gained a `pages` range parameter. An unreadable page selector is an explicit error, never a silent fall back to page 1. Tests: `answer-guards.test.ts` (+8, incl. the end-to-end scenario) and `docai-window-read.test.ts`. Council pass 2, dev job `ee4d1df6-fa26-4e3d-904e-a59532d1665c`. Prior: 2026-07-18f — NO in-dashboard-assistant change: `read_drive_file` gained a STORED-TEXT third rung (falls back to `documents.ocr_text` by `drive_file_id` when the live text export and live text layer both yield nothing, i.e. a scanned PDF), plus two new pure helpers `formatStoredExtraction()` / `explainDriveReadFailure()` in `worker-tools.ts`. The in-dashboard assistant is UNAFFECTED: `read_drive_file` is not in `AGENT_TOOLS` (verified — no reference in `lib/ai-agent/tools.ts` or `providers.ts`), so the dashboard agent never had this tool. Flagged only by the coarse `lib/ai-agent/` path mapping. **Correction recorded while verifying this (R093):** the doc-source read rail is NOT 'Slack-only' as `agent-bridge.md` / `slack-claude-worker.md` have long described it — `enableDocReads` is set by THREE surfaces: the Slack worker (`slack-claude.ts:2067`, `:2208`), the team-chat trigger (`lib/team/claude-trigger.ts:260`), and the portal-chat suggest route (`app/api/portal/chat/suggest/route.ts:203`, staff-gated by `isDashboardUser`). All three are staff surfaces and the R108 Hermes/Telegram exclusion still holds (the tools remain OUT of `WORKER_TOOLS`), but 'Slack-only' is the wrong mental model for blast radius — a change to these handlers reaches team chat and the Portal Chats panel too. Dev job `ee4d1df6-fa26-4e3d-904e-a59532d1665c`. See `slack-claude-worker.md` → 'Doc-source reading rail'. Prior: 2026-07-18e — Claude (**#td-worker-bug WALL REPORTS — rewired after Antonio rejected v1 (dev job a6c3d75b).** V1 fired a thread whenever a correction produced a lesson. He rejected it outright: *"if he got it wrong and I corrected it and he memorized my correction, it doesn't need to create any kind of thread. It's a part of the learning process."* With tens of conversations a day that channel would have been muted — worse than no channel. It was wired to the SUCCESS signal. REWIRED to the two things ONLY CODE can fix: (a) **absence_without_looking** — the answer-guard already detects a reply asserting absence with zero SUCCEEDED lookups, so the detector was sitting there unused; (b) **cannot_do** — new `assertsCannotDo` in `answer-guards.ts`, a flat capability refusal (Antonio's live case: "I can't access external URLs or Slack links — I don't have a browser or Slack API access"), deliberately narrow so business statements ("the client can't sign until…") don't trip it. `runWorkerLoop` now returns `wallsHit` + `succeededTools`; `callWorker` fires `reportWorkerWall` fire-and-forget with `opts.surface`/`clientName`. The thread shows what was asked, what it was about to say, and WHAT IT ALREADY TRIED — so the reader knows whether it looked and failed or never looked. Deduped per thread+kind+day. Ordinary corrections are SILENT again (the capture path no longer reports at all — `lesson-capture.ts` is back to pure memory). If the channel gets busy, that IS the finding: the worker keeps walking into the same missing tool. 6219 tests + build green. Prior: 2026-07-18d — Claude (**READ SCANNED DOCUMENTS — the last council blind spot, closed (dev job a6c3d75b, Antonio "yes add it").** VERIFIED IN THE WILD FIRST: after the 2026-07-18c ship, the AI Venture Labs worker thread shows the guards working — at 18:53 it found the fax correctly (July 3 11:57, 855-641-6935, 4 pages, delivered, 2m29s transmit) where that morning it had said "not in the database"; at 18:54 it ACCEPTED Antonio's correction ("You're right — I was wrong earlier") and went and found the responsible-party change instead of doubling down; at 18:55 it honestly surfaced a source conflict and said it could not confirm the signer without the document. **That last answer was CORRECT** — `ss4_applications.signature_data` is a base64 PNG of a drawn signature with NO signer name, so the signed PDF is the only source, and the worker had no way to read it (`read_drive_file` explicitly refuses image-only PDFs; `docai_ocr_file` existed in the catalog, curated READ, but was never in the worker's set). NEW `read_scanned_document` wraps `ocrDriveFile` (PDF/TIFF/GIF/JPEG/PNG/BMP/WEBP, ≤15MB), granted in `WORKER_READ_ONLY_TOOL_NAMES`, `CRM_READ_TOOL_NAMES` (thread-routing) and `ABSENCE_EVIDENCE_TOOLS`; errors surface as `lookup_failed` and empty output says so explicitly rather than reading as "the document is blank". CHAIN FIX: `search_documents` now returns `drive_file_id` — without it the worker could FIND a document but not open it. END-TO-END VERIFIED against the real file: OCR of "Form SS-4 - AI Venture Labs LLC - Signed.pdf" at 96.8% confidence returns "Name and title (type or print clearly) **Michele Cotti - Member**", signed 07/02/2026 — settling the question the worker couldn't: **Michele signed, not Gaia**; Antonio was right both times. PRIVACY NOTE: OCR of a signed SS-4 returns the responsible party's ITIN in plain text — this tool reads genuinely sensitive documents, which matters for the still-open Portal-Chats client-scope work. 6188 tests + build green. Prior: 2026-07-18c — Claude (**ANSWER GUARDS + THE TWO MISSING LOOKUPS (dev job a6c3d75b). ROOT CAUSE NAILED FROM THE AUDIT TRAIL, and it is narrower than every theory: the worker DID query during the incident — `action_log` shows two `run_sql_query` reads at 15:33:10 and 15:34:29 against `ss4_fax_history` and `fax_history`, **tables that do not exist**. Both errored, and from "the table I guessed isn't there" it concluded "there is no fax table in the database" and told Antonio to check the fax service himself. It never queried `information_schema` for what DOES exist, and never opened `documents` or `action_log` — where the receipt and the send were. **This falsifies a naive tool-trace gate**: counting any executed lookup as proof-of-search would have passed this incident straight through, so `hasSearchedForAbsence` is judged on SUCCEEDED lookups only (`looksLikeFailedLookup` in the new `lib/ai-agent/answer-guards.ts`; the loop pushes to `succeededTools` only when the result isn't an error). GUARDS (`answer-guards.ts`, wired at the end-of-turn exit in `runWorkerLoop`, modeled on the proven `shouldRefusePortalDraftLanguage`): (a) ABSENCE LATCH — a reply that asserts absence with zero SUCCEEDED lookups is not returned; one nudge turn naming documents/activity-log/Drive/information_schema, then it answers. (b) CORRECTION LATCH — when the staff turn reads as a push-back (`isCorrection`) and zero lookups succeeded, it cannot reply without a fresh check, which is what makes "the database and the screenshot both agree" structurally impossible rather than merely forbidden. Both latch ONCE (bounded: one extra iteration), NUDGE rather than block, and fail OPEN. Fixtures are the verbatim incident replies + Antonio's verbatim correction. NEW TOOLS closing the real gap: `search_documents` (stored files — receipts/confirmations live here, not on the record they relate to) and `get_client_history` (the audit trail; its `contains` sweep is deliberately NOT account-scoped because the AI Venture Labs fax row carries a NULL account_id, so any client-scoped search misses it). Both added to `WORKER_READ_ONLY_TOOL_NAMES`, to `CLIENT_DATA_TOOL_NAMES` in `thread-routing.ts` (an existing test caught that omission — investigation threads would not have received them), and to `ABSENCE_EVIDENCE_TOOLS`. `RUN_SQL_QUERY_TOOL`'s description now names `documents` + `action_log`, warns that some audit rows have a null account, and instructs it to check `information_schema` rather than assert non-existence. Also CONFIRMED LIVE: `run_sql_query` logs actor `claude.slack` even when run from Portal Chats (the security reviewer's mis-attribution finding). 6176 tests green. Prior: 2026-07-18b — Claude (**COUNCIL SAFETY PASS + SHARED MODEL GEAR (dev job a6c3d75b, 7-reviewer full council, 869k tokens, verdict FIX-FIRST).** Root cause of the AI Venture Labs incident, agreed by 5 reviewers independently: NOT tool access — Portal Chats/Inbox already set `enableDbRead` (read-only SQL over the whole DB) plus Drive/doc reads, and `SLACK_WORKER_SYSTEM_PROMPT` already forbids all three failures VERBATIM (`slack-claude.ts:124,125,128`) with tests asserting those strings. `slack-claude-worker.test.ts:138-192` is a changelog of the SAME class being "fixed" by prompt text 3 times before — this was the 4th. **Prompt mitigation is empirically falsified; the missing mechanism is a server-side check of the ANSWER against the TOOL TRACE, which does not exist yet** (`toolsUsed` is computed at `worker-tools.ts:2793,2944` but never persisted; `worker-chat/route.ts:548` writes only reply+status). SIX DEFECTS FIXED THIS PASS: (1) `closure_form_review({mark_reviewed:true})` classified READ → AUTO-RAN an unapproved write; `mark_reviewed` + sibling shapes (mark_*, confirm, approve, execute, apply, publish, finalize) added to `ESCALATING_FLAGS` (`tool-risk.ts:67`). (2) `use_tool` wrapped `proposeAction`'s refusal in "queued for your approval" when the rail is OFF (its default) — our own code fabricating a queue that doesn't exist; now gated on `workerActionsEnabled()`. (3) Per-client recall required `clientName`, which the panel sends only on turn 1 (`thread-worker-panel.tsx:133`) — the brain wrote every turn and read almost never; now gated on the client KEY alone (`worker-tools.ts` + `worker-chat/route.ts:538`). (4) Tool results were injected UNFENCED (only the initial body + `read_portal_attachment` were fenced) — a live prompt-injection channel next to an unrestricted `web_fetch`; new `fenceToolResult`/`isUntrustedResultTool` label third-party-authored results as DATA, prefix-matched so new gmail_/drive_/doc_/portal_chat_ tools are covered by default; internal structured lookups stay unwrapped. (5) `get_client_paperwork` dropped `contact_id` when both ids were given (contact-only offers read as "none") and swallowed query errors to `[]` — a FAILED lookup was byte-identical to "none exist"; now matches either id, surfaces `lookup_errors`, and states what it does NOT cover (documents, activity log). (6) stale `lesson-capture` header claiming no-clientKey captures are suppressed — corrected to the live D1 contract (global + scrubbed). **SHARED MODEL GEAR** (Antonio 2026-07-18: "move the option to change model everywhere there is the worker with the settings icon", ONE shared setting): new curated catalog `lib/ai-agent/worker-models.ts` (+`isAllowedWorkerModel` so a typo'd/retired id can't take every surface down at once), new `worker_model` key in `lib/settings.ts`, `resolveWorkerModelAsync` (stored → env `WORKER_MODEL` → `WORKER_MODEL_DEFAULT`, 30s cache, `clearWorkerModelCache` on write) awaited in `callWorker`, admin-only `GET/POST /api/ai-agent/model`, and one shared `components/chat/worker-settings-gear.tsx` mounted on ALL three worker panels (Portal Chats tab, Inbox panel, dashboard sidebar) so they cannot drift. Worker still runs `claude-sonnet-4-6` until Antonio picks otherwise. KNOWN-OPEN (council, not yet built): persist the tool trace + false-absence/correction latches; server-side client scope on Portal Chats (today "one client" is prompt-only while `run_sql_query` is whole-DB and unredacted); `web_fetch` has no `allowed_domains`; no documents/action_log tool (only raw SQL, whose description names neither); fax has no tool and its `action_log` row can have a null account; bridge reads are unaudited and `run_sql_query` logs actor `claude.slack`. 6160 tests + build green. Prior: 2026-07-18 — Claude (**🧠 BUTTON IN THE WORKER TAB (dev job 203cda1a). Antonio: the Worker tab had no 🧠 — correct, the reaction picker only ever rendered on real chat messages (Portal/Team/portal-chat/contact-detail), never on the staff↔worker panel, whose turns live in `agent_messages`. Added a per-reply "🧠 Remember" button in `components/portal-chats/thread-worker-panel.tsx` → new `POST /api/inbox/worker-chat/remember`. SECURITY: the body carries ONLY the message id; the route re-reads that reply's text server-side (a browser can never post arbitrary text into the GLOBAL brain) and requires `recipient='worker'` + `status='done'` + a non-empty reply. Staff-gated like the panel. Reuses the 🧠 path → `distillMarkedMessage` → general client-free rule → GLOBAL save, idempotent on `source_ref='worker:<id>'`. `chat-memory-reaction.ts` refactored: new core `saveMarkedMessageAsMemory` returns `{saved, reason}` (empty | already_saved | nothing_general | error) so the UI can say something TRUE (R099); `saveChatMessageAsMemory` kept as a BOOLEAN wrapper because the two existing react routes test truthiness — returning the object there would have made every failure read as success. The worker-chat GET now returns each turn's `id` and POST returns `messageId` so the button works on restored history AND the just-received reply. 12 tests on the save reasons; 6144 suite + build green.**). Prior: 2026-07-17e — Claude (**BUSINESS BRAIN — the worker learns Antonio's thinking, on every surface (dev job 203cda1a, council-reviewed; branch claude/adam-marra-worker-chat-fdbfec; only P1 is on prod). Five parts: (P1) the GLOBAL lesson-recall RPC `match_decision_memory` is now GLOBAL-ONLY (`client_key IS NULL`) — a client-private lesson can never surface in another client's auto-recall; own-client recall stays on the separate `match_decision_memory_client`. LIVE ON PROD (Antonio applied it; migration `20260718-0100`). (P2) NEW `lib/ai-agent/lesson-capture.ts` — one `captureLessonFromTurn` write path wired into Slack + Hermes (`slack-claude.ts` `detectAndSaveCorrection` is now a thin adapter), Portal Chats + Inbox (`worker-chat/route.ts`), and Team chat (`claude-trigger.ts`), called post-reply. Inputs = staff message + prior worker reply ONLY (never enriched/fenced content — the Adam-Marra fix); also captures the reasoning (the WHY). Scope (Antonio 2026-07-17): client in context → CLIENT-SCOPED raw; no client → GLOBAL but SCRUBBED first (`generalizeForGlobal` strips names/amounts/ids, fails closed). (step 1) 🧠 = GLOBAL: both 🧠 paths (`webhooks/slack-claude` + `chat-memory-reaction.ts`) now `distillMarkedMessage` → general client-free rule → global (fixes the old bug where 🧠 embedded the meta-string "marked important", making it unrecallable by content). (step 2) CORRECTION = TRUTH: a genuine correction supersedes the nearest SAME-SCOPE lesson (`contradictMemory`, extended to carry the new situation+reasoning) — guarded: mode=correction only, 0.93 similarity AND decision-differs, never cross-scope, best-effort append fallback. (P5 / step 3) the in-dashboard sidebar CAN now run the WORKER (`callWorker`) instead of `providers.ts` — behind `WORKER_SIDEBAR_ENABLED` (OFF by default; `app/api/ai-agent/route.ts` `runSidebarWorker`). New 'dashboard' surface prompt in `inbox-worker-prompt.ts`; per-page client scope from the live route via `sidebar-scope.ts` (`clientKeyFromPath` + server `parseSidebarClientKey`); timeout 60→300; the old silent direct-writes are gone by construction (the worker never had them). HOW TO VERIFY: `pg_get_functiondef(match_decision_memory)` contains `client_key IS NULL` (prod ✓); with `WORKER_SIDEBAR_ENABLED` unset the sidebar is unchanged (`callAgent`/`providers.ts`).**)._
_Prior: 2026-07-17b — Claude (**ONE dashboard-assistant hygiene fix (council redo WS1, dev job a9477d06): `loadGlobalMemories` (injected into every dashboard-agent prompt via providers.ts) was concatenating EVERY global `agent_memory` row unbounded — now capped to the 40 most-recent rows / 6000 chars.** Also in the shared worker layer (no in-dashboard change): the `SLACK_WORKER_SYSTEM_PROMPT` MEMORY block switched to ASK-BEFORE-YOU-SAVE (Antonio's D1) and `formatTemplatesForPrompt` now labels template bodies as copy-not-instructions. Full detail in `slack-claude-worker.md` (2026-07-17d). Prior: 2026-07-17 — Claude (**NO in-dashboard-assistant behavior change** — the WORKER engine under `lib/ai-agent/` changed (Adam Marra incident fixes, council-approved, dev job a9477d06): portal-send language guard + send latch in `worker-tools.ts` (new pure detector `draft-language.ts`), verified client card `client-card.ts` injected per-call on the Portal-Chats surface, canonical client-key fix so per-client memory recall/save actually works on the CRM surfaces, DRAFTS-block language + metadata-vs-document prompt rules, surface-neutral `send_portal_message` description, recipient name in send confirmations. `tools.ts` change: `decisionMemorySaveTool` now accepts a server-injected `client_key` (the dashboard agent's own `memory_save` path is unchanged — no caller passes it there). Full detail in `slack-claude-worker.md` (2026-07-17).)_
_Prior: 2026-07-10c — Claude (**NO in-dashboard-assistant change.** The SHARED `worker-tools.ts` `send_email` gained an `attach` param + `WorkerSendContext.emailSendPrep` / `CallWorkerOptions.emailSendPrep` (Inbox worker only): when the model attaches a file, the executor calls `prepareWorkerEmailSend` and returns a confirmation — it does NOT send; a human Confirm button dispatches. Gated entirely by `emailSendPrep` being set (Inbox route only), so Slack / Team Chat / Hermes are unaffected (R108). Coexists with the off-thread-recipient confirm below. Full detail in inbox.md 2026-07-10f.)_
_Prior: 2026-07-10b — Claude (**NO in-dashboard-assistant behavior change**: the shared worker engine (`worker-tools.ts`) gained a `WorkerSendContext.capturedOffThreadAttempts` sink + `WorkerResponse.pendingOffThreadRecipient`, used ONLY by the CRM Inbox worker's new "Confirm & send to an off-thread address" handshake — the in-dashboard `callAgent`/`providers.ts` path is untouched (no `send_email`, no recipient pin). Full detail in `inbox.md` (2026-07-10e). Prior: 2026-07-10 — Claude (**NO in-dashboard-assistant behavior change**: the WORKER action rail was switched OFF (reversible) — no worker/helper on any surface queues actions or launches/ships code (new `worker-actions-switch.ts`, env `WORKER_ACTIONS_ENABLED` default OFF; `propose_action` removed from `WORKER_TOOLS`, `enableCodeTasks` forced false, both 6-digit interceptions gated on the switch). This touches only the `callWorker`/Slack/team-chat/inbox-helper engine under `lib/ai-agent/`; the in-dashboard `callAgent`/`providers.ts` path never had `propose_action` or code-tasks and is unaffected. Sends on "go" and read/investigate unchanged. Full detail in `agent-bridge.md` (2026-07-10) + `slack-claude-worker.md`. Prior: 2026-07-10b — Claude (**Adversarial QA pass on the attachment work — 6 real bugs fixed, 1 escalated.** (1) **Payload blowout**: per-file caps multiplied out to ~107 MB of base64 against Anthropic's ~32 MB request limit (3 email images + 5 uploads + 2×20 MB scanned-PDF blocks), and the whole payload is re-sent on EVERY tool-loop iteration (up to 20). New `capMediaBudget(images, documents)` enforces `MAX_MEDIA_BASE64_BYTES` (16 MB) across BOTH lists, dropping documents before images (the image is what the user asked about) and NAMING every drop. Applied on all three surfaces. (2) `isMediaError` only matched a per-block `400 …image…`; an oversized request returns **413 / "request too large" / "prompt is too long"**, which never mentions "image" — so the text-only retry never fired and the panel got a raw 500. Widened. (3) **Signature logos ate the vision budget**: `extractAttachments` returns Content-ID inline parts (footer logos, tracking pixels) indistinguishably from real attachments; `harvestEmailAttachments` now excludes them via `extractInlineImages`, and walks messages **newest-first** — walking oldest-first let logos from four older replies fill all 3 image slots before reaching the message carrying the screenshot being asked about. (4) **Positional refs were unstable**: `att1` was re-minted each turn from a moving 5-message window, so a new inbound mail could silently repoint `att1` at a different document while the turn-1 text naming it is still replayed. Refs are now `att_<sha1(attachmentId)[0:8]>` — content-derived, either resolves to the same file or doesn't resolve. (5) **SSRF hardening**: `fetchTrustedStorageBytes` validated the requested host then followed redirects unchecked; now `redirect:"manual"` + refuse 3xx. (6) **Size-limit mismatch** (client 100 MB → bucket 25 MB → reader 20 MB) meant a 22 MB upload went green then the worker refused it; both client and the upload route now enforce 20 MB. **PROMPT-INJECTION FENCE (the escalation):** untrusted text — an emailed PDF, a client's upload, and now the raw email transcript itself, which was NEVER fenced — is wrapped by `fenceUntrustedContent()` in `<untrusted-file-content>` with a system-prompt rule that its contents are DATA, never instructions, never approval to send. **This does NOT close the underlying hole:** the Inbox worker runs `enableDbRead` + `enableEmailSend` with a **model-chosen, unpinned `to` address** (only `send_portal_message` has a hard recipient pin). Pinning `send_email`'s recipient to the addresses already on the open thread is REQUIRED before the outbound-attachment work, and is flagged to Antonio. Also flagged, not fixed: the zip decompression bomb in `slack-file-reader.ts` (pre-existing; this change widens its reach to 3 more surfaces). Tests: 91 across `attachment-reader.test.ts`, `email-attachments.test.ts`, `worker-email-attachment.test.ts`; suite 5421 green.)_

_2026-07-10 — Claude (**NO in-dashboard-assistant behavior change**: the in-dashboard `callAgent`/`providers.ts` path already handled image/PDF/CSV/TXT attachments and is untouched.) **New shared module `lib/ai-agent/attachment-reader.ts`** — the transport-agnostic "bytes → something the model can use" layer for the WORKER (`callWorker`) surfaces. Owns: magic-byte image sniffing (`sniffImageMediaType` — bytes beat the declared mimetype, because a `.png` URL or a Slack login-page HTML body will happily lie), `buildImageBlock`, `readAttachmentBuffer` (image → vision block; PDF → text layer, else native document block, else `kind:"scanned"`; everything else → text via the existing pure extractors in `slack-file-reader.ts`, which is NOT reimplemented), `readAttachments(refs, fetchBytes)` (caps: 5 files/turn, 20 MB/file, 5 MB/image, 2 scanned-PDF blocks — every drop is announced in a text note, never silent), `attachmentRefsFromChatRow` (runtime-validates the untyped `attachments` jsonb + legacy `attachment_url`), two fetchers (`fetchTrustedStorageBytes` = public `assets` bucket by URL behind a host allow-list = the SSRF boundary; `fetchWorkerUploadBytes` = PRIVATE `worker-attachments` bucket by PATH via the service key, guarded by `isValidWorkerUploadPath`), and `callWorkerWithAttachments` (retries ONCE without media on a media-shaped 400, reusing the SAME opts object minus `images`/`documents` — the Slack worker hand-rebuilds all 15 flags for its retry, which is one forgotten flag from a silently different second call; do not copy that). **Design decision (both supervisors, recorded so it isn't relitigated): bytes-at-the-door on the user turn, NOT a "go read this image" tool.** A tool the model must choose to call is a tool it will skip, and it then answers about a screenshot it never looked at. `worker-tools.ts` gained `WorkerResponse` export + `PinnedEmailAttachment` + `READ_EMAIL_ATTACHMENT_TOOL` (see `inbox.md`). `AttachmentRead` has a distinct `scanned` variant (file is fine, just no text) so callers that can't carry a PDF block say so honestly instead of reporting a phantom "limit reached". Tests: `attachment-reader.test.ts` (46), `worker-email-attachment.test.ts` (13).)_
_2026-07-09 — Claude (**NO in-dashboard-assistant behavior change**: the SHARED `worker-tools.ts` gained a direct `team_chat_send` tool (`TEAM_CHAT_SEND_TOOL`, posts into the internal Team Workspace AS Claude via `lib/team/post-message.ts`) gated by new `CallWorkerOptions.enableTeamChatSend` — set ONLY by the Slack worker + the @claude Team-Chat trigger. The in-dashboard `callAgent`/`providers.ts` path never sets that flag, so it does NOT receive the new tool; the in-dashboard assistant's existing per-client `send_team_message` AGENT tool (Portal Chats > Team tab) is untouched. Full detail in team-workspace.md (2026-07-09f).)_
_Last verified against code: 2026-07-08d — NO in-dashboard-assistant behavior change: the CRM worker (`/api/inbox/worker-chat`) gained a per-surface SEND rail (Antonio: "the same powerful worker I have in Slack — when I say 'send it' it must send"). Inbox → `enableEmailSend` (threaded email reply); Portal Chats → `enableSlackSend` (portal-chat message). Two safety additions over Slack, in `worker-tools.ts` via a new `WorkerSendContext` (threaded callWorker→runWorkerLoop→executeWorkerTool): (1) **hard-pinned portal recipient** — the Portal Chats send is FORCED to the open client (`pinnedPortalRecipient` from `clientKey`); the executor overrides model-supplied ids so it can never message another client; (2) **per-staff attribution** — every send logs to `action_log` with `sendActor` (`crm-inbox:<email>` / `crm-portal:<email>`). Code-task rail stays OFF (Antonio-only, R111); all non-send actions still go through `propose_action`. Send tools stay OUT of `WORKER_TOOLS` (injected only via the enable flags) so the dormant Hermes worker is unaffected (R108); sending still requires the staff member's explicit "send it" (surface-addenda prompt, generalized from "Antonio" to "the staff member here"). The in-dashboard `callAgent`/`providers.ts` path is untouched. Tests: `slack-portal-send.test.ts` (pin override + actor), `inbox-worker-prompt.test.ts` (per-surface send authorization). See `inbox.md` → "Worker panel" / SEND RAIL. Sandbox blocks real email (SANDBOX_MODE) — email verified by payload; portal send fully testable._
_Prior: 2026-07-08c — NO in-dashboard-assistant behavior change: the CRM worker (inbox panel + Portal Chats Worker tab, `/api/inbox/worker-chat`) reached FULL SLACK PARITY on the read side after Antonio's plan review. (1) MEMORY: every exchange is recorded in `agent_messages` with **sender `crm` / recipient `worker`** (`crm` added to the `agent_message_party` enum by migration 20260709-0200; recipient is NOT `claude` because the dormant Hermes-bridge cron claims any pending `recipient='claude'` row unfiltered — no cron claims `worker`, so the CRM rows stay isolated, "leave the bridge alone"); `agent_messages.thread_id` is a **uuid** column, so the readable scope (`inbox-<mailbox>-<gmailThreadId>` / `chat-<clientKey>`) is hashed to a deterministic UUID by `deterministicThreadUuid` in `lib/ai-agent/inbox-worker-prompt.ts` and kept readable in `context_json.crm_scope_key`; `thread_id` keying keeps `buildThreadContext`/summaries/`recall_conversation` working, `messageId` excludes the current row, raw message kept in `context_json.user_message` for the panel history (GET on the same route restores the conversation on reopen). (2) RAILS: enableDbRead/DocReads/CallReads/Calendly/ClientThreadRead/ThreadRecall/WebSearch(dark) + maxIterations 20 — same read set as the Team Workspace; send/code rails stay OFF (propose_action is the action path). (3) UX: 4-row resizable composer, `components/chat/worker-markdown.tsx` safe renderer (no dangerouslySetInnerHTML)._
_Prior: 2026-07-08b — NO in-dashboard-assistant behavior change: the inbox worker consumer grew a SECOND SURFACE — the **Portal Chats "Worker" tab** (per-CLIENT chat: `components/portal-chats/thread-worker-panel.tsx` → same `/api/inbox/worker-chat` in client mode, threadId `chat-acct-<id>`/`chat-contact-<id>`, `buildWorkerSurfacePrompt('portal-chats')` in `lib/ai-agent/inbox-worker-prompt.ts`). Same shared read-only WORKER_TOOLS + recall + propose_action; Slack-only gated extras still excluded. The portal 🪄 polish button is UNCHANGED (kept as the quick text cleaner)._
_Prior: 2026-07-08 — NO in-dashboard-assistant behavior change: NEW third `callWorker` consumer — the **CRM Inbox "Worker" panel** (Antonio: "the same worker I have in Slack with the same power in inbox"). New `lib/ai-agent/inbox-worker-prompt.ts` composes `SLACK_WORKER_SYSTEM_PROMPT` + an inbox surface override (plain formatting, email-thread context, no sending from this surface); `app/api/inbox/worker-chat` calls `callWorker` with threadId `inbox-<mailbox>-<gmailThreadId>` (persistent per-email-thread memory) over shared read-only `WORKER_TOOLS` (+ recall + propose_action — the Slack-only gated extras `send_portal_message`/code-task/email-send/calendly/doc-reads/call-reads are NOT enabled, so R111 and the R108 gates are preserved). The inbox thread header's old "AI Assist" button (which dispatched `open-ai-agent` to THIS dashboard agent's panel) now opens the Worker panel instead — the dashboard agent itself (`/api/ai-agent`, `callAgent`/`providers.ts`) is unchanged and still reachable from the sidebar. Mailbox-gated via `checkMailboxAccess` (antonio@ threads admin-only). See `inbox.md` → "Worker panel"._
_Prior: 2026-07-07 — Claude (Drive duplicate-upload sweep, LT Program incident class — NO in-dashboard-assistant behavior change: the agent's Gmail attachment-save loop (`lib/ai-agent/tools.ts`) switched from `uploadBinaryToDrive` to `uploadBinaryToDriveUpsert` (stable file name → a retry/re-run refreshes the ONE existing Drive file in place instead of adding a copy). No other behavior in this subsystem touched; full sweep rationale in `documents.md` (2026-07-07b). Branch `claude/objective-cohen-b75f61`, SANDBOX — Antonio ships.)_
_Prior: 2026-07-01 — NO in-dashboard-assistant behavior change: the Slack/Hermes worker's portal-chat readers in `lib/ai-agent/tools.ts` (message list `sender` + inbox `lastBy`) now label staff messages via the shared `staffChatSenderLabel` helper (`lib/portal/chat-sender-name.ts`) → "TD Team" (admin) / "TD Team (auto-reply)" (system), replacing the old `Admin (TD)` / `Admin` literals; the client branch (`sender_context`) is unchanged. Consistency change paired with the MCP `portal_chat_read` fix that stopped rendering an arbitrary tagged member (e.g. "Admin (Gaia Pellegrinelli)") as the author — full root cause in `portal.md`. The in-dashboard `callAgent`/`providers.ts` path is untouched; flagged here only by the coarse `lib/ai-agent/` path mapping. 4343 unit tests green. Sandbox._
_Last verified against code: 2026-06-30 — NO in-dashboard-assistant change: global AI model policy — the shared provider `lib/portal/ai-provider.ts` is now Sonnet/Opus ONLY (no Haiku, no GPT; failover Sonnet→Opus; `temperature` dropped for Opus). The only `lib/ai-agent/` file touched is the Slack worker's lesson-extractor `callAI` in `slack-claude.ts` (`model: "haiku"`→`"sonnet"`). The in-dashboard agent runs its own `providers.ts` (`claude-sonnet-4-6`) path, NOT `callAI` — unaffected. Flagged only by the coarse `lib/ai-agent/` path mapping. See `slack-claude-worker.md` + `offers.md`. Prior same-day: NO in-dashboard-assistant change: `start_code_task`/`promote_code_branch` are now restricted to Antonio's Slack messages only (`enableCodeTasks: slackUserId === SLACK_USER_ANTONIO` in `processSlackEvent`, `slack-claude.ts`) so the worker can no longer launch or ship a Mac Mini coding session off a teammate's (e.g. Luca's) message. The in-dashboard agent never had `enableCodeTasks` set (Slack-only flag, set only by `processSlackEvent`) — unaffected either way. Flagged only by the coarse `lib/ai-agent/` path mapping. See `slack-claude-worker.md` → "Code-task rail". Prior: 2026-06-26 — NO in-dashboard-assistant change: `providers.ts` (`callAgent`/`callClaude`) is untouched and still NOT prompt-cached. Flagged only by the coarse `lib/ai-agent/` path mapping because the Slack/bridge worker engine `worker-tools.ts` gained a dedicated-key override — new pure `resolveWorkerApiKey(override)` + `CallWorkerOptions.apiKeyOverride`, used ONLY by the Slack worker (`SLACK_WORKER_ANTHROPIC_KEY`) for cost isolation; the dashboard agent has no equivalent and is unaffected. See `agent-bridge.md` + `slack-claude-worker.md`. Prior: 2026-06-23 — FOLLOW-ANY-THREAD via 👀 + per-channel canvas (new slack-thread-follows.ts). See client-threads.md. Prior: CLOSE-BUTTON FIX: Close button silently failed (Slack id written to closed_by uuid); now passes null + checks result. See client-threads.md. Prior: CANVAS FIXED: single fixed canvas id, followed-only filter, thread_ts deep links. See client-threads.md. Prior: FULL CARD CONTROL PANEL: Open/Follow/Close/Remove buttons on the 🗂️ card (handleCardAction); shared Canvas dropped. See client-threads.md. Prior: ONE CARD, WORKING LINKS: buildSlackThreadDeepLink uses the workspace subdomain so thread links open; Open+Follow consolidated onto the card. See client-threads.md. Prior: Slack read-method GET fix (getPermalink/conversations.info via slackApiGet); canvas errors now surfaced. See client-threads.md. Prior: 2026-06-22 — SHARED CANVAS + MULTI-CHANNEL + 🗑️ REMOVE: `lib/ai-agent/client-thread-follows.ts` gained `refreshOpenConversationsCanvas` (a #td-support Canvas listing open client conversations; conversations.canvases.create / canvases.edit; needs canvases:write + reinstall), `removeClientThreadCard` (🗑️ → chat.delete + row delete), and the modal gained a channel picker for topic channels. Does NOT affect the in-dashboard assistant; under `lib/ai-agent/`. See `client-threads.md`. Prior: 2026-06-22 — FOLLOW → PERSONAL DM LIST: new `lib/ai-agent/client-thread-follows.ts` adds a per-user "Follow" for client conversations (button on the 🗂️ message → `client_thread_follows`) and a "📌 Following" DM digest (`slack_follow_digests`). Does NOT affect the in-dashboard assistant; logged here only because it's under `lib/ai-agent/`. See `client-threads.md`. Prior: 2026-06-22 — CLIENT-CONVERSATION FORM UX in `slack-claude.ts`: modal submit creates the thread in the background (beats Slack's 3s deadline), a one-click "Open conversation" ephemeral button + clearer root wording, and `getSlackPermalink` (`chat.getPermalink`) replaces bare archive links that gave "no access" on thread parents. Does NOT affect the in-dashboard assistant (separate `callAgent`/`providers.ts` path); logged here only because the files are under `lib/ai-agent/`. See `client-threads.md` + `slack-claude-worker.md`. Prior: 2026-06-21 — SELF-SERVE BEFORE ASKING discipline added to `SLACK_WORKER_SYSTEM_PROMPT` (`slack-claude.ts`): the Slack worker must look facts up in the system (CRM record / run_sql_query / KB/SOPs/sysdocs) before asking Antonio. Does NOT affect the in-dashboard assistant (separate prompt + `callAgent`/`providers.ts` path); logged here only because the file is under `lib/ai-agent/`. See `slack-claude-worker.md` → "SELF-SERVE BEFORE ASKING". Prior: 2026-06-19 — DRAFT SANITIZER (Slack-worker-only) in `worker-tools.ts`: new pure `stripDraftMarkdown()` strips markdown/asterisks from client drafts, applied in `executeWorkerTool` on the Slack worker's `send_email` + `send_portal_message` paths. This does NOT affect the in-dashboard assistant: it sends via the shared `send_email` AGENT tool (`executeTool`), which the worker dispatch sanitizes BEFORE delegating — but the dashboard agent calls that shared tool directly, unsanitized. Logged here only because the change is under `lib/ai-agent/`. See `slack-claude-worker.md` → "Human-tone client drafts". Prior: 2026-06-19 — STEP-LIMIT CONVERGENCE in `runWorkerLoop` (`worker-tools.ts`): on loop exhaustion the shared worker loop now makes one final NO-TOOLS synthesis call instead of returning the generic "working limit" message. NOTE — this does NOT affect the in-dashboard assistant: that path runs `callAgent`→`callClaude`/`callOpenAI` in `lib/ai-agent/providers.ts`, NOT `runWorkerLoop` (only the `callWorker` consumers share that loop — the Slack worker, portal-chat-suggest, and the dormant `hermes-bridge` code path; Hermes itself is a DISMISSED agent, not actively used). Logged here only because the change touches a file under `lib/ai-agent/`; the dashboard agent's behavior is unchanged. See `agent-bridge.md` / `slack-claude-worker.md` → "Step-limit convergence". Prior: 2026-06-18 — CALENDLY READ TOOLS (Slack-only, R108): `worker-tools.ts` gained three read-only Calendly tools — `cal_list_bookings` / `cal_get_event_details` / `cal_get_availability` — reusing SHARED fns extracted from `lib/mcp/tools/calendly.ts` (`listCalendlyBookings` / `getCalendlyEvent` / `getCalendlyAvailability`; the MCP `cal_*` tools call the same fns). Gated by `CallWorkerOptions.enableCalendly` (set in `processSlackEvent`, kept OUT of `WORKER_TOOLS`) + an executor `availableNames` gate, so the Hermes/Telegram research worker never gets them. Read-only — no create/cancel; token `CALENDLY_PAT`. Tests `tests/unit/worker-calendly-reads.test.ts`. See `slack-claude-worker.md` → "Calendly read rail". Prior: 2026-06-18 — PROPOSAL→ORIGIN LINKAGE: `proposeAction` (`worker-tools.ts`) now stores `source_message_id` (FK→`agent_messages.id`), threaded server-side via `callWorker`→`runWorkerLoop`→`executeWorkerTool` (the model never supplies it). Lets an approval surface map a queued proposal back to the conversation that produced it — used by the Slack worker's in-channel approval-code completion (see `slack-claude-worker.md` → "In-channel approval completion"). Additive / backward-compatible (NULL when unknown). Prior: 2026-06-18 — AUTO-RECALL EXTENDED TO THE IN-DASHBOARD AGENT: the pre-loop recall is now factored into `buildAutoRecallSuffix(query)` (`worker-tools.ts`) and called by BOTH `callWorker` AND the in-dashboard agent's `callClaude`/`callOpenAI` (`providers.ts`, keyed off the latest user message via `lastUserText`). So the dashboard assistant now also injects the "RELEVANT PAST LESSONS" block — previously it recalled `decision_memory` only if the model chose `memory_recall` (`loadGlobalMemories` reads the SEPARATE `agent_memory` table, not `decision_memory`). This CORRECTS the prior entry's "ALL worker paths" wording: the covered set is the three `callWorker` consumers (Slack worker, Hermes bridge, portal-chat-suggest) PLUS, now, the dashboard `callAgent` path. Same `AUTO_RECALL_THRESHOLD=0.45` / `AUTO_RECALL_COUNT=3`; best-effort. `buildAutoRecallSuffix` empty-query case added to `tests/unit/worker-auto-recall.test.ts`. Prior: 2026-06-18 — AUTO-RECALL (Decision Memory, ALL worker paths): `callWorker` (`worker-tools.ts`) now PROACTIVELY recalls the most relevant past lessons BEFORE the model loop and injects them into the system prompt as a "RELEVANT PAST LESSONS" block — instead of relying on the model to call `memory_recall` itself, which it almost never did (production had ~70 saved lessons but only 3 recalls ever, 0 on the Slack path, because `SLACK_WORKER_SYSTEM_PROMPT` lacks the base prompt's "recall before deciding" rule). New consts `AUTO_RECALL_THRESHOLD=0.45` + `AUTO_RECALL_COUNT=3` and a pure exported `formatRecalledLessons()` (unit-tested, `tests/unit/worker-auto-recall.test.ts`); recall via `recallDecisionMemory(userBody, …)`; best-effort try/catch so a missing `OPENAI_API_KEY` / embedding error never fails a reply. 0.45 < the 0.7 explicit-recall default, set from measured prod neighbors (related cluster 0.45–0.55, unrelated tail <0.4). The `memory_recall` tool stays available for explicit/deeper lookups. Shipped to `main` via cherry-pick, independent of the formation branch. Prior: 2026-06-18 — DOC-SOURCE READING (Slack worker): `worker-tools.ts` gained four Slack-only read tools — `search_sysdocs` / `read_sysdoc` / `search_sops` / `read_drive_file` — giving the worker read parity with the internal sources Claude Code can read (sysdocs incl. `session-context`, SOPs by topic, Drive file text). Gated by `CallWorkerOptions.enableDocReads` (set only in `processSlackEvent`; kept out of `WORKER_TOOLS`) + an executor `availableNames` gate, so the Hermes/Telegram research worker never gets them (R108). Built after the worker said "the KB has nothing" while the installment-billing rule lived in a sysdoc it couldn't reach. New SOURCES prompt block; prompt ceiling 8800→9600; tests `tests/unit/worker-doc-reads.test.ts`. See `slack-claude-worker.md` → "Doc-source reading rail". Prior: 2026-06-18 — ANTI-DOUBLE-DOWN + NOUN→TOOL prompt rules in `SLACK_WORKER_SYSTEM_PROMPT` (slack-claude.ts): when corrected, re-check with a DIFFERENT tool + recount (don't re-run the same query and re-assert); recount against the rows actually pulled; match the noun to its dedicated tool ("offers" = use_tool(offer_list), not lead/deal stages). Added after a live offers-vs-leads miscount (worker said 8, real 15). Prompt ceiling 8000→8800. Prior: 2026-06-18 — ENGINEERING-DISCIPLINE prompt block in `SLACK_WORKER_SYSTEM_PROMPT` (slack-claude.ts, always on): never assume/invent; CHECK before claiming a tool/capability doesn't exist; challenge the first answer + show both sources on conflict; flag unverified. Plus a `find_tool`-first nudge in the FULL TOOL REACH block. Prompt-bloat ceiling test 7200→8000. Prior: 2026-06-17 — FLEXIBLE ACTION SURFACE (Slack-only, default OFF via env `ASSISTANT_FULL_REACH_ENABLED`): the worker gained `find_tool` + `use_tool` over a generic MCP bridge (`lib/ai-agent/mcp-bridge.ts` — `buildMcpToolRegistry()` capture-shim → `runToolByName`, ~215 tools callable by name, param re-validation) gated by a risk classifier (`lib/ai-agent/tool-risk.ts` — READ auto-runs / WRITE+EXTERNAL → approval / blocked refused; fail-safe = unknown→approval; unit-tested). `proposeAction` gained opt-in `opts.allowBridgeTools` so the SHARED rail / propose_action(Hermes) is unchanged unless `use_tool` opts in; `approval-executor` now runs approved rows via `runToolByName` (DYNAMIC import to avoid a cycle). Worker loop is now TIME-AWARE (`WORKER_WALL_CLOCK_BUDGET_MS`=250s + pure `callTimeoutForBudget()` so a late call can't overrun the 300s function cap; Slack `maxIterations` 12→20). See `slack-claude-worker.md`. Prior: 2026-06-17 — CALL-READING (Slack worker): `worker-tools.ts` gained three Slack-only read tools — `list_calls` / `get_call` / `search_calls` — over Circleback's `call_summaries`. `get_call` returns the FULL call (notes + action items + attendees + the complete word-for-word transcript, capped only by `CALL_RESULT_CAP`=120k chars), unlike the MCP `cb_get_call` 50-turn preview (left untouched). Gated Slack-only via `CallWorkerOptions.enableCallReads` (kept out of `WORKER_TOOLS`) + an executor-level `availableNames` gate in `executeWorkerTool`, so the Hermes/Telegram research worker never gets call transcripts (R108); `processSlackEvent` sets the flag + a CALLS prompt block. Action-item assignees render by name (the Circleback `assignee` can be a `{name,email}` object — shown as the name, not `[object Object]`; same fix applied to the MCP `cb_get_call` tool). Pure renderer `renderCallDetail()` + the gate are unit-tested (`tests/unit/worker-call-reads.test.ts`). See `slack-claude-worker.md` → "Call-reading rail". Prior: 2026-06-16 — PROMPT CACHING in the worker loop (`runWorkerLoop`, `worker-tools.ts`): the `system` prompt is now sent as a cached content block (`cache_control: {type: "ephemeral"}`), so the stable tools+system prefix re-sent on every tool-use iteration is read from cache (~0.1× input price) instead of re-billed — sandbox-verified via `usage.cache_read_input_tokens` (5,949-token prefix written iter 0, read iter 1). Model stays `claude-sonnet-4-6`. The in-dashboard agent (`providers.ts`) is NOT yet cached (follow-up). Prior: 2026-06-15 (b) — the `send_email` tool's HTML now uses the Tony Durante **banner logo** (`public/images/tony-logos.png`, hosted at `${APP_BASE_URL}/images/...`) in a branded header, a sender-based sign-off (support@ → "The Tony Durante LLC Team", antonio@ → "Antonio Durante"), and a contact footer — built inline in `sendEmail`, scoped to the worker's email (other emails / the shared brand shell unchanged). Prior: 2026-06-15 — the shared `send_email` tool (`AGENT_TOOLS` / `lib/ai-agent/tools.ts`) gained a `from` param for sender selection: `support` (support@tonydurante.us, default) or `antonio` (antonio.durante@tonydurante.us); the threading read (`gmailGet`) and the send (`gmailPost`) both run as the chosen mailbox so a `reply_to_message_id` reply stays in the thread that lives in that inbox. Exposed to the Slack worker only via the gated `SEND_EMAIL_TOOL` (`enableEmailSend` + an executor-level gate) — see `slack-claude-worker.md` / `agent-bridge.md`. `SANDBOX_MODE=1` blocks the actual send (`lib/gmail.ts`). Prior: 2026-06-14 — the gmail read tools (`gmail_search`/`gmail_read`/`gmail_read_thread` in `AGENT_TOOLS`) gained an optional `as_user` mailbox param, allow-listed via `lib/ai-agent/gmail-mailbox.ts` (`resolveMailbox` — env `GMAIL_WORKER_ALLOWED_MAILBOXES`, default `support@` + `antonio.durante@`). So all workers (Slack, Hermes, dashboard) can read Antonio's personal inbox on request, but a non-allow-listed mailbox is refused (prompt-injection guard); default mailbox unchanged (`support@`). Prior: 2026-06-13 (c) — the shared `worker-tools.ts` gained a Slack-only `promote_code_branch` tool (gated by `enableCodeTasks`, not in `WORKER_TOOLS`; neither the in-dashboard agent nor the Hermes worker receives it) for the code-task "ship it" promotion — see `slack-claude-worker.md`. Earlier 2026-06-13 — added 4 tools to `AGENT_TOOLS`: reads `portal_chat_inbox` + `portal_chat_read` (full portal thread/inbox) and actions `update_deadline` + `send_team_message` (both on the approval rail). The reads also joined the bridge worker's read surface + thread-routing CRM reads; `recall_memories` was added to the worker read allow-list (closing the save-but-can't-read asymmetry). See `agent-bridge.md`. Prior: 2026-06-10 — shared `callWorker` gained `systemPromptOverride` for the Slack worker consumer (see slack-claude-worker.md)._

## What it is
A built-in AI chat assistant **inside the CRM dashboard** for staff — it can search the CRM, read Gmail/Drive, and take actions through its own tool set. This is **separate** from Claude Code and the Claude.ai MCP connector: it has its **own** tool definitions (`lib/ai-agent/tools.ts`), not the MCP server's ~217 tools.

## How it works
- **Endpoint:** `POST /api/ai-agent`, body `{ messages: [...] }`, returns `{ content, provider, tools_used }`. Rate-limited to **20 requests/min**.
- **Access:** **admin** (Antonio) always; **team** members only if `app_settings.ai_agent.enabled_for_team = true`; **clients are blocked**.
- **Providers** (`lib/ai-agent/providers.ts`): **Claude `claude-sonnet-4-6` (primary, `ANTHROPIC_API_KEY`)** with **GPT-4o (fallback, `OPENAI_API_KEY`)**. `callAgent()` runs the tool-use loop for whichever provider is configured (Anthropic first). Note: direct provider keys, not the Vercel AI Gateway.
- **Max tool-use iterations** (2026-06-05): the loop ceiling defaults to **`AGENT_MAX_TOOL_LOOPS` env var, then 8** (was a hardcoded `MAX_TOOL_LOOPS = 8`). `callAgent`/`callClaude`/`callOpenAI` accept an optional `maxIterations` per-request override; the max-iteration error string now reports the actual limit. The **same env var** governs the Hermes-bridge worker (`worker-tools.ts`) — see `agent-bridge.md`.
- **System prompt:** `lib/ai-agent/system-prompt.ts` (`SYSTEM_PROMPT`) — embeds business knowledge + instructions.

## The agent's tool set (`lib/ai-agent/tools.ts`)
~38 tools, each a schema + execute function over real Supabase tables:
- **Read:** `search_accounts/contacts/leads/deals/payments/services/tasks/tax_returns/deadlines/portal_messages/kb`, `get_account_detail`, `get_dashboard_stats`, `get_sop`, `run_sql_query`, `portal_chat_inbox`, `portal_chat_read` (full portal thread / inbox with unread counts).
- **Write/act:** `create_task`, `update_task`, `update_contact`, `update_account_notes`, `update_service`, `advance_service_stage`, `send_email`, `update_deadline`, `send_team_message` (internal staff-only note — never client-visible).
- **Gmail/Drive:** `gmail_search/read/read_thread/get_attachments`, `drive_search/list_folder/move/upload_file`, `preview_attachment`.
- **Conversation/memory:** `log_conversation`, `save_memory`, `recall_memories` — the agent has **persistent memory** (the "Antonio Brain" — see sysdoc `antonio-brain-architecture`).

## Sibling consumer — Hermes-bridge worker (`lib/ai-agent/worker-tools.ts`, 2026-06-03)
`lib/ai-agent/` now also hosts a **second, separate** model caller for the Hermes ↔ Claude bridge. It is **not** the in-dashboard assistant:
- `WORKER_TOOLS` = a curated **read-only** subset of `tools.ts`'s `AGENT_TOOLS` (search/get/read only — no write/send, and `run_sql_query` is intentionally excluded). `executeWorkerTool()` hard-rejects any name outside the allow-list (defense-in-depth).
- `callWorker()` mirrors `callClaude()` but uses that subset + `WORKER_SYSTEM_PROMPT` (sonnet-4-6). It deliberately does **not** reuse `callAgent()` (which hardcodes the full `AGENT_TOOLS` + dashboard prompt).
- It is driven by the cron worker at `app/api/cron/hermes-bridge`, not `/api/ai-agent`. Full subsystem doc: `agent-bridge.md`.
- **Second `callWorker` consumer — Slack worker (2026-06-10):** `callWorker` gained an optional `systemPromptOverride` so a different caller can swap the base system prompt. `lib/ai-agent/slack-claude.ts` uses it to run a conversational, discuss-first Slack persona (`SLACK_WORKER_SYSTEM_PROMPT`) over the same read-only `WORKER_TOOLS`, driven by `app/api/cron/slack-claude-worker` + the `/api/webhooks/slack-claude` Events API handler. Full subsystem doc: `slack-claude-worker.md`.
- **Phase 2 Slice 1 (2026-06-04):** `WORKER_TOOLS` now also includes **`propose_action`** (the one non-read tool — it only QUEUES, never executes). It validates against the allow-list + schema in `lib/ai-agent/approvable-tools.ts` (`APPROVABLE_TOOL_NAMES`, `computeParamsHash`, `validateToolParams`, `APPROVABLE_TOOL_CONSTRAINTS`) and INSERTs a `pending` row into `approval_queue`. `propose_action` is deliberately **not** in `WORKER_READ_ONLY_TOOL_NAMES`. See `agent-bridge.md` for the full rail.
- **Phase 2 Slice 2 (2026-06-04):** `executeTool()` (this file's full dispatcher) is now the **execution path** for approved proposals. `lib/ai-agent/approval-executor.ts` claims an approved `approval_queue` row, re-checks its `params_hash`, then calls `executeTool(tool_name, params)` for real (kill-switch-gated by `APPROVAL_RAIL_ENABLED`); `lib/ai-agent/approval-callback.ts` writes the outcome back to Hermes via `agent_messages`. Note `executeTool` catches its own errors and returns an `{error:…}` string rather than throwing — the executor inspects the result so a logically-failed action is recorded `failed`, not `executed`. Run by `app/api/cron/approval-executor`; decided via the `approval_decide` MCP tool. See `agent-bridge.md`.
- **Phase 2 Slice 4 (2026-06-04):** `lib/ai-agent/format-approval-proposal.ts` — a **pure, DB-free** `formatApprovalProposal(row)` that renders an `approval_queue` row into a mobile-friendly Telegram message (header + 8-char short id, tool label, surfaced params, risk flags, rationale, APPROVE/REJECT lines). Label / surfaced params / risk flags all derive from `APPROVABLE_TOOL_CONSTRAINTS` (single source of truth) — no per-tool hardcoding; unknown tools fall back to the raw name + all params. Formats only — never approves/executes. Used Hermes-side to present pending proposals from `approval_list`. Tests: `tests/unit/format-approval-proposal.test.ts`. See `agent-bridge.md`.
- **Phase A (2026-06-04):** `WORKER_TOOLS` now also includes **`codebase_read` + `codebase_search`** (read subset **+ propose_action + 2 codebase tools**; the worker-tools test pins `size + 3`). These are NOT `AGENT_TOOLS` — they're the read/search bodies extracted from `lib/mcp/tools/codebase-read.ts` into shared pure functions (`readCodebaseFile`/`searchCodebase`; 100KB cap, env/secrets/deps blocked, binary refused). `executeWorkerTool` dispatches them directly (never via `executeTool`). `proposeAction` also gained an optional `thread_id` (persisted on the `approval_queue` row, NULL when omitted; not in the model-facing schema). New pure health helper `lib/ai-agent/hermes-health.ts` (`isInstanceStale`/`selectStaleOnline`) backs the `/api/cron/hermes-health` monitor. See `agent-bridge.md` for the full Phase A write-up.
- **WP1 (2026-06-04):** `proposeAction` now mints a 6-digit `confirmation_code` (`generateConfirmationCode()`) on every fresh insert and stores it on the `approval_queue` row; `format-approval-proposal.ts` renders a `🔑 Code` line and `APPROVE <short-id> <code>`. The approve gate itself lives in the `approval_decide` MCP tool (`lib/mcp/tools/agent-approvals.ts`), which also adds the Operating-Agent pull rail (`hermes_heartbeat` / `approval_claim` / `approval_complete`) — none of which go through this file's `executeTool`. See `agent-bridge.md` WP1 for the full rail.
- **WP3 (2026-06-05) — Mac Mini executor:** new module `lib/ai-agent/telegram-notify.ts` (`sendTelegramApprovalNotification`, best-effort/never-throws/skips-without-token) is called from `proposeAction` (this file) right beside the Phase B `sendApprovalNotification`, so a fresh proposal is pushed straight to Antonio's Telegram. `lib/ai-agent/approval-executor.ts` gains `executeClaimedRow(id)` (the engine behind the new `approval_execute` MCP tool — reuses `executeTool`/`interpretToolResult`/`computeParamsHash`/`emitApprovalOutcome` with a guarded `WHERE status='executing'` finalize stamping `executed_by`) and the pure `serverShouldBackstop(row, instanceRow, now)` (server is now BACKUP: `runExecutorScan` defers approved rows to the Mac Mini unless it's stale/offline or a row strands past the grace window). The instant-fire on approve was removed. `executeTool` (this file) is still the execution path for both the Mac Mini (`approval_execute`) and the server backup, gated by the ONE master switch `APPROVAL_RAIL_ENABLED`. See `agent-bridge.md` WP3.
- **WP2 (2026-06-05):** thread/investigation wiring — this file is only lightly touched. `createThreadSummary(...)` (`lib/ai-agent/thread-summaries.ts`) gained a 5th `accountsAffected?` arg → `thread_summaries.accounts_affected` (the two existing `callWorker` callsites pass 4 args → NULL, unchanged). The new `thread_create` MCP tool reuses this helper + `WORKER_PROMPT_VERSION` to open a typed thread; `callWorker`'s threaded path (Phase C, below) is unchanged and reuses a pre-created thread row without overwriting it. See `agent-bridge.md` WP2.
- **Phase D (2026-06-04):** three foundations on `worker-tools.ts`. (a) `WORKER_PROMPT_VERSION` = SHA-256 of the base `WORKER_SYSTEM_PROMPT` (computed once at load), passed into `createThreadSummary(...)` so `thread_summaries.prompt_version` records what base instructions the worker had. (b) `proposeAction` stamps `env: currentApprovalEnv()` (`lib/ai-agent/approval-env.ts` — `APPROVAL_ENV ?? NODE_ENV ?? 'production'`) on every queued row, and the executor filters by env at all four query sites — the staging-lane foundation, inert until `APPROVAL_ENV` is set. (c) `proposeAction` accepts an optional `batch_id`; `batchPropose(proposals, {batch_id?})` mints one id across a group (each still validated/decided individually). See `agent-bridge.md` Phase D.
- **Phase C (2026-06-04):** `callWorker` gained an optional `{ threadId, messageId }` — with no `threadId` it is byte-for-byte the old behaviour (full `WORKER_TOOLS` + default prompt). With a `threadId` it narrows the tool list via `getToolsForThreadType` (`lib/ai-agent/thread-routing.ts`), prepends prior-turn context from `buildThreadContext` (`lib/ai-agent/thread-context.ts`, last-20 watermark), appends a per-type prompt addendum, and after the reply calls `resolveThread` (`lib/ai-agent/thread-summaries.ts`) with a derived outcome + one-paragraph summary — all best-effort (a thread-layer failure never breaks the reply). The raw sonnet tool-loop is extracted into `runWorkerLoop(body, tools, systemPrompt)` so the tools/prompt can be swapped per thread. Note thread-routing ↔ worker-tools is a deliberate, call-time-deferred import cycle. See `agent-bridge.md` Phase C.
- **Phase B (2026-06-04):** the approval-outcome path is now centralized in `lib/ai-agent/approval-notifications.ts`. `emitApprovalOutcome` (used by every terminal site in `approval-executor.ts` + the `approval_decide` reject path) writes the Hermes `agent_messages` callback, flips `approval_queue.notification_sent` only on callback success, and mirrors the outcome to the CRM "Team" tab (`internal_threads`/`internal_messages`) via best-effort `sendApprovalNotification` (the executor can't call the `portal_team_send` MCP tool, so it writes those tables directly). `runNotificationSweep` (run at the tail of the executor scan) retries any terminal row still `notification_sent=false`. `worker-tools.ts::proposeAction` now fires a best-effort propose heads-up. `format-approval-proposal.ts` gained `formatApprovalOutcome` + `formatProposeNotification` (same constraint-driven surfacing). See `agent-bridge.md` for the full Phase B write-up.

## Business rules
- **Staff-only** — clients can never use it; non-admin staff need the `app_settings.ai_agent.enabled_for_team` toggle.
- **20 req/min** rate limit per caller.
- It is a **distinct tool surface** from the MCP server — the two tool sets are maintained separately.

## How it's built — key files & tables
- Files: `app/api/ai-agent/route.ts` (entry + access + rate limit), `app/api/ai-agent/attachment-preview/route.ts`, `lib/ai-agent/{providers,system-prompt,tools}.ts`. Background/architecture: sysdoc `antonio-brain-architecture`.
- Tables: `app_settings` (`ai_agent` toggle), `conversations` (logging), the agent-memory store (`save_memory`/`recall_memories`), plus whatever the tools read/write.

## Enum normalization (`lib/ai-agent/enum-normalization.ts`, 2026-06-04)
Several tool params/filters map to real Postgres ENUM columns (`task_priority`, `task_status`, `task_category`, `service_status`, `conversation_channel`, plus the search-filter enums `account_status`, `payment_status`, `deal_stage`, `lead_status`, `tax_return_status`). An invalid value **throws `22P02` on a write** and **silently returns zero rows on a search**. This module is the single source of truth that maps flexible input (any casing + common synonyms: `medium→Normal`, `todo→To Do`) to the exact canonical DB value, or `null` if unrecognized.
- **Canonical value sets** are verified against `pg_enum` and mirror the migration DDL — keep them in sync if an enum changes.
- **Write paths** (`createTask`, `updateTask`, `updateService`, `advanceServiceStage` auto-tasks, `logConversation`) normalize before insert: `createTask` now defaults `priority→'Normal'` and `category→'Internal'` (the old literals `'medium'`/`'Admin'` were **not valid enum members** and made any category-less create_task throw); `updateTask`/`updateService` return a clear error on an unrecognized enum instead of letting the DB throw.
- **Search filters** normalize then **fall back to the original value** on no-match (an exact match returning nothing is harmless — no behavior regression).
- **Propose path** (`worker-tools.ts::proposeAction`) calls `normalizeToolParams()` BEFORE `validateToolParams` + `computeParamsHash`, so a proposal with `medium`/`todo` is accepted and the stored params (and hash) reflect exactly what executes. The schema `enum:` arrays in `AGENT_TOOLS` are the corrected canonical values (`['Low','Normal','High','Urgent']`, etc.) so propose-time validation still rejects genuine garbage.

## Gotchas, invariants & past bugs
- **Two different tool worlds** — the AI agent's `lib/ai-agent/tools.ts` (~34 tools) is NOT the MCP server's ~217 tools. A change in one doesn't affect the other; adding an MCP tool does NOT give the dashboard agent that capability (and vice-versa).
- **Never re-introduce hardcoded enum literals in `tools.ts`** — route every enum-backed param/filter through `enum-normalization.ts`. The `create_task` `'medium'`/`'Admin'` defaults were a live break (invalid enum members → insert threw). Canonical values live in one place by design.
- **`tasks.attachments` is `jsonb NOT NULL` with no DB default** — every task insert MUST supply `attachments: []` (see `lib/operations/task.ts`). `createTask` and `advanceServiceStage`'s auto-task insert omitted it and threw `23502` on every real insert; mocked unit tests can't catch a NOT-NULL violation, so a payload-shape regression test (`tests/unit/agent-tools-insert-shape.test.ts`) pins it. Surfaced by the Slice 3 sandbox rail E2E (`scripts/test-approval-rail-s3.ts`).
- **`advance_service_stage` takes a `service_deliveries.id`, not a `service_id`** — `service_deliveries` has no `service_id` column; the agent gets the id from `search_services`/`get_account_detail` (both expose `service_deliveries.id`). The lookup filters `.eq('id', …)`. The old `.eq('service_id', …)` errored on a nonexistent column, so the tool never worked. Don't revert.
- **Provider fallback can change behaviour** — Claude (sonnet-4-6) primary, GPT-4o fallback; outputs/tool-use may differ if it falls back. `provider` is returned in the response so you can tell which ran.
- **`run_sql_query` runs against the deployment's DB** — staff-only, but on production it hits production. Treat it like any production query.
- **Agent memory is separate** from `session_checkpoints` and from Claude Code memory — it's the dashboard agent's own `save_memory`/`recall_memories` store.
- Model `claude-sonnet-4-6` is hardcoded in `providers.ts` — a model change is a code edit there.

## How to verify current state
- Read `lib/ai-agent/providers.ts` (the models + provider order), `lib/ai-agent/tools.ts` (the tool list), `app/api/ai-agent/route.ts` (access gate + rate limit).
- Team access state: `SELECT value FROM app_settings WHERE key='ai_agent';`
- Note (R096): sandbox via sandbox MCP / `psql`; production `execute_sql` hits production.

---
_2026-06-21 — Client Threads (Phase 1, dev_task 54f89912): added the Slack-only `tag_client_thread` (write) + `find_client_threads` (read) tools in `lib/ai-agent/worker-tools.ts` (gated via `enableClientThreadTag`/`enableClientThreadRead`, kept OUT of `WORKER_TOOLS`, R108) and wired the `#td-support` auto-tag in `lib/ai-agent/slack-claude.ts::processSlackEvent`. Full subsystem doc: [client-threads.md](client-threads.md)._

---
_2026-06-21 (Phase 2) — Client-conversation form: a pinned button in #td-support opens a Block Kit modal (client external_select + topic) that starts a labeled, tagged thread; the worker is grounded with the client/topic and each exchange is logged to the CRM. New helpers + interactivity-route handlers in slack-claude.ts / slack-interactions route. See [client-threads.md](client-threads.md)._

_2026-06-21 (Phase 2.1) — modal now supports "Or type a new topic": optional free-text field; `ensureTopicSlugFromText` slugifies it and adds it to the topic_templates catalog (reusable next time). Script `post-client-conversation-button.mjs` hardened with an auth.test guard (refuses any non-Claude bot token)._

_2026-06-21 (Phase 2.2) — added a GLOBAL SHORTCUT entry point ("new_client_conversation") so the form opens from the ⚡ shortcuts menu (always available, never scrolls away) instead of relying on an in-channel button. Global shortcut has no channel → defaults the new thread to SLACK_SUPPORT_CHANNEL_ID. Route handles type="shortcut"; openConversationModal shared by button + shortcut._

_2026-06-21 (Phase 1b) — per-entity COLLAPSIBLE Conversations panel on contact/account/lead pages (no rollup: a thread shows only under the entity it was tagged to). Reads client_threads for that entity; each row = topic · date · Slack link; expand pulls the thread messages LIVE from Slack (fetchSlackThreadMessages → conversations.replies). New: /api/client-threads (list) + /api/client-threads/[id]/messages (expand), components/conversations/client-conversations-panel.tsx._

_2026-06-21 (Phase 1b polish) — cleanSlackText() renders thread messages readable in the CRM panel (resolves <@user> mentions to names, strips :emoji: shortcodes + *bold*/_italic_/`code`, unwraps <url|label>). Backfilled crm_log rows now expand to their stored conversations content (client_message/response_sent) instead of "no thread"._

_2026-06-22 (Phase 2.3) — CLOSE a conversation (frozen transcript). New client_threads cols transcript/closed_at/closed_by (migration 20260622-0900). closeClientThread snapshots the full thread into transcript + status=closed; reopenClientThread reverts to live. Close from CRM (Close/Reopen button in the panel → /api/client-threads/[id]/close) OR from Slack (✅ reaction on the conversation's starting message). Closed → messages endpoint serves the frozen snapshot (permanent, survives Slack deletion)._

_2026-06-22 (Phase 2.4) — propose-continue dedup: the form, on submit, checks for an existing OPEN conversation for the same client+topic (findOpenConversationForEntityTopic). If found, it updates the modal (buildDuplicateConfirmView) showing "already open, opened <date>" + a link to continue there, or "Start new anyway" (carries the selection in private_metadata confirm:true → second submit skips the check and creates). Prevents form+@Claude duplicates; one open conversation per client+topic._

_2026-06-22 (Phase 2.5) — /client SLASH COMMAND: new app/api/webhooks/slack-commands route opens the same client-conversation modal by typing /client (reliable entry point since the global shortcut is hard to find). Shared openClientConversationModal helper now used by all three entry points (button, shortcut, slash command); interactions route refactored to it._

_2026-06-22 (Phase 3 Part 1+3) — per-client brain. decision_memory gets a client_key + a sibling recall fn match_decision_memory_client (original untouched). saveDecisionMemory accepts clientKey; recallClientDecisionMemory + buildClientRecallSuffix inject "WHAT WE KNOW ABOUT <client>" before the worker answers in a tagged client thread (callWorker opts.clientKey/clientName, set by processSlackEvent). FEED (human-confirmed only, no poisoning): closing a conversation saves its transcript as a client-scoped memory (closeClientThread). Migration 20260622-1100._

_2026-06-22 (Phase 2.6) — /client conversation threads are now dedicated worker threads: a plain reply (no @Claude) in an OPEN client_threads thread is processed by the worker (route.ts isOpenClientConversationThread relaxes Case 3 of the invitation gate; Case 2 explicit @other still skips; closed threads not auto-answered). Labeled message updated to "Just reply here — I am listening". Fixes having to @Claude every message + drifting to separate untagged threads._
