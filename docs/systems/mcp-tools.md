# MCP Tool Server
_Last verified against code: 2026-09-05 — Claude (**Two hardening fixes to the 2026-09-04 multi-key change (entry below), from a full Council security review before shipping — 5 core reviewers + a Security specialist, GO with zero blockers, but three of the six independently converged on the same real gap.** (1) `parseAdditionalStaticKeys()` now DROPS any `TD_MCP_ADDITIONAL_KEYS` entry whose email is the owner's own (case/whitespace-insensitive) — accepting one would have created a second, untracked credential for full owner access that rotating `TD_MCP_API_KEY` does nothing to revoke. (2) It also drops any entry with a blank/whitespace-only email, fixed at the parse source rather than relying on the route's truthy check downstream. Both are inert today — `TD_MCP_ADDITIONAL_KEYS` is still unset everywhere — pure hardening for the day it isn't. **Known, deliberately-deferred gap (Bug Hunter finding, not fixed today):** the actual route-level wiring for the new branch (`app/api/[transport]/route.ts`'s `withAuth`) has no test that constructs a real HTTP request — every test, before and after this entry, hand-builds the auth context and bypasses the route. The underlying logic is proven correct by the unit tests plus a live curl test of the primary-key path against the real deployed sandbox server plus three independent reviewers manually tracing the code — but a genuine route-level integration test remains open for a future session. **System Counselor confirmed live in production (2026-09-05):** exactly one operator (Antonio) has a static key today; Luca's real system access runs through a wholly separate mechanism (CRM dashboard + AI worker, never `TD_MCP_API_KEY`) untouched by this change; the private Drive folder this protects is still live and load-bearing — actively read by this same session's own bank-statement import work for "My Finances." Tests: `mcp-auth-context.test.ts` now 21 (4 new). Build and full suite clean (one pre-existing, documented-flaky, unrelated test excluded).)_
_Prior: 2026-09-04 — Claude (**Static-key auth now resolves a real person, not "any shared key = Antonio" (dev job `3c8780fd`).** A security review found that the 2026-07-29 AUTH-METHOD context (entry below) treated `{method:"static"}` alone as meaning Antonio for every identity-sensitive check — `callerIsOwner()` (gates `drive_search`'s reach into his private accounting folder) and `actingEmailForTeamChat()` (who a team-chat send is credited to). That was harmless while `TD_MCP_API_KEY` was the only static key ever issued; the day a second person's own key existed, their requests would silently inherit Antonio's own Drive access and be misattributed as him in team chat. **Key roster, current:** `TD_MCP_API_KEY` is Antonio's own key (`resolvePrimaryStaticKeyEmail()` in `lib/mcp/auth-context.ts`, rotatable via `MCP_TEAM_CHAT_ACTOR_EMAIL` exactly as before — zero behavior change for him). `TD_MCP_ADDITIONAL_KEYS` is an optional JSON map (`{"email": "their own secret key"}`) for anyone else who gets their own Claude Code session against this server — **empty today; nobody else has one.** The route now resolves a specific email for EVERY static-key request before the auth context is ever built, so `{method:"static"}` carries a real identity the same way `{method:"oauth"}` always has, and `callerIsOwner()`/`actingEmailForTeamChat()` check that resolved identity with ONE shared code path instead of a per-method branch. Fully backward compatible: with `TD_MCP_ADDITIONAL_KEYS` unset (today's actual state), the primary key resolves exactly as it always did. Tests: `mcp-auth-context.test.ts` (17, new), plus two pre-existing test files (`owner-drive-access.test.ts`, `drive-search-owner-gating.test.ts`) updated to construct a static context with its now-required email rather than the old bare `{method:"static"}` shortcut — those two files had been quietly asserting the exact bug this closes. Build and full suite clean (one pre-existing, documented-flaky, unrelated test excluded).)_
_Prior: 2026-08-23 — Claude (**New tool added: `portal_chat_attach_file`** (`lib/mcp/tools/portal.ts`, registered via the already-active `registerPortalTools`) — copies a file from Drive/Gmail/a URL/Supabase Storage into the portal chat's own public attachment storage and returns a link ready for `portal_chat_send`'s `attachments` array, so a file doesn't depend on its Drive sharing permissions to reach a client. Full rationale and the `assets`-bucket-must-stay-public dependency: `portal.md`'s 2026-08-23 entry. Portal Chat section of `lib/mcp/instructions.ts` (+ its mirror `docs/claude-connector-system-instructions.md`) updated from 5 to 6 tools in the same push. Verified: lint clean, full unit suite green (9,516 tests), `next build` clean. NOT yet exercised as a live MCP tool call in this session — the connection serving these tool calls needs to pick up the new registration after deploy.)_
_Prior: 2026-08-16 — Claude (**doc-only, no tool added/removed**: the "Present Plainly" section of `lib/mcp/instructions.ts` (+ its mirror `docs/claude-connector-system-instructions.md`, + the parallel CLAUDE.md copy for Claude Code) gained one worked example — a real 2026-07-31 incident where a session called a live client form "dead" from reading code alone, never loading the page. Same principle-first-plus-one-dated-incident shape already used by R093 and the council's cost-note rule. Wording also reconciled across the two/three copies, which had drifted slightly. Council-reviewed (senior-engineer, ai-architect, bug-hunter, project-director, system-counselor) before shipping; dev job `687137ad`.)_
_Prior: 2026-08-14 — Claude (**doc-only, no tool added/removed**: the 2026-08-12 entry below is now HISTORICAL on the authoring-refusal point. Branch `feat/referrer-commission-on-plan` (SANDBOX ONLY) reversed it — a plan may now carry a referrer or managed partner; the offer-system section of `lib/mcp/instructions.ts` was corrected to describe the manual "Release commission" action replacing both the authoring-time refusal and the hand-settlement card. Separately, two of THIS doc's own tools — `referral_create`/`referral_payout` in `lib/mcp/tools/referrals.ts` — gained a real behavior change, not just doc wording: both now refuse to pay/credit a referral if the referred party's offer carries a payment plan that isn't fully settled yet, same gate the release action uses. Full detail (the gate itself, and the two dashboard buttons that got the same treatment): `referrals-circleback.md`. Business-rule half: MASTER RULES v3.7 (P11/P14 revised).)_
_Prior: 2026-08-12 — Claude (**doc-only, no tool added/removed**: the offer-system section of `lib/mcp/instructions.ts` (+ its mirror `docs/claude-connector-system-instructions.md`) gained a PAYMENT PLANS paragraph — plans shipped to production 2026-08-11 (PR #286) and the connector now knows: signing bills only part 1, later parts are raised from the account page's Partial Payment Schedule (never hand-crafted), wording is always "Partial Payment"/English, plan+referrer is refused at authoring, pre-existing combos suppress the automatic commission behind a hand-settlement card, and there is no authoring UI yet. Business-rule half: MASTER RULES v3.6 (new P14, amended P11). Full subsystem: `offers.md`.)_
_Prior 2026-07-29 — Claude (**Per-request AUTH-METHOD context (dev job 8537adf9).** The transport route's `withAuth` now runs the MCP handler inside an AsyncLocalStorage context (`lib/mcp/auth-context.ts`): `{method:"static"}` for the shared TD_MCP_API_KEY (Claude Code), `{method:"oauth", email}` for a Claude.ai connector token (email resolved best-effort from `oauth_users`; the route previously validated the token and THREW THE USER AWAY). First consumer: `team_chat_send` stamps `on_behalf_of` = the configured operator email (`MCP_TEAM_CHAT_ACTOR_EMAIL`, default Antonio) on static-key requests, or the OAuth user's email, or null — so the dictating human stops receiving their own message's notifications (full design in team-workspace.md 2026-07-29b). Any future identity-sensitive tool should read `getMcpAuthContext()` rather than invent its own plumbing. The handler itself is still built once at module scope — ALS is the only way per-request data reaches tool callbacks.)_
_Prior 2026-07-22d — Claude (**TD's office address now lives in ONE place — `lib/td-address.ts` — with its three PURPOSES kept apart.** It had been hand-written in ELEVEN source files, and the comment in `app/portal/flows/[id]/page.tsx` claimed it lived in two others and NAMED THE WRONG ONES. On the day the office moves, whoever trusts that comment updates a few sites, misses the rest, and a client posts their ORIGINAL wet-ink W-7 plus passport copies to a dead address — unrecoverable. **Deliberately NOT one constant.** Three named exports off one source value, because they answer different questions and are equal only by coincidence today: `MAILING_DESTINATION_*` (where CLIENTS post to us — both portal surfaces + the two ITIN email templates in `lib/mcp/tools/itin-form.ts`), `CAA_IDENTITY_ADDRESS` (TD's address as Certified Acceptance Agent on the W-7, beside the EIN/PTIN/office code — a regulated identity block, not a delivery address), and `CLIENT_ADDRESS_FALLBACK` (stands in for the CLIENT's company address on the SS-4 when their account has none — semantically the client's address, borrowing ours). Collapsing them would make a new document-receiving office silently move the CAA identity block too. **SS-4 precedence untouched:** the client's mailing address wins, then their physical address, and only then the fallback (`lib/operations/ss4.ts`). Also replaced in `lib/mcp/instructions.ts` — prose the assistant repeats to staff and clients. New guard `tests/unit/td-address-single-source.test.ts` fails if anyone hardcodes it again; it strips comments first, since an example in a doc comment is not a delivery path. Test fixtures deliberately KEEP the literal — a tripwire that fires if the value changes. **KNOWN GAP, not fixable from code:** the staff workspace's mailing note at the ITIN Client Signing stage stores the address inside `pipeline_stages.stage_layout` — a DB row. It will NOT follow the constant and must be changed by hand if the office moves, and it is client-facing instruction text, so it is the riskiest remaining copy. Suite 6747 green. Dev job `0d5c7695`.)_
_Last verified against code: 2026-07-16 — Claude (**doc-only, no tool added/removed**: one line in `lib/mcp/instructions.ts` "Verify Before Claiming" (+ its mirror `docs/claude-connector-system-instructions.md`) rescoped — "every claim must cite its source" now explicitly directs the citation to the internal record or the reply's "Technical details" footer, NEVER the reply body, so it no longer contradicts the "Present Plainly" section that follows it. Part of the R095 gate-arming change (dev job ef39fe73); the Claude Code-side twin edit landed in CLAUDE.md, and the enforcement half (the now-BLOCKING `r095_gate.py` Stop hook) is documented in `hooks-guardrails.md` (2026-07-16).)_
_Prior: 2026-07-11 — Claude (**dev-tracker board ship**: the `dev_task_*` tools (`lib/mcp/tools/dev-tasks.ts`) are now the backing tools for the per-channel dev-tracker board — `dev_task_create`/`dev_task_update` gained board fields (`channel` validated against real channels, `summary_plain` plain-English layer, `findings`, `plan`), a non-linear `milestone` advance with lane derived from catalog-driven stage sets per job type, stage-tagged `progress_entry`, and `parent_id` child-spawn; `dev_task_list` gained a channel filter. The MCP Session Start Protocol instruction now points sessions at R112. No tool count change. Full detail in `dev-tracker.md`.) Earlier 2026-07-10 — Claude (**doc-only, no tool added/removed**: the `approval_*` MCP tool descriptions in `lib/mcp/instructions.ts` now carry a ⚠️ DORMANT banner — Antonio switched the worker action rail OFF (2026-07-10, env `WORKER_ACTIONS_ENABLED` default OFF), so no worker/helper queues actions and the approval tools operate only on the (empty) queue, kept for reversibility. See `agent-bridge.md` (2026-07-10).)_
_2026-07-09 — Claude (**+1 tool: `team_chat_send`** — new tool file `lib/mcp/tools/team-chat.ts` (`registerTeamChatTools`, registered in `app/api/[transport]/route.ts`) posts a message into the internal Team Workspace ("team chat") AS Claude via the shared `lib/team/post-message.ts` choke-point (target = channel / thread_id / dm_user_id; @mentions push staff). Staff-only, never client-visible; carries the mandatory draft→"send it" approval discipline in its description. Registered tool FILES now 48. Full detail in team-workspace.md (2026-07-09e).)_
_Last verified against code: 2026-07-08 — Claude (doc note: `@modelcontextprotocol/sdk` bumped ^1.25.2 → ^1.29.0 to stop `@hono/node-server` from replacing `global.Response` (the "No response is returned from route handler" 500s across offer-narrative + cron routes). New gotcha bullet below. No tool added/removed.)_
_Prior 2026-06-29 — Claude (doc note: `lib/mcp/instructions.ts` formation pipeline text updated from 7 → 8 stages: added "SS-4 Sent to IRS" between SS-4 Signed and EIN Received. No tool added/removed — instructions text only. See formation.md.)_
_Prior 2026-06-26 — Claude (doc note: `lib/mcp/instructions.ts` (+ its mirror `docs/claude-connector-system-instructions.md`) `itin_form_*` group text rewritten for the **workspace-only ITIN flow** — `itin_prepare_documents` now registers the PDFs as portal-visible docs and the ITIN is managed from the `/flows/[sd]` workspace (advance Document Preparation → Client Signing posts the client a bilingual portal chat message); the client is NEVER emailed the PDFs (the tool's `send_email` param is DEPRECATED, description updated in `lib/mcp/tools/itin-form.ts`). Mailing address corrected to Seminole (11125 Park Blvd Suite 104-153). No tool added/removed — instructions text only. Propagated with flows.md/workflow-engine.md + ITIN SOP v7.3. **OPEN:** the `itin_prepare_documents` `send_email` CODE path still exists (emails the client the PDFs) — flagged for removal.)_
_Prior 2026-06-21 — Claude (doc note: `lib/mcp/instructions.ts` "Verify Before Acting" closing line strengthened with R109 SELF-SERVE BEFORE ASKING — look any fact up in the system (a client's language=contacts.language, email, invoice/payment status, which service) before asking Antonio; write client drafts in the client's CRM language automatically. Instructions text only, no tool added/removed. Propagated alongside CLAUDE.md R109 + the Slack worker prompt; see slack-claude-worker.md.) Prior: 2026-06-17 — Claude (doc note: `lib/mcp/instructions.ts` formation-pipeline text updated to the 7-stage Workspace v2 (Payment Confirmed → Wizard Submitted → Filed with State → Articles Received → SS-4 Prepared → SS-4 Signed → EIN Received; account created at Articles Received) and the auto-advance line now lists "all Company Formation stages" as auto_advance=false. No tool added/removed — instructions text only. See formation.md/flows.md.)_
_Earlier 2026-06-05 — Claude (WP3: agent approvals group += approval_execute (now 6 tools); approve no longer instant-fires the server — see the Agent approvals group bullet + agent-bridge.md WP3)_

## What it is
The MCP server that exposes TD Operations to Claude (Claude Code + the Claude.ai connector) as callable tools. One endpoint, dual auth, and ~221 tools across 42 active groups. This doc is about the *server infrastructure*; each tool's behaviour is documented in its subsystem doc.

## The single endpoint
`app/api/[transport]/route.ts` handles all MCP traffic via `createMcpHandler` (`mcp-handler`):
- `POST/GET/DELETE /api/mcp` — Streamable HTTP (messages / SSE stream / session end).
- Legacy `GET /api/sse` + `POST /api/message`.
- **Auth (dual):** Bearer token (`TD_MCP_API_KEY`) for Claude Code; OAuth 2.1 access token for the Claude.ai connector (see `auth-oauth.md`).

## How tools are registered (and the source-of-truth rule)
- Each tool file exports a `register<Area>Tools(server)` function; `route.ts` imports it and calls it.
- **`route.ts` is the ONLY source of truth for which tools are active.** A tool file can exist but be inactive (commented import/call). **Never count `server.tool()` across files** — count uncommented `register*Tools` calls in `route.ts`:
  `grep -v '//' app/api/[transport]/route.ts | grep 'register.*Tools'` → re-run for the live number (the "41 groups (~217 tools)" figure is from 2026-05-29 and has since grown).
- **Hermes ↔ Claude bridge group (2026-06-03):** `registerAgentMessageTools` (`lib/mcp/tools/agent-messages.ts`) adds 3 tools — `agent_msg_send`, `agent_inbox_list`, `agent_inbox_reply` — over the inter-agent `agent_messages` table. The worker that *answers* those messages is NOT an MCP tool; it lives at `app/api/cron/hermes-bridge` (read-only sonnet). Full subsystem doc: `agent-bridge.md`. `agent_msg_send` carries a mandatory approval rule in its description.
- **Agent approvals group (2026-06-04 → WP3 2026-06-05):** `registerAgentApprovalTools` (`lib/mcp/tools/agent-approvals.ts`) over the `approval_queue` table — now **6 tools**: `approval_list` (read-only) **and** `approval_decide(id, 'approve'|'reject', confirmation_code?, note?)` **and** the Operating-Agent rail `hermes_heartbeat(instance_id)` / `approval_claim(instance_id)` / **`approval_execute(id)` (WP3)** / `approval_complete(id, status, result?, error_text?)`. **WP3: `approve` no longer instant-fires the server** — it ONLY flips pending→approved; the Mac Mini (primary) claims the row (`approval_claim`) then runs it via `approval_execute(id)`, which executes server-side on the same tested `executeTool` path and finalizes the row. The `app/api/cron/approval-executor` worker is now BACKUP only (runs an approved row only if the Mac Mini is offline/stale or the row strands past the grace window — `serverShouldBackstop`). Execution (server cron AND `approval_execute`) is gated by the ONE master switch `APPROVAL_RAIL_ENABLED`. `reject` flips pending→rejected. **WP1: `approve` REQUIRES the proposal's 6-digit `confirmation_code` — a missing/wrong code leaves the row pending and runs nothing.** `approval_complete` remains the manual/fallback closer (the atomic claim still prevents double-exec). `approval_decide` carries the same mandatory show-the-draft-and-wait-for-OK rule as `gmail_send`. Full subsystem doc: `agent-bridge.md` (WP3).
- **Agent threads group (2026-06-04 Phase C; 2026-06-05 WP2):** `registerAgentThreadTools` (`lib/mcp/tools/agent-threads.ts`) — now **2 tools** over the `thread_summaries` table: `thread_search(query, type?, tags?, limit?)` (read-only) searches the bridge's durable thread memory so Hermes can reference a past investigation instead of re-deriving it; **`thread_create(type, title?, account_id?, contact_id?)` (WP2)** opens a new typed thread and returns `{ thread_id, type, title }` — Hermes calls it before the first `agent_msg_send` of an investigation, then tags every message with the returned `thread_id` (a new optional param on `agent_msg_send`) so the worker gets prior-turn context + a type-filtered tool surface. Full subsystem doc: `agent-bridge.md` (Phase C + WP2).
- **Adding a tool:** add `server.tool(...)` in the area file → add the import + `register*Tools(server)` call in `route.ts` → update `lib/mcp/instructions.ts` → mirror `docs/claude-connector-system-instructions.md`.
- **Removing a tool:** delete the file or move it to `lib/mcp/tools/deprecated/` (as QB was, R097) — never leave dead tool files (they cause wrong counts/confusion).

## ⚠️ Two MCP connections in Claude Code (R096)
_Source: this is an operational rule in CLAUDE.md (R096), confirmed by `.mcp.json` (defines the `td-ops-sandbox` connection). It is NOT encoded in `route.ts` — `route.ts` only implements the server's dual **auth** (Bearer + OAuth). The two connections are a Claude-Code client-config convention, not server code._
- `mcp__td-ops-sandbox__*` → **sandbox** Supabase — use for development (building/testing/verifying sandbox).
- `mcp__af7d85f2-*` → **production** Supabase (the DXT plugin) — use for operations (real clients).
- **EXCEPTION:** `session_checkpoint` + `dev_task_*` always use the production connection (work tracking must persist).
- **Critical:** the `af7d85f2` connection is **hardwired to production** — the sandbox enforcement (`.env.local`, `.vercel/project.json`, the supabase-admin ref check) does NOT apply to MCP calls. Every `mcp__af7d85f2-*__execute_sql` hits production. For sandbox DB work use `mcp__td-ops-sandbox__*` or `psql`.

## Cross-cutting MCP infrastructure (`lib/mcp/`)
- `instructions.ts` → `SERVER_INSTRUCTIONS` — sent in the MCP `initialize` handshake (the assistant's standing rules). Mirror lives in `docs/claude-connector-system-instructions.md`.
- `safe-send.ts` → `safeSend()` — the R037 send pattern (idempotency check → send → status update after → multi-step tracking). All send tools must use it.
- `reminder.ts` → `addReminderMiddleware()` — injects the 5/10/15 checkpoint reminders into tool responses (the Claude.ai equivalent of the Claude Code hooks).
- `action-log.ts` → `logAction()` — writes an audit trail of MCP write operations to `action_log`.

## Business rules
- **Tool-count source of truth = `route.ts` uncommented registrations** — update counts in `instructions.ts`/docs only after verifying with the grep above.
- **R037** — send tools use `safeSend`. **R051** — subagents write results to Supabase before returning. **R096** — the routing rule above. **R097** — QB tools are removed and **QuickBooks is decommissioned/DEAD** (kill-switch OFF since 2026-05-23); do not restore the tools, re-enable sync, or build on QB.
- Tool *descriptions* are the documentation — keep them detailed (prerequisites, cross-references).

## How it's built — key files & tables
- Files: `app/api/[transport]/route.ts` (entry + registrations), `lib/mcp/{instructions,safe-send,reminder,action-log}.ts`, `lib/mcp/tools/*` (44 files), `docs/claude-connector-system-instructions.md`.
- Tables: `action_log` (audit), `oauth_*` (connector auth), `session_checkpoints`.

## Gotchas, invariants & past bugs
- **⚠️ MCP SDK must stay ≥ 1.26.0 — global `Response` poisoning (2026-07-08 incident).** `@modelcontextprotocol/sdk` ≤ 1.25.x bridged Node HTTP via `@hono/node-server`'s `getRequestListener()` WITHOUT `overrideGlobalObjects: false`, which **replaces `global.Response`**. On Vercel Fluid, one warm Node process serves many routes: after one MCP request, every other route returning a `NextResponse` failed Next's `res instanceof Response` check → "No response is returned from route handler" → naked 500 with empty body (no route-level catch ever runs, so no `system_errors` server row and no JSON error for the client). Blast radius was 1,000+ errors/day: offer-narrative generation, `/api/team/claude/process`, and most `/api/cron/*` routes. Fixed by upgrading the SDK to ^1.29.0 (upstream passes `overrideGlobalObjects: false` since 1.26.0). If a future dep swaps fetch globals, the signature to look for is exactly this error with an empty body on routes whose code provably returns in every branch.
- **Don't trust a tool file's existence** — only `route.ts` registration makes a tool active. Counting `server.tool()` across files gives the wrong number.
- **MCP bypasses sandbox safety (R096)** — there is no PreToolUse hook on MCP except `production-write-guard`; `af7d85f2` = production always.
- **Update `instructions.ts` + the mirror doc** whenever tools change, or the connector's standing instructions drift from reality.
- **Deleted tools must leave no file behind** — orphan files in `lib/mcp/tools/` mislead the next session's count.

## How to verify current state
- Active tool groups: `grep -v '//' "app/api/[transport]/route.ts" | grep 'register.*Tools'`.
- Read `lib/mcp/instructions.ts` (the standing instructions) and `lib/mcp/safe-send.ts` (the send contract).
- Note (R096): sandbox via `mcp__td-ops-sandbox__*` / `psql`; `mcp__af7d85f2-*` hits production.
