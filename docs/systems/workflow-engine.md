# Workflow / Catalog Engine
_Last verified against code: 2026-07-08 — Claude (cosmetic only, PWA mobile UX pass dev_task `e1f28dce`: the `/workflows` list table wrapper in `workflows-list-client.tsx` changed `overflow-hidden` → `overflow-x-auto` + `min-w-[720px]` on the table so it side-scrolls on phones instead of crushing columns. No engine/behavior change.)_
_Prior 2026-06-29 — Claude (**8-stage formation: `formation-confirm-ein-received.ts` comment fix.** Comment "7-stage v2 pipeline" → "8-stage v2 pipeline"; "advanceStage refuses if not at SS-4 Signed" → "advanceStage accepts any source stage; guard is by SD status, not stage name". Preview text fixed to `"→ EIN Received"`. No behavioral change — accuracy only. `lib/tasks/workflow-handlers/formation-confirm-ein-received.ts`. See formation.md for the full 8-stage migration.)_
_Last verified against code: 2026-06-26 — Claude (**ITIN review is a WORKSPACE-POINTER workflow (correction to the 2026-06-25 plain-task approach).** SHIPPED. The 2026-06-25 change made `itin-form-completed` create a PLAIN task — which silently LOST the **What's New** feed notification (Portal Chats), where Luca actually watches. That feed is driven by the `workflow_spawned` chat-event the dispatcher emits (`emitTopicForWorkflow` → `emitClientChatEvent`, gated on the catalog row's `auto_topic`); a plain `tasks.insert` emits nothing. Fix mirrors `formation_progress`: `itin_review` is now a **`workspace_pointer: true`** catalog row with **empty `actions`** (no handlers → passes the validity gate, renders no buttons) + the lightweight **`sd_progress_v1`** task_meta schema, still `triggered_by` form_submission/`itin_submissions`, `auto_topic: "ITIN"`. `itin-form-completed` Step 6 dispatches it via `dispatchWorkflowForFormCompletion` (meta = `{service_delivery_id, service_type:'ITIN', sd_stage, account_id, contact_id}`; idempotency on `service_delivery_id`), with a plain-task fallback only when there's no SD. Result: the dispatcher emits `workflow_spawned` → the ITIN note appears in What's New, and its `WorkflowTaskCard` renders **"Open in Workspace"** (`/flows/[delivery_id]`) with NO action buttons (workspace_pointer empties them). `lib/service-delivery.ts` §6b stage-sync generalized to keep BOTH `formation_progress` and `itin_review` pointer cards' `task_meta.sd_stage` fresh on advance (service→slug map). The client-facing message still comes ONLY from the §8c Document Preparation → Client Signing hook — no email. Verified live in sandbox: dispatch spawns the pointer task + emits the chat-event; browser What's New panel shows the ITIN note with the "Open in Workspace" link to `/flows/[sd]` and no buttons. Catalog row `40b8ac4c` rewritten active+pointer in sandbox AND production.)_
_Earlier 2026-06-25 — Claude (**ITIN is now WORKSPACE-ONLY — the `itin_review` workflow + its email handlers were removed** (branch `fix/itin-workspace-only`, SANDBOX). The ITIN flow had two competing control surfaces: the `/flows/[sd_id]` workspace AND a workflow chain whose `itin_review` task carried Approve/Needs-Fix/Waiting/Recall buttons, where "Approve & Send" EMAILED the client the W-7/1040-NR/Schedule OI PDFs. Antonio's decision: the workspace is the ONLY surface; the client gets documents from the PORTAL, never email. Changes: (1) **handlers `itin.approve_and_send` + `itin.recall_and_recorrect` DELETED** (`lib/tasks/workflow-handlers/itin-approve-and-send.ts` + `itin-recall-and-recorrect.ts` removed), unregistered from `workflow-registry.ts` + `workflow-handler-params.ts`, params dropped from `handler-param-schemas.ts`. `itin.confirm_number_received` STAYS (the `itin_irs_processing → itin_number_received` chain is untouched). The `itin_review_v1` schema in `workflow-schemas.ts` is KEPT (harmless; still referenced by tests). (2) **`itin-form-completed/route.ts` Step 6 no longer dispatches the `itin_review` workflow** — it creates a PLAIN task ("Review ITIN documents — …") whose description links to the workspace (`/flows/[sd_id]`) and says "Open the workspace to review and advance"; no `workflow_slug`/`workflow_snapshot`/`task_meta`, so the What's-New card has NO action buttons. The internal team email (Step 5, to support@) is unchanged. (3) **Client communication moved to an `advanceServiceDelivery` hook** (`lib/service-delivery.ts` §8c): advancing an ITIN SD **Document Preparation → Client Signing** posts the client a bilingual (EN/IT) portal chat message — "Your ITIN documents are ready in your portal… print, wet-ink sign, two color passport copies, mail to our office" — stamped `service_delivery_id` (threads into workspace + portal chat), `sender_type='admin'`, plus the standard R103 new-message notification. NO document email. Verified live against sandbox SD `746faa3a` (Stay Legit LLC): advance → `auto_triggers` includes "ITIN client portal message posted (en)", the `portal_messages` row exists, only the throttle-digest email is (sandbox-)blocked, never the PDFs. (4) **Catalog: `itin_review` row deactivated** (`catalog_entries`, `catalog_id='task_workflows'`, status active→draft) in SANDBOX so no ACTIVE row references the deleted handlers — the five downstream ITIN rows (`itin_await_client_mailing`, `itin_caa_certify_and_mail`, `itin_irs_processing`, `itin_number_received`, `itin_data_collection`) stay active and reference only surviving handlers. **PROD-PENDING (Antonio ships):** promote code + the `itin_review` status flip to production, AND mark the existing prod `itin_review` task `1353ccc8-…` Done (its pinned old snapshot would throw on a button click once the handler is gone — that task does NOT exist in sandbox).)_
_Earlier 2026-06-20 — Claude (**`formation_progress` is now a WORKSPACE POINTER, not a control surface.** Live-DB check found the catalog + every pinned task snapshot STILL on the OLD pre-v2 stage vocabulary — Data Collection / State Filing / EIN Application / EIN Submitted / Post-Formation + Banking / Closing — so the 2026-06-17 "remapped to 7 v2 stages" claim below never actually landed (migration `20260617-formation-workspace-v2.sql` did not update this catalog row). Its action buttons gated on dead stage names and never tracked the SD. New direction (Antonio): the `/flows/[delivery_id]` workspace is the single control surface, so the task card became a read-only pointer. **New snapshot field `workspace_pointer: boolean`** (`lib/tasks/types.ts` + `workflow-snapshot-schema.ts`): when true the `WorkflowTaskCard` renders the current stage + an "Open in Workspace" link and NO action buttons, and the snapshot may carry an empty `actions` array (the schema's old `.min(1)` is now a `superRefine` that only requires ≥1 action for non-pointer workflows). `advanceServiceDelivery` (`lib/service-delivery.ts` §6b) keeps the linked formation_progress task's `task_meta.sd_stage`/`workflow_state` synced on every advance via the pure `mergeSdStageIntoTaskMeta` helper (`lib/tasks/sd-stage-sync.ts`) — the workspace advance path never patched task_meta before, unlike the `chain.advance_sd_stage` handler. Catalog + in-flight snapshots flipped to pointer mode in migration `20260620-1900-formation-progress-workspace-pointer.sql`. NOTE the catalog slug lives on the `slug` COLUMN, and `whats_new_events` shares the slug — scope by `catalog_id='task_workflows'`.)_
_Earlier 2026-06-17 — Claude (Company Formation Workspace v2 touched the `formation_progress` task-workflow: the `formation.confirm_ein_received` handler (`lib/tasks/workflow-handlers/formation-confirm-ein-received.ts`) now advances **SS-4 Signed → EIN Received** (was EIN Submitted → Post-Formation + Banking); the catalog row's `actions` are remapped to the 7 v2 stages (separate "Name Check" action dropped; `verify_data_and_name` now targets Filed with State) in migration `20260617-formation-workspace-v2.sql`. `mark_complete` (at EIN Received) still spawns RA Renewal + Annual Report SDs. See formation.md/flows.md.)_
_Earlier 2026-06-16 — Claude (ITIN approve-and-send handler (`lib/tasks/workflow-handlers/itin-approve-and-send.ts`) mailing instructions — both the email block and the EN/IT portal chat messages — now point clients to TD's document-receiving office **Seminole, 11125 Park Blvd Suite 104-153, FL 33772** (was the old Largo address). The CMRA business address used in client legal docs is intentionally unchanged.)_
_Earlier 2026-05-29 — Claude (read framework.ts, dispatch-workflow-for-event.ts, trigger/snapshot schemas, catalog-validity.ts)_

## What it is
The catalog-driven engine that powers most service operations. Two layers:
1. **Catalog framework** — a generic store for every enumerated business concept (services, SD types, pipeline stages, doc types, action-board columns, **workflows**). Adding/changing a concept is data, not code.
2. **Workflow engine** — workflows themselves live as catalog rows; when an event happens (a form is submitted, an SD is created) a dispatcher finds the matching workflow and spawns a task. The big payoff: **adding a new workflow variant is pure SQL/catalog — zero code change, zero risk to siblings.**

## Catalog framework
- **Tables:** `catalog_definitions` (the catalogs) → `catalog_entries` (the rows) → `catalog_decision_log` (every mutation logged) → `catalog_pending_review` (unrecognized external values land here for triage).
- **Entry status:** `active` / `deprecated` / `exception_only` / `draft`. Actor kinds: `chat` / `ui` / `migration` / `admin_api`.
- **API** (`lib/catalog/framework.ts`): `getCatalog`, `listEntries`, `getEntry(ById)`, `addEntry`, `renameEntry`, `deprecateEntry`, `restoreEntry`, `tagEntry`, `addTranslation`, **`updateMetadata`** (the canonical metadata write path — writes a decision-log row, supports optimistic-concurrency via `expectedUpdatedAt`, and a `status` param for publish), `labelFor`/`slugFor`, `resolveExternalValue`, `listPendingReview`/`resolvePendingReview`.
- **MCP tools:** `catalog_add`, `catalog_list`, `catalog_pending`, `catalog_update`. CRM page: `/catalog`. (R106: services are a catalog — code imports from `lib/services/index.ts`, never hardcodes types.)

## Workflow engine
### Triggers (how an event finds its workflow)
`lib/tasks/workflow-trigger-schema.ts` defines a **discriminated union** on `source`:
- `form_submission` (with a `filter`, e.g. `service_type`),
- `sd_created` (with `filter.service_type`).
Extensible (future: payment_received, signature_signed…). `parseTriggeredBy` safe-parses — a malformed catalog row is **skipped with a warn, never crashes**. `matchesFilter` is strict exact-match.

### Dispatch
`lib/tasks/dispatch-workflow-for-event.ts`: `dispatchWorkflowForFormCompletion()` and `dispatchWorkflowForSdCreated()` scan `task_workflows` catalog rows, find the one whose `triggered_by` matches the event, build the task meta (via a caller-supplied `buildTaskMeta` callback — meta-building is form-specific), and spawn the workflow task. `createSD` fires `dispatchWorkflowForSdCreated` automatically (fire-and-forget).
**Defense-in-depth outcomes** (each falls back to legacy, never silently wrong): `no_trigger_match`, `ambiguous` (two active workflows match the same event = a catalog DATA error, must be fixed in catalog), `snapshot_invalid`, `meta_invalid`, `spawn_failed`.

### Snapshot pinned at task creation
`lib/tasks/workflow-snapshot-schema.ts`: when a workflow task is created, the workflow definition is **pinned as a snapshot** on the task. Catalog edits never affect in-flight tasks. `parseWorkflowSnapshot` validates it; **`buildSnapshotForStorage({slug, metadata})`** merges the catalog `slug` into metadata so the snapshot parses at render time. To retro-update in-flight tasks, write a `jsonb_set` migration.

### Validity gate (deploy/publish safety)
`lib/tasks/catalog-validity.ts` → `validateWorkflowCatalog()`: a pure-function gate asserting every active workflow's `handler` / `attachment_template` / `task_meta_schema` resolve to registered code, the snapshot parses, each action's `handler_params` parse against the handler's Zod schema, and **no two active workflows share a trigger fingerprint**. DI'd registries keep tests DB-free. The Publish action runs this before flipping a workflow to `active`.

### The editor + other pieces
- **Editor** (`/workflows`, admin): author/edit/publish workflows from the UI without SQL. `handler_params` Zod schemas live in `lib/tasks/handler-param-schemas.ts` (client-safe). Save Draft → `status='draft'` (dispatcher ignores); Publish → runs the validity gate, then `status='active'`. Stale-edit detection via `expectedUpdatedAt`.
- **Chained workflows** (`chain-transitions.ts`): `chain.spawn_next_workflow` (ITIN pattern), `chain.advance_sd_stage`.
- **SLA** (`sla-eligibility.ts`): catalog-driven escalation (`sla.auto_reassign`, `sla.notify_email_to`); import `SLA_STATE`/`SLA_META_KEYS` constants — never string-literal `"warn"`/`"escalated"`. `WORKFLOW_SLA_DRY_RUN` disables writes during rollout.
- **Default assignee** (`default-assignee.ts`): `defaultTaskAssignee()` → env `DEFAULT_TASK_ASSIGNEE`, fallback "Luca".
- **Templates:** `task_title_template` + `description_template` (token `{name}` via `lib/template-interpolation.ts`, `interpolateStringStrict`).

## How it's built — key files & tables
- Files: `lib/catalog/framework.ts`, `lib/tasks/{dispatch-workflow-for-event,workflow-trigger-schema,workflow-snapshot-schema,catalog-validity,handler-param-schemas,workflow-registry,workflow-handler-params,chain-transitions,sla-eligibility,default-assignee}.ts`, `lib/tasks/workflow-handlers/`, `app/(dashboard)/workflows/*`, `lib/mcp/tools/catalog.ts`.
- Tables: `catalog_definitions`, `catalog_entries` (incl. `catalog_id='task_workflows'`), `catalog_decision_log`, `catalog_pending_review`, `tasks` (carry `workflow_snapshot` + `task_meta`).

## Gotchas, invariants & past bugs
- **ALWAYS build a stored snapshot via `buildSnapshotForStorage({slug, metadata})`** — never hand-roll `{...metadata, slug}`. (Carved in stone after the `cf0cb867` bugfix: the slug must come from the catalog row's `slug` column or `parseWorkflowSnapshot` rejects it at render.)
- **Snapshots are pinned** — editing a workflow in the catalog does NOT change in-flight tasks; use a `jsonb_set` migration to retro-update.
- **Ambiguous trigger = catalog data error** — if two active workflows match one event, the dispatcher falls back and logs loud; fix the overlapping trigger, don't let it pick randomly.
- **Use `updateMetadata` for catalog writes** — it logs the decision and supports stale-edit detection; don't raw-`update` catalog rows.
- **PostgREST JSONB nested paths use UNQUOTED keys** (e.g. `metadata->triggered_by->filter->>service_type`).
- **Import SLA constants**, never string-literal them — a typo should be a TS error, not a silent UI bug.

## How to verify current state
- Read `lib/catalog/framework.ts` (the generic API + tables), `lib/tasks/dispatch-workflow-for-event.ts` (the two dispatchers + fallback outcomes), `lib/tasks/catalog-validity.ts` (the publish gate).
- Active workflows: `SELECT slug, status, metadata->'triggered_by' FROM catalog_entries WHERE catalog_id='task_workflows' AND status='active';`
- Run the gate locally: `scripts/check-catalog-validity.ts` (one-shot runner).
- Note (R096): sandbox via sandbox MCP / `psql`; production `execute_sql` hits production.
