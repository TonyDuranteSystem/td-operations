# Company Formation
_2026-07-13 — Claude (**Formation now records the lead→account conversion + moves the lead's Team Chat conversations.** When a formation company materializes (`lib/operations/formation-materialize.ts`), it now writes `leads.converted_to_account_id` + `converted_at` — previously unset on this path — and calls `reanchorLeadConversations(wp.lead_id, accountId)` so any Team Chat conversation opened on the originating lead re-anchors to the new account. Additive/best-effort; does not change the formation flow itself. Part of dev_task `be582c5e` (Conversations buckets + lead→client auto-move); full detail in `team-workspace.md`. Shipped to prod 2026-07-13.)_
_2026-07-09b (**In-workspace SS-4 fax panel** — "SS-4 Signed" stage, branch `claude/ecstatic-franklin-c201ec`, SANDBOX — Claude. Antonio: put the ready-to-fax IRS package right in the workspace with a review + one-click send, instead of the old passive hand-off to `/tools/fax`. **What it does:** on the "SS-4 Signed" stage a new `ss4_fax_panel` component (`components/flows/ss4-fax-panel.tsx`, backed by `GET /api/flows/[id]/ss4-fax`) lets staff **View** the combined signed-SS-4 + Articles package and **Send it to the IRS** with a pre-filled-but-editable form (IRS number defaults to the **domestic** EIN fax `IRS_EIN_FAX_DOMESTIC = 855-641-6935` in `lib/fax/faxage.ts`; Reason + Cover note are **internal**/log-only), a mandatory **confirm** step (irreversible), and a **Re-send for signature** button. **The faxable file is a real workspace document:** `app/api/ss4-signed/route.ts` now registers the merged package via `autoSaveDocument` as `document_type_name='SS-4 + Articles (IRS Package)'`, `portal_visible:false`, SD-stamped — idempotent by `drive_file_id` (the Drive upsert refreshes the SAME file in place), so a **re-sign rebuilds the package into the same row** (no duplicates) and the panel always shows the latest. **Send goes through the shared `/api/tools/fax/send` engine** (one send/idempotency/audit path); that route gained an **opt-in double-send guard** (`dedupe_service_delivery_id` + `confirm_resend`): a prior `fax_sent` for the flow → HTTP 409 the panel turns into an explicit "send again?" — general fax-tool callers omit these fields, so their behavior is unchanged. **Stays put after sending** (advance is still the fax-confirmation upload). **Client safety:** the package is NEVER client-visible — `portal_visible:false` (excluded from the account docs query) AND no `flow_stage` (fails the `isClientSafeFlowDoc` allowlist); pinned by `tests/unit/ss4-fax-package-visibility.test.ts`. **Scenarios handled:** normal; wrong-before-sign (regenerate, existing); wrong-after-sign → re-send → client re-signs → package auto-rebuilt (panel shows "waiting for client" meanwhile, fax disabled); Articles-missing / no-Drive-folder → panel warns + points to manual fax (never faxes SS-4-only); double-send guard; Faxage error surfaced (R099). Stage layout swap (`fax_irs`→`ss4_fax_panel`) in `scripts/migrations/20260709-1400-ss4-fax-panel-stage-layout.sql` (applied to SANDBOX; pipeline_stages is protected — prod on Antonio's approval). Also: `fax-irs.tsx`'s comment claiming an ITIN fax stage is STALE — verified all 8 ITIN stages have NO fax (ITIN ships by mail); Tax Return keeps its own `fax_irs`, untouched. Build green, full suite 5261 green, lint clean. **QA CAUTION:** in sandbox `uploadBinaryToDrive` is mocked (`sandbox-mock` id) so the package is "faxable" but clicking **Send** would call Faxage for real against whatever number is entered — do NOT confirm-send to the real IRS number in sandbox; use a safe test fax number. NOT on production — Antonio ships._
_2026-07-09 (**Articles of Organization now reach Google Drive + the SS-4 IRS package is never silently incomplete**, branch `claude/ecstatic-franklin-c201ec`, SANDBOX — Claude. **Origin: Slack report 2026-07-08 (Art of Profit Academy LLC; also Numero Uno Social LLC, Automatiko LLC): the Articles were missing from the account's Drive folder AND the signed-SS-4 fax package contained only the SS-4.** Root cause (verified in code + live prod data): the workspace uploads the Articles at the CONTACT-SCOPED "Filed with State" stage — before the company is materialized — so `app/api/flows/[id]/upload-document` has no account Drive folder to target (and `resolveContactLinkedDriveFolder` is null for a first company) and parks the binary in Supabase Storage (`onboarding-uploads`, path `flow-uploads/<sd>/<file>`) with the documents row stamped `drive_file_id='storage:flow-uploads/...'`. The upload then auto-advances → materialize creates the account + Drive folder, but step 10a only backfilled `documents.account_id` — it **never copied the binary into Drive**. So (1) the Articles never reached Drive; (2) `app/api/ss4-signed` step 4 scans only the Drive "1. Company" folder for an `/articles/i` PDF, found none → `irs_package: skipped`, faxing an SS-4-only package. (Portal still showed the Articles via the "Filed with State" flow-doc allowlist, hence "in portal but not Drive".) **Fix (two parts):** **(1) Root cause** — new helper `lib/flows/relocate-flow-storage-docs.ts` (`relocateFlowStorageDocsToDrive` pure/DI core + `relocateFormationFlowDocs` wrapper): at materialization (new step **10a-bis**, after the 10a account backfill) it downloads each "Filed with State" doc still pointing at `storage:%` and uploads it into the company's "1. Company" subfolder (id captured as `companyDocsSubfolderId` from `ensureCompanyFolder().subfolders`), then repoints the documents row at the real Drive file. Idempotent (a relocated row no longer matches `storage:%`; a same-name file already in the folder is re-linked, not re-uploaded — LT Program dup class), best-effort (never fails materialization). Because the SS-4 merge reads from Drive, fixing Drive placement also fixes the IRS package for every future formation. **(2) Safety net** — new `lib/ss4/resolve-articles-pdf.ts` (`resolveArticlesPdf` core + `resolveArticlesForSs4` wrapper): `app/api/ss4-signed` step 4 now resolves the Articles Drive-scan-first, then falls back to the documents table (fetching the binary from Drive OR Storage), builds the merged bytes even when the account has no Drive folder (email still gets the complete package), and **NEVER silently ships an SS-4-only package** — if the Articles can't be found anywhere it sets `articlesMissing`, which prefixes the support email subject/body with a loud ⚠️ "attach Articles manually" warning AND files a `reportSystemError` (system-health widget). Unit tests: `tests/unit/relocate-flow-storage-docs.test.ts` (6), `tests/unit/resolve-articles-pdf.test.ts` (5). Full suite green (5254), build green, lint clean. **Not built yet: the one-time remediation** for the 3 stuck companies (Art of Profit already hand-fixed in Drive on 7/9; Numero Uno + Automatiko already faxed SS-4-only — flag Luca to re-fax) — separate approved step. **Sandbox caveat:** Drive is mocked in sandbox (`uploadBinaryToDrive`→'sandbox-mock', `downloadFileBinary`→placeholder), so the real Drive copy/merge only exercises on production; verification here is unit + build. NOT on production — Antonio ships._
_2026-07-06b (**completeSDInPlace added to the SD operations layer**, branch `claude/practical-einstein-ce1e5c`, SANDBOX) — Claude. New helper in `lib/operations/service-delivery.ts`: marks an SD `completed` WITHOUT a stage change (status flip + `end_date` + stage_history append, TOCTOU-guarded on `status='active'` so concurrent callers get `{completed:false}` and can gate one-shot side effects). Exists because terminal stages not literally named "Completed"/"TR Filed"/renewal-"Closed" never complete via `advanceServiceDelivery` (its rule keys off stage NAMES), and re-advancing into the same stage re-fires the generic client notification. First consumer: the ITIN Approved letter finalize (`lib/itin/finalize-approval.ts` — full story in flows.md 2026-07-06). Formation itself is untouched. Unit tests `tests/unit/complete-sd-in-place.test.ts`._
_2026-07-06 (**createSD person-link hygiene**, branch `claude/dazzling-kare-95a14c`, SANDBOX) — Claude. Non-ITIN SDs created with only an `account_id` now auto-resolve `contact_id` from `account_contacts` (shared `resolvePrimaryContactId` helper in `lib/operations/service-delivery.ts` — primary contact first, `contact_id` alphabetical tiebreak; the ITIN Phase B block reuses the same helper) while KEEPING `account_id` set. An account with no linked contacts leaves `contact_id` null — never blocks SD creation. WHY: the sd_created workflow dispatcher tags its What's New note with `delivery.contact_id`; company-only SDs produced company-only notes, which were unreachable from person threads until the 2026-07-06 What's New scope fix (portal.md same date). Dual-tagging at the source routes formation/onboarding/closure workflow notes to the primary contact's person thread directly. Containment (reader audit, all `service_deliveries.contact_id` consumers checked): portal service access (`app/api/portal/services/[id]`) and the flows page (`app/portal/flows/[id]`) now grant contact-match access ONLY when `account_id` is null (ITIN-style personal SDs) — company SDs are gated by account membership alone, so a contact later unlinked from the company keeps NO access through the stale tag; staff inbox service chips (`app/api/portal/chat/threads`) bucket contact-only SDs only, so chips render exactly as before. 5 new unit tests in `operations-service-delivery.test.ts`. No backfill of existing SD rows (visibility for those is covered by the What's New query-side fix). NOT on production — Antonio ships._
_2026-07-02b (**Phase B safety nets: SS-4 client reminders + staff stale-draft alert** — same branch, SANDBOX) — Claude. Daily cron `/api/cron/action-required-reminders`: clients with an SS-4 still awaiting signature are re-reminded (3-day spacing, max 2 reminders, stops on signing); staff (support@) are alerted when a formation SD sits at "SS-4 Prepared" with the SS-4 stuck in `draft` or missing >3 days — the alarm that would have caught the Michele Cotti case. Engine + state model in portal.md (2026-07-02b)._
_2026-07-02 (**SS-4 "ready to sign" now NOTIFIES the client** — Client Action-Required notifications Phase A, branch `claude/focused-stonebraker-a5f331`, dev_task `735adcf9`, SANDBOX) — Claude. **Origin: Michele Cotti / AI Venture Labs** — his formation sat at "SS-4 Prepared" and he had to ask us for an update; the audit found (a) every SS-4 send path was silent, and (b) his "Sign the SS4" portal tab existed only because the sign page leaked DRAFT SS-4s as signable (his SS-4 was never actually sent, still `draft`, still naming Gaia Pellegrinelli as responsible party — that data fix is a separate prod OPS item). Changes: **(1)** every `ss4_applications` transition to `awaiting_signature` now calls `notifySs4ReadyToSign` (`lib/portal/action-required.ts`) — all FOUR sites: `createSS4` with `ready_to_sign` (`lib/operations/ss4.ts` step 12), the workspace "Send to Client for Signature" (`app/api/flows/[id]/send-ss4`), "Re-send for Signature" (`resend-ss4`), and the MCP `ss4_update` explicit promotion (real transitions only, not content edits). The client gets a clickable portal chat message + IMMEDIATE bilingual email + bell/push deep-linking `/portal/sign/ss4`, targeted at the **SIGNER** (`ss4.contact_id` — the responsible party, never every MMLLC member; account-wide fallback for legacy rows without contact_id). Routes return `client_notified` per-channel results; dispatch is best-effort (never fails the staff action) and audited to `action_log`. **(2)** Sign-page draft leak FIXED (`app/portal/sign/page.tsx`): a `draft` SS-4 no longer renders as "awaiting" (only `awaiting_signature` is actionable; `signed`/`submitted`/`done` count as signed). Blast radius verified: exactly 1 prod draft row (Michele's). **(3)** Formation tracker "Sign your SS-4" amber glow is now gated on the SS-4 actually being signable: `getFormationTracker` returns `ss4SignPending` (queried only at "SS-4 Prepared"; fail-OPEN null on lookup issues) → `buildFormationTrackerSteps(..., signActionReady)` suppresses the sign glow when false. Full notification engine + language normalization in portal.md (same-date entry). NOT on production — Antonio ships._
_2026-06-29 (SS-4 signature REQUIRED + re-send when bad, branch `fix/ss4-signature-required-and-resend`) — Claude. **BUG: an SS-4 could be "signed" with NO real signature, and once signed there was no way to re-send.** Numero Uno Social LLC (Marinela Marku) tapped the signature pad ONCE; `signature_pad.isEmpty()` is false for a single dot, so the old guard (`!sigPadRef.isEmpty()`) accepted it. The dot is invisible on the PDF → the filed SS-4 looked unsigned (confirmed by viewing the stored signed PDF: typed name + date present, signature line blank; the captured `signature_data` PNG was a single dot). Fix: (1) NEW pure helper `lib/signature-validation.ts::isMeaningfulSignature` (≥6 points AND bounding-box max-dim ≥40px) + unit tests; the client signing page (`app/ss4/[token]/[code]/page.tsx`) now disables **Sign & Submit** until a real signature is drawn (`sigValid` from `pad.toData()` on `endStroke`) and re-checks in `handleSign` (blocks + error toast otherwise). Replaces the old `sigEmpty` gate. (2) NEW re-send path: `app/api/flows/[id]/resend-ss4` re-opens a `signed`/`submitted` SS-4 → `awaiting_signature`, clearing `signature_data`/`signed_at`/`signed_ip`/`pdf_signed_drive_id` (token + access_code preserved so the client's existing link still works), TOCTOU-guarded on `status IN ('signed','submitted')`. `components/flows/ss4-panel.tsx` signed state now shows a **"Re-send for Signature"** button (was a dead-end "Signed by client" confirmation). Architecture recap: client draws signature → pdf-lib overlays it client-side → POST `/api/ss4/[token]/upload-signed` (Storage) → `/api/ss4-signed` copies to Drive + merges Articles + emails support. NOT on production — Antonio ships._
_2026-06-25 (SS-4 formation date now staff-confirmed from the Articles, branch `fix/formation-date-from-articles`) — Claude. **BUG: the SS-4 formation date (Line 11 "date business started") was the DAY THE COMPANY WAS PROCESSED in the CRM, not the real state filing date** (Numero Uno Social LLC showed 6/25 — the materialization day — instead of the 6/18 NM filing date; data-fixed in prod). Root cause: the formation workspace creates the company by AUTO-ADVANCING into "Articles Received" when the Articles of Organization are uploaded (`app/api/flows/[id]/upload-document/route.ts` → `advanceServiceDelivery` → the materialize block in `lib/service-delivery.ts`). That path never passed a `formation_date`, so `materializeFormationCompany` defaulted it to `today` (`lib/operations/formation-materialize.ts:111-112`). The old admin "Upload Articles" button (`upload-articles` route) DID pass a date, which is why earlier companies (AI Venture Labs) were correct. Fix (approach: staff confirms an OCR-prefilled date): (1) shared pure parser `lib/articles-parse.ts::parseFormationDate` (+ unit tests) — also adds the NM "Date Filed: M/D/YYYY" header pattern the original `ocr-articles` regex MISSED; (2) `AdvanceStageParams.formation_date` threaded into the materialize call; the advance + upload-document routes accept it (ISO-validated); (3) best-effort OCR endpoint `app/api/flows/[id]/articles-formation-date?storage_path=` reads the just-staged Articles and suggests the date; (4) `components/flows/document-upload.tsx` — when uploading at the "Filed with State" formation stage, it stages the file, OCR-prefills, and REQUIRES staff to confirm the formation date in a modal before the upload commits/materializes. Non-UI callers (articles-detector cron, MCP) still fall back to today — a separate follow-up. The legacy `ocr-articles` route still has its own (now inferior) inline parser — migrate it to `lib/articles-parse` later. Audit done: only Numero Uno was wrong; Overview Group (same-day) verified correct._
_2026-06-24 (SS-4 signer rule now enforced on BOTH generation paths, branch `code-task/b6877282`) — Claude. **Corrects the "(2) Signer guard already correct / no change needed" claim in the entry below — it was incomplete.** There are TWO SS-4 generation paths and only one enforced the rule: (A) `lib/operations/ss4.ts::createSS4` (flow Workspace button + `ss4_create` MCP tool) DID block an MMLLC with ≠1 flagged signer; (B) `app/api/crm/admin-actions/generate-document/route.ts::generateSS4` (the CRM account-page "Generate SS-4" button, via `fetchAccountAndContact`) had NO signer check — it stamped the FIRST linked contact as responsible party. That path is how AI Venture Labs LLC's SS-4 got Gaia Pellegrinelli instead of Michele Cotti (the flagged signer). Fix: extracted the blocking rule into one shared, unit-tested pure helper `lib/operations/ss4-signer.ts::decideSs4Signer` (+ `ss4SignerAlertMessage`). Path B now queries members (ordered is_signer desc, is_primary desc), runs the rule, and: SMLLC → uses the owner (no alert, per Antonio 2026-06-24); MMLLC with exactly one flagged signer → uses that member (resolving a company member via its representative); MMLLC with 0 or >1 flagged → returns `{ error }` (HTTP 400) surfaced by `generate-ss4-dialog.tsx` as a toast/inline alert telling staff to flag exactly one signer in the Members section. The regenerate branch now also writes `contact_id` (previously it refreshed the responsible-party NAME but left the stale `contact_id`). createSS4 (path A) is unchanged — its inline rule already matched; `decideSs4Signer` mirrors it, so keep the two in sync. NOTE: "FL" vs "Florida" is intentionally NOT normalized — the form prints whatever the CRM holds (Antonio: they are the same thing). Still rides on the unapplied migration `20260625-1200-ss4-mailing-address.sql` — do not merge before that migration runs on the target DB. Michele's EXISTING SS-4 row still needs a one-time data correction (separate from this code change)._
_2026-06-24 (SS-4 mailing address + signer guard, branch `code-task/b6877282`) — Claude. **(1) SS-4 mailing address is now dynamic.** Previously `lib/pdf/ss4-fill.ts` wrote the hardcoded Ulmerton Rd TD address to Lines 4a/4b. Now `SS4FillData` carries `mailingStreet` + `mailingCityStateZip` (required fields). `createSS4` (`lib/operations/ss4.ts`) fetches the account's `business_mailing_address_id → addresses` join; falls back to `physical_address` (free-text, split on first comma); falls back to the new Park Blvd TD address (`11125 Park Blvd, Suite 104-153 / Seminole, FL 33772`). Values are stored in two new `ss4_applications` columns (`mailing_street`, `mailing_city_state_zip`, migration `20260625-1200-ss4-mailing-address.sql`). The live PDF renderer (`GET /api/ss4/[token]/pdf`) reads these columns and falls back to Park Blvd for legacy rows. **(2) Signer guard already correct.** `lib/operations/ss4.ts` already blocks MMLLC SS-4 generation with outcome `needs_signer_selection` when `is_signer` count ≠ 1; `generate-ss4/route.ts` already maps that to `{ success: false, error: result.message }` and `Ss4Panel` renders it in red. No UI change needed — the guard was already fully wired._
_Hotfix 2026-06-24 — Claude (PRODUCTION BUG: the formation wizard submission never advanced the SD off **"Payment Confirmed"**. The SD is pre-created at "Payment Confirmed" by `activate-service`, so when `handleFormationSetup` (`lib/jobs/handlers/formation-setup.ts`) ran on wizard submit it hit the "SD already exists" branch and just **skipped** — leaving the SD stuck at stage 1 forever (Davide Priori, blocking Luca). Fix: after resolving the SD (existing OR freshly created), advance it **Payment Confirmed → "Wizard Submitted"** via `advanceServiceDelivery`, gated on BOTH `stage === "Payment Confirmed"` AND a `wizard_progress` row for the contact with `status='submitted'`. The stage gate makes it idempotent (a re-run finds "Wizard Submitted" ≠ "Payment Confirmed" → no-op) and prevents regressing a later-stage SD. Verified end-to-end in sandbox: advanceServiceDelivery moved a seeded CF SD Payment Confirmed→Wizard Submitted (success, no error); "Payment Confirmed" has requires_approval=false so the approval gate doesn't block. Davide Priori's SD was already hand-fixed in the DB; this prevents recurrence. Committed directly to main + production per Antonio's hotfix approval.)_
_Hotfix 2026-06-23 — Claude (PRODUCTION BUG: new Company Formation SDs were created at the non-existent v1 stage **"Data Collection"**. The v2 pipeline (migration `20260617`) has no such stage — `stage_order=1` is **"Payment Confirmed"**. `createSD` trusts `target_stage` + `target_stage_order` verbatim (skips validation when both are passed), so both formation SD-creation call sites wrote an invalid stage: `lib/operations/activate-service.ts` (contact-scoped SD at activation) and `lib/jobs/handlers/formation-setup.ts` (SD at wizard submit). Both changed to `target_stage: "Payment Confirmed"`. Also fixed the CF test seeder `lib/mcp/tools/testing.ts::scenarioFormationStage1` and a stale CF example comment in `lib/services/stages.ts`. Other service types (ITIN/Closure/Banking/Client Onboarding) legitimately keep their own "Data Collection" stage_order=1 — untouched. Committed directly to main + production per Antonio's hotfix approval.)_
_Latest 2026-06-22 — Claude (Materialization now LINKS THE FORMATION OFFER to the new account, branch `fix/decision-note-visibility`. The portal "Set up your new company" banner (`app/portal/page.tsx`, `formation-in-progress-banner.tsx`) shows for `offers` with `contract_type='formation' AND status='completed' AND account_id IS NULL` keyed by `client_email` — so it persisted forever after formation because the offer's `account_id` was never set. `formation-materialize.ts` step 10d now sets `offers.account_id` on the linked formation offer right after linking the SD: matched by the SD's `source_offer_token` (canonical), falling back to `wizard_progress.lead_id` when the SD carries no token. Best-effort. Verified both paths clear the banner. (Same QA: the "client suggests a name on rejection" feature does NOT and never did create a "New LLC Names Needed" request — the suggested name lands as a PENDING candidate so the workspace is no longer all-dead and the staff "Request New Names" button correctly doesn't appear; no code change needed there.)_
_Also 2026-06-20 (full E2E browser QA) — Claude (Walked all 7 stages incl. SS-4 on a reset test contact; two more SS-4 bugs found + fixed. **(C) Unsigned SS-4 leaked to the client portal.** `lib/operations/ss4.ts` step 10 registered the unsigned SS-4 `documents` row with `portal_visible: true` — so it showed on the client Documents page (the task's "only unsigned SS-4 shows"). Per Antonio the SS-4 (signed OR unsigned) is INTERNAL only; changed to `portal_visible: false`. The client still signs via the Sign-Documents flow (driven by `ss4_applications.status`), not this row, and staff still see it in the workspace viewer (SD-scoped). With the earlier `ss4-signed` fix, BOTH SS-4 docs are now `portal_visible=false`. **(D) Draft-state "Preview SS-4" button was still small.** The original enlarge fix's `replace_all` only caught one of the two identical-looking links (different indentation); the draft-state link in `ss4-panel.tsx` kept the old text-link style — now the same enlarged bordered button as the awaiting-signature one. **Sandbox note:** the `signed-ss4` storage bucket was missing in sandbox (exists in prod) so `upload-signed` silently failed and the signed doc wasn't created; created the bucket in sandbox. Verified live: after signing, both SS-4 docs are SD-linked + internal, the signed PDF stores, the workspace viewer shows all 3 docs, and the client portal shows Articles but NEITHER SS-4.)_
_Latest 2026-06-20 — Claude (Formation-workspace QA fixes, branch `fix/formation-workspace-bugs`. **(1) Banking Fintech SD is NO LONGER created at EIN.** Per Antonio, formation finishes at the EIN; banking is self-service. Removed the SD-creation block from ALL THREE EIN paths — `lib/operations/ein-received.ts` (`triggerEINReceivedWorkflow`), `record-ein-received/route.ts`, and the `enter_ein` contact-action (`contact-actions/route.ts`); each now pushes a `banking_sd_skipped`/skip note and `banking_sd_id` stays null. Tests updated (`ein-received-trigger.test.ts`). The old `/portal/wizard` banking tabs + sidebar "Bank Applications" relabel were SD-driven and thus retire with it. **(2) Signed SS-4 is INTERNAL-only.** `app/api/ss4-signed/route.ts` now writes the signed-SS-4 `documents` row `portal_visible=false` (both Drive + storage-fallback paths); `Company Formation` is added to the client-safe flow-doc allowlist with **only** `Filed with State` (Articles) / `Articles Received` / `EIN Received` (CP 575) — NOT `Signed`. So the client portal shows Articles + EIN letter but never the SS-4 (responsible-party tax ID). The signed SS-4 still shows in the staff workspace viewer (service_delivery_id-scoped, not portal_visible-gated). **(3) Expanded EIN-completion message** (`save-ein/route.ts`, EN+IT): keep docs safe, never share the signed SS-4, open a bank with EIN + Articles, recommended banks (Relay/Mercury/Sokin/Payset/Wise), describe-your-business guidance. **(4) New self-service `/portal/banks` page** + sidebar "Bank Applications" nav item (active tier only) replacing the old banking wizard. **(5) Materialization backfill:** `formation-materialize.ts` step 10a relinks ALL docs stamped with the formation SD that still have `account_id=NULL` (the Articles uploaded on "Filed with State" before the account exists), so they reach the account-scoped portal Documents page. **(6) Account "EIN Application" card** (`ss4-pipeline-card.tsx`) now links to the formation workspace `/flows/[sd]`. See flows.md for the workspace-side overview/SOS/doc-viewer fixes. NOT yet deployed.)_
_2026-06-29 — Claude (PIPELINE EXTENDED to **8** stages — added "SS-4 Sent to IRS" (order 7) between "SS-4 Signed" (6) and "EIN Received" (moved to 8). Bug: uploading the fax receipt at "SS-4 Signed" auto-advanced the SD directly to "EIN Received" (skipping the waiting period). Fix: new intermediate stage acts as the waiting stage; the document_upload auto-advance at "SS-4 Signed" now correctly lands at "SS-4 Sent to IRS". From there, staff click "EIN Received" (action_button) when the CP 575 arrives — this triggers `formation.confirm_ein_received` which sets `accounts.ein_number` + advances to "EIN Received". Client portal shows "SS-4 Faxed to IRS – Waiting for EIN · Faxed <date>" with the fax-receipt-upload date from SD stage_history (same mechanism as `filedAt` on "Filed with State"). Removed the premature `action_buttons → EIN Received` from "SS-4 Signed" (was allowing staff to skip the new stage). `pipeline_stages` client_label for "SS-4 Signed" corrected from "SS-4 sent to IRS" → "SS-4 signed". `audit-chain` STAGE_EIN_RECEIVED: 7→8; `place-client-wizard` Stage 7→8; `testing.ts` fixture stageOrder 7→8; comments in `pipeline-utils.ts`, `faxage-ss4-confirm/route.ts`, `formation-confirm-ein-received.ts` updated. Sandbox DB applied. Migration: `20260629-1000-ss4-sent-to-irs-stage.sql`. NOT on production — Antonio ships.)_
_Last verified against code: 2026-06-17 — Claude (PIPELINE REDESIGNED to **7** client-perspective stages — Company Formation **Workspace v2** (branch `feat/formation-workspace-v2`, migration `20260617-formation-workspace-v2.sql`, **NOT yet applied**): **Payment Confirmed → Wizard Submitted → Filed with State → Articles Received → SS-4 Prepared → SS-4 Signed → EIN Received**. Supersedes the abandoned 8-stage redesign (separate "Name Check" stage) — v2 folds the SoS name check into the working "Wizard Submitted" stage and makes "Filed with State" a loop stage (approved → Articles, or name rejected → refile). Each stage carries `stage_layout` (drives `/flows/[sd]`) + `client_label`/`client_label_it` (drives the portal tracker). **Side-effects moved to "Articles Received"** (hardcoded by stage name in `lib/service-delivery.ts` §12 compliance-date init + §13 welcome-package enqueue — was "Post-Formation + Banking"). **SS-4 fax no longer auto-advances** (`markFaxAsSent`/`pipeline-utils.ts`; `advanceToEinSubmitted` DELETED) — the SD stays at "SS-4 Signed" until staff advance to "EIN Received". **ALL FOUR EIN-advance paths retargeted "Post-Formation + Banking" → "EIN Received":** the `formation.confirm_ein_received` workflow handler, the "Record EIN Received" button (`record-ein-received/route.ts`), `triggerEINReceivedWorkflow` (`ein-received.ts`), and the `enter_ein` contact-action — all keep their existing idempotent side-effects (Banking SD, tier→active, welcome package). `place-client` presets remapped; `audit-chain` repair thresholds remapped (Articles=4 / SS-4 Prepared=5 / EIN Received=7); `active-onboardings`/`contact-detail`/`ss4-pipeline-card`/place-client-wizard stage arrays + labels updated; test fixtures updated. data_viewer reads `wizard_progress` for formation SDs; new RA-activation button + formation SoS link (see flows.md). **Account creation still happens at materialization (`formation-materialize.ts`), unchanged.** **Promotion note:** the migration writes `pipeline_stages.stage_layout`, which production lacks — apply `20260615-1700-flow-workspaces-prod-columns.sql` FIRST on production.)_
_Earlier 2026-06-16 — Claude (added `setServiceDeliveryShipping(sdId, {courier, trackingNumber})` — the single authorized write path for the ITIN client shipping-tracking columns (`shipping_courier` / `shipping_tracking_number` / `shipping_submitted_at`, migration `20260616-2300`), since service_deliveries is a protected table. Called by the portal shipping API; stamps `shipping_submitted_at=now()`. See flows.md/portal.md for the full feature.)_
_Earlier 2026-06-14 — Claude (added `revertServiceDelivery()` — flow Workspace "← Go Back" inverse of advance: steps the SD back one stage by name, deletes the previous/target stage's documents, resets a completed final stage to active, and undoes the +1y renewal-date bump when leaving "Closed"; prior 2026-06-11 contract-first entity-type resolution via resolveEntityTypeForFormation (Adam Mihaly/LUMA incident); prior 2026-06-10 uq_formation_sd_active_per_offer index)_

## What it is
The end-to-end flow of creating a client's US LLC: from a signed formation offer + payment, through filing the company with the state, to receiving the EIN — at which point the client becomes a fully active client. It's the core product, multi-step, and several automatic side-effects fire at each stage, so it's high-risk to touch blindly.

## The lifecycle (happy path)
1. **Signed + paid** → `activateService()` (`lib/operations/activation.ts`) orchestrates lead→contact, ensure-account, create service deliveries, set tier. It creates a **"Company Formation"** service delivery (SD) — **contact-scoped (`account_id=NULL`)** because the LLC does not yet exist (Articles pending). The SD's `notes` carries the offer_token for dedupe, and the token is also stored first-class in `service_deliveries.source_offer_token`. **A partial unique index `uq_formation_sd_active_per_offer` on `(contact_id, source_offer_token)` WHERE `service_type='Company Formation' AND account_id IS NULL AND status='active' AND source_offer_token IS NOT NULL` makes a duplicate physically impossible** — two concurrent/retried activations for the same offer can no longer both insert (the TOCTOU that gave Michele Cotti two formation SDs 2s apart, 2026-06-10). `activate-service` catches the unique violation and re-selects the winner, so the race is silent. The key is per-offer, so a legitimate second simultaneous new-company formation (different offer = different token) is unaffected. `formation-setup.ts` dedupes at wizard submit so no double-create.
2. **Account starts at tier `formation`** (company being formed, no EIN yet). Account status "Pending Formation". The client sees a **formation-specific dashboard/wizard**. Tier is derived by `tierForContract('formation')` → `'formation'` (`lib/portal/auto-create.ts`). **Exception — returning active client:** when the contact is already at tier `active` from another LLC, `syncTier` correctly refuses to downgrade. No new account row is created at payment either. The portal switcher (`getInProgressFormations` in `lib/portal/queries.ts`) sources the new entity directly from the contact-scoped Company Formation SD — the client sees a switcher with the existing LLC + "New company (in formation)", and the formation entity carries `tier='formation'` implicitly via `resolveSelectedEntity`.
3. **Formation data reviewed** (`formation_form_review` tool) — can upgrade tier onboarding→formation and advances the SD.
4. **Filing with the state** happens via Harbor Compliance (see the compliance system / `hc_*` tools).
5. **EIN received** → the formation→active hand-off runs (see below). Tier advances to **`active`**; the client gains full portal access (documents, invoices, chat).

## The EIN→active hand-off (the critical transition)
Canonical implementation: `triggerEINReceivedWorkflow()` in `lib/operations/ein-received.ts`. Triggered by the **"Record EIN Received" button** (`POST /api/crm/admin-actions/record-ein-received`) or the `enter_ein` action (`app/api/crm/admin-actions/contact-actions/route.ts`). It finds the active "Company Formation" SD and, **each step idempotent**:
1. Creates (or finds) a **"Banking Fintech"** SD.
2. Advances the formation SD to stage **"Post-Formation + Banking"**.
3. `syncTier({ newTier: 'active' })` — promotes the account.
4. Enqueues the **`welcome_package_prepare`** job.
5. Logs everything to `action_log`.

## Business rules
- **R102** — Portal tier has exactly 4 values (`lead`, `formation`, `onboarding`, `active`). All tier writes go through `syncTier()` in `lib/operations/sync-tier.ts` — **never write `portal_tier` directly, never advance tier manually.** `formation` = no EIN yet; EIN advances it to `active`.
- **R106** — Service types live in the catalog (`catalog_entries`, `catalog_id='services'`); code imports from `lib/services/index.ts`. `createSD` auto-sets `service_type_entry_id`.

## How it's built
### Key functions / files
- `lib/operations/activation.ts` → `activateService()` — the top-level "payment confirmed → spin up the client" orchestrator.
- `lib/operations/service-delivery.ts` → `createSD()` (central SD creator), `advanceStage()`, `completeSD()`, `revertServiceDelivery()` (flow Workspace "← Go Back" — inverse of advance: resolves prev stage by name, deletes the target stage's `flow_stage` documents, resets `completed`→`active`/clears `end_date`, and on leaving a "Closed" renewal final subtracts 1y from `ra_renewal_date`/`annual_report_due_date`). Backs `POST /api/flows/[id]/revert` + `components/flows/go-back-button.tsx`. Does NOT reopen auto-closed tasks or delete the underlying Drive file (only the documents row).
- `lib/operations/ein-received.ts` → `triggerEINReceivedWorkflow()` — the EIN→active hand-off.
- `lib/operations/sync-tier.ts` → `syncTier()` — the ONLY legal tier writer (R102).
- `lib/portal/auto-create.ts` → `tierForContract()`, ensures a minimal CRM account for formation clients.
- `lib/mcp/tools/formation.ts` → `formation_form_create/get/review`, `formation_confirm`. Related: `member_info_form_*`, `ss4_*` (MMLLC EIN docs).
- UI: `app/formation-form`, CRM "Record EIN Received" button.

### What `createSD` does (every SD, not just formation)
Resolves the first pipeline stage for the service type; propagates `is_test` from the account/contact; resolves `service_type_entry_id` from the catalog; inserts `service_deliveries`; then **fires `dispatchWorkflowForSdCreated`** (the workflow engine hook — fire-and-forget). ITIN SDs are forced to contact-only (account_id=null).

### Tables
`service_deliveries`, `service_delivery_stage_history`, `accounts` (`portal_tier`, `status`, EIN), `contacts`, `account_contacts`, `pipeline_stages`/catalog, `leads`, `pending_activations`, `jobs` (welcome package), `action_log`.

## Gotchas, invariants & past bugs
- **Never advance tier by hand or write `portal_tier` directly** (R102) — EIN→active happens only through the EIN-received workflow / `syncTier`.
- **Materialization forces `formation` tier with `allowDowngrade: true`.** `accounts.portal_tier` **defaults to `'active'`** at insert, so a plain `syncTier('formation')` is treated as a downgrade and silently no-ops — leaving a just-formed company wrongly at `active`. `formation-materialize.ts` passes `allowDowngrade: true` so the new account becomes `formation`. Safe because materialization always runs **before** the EIN exists and the `already_materialized` guard blocks re-runs on an account that has since gone active. The contact-tier cascade still uses `computeContactTier` (MAX across the contact's accounts), so a returning client who already owns an active company keeps their `active` contact tier — only the new company's account row is `formation`.
- **MMLLC nuance:** the `ein-received.ts` helper **intentionally excludes** the MMLLC member-info portal-message flow, so silent inline EIN edits don't auto-message clients. MMLLC clients must be promoted via the explicit "Record EIN Received" button, which includes that messaging.
- **Entity type is resolved CONTRACT-FIRST at materialization (2026-06-11, Adam Mihaly/LUMA incident).** `formation-materialize.ts` calls `resolveEntityTypeForFormation` (`lib/portal/entity-type-from-contract.ts`): priority is admin override (`entity_type` param on the materialize call / `upload-articles` form field) → **signed contract** `contracts.llc_type` via this formation's `wizard_progress.lead_id` → offers → contracts (contact-wide fallback only when all signed contracts agree) → `formation_submissions.entity_type` → `wizard_progress.data.entity_type`. If nothing resolves, materialization FAILS with outcome `missing_entity_type` — it never silently defaults to SMLLC (the old default created LUMA as SMLLC while the signed contract said MMLLC, and the SS-4 went to the client with 1 member). A contract-vs-form conflict resolves to the contract and pushes an `entity_type_conflict` warning step. `accounts.member_structure` (`single_member`/`multi_member`) is written in lockstep with `entity_type`, and the wizard-fallback self-heal row records the RESOLVED code, never a default.
- **SS-4 regeneration (CRM):** the Generate SS-4 dialog can **regenerate an UNSIGNED SS-4 in place** (`generate-document` route, `regenerate: true`) — refreshes entity type, member count, title, county from the account's current data while keeping the same token, so the client's existing link shows the corrected form. Signed/submitted/faxed SS-4s are locked and refuse regeneration.
- **`createSD` fires the workflow dispatcher** (sd_created trigger) fire-and-forget in a try/catch — a workflow failure won't block SD creation, but check the dispatch result/logs if an expected follow-up didn't spawn.
- **Shared "Closing" stage:** the Closure system shares stage names with formation; any stage migration must scope `service_type='Company Formation'` only.
- **⚠️ In-flight redesign (NOT shipped):** "Flexible Formation Lifecycle" (formation ends at EIN=active, banking/lease decoupled, OA self-service) is on branch `feat/flexible-formation-lifecycle` (dev_task `cdc79f20`), planned + audited but **not built**. Do not assume the redesigned behaviour in current production — verify against the code above.

## How to verify current state
- Read `lib/operations/ein-received.ts` (the canonical EIN→active steps), `activation.ts` (`activateService`), `service-delivery.ts` (`createSD`).
- A client's formation SD: `SELECT id, stage, status, account_id, contact_id FROM service_deliveries WHERE service_type='Company Formation' AND (account_id='<account_id>' OR (account_id IS NULL AND contact_id='<contact_id>'));` — in-flight formations are contact-scoped (`account_id IS NULL`), materialized formations are account-scoped.
- Tier logic: `lib/operations/sync-tier.ts` + `tierForContract` in `lib/portal/auto-create.ts`.
- Pipeline stages for Company Formation: query `pipeline_stages` / the services catalog.
- Note (R096): sandbox via sandbox MCP / `psql`; production `execute_sql` hits production.
