# Staff Sticky Notes (floating post-its)
_Last verified against code: 2026-09-08d — Claude (**FOUR REAL REGRESSIONS FROM THE 2026-09-08c ROUND, FOUND BY AN EtoE QA + BUG HUNTER PASS AND FIXED.** Antonio, after using the horizontal-cascade/menu/Move round below for a bit: "it works but worse than before and it doesn't do what promise" — "do and EtoE QA." System Counselor orientation (2026-09-08c's own doc comments and the shipped code were re-checked against live behavior, not trusted at face value) + a live browser pass + an independent Bug Hunter code review, both requested per Antonio's own established two-reviewer pattern for this subsystem, found four real, cited problems — approved for a fix with "go ahaed":

**(1) The "+" button's menu opened at a fixed screen corner, disconnected from the button itself, once the button had been dragged.** The button has been draggable since 2026-07-23; the menu introduced in the 2026-09-08c round never accounted for that — it was a literal fixed CSS position, unrelated to wherever `useDraggableFab` had actually moved the button to. Live-reproduced: dragged the button to a new spot, clicked it, the menu opened at the OLD default corner instead. Fixed by measuring the button's own real position (via its existing ref) the moment the menu opens, and positioning the menu relative to that measurement — only when the button has actually moved; the untouched default case still uses the original CSS corner unchanged.

**(2) A plain click on the "+" button could silently do nothing.** The button's drag-vs-click distinction (shared with every other draggable button in this codebase, `lib/ui/draggable-fab.ts`) treats any pointer movement past 8px between press and release as a drag, suppressing the click entirely — a reasonable, deliberate threshold on its own, but the 2026-09-08c round made a plain click twice as consequential (it now has to clear TWO steps — open the menu, then pick an option — instead of one), so a click silently eaten by ordinary mouse/trackpad wobble costs more than it used to. Verified precisely with controlled pointer-event tests: a true zero-movement click always opened the menu; a click with roughly 10px of incidental movement often did not. `useDraggableFab` gained an optional per-caller threshold override (default unchanged, so the chat launcher and the Captures popup handle are untouched); the notes "+" button opts into a wider 16px tolerance. Re-verified live: the same 10px movement that used to fail now opens the menu, and a genuine, deliberate drag (200px+) still correctly suppresses the click exactly as before — confirmed both ways, not just the one that was broken.

**(3) The horizontal cascade could overlap notes within the SAME row, not just between rows, on ordinary laptop screens.** The Bug Hunter's math was more precise and worse than my own first live finding: at `perRow=5`, the 4th and 5th columns' independent per-note edge-clamping (in `notePosStyle`, unaware of where a row NEIGHBOR actually landed) compresses into real overlap across the ENTIRE common laptop width range (1024px up to ~1409px, covering 1366×768 — historically the single most common laptop resolution) with only 5 notes on screen, no row-wrapping required at all. Separately, the row-to-row vertical gap (0.04 of viewport height) was sized for a viewport taller than ~1000px to clear a collapsed note's own ~40px height — true on a large monitor, false on most laptop screens, so a 6th note wrapping to row 2 visibly overlapped row 1. Both fixed together in `cascadePos`: `perRow` dropped from 5 to 4 (verified by hand that 4 columns' raw fractions stay clear of the edge-clamp across the whole 1024px+ range this layer renders at) and the row gap doubled from 0.04 to 0.08 (clears a 40px chip with margin down to roughly 600px of usable height). Re-verified live at the exact 1280×720 viewport that reproduced the original bug: six notes, zero overlapping pairs, four in row one and two in row two with a comfortable gap.

**(4) Still open, not fixed this round, no new direction from Antonio yet:** the shallow horizontal band substituted for true "avoid covering important info" (2026-09-08c) still measurably covers each page's own title on pages other than the dashboard home page (confirmed on Accounts, Research, and Finance). This was a technical judgment call made at build time without a distinct yes/no from Antonio on that specific narrowing, flagged as such when reporting these four findings. Left exactly as-is pending his direction — his "go ahaed" approved the four items above, not a redesign of this one.

The select-mode click-to-select mechanism (plain click anywhere on a note's chip, no checkbox precision or modifier key) was specifically investigated as a possible fifth regression — the Bug Hunter actively tried to break it and could not; it already worked exactly as Antonio described wanting, unchanged since the prior (2026-09-08, Parked) round, and needed no fix.

Full suite green (10,725 tests — one file flaked once on a full run, passed clean on immediate re-run, confirmed unrelated to any file this round touched), lint clean, clean build, one new unit test added for the wider drag-threshold behavior. Live-verified all three code fixes on a stable, non-shared deployment URL first (the shared sandbox alias was mid-contended by another active session's team-chat work during this exact investigation — see the 2026-09-08c entry above for the established workaround), then re-promoted to the shared alias Antonio actually uses and re-confirmed the build stamp matches. All QA test notes deleted afterward. Still nothing near production.)_
_Prior: 2026-09-08c — Claude (**HORIZONTAL CASCADE, THE "+" BUTTON BECOMES A MENU, AND A NEW "MOVE" ACTION — three more requests from Antonio's own live testing of the Parked ship below, same sandbox session.** Antonio, after checking his own real account (3 real notes, never parked): "why I still see the notes in the middle of the page? can I select all of them once and move all togehter?" Then, from a photo of his physical monitor: "I dont' want only to park them, I want to move in the screen changing spot... When I create a new note i want it to appear in a blank spot instead of covering important info in the page and they must be ordered orizzontaly instead of vertically." Then: "why don't inglobe the select button in the '+' icon of creating a new notes instead of creating a noisy with a lot of icones? When I click on the current '+' a menu will open with the options, 'create a new note' - 'select to move...' - 'select to park'." Approved as one build: "build it."

**(1) Horizontal, not vertical cascade.** `cascadePos` (`lib/notes/note-position.ts`) was column-major — each never-moved note's default slot stepped DOWN the same left column, which is exactly the "middle of the page, stacked" look Antonio was pointing at. Rewritten row-major: fills left-to-right (`perRow=5`, `step=0.18` of viewport width) along one shallow horizontal band before wrapping to a second row 0.04 lower. "Blank spot" is out of scope for the reason already on record in this same file's own code comment — there's no generic way to know what a given page's real content looks like from here — so a fixed horizontal band under the header, not full page-content-avoidance, is what was proposed and approved. `tests/unit/note-position.test.ts`'s old "fills a column" case replaced with a row-major equivalent (asserts 5 notes share one row before a 6th starts a new one). Live-verified by creating 6 real notes: the first 5 landed on one row, left-to-right, the 5th correctly capped at the on-screen edge; the 6th wrapped to row 2.

**(2) The "+" button opens a small menu instead of sitting next to a second icon.** The separate "select notes to park" button that shipped in the entry below is REMOVED. The existing "+" (unchanged position/drag behavior — `useDraggableFab`'s ref stays on the same element) now toggles a small menu: "New note" and "Select notes" (the latter hidden when there are 0 or 1 notes on screen — nothing to bulk-select). Disclosed simplification: the menu panel renders at a fixed spot near the button's default corner, not wherever the button itself may have been dragged to — Antonio drags notes, not this control, in every session so far, so this wasn't built to track it.

**(3) A new "Move" action, separate from Park.** Select mode (reached via the new menu) now offers Move alongside the existing Park in its floating toolbar. Move takes every selected note and reassigns it a fresh cascade slot among the notes NOT selected — same `cascadePos` function as a brand-new note, just run against the current on-screen occupancy — then exits select mode. The notes stay exactly what they were (regular on-screen notes), just moved; Park still sends them to the header shelf, untouched. **The mechanic that made this possible:** a note's on-screen position is decided in two layers that each independently cache their answer — the parent's own position-assignment map (computed once per note list and remembered in a ref) and each note's own internal position state (a `useState` that, per React's own contract, only reads its initial value at first mount and never re-reads it just because a parent recomputed something). Move has to invalidate both: a new epoch counter forces the parent's assignment map to recompute, and a per-note version counter folded into that note's list `key` forces React to actually remount it so its internal state re-reads the fresh value — the exact stale-position trap this file's own `note-position.ts` header comment already describes for a different case (2026-09-05 entry below), now hit and fixed a second time in a new spot. Live-verified with real before/after coordinates: selected 2 of several on-screen notes, clicked Move, confirmed both actually changed position (not just the selection state), confirmed neither showed up in the Parked shelf (checked the parked feed directly, not just visually), confirmed select mode closed, and confirmed the new positions were still there after a full page reload — not just held in memory.

Full suite green, clean build, each of the three committed and deployed separately, re-promoted to the same shared sandbox alias Antonio already had open each time. All QA test notes deleted afterward. **Still open, unresolved from Antonio's own report:** a "small hand" cursor artifact visible in his photo, sitting outside the note being interacted with — not yet diagnosed; asked him whether it only appears while actively dragging a note or is present at rest, awaiting his answer. Nothing in this round touched the migration or went near production — same standing state as every entry below.)_
_Prior: 2026-09-08b — Claude (**END-TO-END QA + BUG HUNTER on the Parked-state ship, requested live on the shared sandbox link — 3 real bugs found and fixed, one of them a production migration that would have failed outright.** Antonio: "browse here [sandbox link] and do an EtoE QA with the bug hunter." My own live walkthrough (create/park/unpark from every surface, bulk-select, both header controls, mobile) plus a Bug Hunter pass reading every changed file end to end.

**Bug 1 — search icon opened the box but never focused it.** Clicking the collapsed search icon (or Cmd+K while collapsed) visibly expanded the box, but the input never actually received keyboard focus — typing immediately after landed nowhere. Root cause: `GlobalSearch`'s own `.focus()` call fires on the SAME synchronous tick that triggers the expand, before React commits the width-expansion re-render, so it's called on an input still sitting inside its width-0/overflow-hidden collapsed wrapper — enough to bubble an `onFocus` up (which is why the box visibly opened) but not enough to stick. Fixed: once `expanded` is confirmed true by ANY path, `CollapsibleSearch` now re-focuses the input itself, after React has committed the real width. One mechanism for every trigger.

**Bug 2 — opening a parked note ALWAYS navigated away, never in place, 100% of the time.** Not a rare degrade like the Alerts bell's own documented gap below (2026-09-04d) — every single parked note, because `isLiveOrRevivedFor` unconditionally excludes parked notes from the `?scope=active` feed the open-in-place listener checks, so `requestOpenNote` could never claim one. Fixed: `sticky-notes-layer.tsx` now ALSO loads the parked feed (same `'staff-notes-parked'` query key the header trigger already uses — a cache hit, not a second request) and checks it too. Still synchronous-only, still honestly degrades to navigation for the narrower snoozed/archived-but-alerting case, same as before.

**Bug 3 — the neighbor-avoidance nudge (shipped a few hours earlier the same day) could apply a position it never actually tested.** It searched for a clear spot WITHOUT the on-screen bound, then clamped to the bound only at the very end — so on a short viewport with several notes already clustered, the final applied position could be one that was never checked, silently reintroducing the exact overlap that fix was built to prevent. Fixed: the bound is now part of the search loop itself, so whatever position is finally applied was always a value the loop actually tested — worst case an honest, tested best-effort, never an untested guess.

**Bug 4 — the mutual-exclusivity CHECK constraint would have failed the moment it was run against production.** The Bug Hunter flagged the risk from reading the migration; checked it directly against production with a read-only query (nothing written) and confirmed it, concretely: 38 real rows already have both `archived_at` and `snoozed_until` set today, from the exact pre-existing gap this same change fixes. A bare `ALTER TABLE ADD CONSTRAINT CHECK` validates every existing row by default — so applying this as first written would have blocked the whole migration outright. Fixed: added a backfill immediately before the constraint, clearing `snoozed_until` wherever `archived_at` is also set — behavior-preserving, not a judgment call, since every read path in this file already checks archived_at first, so snoozed_until on those rows was already being silently ignored in practice. Sandbox needed no re-apply (confirmed zero violating rows there at the time it was originally migrated).

**Two minor Bug Hunter findings intentionally left as-is, both self-recovering:** a stale nudge offset can survive after the neighbor that caused it is removed via bulk-park (cosmetic, cleared by any manual drag or collapse/re-expand); a partial bulk-park failure shows one aggregate error without naming which note(s) failed (a user has to re-scan). Everything attacked but NOT broken: mutual exclusivity across all six park/unpark/snooze/archive actions, true simultaneous nudge-vs-nudge between two expanding notes (sequential event dispatch makes it impossible), the nudge effect's own dependency array (no infinite-loop risk), `refreshOnMount` firing exactly once per page load inside the new overflow menu, both `ui_events` realtime key-lists, zero client-facing surface touched.

All three fixes re-verified live on the SAME sandbox link Antonio was already looking at (redeployed, re-promoted to the shared alias each time, not a private URL) — typed text confirmed landing in the search box after a real click; a parked note's editor confirmed opening with the page address unchanged; the collision-avoidance re-confirmed still clearing neighbors correctly after the rewrite. Full suite green, clean build, each fix committed separately. Still nothing near production — the rewritten migration needs Antonio's own review before he applies it there, same as always.)_
_Prior: 2026-09-08 — Claude (**PARKED — A FOURTH PER-PERSON NOTE STATE, plus the desktop header rewrite that clears room for it.** Antonio sent a screenshot of floating notes cluttering a busy CRM page, covering real content: "they are just in the middle of screen. the place it too bad. we need another place and solution to manage them. call the UX Designer. I want a solution to select of them an move all together in another place." A second screenshot, of the desktop header itself: "you can reduce the 'Enable notification', 'Help' and the serch bar. The seacrh bar cuold appear only when we click on it. you could also consider three dots for 'enable notification' and 'help'. so you can put the notes on the top." Asked to define the exact state model before any code was written, he was precise: "'moving a note there' is where the notes are gropued and open. a new note will appear on the screen wherever. 'parked' button move it to the top, 'done' disappear from the screen, Snooze, disappear and reappear when it's time" — confirming Parked is a genuinely NEW 4th per-person note state (Active/Parked/Done/Snoozed), not a repurposed Snooze.

**Challenged by the Bug Hunter before a line of code was written** (3 blockers, 3 majors, 3 minors, all folded into the build below) — most notably a REAL PRE-EXISTING gap, independent of Parked entirely: `setMyNoteState` (the one function archive/unarchive/snooze/unsnooze already went through) only ever wrote the ONE field being changed (`archived_at` or `snoozed_until`), so a revived Done note's card still offered an unconditional Snooze button, and clicking it left BOTH `archived_at` AND `snoozed_until` set on the same per-person row at once — an inconsistent state that predates this feature entirely and is fixed by the same rewrite (see the API item below). Antonio then approved building everything in one pass, backend plus both header redesigns: "buuild everything together. FYI I use desktop most of the time" — which is also why the bulk select-and-park UI (below) is desktop-only: mobile's note list is already a scrollable sheet, not scattered floating clutter, so that specific problem doesn't exist there; individual per-note parking still works everywhere.

(1) **Migration** `scripts/migrations/20260908-0000-staff-note-parked.sql` — applied to sandbox only so far, NOT yet promoted to production. Adds `parked_at TIMESTAMPTZ NULL` to `staff_note_state`, plus a CHECK constraint `staff_note_state_one_status_check` enforcing at most one of `archived_at`/`snoozed_until`/`parked_at` non-null per row. The migration's own header documents that this dual-layer enforcement — the app nulls the other fields on any write, the DB constraint is the backstop for anything that bypasses the app — is deliberate. **⛔ DEPLOY ORDER IS LOAD-BEARING, the same lesson this file has already paid for twice — see the 2026-07-23 and 2026-09-04c entries below:** production code referencing `parked_at` must not ship until the column is verified live in production by direct query, not by trusting the deploy succeeded.

(2) **`lib/notes/staff-notes.ts`:** new `isParkedFor(note, userId)` — explicit per-person-row-only, deliberately NO legacy-column fallback, since `parked_at` postdates the single-shared-column era entirely and there is nothing to fall back to (unlike `archived_at`/`snoozed_until`, which do carry the legacy fallback documented in the 2026-07-23 entry below). `isLiveFor` now also excludes parked notes from the floating layer. `isLiveOrRevivedFor` (2026-09-07 entry below) now returns false immediately if the note is parked for that person — a parked note does NOT revive back onto the scattered floating layer on a fresh reply the way a Done note does; it deliberately stays in the Parked shelf and turns red there instead, via the EXISTING, unmodified alerts feed. Confirmed by reading `computeNoteAlerts` (`lib/notes/staff-alerts.ts`) itself this session: its `note_reply` alert is gated ONLY on `isSnoozedFor`, never on archived or parked state at all, so a reply on a parked note already produces an alert today with zero changes needed there. `otherPersonState`'s return type gained a `'parked'` case. New `listParkedNotesForUser(userId)` mirrors the existing `listActiveNotesForUser` shape.

(3) **API** (`app/api/crm/staff-notes/route.ts`): a new `?scope=parked` GET branch returns `{notes, me, members}`; new `park`/`unpark` PATCH actions. `setMyNoteState` — now the one function ALL of archive/unarchive/snooze/unsnooze/park/unpark go through — was rewritten for mutual exclusivity: it now explicitly nulls the other two status fields whenever it writes any one of them to a non-null value. This is the fix for the pre-existing Done/Snooze gap the Bug Hunter found, above.

(4) **New component** `components/dashboard/parked-notes-trigger.tsx` (`ParkedNotesTrigger`) mirrors the existing `StaffAlertsBell`'s compact/full dual-rendering pattern exactly — icon+badge on the mobile header, button+dropdown on the desktop header — so Antonio and Luca reuse a mental model they already know. Fetches the new `scope=parked` feed under its own react-query key `'staff-notes-parked'`, but cross-references the SAME shared `'staff-alerts'` query key the floating layer and the Alerts bell already use for red/unread styling — deliberately reusing that cache rather than inventing a second, parallel read-tracker that could disagree with the bell about the same note. Base state (parked notes exist, none unread) renders neutral/amber; any unread parked note turns the whole trigger solid red with the same `animate-pulse` treatment the Alerts bell got in the 2026-09-07 entry below. The dropdown/sheet lists each parked note with a snippet, the client name if any, and a "Send back to screen" button calling the new unpark action; opening a note goes through the existing `requestOpenNote` mechanism (`lib/notes/open-note.ts`, 2026-09-04d entry below) that the Alerts bell already uses, so it opens in place rather than navigating away. **Live QA (below) found the desktop dropdown was gated on `count > 0`, so clicking the trigger at zero parked notes was a dead click** — the mobile compact version correctly hides itself entirely at zero, but the desktop trigger stays visible as a stable header fixture and should still respond; fixed to always open, showing the existing "Nothing parked right now" empty state.

(5) **`sticky-notes-layer.tsx`:** an individual Park button joins the existing Snooze button in `NoteCardBody`'s action row — a floating note is by definition never already parked (parked notes are excluded from the active feed entirely), so this is a one-way action, not a toggle; it removes the note from the floating layer immediately via the existing invalidate-and-refetch pattern. Also added a **desktop-only select mode**: a new toggle button (checkbox-square icon) near the existing new-note "+" button collapses every floating note into a compact, checkable chip regardless of its own individually-expanded/collapsed state (avoiding the ambiguity of which part of an open card selects vs. opens it), dragging is disabled while selecting, and a small floating toolbar shows a live selected-count with a "Park" button that fires the `park` PATCH for every selected note's id in parallel, plus a "Cancel" button. This is the direct answer to Antonio's original ask to select several notes and move them together — desktop-only for the reason given above.

(6) **`notes-board.tsx`** (the full `/notes` page): a 4th "Parked" section joins the existing On-your-screen/Snoozed/Done sections, grouped by the SAME per-person-state logic as the other three. Its Card component gained a `showUnpark` action rendering "Send back to screen." The existing OtherStatus line — who-stands-where on a shared note, via `otherPersonState` — now also reports "parked" instead of falling through to "still open."

(7) **`note-editor.tsx`** (the single-note editor used everywhere a note opens): the existing Done/"Put it back" pair gained a third, mutually-exclusive branch — parked-for-this-person now shows "Send back to screen" instead of Mark-done/Park; neither-Done-nor-Parked now shows BOTH a "Mark done" button and a new, separate "Park" button side by side, mirroring exactly how Done and Snooze already coexist as independent buttons in this same action row (Snooze here is driven by the come-back-date field elsewhere in the editor, not a quick-action button).

(8) **`dashboard-header.tsx`** (the desktop CRM header — a full rewrite of this one file; nothing else in the dashboard touched): the direct answer to Antonio's header screenshot. `GlobalSearch` (its own component code unmodified) is now wrapped so it starts collapsed to a small search icon and only expands to full width on focus — clicking the icon, the existing Cmd+K shortcut, or the existing `focus-global-search` custom event — and collapses back automatically on blur or Escape. **Technical care point:** `GlobalSearch` stays permanently mounted at all times, never conditionally unmounted, specifically so its own always-on Cmd+K listener never stops working while visually collapsed — only its CSS width collapses to zero, which still allows the programmatic `.focus()` both triggers use. `DashboardPushToggle` ("Enable Notifications") and `HelpToggle` ("Help") are now tucked behind a new "..." overflow menu button. **Second technical care point, same shape as the first:** `DashboardPushToggle` has a documented `refreshOnMount` side effect (keeps a browser's push subscription fresh with the server once a day) that must run on every dashboard page load, not just the first time someone happens to open the new menu — so the menu's dropdown panel is ALSO kept permanently mounted, with only its `hidden` class toggled open/closed, preserving the original one-instance-per-page-load contract with zero functional change to that component. The freed header space now also holds the new `ParkedNotesTrigger`, placed next to the existing `StaffAlertsBell` since both are "does something need my attention" indicators and belong together. `CaptureButton` (the recent, unrelated, actively-developed screenshot/share tool) was deliberately left exactly where it was and NOT touched — Antonio's ask was specifically search/help/notifications, no reason to fold in more than what was asked.

(9) **`sidebar.tsx`** (the mobile top bar specifically — a small, additive change; nothing else in this large file touched): a compact `ParkedNotesTrigger` joins the existing compact `StaffAlertsBell`. Mobile's own header was NOT otherwise redesigned — no collapsing search, no overflow menu — since Antonio's header complaint was specifically about the desktop screenshot, and mobile's search icon already opens the same command-palette-style search another way.

(10) **`ui-event-listener.tsx`:** the new `'staff-notes-parked'` react-query key was registered in BOTH of this file's two separate hardcoded key-lists — the realtime cross-tab invalidation map under the existing `'notes'` kind, and the tab-wake-catch-up list. This file's own existing code comment already warns that these two lists are a historical source of stale-feed bugs when a new note-related key is added to only one of them; both were updated together here.

(11) **Tests:** `tests/unit/staff-notes.test.ts` gained a dedicated describe block for `isParkedFor` (true only for the person who parked it, no legacy fallback), `isLiveFor` excluding a parked note, and `isLiveOrRevivedFor` NOT reviving a parked note even after a fresh reply from someone else — the deliberate contrast with how a Done note DOES revive (2026-09-07 entry below) — plus a new `otherPersonState` `'parked'` case folded into the existing describe block. Because `parked_at` was added as a REQUIRED field on the shared `NoteStateRow` type (not optional), every existing test literal constructing a `staff_note_state` row — across BOTH `tests/unit/staff-notes.test.ts` and the entirely separate `tests/unit/staff-alerts.test.ts` — stopped compiling. This was caught only by a full, project-wide TypeScript typecheck, not by linting just the changed files, which would have missed it entirely. **Worth documenting as its own lesson:** this session first ran a narrow, filtered-by-filename typecheck and it caught only 2 of the real total of 29 broken literals — it never even looked at `tests/unit/staff-alerts.test.ts`, which the filter excluded outright — until a second, broader, unfiltered project-wide check surfaced the remaining 27. All 29 were fixed the same way: adding `parked_at: null` (or the real value, for the new parked-specific literals) to each constructed row. The filtered check alone would have shipped a type-broken test file.

**Verification status:** full suite green (10,678 tests), lint clean, `next build` clean, and this time genuinely live-browser-verified on sandbox (private deployment, shared alias untouched) — not just code-reviewed: created real notes, parked one from the floating card and confirmed it vanished from the screen and the header count updated live, unparked it from the header dropdown and watched it reappear floating, confirmed the same note in the Notes page's new Parked section with a working "Send back to screen," opened the full editor and confirmed both the parked-branch ("Send back to screen") and the neither-branch (separate "Mark done" + "Park" buttons) render correctly, selected two of three floating notes in the new bulk mode and parked them together while the third stayed on screen, and confirmed the mobile compact trigger (badge count, sheet listing, working unpark) at a 375px viewport. The one real bug that live testing caught — the dead-click dropdown above — was fixed and the fix redeployed; everything above reflects the redeployed code. All QA test notes deleted afterward; sandbox left clean. **Still open:** production promotion of the `20260908-0000-staff-note-parked.sql` migration (Claude Code cannot run schema DDL against production directly — needs Antonio's own action in the Supabase SQL editor, same as every migration in this file), and the production ship itself, which needs Antonio's separate explicit word, same as every prior entry here.)_
_Prior: 2026-09-07b — Claude (**END-TO-END QA PASS on the whole 2026-09-07 change set — dev job 00b702a1 + 1cab685e, Bug Hunter + live browser verification.** Antonio: "do an EtoE QA with the bug hunter." Found and fixed ONE real blocker, confirmed one accepted minor, and surfaced one genuinely separate pre-existing bug (spun off, not fixed here).
**THE BLOCKER — the header/sidebar clearance fix (below) had no ceiling, only a floor.** `notePosStyle`'s shift is ADDED to every note's position uniformly to clear the header/sidebar on the LOW edge — correct there, but with nothing capping the HIGH edge, dragging a note toward the right or bottom (a completely ordinary "tuck it out of the way" action) could push it fully past the viewport with no visible trace and no way to drag it back. On a 1366px-wide window — one of the most common laptop resolutions — any drag past roughly x=0.84 (well inside the reachable 0–0.92 range `clampFrac` already allowed) made the note fully invisible. Caught by the Bug Hunter reading the actual math, not by any of this session's own live tests, which had only ever pushed a note toward the LOW corner. Fixed by switching `notePosStyle` from a floor-only `max()` to a real `clamp(floor, shiftedValue, ceiling)` on both axes — the ceiling reserves each render site's own real footprint (`COLLAPSED_SIZE_REM`/`EXPANDED_SIZE_REM` in `sticky-notes-layer.tsx`) plus a margin, so a note can never render past the opposite edge either. Verified live, precisely: dragged a real note to the exact scenario the Bug Hunter described (x≈0.92, y≈0.92 on a 1280×720 window) and read its actual rendered position — landed at exactly the calculated ceiling (`left: 1084px` = `100vw − 12rem`, `bottom: 704px`, 16px inside both edges), fully on-screen.
**Accepted, not fixed:** dragging two DIFFERENT notes into the same protected floor/ceiling zone can now land them on top of EACH OTHER instead of on the header/sidebar — strictly better than the original bug (nothing ever blocks navigation chrome again), narrow (only when a second note is already at that exact reserved spot), and fully recoverable (drag either one away). A second Bug Hunter finding — a note archived only via the pre-2026-07-23 legacy shared column can generate a bell alert that the floating card will never honor, since the revive logic deliberately never reads that legacy fallback (same reasoning that protects it from the cross-user resurrection hazard, see the reply-revive entry below) — accepted as the same shape of honest degrade already signed off for `note_update`/snoozed notes; rare and decaying (needs a note older than the 2026-07-23 migration with zero per-person touches since).
**Spun off, NOT fixed in this pass (out of scope — pre-existing, unrelated to any 2026-09-07 change):** found live while testing the ceiling fix — `sticky-notes-layer.tsx`'s `useEffect(() => prunePositions(notes.map(n=>n.id)), [notes])` fires once with an EMPTY list on initial mount (`notes` is `data?.notes ?? []`, and `data` is `undefined` until the first fetch resolves) — `prunePositions([])` deletes every stored position, not just stale ones. Reproduced directly: dragged a note, confirmed the position persisted to storage, did a genuine hard reload, and it came back at its default cascade slot with storage wiped to `{}`. Whether this bites real usage (client-side sidebar navigation keeps the query cache warm; only a genuine hard reload empties it) is unconfirmed — flagged as its own follow-up task, not chased down here.
**Sandbox-testing note, not a code issue:** the shared sandbox alias got overwritten mid-QA by another active session's deploy — twice. Confirmed by the build-stamp banner showing an unexpected commit hash, and resolved by deploying WITHOUT promoting to the shared alias (`vercel deploy` alone, skipping `deploy-sandbox.sh`'s `alias set` step) and reaching that specific deployment directly via a Vercel protection-bypass query param (the bypass secret lives in the Projects API's `protectionBypass` field, not `vercel env ls` — see the reference memory this session pulled it from). Worth remembering for any session QA-ing on this shared project while another session might also be deploying.)_
_Prior: 2026-09-07 — Claude (**A DONE NOTE COMES BACK WHEN SOMEONE REPLIES, and the desktop icons/Alerts button got two visual fixes — dev job 00b702a1 + 1cab685e, 5-reviewer council.** Antonio: he made a note for a client, Luca replied (correctly went red), Luca marked it Done right after, Antonio replied again — Luca got only the generic bell, the sticky note itself never came back. Root cause: marking a note Done drops it from that person's floating feed entirely (`isLiveFor`); the floating card's red state only ever checks notes already IN that feed, so an archived note can never be flagged red no matter what alerts exist for it — two independent gates, only one of which knew about Done.
**First fix attempt was a council BLOCKER, not shipped:** clearing `staff_note_state.archived_at` for reply recipients — this silently erases the person's own Done decision and duplicates a mechanism that already existed (`noteActivityAt`, the "Updated after you marked it done" Notes-tab badge, already covers replies). Switched to the read-side alternative the AI Architect proposed and it independently voided every other reviewer's non-blocking finding too (no write left to race, fail silently, or need error handling for).
**Shipped shape:** new `isLiveOrRevivedFor(note, userId, now)` in `lib/notes/staff-notes.ts` — same as `isLiveFor`, but a Done note also counts as live if the LATEST reply (via `latestReplyOf`, not `noteActivityAt` — deliberately reply-only, preserving the 2026-07-29 decision that an EDIT after Done stays badge-only, confirmed with Antonio to keep that distinction) is newer than this person's own explicit `archived_at`, and isn't their own reply (replying means you already read the thread). Only reads an EXPLICIT per-person `staff_note_state` row, never `isArchivedFor`'s legacy-column fallback — same reasoning as everywhere else in this file: the fallback belongs to nobody in particular, so acting on it here could revive a note for someone who never touched anything. `listActiveNotesForUser` (the floating layer) is the ONLY caller — `listNotesForAccount`/`listNotesForContact`/`filterLiveForUser` keep `isLiveFor` unchanged; nobody asked for a Done note to reappear on those widgets. No DB change, no write path, no migration.
**Two small visual fixes bundled in from the same conversation** (Antonio sent screenshots): (1) the Staff Alerts bell's unread state was a pale `bg-red-50` tint with no animation — Antonio: "it must be solid blinking red, now it's too light." Both renderings (desktop button, mobile badge+icon) now switch to a solid `bg-red-600`/`text-white` with `animate-pulse` (the same Tailwind utility already used for attention states elsewhere in this codebase — sidebar unread badge, AI panel send button — reused rather than inventing new keyframes). (2) The floating desktop note icons were landing on top of BOTH the sticky header and the sidebar — two separate root causes, found in two rounds (Antonio caught the second live, on a real production screenshot, after the first shipped to sandbox alone): the header (`h-14`/56px, `sticky z-30`) only clears the cascade's default vertical start (`y:0.08` in `note-position.ts`) on viewports taller than ~700px; the sidebar (`w-64`/256px) drops to `z-auto` at the exact `lg` breakpoint this layer only ever renders at (it goes `static`/in-flow there, no longer a floating overlay, so it can't rely on stacking order against a `fixed` z-45 note), and the cascade's default horizontal start (`x:0.04`) is a viewport-width FRACTION that all but the widest windows fail to clear. Both fixed the same way, in `notePosStyle()` (`sticky-notes-layer.tsx`): `calc(fraction + max(0px, clearance - cascade-start))` on each axis — shifts the WHOLE grid together rather than flooring one note independently. A per-note floor was tried FIRST for the header fix alone and shipped a real regression to sandbox before being caught: it correctly floored row 0, but compressed the gap to row 1 on any viewport short enough to need it — found by creating real notes and looking, not by reasoning about the CSS, and fixed before it ever reached Antonio. Deliberately NOT touched, on both axes: the stored/dragged fraction itself, so a manually-dragged note still tracks the cursor exactly; only where the cascade's own landing zone renders is affected. `note-position.ts` and its 11 existing tests are untouched.
8 new unit tests in `tests/unit/staff-notes.test.ts` pin `isLiveOrRevivedFor` (revives on a later reply from someone else; does not revive on no reply, an older reply, your own latest reply, while snoozed, or for a legacy pre-migration note with no per-person row). Full suite green (10,572 tests) before shipping; the one unrelated pre-existing local-build gap (`sharp`/`@sparticuz/chromium`/`puppeteer-core` missing from this machine's `node_modules`, present even on the main checkout, unconnected to anything touched here) is a known environment issue, not a regression from this change — Vercel's sandbox build installs cleanly from `package.json` regardless.)_
_Prior: 2026-09-05b — Claude (**The collapsed icon (below) now shows a short text snippet next to the glyph, not just a bare icon** — Antonio: "can the small icon have a short description at the button what it is about?" A hover-only tooltip wasn't enough; the icon widened from a fixed `h-10 w-10` circle to a `h-10` pill (capped `max-w-[180px]`) with the icon plus a truncated slice of the note's own text, CSS-truncated with an ellipsis when it overflows. The tooltip (fuller preview) is unchanged, additive. Verified live, not just by reading the class name: created a long test note and checked the actual computed style (`text-overflow: ellipsis`, `overflow: hidden`) plus that the text genuinely overflows its box (`scrollWidth > clientWidth`), then confirmed it visually in a screenshot — an earlier claim in this same conversation that it was "cut off with an ellipsis" had been asserted from knowing what the CSS class does, not from having actually looked, and was caught and corrected. Re-confirmed drag / click-to-expand / minimize all still work with the new markup.)_
_Prior: 2026-09-05 — Claude (**FLOATING NOTES: collapsed-by-default icons, a draggable note-writing popup, red/yellow unread state, and the actual fix for notes landing on top of each other.** Four related changes in one session, all from Antonio hitting the floating layer's rough edges in real use:

(1) **DesktopNote collapses to a small icon by default** (a `h-10 w-10` circle, was always a fixed `w-60` open card) — Antonio: "reduce it in icon but always visible to open when we need." Click expands in place to the existing card; a new Minimize button (in NoteCardBody's action row, only rendered when a collapse handler is passed, so `MobileSheet`'s reuse of the same body is unaffected) collapses it back. Click-vs-drag reuses the same measured-distance threshold and ref-based click suppression already proven for the draggable FAB buttons (`isDragGesture` from `lib/ui/draggable-fab.ts`) — the exact stale-closure bug the 2026-07-23 entry below warns about (a drag also opening the thing being dragged, because suppression was React state read stale at click time) was checked for and guarded against here too, with a ref.

(2) **NoteEditor's header is now a drag handle**, desktop only (`sm:` and up, gated by a width check so the mobile bottom-sheet layout is untouched) — the create/edit popup was a fixed modal with zero positioning logic, "static" in Antonio's own words. Offsets the panel via a clamped CSS transform (kept reachable, same invariant as every other draggable element here) rather than the fractional position system notes use, since this modal is centered by flexbox, not absolutely positioned.

(3) **Red until read, back to normal once opened.** Reuses the Staff Alerts bell's OWN dismissal tracking (`staff_alert_state`, same `'staff-alerts'` query key as `staff-alerts-bell.tsx`) rather than a second, parallel read-tracker that could disagree with the bell about the same note. A note the viewer authored is never red (`computeNoteAlerts` already never alerts an author on their own note). A fresh reply on an already-read note flags it red again — confirmed wanted, not just a first-share flag. Dismissal fires on the desktop icon's click-to-expand and on opening the mobile sheet (mobile has no separate collapse/expand step — opening the sheet already reveals every note's full text, which IS reading it). No new persisted state, no API/route changes — purely a new consumer of the existing feed and PATCH action.

(4) **THE ACTUAL BUG, found only after (1)-(3) shipped, when Antonio reported notes still landing on top of each other:** `cascadePos` (`lib/notes/note-position.ts`) took a plain array index and derived a slot from it. The active feed sorts newest-first, so a brand-new note is always index 0 — and a note ALREADY on screen keeps whatever position it got at ITS OWN first mount (a `useState` initializer only reads its argument once, ever); it does not re-run just because a fresh computation produced a different value for it. So every never-moved note computed the IDENTICAL index-0 slot the instant it appeared — not approximately overlapping, the same pixel. This is almost certainly the real cause of Antonio's ORIGINAL complaint too (large cards stacked exactly on top of each other, before any sizing change) — shrinking the card to an icon in (1) only changed how the same underlying bug looked, it did not fix it. Two things had to change together: `cascadePos` now takes the list of already-occupied slots and walks past any it collides with (`SLOT_EPSILON = 0.01`), AND slot assignment for the whole note list now happens in ONE pass, in the parent (`StickyNotesInner`'s `notePositions`), remembered in a `useRef` map for the life of the page — so a note's slot, once decided, is never silently recomputed and then ignored by an already-mounted card. A first attempt fixed only the `cascadePos` half (a plain `useMemo` recomputing fresh on every note-list change) and looked correct in code review; it still failed live — an already-open note kept its stale position while a freshly-created one collided with it — caught only by creating several real notes in the sandbox and reading their actual on-screen coordinates, not by reasoning about the diff. 11 new unit tests in `tests/unit/note-position.test.ts` (this file had none before) pin the occupied-slot-skipping behaviour so this can't silently regress.

Sandbox-verified repeatedly against real created notes, not just unit tests: icon size, click-to-expand/collapse, drag-and-persist, the editor's own drag, the red-to-normal transition with a genuine second-author test note and a genuine reply, and — for (4) — creating 3-4 notes in one sitting and confirming distinct, non-colliding on-screen positions both from an empty note list and with pre-existing notes already active. All test notes deleted after each check.)_
_Prior: 2026-09-04e — Claude (**Sticky notes can now carry ONE attachment (image or document) — author-only, the SAME rule that already governs editing a note's own text, no new permission concept.** Built for the Capture/Share feature (screenshot -> mark it up -> send it to a sticky note as one of its destinations; full design in the new `docs/systems/captures.md`). Four nullable columns added to `staff_notes` (`attachment_url`, `attachment_name`, `attachment_mime_type`, `attachment_size_bytes`, migration `20260904-1620-staff-notes-attachment.sql`) — ONE attachment per note, not a gallery, matching Antonio's explicit scope ("just the attachment"). New PATCH action `attach_capture` (`app/api/crm/staff-notes/route.ts`) is gated by the existing `mayEditBody` predicate (author-only) — attaching a picture to a note follows the exact same rule as editing its text, not a separate concept to maintain. Also added `listMyNotesForUser` (`lib/notes/staff-notes.ts`) — a plain "every note I personally authored, newest first" query feeding the new destination picker's own note list; this is NARROWER than and separate from the visibility predicate above (`isNoteVisibleTo`) — it does not change or relax who can see a note, it only lists the ones the CURRENT caller could attach a picture to (their own).)_
_Prior: 2026-09-04d — Claude (**"OPEN IN PLACE" — clicking a Staff Alert no longer navigates away from whatever CRM page you were on.** Antonio: "can another page in the crm be opened if I click on a message or a notification? because if I'm working on something and i receive a notification and I want to read it, if i click on it I will leave the page where I'm working on." Confirmed real and current before building anything: even the just-shipped notes bell (2026-09-04b/c below) always did `router.push('/notes?note=...')`.
New `lib/notes/open-note.ts` mirrors this subsystem's own "Discuss this note" mechanism (`lib/team/open-team-chat.ts`) exactly — same cancelable-`CustomEvent` pattern, `requestOpenNote({noteId})` returns true only when `sticky-notes-layer.tsx`'s new listener called `preventDefault()`. `staff-alerts-bell.tsx`'s `openAlert` now tries this FIRST, falling back to `router.push(alert.url)` only when it returns false. Same fix applied on the Team Chat side of this same round — see `docs/systems/team-workspace.md` 2026-09-04 (the bell's Open button now tries `requestOpenTeamChat` first for a chat alert, identical pattern).
**SYNCHRONOUS ONLY, by design — the navigation fallback for a rare case is an HONEST DEGRADE, not a bug:** `preventDefault()` only affects `dispatchEvent`'s return value if called before it returns, so the listener can only claim a note already sitting in the floating layer's OWN loaded feed (`scope=active` — unsnoozed, not archived-for-me). A note behind a `note_update` alert CAN be snoozed or archived-for-me (that alert kind is not gated on either — see the 2026-09-04c entry below) and so be absent from the active feed; for that narrow case the click still navigates to `/notes?note=<id>` (the full page, which shows every note) rather than doing nothing. Considered and rejected: an async fetch-then-open fallback — `preventDefault` can't fire late enough to stop a navigation that's already happened by the time an async fetch resolves, so it would just pop the editor a second time ON TOP of the navigation instead of replacing it.
No API/DB change. No dedicated test for `requestOpenNote` itself — it has zero conditional logic beyond the `typeof document` SSR guard, and its template (`open-team-chat.ts`) has shipped test-free since 2026-07-23 for the same reason; the LISTENER side (the actual behavior) is verified by browser QA, matching how this file and `floating-chat.tsx` have always covered this class of wiring.)_
_Prior: 2026-09-04c — Claude (**STAFF ALERTS SHIPPED SILENTLY BROKEN, then a real logic gap on top — both found by Antonio in real use within minutes, both fixed.** Two distinct incidents from the SAME ship:
(1) **The migration for `staff_alert_state` was applied to sandbox only and never promoted to production** — the surgical-ship process (fresh branch off main, cherry-pick, test, merge) shipped the APPLICATION CODE, but the separate, required "Antonio promotes the DDL" step was simply skipped. Every call to the new endpoint 500'd against a table that didn't exist; the UI's `useQuery` swallowed the failure and rendered the ordinary empty "All caught up" state — so the outage was invisible until Antonio manually tested it ("I sent a note to Luca and he didn't receive anything"). Worse: `execute_sql`'s own documented promotion pattern (`reason: "migration:<filename>"`) now hard-refuses ALL schema DDL regardless of the reason string — confirmed live, this path is dead. Antonio ran the `CREATE TABLE` himself directly in the Supabase SQL editor. **Lesson: after any surgical ship that includes a new migration, promoting that migration to production is a SEPARATE, MANDATORY step — verify the table actually exists in production (not just that the deploy succeeded) before considering the feature shipped.**
(2) **A real design gap in `computeNoteAlerts`, independent of (1):** the `note_update` kind originally required `updated_at > created_at` to fire — true for an edit or a re-share of an EXISTING note, but FALSE for a brand-new note shared with someone at creation (the single most common way a note reaches a second person, and literally the first thing Antonio tried). That note never got a `note_update` alert until someone dismissed-then-later-edited it. Fixed: the check is now purely dismissal-timestamp-based (fire unless already dismissed as of the note's current `updated_at`) — `updated_at` vs `created_at` is now used ONLY to word the title ("shared a note" vs "updated a note"), never as a gate. Caught the QA gap too: this session's own earlier "simulate all scenarios" pass never actually tested a freshly-created shared note with no subsequent edit — every test scenario bumped `updated_at` first. `tests/unit/staff-alerts.test.ts` now has a case pinning this exact bug.)_
_Prior: 2026-09-04b — Claude (**STAFF ALERTS — a persistent, dismissible notification list, sourced from this subsystem's own data. dev job b1fb9705, 6-reviewer council on the plan + a second adversarial pass on the shipped code.** Antonio: "I want a notification in the CRM from the sticky note... always visible until i dismiss it" — the existing OS push (`replyNotifyTargets`/`editNotifyTargets`, unchanged) is ephemeral and the sonner-toast layer (`realtime-notifications.tsx`) never covered notes at all. New bell next to "Enable Notifications" (desktop header + mobile top bar) opens a list that stays until acted on.
**DELIBERATELY a read-side computation, not a write-path table** — the first-drafted design (write a row at every push call site) was replaced after two council reviewers independently found it would drift (more push call sites than counted, some shared with Portal Chats) — `computeNoteAlerts` (`lib/notes/staff-alerts.ts`) derives two alert kinds fresh, on every request, from data this subsystem already owns: `note_reply` (one per `staff_note_replies` row from someone else, suppressed while snoozed — mirrors `replyNotifyTargets` exactly) and `note_update` (a note shared with you or edited since you last dismissed it — timestamp-compared against `staff_notes.updated_at`, so a fresh change after a dismiss reappears; mirrors `editNotifyTargets`, including its snooze-blind spot — carried over on purpose, not invented, so the list can never disagree with what already pushed). A note's own author never gets alerted by it (only the AUTHOR can edit/share, so a self-triggered `note_update` would be about their own action) but DOES see replies to their own note — the one bug this session's own tests initially got backwards.
**Only new persisted state:** `staff_alert_state` (migration `20260904-1400-staff-alert-state.sql`) — one row per (person, thing) recording "dismissed, as of when," nothing else. Same lockdown as `staff_notes`/`staff_note_state`: RLS enabled, NO policy. `note_id` and `reply_id` are both `ON DELETE CASCADE` — hard-deleting a note (already a real, author-only action on this subsystem) cleans up its alert-state rows automatically, live-verified. Two shapes share the table via a nullable `reply_id` + two partial unique indexes (`user_id,reply_id` where set / `user_id,note_id` where null) — dismissing is UPDATE-then-INSERT-if-0-rows in the API route, never a native upsert (a partial index's WHERE clause has no supabase-js upsert equivalent).
**Reuses this subsystem's realtime bus for free:** `ui_events` kind `notes` already fires on every note mutation (reply/edit/share/create/delete) with NO payload (the existing, load-bearing privacy rule — see below); the panel's query key just rides the same signal (`ui-event-listener.tsx`), no new emit call anywhere.
**The panel's Reply and Mark-done buttons call this subsystem's OWN existing `PATCH /api/crm/staff-notes` actions (`reply` / `archive`) verbatim** — no parallel mutation path, so a reply from the panel is provably identical to one typed in the full editor (live-verified against the database, not just read from code).
**A real bug an adversarial pass on the SHIPPED code found, that the plan-level review could not have:** closing the panel (backdrop tap, the mobile X, re-tapping the bell) unmounted every row unconditionally, silently destroying an in-progress reply — contradicting this exact file's own established pattern (`sendReplyIfDirty` in the full editor, "the reply draft is guarded like the body," 2026-07-29c entry below). Fixed: every close path now confirms first when a reply is being typed, matching that precedent instead of exempting the new surface from it.
**Key files added:** `lib/notes/staff-alerts.ts` (the pure computation, 13 tests), `app/api/crm/staff-alerts/route.ts` (GET the feed / PATCH to dismiss — staff-only, reuses `NOTE_COLUMNS`/`visibleToOrClause` from `lib/notes/staff-notes.ts` unchanged), `components/dashboard/staff-alerts-bell.tsx` (bell + badge; desktop dropdown, mobile bottom sheet matching this subsystem's own `sticky-notes-layer.tsx` mobile pattern).
**Deliberately NOT built yet, sequenced separately:** Team Workspace chat notifications (that code's own attachment path had 4 fix passes in the 4 days immediately before this council review — a live reason to wait for it to settle, not a stale scar) and a "Claude drafts a reply for you" hand-off into the existing AI agent (needs its own adversarial pass on the AI-send guardrails before building, per Antonio's explicit sequencing).)_
_Prior: 2026-09-02 — Claude (**LINKIFY EVERYWHERE — the two dead-click bugs the previous entry's "card previews stay plain" and "accepted asymmetry" left behind, both hit by Antonio in real use** (dev job acb315af, bug-hunter + system-counselor investigation, then fixed). Symptom: a chat-message deep link pasted into a note's body did nothing when clicked — "opens just the note" instead of the chat. Two independent causes, both closed:
(1) Every COMPACT preview — the floating card (desktop + mobile sheet), the Notes-list card, and the calendar (month-grid mini list + day-detail `NoteMini`) — rendered `note.body` as plain text inside an element whose own onClick always opened the full editor; a pasted URL there was inert, indistinguishable from clicking anywhere else in the text. `LinkifiedText` (moved out of the editor into its own shared `components/dashboard/note-linkified-text.tsx` so all five surfaces import the same one, not five copies) now wraps the body text in each of them too. The anchor's own `stopPropagation()` is what makes a click ON the link navigate instead of bubbling up to the card's "open the note" handler — no new logic needed there. The calendar's month-grid per-note row was a `<button>`; a real `<a>` can't legally nest inside one (same reason the day CELL itself stopped being a `<button>` — see the 2026-07-21 entry below), so it's now a keyboard-accessible `role="button"` div, same pattern.
(2) The editor's OWN "accepted asymmetry" (previous entry) turned out to bite the person most likely to test their own link: the AUTHOR reopening their own saved note still gets the plain editable `<textarea>` (`canEditBody = isCreate || isAuthor`), which can't render an anchor. Rather than take away the author's ability to edit, a `NoteLinks` component under the textarea extracts every URL from the live (unsaved-included) body text via the same `splitLinkSegments` and renders each as its own small clickable pill — recomputed on every keystroke, so a just-pasted link is clickable immediately, not only after Save.
Non-authors and replies are unchanged (they already routed through `LinkifiedText`). No API/DB change — purely how five client components render text that was already being stored.)_
_Prior: 2026-07-29d — Claude (**COPY-LINK + LINKIFIED NOTE TEXT** (dev job 2acbfd8d, light council). Portal-chats messages/threads and Inbox gmail threads now have Copy-link controls producing absolute URLs of the EXISTING deep-link consumers (`?message=` scroll+highlight; `/inbox?thread=&mailbox=`). Notes side of it: (1) `NoteComposeDialog`/`NoteQuickCreate` accept an explicit `originUrl` — RELATIVE pathname+search ONLY (`safeOriginPath` silently nulls absolute strings; the absolute form is clipboard-only); the fallback capture now STRIPS an inherited `?message=` param so a note made on a deep-linked page doesn't point at someone else's message. Portal-chats "Make a note" passes the exact-message path; Inbox notes pass `/inbox?thread=…&mailbox=…` (previously degraded to bare `/inbox` — thread selection is React state, not URL). (2) **Linkify, notes ONLY**: `splitLinkSegments` in pure `lib/notes/note-origin.ts` (strict `https?://`, trailing sentence punctuation peeled) + `LinkifiedText` in the editor — applied to the read-only body block and reply bodies; card previews stay plain; anchors carry `data-no-drag` + stopPropagation + `noopener noreferrer`; NO innerHTML anywhere. The author's own textarea can't linkify (it's an input) — accepted asymmetry. Copied links use the internal CRM domain by design (R005: staff-only; a client clicking one hits the login wall — do NOT route via APP_BASE_URL).)_
_Prior: 2026-07-29c — Claude (**REPLIES — per-person coloured answers inside a note + reply pushes** (Antonio: "the reply [must] be a different color... and a notification for the reply to the person who is sent to"; dev job 91120722, 3-reviewer council).
**Model:** a reply is its own row in NEW table `staff_note_replies` (migration `20260729-1800-staff-note-replies.sql`; FK → note ON DELETE CASCADE, author FK SET NULL, body CHECKs mirror the parent, RLS deny-all, NOT in generated types → `repliesTable()` any-cast). Replies INHERIT the parent note's visibility — re-sharing/teaming a note exposes its old replies to the new viewer, privatising hides others' replies from their own authors; accepted at 2 staff and stated here deliberately. **⛔ DEPLOY ORDER LOAD-BEARING (same as staff_note_state):** feeds embed the relation in `NOTE_COLUMNS`, so the table must exist BEFORE this code deploys or every note screen 500s.
**THE RULE THAT CHANGED: the note's TEXT is the AUTHOR's alone** — new predicate `mayEditBody` (author-only, ALL visibilities incl. team), server-enforced on the `edit` action; everyone else answers via the `reply` PATCH action. `mayTouchNote` is UNTOUCHED (done/snooze/reply stay open to everyone the note reaches) — do not collapse the two predicates; their separation is the fix. Both recipient-guidance strings (the visibility-403 and the editor hint) now point at the Reply box.
**⛔ REPLIES DELIBERATELY DO NOT BUMP THE NOTE'S `updated_at`** (council blockers): a bump gave the author a false stale-edit 409 on their own next Save and silenced `shouldSendEditPush`. Activity is derived READ-SIDE: `noteActivityAt` (max of row `updated_at` and reply `created_at`s) feeds the "Updated after you marked it done" badge; `sortReplies` orders client-side (PostgREST embeds are UNORDERED — never trust embed order); `latestReplyOf` powers the card's one-line preview.
**Reply flow:** PATCH `reply` is its OWN branch (no note-row patch): `validateReplyBody` (reply-worded errors, trim-first) → INSERT with the error CHECKED (FK 23503 = note was hard-deleted mid-reply → friendly 404, never a phantom success) → push AFTER the checked insert to `replyNotifyTargets` = everyone reachable minus the replier **minus anyone whose per-person snooze is still in the future** (a come-back date shields its owner from reply buzzes; they catch up via the badge) → returns the fresh embedded row so the editor shows the thread immediately.
**Editor:** non-authors get a read-only body block ("Note — by <author>") + tinted chronological replies (author = amber, others = sky; tint keys on `author_user_id === note.author_user_id`, null-safe → other-tint) + a Reply composer (hidden in create mode — no note id yet). **The reply draft is guarded like the body:** `sendReplyIfDirty` fires fail-closed BEFORE any quick action and before Save, and a backdrop tap does NOT close the editor while body or reply text is unsaved (the 2026-07-28 lost-reply incident, blocked in its new shape). Delete confirm copy says replies are destroyed too (cascade). Cards (floating + Notes tab) show the latest reply as a clamped tinted one-liner + count.
**Accepted bound:** every feed embeds ALL replies of every visible note (≤500 notes × ≤4000-char replies) — fine at 2 staff; cap to latest-N + count if volume ever grows.)_
_Prior: 2026-07-29b — Claude (**DELETE ON THE CARD** — Luca's 2026-07-22 ask, second half: the card (desktop + mobile sheet, shared `NoteCardBody`) now carries a trash icon, AUTHOR-ONLY like every visibility control, hitting the same `DELETE /api/crm/staff-notes?id=` the editor uses. Two-step on purpose: the icon tap only reveals a full-width red "Delete forever? / Keep" bar — the destructive tap is never on a 14px icon. His first half (Done on the card) already existed.)_
_Prior: 2026-07-29 — Claude (**THE SHARE-BACK INCIDENT FIX + FULL-EDITOR CREATION + ORIGIN LINKS.** Root cause chain of 2026-07-28, prod-confirmed: Antonio shared a note with Luca; Luca typed a reply then clicked the share chip targeting Antonio; the share action overwrote the ONE `shared_with_user_id` slot Luca→Antonio, so Luca (neither author nor recipient) lost the note from every screen, and his typed reply — persisted only on Save, never by a chip click — died in his browser. Edits notified nobody. Fixes, all in this change:
(1) **WHO SEES a note is the AUTHOR's call alone.** `share`/`team`/`private` PATCH actions 403 for everyone else, and both UIs HIDE the share/visibility controls for non-authors (`meId` now flows to the card + editor) — server rule + no dead clicks. A recipient answers by typing + Save; the author is pushed.
(2) **Touch guard widened to the visibility rule** via `mayTouchNote` (author OR team OR shared_with) — fixing a second live bug: a TEAM note has NO shared_with (coherence CHECK), so the old inline `author || shared_with` guard 403'd every non-author's Done/snooze/edit on team notes.
(3) **The editor saves a dirty body BEFORE any one-click action** (fail-closed: save fails → action aborted, text kept) and refreshes its working baseline (`body` + `updated_at`) from the PATCH response, or the very next Save would trip the stale-edit guard on OUR OWN write. The auto-save preceding a visibility change sends `suppress_notify` so new text is never pushed to someone about to lose access.
(4) **Edit pushes**: editing a shared/team note pushes everyone it reaches except the editor (`editNotifyTargets`), deep-linked to `/notes?note=<id>`; ALL note pushes now use that deep link (was `/`). Burst-guarded by `shouldSendEditPush`: quiet window 2 min, but the FIRST change after creation always pushes (that is the Luca-reply case). The Notes page consumes `?note=` (Suspense-wrapped), opens the exact note from any state group, shows "That note is gone" when it's deleted/re-privatised (feed already visibility-filters, so no leak), and Done cards carry an "Updated after you marked it done" badge (read-side compare of `updated_at` vs my state row — nobody's state is written).
(5) **Creation = the FULL editor everywhere** (floating +, Notes page New-note, portal-chats Make-a-note, Inbox Note button — the old mini composers are deleted; `note-quick-create.tsx` now wraps `NoteEditor` in create mode and fetches the team list via new lightweight `GET ?scope=members`). Create mode BUFFERS everything locally and fires ONE POST on Save — there is no note id yet, so immediate actions are hidden. POST now accepts `come_back` (validated future instant) + `come_back_display` (creator's local wording, used only in push text): the date writes per-person snooze rows for ALL initial viewers BEFORE the realtime emit (a "for Luca, back Monday" note must not float for Luca today); a failed state write returns the note WITH a `warning` the editor surfaces — never silent partial success.
(6) **Origin links rendered at last**: `origin_url` was stored on every note but shown nowhere. `safeOriginPath` + new `describeOrigin` moved to PURE `lib/notes/note-origin.ts` (client-safe; `staff-notes.ts` re-exports so server/test imports are unchanged) and the card + editor render a "From: Inbox / Portal chats / Client page…" button (`data-no-drag`, render-time re-guard).
NOTE the test-file trap found while verifying: `staff-notes.test.ts`'s safeOriginPath case `"/x y"` contains an invisible CONTROL character, not a space — the suite is honest; spaces in paths are allowed. Editor's Done/Put-back toggle now reads PER-PERSON state (`isArchivedFor`), not the frozen legacy column. Luca's lost reply was unrecoverable — no history table; the text never reached the server.)_
_Prior: 2026-07-27 — Claude (**`visibility` is now registered with the code↔database contract gate.** The column has carried a CHECK in production (`private` / `shared` / `team`) that NOTHING in the code was verified against — it surfaced the moment the committed prod constraint snapshot was refreshed, having been invisible while that file sat stale. `NoteVisibility` is now derived from an exported `STAFF_NOTE_VISIBILITIES` tuple in `lib/notes/staff-notes.ts`, which the gate compares against production's CHECK on every push. Behaviour is UNCHANGED — the three values are exactly what the code already used; what changed is that a future fourth value can no longer be written silently and rejected (supabase-js returns errors rather than throwing). ⚠️ Adding a visibility value now requires a migration to the CHECK BEFORE the code writes it. See **hooks-guardrails.md**.)_
_Last verified against code: 2026-07-23d — Claude (**DISCUSS + DELETE in the note editor.** The editor (`components/dashboard/note-editor.tsx`) now carries the same "Discuss" as the card, and a real **Delete** — a hard delete via new `DELETE /api/crm/staff-notes?id=`, distinct from "Mark done" (per-person soft archive). Delete removes the note for EVERYONE and is **AUTHOR-ONLY**, enforced server-side (a recipient gets a friendly 403 → use Done); two-tap confirm. Hard delete is correct and does NOT breach R100 (that governs CLIENT-visible content; staff_notes is internal-only, RLS deny-all, no FK chain to client-visible state — R100 permits hard delete there); `staff_note_state` rows cascade. A chat opened from the note is NOT linked to it and is left untouched (Antonio: "just delete the note; leave the chat alone"). Sandbox-verified: author deletes → gone; a foreign-authored note is refused and survives.)_
_Prior: 2026-07-23c — Claude (**"DISCUSS THIS NOTE" — a chat icon on every note opens the EXISTING floating chat on the right conversation (NOT a comment box inside the note).** Antonio's model, after a 4-core council. Note about a CLIENT → that client's conversation (PER-CLIENT: every note about the same client opens the one place their talk lives; reuses `findOrCreateConversation`, which seeds + notifies the other staff). Note with NO client → the direct chat with the teammate (the note's shared-with person, else the configured support person, else the one other staff member; reuses `findOrCreateDm`). Either way the composer is PRE-FILLED with `About this note: "<excerpt>"` — nothing is auto-sent, so no double-post / no duplicated push. Endpoint: `app/api/crm/staff-notes/discuss`. **DELIBERATELY NOT a per-note thread under Luca** — the council proved no existing primitive gives a PRIVATE per-note thread (a DM dedupes to one per pair; a discussion is team-wide), so that needs a new visibility concept; Antonio chose to live with this shape first. **Council blockers handled:** the button is never a DEAD CLICK — a cancelable event (`lib/team/open-team-chat.ts`, one place for the name) lets the floating chat `preventDefault` only when it can show the thread; `requestOpenTeamChat` returns "handled", and when nobody handles it (chat kill-switched off, or already on `/team-chat` where the window renders null) the button navigates to `/team-chat?thread=<id>`. Mobile opens the SHEET, desktop the WINDOW (separate state). A deliberate Discuss click IS engagement so marking-read-on-open is correct. The button carries `data-no-drag`. Keyed on the CLIENT, never the mutable note text. **Pre-existing gap, flagged not fixed:** `findOrCreateConversation` is select-then-insert with no unique index, so two near-simultaneous Discuss clicks on one client note could briefly fork — same gap the New-conversation dialog already has; a small separate DDL if it bites. Sandbox-verified end to end.)_
_Prior: 2026-07-23b — Claude (**"WHO'S IT FOR?" AT CREATION.** The New note dialog now sets the recipient up-front — Just me (default) / each teammate by name / Whole team — instead of the old create-private-then-share-from-the-card flow. The create endpoint (`app/api/crm/staff-notes` POST) gained a `recipient` field, validated the SAME way the `share` PATCH action is (real staff only, never a partner/client, never yourself) and honouring the DB coherence CHECK (shared MUST name a person; team/private MUST NOT). A note made for a teammate fires the same push the share action sends. **DEFERRED to council, NOT built:** the "Discuss this note" button — it needs an external "open this thread" entry point into the always-on floating chat, which does not exist today, so both its behaviours (open the client conversation; the note-anchored Luca thread Antonio wants) go to review together rather than half-shipped.)_
_Prior: 2026-07-23 — Claude (**DONE AND SNOOZE ARE NOW PER PERSON, and both buttons are DRAGGABLE — LIVE on production 2026-07-23.** Migration `20260723-1000-per-person-note-state-and-thread-later.sql` was run by Antonio in the Supabase SQL editor BEFORE the code shipped (execute_sql refuses production DDL by design, so this is always his step). **DEPLOY ORDER IS LOAD-BEARING: migration first, code second — the feeds embed `staff_note_state`, so code without the table 500s every note screen.** Production backfill verified after the fact: 12 state rows from 9 notes = 6 private (author only) + 3 shared (author + recipient).

**⛔ THE RULE THIS SUBSYSTEM NOW TURNS ON: a note is ONE thing, but "I have dealt with it" is PER PERSON.** Antonio, 2026-07-23: *"when I create a note for Luca, I have to click Done to make it disappear. When I click Done, it disappears also for Luca, so Luca and I are two different things."* He was right, and it was worse than reported — `archived_at` AND `snoozed_until` were both single columns on `staff_notes`, so snoozing a shared note also pulled it off the other person's screen and put it back at a time they never chose. He only hit the Done half because none of his shared notes had been snoozed.

**Where state lives now:** `staff_note_state (note_id, user_id, archived_at, snoozed_until)` — sparse, NO ROW = live for that person (migration `20260723-1000-per-person-note-state-and-thread-later.sql`; RLS enabled with no policy, same posture as `staff_notes`). The pure helpers are `isArchivedFor` / `isSnoozedFor` / `isLiveFor` / `otherPersonState` / `otherViewersOf` in `lib/notes/staff-notes.ts`; the API writes via `setMyNoteState`.

**⚠️ THE LEGACY-FALLBACK TRAP, do not simplify it away.** `staff_notes.archived_at` / `.snoozed_until` are FROZEN: still read as a fallback for notes written before the change, NEVER written again. The fallback stops applying the moment ANYONE has a per-person row — otherwise one person clearing a note would re-clear it for everyone through the back door, i.e. the original bug resurrected. `tests/unit/staff-notes.test.ts` has the case that catches exactly this, and it is the one that fails if the fallback is widened. Dropping the two columns is a deliberate follow-up, not a tidy-up.

**Because Done is now private, the Notes tab shows WHERE THE OTHER PERSON STANDS** (Antonio's ask: *"I will check in notes in the tab for the notes status"*) — a shared note vanishing no longer tells you Luca dealt with it, so the card says "Luca: done / snoozed / still open". Team notes are summarised as a count (a name list is noise and scales badly); private notes render nothing. The three sections group by YOUR state and the Snoozed section shows YOUR wake time.

**Feeds filter in CODE, not in SQL.** Done/snooze depend on a row in a sibling table that may not exist, so `listActiveNotesForUser` / the per-record widgets fetch what you can SEE and apply `isLiveFor`. KNOWN BOUND, stated not hidden: the database limit applies BEFORE the per-person filter, so at hundreds of notes some could be trimmed — at single digits and two staff it cannot bite; if volume grows this wants a database function that filters and limits in one pass.

**BACKFILL — the part that had to be proven, not assumed.** Sandbox had ZERO done/snoozed notes so the backfill was a no-op there, while production had six. A matching case (shared + already Done) was seeded in sandbox, the real backfill run against it, and BOTH the author and the recipient confirmed to get a row — so nothing resurfaces on either screen at deploy. Probe deleted, cascade verified, zero orphans.

**THE BUTTONS ARE DRAGGABLE — and "Mobile has no dragging" below is REVERSED (Antonio, 2026-07-23).** Both the notes pill/+ and the green chat launcher move anywhere, desktop and phone, remembered per button per device, double-click/tap to reset. Shared hook `components/ui/use-draggable-fab.tsx` + pure `lib/ui/draggable-fab.ts`. The two hazards the old decision avoided are handled explicitly: an 8px tap-vs-drag threshold by real distance (a wobble while tapping must not move the button) and pointer capture + `touch-none` (or the browser hands the drag to the page scroller). **Defaults also moved off the composer band** (`bottom-24` on mobile): the notes pill had been sitting on the Attach button of every chat composer since it shipped, and the chat launcher on Send — on Portal Chats that Send is how a client gets answered, so the phone could not do its job.
**Bug worth remembering:** the first cut let a drag also OPEN the thing being dragged. Click suppression was React STATE, read inside the consumer's `onClick` — state is captured per render, so the click after a drag read a stale `false`. It must be a REF (exposed via a getter), read at call time.)_
_Prior: 2026-07-21c — Claude (**MAKE A NOTE FROM PORTAL CHATS + INBOX; calendar-day notes now open.** (a) Portal Chats: a "Make a note" item in the per-MESSAGE dropdown (beside "To Do"), pre-filled with the message text and tied to that client. (b) Inbox: a "Note" button next to the email subject, pre-filled with the subject and tied to the thread's linked client. Both capture `origin_url` (pathname+search) so the note links back to the exact place. Shared `components/dashboard/note-quick-create.tsx` exports BOTH `NoteComposeDialog` (controlled — raised by the portal-chats menu item, which has no button of its own) and `NoteQuickCreate` (button+dialog — the Inbox header). Deliberately NOT a portal-chats catalog quick-action: the Inbox cannot use that mechanism at all, so one shared component gives both surfaces identical behaviour instead of two mechanisms to maintain. (c) BUGFIX: clicking a note inside a calendar DAY CELL did nothing — the cell was a single `<button>`, so the note previews inside it were inert text. The cell is now a keyboard-accessible `div` and each note preview is its own button that `stopPropagation()`s and opens the editor; clicking the empty part of a day still selects it. Nested buttons are invalid HTML, which is why the cell had to stop being one.)_
_Prior: 2026-07-21b — Claude (**NOTE EDITOR + custom-date SAVE BUTTON.** Three things Antonio hit in real use: (1) the custom date/time picker saved on CHANGE, so it fired the moment the DATE was picked — before a time could be set — and the note snoozed away mid-edit. It now HOLDS the value and only writes when **Save** is pressed. **Never save a `datetime-local` on change.** (2) Notes could be created and acted on but never re-read or re-worded — "Now I can't do anything". (3) Calendar days showed their notes but nothing was openable. FIX: one shared `components/dashboard/note-editor.tsx` opened by tapping a note's TEXT on EVERY surface — floating card, mobile sheet, Notes list, calendar day. It edits body + client + come-back date behind a single Save (body carries `expectedUpdatedAt` for the stale-edit guard), and offers visibility + done/put-back as immediate actions. On the floating card only the BODY opens it, never the whole card — the card is the drag handle on desktop, so the body carries `data-no-drag`. No new API: it composes the existing edit / set_client / snooze / unsnooze / share / team / private / archive / unarchive actions. The Notes tab feed already returns `members`, so the share buttons work there too.)_
_Prior: 2026-07-21 — Claude (CALENDAR VIEW added to the Notes tab: month grid Mon-first with month/year arrows + Today + today highlighted, click a day for its notes; mobile falls back to an upcoming LIST because a 7-column grid at ~380px gives unreadable ~50px cells. Calendar maths is pure + unit-tested in `lib/notes/note-calendar.ts` (21 tests) — the bug class it guards is UTC-vs-local day bucketing: a note set for 09:00 local must land on the LOCAL day. Antonio's settled model, in his words: the floating layer shows "what we want to have and what is aspiring today", the Notes tab holds "the summary of all notes" — so a dated note DISAPPEARS from the screen until its time and stays visible in the tab. That is why there is NO separate due-date column: the come-back instant IS the calendar date.)_
_Prior: 2026-07-21 — Claude (round 2: custom snooze date/time picker; `/notes` page + sidebar entry placed 2nd under Home; attach-a-client via the shared `AccountCombobox` + a `set_client` action that verifies the id exists; client NAME resolved at READ time via the FK nested select so a renamed company can't leave a stale label.)_
_Prior: 2026-07-21 — Claude (pass 1 shipped: `staff_notes` table, floating layer, API, three-level visibility, snooze, share-to-person with phone push, archive.)_

## What it is
Private-by-default post-its for STAFF (Antonio + Luca). They float on the CRM dashboard, follow
you across pages, and can be handed to a teammate. Internal only — a client must never see one.

This is the CRM dashboard, not the client portal.

## Business rules
- **Three visibility levels, switchable with a click:**
  - `private` — only the author (the DEFAULT for every new note)
  - `shared` — the author + exactly ONE named teammate (`shared_with_user_id`); this is
    "hand it to Luca" and it pushes to that person's phone
  - `team` — every staff member
- **THE ONE visibility rule** (`isNoteVisibleTo` / `visibleToOrClause` in `lib/notes/staff-notes.ts`):
  visible to U iff `author_user_id = U` OR `visibility='team'` OR
  (`visibility='shared'` AND `shared_with_user_id = U`). Every reader imports it — never hand-roll.
  **Antonio explicitly declined an owner-override: he does NOT see Luca's private notes.**
- **Snooze = scheduling, not just hiding.** Presets are 10 min / 1 hour / tomorrow 9am / pick a
  date & time. A snoozed note leaves the floating layer and reappears at its time. Antonio's
  model: screen = now, Notes tab = everything.
- **Done** is `archived_at` (soft) — recoverable from the Notes tab's Done section. **A fresh
  REPLY (not an edit) after Done brings the note back onto the floating screen automatically**
  (2026-09-07) — nothing is overwritten; the person's own Done timestamp is untouched, the note
  just becomes eligible for the floating feed again because there's newer reply activity than
  their archive time. An edit after Done still only badges the Notes-tab card, unchanged.
- **Parked** (`parked_at`, 2026-09-08) is a FOURTH per-person state, distinct from Done and
  Snooze — moved to the "Parked" header control (desktop + mobile) instead of floating on
  screen. Mutually exclusive with Done/Snooze at both the write site and a DB check constraint —
  a note is at most one of Done/Snoozed/Parked for any one person at a time. Unlike Done, a
  fresh reply on a Parked note does NOT bring it back onto the floating screen — it stays parked
  and turns red there instead (via the same alerts feed everything else uses). Select several
  floating notes at once (desktop only) to park them together.
- A note optionally carries the client it's about (`account_id` / `contact_id`), auto-captured
  from the page you were on and changeable via the client picker.

## How it's built
- **Table `staff_notes`** (migration `20260721-1400-staff-notes.sql`): `body`, `color`,
  `author_user_id`/`author_name`, `visibility`, `shared_with_user_id`/`shared_with_name`,
  `account_id`, `contact_id`, `origin_url`, `snoozed_until`, `archived_at`, timestamps.
  CHECKs: non-empty body, body ≤ 4000, visibility in the three values, and a **coherence check**
  — `shared` MUST name a person and non-`shared` must NOT, so un-sharing can never leave a stale
  recipient that keeps the note visible.
- **RLS is ENABLED with NO policy** → a direct anon/authenticated PostgREST read returns ZERO
  rows. The app reads via the service-role client behind `requireStaff()` + the predicate above.
  A client can never reach a staff note.
- **Key files:** `lib/notes/staff-notes.ts` (the rule + feeds), `lib/notes/note-calendar.ts`
  (pure calendar maths), `lib/notes/note-position.ts` (per-device fractional positions),
  `lib/notes/staff-alerts.ts` (the Staff Alerts feed, computed read-side — see the
  2026-09-04b entry above) + `app/api/crm/staff-alerts/route.ts` + `components/dashboard/staff-alerts-bell.tsx`,
  `app/api/crm/staff-notes/route.ts` (GET/POST/PATCH), `components/dashboard/sticky-notes-layer.tsx`
  (the global floating layer), `components/dashboard/notes-board.tsx` (Notes tab, List/Calendar
  switch), `components/dashboard/notes-calendar.tsx`, `app/(dashboard)/notes/page.tsx`,
  `components/dashboard/parked-notes-trigger.tsx` (the Parked header control, desktop + mobile —
  2026-09-08 entry above), `components/dashboard/dashboard-header.tsx` (desktop CRM header,
  collapsible search + overflow menu), `components/dashboard/sidebar.tsx` (mobile top bar).
- **Feeds:** `?scope=active` = floating layer (live, not snoozed, not parked, OR
  archived-with-a-newer-reply — `isLiveOrRevivedFor` in `lib/notes/staff-notes.ts`) and also
  returns `me` + the shareable staff `members`; `?scope=parked` = the Parked header control
  (2026-09-08); `?scope=all` = the Notes tab (incl. snoozed + parked + done); `?account_id=` /
  `?contact_id=` = per-record, still plain `isLiveFor` — only the floating layer revives a Done
  note on reply, and Parked never revives there regardless of feed.
- **Realtime** reuses the existing `ui_events` bus with a `notes` kind. **NO PAYLOAD, ever** —
  the bus re-dispatches to every staff tab, so a note body in the payload would broadcast a
  private note.
- **Positions are per-device** in localStorage as viewport FRACTIONS (never pixels — a spot set
  on a 27" iMac is off-screen at 380px), clamped, pruned against live notes, all reads/writes
  wrapped so a throwing localStorage can't crash the layer.

## Gotchas, invariants & past bugs
- **`staff_notes` is NOT in the generated DB types** — access goes through `notesTable()`
  (`(supabaseAdmin as any).from('staff_notes')`). A direct typed `.from('staff_notes')` FAILS
  THE BUILD. This already broke one sandbox deploy.
- **Client names are resolved at READ time** via the FK nested select — deliberately not stored,
  so renaming a company can't leave a stale label on old notes.
- **Calendar bucketing is LOCAL-day, never UTC.** `localDayKey()` uses local date parts; using
  `toISOString()` would shift a 09:00-local note onto the wrong day for anyone off UTC.
- **The floating layer mounts OUTSIDE `<main>`** (next to the AI panel) so it never fights
  pull-to-refresh, at z-index 45 — above the mobile top bar, BELOW every modal so a note can
  never trap a dialog's buttons. It has its OWN error boundary: an unhandled throw there would
  otherwise white-screen the whole CRM (there is no global error boundary).
- **Desktop notes render ABOVE both the sticky header AND the sidebar** (z-45; the header is
  z-30/`h-14`/56px, the sidebar is `w-64`/256px and drops to `z-auto` at the `lg` breakpoint this
  layer only ever renders at, since it goes `static`/in-flow there instead of a floating overlay —
  so it can no longer rely on stacking order to stay on top of a `fixed` z-45 note). `notePosStyle`
  in `sticky-notes-layer.tsx` shifts the WHOLE cascade grid down-and-right together via
  `calc(fraction + max(0px, clearance - cascade-start))` on each axis — not a per-note floor, which
  was tried first and shipped a real regression (compressed the gap between row 0 and row 1 on any
  short viewport, caught live before shipping, not by reasoning about the CSS) — so relative
  spacing between notes is always preserved; only the anchor point moves. The header clearance only
  bites on a short WINDOW; the sidebar clearance bites on nearly every window, since cascadePos's
  first column is a viewport-width FRACTION but the sidebar is a fixed 256px (2026-09-07, Antonio's
  second screenshot: notes still sitting on the sidebar's own nav links after the header-only fix).
  Both are CSS-only — the stored/dragged fraction itself is untouched, so this never changes where
  a note's position is remembered, only where the cascade's own landing zone visually renders.
  **The clearance also has to cover a note with an already-STORED position, not just a fresh
  cascade slot** — the shift alone only guarantees clearance for anything at or beyond cascadePos's
  own starting fraction (x=0.04 / y=0.08); an old dragged position (or one saved before this
  clearance logic existed) can sit anywhere down to 0, which the shift under-corrects. Fixed with
  an outer `max(shiftedValue, flatClearance)` — a no-op for anything the cascade itself would ever
  generate, a hard floor for anything closer to the edge (Antonio's actual notes on this exact page
  were this case: each had its own saved spot from before the fix, which is why they were scattered
  instead of lined up in a grid — confirmed by writing a note's stored position to the literal
  corner via localStorage and re-measuring, not by re-reading the CSS).
  **`HEADER_CLEARANCE_REM` is calibrated for SANDBOX, not production, on purpose:** sandbox's own
  dashboard layout adds `mt-10` (2.5rem) above the whole app to make room for the fixed orange
  "NOT PRODUCTION" banner (`app/(dashboard)/layout.tsx`) — production has no banner and no `mt-10`,
  so its header genuinely starts at the true top of the viewport, 2.5rem higher than sandbox's
  does. One constant can't know which environment it's in, so it's set for the taller (sandbox)
  case; production ends up with a bit of harmless extra headroom instead of sandbox ending up
  under-cleared. A first version of this constant was calibrated against production's header alone
  and looked right in a screenshot, but still measurably overlapped the header in sandbox by 24px —
  caught by reading the actual rendered element positions (`getBoundingClientRect`) after reload,
  not by looking at another screenshot.
  **The floor needs a matching CEILING, or it just moves the invisible-note bug to the opposite
  edge** (2026-09-07, caught by an end-to-end review, not by any live test up to that point — every
  prior live check in this fix's own history only ever pushed a note toward the LOW corner). The
  shift that clears the header/sidebar is added to EVERY note uniformly; with nothing capping the
  high end, `writePosition`'s own `clampFrac(v, 0.92)` — which exists specifically so a dragged note
  can never go fully off-screen — no longer holds once the shift is layered on top of it, since the
  shift is added to whatever the stored fraction already was. `notePosStyle` is `clamp(floor,
  shiftedValue, ceiling)` on both axes now, not a floor-only `max()`; the ceiling reserves each call
  site's own real footprint (`COLLAPSED_SIZE_REM` for the pill, `EXPANDED_SIZE_REM` for the open
  card — the card's real height is content-driven, so this is a reasonable typical-note reserve, not
  a hard cap on an unusually long reply thread) plus `EDGE_MARGIN_REM`. Verified by dragging a real
  note to the exact high corner and reading its rendered position — landed precisely at the
  calculated ceiling, not clipped.
- **Mobile has no dragging.** Floating cards collapse to a bottom-LEFT pill + sheet (bottom-right
  belongs to toasts), and the calendar falls back to a list.
- **A note's on-screen position is decided ONCE, for the whole active list together, and
  remembered for the page's life** (`notePositions` in `sticky-notes-layer.tsx`, a `useRef` map,
  not a plain `useMemo`). Never assign or recompute one note's slot independently of the others —
  an already-mounted note ignores a freshly-passed position anyway (its own `useState` only reads
  its initial value once), so recomputing from scratch without remembering what was already
  decided reintroduces the exact "every note lands on the identical pixel" bug this was built to
  fix (2026-09-05, root cause: the active feed sorts newest-first, so array-index-keyed placement
  gave every brand-new note the same slot).
- **Do NOT merge notes into `/calendar`** — that page is the CLIENT-COMPLIANCE calendar
  (renewals, annual reports, tax returns, payments). It uses a plain user-scoped client with no
  visibility predicate, so notes there would either return nothing or leak private ones.

## How to verify current state
- Visibility rule + revive-on-reply + Parked: `npx vitest run tests/unit/staff-notes.test.ts`
  (71 tests) — private/shared/team, `isLiveOrRevivedFor` (a later reply from someone else revives
  a Done note; an older reply, your own reply, a snooze, a parked note, or a legacy note with no
  per-person row do not), and `isParkedFor`/`otherPersonState`'s `'parked'` case. Live check: mark
  a shared note Done as the recipient, have the author reply, confirm it reappears red on the
  recipient's floating screen without their Done timestamp changing. For Parked: park a note from
  the floating card, confirm it leaves the screen and the header's Parked control's count updates
  live; unpark from either the header dropdown/sheet or the Notes page's Parked section and
  confirm it reappears floating.
- Calendar maths: `npx vitest run tests/unit/note-calendar.test.ts` (21 tests) — local-day
  bucketing, Mon-first 42-cell grid, month/year rollover, overdue.
- Cascade positioning: `npx vitest run tests/unit/note-position.test.ts` (11 tests) — a slot is
  never handed out twice. Live check: create 3-4 notes in one sitting on desktop and confirm they
  land at distinct, non-overlapping spots — both starting from zero notes and with others already
  active.
- Live gate: an unauthenticated `GET /api/crm/staff-notes?scope=active` must return 401 and
  create nothing.
- Privacy end-to-end (sandbox): insert a private note as A and a private note as B, then run the
  predicate as each — A must not see B's, and vice versa.
