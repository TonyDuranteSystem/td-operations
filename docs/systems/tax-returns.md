# Tax Returns & Filings
_Last verified against code: 2026-07-03 (TRIAGE-FIRST REVIEW — work queue, not archive) — Claude (Antonio's prod QA: "I see all transactions, not only the 9 I must work on — collapse the booked ones." The review section of `components/portal/tax-financials-review.tsx` is rebuilt into three tiers: TIER 1 "Needs your decision · N" — only still-uncategorized groups, expanded, Money-in/Spending split, green "All booked" banner when empty; TIER 2 "Booked — worth a glance · N" — booked rows the AI leaned personal/unsure, collapsed; TIER 3 "Booked automatically · N groups" — collapsed to per-bucket summary rows (label · merchants · $total), expandable to full cards so audit/one-tap corrections stay available. New bilingual GUIDE BOX states the contract: what the screen is, what you MUST do (answer Tier 1), what you CAN do (open sections, flip anything), answers are remembered next year, plus the booked-vs-needs-you counter. The "All as:" BULK-ANSWER buttons are REMOVED — root cause of the B&P2 incident (~200 silent binding manual answers from one exploratory click); a single group answer already covers the merchant's rows and auto-learn propagates it; if bulk ever returns it must carry confirm+undo. Shared component — same triage for client portal and staff tool; pure presentation, no engine/API change.)_
_Prior: 2026-07-03 (ProgressCard — prominent background-work states) — Claude (Antonio's prod-QA feedback, asked twice: processing banners need an hourglass/spinner, size, and plain words. New shared `ProgressCard` in `components/portal/tax-financials-review.tsx` (big animated Loader2 in a blue circle, bold title, what's-happening detail, expected-duration line) replaces three quiet one-liners: staff aiPending ("Smart categorization is working… usually 1–3 minutes; flags-never-guesses note"), staff upload-stage ("Almost ready — we're reading your statements, 2–5 min"), and the client-facing bilingual "We're preparing your statements…" (30–45 min full-year note; failed-file warning preserved). Pure UI, both modes; no logic change.)_
_Last verified against code: 2026-07-03 (SMART CATEGORIZATION v2 — Phase 0 COMPLETE + Phase 1 EVAL HARNESS + a revenue-dropping bug found by it) — Claude (Phase 0 foundations all landed (2 commits): MAX_TOKENS 8192 + stop_reason truncation surfacing; per-batch persistence BOTH AI paths (killed runs lose nothing; client deterministic updates persist BEFORE the AI loop); TOCTOU `.eq(category,uncategorized)` on ai:high writes; kill switch `AI_CATEGORIZATION_DISABLED=1`; `ai:high@v2` version stamps; learned-rules direction reconciliation + partial UNIQUE indexes (migration 20260703-1500); `ai_categorization_runs` observability table (migration 20260703-1600) written by both handlers; `ai_categorization_override` action_log telemetry in both answer routes; Chase parser contract tests. PHASE 1: pure metrics engine `lib/tax/categorization-eval.ts` (dollar-weighted precision per source, critical classes owner-draw-as-expense / transfer-leg-misbooked, open-question-group count, |ΔP&L$|, gates 98%/0.5%/zero; 10 unit tests) + runner `scripts/qa/categorization-eval.ts` (frozen fixture rules via the engine DI — leakage-free; fixtures live OUTSIDE the repo, client PII). FIRST EVAL REPORT (B&P golden, 391 labeled rows): deterministic-only 78% auto / 99.25% $-precision; full pipeline **94.1% auto / 99.41% $-precision / 0 critical errors / 9 open question groups** — ALL GATES PASS (provisional: panel=1 set, held-out pending). ⚠ THE HARNESS FOUND A LIVE PROD BUG on its first label validation: Wise Payment References are non-unique free text — 4 distinct customers paying €1,497 the same day with the same note collided on the (ref,date,amount) dedup identity and 3 REAL SALES were silently dropped at insert (€5,988 missing revenue; true B&P EUR income is €135,511.77, not €129,523.77 — every prior "golden" number inherited this engine artifact, exactly the circularity the adversarial review predicted). FIX: parseWiseCSV now suffix-dedupes refs like Relay/generic (regression test with the real 4-customer shape). Fix is ON THE BRANCH, not yet on prod — prod P&Ls with same-day same-amount same-note rows under-report until shipped.)_
_Last verified against code: 2026-07-03 (P&L tool prod-QA hotfix — Generate re-runs the deterministic pass) — Claude (Origin: Antonio's prod QA, workspace "B&P2": (a) the deterministic pass ran only at INGEST, so creating a workspace with a wrong company name ("B&P2" instead of the legal "B&P International LLC" the statements carry) killed own-entity self-transfer detection, and renaming afterwards changed nothing; (b) the AI job was killed mid-run by the function window and sat 'processing' — the 15-min reaper in `/api/cron/process-jobs` requeues it (self-healing, resumes where it stopped since labeled rows are skipped). FIXES: `POST /api/tools/pnl/[id]/generate` now re-runs `recategorizeWorkspace` with the workspace's CURRENT company_name + members BEFORE stamping + AI enqueue (idempotent; `manual:` rows immune) — Regenerate now repairs a fixed name in place; warmer upload-stage copy ("Almost ready — we're reading and preparing your statements…"); legal-name hint under the Company-name field in the blank-workspace form. B&P2 itself was renamed in place via SQL; pressing Regenerate applies the fix. Suite 4549 green.)_
_Prior: 2026-07-02 (SMART P&L CATEGORIZATION, Phase 6 — GOLDEN REPRO PASSED + Wise reference folding) — Claude (Headless golden repro on SANDBOX (workspace `a520ec93`, left in place for browser QA): the same 3 B&P files that produced the $594k P&L now yield EUR income **€129,523.77 exactly** (own-company Chase→Wise rows OUT of income — 0 leaks), 105 Chase internal-transfer rows in `conversion`, dividends in `distribution`, and the ~$266k of customer wires FLAGGED (94 rows, excluded from totals — sandbox has NO `ANTHROPIC_API_KEY`, so the AI pass skips gracefully; ⚠ add the key to the sandbox Vercel env to QA the AI layer). Learning-loop E2E PASSED: staff answer on 6 AXI wires → workspace-scoped rule learned → re-run auto-categorized the remaining 5; client-path leak check PASSED. REPRO-DRIVEN FIX: `parseWiseCSV` now folds the Payment Reference into SENT-row descriptions (`"Sent money to Andrea Bosco with reference Dividend"`) — the note was invisible to rules/AI, so €29,038 of B&P owner dividends were booked as EXPENSES (the client's "distributions don't look right" complaint). Dedup-safe: refs come from the reference column itself; the no-reference hash uses the original description. Tests in `bank-csv-parsers.test.ts`; suite 4464 green. Known refinement (not a blocker): merchant-root learning on wire lines uses the bank-header prefix — coarse but scoped + direction-gated.)_
_Prior: 2026-07-02 (SMART P&L CATEGORIZATION, Phase 4 — AUTO-LEARN) — Claude (The learning loop now covers the workspace tool: (1) migration `20260702-2100` adds `bank_categorization_rules.workspace_id` (FK→pnl_workspaces ON DELETE CASCADE) + CHECK `account_id IS NULL OR workspace_id IS NULL` — a rule is global, account-scoped, or workspace-scoped, never both. ⚠ PROD DEPLOY ORDER: apply the migration BEFORE deploying — the loaders now filter `.is('workspace_id', null)`. (2) LEAK GUARD: `getCategorizationRules` excludes workspace-scoped rows (they carry account_id NULL and would otherwise match the global branch for EVERY client — never remove that filter). `loadRulesForWorkspace` (blank) loads global + the workspace's own rules, workspace rules taking per-client precedence. (3) LEARNING WRITERS: the workspace answer route now learns on every staff answer — account-scoped when the workspace is forked/linked, workspace-scoped when blank (deliberate change to the old "sealed leak #2" stance; global writes remain impossible). `learned-rules.ts` generalized to `LearnScope` (portal call site unchanged, bare-string account id still accepted) + `LEARN_PATTERN_STOPLIST` (EN+IT generic banking words never become contains-rules). (4) PROMOTION: `saveWorkspaceToClient` promotes the workspace's active learned rules to account scope BEFORE its recategorize run (so they apply to the save itself); count in the action_log row. Tests: learned-rules 15 green (incl. the real Alpha Business in/out direction-collision case + scope isolation), full suite 4461. Deferred: staff "Learned rules" audit panel UI (deactivation via DB `active=false` meanwhile).)_
_Prior: 2026-07-02 (SMART P&L CATEGORIZATION, Phases 1–3) — Claude (Origin: B&P International $594k revenue incident — the /tools/pnl workspace counted internal transfers + a whole unrecognized Chase account as income. (1) FLAG-DON'T-GUESS: `getWorkspaceFinancialsView` now builds with `defaultUncategorizedBySign:false` — unknown money is EXCLUDED from staff totals, shown in a red banner + an explicit BS reconciling line ("Unclassified cash movement" = `uncategorizedTotal` exactly, mirrored in the Excel BS section). `computePnlTotals` gained additive `foldedUncategorizedCount/Income/Expense` fields (portal folding path byte-identical, gate 6 untouched). (2) GENERATE STAGE (Antonio): workspaces open in UPLOAD mode — statements upload + parse in background, NO totals until staff press **Generate P&L** (`POST /api/tools/pnl/[id]/generate`, 409 while ingest jobs pending, stamps `pnl_workspaces.generated_at` — migration `20260702-1930`); files added later mark the view STALE until Regenerate. (3) SEED VOCABULARY: migration `20260702-2000` seeds global rules for the bank's own type tokens (ACCT_XFER → internal transfer; Online Transfer to/from MMA/CHK/SAV with `transaction#` guard → internal; FEE_TRANSACTION / SERVICE CHARGES → fee; INTEREST PAYMENT → interest income). Deliberately NOT seeded: incoming-wire income (own-money risk), Zelle (human review), DEPOSIT_RETURN (would inflate expenses — pre-mortem). (4) OWN-ENTITY INCOME FIX (shared core, `lib/tax/transfer-matcher.ts`): income rows whose COUNTERPARTY equals an own-name — incl. the &-dropped bank spelling via new `nameVariants` — reclassify to internal transfer; fixes Wise "Received money from <own company>" booked as revenue (€29,269 of the B&P error). Contains-matching still never touches income. (5) WORKSPACE AI PASS: new `recategorizeWorkspaceAi` (lib/tax/workspace-recategorize.ts) + `recategorize_workspace_ai` job, enqueued by Generate when uncategorized rows remain — policy shared with the client path via new pure `decideAiSuggestion` in categorization-engine.ts (high-confidence → category on still-uncategorized rows only, tagged ai:high; everything else advisory hints; manual rows immune); workspace GET reports `aiPending`, UI polls + shows progress. Tests: pnl-totals (folded fields + BS-gap invariant), transfer-matcher (income strict path, nameVariants, Chase RTP variant), categorization-engine (Chase seed shapes incl. negative AXI/Chelton/Zelle cases, decideAiSuggestion). Full suite 4455 green. Next phases (planned, not yet built): auto-learn workspace scope + promotion on save; golden repro on sandbox.)_
_Prior: 2026-07-02 (night) — Claude (ACCOUNTANT PDF QUALITY PASS, follow-up to the overlap fix: (1) `normalizeTaxPayloadForPdf` in `lib/form-to-drive.ts` (exported, unit-tested in `tests/unit/normalize-tax-payload.test.ts`) — folds flattened wizard repeater keys into renderable arrays: `member_N_*`→`members_list` (moved out of saveFormToDrive inline code, same behavior + now drops the folded raw keys) and NEW `related_party_transactions_N_rpt_*`→`related_party_transactions`, so the accountant sees "Transaction N" blocks with readable direction/type labels instead of raw codes (`from_llc`→"Money paid TO this party") dumped as flattened rows in Additional Information; also drops alias keys (ein/llc_ein/email/personal_email/phone/first_name/company_name/…) ONLY when their canonical twin holds the identical value — divergent values always stay visible. (2) `lib/pdf/itin-data-summary.ts` DELETED — dead code, zero callers since April; the live ITIN summary goes through FORM_CONFIGS.itin in form-to-drive (already collision-proof). (3) Backfill script `scripts/backfill-tax-data-pdf-layout.ts` — regenerates the 2025 Tax_Data PDFs in Drive with the fixed layout (dry-run default, --live to mutate; renames old files to `_OLD_<date>`, never deletes; verifies 0 overlaps per PDF with pdfjs before upload; reads `.env.prod.local` via --env, derives Supabase URL from EXPECTED_SUPABASE_REF). Dry-run 2026-07-02: 62/64 OK, 2 skipped (Prometiq Media LLC, Financialot LLC — no drive_folder_id).)_
_Prior: 2026-07-02 (evening) — Claude (ACCOUNTANT PDF OVERLAP FIX: `generateFormSummaryPDF` in `lib/form-to-drive.ts` no longer overprints values onto long labels — all four field render sites now use one measured-layout `drawFieldRow` helper (`valueStartX`/`wrapByWidth`/`PDF_LAYOUT`, exported + unit-tested). Reported by the accountant (Smit Shah) for INTERNATIONAL AUTOMOBILE AUTHORITY LLC's SMLLC package: "Personal Expenses Paid Through LLC (USD)" value 34659.79 was unreadable. Geometric regression test `tests/unit/pdf-field-layout.test.ts` (pdfjs text-position parse, asserts zero same-line intersections across all FORM_CONFIGS). Affects the summary PDFs of ALL six form types (tax/formation/onboarding/ITIN/banking/closure) — layout only, data content unchanged. See Gotchas.)_
_Prior: 2026-07-02 (later) — Claude (STANDALONE P&L TOOL — SHIPPED TO PRODUCTION (PR #139 merged 2026-07-02; migration `20260701-2340-pnl-workspaces.sql` applied to the prod DB and tables verified — supersedes the "SANDBOX ONLY" note in the entry below). Plus: added an in-page **"How it works"** help panel to `/tools/pnl` (`app/(dashboard)/tools/pnl/page.tsx`) — a collapsible plain-English guide for staff: what the tool is, blank vs fork, what you can do, the Save-to-client Merge/Replace action, and a "good to know & risks" list (isolation, Replace overwrites, fork is a snapshot, MMLLC-only). Refreshed the stale page doc-comment (it described the old client-bound behavior). Static JSX only — no logic change.)_
_Prior: 2026-07-02 — Claude (STANDALONE P&L TOOL — ISOLATED WORKSPACES (fixes a data-safety bug). The `/tools/pnl` staff tool previously drove a REAL client's account+year — it preloaded the client's stored data AND wrote staff test uploads into the client's real `bank_transactions` ("Statements are saved to the client's records"). It now runs ISOLATED **workspaces**: a staff run is independent (blank OR forked from a client) and touches NOTHING real until an explicit, audited "Save to client". Tables (migration `20260701-2340-pnl-workspaces.sql`): `pnl_workspaces` (blank or forked; `entity_type` registry-selected — MMLLC only today; `company_name`/`ein`/`prior_return_snapshot`/`linked_account_id`), `pnl_workspace_members` (`member_type` individual|company — ENTITY members supported), `pnl_workspace_transactions` (mirrors the exact bank_transactions columns the engine reads, scoped by `workspace_id`, dedup `UNIQUE(workspace_id,transaction_ref,transaction_date,amount)`); all RLS-on-no-policy (service-role only). PARALLEL PATH — NO wizard code touched. PARITY: the deterministic categorization core was extracted to `computeRecategorizationUpdates` (`lib/tax/categorization-engine.ts`) which BOTH the client `recategorizeAccountYear` (delegates, byte-identical) AND the workspace path call — so a fork categorizes identically (E2E: fork B&P net income == client to float precision). Engine reuse: `getWorkspaceFinancialsView` (`lib/tax/workspace-orchestration.ts`) assembles the SAME `FinancialsView` via `buildFinancialDraft`/`evaluateGates`/`resolveOwnership` — with NO ownership sync-back. Ingest: `ingestWorkspaceCsv` (`lib/tax/workspace-ingest.ts`) + NEW job type `ingest_workspace_statement` (own handler `lib/jobs/handlers/ingest-workspace-statement.ts`; the wizard's `ingest_bank_statement` is UNTOUCHED) + workspace-scoped storage `pnl-workspaces/{id}/`. SAVE-TO-CLIENT (`lib/tax/workspace-save.ts`) is the ONLY write to real `bank_transactions`: pure `decideSaveToClient` gate (concurrency in-flight → refuse; empty target → insert; non-empty → must choose merge|replace, else refuse), REVERSIBLE Replace (full JSON backup to storage BEFORE delete), `action_log` audit (`action_type='pnl_workspace_save_to_client'`), then calls the client's normal `recategorizeAccountYear`+`resetFinancialsAttestation`. SEALED LEAKS (a workspace never writes real/global state until Save): no global-catalog add-bucket, no rule-learning, no ownership sync-back, no `tax_return_submissions`, no client storage folder. Routes `app/api/tools/pnl/**` (all `isDashboardUser`): create blank|fork, list, view, delete+purge, upload, answer, save, download (Excel via `buildFinancialsWorkbook` — same builder as portal), statement-delete, category-transactions, set-bucket. UI `app/(dashboard)/tools/pnl/staff-financials.tsx` (workspace list, blank/fork toggle, member intake form incl. entity members + %-total, open→`TaxFinancialsReview` with `apiBase`, Save-to-client with Merge/Replace prompt on 409). Shared component `components/portal/tax-financials-review.tsx` now takes an optional `apiBase` prop (default `'/api/portal/tax-financials'` = portal BYTE-IDENTICAL); `add-bucket` hidden in staff mode. ENTITY SCOPE: MMLLC only; the taxpaying C-Corp land tool is a SEPARATE future project (organizing principle: who pays US tax — MMLLC pass-through owes none, informational; the land C-Corp does). HOW TO VERIFY: `DOTENV_CONFIG_PATH=.env.local npx tsx --tsconfig tsconfig.json scripts/qa/pnl-workspace-e2e.ts` — 29 assertions against REAL B&P sandbox data (isolation zero-writes / fork parity / save merge-replace-backup-audit / leak-zero / entity guard / concurrency; auto-cleans). Unit: `tests/unit/categorization-engine.test.ts` (parity core), `tests/unit/workspace-save-decision.test.ts`. `npm run build` green; suite 4326. SANDBOX ONLY — not on production. Full plan: sysdoc `pnl-workspace-isolation-plan` + dev_task `56783fa2`.)_
_Prior: 2026-07-01 — Claude (TAX-FINANCIALS RUNNABLE STANDALONE BY STAFF + Excel now matches the screen. (1) New staff page `/tools/pnl` (`app/(dashboard)/tools/pnl/{page,staff-financials}.tsx`, isDashboardUser) — staff pick ANY client + year and drive the SAME `TaxFinancialsReview` clients use in `/portal/tax-financials` (upload PDF/CSV → ingest+save to records → categorization → 6 gates → P&L/BS → Excel), via a new `mode:'staff'` prop on the component (hides the client attestation + the `/portal/wizard` link; defaults to `'client'`, portal unchanged). The 10 `app/api/portal/tax-financials/*` routes already allow staff (`isAccountOwner` → true for non-`client` roles). (2) SCREEN==FILE FIX: the download route (`app/api/portal/tax-financials/download`) used `generatePnlExcel` (re-derived from raw transactions — different uncategorized handling + beginning balances from statement-opening, NOT the prior return), so the file could disagree with the screen. It now renders from the SAME engine draft via new `lib/tax/financials-excel.ts::buildFinancialsWorkbook` (P&L/BS/K-1-capital from the `FinancialDraft`; detail sheets from transactions). (3) New standalone prior-year-RETURN upload (`app/api/portal/tax-financials/prior-return` POST + control in the staff page) reuses `extractPriorReturn`, stores `prior_return_extracted` on the account+year completed submission (creates a minimal one if none — side-effect-free: no DB triggers, board SD-anchored, What's New review_status-keyed). `generatePnlExcel`/`bank_statement_pnl` still exist for other callers (draft migration = tracked follow-up). CPA-audit accounting refinements (BS FX year-end spot; liabilities capture; guaranteed payments; entity gate; Sch-B Q4 exception; tax-basis label) tracked separately, NOT here.)_
_Prior: 2026-06-29 — Claude (MMLLC OWNERSHIP = 100% + BEFORE-YOU-START GATE + FLEXIBLE BANK GUIDE. (1) Tax MMLLC wizard has NO separate owner step — the members list IS the full roster, so member ownership must total EXACTLY 100% (it was wrongly using the formation-era "additional members <100%, owner takes the remainder" rule, which rejected a valid 50/50 split — Luca repro). Fixed in app/portal/wizard/wizard-client.tsx submit validation, scoped to wizardType==='tax' (Math.abs(sum-100)<=0.5); formation MMLLC keeps the owner-remainder rule. Added an early gate on the members step (validateStep) + a live ownership-total readout in the members UI. lib/jobs/validation.ts (formation path) untouched. (2) "Before You Start" step: stronger amber attention message (WE build the P&L + Balance Sheet from their bank statements → download ALL statements for the year first, every bank/currency) + a REQUIRED "I have read this" checkbox that gates Next (formData.prepare_acknowledged; validateStep returns it for stepId==='prepare'). (3) Flexible any-bank CSV guide: new POST /api/portal/bank-guide (catalog match → AI-generate + cache to catalog_entries bank_export_guides → generic fallback) + pure helpers lib/tax/bank-guide.ts (21 tests); the wizard "Show me how to download the CSV" lookup now returns steps for ANY bank, not a fixed list. Sandbox-validated incl. full MMLLC E2E on the Dynamiq test account (Luca confirmed it worked). 4057 unit tests + build green. NOTE: Anthropic monthly spend cap until ~July 1 keeps the AI bank-guide path on the generic fallback until quota resets.)_
_Prior: 2026-06-27g — Claude (WIZARD 'Before You Start' CSV-prep step. New FIRST step (id 'prepare') in the MMLLC tax_return wizard (components/portal/wizard/wizard-configs.ts) — info-only (no fields → validateStep passes, always advanceable). Rendered by PrepareCsvStep in app/portal/wizard/wizard-client.tsx: a prominent 'get your transactions as a CSV before you continue' message (EN/IT) + a bank-name lookup showing the per-bank CSV-download steps from the bank_export_guides catalog (same data as the inline per-row guide). Sets the CSV expectation up front (the P&L/BS is a gift, not our job to parse messy PDFs); PDF still accepted downstream but discouraged. CAVEAT: adding a first step shifts saved step indices — an in-flight tax wizard resumes one step off (low in-flight count; acceptable; saved-step is by index). Suite 4036 + build green. To deploy to sandbox for Antonio's browser QA; NOT on prod. Pairs with the generic-CSV parser below.)_
_Prior: 2026-06-27f — Claude (GENERIC bank-agnostic CSV parser — the flexibility fix so a NEW client on a NEW bank works with no code. Strategy decision (Antonio): push clients to upload CSV (cheap/free + reliable) rather than us parsing costly PDFs; the system must read ANY CSV. New `parseGenericCSV` in lib/bank-csv-parsers.ts: sniff dialect (delimiter + comma-decimals, reuses sniffCsvDialect/parseDelimitedRows) → locate the header row within the first 15 rows (handles preamble) → fuzzy-map columns by synonym (date / amount / debit+credit / balance / currency / description / counterparty, EN+IT) → flexible date parse (ISO + M/D/Y vs D/M/Y auto-detected from data, flags ambiguity) → signed amount incl. (parentheses)=negative and debit/credit→credit−debit → stable content-hash refs. Wired in parseBankStatement CSV routing AFTER the exact per-bank parsers and ONLY when detectCsvSignature===null (so wise_transfers + recognized-but-unparsed formats still go to AI, never the generic parser); unmappable layout returns 0 rows → AI fallback unchanged. extraction_method 'generic_csv' added to the union. Net effect: Wise/Mercury/Relay/Revolut/Slash keep their exact parsers; ANY other bank's CSV now parses free+instant with no per-bank code; a simple zip-inner 'date,amount' CSV that used to hit AI is now free. Tests: tests/unit/generic-csv-parser.test.ts (12: signed/ISO, debit-credit, money-in/out, parentheses, M/D vs D/M detection, EU ;+comma-decimals, preamble, currency, unmappable→0, ambiguity flag, routing→generic no-AI). Full suite 4036 + build green. NEXT (not built): wizard opening 'prepare your CSV' step + the bank-download lookup at the start (UI; needs placement decision + browser QA). Branch only, not on prod.)_
_Prior: 2026-06-27e — Claude (LARGE multi-page PDF CHUNKING — the real fix for the Chase under-extraction (NOT OCR). Phase-0 diagnosis disproved the OCR theory: the failing Chase file is a 60-page MACHINE-READABLE PDF (Claude got 94 of ~521 tx — a single pass under-reads a long PDF), and it's byte-identical to the Chase-zip inner PDF; Document AI sync OCR can't even take it (30-page cap) and wouldn't help (text already present). Fix in lib/bank-statement-ai-extract.ts: the proven single-pass logic is now `extractSinglePass` (+ optional maxAttempts/budgetMs); `aiExtractBankStatement` is a wrapper that, for a PDF with >15 pages, splits it via pdf-lib into 10-page sub-PDFs (`splitPdfIntoChunks`), runs extractSinglePass per chunk, and MERGES (concat tx → re-dedupe refs across the combined set since page windows don't overlap → reconcile against FIRST chunk opening + LAST chunk closing + Σ all). Time-bounded: ~220s spread across chunks (≥25s each), hard-stop at 250s elapsed (remaining chunks flagged + reconciliation catches incompleteness), all inside the 300s ingest job. SAFETY: any pdf-lib parse failure / ≤15-page PDF → returns null → exact single-pass behavior (chunking never worsens a readable file); CSVs untouched. extraction_method stays 'ai'. Tests: 3 new in bank-statement-ai-extract.test.ts (18 total) incl. real pdf-lib 20-page split→2 chunks→merge+reconcile, ≤15pp single-pass, unparseable→fallback. Built + full suite green. Verify on prod with the real 60-page Chase PDF (expect ≫94 tx). True OCR for genuinely-scanned image PDFs remains UNNEEDED so far (none seen) — backlog only.)_
_Prior: 2026-06-27d — Claude (AI EXTRACTION RELIABILITY — retry the non-deterministic empty/transient read. Real-data QA showed PDF AI-extraction is non-deterministic: the SAME readable Chase PDF returned 94 tx on one attempt and 0 ('could not read') on another. `aiExtractBankStatement` (lib/bank-statement-ai-extract.ts) now retries up to 3 attempts when a call returns an EMPTY result (0 tx, not a max_tokens truncation) or a TRANSIENT API error (408/409/425/429/500/502/503/504/529), with 1s/2s backoff, bounded by a 250s global deadline (per-attempt timeout = min(240s, remaining)) so all retries stay inside the 300s ingest-job window. A SUCCESSFUL first call still makes exactly ONE request (no behavior/cost change for the working banks Relay/Mercury/Wise); a permanent 4xx (e.g. 400) is NOT retried; a truncated result is taken+flagged, not retried. On recovery the result carries a 'Recovered after retry' note. LIMIT (honest): a genuinely unreadable/scanned PDF (0 tx on every attempt — e.g. the Chase zip's image-only PDF_VERSION.pdf) still ends empty and is surfaced as ingestFailed for human review — true OCR of scanned statements is separate future work, NOT done here. 5 new tests in bank-statement-ai-extract.test.ts (15 total). Built + full suite green. Verify on prod after ship.)_
_Prior: 2026-06-27e — Claude (TYPE-PLUMBING ONLY, no behavior change: `ReviewHistoryEntry` (lib/tax/review-status.ts) got a jsonb `[key: string]: Json` index signature so it's assignable to a `Json` column — clears 4 `review_history` push type-errors with no logic change. Part of a repo-wide DB-types regen + pinning the Supabase CLI to 2.107.0 for deterministic generation. Review-status state machine / transitions / history semantics unchanged.)_
_Prior: 2026-06-27c — Claude (TWO FIXES from real-data stress QA. (1) IDEMPOTENT INGEST / "failed file whose data is in" bug: PDF AI-extraction is non-deterministic — a RETRY of an already-ingested file can return 0 tx ("could not read") and flip the file to FAILED even though its rows persist via source_file_id and are counted in the P&L (real case: a Chase Jan PDF showed 94 tx in the P&L but ingestFailed reported it failed). Fix: `ingestPortalCsv` now SHORT-CIRCUITS at the top — if this exact content (source_file_id = sha of bytes) already has rows for account+year, it returns ok:true ('already processed — N transactions on file') WITHOUT re-parsing. A genuinely new/edited file has different bytes → different source_file_id → falls through to normal parse. This also stabilizes flaky AI retries (best result is never overwritten by a worse one). (2) EMPTY-FILE GUARD: `/upload` now rejects a 0-byte file with 400 'This file is empty…' (previously accepted → failed downstream as unreadable). Tests: portal-csv-ingest idempotency case added (suite 6). NOT FIXED (flagged, out of scope/too risky to blind-change): (A) underlying AI PDF extraction accuracy on Chase-style PDFs (some 'could not read', Mercury PDFs vary run-to-run) — CSV/deterministic sources are exact; needs dedicated extractor work. (B-cosmetic) per-file 'inserted' count inflates on duplicate files (counts ignored-dup upserts) — label-only, would need a .select() per row in the hot path. Built + full suite green. Verify on prod after ship.)_
_Prior: 2026-06-27b — Claude (ATTEST IN-FLIGHT GUARD — bug found during stress QA. `POST /api/portal/tax-financials/attest` did NOT check whether statement ingestion was still running: with `ingestPending>0` and 0 transactions it returned `attested:true` and fired `runAttestHandoff` (zero-data Excel to Drive + premature staff task). The client UI disables the button while ingestPending>0, but the server didn't enforce it (bypass/race exposed it). Partly self-healing — a later ingest that inserts rows calls `resetFinancialsAttestation`, invalidating a premature attestation — but the junk handoff still fired. Fix: new `lib/tax/ingest-status.ts::countInFlightIngestJobs(accountId, taxYear)` (counts pending/processing `ingest_bank_statement` jobs scoped to the year via JSONB payload); the attest route now returns 409 'Your statements are still being processed…' when >0, before the gate/coverage checks. New test `tests/unit/ingest-status.test.ts` (4). NOTE: the financials GET route still computes its own `ingestPending` (by-path dedup) inline — left as-is; this guard is a simpler any-in-flight check. Built + full suite green. Found via prod synthetic stress QA (account QA Stress Prod TEST LLC); sandbox was running STALE code so QA ran on prod. NOT yet shipped — awaiting Antonio's go.)_
_Prior: 2026-06-27 — Claude (ZIP FAN-OUT — big multi-statement .zip no longer times out. Found during real-scenario prod QA with Dynamiq's real files: a 1.5 MB Mercury zip (~12 monthly statements) could not be AI-extracted within one ingest job's time budget → reaped/retried/failed; the smaller Relay/Chase zips were fine, so it scales with archive volume. Root cause: the `ingest_bank_statement` handler processed the WHOLE zip in ONE job (`parseBankStatement`→`parseZipArchive` parses every inner statement inline). Fix: the handler now detects a `.zip`, EXPANDS it cheaply (new `extractZipStatements()` in `lib/bank-statement-parser.ts` — `fflate.unzipSync` + same PDF/CSV filter as `parseZipArchive`, no AI), and enqueues ONE `ingest_bank_statement` job per inner statement via `saveAndEnqueueStatementUpload` (content-hashed path + idempotent dedup), then completes with an 'expanded into N statement(s)' summary — the zip itself is never ingested inline. Each inner .pdf/.csv runs through the SAME handler as its own small job (zip branch skipped), so each piece finishes inside budget; the existing `notifyIfIngestComplete` fires the client 'statements ready' message once ALL inner jobs land. Idempotent on retry (already-queued inner files skipped). Corrupt/empty archive → ok:false no-retry; all-inner-enqueue-failure → throw (transient retry). `ingestPortalCsv` keeps its own zip capability for any other caller (no regression). New tests: `tests/unit/extract-zip-statements.test.ts` (3, real fflate zip) + `.zip fan-out` block in `tests/unit/jobs-ingest-bank-statement.test.ts` (5). Built + full suite green in sandbox. NOT yet shipped to prod — awaiting Antonio's go.)_
_Prior: 2026-06-26d — Claude (UPLOAD ROUTE NOW ASYNC — the real fix for the upload 500. The 26-06-26b/c fixes removed the dangling promises but the upload route STILL 500'd ("No response is returned from route handler") on prod, because the cause was the HEAVY SYNCHRONOUS INGESTION itself (`POST /api/portal/tax-financials/upload` ran `ingestPortalCsv` — parse → categorize → per-row insert loop → full-year `recategorizeAccountYear` — inside the request; that work overran/teardown the serverless function before it could respond; sandbox returns 200 because it can't reproduce the prod runtime limits). Fix: the route is now THIN and mirrors the wizard path — it archives the raw file to `onboarding-uploads` and enqueues ONE `ingest_bank_statement` job (new helper `lib/tax/portal-upload-enqueue.ts::saveAndEnqueueStatementUpload`, DIRECT job_queue insert + content-hashed path `tax/{account}/{year}/{sha16}_{name}` + idempotent on existing non-failed job for that path), then AWAITS a BOUNDED `triggerWorker()` (5s timeout — awaited so it never dangles) and returns `{ok, queued, alreadyQueued, fileName}`. The existing `ingest_bank_statement` handler downloads + runs `ingestPortalCsv` in the background; the financials view already counts `ingestPending` by file and the client (`tax-financials-review.tsx`) already polls every 20s + shows the 'preparing' panel, so transactions/P&L fill in as the job drains (worker kick = prompt; 5-min `process-jobs` cron = safety net). UX trade-off: the uploader's instant "248 tx, Jan–Dec" + instant duplicate-alert is gone (same as the wizard) — replaced by "received — reading now" copy. `maxDuration` dropped 300→60 (no heavy work left in-request). New unit test `tests/unit/portal-upload-enqueue.test.ts` (5). Verified prod live QA on a fresh test account: uploads return 200 reliably, jobs drain, P&L renders. Feature PARKED during the fix; unparked after prod re-verify.)_
_Prior: 2026-06-26c — Claude (PROD HOTFIX follow-up — the REAL cause of the upload 500. The 26-06-26b fix (move AI pass to a job) did NOT fully fix it: `enqueueJobs()` itself fires `triggerWorker()` (an `await fetch(/api/jobs/process, {timeout:5000})`) WITHOUT awaiting it, so calling enqueueJobs from a request handler leaves THAT fetch dangling past the response → Vercel function teardown → "No response is returned from route handler" → empty 500 (confirmed on prod: uploads went 200 then degraded to 8/8 500 as warm instances were poisoned; other tax-financials routes were unaffected, isolating it to the upload route's enqueue). Fix: the two REQUEST-PATH enqueues — `ingestPortalCsv`'s `recategorize_ai` job and `enqueueStatementIngestJobs`'s `ingest_bank_statement` jobs (wizard-submit) — now INSERT directly into `job_queue` (no `enqueueJobs`, no `triggerWorker`). The 5-min `process-jobs` cron drains the pending rows (advisory AI + statement ingestion both tolerate the short delay; the financials page already shows a 'preparing' state). Verified sandbox 8/8 uploads → 200 + recategorize_ai inserted pending; prod re-verified 10×. 3998 unit tests + build green. Feature PARKED during the fix; unparked after prod re-verify.)_
_Prior: 2026-06-26b — Claude (PROD HOTFIX — upload no longer 500s the client. Found during prod QA after the tax-financials system shipped to production: `POST /api/portal/tax-financials/upload` returned an empty 500 ("No response is returned from route handler") to the client EVEN THOUGH the CSV ingested fine. Root cause: two fire-and-forget promises ran after the result was computed — the background AI categorization pass inside `lib/tax/portal-csv-ingest.ts` (`void recategorizeAccountYear(..,{aiAssist:true})`) and the raw-CSV storage archive in the route. On Vercel a promise that outlives the HTTP response gets the function torn down mid-flight (same class as the wizard fire-and-forget). The cron worker (`process-jobs`) hit it too. Fix: (a) ingestPortalCsv now ENQUEUES a new `recategorize_ai` job (idempotent — one pending per account+year; `lib/jobs/handlers/recategorize-ai.ts` + registry) which the worker AWAITS to completion, instead of a dangling promise; (b) the upload route AWAITS the storage archive (small/fast; failures logged, never fail the request — ingestion already committed). No promise now outlives any response. The deterministic categorization still runs synchronously so the P&L is immediate; AI hints land via the job after. Verified in sandbox: upload returns 200 + recategorize_ai enqueued + worker processes it; 3997 unit tests + build green. Shipped to prod; feature PARKED (`app_settings.tax_season_paused=true`) during the fix and unparked after prod re-verify. Engine/categorization/P&L/BS/K-1 confirmed CORRECT on a real prod MMLLC test account (Revenue computed exactly, balance sheet balanced, K-1 ownership 100%).)_
_Prior: 2026-06-26 — Claude (FINANCIALS SELF-SERVICE — 4 robustness fixes, branch `fix/tax-financials-trust-and-ux` (off feat/mmllc-tax-financials + main), SANDBOX. (1) NO MORE MISLEADING $0 P&L DURING INGESTION: the financials page (`/portal/tax-financials`) computes on demand from `bank_transactions`, which land asynchronously as per-file `ingest_bank_statement` jobs finish (~CSV 5s, PDF 1-3min via AI — a full year/3 banks ≈ 35-45min; measured on Dynamiq's real 36-statement set, 3451 tx). The API route now counts in-flight + failed ingest jobs by DISTINCT FILE (`payload.path`, NOT by job — the stuck-job reaper re-enqueues, so counting jobs over-reported failures) → returns `ingestPending`/`ingestFailed`; `components/portal/tax-financials-review.tsx` shows a 'we're preparing your statements (N processing)' panel instead of $0 when nothing has landed, a 'still processing, numbers partial' banner when partial, BLOCKS attestation while `ingestPending>0`, and self-polls every 20s. Zero pending ⇒ behaviour identical to before. (2) DON'T-MERGE GUIDANCE: the wizard bank-statements hint (wizard-configs.ts, EN+IT) and the unreadable-file rejection (`lib/tax/portal-csv-ingest.ts`) now explicitly tell clients NOT to merge/combine/edit files (merge-csv.com breaks the format — the Luca round-4 failure). (3) SUBMITTED-BANNER TIMING: `app/api/portal/wizard-submit/route.ts` sets `review_status='submitted'` SYNCHRONOUSLY on first tax submit (guarded `.is('review_status', null)` so resubmit-after-revision is left to tax-form-setup step 9), so the portal banner reads 'submitted — under review' immediately instead of briefly showing 'Complete your tax form' (which made Luca re-submit). (4) STATEMENTS-READY NOTIFICATION: NEW `lib/jobs/ingest-complete-notify.ts::notifyIfIngestComplete` — called from the ingest handler after a file lands; when it's the LAST ingest job for the account+year (no other pending/processing), posts a one-time locale-aware system portal message linking to /portal/tax-financials (idempotency marker `financials_meta.ready_notified`; mirrors wizard-failure-notify; never throws). 7 new unit tests + 2 existing tests updated for new copy/`.is()`; suite 3905 green; build clean. Sofia/Dynamiq full real case verified E2E (3 banks, CSV+PDF, multi-currency, 0 failures); B&P 2nd-client test set up for human finish. NOTHING on production.)_
_Prior: 2026-06-23 — Claude (INCOME QUESTION REMOVED — corrects a wrong premise. The earlier "income question" (do you earn/spend in another account → it may belong on owners' home-country returns) was built on a false model and is DELETED. CPA-correct model (Antonio): a foreign-owned US LLC invoices everything through the LLC; we file the LLC's US return from the LLC's own books; what owners do personally / outside the US is NOT our concern and not on the return. The only completeness concern is whether the LLC's books are whole — a missing LLC account, missing months, unbalanced sheet, unresolved ownership — which the completeness summary already states neutrally (the balance_sheet_off line: "usually a missing account, missing months, or money held in another currency or account we don't see"). Removed: lib/tax/completeness.ts income_question / IncomeQuestionState / FOREIGN_ACTIVITY_FLOOR / IncomeAnswer (CompletenessSummary is now {items, can_accept_as_is}; can_accept_as_is = no blocking gate fails); financials-orchestration foreignActivityTotal + financials_meta.income_attestation load; DELETED route app/api/portal/tax-financials/income-answer; attest route income gate; review component income-question UI + the home-country attestation bullet + the income-gate message. KEPT (CPA-sound, re-audited): the completeness summary items, the accept-as-is attestation (its "we worked only with what you gave us; missing accounts/income are your responsibility" point is the correct neutral framing), foreign-currency→USD conversion (about the LLC's own transactions), prior-return carry-forward, first-year preselect. 8 completeness tests (income tests dropped); suite 3,503 green; build clean. SANDBOX ONLY, branch feat/mmllc-tax-financials. NOTE: the 2026-06-22 entries below describe the income question as if live — it is now removed; this entry supersedes that premise.)_
_Prior: 2026-06-23 — Claude (TWO small follow-ups on the financials self-service, Antonio-approved. (1) FIRST-YEAR PRESELECT: the tax wizard prefill (app/portal/wizard/page.tsx) now preselects prior_return_case='first_year' when the account's formation_date falls within (or after) the filing year — reuses the pure `firstYearCoherent(formation_date, taxYear)` from prior-return-case.ts (single source of truth; submit route re-cross-checks regardless). Sits in an else after the existing we_filed preselect (a real prior 'TR Filed' wins). Removes a one-answer question for brand-new companies; same pattern as the we_filed preselect. (2) RICH ATTESTATION EXPLAINER: the portal review confirm step (components/portal/tax-financials-review.tsx) now shows a 'Before you confirm' box above the attestation checkbox — 5 plain-language bilingual points: what the P&L/BS is + where it came from, what confirming does (we prepare the return on this basis), responsibility for unreported accounts/income, the foreign-owner home-country-income caveat, and the 'not ready? edit/upload instead' escape. The legal checkbox sentence is unchanged; the box only educates. Income-question trigger DECISION: kept BROAD (fires on conversion OR non-USD ≥ $1000, incl. USD-to-USD own-account transfers) — Antonio's call. No new lib functions (firstYearCoherent already tested, 5 cases); build + full suite green. SANDBOX ONLY, branch feat/mmllc-tax-financials.)_
_Prior: 2026-06-22 — Claude (WE-FILED AUTO-CARRY-FORWARD — when WE filed last year's return (prior-return case `we_filed`), the system now reads OUR own filed return and feeds this year's beginning balances, instead of always deferring to staff. Why it was needed (Antonio's QA): `we_filed` only DETECTED the filed record (tax_returns 'TR Filed') — it never pulled the numbers, because `tax_returns` stores no balance data and the only auto-extract path was the client-upload `filed_elsewhere` case. NEW `locateAndExtractOurFiledReturn` (lib/tax/prior-return-extract.ts): finds the filed-return PDF via the `documents` table (account-scoped, `document_type_name` ∈ Form 1065/1120/1120-F/Tax Return, mime pdf), ranks candidates by `rankFiledReturnCandidates` (pure: year-in-filename first — `documents.tax_year` is NULL in practice so the year is read from the file name like 'Form 1120 2024 - …'; 1065 over 1120; newest tiebreak; deduped), downloads top ≤3, runs the SAME `extractPriorReturn` validated extractor, returns the first VALIDATED record. SAFE BY CONSTRUCTION: the extractor re-checks tax_year + EIN + balance identities from inside the PDF and quarantines any mismatch, so a wrong/loose filename can never feed the wrong year; every miss (no doc / unreadable / quarantined) falls back to the ORIGINAL 'staff tie out' behavior → zero regression. `we_filed` union (prior-return-case.ts) gains `validated`/`quarantined` (+extracted/issues/source); the `we_filed` branch calls the locator after detect. NEW pure `validatedExtraction(prior)` is the single accessor both prior-return sources read through — `priorEndingCash`/`priorBeginningCapital` (financials-engine) + `priorK1s` (orchestration) now read filed_elsewhere OR we_filed validated identically. Gate 2 restructured: we_filed `validated` falls through to the real tie-out (was wrongly hitting the claim-mismatch 'fail'); `on_file` → na 'staff tie out'; `claim_mismatch` → fail; `quarantined` (either source) → fail 'staff reviewing'. Runs fire-and-forget at submit (processPriorReturnCase). Mostly helps MMLLC 1065 (Schedule L + K-1s); SMLLC pro-forma 1120 usually has no Schedule L → quarantines → staff (unchanged). 20 new unit tests (rank/locator DI + validatedExtraction + gate-2 equivalence); suite 3,505 green; build clean. QA CAVEAT: Drive `downloadFileBinary` is mocked in sandbox, so true E2E extraction is prod-only — unit tests cover the logic. SANDBOX ONLY, branch feat/mmllc-tax-financials.)_
_Prior: 2026-06-22 — Claude (COMPLETENESS SUMMARY + "provide more / accept as-is" + income question, generic for ALL clients — dev_task 95127bb2, generalizing the Dynamiq learnings. NEW pure `lib/tax/completeness.ts::buildCompletenessSummary` turns the draft + the six gates into (a) machine CODES the review UI renders bilingually (reconciliation_gap, no_prior_year, balance_sheet_off [with the signed A−(L+C) gap], capital_rollforward, ownership_incomplete, unattributed_owner_moves, missing_fx_rate) and (b) a targeted INCOME question that fires only when foreign/cross-account movement ≥ `FOREIGN_ACTIVITY_FLOOR` ($1000 USD — Σ|amount| over conversion rows + non-USD rows, in USD). `getFinancialsView` now loads `financials_meta`, computes foreignActivityTotal + the non-USD currencies lacking an IRS rate, and attaches `completeness` to the view. The income answer is stored in `tax_return_submissions.financials_meta.income_attestation` ({answer:'earn_spend'|'parked_only', at}) via NEW `POST /api/portal/tax-financials/income-answer` (owner-only, locked post-review like /coverage). GATING (Antonio's approved design): soft-warn ALL balance-sheet/tie-out gaps — these clients owe no US tax and the BS is informational, so they never hard-block; but a REQUIRED income question must be ANSWERED before accept-as-is (`can_accept_as_is = noBlockingGate && (!required || answered)`), enforced BOTH in the UI button and SERVER-SIDE in the attest route (422). Either answer unblocks (client owns it); 'earn_spend' surfaces a prominent warning that the unseen income belongs on partners' home-country returns and is recorded for K-1 follow-up. The attestation checkbox is reframed as explicit accept-as-is ("I accept these numbers as they are, based on the information I have provided… unreported accounts/income are my responsibility") — DRAFT wording, pending Antonio/CPA blessing. NOTE: gate 6 (uncategorized) already auto-passes in the portal (defaultUncategorizedBySign) so canConfirm was already always-true there; this change adds the income gate, not a new hard block. 10 new unit tests (completeness.test.ts); full suite 3,488 green; Next build clean. SANDBOX ONLY — branch feat/mmllc-tax-financials, not on production.)_
_Prior: 2026-06-17 — Claude (MMLLC + Corp documents step — removed the "Financial Statements (optional)" upload from TAX_DOCUMENTS_BASE (Antonio, after the Dynamiq MMLLC test). It sat beside the required per-bank CSV repeater and confused testers: Luca dropped Relay's 12 monthly CSVs into this optional box instead of into Relay's own bank box, leaving Relay's required slot empty → the Submit button stayed silently disabled (canProceed=false) with no explanation, which read as "the button doesn't work". The P&L/Balance-Sheet engine ONLY ingests the per-bank `bank_accounts_\d+_statements_` files — this field had ZERO downstream readers (verified by grep across lib/ + handlers), so dropping it removes a P&L-corruption trap (files put here would silently never reach the engine). Shared by the MMLLC config + the Corp config (TAX_CORP_FIELDS). Real entity types in the data: Single Member LLC, Multi Member LLC, C-Corp Elected — NO S-Corp. In practice the removal affects the multi-member wizard; C-Corp Elected does not normalize to a Corp routing value today so it currently falls through to the SMLLC steps (separate latent routing gap, flagged not fixed). The per-bank `bank_accounts` repeater + prior-return matrix + disclaimer are UNCHANGED. NOT YET addressed (flagged to Antonio): the submit button gives no reason when disabled — a separate shared-wizard UX change, not done without his word.)_
_Prior: 2026-06-17 — Claude (SMLLC documents step — removed the "Bank Statements (optional)" upload (Antonio: a single-member LLC's numbers are entered in the Financial step, so it does not need bank statements). That field was never ingested anyway — tax-form-setup only ingests the MMLLC per-bank `bank_accounts_\d+_statements_` repeater; the SMLLC `bank_statements` field fed nothing, so it was pure confusion (it was the step on Damiano Mocellin/Orizzonti LLC's "Submission failed" screenshot). Then (same day, Antonio) also removed the "Financial Statements (optional)" upload for the same reason (redundant — numbers entered manually; never ingested). TAX_SMLLC_DOCUMENTS now = prior_year_return (optional, KEPT — last year's 5472/1120 starts this year's books) + disclaimer. (MMLLC TAX_DOCUMENTS_BASE keeps its own financial_statements + bank_accounts repeater — untouched.) Damiano's submit failure was the SAME transient lost/errored-response as Luca's MMLLC case (his data WAS received — submission 715f98d0, "we received your tax info" event fired), covered by the shared wizard-client onSubmit retry. NOTE: the member-info + banking forms are SEPARATE submit handlers and still lack that retry — Damiano hit "errore" on those too over weeks.)_
_Prior: 2026-06-15 — Claude (P&L EXPENSE SIGN FIX (F4), grounded in QuickBooks/standard accounting (contra-expense) + IRS 1065: computePnlTotals summed expenses/COGS by Math.abs, so a POSITIVE amount inside an expense category (a vendor refund/reversal) was counted as MORE spending instead of reducing it. Antonio's Uxio replay: 3 Aurora reversals (+2002/+3002/+4002 = +9,006) overstated expenses by 18,012 and understated net income by the same (showed 352,171.46; correct 370,183.46). Fix: expenses and COGS now use NEGATED SIGNED sums (-reduce(amount)) so returned money is a contra-expense that reduces the total, exactly as sales returns reduce net revenue. Shared math → fixes BOTH the on-screen financials and the downloaded Excel at once. 3 new pnl-totals tests (contra-expense, COGS refund); full suite 3,219 green. STILL OPEN this session: duplicate USD column when single-currency, balance-sheet 'FX Adjustment' plug → real M-2 equity (beginning capital + contributions + net income − distributions), and the downloaded Excel K-1 ownership reading stale account_contacts instead of the resolved roster.)_
_Prior: 2026-06-15 — Claude (COVERAGE ROUTE 404 FIX — Antonio: "in Year coverage, when I click nothing happens". Root cause was NOT the code: `.vercelignore` had a bare `coverage` pattern (meant for the test-coverage output dir) that ALSO matched the API route dir app/api/portal/tax-financials/coverage/, so `vercel deploy` stripped that route from the upload → POST /api/portal/tax-financials/coverage returned a routing 404 (HTML) → the client's answerCoverage() caught it silently → "nothing happens". Git tracks the file (no .gitignore entry) so it built fine LOCALLY (route table emitted it) — the local-vs-deployed mismatch was the tell. /answer worked because no ignore pattern matched it. Fix: anchored the pattern to `/coverage` (root-only) in .vercelignore. VERIFIED live on sandbox after redeploy: POST /coverage → 200 {recorded:true}, answer sticks (was 404 HTML). Probed siblings — only `coverage` collided; test-results/playwright-report have no source-dir collisions. NOTE for ship: this .vercelignore fix MUST be in the production promotion or the coverage feature 404s in prod too.)_
_Prior: 2026-06-13 — Claude (1000-ROW CAP FIX — P&L/BS/categorization were computed on only the FIRST 1000 transactions (supabase-js PostgREST default), silently wrong for any account with >1000 tx in a tax year. Found while setting up Antonio's recalc test: Uxio Test LLC had 1,992 rows, the financials view read 1,000; real clients (Dynamiq ~4,800) far worse. NEW lib/bank-transactions-fetch.ts: fetchAllPaged (pure, unit-tested pagination driver) + fetchAllBankTransactionsByYear (pages .range() in 1000s, always appends `id` as a unique tiebreaker so non-unique sort keys like transaction_date can't skip/dupe rows across pages). Wired into ALL unbounded reads in the financials path: getFinancialsView (financials-orchestration — id order, engine re-sorts), recategorizeAccountYear (categorization-engine — full-year transfer-pair matching was broken past row 1000), and generatePnlExcel + generatePnlCsv current+prior-year reads (pnl-generator — transaction_date order preserved so computeAccountBalances still picks the true year-end balance). VERIFIED on sandbox: helper reads 1,992/1,992; the recalc test fixture (3 seeded uncategorized QA rows, refs qa-recalc-1..3) now surfaces as a question group; full-dataset net income $352,171 vs the truncated $205,361 — the old figure was materially wrong. RECALC CONFIRMED: answering a question (answer route) reloads the page → getFinancialsView recomputes P&L/BS/gates on demand (nothing is stored frozen). 7 new helper unit tests.)_
_Prior: 2026-06-12 — Claude (WIZARD→FINANCIALS BRIDGE, found by Antonio's real 13-CSV submission (zero ingested, no path to the P&L screen): (1) tax-form-setup handlePortalWizardTaxSetup gains 'portal_csv_ingest' step — bank_accounts_*_statements_ paths ingested DIRECTLY from onboarding-uploads via ingestPortalCsv (bankLabel/accountKind from submitted_data per idx); the legacy Drive-scan stays as staff fallback (deterministic refs make re-scan harmless; the Drive path is also MOCKED in sandbox which is why nothing ingested there). (2) Tax MMLLC/Corp success screen now leads with 'We are preparing your P&L and Balance Sheet…' + primary button to /portal/tax-financials. (3) ingestPortalCsv now SURFACES per-row insert errors (a silent 0-insert hid the drift for a full submission) — all-rows-failed returns a loud error. (4) SANDBOX DRIFT FIXED, migration 20260612-2100 (SANDBOX-ONLY, prod already correct — DO NOT PROMOTE): sandbox's dedup index was a partial 2-col (account_id, transaction_ref) that no onConflict matched → every upsert errored; replaced with production's 4-col UNIQUE (account_id, transaction_ref, transaction_date, amount). REPLAY VERIFIED with Antonio's real files: 13/13 ingested, 1,989 transactions (Wise 1,319 + 12 Relay monthlies), 98.4% auto-categorized → 0 questions after AI passes; financials screen computes revenue 1,231,853.88 / net 205,361.70 and the gates correctly flag the test data's real inconsistencies (unverified we_filed claim; statement reconciliation). PHANTOM-MEMBER FIX (same day, Antonio's catch — declared Sofia/Donato 50/50 but Uxio appeared as a 100% member, total 200%): two injection paths closed. (a) resolveOwnership: when the wizard declares members, that list IS the roster — prior-K-1 / account_contacts people NOT on it become staff-review conflicts (possible mid-year exit / stale CRM), never members; without a wizard list all sources merge as before (staff context). (b) getFinancialsView: a draft reset reuses the submission row, so stale owner_* keys from the removed owner step survived next to the fresh member list and were unshifted as a phantom owner — owner_* now seeds the roster ONLY when there is NO member list (legacy submissions). Sync-back to account_contacts stays held while any conflict exists. Verified live on sandbox: Sofia 50/Donato 50, total 100, gate 5 passes, K-1 shares 102,680.85 each, Uxio flagged for staff review.)_
_Prior: 2026-06-12 — Claude (OWNER STEP REMOVED from TAX_MMLLC (Antonio's catch: 'Your Information' duplicated the member card — the filler IS a member). Now 5 steps starting at Members & Ownership ('including you'); email/phone never asked (portal login + contact record); owner_local_tax_number replaced by per-member member_foreign_tax_id (optional — feeds each member's W-8BEN, also on the accountant PDF). Downstream verified safe: wizard-submit clientName falls back to user metadata; tax-form-setup contactFieldMap skips absent owner_* keys (SMLLC/Corp keep TAX_OWNER_BASE and their CRM sync); extractWizardOwner returns null and the members list already includes the filler; savedStep is clamped (old 6-step drafts resume on the last step). Verified live: Step 1 of 5 = members with pre-filled card + foreign-tax-id field.)_
_Prior: 2026-06-12 — Claude (BANK CSV-EXPORT GUIDES, master plan §3.1/§8: NEW catalog 'bank_export_guides' (catalog_definitions + 8 entries, migration 20260612-1900, sandbox-applied) — per-bank step-by-step download instructions, metadata {match_terms[], steps_en[], steps_it[], note_en, note_it, source}; staff add/edit banks via the catalog, NO deploy. Wizard page.tsx loads active entries for tax wizards → bankGuides prop → wizard-client renders the matched guide (lowercase substring on match_terms, min 3 chars typed) in an emerald box under each bank card's fields, live as the client types; unknown bank → no box, never blocking (free text stays free). Content verified against official help centers 2026-06-12: Mercury (Transactions→filter→Export), Relay (12 monthly statement CSVs — select all, upload all here), Wise (per-currency statements, 365-day max), Revolut (custom range), Slash (CSV undocumented publicly but real client files exist — steps generic + 'ask support, it exists'), Airwallex (Balance Activity Report — the normal statement is PDF-only), Chase (Spreadsheet type; UNOFFICIAL ~1,000-row cap → quarter-by-quarter warning baked into the steps), PayPal (Activity download, 12-month max fits a year, async generation note). Verified live in browser: typing mercury/wise/chase shows each guide, 'Banco BPM' shows none. The bank_name field's hint ANNOUNCES the feature ('type the bank's name — the exact download steps will appear right below') so clients know to expect it — Antonio's catch: an unannounced feature is invisible.)_
_Prior: 2026-06-12 — Claude (§14 copy fixes after Antonio's review: us_office_warehouse now LEADS with the exclusion ("Beyond the company's US business address…" — every client has an RA/virtual-mailbox address through us and would otherwise answer Yes); comp_foreign_accounts rewritten with Antonio's bank-vs-exchange taxonomy: US banks (Mercury/Relay/Revolut/Slash) vs fintech EXCHANGE accounts used to receive EUR and convert (Wise/Airwallex) — the hint names the European-IBAN trap explicitly and says neither counts (Baker precedent: Dynamiq holds Wise, Schedule B Q8 filed No); question targets 'a real bank in another country' + Yes now opens TWO conditionals: comp_foreign_accounts_country (the return must name it) and comp_foreign_accounts_over_10k (FBAR/FinCEN-114 threshold — heavy penalties, staff handles). Both added to the accountant PDF. comp_digital_assets Yes now opens TWO conditionals too: comp_digital_assets_detail (required textarea — exchange + received-as-payment / sold-converted / only-held; each has different tax treatment) and comp_digital_assets_report (optional file — exchange yearly report; the bank-only P&L CANNOT see on-exchange activity, the known blind spot — Nexo's Kraken 1099-DA precedent). Both on the accountant PDF. CORRECTED per Antonio to MATCH THE IRS 1065 question: only-buy-and-hold = No (Schedule B Q30 asks receive-as-payment or sell/exchange/dispose only — verified on the Baker return); the question now states it explicitly and the hint leads with the 1099/1099-DA signal; the upload is titled '1099/1099-DA or yearly transaction report'. RESTRUCTURED per Antonio's never-pending rule: free-text detail replaced by comp_digital_assets_scenario SELECT (received_payment / sold_converted / both) + comp_digital_assets_exchange text + comp_digital_assets_1099 Y/N branch — Yes REQUIRES the 1099 PDF upload, No REQUIRES the yearly CSV export (same CSV-only rule as banks, server-guarded in both upload routes); 'we will request it later' language REMOVED everywhere — required docs block the step, autosave covers stop-and-find.)_
_Prior: 2026-06-12 — Claude (§14 MMLLC FORM REDESIGN, approved by Antonio after his QA — sources: Baker's Zoho 1065 intake + Baker's filed Dynamiq 2024 return (43 pages) + IRS i1065/i1065s23/i8804. THE BUG: the MMLLC tax wizard NEVER rendered members (members machinery keys on step id 'members'; the step didn't exist). NEW 6-step TAX_MMLLC: owner → members (NEW TAX_MEMBER_FIELDS per partner: type, passport names, citizenship + residence country, home address, ITIN status have/applied/none + conditional number, %, optional W-8BEN; pre-filled from CRM in wizard page.tsx — account_contacts ∪ contacts incl. itin_number/address — client CONFIRMS rather than types) → company (plain hints; business code NOT asked — proposed from activity, accountant confirms) → us_activity (5 factual Y/N replacing self-certified ECI: office/warehouse, people in US + conditional payroll, services in US, rental, FBA/3PL inventory) → compliance (explicit Y/N selects: FBAR-style foreign accounts w/ Wise-Revolut nuance, owns-other-companies, foreign trusts, digital assets, canceled debt, major assets, anything-else) → documents (unchanged: CSV repeater + prior-return matrix). DROPPED: prior_year_returns_filed/financial_statements_sent/mmllc_foreign_partners/mmllc_assets_over_50k + 13 unexplained legacy Y/N (computed or superseded; legacy keys still render in old submissions). MEMBER-REMOVE FIX: wizard-client members repeater now SHIFTS later members down (orphaned-index bug); extractWizardMembers treats member_count as authoritative. form-to-drive: members_list synthesized from flattened keys for the accountant PDF + new field labels. EXTRACTOR FIXED + fully E2E-validated on the real Baker 1065: relevantSections MAX 120k + always keeps the 20k TAIL (K-1s come LAST — partner 2's K-1 was past the old 60k cap), Schedule L two-amounts-per-line guidance, MAX_TOKENS 8192 → result: status VALIDATED, both K-1s (545,388+597,009=1,142,397 ✓), full Schedule L, zero issues.)_
_Prior: 2026-06-11 — Claude (Slice 9 — coverage questions + attest handoff + banner link, master plan §3.4/§3.7, the FINAL slice: NEW lib/tax/coverage.ts (pure) — coverageQuestions derives per-bank leading/trailing/internal month gaps ("Did X have any activity before March / after October / in the missing months?"); closes the truncated-tail hole gate 1 cannot see. Answers in NEW tax_return_submissions.financials_meta JSONB (migration 20260611-2300, sandbox-applied) via POST /api/portal/tax-financials/coverage ('no_activity' completes the dataset; 'had_activity' marks the export incomplete → client must replace the file). UNANSWERED or INCOMPLETE coverage BLOCKS the attestation (attest route 422 + UI button disabled). NEW lib/tax/attest-handoff.ts — fire-and-forget after attest: client-confirmed Excel archived to Drive 3.Tax/{year} (findTaxFolder + uploadBinaryToDrive; re-ingestion harmless via deterministic refs W3) + staff final-pass task (deduped, category Filing, plan A4 — staff review then tax_send_to_accountant). Upload route now also persists the raw CSV to storage (content-hash named) for the accountant package. TaxBanner gains showFinancialsLink (MMLLC/Corp with a submission) → /portal/tax-financials.)_
_Prior: 2026-06-11 — Claude (Slice 8 QA finding + fix — ATTESTATION RESET: browser QA on the live screen found the client could attest, then delete a file / answer a question / upload a new file, leaving a valid attestation over CHANGED numbers. Fix: lib/tax/attestation.ts::resetFinancialsAttestation — any data mutation after attest (answer route, statement delete route, ingestPortalCsv) flips confirmation_accepted back to false + writes a 'financials_attestation_reset' review_history entry; the client re-confirms after their change. GET view now returns `attested` (server truth) so the UI reflects it on load. VERIFIED LIVE in the browser: attest → reopen question → answer → confirmed banner replaced by the checkbox, DB shows confirmation_accepted=false + 1 reset entry. Full QA pass also verified: gates storyline (3 fails off-by-300 → all pass), question chips direction-filtered, answers write manual: notes, K-1 table, file delete, W5 review_status untouched. Known gap BY DESIGN: gate 1 does not catch a TRUNCATED TAIL (running balances stay coherent when the export simply ends early) — that is the coverage/activity-question layer's job (plan §3.4, Slice 9 scope).)_
_Prior: 2026-06-11 — Claude (Slice 8 portal review/confirm screen, master plan §3.5-3.7: NEW /portal/tax-financials page + components/portal/tax-financials-review.tsx — gates as checkmarks (pass ✓ / na — with reason / fail !), P&L + BS summary cards, per-member K-1 capital table, per-file cards with delete, PATTERN-GROUPED questions (NEW lib/tax/question-groups.ts: merchantRoot strips ••card suffixes/dates/digit-runs; one answer covers the whole merchant group; plain-language choices map to categories — 'Personal (owner) spending'→distribution/personal_draw NEVER expense), Excel download, attestation. NEW routes: POST /api/portal/tax-financials/answer (bulk update, only-uncategorized re-filter server-side, notes 'manual: client answer (…)' so engine re-runs never overwrite, post-confirm 409), POST /attest (client-role ONLY — staff excluded; HARD gate canConfirm; writes confirmation_accepted + client_ip/user_agent + review_history 'financials_attested' entry; review_status UNTOUCHED per W5), GET /download (generatePnlExcel passthrough, owner-only). Sandbox QA seed: Uxio Test LLC 2025, 8 txs refs qa-tf-1..8 sources upload:qa-tf-sha1/2 (gate 3 fails at +300 until the 2 question groups are answered, then ties at 7,750; ownership_pct=100 set on the account_contact).)_
_Prior: 2026-06-11 — Claude (Slice 7b portal ingestion + API, master plan §3.3/§6/§7/W3: NEW lib/tax/portal-csv-ingest.ts — ingestPortalCsv: parseBankStatement by content signature (client's bank label is fallback identity only, never routing) → legacy categorization + member detection → year filter (wrong-period file → guiding error naming the year) → analyzeDuplicates (identical file → alert, NO insert) → source-keyed upsert with source_file_id='upload:<sha256>' + ignoreDuplicates on the dedup index → deterministic recategorization in-request + AI-assist pass fired in background (instant client feedback). NEW routes (all OWNER-ONLY via lib/portal/owner-access.ts isAccountOwner — tax financials are non-delegable, teammates never see them; staff bypass): GET /api/portal/tax-financials (getFinancialsView + per-file source cards), POST /api/portal/tax-financials/upload (multipart, CSV-only + 20MB guard with guide-grade errors, maxDuration 60), DELETE /api/portal/tax-financials/statement (source-keyed cascade, 409 when post-confirm locked).)_
_Prior: 2026-06-11 — Claude (Slice 7a financials engine + six gates, master plan §4: NEW lib/tax/ownership-resolution.ts — resolveOwnership merges prior-K-1 %s > wizard member %s > account_contacts by normalized-name match, reports conflicts (>0.5% spread, resolved to highest precedence + staff flag) and missing %s (blocks K-1s); the OWNER's % is never inferred as the remainder. NEW lib/tax/financials-engine.ts — buildFinancialDraft (PURE): P&L via shared computePnlTotals, per-member capital roll-forward (beginning from validated prior K-1 item L, contributions/distributions attributed by counterparty name match, unattributed spread by % WITH a client-confirmation note), cash-basis BS v1 (assets=cash, liab=0), per-bank derived beginning balances (first balance_after − amount). NEW lib/tax/verification-gates.ts — evaluateGates: G1 statement reconciliation (NA when no balance column), G2 prior-ending==current-beginning ±$1 (NA first-year/never-filed/on-file, FAIL quarantined or mismatch), G3 A=L+C, G4 M-2 ties, G5 ΣK-1==net income + Σ%==100 (consumes ownership resolution), G6 uncategorized==0 (ONLY blocking gate — canConfirm). NEW lib/tax/statement-uploads.ts — analyzeDuplicates L1 SHA-256 identical file / L2 ref overlap %+months / L3 same-bank period overlap (guide-grade alerts), uploadSourceId('upload:<sha>') W3 namespace, deleteStatementRows source-keyed cascade refused when !isClientEditable (post-confirm lock). NEW lib/tax/financials-orchestration.ts — getFinancialsView assembles all of it from DB (draft computed on demand, never stored); W6 sync-back writes resolved %s to account_contacts ONLY when complete + conflict-free. 49 new tests incl. a full coherent-year scenario passing all six gates.)_
_Prior: 2026-06-11 — Claude (Slice 6 prior-return ingestion, master plan §5: NEW lib/tax/prior-return-extract.ts — extractPriorReturn(pdf) = pdf-parse → claude-sonnet-4-6 forced tool schema (form type/year/EIN, Schedule L beginning+ending incl. cash L1, M-2, every K-1) → validatePriorReturn identity checks (A=L+C ±$2 both columns, M-2 ties to L capital, ΣK-1==capital, Σ%≈100, year+EIN match) — ANY issue → status 'quarantined', never silently used; scans/unreadable PDFs → 'failed' for staff. E2E-verified on a real filed 1120 (Beril, pro-forma with no Schedule L → correctly quarantined). NEW lib/tax/prior-return-case.ts — processPriorReturnCase resolves the wizard matrix: A we_filed (verified vs tax_returns 'TR Filed'; client claim without a record → claim_mismatch flag), B filed_elsewhere (download upload → extract+validate → store), C first_year (cross-check accounts.formation_date; formed earlier → claim_mismatch), D never_filed (timestamped declaration — wording PENDING Antonio/lawyer — or 'Filing' staff task for back-filing quote when client wants cleanup). Stored in tax_return_submissions.prior_return_extracted (discriminated by case/status). Wizard: MMLLC docs step prior_return_case select + conditional PDF upload (PDF-only server guard in both upload routes) + declaration checkbox; Case A preselected by page prefill when 'TR Filed' exists at year-1 (submit route re-verifies regardless). Fire-and-forget from wizard-submit — never blocks submission.)_
_Prior: 2026-06-11 — Claude (Slice 5b AI-assist categorization: NEW lib/tax/ai-categorizer.ts — aiSuggestCategories() batches uncategorized txs (40/call, capped 80 batches) to claude-sonnet-4-6 with a forced suggest_categories tool schema; suggestions validated hard (ids must come from the batch, category from the enum, never 'uncategorized'); fail-open — any API/key failure categorizes nothing. recategorizeAccountYear() pass 3 (opts.aiAssist, ON in bank_statement_process): applies ONLY high-confidence suggestions to rows still uncategorized after rules+transfer passes, tagged notes='ai:high'. Precedence manual > rules > AI; a re-run never downgrades an ai-tagged row to uncategorized; a rule override clears the ai tag. bank_statement_recategorize tool now writes notes 'manual: …' so engine re-runs can never clobber staff corrections (5a gap found in 5b). Migration 20260611-1800: +2 PDF-vocabulary seed rules ('Intl. Transaction Fee'→fee, 'Corporate Card -'→expense) — benchmark on Dynamiq's live 4,807-tx set showed PDF-extracted descriptions share NO vocabulary with the CSV seeds. Benchmark harness: scripts/benchmark-ai-categorization.ts (pattern-level, weighted = per-tx coverage).)_
_Prior: 2026-06-11 — Claude (Slice 5a categorization engine: NEW lib/tax/categorization-engine.ts — DB-backed rules from bank_categorization_rules (per-client > global > legacy built-ins, first match wins; direction gating in/out/any so 'STRIPE' only categorizes inflows as revenue; bad regex in a rule never breaks ingestion; rows whose notes start 'manual:' are never overwritten). NEW lib/tax/transfer-matcher.ts — pairs equal-amount opposite-sign legs across DIFFERENT banks/sub-accounts within 5 days → both become conversion/internal_transfer, excluded from P&L (the Dynamiq inflation case). recategorizeAccountYear(account, year) = rules pass + transfer pass, idempotent, wired into bank_statement_process post-ingest. Migration 20260611-1700: rules.direction column + 8 conservative seed rules from VERIFIED bank vocabulary only (Slash types, Mercury category, Stripe payouts) — sandbox only until ship.)_
_Prior: 2026-06-11 — Claude (Slice 2 samples + parsers: REAL client CSV formats secured for Mercury, Slash, Revolut (+ the Wise TRANSFERS export variant — detected and routed to AI; double-count warning if a client uploads both Wise variants). New deterministic parsers in lib/bank-csv-parsers.ts: parseMercuryCSV (MM-DD-YYYY, Status=Sent only, Mercury Category carried in description, NO balance column), parseRevolutCSV (books Total amount incl. fee — what the Balance reflects; State=COMPLETED only; real ID column as ref), parseSlashCSV (year-less 'Dec 30' dates anchored by taxYear hint or embedded MM.DD.YY fee dates, blank-timestamp carry-forward, descending year-boundary decrement). parseBankStatement gains taxYear option + mercury_csv/revolut_csv/slash_csv methods. Samples archived in Drive '_Engineering — CSV samples (internal)'. Chase CSV still sample-less → AI fallback.)_
_Prior: 2026-06-11 — Claude (Slice 4 CSV parsers: NEW lib/bank-csv-parsers.ts — content-signature detection (header row, NEVER filename/label), deterministic Relay parser (real-export columns, M/D/YYYY US dates, SETTLED-only, content-hash refs incl. running balance), CSV dialect sniffing (Italian-Excel re-save: semicolons + comma decimals), stableRowRef/dedupeRefs. parseBankStatement routes relay_csv/wise_csv by signature → unknown layouts to AI. DETERMINISTIC REFS EVERYWHERE (W2/W3): Wise blank-reference rows get content-hash refs; AI extractor refs changed from order-dependent ai-<date>-<index> to content hashes with stable duplicate suffixes — same row now collides harmlessly on the dedup index across any run/path/re-ingestion.)_
_Prior: 2026-06-11 — Claude (Slice 3 schema, migration 20260611-1400: tax_return_submissions.prior_return_extracted JSONB (validated prior-return extraction storage); NEW table bank_categorization_rules (DB-driven global + per-client rules — engine reads it from slice 5); bank_transactions.transaction_ref now NOT NULL + non-blank CHECK (refs are the dedup identity). Applied to SANDBOX only; prod promotion with the slice ship.)_
_Prior: 2026-06-11 — Claude (P&L math fixes F1/F3 + F2 visibility, master plan §4: refunds now SIGNED in computePnlTotals (a refund received reduces expenses); NEW category `contribution` (owner top-ups — "Top Up via" rule — are equity, never revenue; feeds future capital roll-forward); uncategorized count+net now printed in red on the P&L sheet so the Excel can't pretend to be complete. `computePnlTotals` exported as the SINGLE source of P&L math — bank-statements.ts tool + generatePnlCsv now call it instead of duplicating formulas. Plan of record: sysdoc tax-financials-self-service-master-plan.)_
_Last verified against code: 2026-06-14 — Claude (Portal tax banner is now flow-stage-aware: new pure `lib/tax/flow-banner.ts::flowStageBannerState` maps a flow-workspace SD stage → a client banner state (complete_form / under_review / preparing / revision_requested / sign / signed / filed / completed; unit-tested). `components/portal/tax-banner.tsx` consults it ONLY after `review_status` (so production review_status clients are unaffected) and before the legacy fallback — fixes flow Tax Returns that have no `review_status` but a real SD stage (e.g. "Sent for Signature") wrongly showing the legacy "Edit submission" banner. "Sent for Signature"/"Tax Return Prepared" → "Sign your tax return" → /portal/sign. Full writeup in `portal.md`.)_
_Last verified against code: 2026-06-25 — Claude (unrelated to tax logic: `lib/installment-handler.ts onFirstInstallmentPaid` now ALSO pays a managed partner their recurring **renewal share** once per year — gated to years after formation, idempotent per (account, year). Tax-return SD creation in this handler is unchanged. Full design in `partners-team.md`. SANDBOX, branch `feat/partner-deal-config`.)_
_Last verified against code: 2026-06-22 — Claude (Tax wizard submit is now fire-and-forget: `app/api/portal/wizard-submit/route.ts` step 6 changed from blocking `await handleTaxFormSetup(...)` to an inline IIFE without await. The response is returned immediately after the IIFE is kicked off. The handler runs in the background; the client gets a portal message when it completes. Cron `/api/cron/process-jobs` remains the safety net if the background task fails. Root cause of the change: Borisz / Zhang Holding LLC 2026-06-19 — connection drop during the 10–30s handler blocked the confirmation screen even though data was already safely saved.)_
_Prior: 2026-06-17 — Claude (SMLLC documents step — removed the "Bank Statements (optional)" + "Financial Statements (optional)" uploads from TAX_SMLLC_DOCUMENTS (Antonio). A single-member LLC enters its figures as numbers in the "Financial Information" step; both uploads were optional, never ingested by any code (verified zero readers in lib/ + app/api/portal/), and only caused confusion. Prior-year return (optional) + disclaimer are KEPT. SURGICAL production ship off main — single-member only; the multi-member/Corp tax-financials redesign + P&L page are NOT part of this change and remain unshipped on the tax branch.)_
_Prior: 2026-06-14 — Claude (Portal tax banner is now flow-stage-aware: new pure `lib/tax/flow-banner.ts::flowStageBannerState` maps a flow-workspace SD stage → a client banner state (complete_form / under_review / preparing / revision_requested / sign / signed / filed / completed; unit-tested). `components/portal/tax-banner.tsx` consults it ONLY after `review_status` (so production review_status clients are unaffected) and before the legacy fallback — fixes flow Tax Returns that have no `review_status` but a real SD stage (e.g. "Sent for Signature") wrongly showing the legacy "Edit submission" banner. "Sent for Signature"/"Tax Return Prepared" → "Sign your tax return" → /portal/sign. Full writeup in `portal.md`.)_
_Prior: 2026-06-12 — Claude (WinAnsi PDF encoding: `sanitizeForPdf()` added to `generateFormSummaryPDF` in `lib/form-to-drive.ts`; unit tests in `tests/unit/sanitize-for-pdf.test.ts`. Triggered by Giacomo Costa (Maltese ħ) replay of submission `6a5503ac-80dc-4893-b5d2-1d1426f61f43`. See Gotchas.)_
_Prior: 2026-06-11 — Claude (SMLLC tax intake wizard clarity + Form 5472 compliance: beneficial-owner fields removed, owner address clarified as the personal residence, related-party transactions gated + REQUIRED with categorized 5472 data, always-visible field hints, server-side `validateTaxData` backstop. See the "SMLLC tax intake wizard" section. PRs #106/#107.)_
_Prior: 2026-06-10 — Claude (AI bank-statement extraction: `parseBankStatement` now falls back to Claude-based extraction (`lib/bank-statement-ai-extract.ts`) for any non-Wise bank or any 0-row parse — PDF read natively, CSV as text, forced structured output. Reconciliation guard: opening + Σamounts ≈ closing or the result is flagged `reconciliation.reconciled=false` for human review (multi-currency/truncation → null). `.zip` archives of monthly statements unpacked via fflate and merged. `bank_statement_process` + tax-form-setup step 7 now scan the `3.Tax/{year}` subfolder and accept `.zip`; pattern includes chase. Kill-switch env: `BANK_STATEMENT_AI_DISABLED=true`. Requires `ANTHROPIC_API_KEY`. Validated 5/5 vs real client statements 2026-06-10 — incl. catching that Nexo's "bank statement" is actually a Kraken 1099-DA.)_
_Prior: 2026-06-10 — Claude (Slice 6: staff Tax Board is now a catalog-driven Kanban at `/tax-returns` — columns from `pipeline_stages.board_visible`, SD-anchored cards, review_status overlay shared with the Slice 5 tracker via `lib/tax/tax-stage-overlay.ts`, off-catalog stages → "Other" column (no silent drops), days-in-stage traffic lights from `stage_entered_at`/`stale_days`. Read-only; drag-drop/bulk/assignees = Slice 7. New: `lib/tax/tax-board.ts` + `board-data.ts`, `components/tax-returns/tax-kanban-board.tsx` + `tax-kanban-card.tsx`. See "Staff Tax Board" section.)_
_Prior: 2026-06-10 — Claude (Bug fix: portal tax submissions copied 0 attachments to Drive + produced no P&L/Balance Sheet — root cause was a storage-bucket mismatch (portal uploads to `onboarding-uploads`, but `saveFormToDrive("tax_return")` defaulted to the external form's `tax-form-uploads`). Fix: added `opts.bucket` override to `saveFormToDrive`; portal handler passes `onboarding-uploads`, `tax_form_review` infers bucket from path prefix. See new gotchas. Affected: MPG Performance / Dynamiq SR / Nexo Agency 2025.)_
_Prior: 2026-06-10 — Claude (Slice 5: client progress tracker on portal home — `lib/tax/progress-tracker.ts` + `components/portal/tax-progress-tracker.tsx`; 13 catalog-driven steps from pipeline_stages client_label/client_label_it (new column, migration `20260610-1900-tax-tracker-client-label-it.sql`); position = SD stage with review_status overlay so the dot matches the banner during the review loop; hidden pre-1st-installment / terminated / unknown stage names.)_
_Prior: 2026-06-10 — Claude (Phase 2 complete: dropped india_status/sent_to_india/sent_to_india_date/india_follow_up_count columns + india_status enum from tax_returns; database.types.ts updated manually with accountant_* cols + review_status/review_history on tax_return_submissions; augmented type bridge removed; observability trigger + seed script updated to sent_to_accountant.)_
_Last verified against code: 2026-06-10 — Claude (SD query bug fix in applyConfirmedTaxSubmission: added .lt("stage_order", 50) + order(stage_order desc) so a client with two active Tax Return SDs always advances the correct one. portal_messages null sender_id bug fixed.)_
_Last verified against code: 2026-06-09 — Claude (Slices 1–4: tax submission review workflow — `review_status` sub-state on `tax_return_submissions`, 6 new pipeline stages (Data Submitted 45, Under Review 46, Revision Requested 47, Approved 48, Confirmed 49, 2nd Installment Paid 35), gap-renumbered existing stages ×10, SD stays at "Data Submitted" through the whole review loop, only `confirmed` releases forward. Pure state machine in `lib/tax/review-status.ts`. Staff actions: `POST /api/crm/tax-review/action`. Client confirm: `POST /api/portal/tax-confirm` → `applyConfirmedTaxSubmission`. Portal banner (`components/portal/tax-banner.tsx`) now has 7 states driven by `review_status`; `getPortalTaxReturns` now joins the latest submission per account. Migrations: `20260609-2015-tax-review-slice1-columns.sql` + `20260609-2300-tax-pipeline-renumber-review-stages.sql`.)_
_Slice 0: 2026-06-09 — Claude ("Sent to India → Sent to Accountant" rename, Phase 1: new `sent_to_accountant` / `accountant_status` / `sent_to_accountant_date` / `accountant_follow_up_count` columns + `accountant_status` enum + `Sent to Accountant` status label added ALONGSIDE the legacy `india_*` columns and `Sent to India` label; data copied; all code cut over to the new names. Legacy columns NOT dropped yet — Phase 2. Migration split in two files because of Postgres SQLSTATE 55P04. Type bridge: `lib/database.types.augmented.ts`.)_

## What it is
Tracking and filing clients' US tax returns through tax season: collecting their data, sending the package to the accountant, tracking the long status pipeline, handling extensions, and pausing/resuming work tied to installment payments. Return types: **1065** (MMLLC / partnership), **1120-S** (S-corp), **1040NR** (individual non-resident). _(The accountant team was historically referred to in code/UI as "India"; renamed to "Accountant" 2026-06-09 — see header note.)_

## SMLLC tax intake wizard — clarity + Form 5472 compliance (shipped 2026-06-11, PRs #106/#107)

The portal `tax` wizard (entity SMLLC) is how a foreign-owned single-member LLC submits the data for its pro-forma **1120 + Form 5472**. Config: `TAX_FIELDS` in `components/portal/wizard/wizard-configs.ts`; rendered by `app/portal/wizard/wizard-client.tsx` via the shared `WizardField`.

**The problem (before).**
- It asked **beneficial-owner questions** (100%-direct owner, ultimate-owner name/address/country/tax-id) that make no sense for a single member — **8 clients entered *themselves* as the "ultimate owner".**
- The **owner address** didn't say it was the *personal* residence — clients entered their citizenship country, or the company address.
- **Related-party transactions were an OPTIONAL free-text repeater** — a client could skip them entirely, and the foreign party's tax ID + the transaction *type* weren't captured. Result: 5472 reportable-transaction data frequently missing or uncategorized. **An incomplete Form 5472 is treated by the IRS as a failure to file → a $25,000 penalty** (per related party, +$25k/30 days after notice) — and this applies even though SMLLC clients owe **no US income tax** (5472 is an information return; "I don't pay US tax" does NOT remove the penalty).
- Per-field help was hidden behind a click-to-reveal **"i" tooltip clients never clicked.**

**What the system does now (after).**
- **No beneficial-owner fields** on the SMLLC owner step. SMLLC company fields are isolated in their own `TAX_SMLLC_COMPANY` array, so the MMLLC/Corp tax forms are untouched.
- **Owner address is explicitly the personal residence** — "Personal Home Address — where you physically live", with the "Italian citizen living in Dubai/Portugal → enter the Dubai/Portugal address" example, and the country field clarifies residence ≠ citizenship.
- **Related-party transactions are gated and required.** A required Yes/No question (transactions with any company you/your family own, or that owns your LLC). **"No"** shows an amber **$25,000-penalty confirmation**; **"Yes"** REQUIRES ≥1 fully-completed transaction. Each transaction captures: party name, **full address**, country, **required foreign Tax ID/VAT** (5472 Part III — "None" allowed only if the party genuinely has none), **amount**, **direction** (money in vs out), **transaction type** (a dropdown mapped to the 5472 **Part IV** categories: sale of goods, services, rent, royalties, interest, loan, capital, management fee, reimbursement, other), and a **required description**.
- **US business activities** is now a Yes/No gate + a conditional detail box (was a vague free-text field).
- **Field help is always visible** under every field (the "i" tooltip was removed — a shared `WizardField` change, so it applies to **all** portal wizards).
- The **accountant Drive package** (`lib/form-to-drive.ts`) surfaces all new/renamed keys and prints direction/type as readable labels (not raw codes like `to_llc`).

**Two enforcement layers.** Client: `validateStep` (`wizard-client.tsx`) blocks Next/Submit until a "Yes" has ≥1 complete transaction. Server backstop: `validateTaxData` (`lib/jobs/validation.ts`, wired into `validateWizardData` for `tax`/`tax_return`) re-checks the same at the submit endpoint — **narrow + data-driven**: it fires ONLY on an explicit "Yes", so it can never 400 a "No" answer or an MMLLC/Corp submission. The team still reviews every submission before filing (the wizard never auto-files to the IRS), so categorization is double-covered. IRS source: Form 5472 instructions (Rev. Dec 2024).

**Verify:** open `/portal/wizard?type=tax` as an SMLLC client (account `entity_type` must be single-member — "Single Member LLC") → Financial step → toggling the related-party Yes/No shows the warning on "No" and forces ≥1 complete transaction on "Yes". Unit tests: the `validateTaxData` block in `tests/unit/validation.test.ts`.

## The status pipeline
A tax return moves through a status on `tax_returns.status`. The **current** flow (installment + wizard model) is:
`Payment Pending` → `1st Installment Paid` → `Wizard Available` → `Data Received` → `Sent to Accountant` → `TR Completed - Awaiting Signature` → `TR Filed`, with `Extension Requested` / `Extension Filed` as the extension branch and `Not Invoiced` for un-billed returns. _(`Sent to Accountant` was `Sent to India` until the 2026-06-09 rename; the old label still exists in the `tax_return_status` enum and is kept mapped defensively in code, but the current flow only produces `Sent to Accountant`.)_

> **Legacy statuses — still in the enum, NOT produced by the current flow:** `Activated - Need Link` and `Link Sent - Awaiting Data` are vestiges of the old *"email the client a data link, then wait for their data"* model that predates the installment + wizard redesign. The data link was replaced by the portal wizard, and the old "2nd Installment Paid" stage was renamed "Wizard Available". No tax return currently carries either status, and the pipeline stages that once produced them (`Payment Verified`, `Data Link Sent`) no longer exist. They remain valid enum values — an admin could still set one manually — so they are kept mapped defensively: `lib/operations/tax-return-sd-bridge.ts` aliases both to live SD stages, and the portal pause-banner keeps them in its pre-data-receipt set so a manually-set legacy row still pauses correctly during a tax-season pause. **Do not treat them as live pipeline steps.**

Separately, boolean **workflow-progress flags** track milestones: `paid`, `link_sent`, `data_received`, `sent_to_accountant`, `extension`, `accountant_status`. _(Renamed from `sent_to_india` / `india_status` on 2026-06-09; the legacy columns still exist in the DB until Phase 2 but code no longer reads/writes them.)_

## Tax submission review workflow (Slices 1–3, shipped 2026-06-09)

The client portal wizard submit no longer auto-completes the return. Instead it parks the submission in a **staff review loop** tracked on `tax_return_submissions.review_status`:

```
null → submitted → under_review → approved → confirmed → (releases)
                         ↓                      ↓
                 revision_requested          reopened → submitted
                         ↓
                    resubmitted → under_review
```

- **Pure state machine:** `lib/tax/review-status.ts` — `canTransition`, `isClientEditable`, `advancesServiceDelivery`, `buildReviewHistoryEntry`. Only `confirmed` sets `advancesServiceDelivery=true`.
- **SD stays at "Data Submitted" (stage 45)** through the whole review loop. Only the client's `Confirm` advances the SD to "Data Received" (50) via `lib/tax/apply-confirmed-submission.ts`.
- **`review_history`** (JSONB) on `tax_return_submissions` keeps an immutable log: `{from, to, at, by, actor}` per transition.
- **Staff actions:** `POST /api/crm/tax-review/action` — body `{submission_id, action, note?}`. Valid actions: `start_review`, `approve`, `request_changes` (note required), `reopen`. Each resolves the open What's New card so a re-submission raises a fresh one.
- **Client confirm:** `POST /api/portal/tax-confirm` — only allowed when `review_status=approved`. On success: appends history entry, calls `applyConfirmedTaxSubmission`.
- **Portal banner:** `components/portal/tax-banner.tsx` has 7 client-facing states keyed by `review_status` (null=amber action-required, submitted/resubmitted=blue+Edit, under_review=blue locked, revision_requested=amber+Edit, approved=green+Confirm+Edit, confirmed=blue locked, reopened=amber+Edit). Legacy path for pre-Slice-2 submissions uses `dataReceived`/`sentToAccountant` boolean fallback.
- **New pipeline stages** (migration `20260609-2300-tax-pipeline-renumber-review-stages.sql`):
  - Existing stages ×10 (stage_order -10…90 with gaps)
  - New: `2nd Installment Paid (35)`, `Data Submitted (45)`, `Under Review (46)`, `Revision Requested (47)`, `Approved (48)`, `Confirmed (49)`
- **`getPortalTaxReturns`** (`lib/portal/queries.ts`) now joins the latest submission per account and returns `review_status` + `submission_id` alongside the tax return row.
- **Slice 4 (2026-06-09):** What's New board cards (`components/dashboard/action-board.tsx`) with `source_ref = "tax_submission:<id>"` now render contextual action buttons via `TaxReviewActions` sub-component. Buttons shown by status: `submitted`/`resubmitted` → "Start Review"; `under_review` → "Approve" + "Request Changes" (inline note textarea); `confirmed` → "Reopen". Each button POSTs to `POST /api/crm/tax-review/action`. `GET /api/crm/tax-review/action?submission_id=...` added to fetch current `review_status` (staff-only). After terminal actions (approve/request_changes/reopen) the card is auto-resolved by the POST handler and drops off the board; after `start_review` the card stays and buttons refresh to `under_review` state.

## Staff Tax Board — Kanban (Slice 6, 2026-06-10)
The internal `/tax-returns` dashboard is a **catalog-driven Kanban** (replaced the old static status-grouped section list). One column per `pipeline_stages` row where `board_visible=true` (service_type `Tax Return`), in `stage_order`.
- **Cards are SD-anchored, not tax_returns-anchored.** Each card = one in-flight Tax Return service delivery (`status NOT IN (completed,cancelled)`), joined to its account (company, entity type), its `tax_returns` row matched by `company_name` + `tax_year` (no FK — same imperfect match as `getPortalTaxReturns`), and its latest `tax_return_submissions.review_status`.
- **Same effective-stage axis as the client tracker.** A card's column = the SD stage overlaid with `review_status` via the SHARED `lib/tax/tax-stage-overlay.ts` (`REVIEW_STATUS_STAGE` + `overlayEffectiveStageName`) — so a submission `under_review`/`approved` shows in that review column even though the SD parks at "Data Submitted". The overlay takes the FURTHER of (SD stage, review stage); a stale review_status can never drag an advanced SD back. This module is the single source of truth shared with the Slice 5 progress tracker.
- **No silent drops.** Cards whose effective stage is NOT a board column (legacy/renamed stages like `Filed` vs catalog `TR Filed`, generic `In Progress`, intake stages) land in a synthetic **"Other / Needs Attention"** column with a visible count. Verified in sandbox: 227 active SDs → 227 cards, 2 in Other.
- **Days-in-stage** from `service_deliveries.stage_entered_at`; traffic-light (`stalenessLevel`) keyed off each column's `pipeline_stages.stale_days` (≥ threshold = red, ≥ half = amber, else/no-rule = green). Pure logic + grouping in `lib/tax/tax-board.ts` (`buildBoardColumns`/`daysInStage`/`stalenessLevel`, 13 unit tests); I/O fetch in `lib/tax/board-data.ts`; UI in `components/tax-returns/tax-kanban-board.tsx` + `tax-kanban-card.tsx`.
- **Slice 7a — card detail + assignee** (shipped 2026-06-10): clicking a card opens a detail drawer (`components/tax-returns/tax-card-detail.tsx`) with all facts + an assignee picker (`assignTaxBoardCard` server action in `app/(dashboard)/tax-returns/board-actions.ts`, audited, no client notify). Assignee list `TAX_BOARD_ASSIGNEES` lives in `lib/tax/tax-board.ts` (pure module — a `'use server'` file may only export async functions).
- **Slice 7b — drag-to-advance** (2026-06-10): drag a card to a new stage → `advanceTaxBoardCard` → `advanceServiceDelivery({skip_notify:true})`. **SILENT** (Antonio 2026-06-10): staff moves never push the client; the progress tracker reflects the new stage on next load (all tax stages already have `notify_client_email=false` + no auto_tasks). **Two-path boundary (a correctness rule, NOT a shortcut):** only REAL SD-stage columns are drop targets and their cards draggable; the five review sub-state columns (Data Submitted/Under Review/Revision Requested/Approved/Confirmed) and "Other" are NOT — review transitions stay on the Slice-4 What's New buttons where the state machine (note required for request_changes, client-only Confirmed) is enforced. Draggability keys off the card's CURRENT effective column, not raw `review_status`, so a post-review card at "Data Received" stays draggable. Pure legality (`resolveDrop`/`isDroppableColumn`/`isReviewSubstateColumn`, `lib/tax/tax-board.ts`) is unit-tested AND re-validated server-side in `advanceTaxBoardCard` (never trusts the drag). Backward drags allowed (staff correction). Browser-verified in sandbox (legal advance silent, illegal drop rejected, backward works).
- **Slice 7c — bulk actions** (2026-06-10): a "Select" toggle enters selection mode; only real-stage (draggable) cards get a checkbox (review/Other cards are not bulk-selectable). The bulk bar offers **bulk assign** (`bulkAssignTaxBoardCards`, one UPDATE, no notify) and **bulk advance** (`bulkAdvanceTaxBoardCards`) to a target chosen from a Move-to picker that lists ONLY real stages. Bulk advance is gated by a **confirm dialog** showing eligible-vs-skipped counts + "clients not notified". Each card is re-validated server-side with the same per-card legality as a single drag (`summarizeBulkAdvance` partitions eligible/skipped via `resolveDrop`); ineligible cards are SKIPPED and reported, never force-moved — the action returns a per-card breakdown (succeeded/skipped/failed), no silent partial success. All advances SILENT (skip_notify). `MAX_BULK=200` guard. Browser-verified in sandbox (bulk assign 2 cards; bulk advance 2 cards Wizard Available→Data Received with zero client notifications). 25 board unit tests.
- **Slice 8 — deadline-pressure badge + filter** (2026-06-10): each card shows a deadline badge (`Upcoming` amber ≤30d, `Due soon` red ≤14d, `Overdue` solid-red past) from `deadlinePressure(effectiveTaxDeadline(card), now)` (pure, in `lib/tax/tax-board.ts`, unit-tested). The effective deadline is the EXTENDED date once an extension is filed (`resolveExtensionDeadline`, Sep 15 / Oct 15), else the return's `deadline` or the statutory date (MMLLC/partnership Mar 15, others Apr 15 — same dates the `deadline-reminders` cron uses). A "Deadline pressure" filter chip narrows to badged cards. **Scope note:** the rest of Slice 8 was found ALREADY BUILT and was NOT rebuilt — payment-aware auto-advance lives in `onFirstInstallmentPaid`/`onSecondInstallmentPaid` (`lib/installment-handler.ts`, data-driven via `resolveSecondInstallmentAdvance`, fired by the bank-feed matcher + crons); the `deadline-reminders` cron already emits 30-day reminders; client→board sync is automatic (the board reads SD stage + review_status live). The spec's "board action → What's New note" was deferred as low-value (a staff board move is already the action; emitting a What's New card for it is redundant noise).
- **Year:** the board shows `currentYear - 1` (filing season files the prior year's returns). The old `components/tax-returns/tax-board.tsx` (+ `tax-card.tsx`, `edit-tax-dialog.tsx`, `actions.ts`) are no longer mounted by the page but kept for their server actions until Slice 7 reuses or replaces them.

## The accountant hand-off
- `tax_send_to_accountant` (`lib/mcp/tools/tax.ts`) gathers the document package from Drive `3.Tax/{year}/` — **Tax Organizer PDF, P&L Excel (MMLLC/Corp), prior-year return, bank statements** — and sends one email with all attachments to the accountant (default `tax@adasglobus.com`), then updates the return's status (`status → "Sent to Accountant"`, `accountant_status → "Sent - Pending"`).
- **Always run `dry_run=true` first**, review the package, get Antonio's approval, then run with `dry_run=false`.
- **Idempotent**: skips if already sent (stamps `sent_to_accountant_date`) unless `force_resend=true`.

## Tax pause + installment reactivation
- During tax season, Tax Return service deliveries can be **parked `on_hold`**. The global master switch is `app_settings.tax_season_paused`.
- `reactivateOnHoldTaxReturns()` (`lib/tax/reactivation.ts`) flips an individual `on_hold` Tax Return SD back to `active` **once the client's 2nd installment is paid** — run synchronously from `onSecondInstallmentPaid` (`lib/installment-handler.ts`) and as a daily safety-net cron (`/api/cron/tax-reactivation`).

## 2nd-installment → wizard advance (Phase 1 "Card = Truth", 2026-06-08)
The card now advances to the wizard stage on a **real, bank-confirmed** 2nd-installment payment — fixing the bug where wire-paying clients got stranded pre-wizard (the matcher marked the invoice Paid but nothing fired the stage advance).
- **Trigger:** `matchAndReconcile` (`lib/bank-feed-matcher.ts`) fires `onInstallmentPaid` (`lib/operations/payment.ts`) when it confirms an **installment** invoice Paid — classified via `payment_category` (`isFirstInstallment`/`isSecondInstallment`, `lib/billing/payment-classification.ts`), guarded to `account_type='Client'`, fire-and-forget so a handler error never rolls back the match. (The June `annual-installments` cron + manual CRM mark-paid remain the other entry points.)
- **Advance rule is DATA-DRIVEN — no hardcoded stage names.** `onSecondInstallmentPaid` calls `resolveSecondInstallmentAdvance('Tax Return')` (`lib/services/stages.ts`): the **target** stage is the one flagged `auto_actions: [{ "type": "second_installment_target" }]`, the **source** stages are every stage at `stage_order >= 1` below it (bundle stages — EXCLUDES the negative/zero standalone-intake stages "Company Data Pending"/"Paid - Awaiting Data"). Editable in **/config** (stage edit dialog → "2nd-installment wizard target" checkbox). Fails safe (skips) if no stage is flagged. Marker helpers: `lib/services/stage-actions.ts`.
- **Idempotent:** the handler's lease task + "[READY] Send tax return to Accountant" task now dedup by title, so a second run (matcher + cron/manual) never duplicates them. _(Renamed from "...to India" 2026-06-09; the dedup lookup matches BOTH the new and the legacy title so a task created before the rename is not duplicated during the transition.)_
- **`data_received` always stamps a date.** The dashboard toggle (`app/(dashboard)/tax-returns/actions.ts`) now sets/clears `data_received_date` with the flag. A dateless `data_received=true` is the legacy bug that BLOCKS a client from submitting (`app/api/portal/wizard-submit/route.ts` requires `data_received=false`) and hides their banner — never create one. A backfill cleared the historical dateless flags (`scripts/backfill-tax-data-received-2025.sql`).
- **Silent bulk advances:** `advanceServiceDelivery` (`lib/service-delivery.ts`) accepts `skip_notify` to suppress the portal notification + push for reconciliation/backfills so correcting many cards at once does not spam clients.

## Business rules
- **Send-to-accountant is dry-run-first + approval-gated** (built into the tool's contract) — never blast documents without reviewing the package.
- **Tax pause is gated on the 2nd installment** — a client's tax work resumes when they've paid installment 2, OR when the global pause is lifted.

## How it's built
### Key files
- `lib/mcp/tools/tax.ts` — the bulk: `tax_search`, `tax_tracker`, `tax_update`, `tax_form_create/get/review`, `tax_send_to_accountant`, `tax_extension_list/update`, `tax_return(s)`, `tax_year`.
- `lib/mcp/tools/tax-quote.ts` — `tax_quote_create`, `tax_quote_submissions`.
- `lib/tax/reactivation.ts` — `reactivateOnHoldTaxReturns()` (installment-gated resume).
- `lib/tax/extension-deadline.ts` — `resolveExtensionDeadline()`, `formatDeadlineForDisplay()`.
- `lib/installment-handler.ts` (`onSecondInstallmentPaid`), `app/api/cron/tax-reactivation`.
- Client form: `app/tax-form`. Docs: Google Drive `3.Tax/{year}/`.

### Tables
`tax_returns` (`status`, `deadline`, `return_type`, `tax_year`, `accountant_status`, `sent_to_accountant`, `sent_to_accountant_date`, `accountant_follow_up_count`, extension fields, the workflow-progress booleans), `service_deliveries` (Tax Return SDs, `active`/`on_hold`), tax form submissions, `app_settings` (`tax_season_paused`).

## Gotchas, invariants & past bugs
- **The accountant-workflow fields are `sent_to_accountant` / `accountant_status` / `sent_to_accountant_date` / `accountant_follow_up_count`** (renamed from `india_*` on 2026-06-09, legacy columns dropped Phase 2 2026-06-10). Don't read them as a literal country status. The legacy `Sent to India` enum label still exists in the `tax_return_status` DB enum for rollback safety and is mapped defensively in `tax-return-sd-bridge.ts`, `enum-normalization.ts`, the portal queries exclusion list, and `tax.ts` `isInProgress`.
- **Never flip `app_settings.tax_season_paused` to reactivate ONE client** — that reactivates ALL on-hold returns. Use the per-SD installment reactivation (`reactivateOnHoldTaxReturns`) for a single client whose 2nd installment landed while the global pause is still on.
- **`tax_send_to_accountant` is idempotent + dry-run-first** — check the package before sending; re-send needs `force_resend=true`.
- **Status (a long string) and the workflow-progress booleans are separate** — update the right one; don't infer one from the other.
- **Prerequisites for the accountant send**: the tax form must be completed and the documents must already be in the Drive `3.Tax/{year}/` folder, or the package will be incomplete.
- **Upload bucket is path-dependent (2026-06-10 bug fix).** The PORTAL wizard uploads ALL tax files to the shared `onboarding-uploads` bucket (`app/api/portal/wizard-upload[-url]/route.ts`, path scheme `tax/{account_id}/...`). The EXTERNAL public tax form uploads to `tax-form-uploads` (path scheme `{slug}-{year}/...`). `FORM_CONFIGS.tax_return.bucket` defaults to `tax-form-uploads` (external), so `saveFormToDrive("tax_return", ...)` MUST be passed `opts.bucket="onboarding-uploads"` for portal submissions — otherwise `copyUploadsToDrive` downloads from the wrong bucket, EVERY file fails (`0 files copied`), and because the bank statements never reach `3.Tax/{year}/`, step 7's statement parse finds nothing and produces no P&L/Balance Sheet. The portal handler (`tax-form-setup.ts` step 6) hardcodes the override; `tax_form_review` (`tax.ts`) infers it from the path prefix (`tax/` → portal). This was the root cause of MMLLC portal submissions arriving with no attachments and no P&L in June 2026.
- **Statement parsing: Wise fast path + AI fallback + zip (2026-06-10).** `parseBankStatement` accepts PDF, CSV and `.zip`. Wise CSVs/PDFs use the free hand-coded parser; everything else (Mercury/Relay/Chase/any bank) — and any parse that yields 0 rows — goes to Claude-based extraction (`lib/bank-statement-ai-extract.ts`). **Never trust an unreconciled AI extraction for a filing**: check `reconciliation.reconciled` — `false` = the transactions don't add up to the statement's own closing balance, human review required; `null` = statement printed no balances / multi-currency / archive partially unverifiable. Truncation (`max_tokens`) is reported in `errors` as TRUNCATED. Kill-switch: `BANK_STATEMENT_AI_DISABLED=true`. Needs `ANTHROPIC_API_KEY` (set on prod; sandbox set 2026-06-10 via CLI after dashboard save silently failed on a stuck var).
- **AI extraction reads what the document actually is — review the bank_name it reports.** Real case (2026-06-10): Nexo Agency's only "bank statement" upload was a **Kraken 1099-DA crypto form** ($1.04M gross proceeds from stablecoin disposals — NOT revenue). The extractor labeled it `bank=Kraken (Payward Interactive)`. Feeding it to the P&L as income would have misstated the return; a human must reclassify or request real bank statements.
- **`portal_messages.sender_id` is NOT NULL** — any code inserting a system-generated portal message must use the zero-UUID placeholder `"00000000-0000-0000-0000-000000000000"`, not `null`. Passing `null` violates the constraint and the insert silently fails (supabase-js default mode returns an error object, not a thrown exception). Convention documented in `lib/portal/chat-events.ts`. Fixed in `app/api/crm/tax-review/action/route.ts` and `app/api/portal/wizard-submit/route.ts` on 2026-06-10.
- **PDF summary: label/value column overlap (2026-07-02 bug fix).** `generateFormSummaryPDF` used to draw every value at a FIXED x=200 while labels start at x=50 (x=70 nested) — any label wider than the column (e.g. "Personal Expenses Paid Through LLC (USD):" = 192pt at 9pt bold) got overprinted by its value, making the number unreadable (reported by the accountant for an SMLLC package; also hit every flattened `related_party_transactions_N_rpt_*` row in "Additional Information"). Fix: all four field render sites now go through one `drawFieldRow` closure — value starts at `valueStartX()` (aligned x=200 when it fits, else after the measured label + 8pt gap), and values that can't fit beside the label wrap below via `wrapByWidth()` (measured-width wrap, hard-breaks giant words; replaces the old 80-char count wrap). Layout constants in exported `PDF_LAYOUT`. Regression net: `tests/unit/pdf-field-layout.test.ts` parses generated PDFs with pdfjs and asserts no two same-line text items intersect — NEVER add a `drawText` for a field value at a hardcoded x; use `drawFieldRow`.
- **PDF summary: WinAnsi character encoding (2026-06-12 bug fix).** `pdf-lib` StandardFonts use the WinAnsi encoding — they only support U+0000–U+00FF (Latin-1). Client names or form values containing characters outside that range (e.g. Maltese ħ/Ġ/Ċ/Ż, Arabic, CJK, emoji) crash `generateFormSummaryPDF` with `WinAnsi cannot encode "ħ" (0x0127)`. Fix: `sanitizeForPdf()` in `lib/form-to-drive.ts` (exported, has unit tests in `tests/unit/sanitize-for-pdf.test.ts`) maps known Maltese substitutions (ħ→h, Ġ→g, Ċ→c, Ż→z) and replaces everything else with `?`. Applied to all four user-data `drawText` call sites in `generateFormSummaryPDF`. If a new character set is seen in the future, add explicit rules to `sanitizeForPdf` before the fallback `?` catch-all.

## How to verify current state
- Read `lib/mcp/tools/tax.ts` — the `tax_search` status list (authoritative status enum) and `tax_send_to_accountant` (package contents + idempotency).
- Read `lib/tax/reactivation.ts` for the installment-gated resume logic.
- A client's returns: `SELECT tax_year, return_type, status, deadline, sent_to_accountant_date FROM tax_returns WHERE account_id='<id>' ORDER BY tax_year DESC;`
- Pause state: `SELECT value FROM app_settings WHERE key='tax_season_paused';` (verify the exact key in code first).
- Note (R096): sandbox via sandbox MCP / `psql`; production `execute_sql` hits production.
