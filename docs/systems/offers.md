# Offers & Contracts
_Last verified against code: 2026-08-18 — Claude (**Corrected the Airwallex bank ADDRESS in `bank-defaults.ts`'s `AIRWALLEX_EUR` constant** — it held Tony Durante LLC's own Florida office address instead of the bank's own registered address (Banking Circle S.A., Amerika Plads 38, Copenhagen, Denmark 2100). Confirmed via a full search of every consumer that no current contract render, invoice PDF, or invoice email actually displays this `.address` field (the contract's bank panel shows beneficiary/IBAN/BIC/bank name only, never address) — so this was a latent data-hygiene fix, not a live client-facing correction; the corresponding LIVE bug was in a knowledge-base article, not code — see `billing-invoicing.md` 2026-08-18b. `RELAY_USD`/`MERCURY_USD`/`REVOLUT_USD`'s own `.address` fields are unchanged — those correctly hold the account holder's (TD's own) address, the right convention for a US ACH wire's beneficiary address, unlike Airwallex's international SWIFT wire where the bank's own address is the meaningful field.)_
_Prior: 2026-08-14c — Claude (**STAFF NOTIFICATION — "release commission" prompt, SANDBOX ONLY, branch `feat/referrer-commission-on-plan`.** The gap flagged in the entry below ("queued, not yet planned") is now built. A cron (`/api/cron/plan-referrer-notify`, every 15 min) sweeps payment-plan offers carrying a referrer/managed partner, not yet released, and asks `computePlanSettlement` fresh each time whether the WHOLE plan is now genuinely paid in real cash. The first time it sees yes, it drops a note in that account's What's New feed ("… payment plan is now fully paid — release the referrer's/managed partner's … on the account page") and never repeats it. Never releases anything itself.

**Why a sweep, not a hook on the payment writer — a real design correction, not a stylistic choice.** The first design hooked this into `applyMoneyToInvoice` (the shared function every automatic and most manual payment-confirmation paths call). A council review (bug-hunter + AI-architect, both independently) found that premise false: the dashboard's two "Mark Invoice Paid" actions (`payments/invoice-actions.ts`, `finance/actions.ts`) settle an invoice with a raw write that bypasses that function entirely — a common, everyday staff action, not an edge case, and exactly the "manual confirmation" path Antonio asked to be covered. A sweep reads plan STATE instead of watching write PATHS, so it cannot miss that path, or any future one. The review also corrected an earlier stated risk figure (the known bank-matcher amount-not-recorded gap is ~5-6% of settled invoices per job `c2751393`/its own test, not the ~2% first quoted) and flagged (fixed in the build): use the settling PAYMENT as the emitted event's source/deep-link, never the offer itself (offers have no standalone page and the link would 404).

**Reliability, by design, per Antonio's explicit "must be super reliable":** each candidate offer is processed in its own try/catch (one bad row can't silently kill the rest of the sweep); a failed notification insert is inspected and logged as an error rather than swallowed (unlike its older sibling emitters); the run is wrapped and reported to `cron_log` on both success and failure. Idempotent on the settling payment's id via the existing chat-event marker — proven live on deployed sandbox: seeded a fully-settled referrer plan, the sweep notified once, a second run correctly found nothing new, and a second offer with only one of two parts paid was correctly left alone. Registered in the `cron_coverage` manifest test (drift between `vercel.json` and the manifest now fails a test, same pattern as every other cron).

**Still open, deliberately not solved here:** the same known bank-matcher gap (a settled part that never got its cash amount recorded) means this sweep can miss a genuinely-finished deal exactly the way the release button's own eligibility check can — accepted, matching that button's existing philosophy, not a new risk this feature introduces.
)_
_Prior: 2026-08-14 — Claude (**REFERRER/PARTNER-ON-A-PLAN — LIVE ON PRODUCTION** (PR #293, merge `86897a86`, branch `feat/referrer-commission-on-plan`). The 2026-08-13b entry below built the release mechanism; this entry covers everything since — four rounds of same-day adversarial fixing, an independent 7-reviewer council pass, and today's follow-up fix, all before Antonio's explicit ship.

**Bugs found and closed, in order:** (1) a code-level review found a referrer AND a managed partner could both be paid on one offer, and two near-simultaneous release clicks could both pay — closed with the mutual-exclusion check plus an atomic claim column (`offers.commission_released_at`, migration `20260814-0100`) serializing concurrent requests. (2) A further pass found a referrer paired with a partner that has no real payout model was paid NOTHING (two different checks disagreed on "does a partner count") — `hasWorkingPartnerPayout()` now checks for an actual payout model, not bare `partner_id` presence, and `shouldReleasePlanReferrerCredit()` is defined as its exact negation so the two rails are structurally exclusive by construction, never independently computed (`lib/partners/partner-deal.ts`). (3) Proven with LIVE data, not code review alone: the SAME referrer bringing the SAME client back for a SECOND deal was paid only once (dedup keyed on referrer+client with no notion of "which deal" — now scoped by `offerToken`), and a partner's payout released fine but the `accounts.partner_id`/`partner_deal` record their FUTURE renewal depends on was silently never saved (write's own error now checked and folded into the response). (4) A 7-reviewer full-tier council pass then found, independently by three reviewers with a worked example: a referrer whose relationship started on a LEAD (before any offer existed) had an OLDER tracking record the release button didn't know about — releasing created a second, disconnected record instead of reconciling the original, leaving it stuck looking unpaid. Fixed via `linkLeadReferralToOffer()` (`lib/operations/referral.ts`), which the release route now calls before crediting.

**Same-day follow-up, found by Antonio's own question, not a review pass:** the four OLDER ways to pay a referral — the CRM "Add referral"/"Issue credit" buttons and the `referral_create`/`referral_payout` MCP tools — predate this whole mechanism and had no idea the plan-settlement gate exists; any of them could pay a referrer early, before a plan finished. Closed by `blockedByUnsettledPlan()`, one shared check reusing `computePlanSettlement`, wired into all four. Proven live on deployed sandbox with real HTTP calls (not database assertions): refused naming the exact shortfall on an unsettled plan, then let the identical call through once settled. Full detail: `referrals-circleback.md`.

**Correction to the 2026-08-13b entry below:** its UI line ("a human can act on their own judgment... which the code alone cannot always safely decide") implied a bypass exists on the account-page action. It does not — the server always re-checks and refuses cleanly regardless of what the screen shows; the comment was corrected in `components/accounts/account-detail.tsx`.

**Deliberately still open, Antonio's call:** whether releasing a second partner on an account that already has a first partner's renewal record could overwrite it (traced, zero live cases); a suggested automatic reconciliation check ("claimed released but nothing was actually paid") — proposed, not built. **Also queued, not yet planned:** a staff notification when a plan's final part is confirmed paid on a referrer/partner deal — prompts staff to release, does not release automatically; must cover both the automatic bank-feed matcher AND a manual payment confirmation, not just one.

Final numbers: 9,139 unit tests, 62 integration tests, code↔DB constraint contract, full build — all green on the merged result. Zero production errors in the deploy window. Zero production offers, historically, have ever combined a plan with a referrer/partner — this ships ahead of the first live case, not in response to one.
)_
_Prior: 2026-08-13b — Claude (**A REFERRER OR MANAGED PARTNER CAN NOW COEXIST WITH A PAYMENT PLAN — commission/payout releases ONCE, by a human, only when the whole plan is genuinely settled in real cash (Antonio, reversing the same-day 2026-08-10 refusal; dev job `c0a61e44`).**

**Why the refusal came out:** the refusal existed because activation credited a referrer's WHOLE commission the moment the FIRST part was paid, and the credit-note issuer can only key one note per referral — no way to correct an over-credit if later parts never arrived. Antonio's replacement rule removes the reason: pay once, at the end, when every part is cash-settled. No accrual, no correction needed.

**Removed at all four doors:** `refusePlanWithReferralPartner` (deleted from `lib/offers/payment-plan.ts`, along with its tests), `createOffer`'s step-1 and step-6c checks (`lib/operations/offers.ts`), `offer_update`'s referrer/plan conflict check (`lib/mcp/tools/offers.ts`), and the Create Offer dialog's `splitLockedByCommission` gate (`components/offers/create-offer-dialog.tsx`) — a referrer no longer disables the split checkbox at all.

**Calendly stopped inventing referrers (`lib/calendly/parse-invitee.ts`, `app/api/webhooks/calendly/route.ts`).** The "how did you find out about us" booking answer used to be keyword-matched into `leads.referrer_name` — a marketing-channel answer ("Instagram", "a friend told me") treated as a real person, attaching a real 10% commission to text nobody chose. Found when Antonio's own lead (Nicholas Tosello, "Referral") blocked his split for exactly this reason. The answer still survives verbatim in the lead's notes (`buildLeadNotes` dumps the full raw Q&A regardless) — only the inference into a payable field is gone. The verified referral-CODE→real-contact resolution path (a real Calendly-link code resolving to an actual client) is untouched; that is genuine attribution, not a guess. **Existing leads with a pre-2026-08-13 junk `referrer_name` are NOT bulk-cleaned** (Antonio: "forget the past") — clearing one is a per-lead manual action on the Referrer field.

⛔ **THE RELEASE GATE IS DELIBERATELY STRICTER THAN "FULLY SETTLED."** `computePlanSettlement` / `computePlanSettlementFromStatus` (`lib/offers/payment-plan-state.ts`) require, per part, `invoice_status==='Paid'` AND real `amount_paid` covering the agreed amount (within `PLAN_TOTAL_TOLERANCE`) — NOT the looser `PlanStatus.fullySettled`, which a pure-credit settlement (the Regenerate button, or an invoice born Paid because existing account credit covered it at creation) can satisfy with zero real cash. Both were checked directly: neither writes `amount_paid`. **A known, accepted false negative, pinned by its own test:** job `c2751393` verified 6 of 115 production auto-matched invoices have `amount_paid=0` despite real cash genuinely arriving (a bank-matcher recording gap). This gate cannot tell that apart from a real credit-only settlement, and deliberately does not try — a missed release a human notices costs far less than an automatic-looking payout on money never received. This is why release is a human-confirmed action showing the real numbers, never a background trigger.

**Nothing is created until release — no interim "pending" row.** The activation-time suppression (`lib/operations/activate-service.ts`, all three commission rails: offer-referrer, Calendly-lead-referral, managed-partner) still holds back automatic crediting exactly as before, but no longer raises the old hand-settlement `portal_issues` card (deleted along with `lib/offers/tranche-commission-issue.ts` — its message described the abandoned per-part-accrual design and would have told staff to hand-credit under a rule that no longer applies, risking a real double-pay against the new automatic release). The Calendly-lead-referral rail is unchanged in shape — its pending `referrals` row already sat in a valid, always-visible status; only its wording changed.

**Release: `POST /api/offers/release-commission`** (dashboard-only, same gate as `/api/referral/[id]/issue-credit`). Re-verifies eligibility server-side always — never trusts the GET route's cached value. Runs the referrer and partner rails independently (`Promise.all` — one failing does not block the other, matching how activation always ran them as separate steps). Referrer credit reuses the existing `createManualReferralCredit` (its own dedup + idempotency, unmodified) — a free-text-only referrer with no linked contact/account is refused with an actionable message (a credit note cannot post against a bare string). **Partner payout is created at `status:'pending'`** — verified BEFORE building this that the existing `approve_payout` admin action only accepts `pending`/`manual_review`/`requested`; any other starting value would have made a released payout permanently unapprovable through the existing, unmodified UI. Commission/payout basis is the plan's REAL cash-verified total (`settlement.totalAgreed`, from the same rows already proven eligible) — not a re-derived offer gross that could theoretically disagree with what was truly collected, and not the display-string parse the old activation path used. Rounded to 2 decimals (the shared `calculateCommission` applies none itself — confirmed by reading it).

**A raised tranche part's amount is now checked against the plan** (`app/(dashboard)/payments/invoice-actions.ts`, `PLAN_TOTAL_TOLERANCE`) — nothing did before. A mismatch refuses the raise rather than silently letting a part settle for less (or more) than the plan promised, which the release gate would otherwise trust as truth.

**UI: account page, "Partial Payment Schedule" box** (`components/accounts/account-detail.tsx`, `PaymentPlanPartsSection`) — a "Release commission" action appears whenever the offer carries a referrer or partner. It always opens a confirm panel showing the real total agreed and real cash received before anything fires, EVEN when the automatic gate has not lit up (the known false-negative above) — a human can act on their own judgment with the real numbers in front of them, which the code alone cannot always safely decide.

Verified: 9,060 unit tests (26 new, covering the settlement gate including the false-negative case), unpiped build exit 0, lint clean on every touched file. Sandbox click-test with real payment events pending before merge.
)_
_Prior: 2026-08-13 — Claude (**A SPLIT SETUP FEE CAN NOW BE AUTHORED FROM THE CREATE OFFER SCREEN — the missing half of WS-C, found by Antonio on a real client (dev job `c0a61e44`, Antonio-approved ship).** WS-C shipped every stage AFTER the split — the offer's schedule block, the contract wording, the first invoice minted at signing, the raise-a-later-part box on the account page — but NOTHING that could write a plan. The only door was the `offer_create` / `offer_update` MCP tool, so a split deal could not be sold from the CRM at all; zero production offers carried a plan not because the feature was unused but because it was unauthorable. Found 2026-08-13 when Antonio tried to sell Nicholas Tosello (€3,500 Florida MMLLC + ITIN, 50% on signing / 50% at 30 days) and there was no field. **New: a "Client pays the setup fee in parts" section in `components/offers/create-offer-dialog.tsx`, plus a one-line `payment_plan` passthrough in `app/api/crm/admin-actions/create-offer/route.ts` — the route was silently DROPPING the field while `createOffer` already accepted and validated it (a UI could have been built against it at any time and would have posted into a black hole).** The section deliberately MIRRORS the engine's refusals rather than inventing softer ones, because a UI that lets you type a refusable plan only moves the error later: part 1 is always the signing trigger (the activation anchor — the engine allows at most one), the currency is the offer's and is not per-part (credit, bank matching and activation are all single-currency), later parts are `date` or `manual` only (the wired-event registry is empty by design), a plan whose parts do not sum to the setup-fee header raises a live warning naming both figures (that mismatch is the "cannot state what is due" condition that strips every payment control at signing — see the section below), and the whole section is LOCKED with its reason shown when the offer carries a referrer or a managed partner (commission credits in full on the first payment). An invalid plan is never POSTed; `createOffer` validates again at the door. **The authoring echo is reproduced here from the MCP tool** — the author sees the exact `clientFacingPartLabel` sentences the client will read on the offer and in the signed contract, at the one moment they can still be changed. **A plain-English line states that only part 1 invoices itself and every later part is raised by hand — a `date` trigger is a REMINDER, never a scheduler** (nothing in the system fires on it; the alternative is discovering it via a payment that never arrived). Verified: 9,045 unit tests, unpiped build exit 0, and CLICKED on the deployed sandbox as staff — both amounts, the trigger selector, the free-text wait label, the live mismatch warning and the client echo all rendered. **TRAP RECORDED: `scripts/deploy-sandbox.sh` aliases a PREVIEW deployment, which puts the entire sandbox alias behind Vercel SSO (even `/api/health` 302s to `vercel.com/sso-api`) while the script cheerfully prints "sandbox now serves X" — redeploy with `--prod` to the sandbox project's production target to make it public.**

⛔ **ONE TOLERANCE, ONE PLACE — `PLAN_TOTAL_TOLERANCE = 0.01` + `planTotalMatchesGross()` in `lib/offers/payment-plan.ts`.** How far a plan's parts may differ from the offer's gross is now a single constant used by the authoring screen, `decideSigningBill` and `resolveDueNow`. It exists because the dialog shipped its own looser threshold (0.5) while both engine sites used 0.01, and **there is no server-side plan-vs-gross crosscheck** — `createOffer` and `offer_update` validate plan STRUCTURE and the referrer/partner conflict only, so the authoring screen is the only gate and a plan written via the MCP tool bypasses the tolerance entirely (caught at consumption as the loud whole-fee degrade). A three-way split of a fee that does not divide (3500 → 1166.66 × 3 = 3499.98) passed the old gate and was then refused by every consumer: offer page hid the payment controls, contract could not state an amount, signing billed the WHOLE fee. **A gate looser than the thing it guards is not a gate.** Pinned by `tests/unit/plan-total-tolerance.test.ts`, which fails three ways if anyone loosens it. Note the true boundary is EXCLUSIVE in practice: a hand-typed one-cent error (3500.01 vs 3500) evaluates to 0.010000000000218 and is REFUSED — float drift from splitting into thirds is ~1e-13, four orders of magnitude below, so legitimate splits are never falsely rejected.

⚠️ **THE `min` ON A DATE-TRIGGERED PART IS A PICKER NICETY, NOT A GUARD** (hunter, pass 4 — recorded so nobody trusts it). It constrains the native calendar popup only: there is no `<form>` element in the dialog (submission is a plain onClick), so native constraint validation never runs, and `validatePaymentPlan` checks format + calendar-reality with NO past-date branch. A **typed** date still reaches the client's offer, signed contract and portal schedule ("due by 1 September 2025" for a payment that has not happened) — and typing is exactly how the year typo it was meant to stop gets in. The authoring echo is the real defence: it renders the sentence the client will read. A genuine past-date refusal belongs in `validatePaymentPlan` so every door is covered; it is NOT built.)_
_Prior: 2026-08-11c — Claude (**GATE RULING — how the overdue chase treats a payment-plan part: THREE STATES, protected BY STRUCTURE, and chase-on-sent is CORRECT (architect + Antonio, WS-C gate cell G, dev job `c0a61e44`).** The original cell expectation "a raised part fires no reminder" was mis-specified — it collapsed three distinct states into one. The ruled formulation, each proven live on the sandbox: (1) **untriggered** — no invoice exists yet, so the chase cannot see it: structural invisibility, nothing to configure; (2) **raised but unsent** — a Draft, and `lib/billing/dunning.ts` promotes only `Sent`/`Partial` to Overdue and reminds only `Overdue`, so a Draft is invisible by the same filter that protects every other draft (proven: zero reminder jobs ever for the Draft parts, reminder counters untouched); (3) **raised, sent, unpaid past due — CHASED like any other invoice, and that is collection working, not the plan leaking**: the live run considered it, flipped it Overdue, and queued the reminder job (cron_log 2026-08-12T00:01:41Z, job cd84809a). Antonio's ruling on the copy: **leave the reminder wording alone** — a client who was sent an invoice and did not pay it is overdue; plan-aware phrasing is an optional COPY job (`471b3772`), never a machinery change. **Honest caveat, part of the cell record:** the Sent state was PLANTED (status + backdated due date), no real email was sent — so the cell proves the chase engine reads invoice state correctly, NOT that the send path itself behaves; do not record it as more. The two states that must be protected are protected by structure, not configuration — the stronger result, and the reason this needs no plan-specific dunning code at all.)_
_Prior: 2026-08-11b — Claude (**A payment-less offer now gets a REAL post-sign screen — and the lease landlord block follows the new signature. Antonio, sandbox QA.** (1) `app/offer/[token]/contract/page.tsx`: when a signed offer has NO card link and NO bank details, the only feedback used to be a status line under the still-visible contract; the client got no confirmation screen after signing a legal document. That branch now hides the contract and renders the same success panel as the paid path — checkmark, "Contract Signed Successfully!", portal next-steps line, a new bilingual note ("download your signed copy / message us in the app"), Download Signed PDF, back link. The PDF download handler was extracted to one shared `attachPdfDownload` used by both branches (it was inline in the paid branch only). Paid offers are UNCHANGED: card/bank choice + wire receipt upload as before. (2) `lib/lease-pdf.ts` landlord signer/title defaults: "Antonio Durante / Managing Member" → "Antonio Noel Durante / Executive Director" — the landlord IS the firm, so this follows the contract-template change of the same day. DELIBERATELY REFUSED without a fresh explicit ruling: the onboarding "Managing Member (Primary Contact)" label and the tax-statement signer title are the CLIENT’s roles on the CLIENT’s documents, not Antonio’s signature — blanket-applying his new title there would mislabel client paperwork. 39 lease unit cells green.)
_Prior: 2026-08-11 — Claude (**Consulting-firm signature block updated on ALL FOUR contract templates — Antonio, verbatim, during the ca788354 sandbox QA pass.** The firm side of every Statement-of-Work signature section now reads **Name: Antonio Noel Durante / Title: Executive Director** (was "Tony Durante / Managing Member"). Eight paired occurrences replaced across `app/offer/[token]/contract/page.tsx`, `service-agreement.tsx`, `standalone-service-agreement.tsx`, `renewal-agreement.tsx` — zero old-form strings remain. Deliberately NOT touched (only the contract signature block was requested): the lease landlord title default in `lib/lease-pdf.ts`, the onboarding "Managing Member (Primary Contact)" label, and the tax-statement signer titles.)
_Prior: 2026-08-08b — Claude (**HOTFIX — the money rails must FETCH the credit, not merely read for it (dev job `c0a61e44`).** The card checkout shipped selecting neither `credit_amount` nor `currency`, so `computeOfferPayable` saw undefined, treated the credit as zero and charged the GROSS — the exact bug WS-A shipped to fix, live on the only rail that moves real money. It compiled because the reads were `as`-cast. THE RULE: every rail that quotes or charges an amount must SELECT `currency` and `credit_amount` and pass them to the calculator, and must NOT use `as` to read them — the cast is what blinded the compiler. Two sibling rails had the same shape: the contract's bank panel recomputed the wire amount at signing without the credit (a client would be told to wire the gross while the offer and the invoice said the net, and the overpayment would land nowhere because settlement caps at the invoice total), and the Whop card link was minted from the raw header at gross+5% with no overage detector on that rail. The contract page no longer imports the gross-only calculator AT ALL, so it cannot regress by omission; the Whop plan prices the net and mints no link when credit covers the offer. Verified on production before the fix: zero offers carried a credit, so nobody was overcharged — armed, not fired)_
_Prior: 2026-08-08 — Claude (**WS-A PAID-CALL CREDIT ON THE OFFER — dev job `c0a61e44`, Antonio-approved ship.** Three new display-only columns (`credit_amount`, `credit_payment_id`, `credit_kind`). NET EVERYWHERE: one amount authority, `computeOfferPayable`, returns gross/credit/net and EVERY rail reads it — the card charges the net plus the fee ON the net, the bank box quotes the net, and the contract's Key Terms render the arithmetic openly. No surface computes its own figure; that split is what let a client be shown €1,243 and charged €1,575. ONE currency rule, `resolveOfferCurrency`: explicit `offers.currency` wins, header sniffing is the fallback, the services blob is NEVER sniffed (a recurring "$2,000/year" line used to flip a EUR offer to USD). AUTO-PROPOSE: `createOffer` resolves the person by EMAIL — the same key the signing webhook uses — because a lead carries no contact id until it signs, so gating on one made the feature dead in its own headline case. Display-only: offer linkage is untouched. Warnings ride out on the result and HOLD THE SCREEN (they were toasted immediately before `window.open` stole focus). Label honesty: `credit_kind='paid_call'` only when every contributing credit really is one, else the neutral wording; carried across a revision)_
_Prior: 2026-08-06 — Claude (**THE OFFER AMOUNT ENGINE — WS-A3 sites #1+#2, early ship (dev job `c0a61e44`, Antonio-approved cherry-pick).** New `lib/offers/compute-offer-totals.ts` is THE single amount calculator: `computeOfferTotals` reproduces the signing webhook's money-of-record semantics verbatim (quirks pinned by the 18-test characterization suite `tests/unit/compute-offer-totals.test.ts` — minus-stripping, EU dot-thousands, recurring/included skips, pre-condition groups, header fallback with EU handling, header-based currency detection) + `computeNetOfCredits` (same-currency only, capped, never negative — locked decision D3, consumed by WS-A's credit phase). MIGRATED IN THIS SHIP: (1) `offer-signed` webhook — pure delegation, its FOUR formerly-independent in-route currency detections read ONE engine value; (2) `create-checkout` — the card charge now equals what the webhook records. **REPLAY over all 77 open production offers before the charge-rail migration: 77/77 amounts identical; ONE currency divergence and it was a LATENT MISCHARGE in the old rail** — `antonio-schino-2026` (all-EUR one-time services + EUR header) had its old group-scan flipped to USD by a recurring "$2,000/year" line → a card payment would have charged $2,500 instead of €2,500; the engine's header detection is correct. The old checkout fallback's plain parse ("€1.500" → €1.50 charge) is also fixed. Remaining ~20 parser call sites (display pages, dialog, reporting) migrate in the full WS-A branch; until then the offer PAGE's price-string currency sniffing may still disagree with the engine on display — the money paths (webhook + checkout) are unified as of this ship. Post-ship check: Schino card-page currency probe.)_
_Prior: 2026-08-05 — Claude (**FORMATION STATE PINNED ON THE OFFER — WS-B of the sales-model-gaps program, dev job `c0a61e44`.** New nullable column `offers.formation_state` (codes NM|WY|FL|DE) + `pending_activations.formation_state` (migration `20260805-2300-offer-formation-state.sql`). **Single source of truth for the state list is `lib/formation/states.ts`** (codes, names, NM default, `normalizeFormationState`, the pure `resolveFormationStateCode` authority chain) — architect-mandated ONE-PLACE rule (R3-1.2); adding a state = edit that file only; deliberately a code constant, NOT a catalog entry, and deliberately NO DB CHECK (a CHECK would be a second copy of the list). Server-side lookups live in `lib/formation/state-lookup.ts::formationStateForClient` (newest SIGNED/COMPLETED offer's state for a lead/contact — signed-only on purpose: a draft's state is not authoritative). Flow: Create Offer dialog radio row (formation offers only) → `create-offer` route → `createOffer` (normalized on insert) → shown on BOTH offer pages' hero meta ("State of Formation", EN/IT) and the contract's Key Terms + SOW tables (formation contracts only — an onboarding client's company already exists) → offer-signed webhook copies it onto `pending_activations` → `formation_form_create` defaults its `state` from the lead's signed offer when the param is omitted (param is now a real NM|WY|FL|DE enum — it used to accept ANY string unvalidated) → the SD flow-advance resolver (`lib/service-delivery.ts` materialization block) resolves wizard → submission → OFFER → NM instead of its old wizard-only silent-NM (the exact Wyoming-offer-files-as-NM legal mismatch this fixes); the admin override at Articles upload still wins above everything, unchanged. **revise-offer now carries a per-field keep-or-drop TRIAGE comment block** (architect-accepted): `contact_id`, `entity_type`, `formation_state`, `card_fee_rate`, `referrer_contact_id` are now COPIED to v2 (the last two were confirmed money-losers being silently dropped); `selected_services`/`payment_links`/`expires_at`/view counters stay deliberately NOT copied (recorded reasons in the route); partner_*/referrer_commission_* flagged for their own pass. When adding ANY new offers column, decide its fate in that block in the same change or it vanishes on v2. **ACCEPTED TECHNICAL DEBT (architect R3-2, recorded here on purpose):** offer prices remain parsed TEXT strings throughout (`parseFloat(price.replace(...))` in ~10 files, 25 call sites); the planned WS-A3 shared calculator centralizes the quirks under characterization tests — it does NOT make prices numeric. Do not mistake "quirks preserved under test" for "solved"; numeric price fields on services/cost_summary are the future migration, out of scope for the current program. Sandbox migration applied 2026-08-05; COMMENT ON statements in the migration file did not survive the sandbox executor (cosmetic — run at prod promote).)_
_Prior: 2026-08-01 — Claude (**SIGNED-CONTRACT DOWNLOAD MOVED OFF THE ANON STORAGE CLIENT — storage lockdown Stage 2, dev job `97177e49`.** The offer-contract signing page + its renewal sibling used to re-download the signed PDF (the returning-client fallback, when the just-signed blob is gone) by `supabasePublic.storage.from('signed-contracts').list(token)` + `download(newest)`. That required the `signed-contracts` bucket to be anon-readable AND — because anon INSERT stays — let an attacker plant `{token}/zzz.pdf` that the "newest" sort would serve to the client (content injection of a legal doc). **NOW:** new token-gated route `GET /api/offer/[token]/contract-pdf?code=<offer access_code>` (`app/api/offer/[token]/contract-pdf/route.ts`) verifies the offer's `access_code` SERVER-side via the shared `accessCodeError` guard (constant-time, rate-limited, fail-closed on a blank code) and signs the EXACT recorded path (`contracts.pdf_path`, latest row by `created_at` — an offer can have up to 3 contract rows) with a 60s signed URL via `createRecordedSignedUrl` (`lib/storage/signed-download.ts`). **The offer TOKEN alone is NOT the credential** — tokens are human-guessable (`slugifyName(clientName)-YEAR`), so the unguessable UUID `offers.access_code` is required; the page already has it in memory from its anon offer load and sends it. Verified: all 103 signed contracts have a non-null offer access_code, so requiring it breaks no client. `middleware.ts` adds `/api/offer/` to `PUBLIC_PREFIXES` (token-gated, not session-gated — see `auth-oauth.md`). The signed-PDF UPLOAD path (POST to `object/signed-contracts` at sign time) is untouched (INSERT kept). Bug-hunter ×2 (0 client-breaking; the token-only hole it found is closed by the code gate); E2E-verified on a live sandbox deploy (right code → real PDF download, wrong/no code → 403, pages render). **KNOWN, flagged not fixed:** (a) annual RENEWAL contracts (`annual_agreements`, no `access_code`, not in `offers`) are not served by this route — safe because their download button only appears in-session with the blob present; needs its own re-download credential before any renewal re-download surface is wired here. (b) The `offers` TABLE itself is still anon-readable (`Allow public read by token` policy is `USING(true)`), exposing offer PII + access codes — the offers-table equivalent of the July lease-table lockdown, the real remaining exposure for contract data, tracked on `97177e49`. Lease half: `lease-oa.md`. Stage-1 (onboarding/banking/itin/wire read revoked, formation bucket made private) shipped to prod 2026-07-31; migration `scripts/migrations/20260731-2015-storage-lockdown-stage1-drop-anon-read.sql`.)_
_Prior: 2026-07-26b — Claude (**OFFER NARRATIVE: "DISCUSS WITH AI" REFINE CHAT (Antonio-directed, trusts the author).** A chat panel in the Create Offer dialog (`components/offers/create-offer-dialog.tsx`, `refineNarrative`) lets Antonio talk to the AI to change the generated narrative in his own words ("shorten the intro", "he's Dubai-based and will route funds to his Dubai company — mention that", "drop step 3"). New endpoint `app/api/crm/admin-actions/refine-offer-narrative/route.ts` (Sonnet, **temperature 0**) returns ONLY the sections it changed (`validateNarrativeChanges` in `lib/offer-narrative.ts` — validates only the present keys + the single-language intro rule), which the dialog applies over the current textarea state so **untouched sections and hand-edits are never clobbered**. Prompt builders `buildRefineSystemPrompt` / `buildRefineUserPrompt` in `lib/offers/narrative-business-rules.ts`. **KEY DESIGN DECISION (Antonio, 2026-07-26): the refine chat does NOT judge/refuse/lecture — it is a faithful writing assistant.** The guardrails (no bookkeeping, no over-promise, correct filing) live on the AUTO-`generate` path (where the AI writes unprompted — the original Joan-Roque bug) and STAY there. On the refine path Antonio is the author: it writes what he dictates, using the business rules + the full service menu (`getAllSellableServices`) as *reference for accurate wording, not restrictions*. This reversed an over-strict first cut that refused a legit instruction and falsely claimed TD doesn't offer banking. It never invents facts he didn't give, writes in the client's language, and addresses the CLIENT name only. **Draft-only: the chat returns changed sections to the dialog — it never writes to an offer record or sends anything; every offer stays a human-reviewed draft.** The rules loader was extracted to `lib/offers/load-business-rules.ts` and is shared by BOTH generate + refine so they can't drift. The input is a multi-line textarea (⌘/Ctrl+Enter sends). **DEFERRED — Phase 2 "learn & reuse across clients" NOT built** (council: 2 blockers — a learned lesson must be style-only + subordinate to the hard rules, distilled from Antonio's instruction not the client transcript, scrubbed + shown before save, merged-not-appended into the ONE rules article with a cap, confirmed in a separate queue not the offer hot path, prunable; Phase 0 mobile structured editors flagged as a cheap prerequisite). Verified: full suite green, eslint, next build. Dev job `558a378a` (child of `956300c5`).)_
_Prior: 2026-07-26 — Claude (**AI OFFER NARRATIVE: BUSINESS RULES MOVED OUT OF CODE INTO AN EDITABLE KB ARTICLE.** The "Generate with AI" narrative generator (`app/api/crm/admin-actions/generate-offer-narrative/route.ts`) used to HARDCODE TD's service model, exclusions, and per-entity tax filing as prompt prose — and only the `onboarding` branch did it, and it was never told the entity type or the real services. Result on a real single-member LLC onboarding offer (Joan Roque): the narrative invented bookkeeping / "set up accounting systems" / "record every transaction" / "audit-ready books" / year-end tax prep — services TD does NOT offer (they are contract Key Exclusions) and that a foreign-owned SMLLC does not need. **ROOT CAUSE + FIX (2 council rounds + bug-hunter E2E, Antonio-approved):** the business rules (what TD does/doesn't offer, the U.S. tax filing by company type — SMLLC = pro-forma 1120 + Form 5472 information return, MMLLC = 1065 with P&L + balance sheet, no bookkeeping — and the Client Portal) now live in an EDITABLE knowledge-base article tagged `offer_narrative_rules` and are injected into EVERY contract type. Pure, testable builders + helpers live in `lib/offers/narrative-business-rules.ts` (`resolveBusinessRules`, `offerIncludesManagement`, `renderServiceLines`, `buildSystemPrompt`, `buildUserPrompt`, `FALLBACK_BUSINESS_RULES`). **Fail-safe, not fail-open:** if the article can't load the writer degrades to a MINIMAL floor (never bookkeeping; stay generic on tax) — the floor is a floor, NOT a mirror of the rich content, so the two can't drift; a genuinely-missing/mistagged article is reported LOUDLY (`reportSystemError`) as a config error, a transient DB error falls back quietly. **Service descriptions come from the editable services catalog** (the dialog sends `{name, description}` per selected service, not bare names). **Entity type is threaded through** (`normalizeEntityType`) so the tax wording is correct per company type. **Management/portal over-promise gate:** a standalone offer (ITIN-only, notary-only, banking-only) sets `includes_management=false` (dialog computes it from the real selected services: contract_type ∈ formation/onboarding/renewal OR has_annual OR category=primary; server falls back to the RAW contract type, never defaults ON) so the writer describes ONLY the sold service — no registered agent / annual filing / portal it didn't buy. **The only per-type logic left in the prompt is two STRUCTURAL switches — the company stage (formation/onboarding/renewal) and management on/off — NOT hardcoded service content.** Also fixed a real sibling bug: `kb_create` persisted `tags` for responses but DROPPED them for articles (`lib/mcp/tools/knowledge.ts`), so any tag-based lookup of a tool-created article silently failed. Bug-hunter round 2 caught + fixed two more: the narrative's contract_type diverged from the offer record's (an onboarding+standalone bundle could describe forming a NEW company) — now uses `derivedContractType`; and management detection ignored `has_annual`/`category=primary` (could under-describe a real management offer). CPA/tax wording: no S-corp/1120-S (unavailable to non-resident owners), softened "owe no US income tax" → "generally, absent US income", "no bookkeeping required", balance sheet "where required". Idempotent seed migration `scripts/migrations/20260726-1500-seed-offer-narrative-rules.sql` creates the article; the prod article was seeded via the same SQL before code deploy (the article is EDITABLE by Antonio — change the wording in the KB, no code change). E2E scenario simulation `tests/unit/offer-narrative-scenarios.test.ts` (10 scenarios: SMLLC/MMLLC onboarding, formation, renewal, ITIN-only, banking-only, unknown entity, KB-missing floor, Italian, mgmt on/off). Verified: 7128 unit tests, eslint, next build. Sandbox-deployed + prod-shipped. Dev job `956300c5`.)_
_Prior: 2026-07-21 — Claude (**THE SIGNING PAGES USED TO TELL A CLIENT THEY HAD SIGNED WHEN NOTHING WAS SAVED.** dev job `023c7d06`. On the contract page and its three sibling components (`service-agreement`, `standalone-service-agreement`, `renewal-agreement`) the signed-PDF storage POST, the `contracts` insert and the `offers` status flip ALL ran unchecked. A client could sign, see "Contract Signed Successfully!", pay by wire — and TD would hold no signed PDF, no contract row, and an offer still marked `sent`, while `/api/webhooks/offer-signed` fired anyway and created a `pending_activation`. **Worse than a missing check:** the status update ran in a 3-attempt retry loop that exits NORMALLY when all three fail, so it LOOKED like it handled errors. **Now:** every write is checked and the ordering invariant is enforced — artifact → record → status → webhook, each gating the next; the webhook is skipped when the status flip fails. Client-facing copy lives in the pure `lib/public-forms/signing-failures.ts` (bilingual EN/IT, keyed by stage) because this repo has ZERO component tests and blocks pushes without unit tests — the I/O stays inline in each page ON PURPOSE, since a shared helper that performed the write would put every signing page behind one code path. **Careful distinction, do not "simplify" it away:** when the signature IS saved and only the status flip failed, the client is NOT told the document is unsigned — they are told it saved and asked to confirm. Telling someone their signed document is unsigned is its own harm. **Retry is now a real path, which forced two further fixes:** (1) the PDF-capture step destructively rewrites the DOM (inputs→spans, canvases→imgs), so a second attempt hit `canvas.parentElement === null` and threw a raw TypeError onto a legal-signing screen — guarded by `frozenForPdfRef` so the freeze runs once and a retry reuses the frozen DOM, which captures identically; (2) `contracts.offer_token` was VERIFIED against production to carry a plain index, NOT a unique constraint, so a legitimate re-sign inserts a second row rather than hard-failing. **VERIFIED IN A REAL BROWSER on a live sandbox deployment:** blocked the storage write, signed, and retried — the client saw the correct "NOT signed" message both times, no raw error, action bar restored, button usable; the database showed 0 contract rows, offer still `sent`, 0 pending activations. **NOT PROVEN, stated rather than hidden:** the SUCCESS path has never been executed anywhere — sandbox has only 7 of production's 20 buckets and `signed-contracts` is not one of them. The council's judgement for shipping anyway: the success path was NOT modified (the PDF path still carries `Date.now()`, no new folder segment), and the three added guards are structurally incapable of firing on a successful write (`res.ok` off a real Response is always boolean; supabase-js `error` is null on success). Post-ship gate: sign a throwaway internal offer end-to-end on production, then delete it. Four council rounds; operating agreements were SPLIT OUT to `claude/oa-signer-count-redesign` because that flow needs a redesign (it maintains a separate `signed_count` and enforces it from the browser — a retry could finalize a multi-member OA on ONE signature). Full detail of the form half in `documents.md`.**)_
_Prior: 2026-07-14 — Claude (**PAY-BY-CARD FROM THE PORTAL WAS BROKEN + a live billing hole.** dev_task `ba7bfd8d`. (1) The portal frames the offer page CROSS-ORIGIN; the Pay button did `window.location.href = checkoutUrl`, which navigated THE FRAME to Stripe. **Stripe Checkout refuses to render inside a frame** → the client sat on Stripe's grey loading skeleton forever (that skeleton IS the screenshot). Only the pay CLICK broke — the proposal renders fine framed — which is why it survived every page-load test. The obvious fix (`window.top.location`) is WRONG: a cross-origin frame may drive the top window only while it holds transient user activation (~5s), and the pay handler must first `await` the Stripe session creation. When activation lapses browsers do **not** throw — they ignore it SILENTLY, so a try/catch fallback never fires and the button is dead again, intermittently. FIX (`lib/payments/checkout-redirect.ts`): open the tab **synchronously on the click, before the await** (`openCheckoutTab`), then point it at Stripe (`deliverCheckout`); if a popup blocker refused, `deliverCheckout` returns `needs_manual` and the page renders a real `target="_top"` link — a fresh click carries fresh permission and always works. The stale `payment_links[0].url` fallback was DELETED (it could hold an old session at a different price/gateway). (2) SECURITY — `POST /api/offers/create-checkout` is PUBLIC (token-only, no session) and let the REQUEST BODY override `selected_services`: a client who had SIGNED for an optional add-on could POST `{selected_services: []}` and be billed for the required services only. Freezing the checkboxes closes nothing. `resolveBillableSelection` (`lib/payments/billable-selection.ts`) now IGNORES the body once the offer is signed/completed — the stored selection IS the contract. (3) The proposal re-ticked RECOMMENDED optionals on load instead of restoring the client's SIGNED selection, so the page could show a different price than checkout bills; it now restores `selected_services` and freezes the boxes once signed. (4) A cancelled payment dead-ended on `/offer/{token}` — a page with NO pay button and an email gate — so no client could retry; it now returns to `/portal/offer` (payment is portal-only, R092). **NOT in this change:** actually charging the 5% card fee. That is a separate job — the fee is displayed everywhere and charged nowhere (it regressed when Stripe replaced Whop; the Whop plan is still built at `base*1.05`), and fixing it means moving the invoice, receipt, PDF, portal mirror, Whop link and contract wording together. A first attempt was reverted after adversarial review found it wrote only one of the invoice's money columns. VERIFIED: 21 new unit tests, eslint, tsc, next build. SANDBOX.)_
_Last verified against code: 2026-07-09 — Claude (REFERRER PICKER on the Create Offer dialog, per Antonio: "instead of writing a dropdown I can pick the client to assign the referrer … the referrer is always a client (contact or account) … a free text as well and don't forget the partner and all roles". The read-only "Referrer: X (client)" line in `components/offers/create-offer-dialog.tsx` is replaced by `components/offers/referrer-picker.tsx`: a searchable picker over the SAME actor space as the referrals "Add referral" modal (`GET /api/referral/manual?q=` — accounts of ANY type incl. Partner + contacts), a company selector when a picked contact owns several companies ("credit goes to"), and a **free-text fallback** ("Enter a name manually") for a referrer not yet in the system. Picking pins the referrer by ID: the dialog sends `referrer_contact_id` + `referrer_account_id` (alongside `referrer_name`/`referrer_type`) → `create-offer` route → `createOffer` (`lib/operations/offers.ts`, new `referrer_contact_id` param) → new column `offers.referrer_contact_id uuid` (migration `20260709-1200-offer-referrer-contact-id.sql`, FK to contacts; applied sandbox + prod). WHY: the old free-text name was resolved at payment time by a fragile ILIKE-match-or-CREATE-a-contact in activate-service Step 3.5, which mis-attributed the reward Credit Note or spawned a duplicate contact. The pinned id makes the pay→credit chain deterministic (see `referrals-circleback.md`). Free text keeps the old name-match/create path. A Partner account picked as referrer sets `referrer_type='partner'` (price_difference commission) — this is REFERRER attribution, separate from the managed-partner **Partner deal** section (`partner_id`, Step 3.6). VERIFIED: 5224 unit tests, eslint, next build. LIVE ON PRODUCTION — branch `claude/brave-cohen-8425cc`.)_
_Prior: 2026-07-07 — Claude (Drive duplicate-upload sweep, LT Program incident class — the offer-signed webhook's signed-contract PDF upload (`app/api/webhooks/offer-signed/route.ts`) switched from `uploadBinaryToDrive` to `uploadBinaryToDriveUpsert` (stable file name → a retry/re-run refreshes the ONE existing Drive file in place instead of adding a copy). No other behavior in this subsystem touched; full sweep rationale in `documents.md` (2026-07-07b). Branch `claude/objective-cohen-b75f61`, SANDBOX — Antonio ships.)_
_Prior: 2026-07-07 — Claude ("Unknown error" incident fix. The Create Offer dialog (`components/offers/create-offer-dialog.tsx`) no longer collapses non-JSON API failures into a bare "Unknown error": both the Generate-with-AI and create-offer fetches now read the error body as TEXT first (`readErrorBody`), detect the middleware's new `401 code: SESSION_EXPIRED` contract (→ "Your session expired — refresh the page, log in again" toast), surface `error` or `HTTP <status>` otherwise, and fire-and-forget a capture to the error auto-audit system (`reportDialogError` → `/api/system-errors/report`). The two offer API routes also report server-side failures via `reportSystemError`. Root cause of the 2026-07-07 incident: expired admin session → middleware 307 → browser re-POSTs `/login` → 405 HTML → old `res.json().catch(() => ({error:'Unknown error'}))` swallowed it. Full system: `error-auto-audit.md`; middleware contract: `auth-oauth.md`. SANDBOX, branch `claude/festive-wiles-4d3c84`.)_
_Prior: 2026-06-30 — AI OFFER NARRATIVE reliability fix. "Generate with AI" was failing in production ("AI generation failed") because a rich ~4096-token Sonnet narrative ran past the shared provider's 30s timeout AND the GPT-4o-mini fallback also timed out. FIX: (1) `app/api/crm/admin-actions/generate-offer-narrative/route.ts` now sets `export const maxDuration = 300` and passes `timeoutMs: 90_000` to `callAI` so Sonnet has room to finish; (2) the route surfaces the REAL provider error to the admin-only toast instead of an opaque "AI generation failed" (R099). PROVIDER POLICY CHANGE (`lib/portal/ai-provider.ts`, affects ALL callers): Sonnet/Opus ONLY — no Haiku, no OpenAI/GPT. Failover is now Sonnet→Opus (Anthropic-internal); `temperature` is dropped for Opus (it 400s otherwise). Tests: `tests/unit/ai-provider.test.ts`. SANDBOX, branch `claude/offer-narrative-ai-fix`._
_Last verified against code: 2026-06-25 — Claude (Create Offer dialog `components/offers/create-offer-dialog.tsx` gained an optional **Partner deal** section: pick a managed partner + setup payout + renewal payout (USD). Threaded via `createOffer` + the `create-offer` route into `offers.partner_id` / `partner_payout_model='flat_fee'` / `partner_payout_rate` (setup) / `partner_renewal_payout` (renewal). Full design in `partners-team.md`. SANDBOX, branch `feat/partner-deal-config`.)_
_Last verified against code: 2026-06-20 — AI OFFER NARRATIVE now grounded in the full call transcript. The "Generate with AI" button in Create Offer calls `app/api/crm/admin-actions/generate-offer-narrative/route.ts` (Claude Sonnet via the shared `callAI`, OpenAI fallback) → produces the offer's intro + strategy/next_steps/future_developments/immediate_actions JSON (validated by `validateNarrative` in `lib/offer-narrative.ts`). The dialog now passes `lead_id`/`account_id`; the route fetches that client's most-recent `call_summaries` row and feeds its notes + FULL transcript (capped ~14k chars via the new pure `renderCallForOffer`) into the prompt, so the narrative reflects what the client actually said — not just the capped summary notes. System prompt loosened (intro 4-6 sentences, 2-3-sentence descriptions, 4-5 steps, reference call specifics) + `maxTokens` 2048→4096. PRESERVED: the single-language intro rule (only the client's language intro is filled, the other stays "") and the formation/onboarding/renewal contract-type rules. Best-effort: no call / fetch error → falls back to notes-only. Tests: `renderCallForOffer` cases in `tests/unit/generate-offer-narrative.test.ts`. Prior: 2026-05-29 — Claude (read publish.ts, offer-signed webhook, sync-offer-email.ts)_

## What it is
The sales artifact a prospect signs to become a client: an offer/contract published to a client-facing link, signed online, then paid. It's the money entry point. The single most important thing to understand: **signing, paying, and activating are three separate states** — confusing them causes wrong actions on real leads (this is exactly what R094 exists to prevent).

## The three independent states (do not conflate)
1. **`offers.status`** — the document: `draft` → `published` → `signed` → `completed` (and `superseded` when re-issued). Final statuses = `signed`/`completed`/`superseded`.
2. **`pending_activations.status`** — the payment wait: `awaiting_payment` → `payment_confirmed` → `activated`.
3. **`leads.status`** — the sales funnel: stays at its pre-payment value (e.g. "Offer Sent") through signing and only flips to **`Converted`** when payment is confirmed.

## Lifecycle (happy path)
1. **Create** (`offer_create`) → `draft`.
2. **Publish** (`publishOffer`, `lib/offers/publish.ts`) → strict gate: only `draft` can publish → status `published`; sends the client email via **safeSend** (R037); client-facing link on `app.tonydurante.us` (R012). Access code generated.
3. **Client signs** on the contract page → `offers.status='signed'` → the page calls the **offer-signed webhook**.
4. **offer-signed webhook** (`app/api/webhooks/offer-signed/route.ts`): verifies the offer is signed; **idempotent** (no-op if a `pending_activation` already exists); creates `pending_activations` (`status='awaiting_payment'`, `signed_at`); auto-saves the signed document; may create a TD invoice per `decideInvoiceAtSigning` (`lib/portal/offer-invoice-policy.ts`); **links lead→contact** (`leads.converted_to_contact_id`) but **does NOT flip `leads.status`**.
5. **Payment confirmed** (`confirm-payment` admin action / `stripe` webhook / `whop` webhook / wire-match cron) → flips `leads.status='Converted'` and runs `activateService()` to spin up the client (see formation doc).

## Business rules
- **R094** — `leads.status='Converted'` means **PAYMENT confirmed, not offer signed.** Since commit `4d5f403` (2026-04-17) the offer-signed webhook only links `converted_to_contact_id`; the `Converted` flip happens after payment in `confirm-payment`/`stripe`/`whop`. Before acting on a `Converted` lead, check `pending_activations.status` (`activated` vs stuck at `payment_confirmed`). Signed-but-unpaid leads stay at their pre-sign status.
- **R037** — Offer send/publish go through `safeSend()` (idempotency → send → status update after).
- **R012 / R005** — Client-facing offer links use `APP_BASE_URL` (`app.tonydurante.us`); never the internal `td-operations.vercel.app`.
- **R035** — Never send an offer to a client without testing it first via `?preview=td`.
- **A SPLIT SETUP FEE IS A "PARTIAL PAYMENT" — NEVER AN "INSTALMENT" OR A "RATA". ENGLISH ONLY** (Antonio, 2026-08-11: "I don't want a fucking nothing in italian. Luca and I work in english" — superseding his 2026-08-09 approval of an Italian label; the platform's EXISTING Italian elsewhere is untouched). This is a rule about client-facing words, and it exists because the collision is real in two places at once:
  - **In the client's mind.** "Instalment" (and its Italian equivalent "rata", the word the renewal contracts themselves use) is the vocabulary of the ANNUAL management contract and its January/June payments. A formation client splitting a one-off setup fee is not entering that arrangement, and saying so invites them to think they are.
  - **In the code.** The two are separate machinery on purpose. Paying an invoice categorised `installment_1`/`installment_2` fires `onInstallmentPaid`, which lifts the client's accountant hand-off gate and advances their tax card, and it feeds the June cron and the instalment badge. A split setup fee therefore uses its own category, `setup_tranche` — reusing an instalment category would be a functional bug, not a naming preference.
  - **Where it is enforced:** `lib/offers/payment-plan.ts` owns the only sanctioned client-facing phrasing (`PARTIAL_PAYMENT_LABEL`, `clientFacingPartLabel`, `clientFacingSchedule`, `trancheInvoiceDescription`). The guard is `tests/unit/payment-plan.test.ts`, which asserts that no label or description contains "instalment", "installment", or "rata", in either language. If you are changing client-facing tranche wording, that test is the thing that will stop you — and the reason is in the module header.
  - **Invoice descriptions stay English** even for an Italian client, because every other invoice description in the system is English and localising one line would make a single row of their invoice list disagree with the rest.

## How it's built
### Key files
- `lib/offers/publish.ts` → `publishOffer()`, `resendOfferEmail()`, `buildPortalNotificationEmail()`.
- `lib/offers/sync-offer-email.ts` → `syncLeadEmailToOfferArtifacts()` (propagates a lead email change to non-final offers).
- `lib/mcp/tools/offers.ts` → `offer_create`, `offer_get`, `offer_list`, `offer_update`, `offer_send`, `offer_resend`, `offer_token`.
- `app/api/webhooks/offer-signed/route.ts` — the sign event.
- `lib/portal/offer-invoice-policy.ts` → `decideInvoiceAtSigning()` (whether/what to invoice at sign time).
- Payment confirmation: `app/api/crm/admin-actions/confirm-payment/route.ts`, `app/api/webhooks/stripe/route.ts`, `app/api/webhooks/whop/*`, `app/api/cron/*wire*`.
- `app/offer` (the contract/sign page). Activation: `lib/operations/activation.ts`.
- **AI narrative generator:** `app/api/crm/admin-actions/generate-offer-narrative/route.ts` (system/user prompt + call-transcript enrichment via `fetchCallContext`), `lib/offer-narrative.ts` (`validateNarrative` + the pure `renderCallForOffer` transcript formatter), `components/offers/create-offer-dialog.tsx` (the "Generate with AI" button; passes `lead_id`/`account_id`). Reads `call_summaries` (`notes`, `transcript`, `lead_id`, `account_id`).

### Tables
`offers` (`token`, `status`, `access_code`, `payment_links`, `bank_details`, `lead_id`, `account_id`, `client_name/email`, `language`, version fields), `pending_activations` (`status`, `offer_token`, `signed_at`), `leads` (`status`, `converted_to_contact_id`), `payments`.

## Gotchas, invariants & past bugs
- **Signing ≠ payment ≠ activation.** A signed offer with no payment is NOT a converted lead. Always check all three states before acting.
- **The offer-signed webhook is idempotent** — re-firing is safe (it checks for an existing `pending_activation`).
- **Only `draft` can be published**; re-publish is guarded by the offer's own current status inside `safeSend`, not by an inherited flag.
- **Email-change sync skips final offers** — `syncLeadEmailToOfferArtifacts` only updates offers NOT in `signed`/`completed`/`superseded`, so a signed record's identity is never mutated.
- **Don't reintroduce a `leads.status` flip at sign time** — that regression is precisely what R094 forbids (it caused actions to fire on unpaid leads).
- **NEVER navigate the offer page's own frame to a payment provider, and NEVER rely on an exception to detect a refused navigation** (2026-07-14, dev_task `ba7bfd8d`). The portal frames the offer page cross-origin and **Stripe Checkout will not render inside a frame**. Open the payment tab **synchronously in the click handler, before any `await`** (`openCheckoutTab`), because a cross-origin frame loses the right to open/navigate a few seconds after the click — right about when the Stripe session finishes being created — and browsers refuse it **silently** (no throw, console warning only). Always honour `needs_manual` from `deliverCheckout` by rendering a real `target="_top"` link; that is the only guaranteed path when a popup blocker intervenes. All of this lives in `lib/payments/checkout-redirect.ts`.
- **`POST /api/offers/create-checkout` is PUBLIC — never trust its body for anything billable** (2026-07-14, `ba7bfd8d`). It is token-only with no session. It once let the body override `selected_services`, so a signed client could bill themselves for less than their contract. Once an offer is `signed`/`completed` the STORED selection is the contract and the body is ignored — enforced by `resolveBillableSelection` (`lib/payments/billable-selection.ts`), not by the UI. Disabling checkboxes is cosmetic; the endpoint is callable directly.
- **The 5% card fee — NOW CHARGED (2026-07-15, dev_task `6ec6872a`, SANDBOX/branch `claude/card-fee-charge-6ec6872a`, NOT yet on prod).** It had regressed when Stripe replaced Whop (~2026-04-03): both Stripe paths handed the BASE amount while every screen advertised "+5%" (no Stripe payment in history ever collected it — Tamás Fazekas 2026-07-14 shown €3,150, charged €3,000). Both checkout routes now charge `base × (1 + rate)`, where `rate` is PINNED on the offer at creation (`offers.card_fee_rate`) so a later config change never re-prices a signed deal, and the Stripe webhook books the fee onto the invoice from the ACTUAL charge before the settle. Full mechanism + the money invariants + the "no auto-refund" assumption live in `billing-invoicing.md`. The earlier reverted attempt (updated only one money column) is why this went through 3 supervisor rounds; the shipped design routes booking through the existing single money-writer and keeps `card_fee_amount` as the authoritative column. **DEFERRED (gated fast-follow, do NOT expose without the other half):** a CRM screen to edit the rate + driving the "+5%" contract wording from the pinned rate — until that ships the rate stays 5% everywhere (display == charge, no drift), and the editor must not be exposed or a 7% charge could sit against a 5% contract.

## ⛔ STANDING WARNING: A GREEN CHECK IS NOT EVIDENCE UNTIL YOU KNOW WHAT IT MEASURED

Six times in the payment-plan workstream alone (2026-08), a check reported on something other than what it claimed. Same shape every time, six different disguises — so treat this as a rule, not an anecdote:

1. **A query on a column that does not exist** returns empty, so an absence test passes vacuously. Always carry a positive control.
2. **A test mock that ignored the filter it was given** returned every fixture row regardless of what the code asked for, so a "refuses when a paid part exists" test passed for the wrong reason.
3. **A word ban written as a substring match** fired on the ordinary Italian word *separatamente* (it contains *rata*), so the guard was rejecting innocent copy rather than the banned vocabulary.
4. **A test fixture asserting its own validity** (`.plan!`) handed `undefined` into the function under test, so a mistake in the FIXTURE surfaced as a crash inside production code and read like a production bug.
5. **A snapshot refreshed from rows I supplied myself** would have accepted whatever I typed; only comparing a locally-computed digest against the one the database computes over its OWN rows made it evidence.
6. **A helper that looked like protection but was inert** — a per-part idempotency key passed to an issuer that hardcodes its own key and ignores the argument entirely.

**The test to apply:** before treating a green result as proof, state what would have to be broken for it to go red. If you cannot name that, the check is decoration. Prefer mutation (break the code, watch the specific cells fail) over green, and prefer a positive control over an empty result.

## ⛔ A DISPLAYED PRICE IS NOT WHAT GETS CHARGED (2026-08-10 — the rule that outranks every "just hide the figure" fix)

Learned while making the contract's payment panel safe for a split setup fee, and it generalises well beyond payment plans.

**Every price on the post-signature payment panel is a STORED STRING, not a figure computed at render:**
- the wire amount is whatever was typed into the offer's bank details when the offer was authored;
- the card option's price, AND the link it points at, come from the stored payment link — which carries its **own baked-in amount**.

Two consequences that are easy to get wrong:

1. **Declining to UPDATE a figure is not declining to QUOTE it.** A fix that stops recomputing the wire amount leaves the client reading the ORIGINAL stored total and wiring it. Same harm, older date.
2. **Hiding a price is never a fix — only removing the ability to pay is.** The stored card link charges its baked-in amount **on click**, regardless of what the page displays next to it. A panel showing no price beside a live button is strictly worse than useless: it removes the client's only clue while leaving the charge intact.

**And the same applies to the frozen copy captured into the signed contract, where it matters MORE** — a stale figure baked in there is one the client keeps and can act on indefinitely, long after any screen has been corrected.

**Therefore:** when the system cannot state what is due, it must remove **every** payment control (card and wire) and say so in words. Currently that condition is a payment plan disagreeing with its own offer's total — the amount engine reports it, and both the live React panel and the frozen HTML honour it. Any future "we can't price this reliably" condition must follow the same shape.

**Why the overpayment is not benign:** the client wires the whole total, the invoice of record settles at the first part, and the surplus **floats** — there is no disposition for a floating surplus yet. So "nothing was charged wrongly" is not "no harm done".

## How to verify current state
- Read `app/api/webhooks/offer-signed/route.ts` (confirm it links contact but does NOT set `leads.status`), `lib/offers/publish.ts` (publish gate + safeSend), `lib/portal/offer-invoice-policy.ts`.
- For one offer: `SELECT status, lead_id, account_id FROM offers WHERE token='<t>';` then `SELECT status FROM pending_activations WHERE offer_token='<t>';` and `SELECT status, converted_to_contact_id FROM leads WHERE id='<lead_id>';` — the three states should tell a consistent story.
- Note (R096): sandbox via sandbox MCP / `psql`; production `execute_sql` hits production.
